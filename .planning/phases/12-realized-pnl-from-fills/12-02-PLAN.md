---
phase: 12-realized-pnl-from-fills
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - src/pnl.py
  - dashboard/api/migrations/016_realized_pnl_fees.sql
  - src/db_schema.sql
  - src/db.py
  - src/trade_logger.py
autonomous: true
requirements: [PNL-02]

must_haves:
  truths:
    - "src.pnl.realized_pnl computes cent-exact, side-aware realized P&L from both fills minus TAKER_FEE on both legs, never subtracting SLIPPAGE_BUFFER"
    - "tests/test_pnl.py (cases 1-5) passes GREEN against the new helper"
    - "The alpaca_trades table gains a nullable fees column via additive/idempotent migration 016, mirrored in src/db_schema.sql"
    - "update_alpaca_trade (db.py and TradeLogger) accepts an additive fees kwarg that persists into the fees column"
  artifacts:
    - path: "src/pnl.py"
      provides: "pure realized_pnl(side, entry_fill, exit_fill, qty, taker_fee) helper"
      contains: "def realized_pnl"
    - path: "dashboard/api/migrations/016_realized_pnl_fees.sql"
      provides: "ADD COLUMN IF NOT EXISTS fees DOUBLE PRECISION on alpaca_trades"
      contains: "realized_pnl_fees"
    - path: "src/db.py"
      provides: "update_alpaca_trade(..., fees) SET fees = %s"
      contains: "fees"
    - path: "src/trade_logger.py"
      provides: "TradeLogger.update_alpaca_trade(..., fees) forwarding"
      contains: "fees"
  key_links:
    - from: "src/db.py::update_alpaca_trade"
      to: "alpaca_trades.fees"
      via: "UPDATE ... SET fees = %s"
      pattern: "fees = %s"
    - from: "src/trade_logger.py::update_alpaca_trade"
      to: "src/db.py::update_alpaca_trade"
      via: "forwarded fees arg"
      pattern: "fees"
---

<objective>
Build the foundation Plan 03 wires against: the single-source-of-truth pure P&L helper, the additive
`fees` column (migration + schema mirror), and the additive `fees` kwarg threaded through both
`update_alpaca_trade` layers. This turns Plan 01's cases 1-5 GREEN and gives the monitor a place to
store the fee total.

Purpose: PNL-02 requires realized P&L from real fills, net of fees — this plan owns the math and the
persistence contract; Plan 03 owns the wiring.
Output: src/pnl.py, migration 016 + schema mirror, fees kwarg in db.py + trade_logger.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-realized-pnl-from-fills/12-CONTEXT.md
@.planning/phases/12-realized-pnl-from-fills/12-RESEARCH.md
@CLAUDE.md

<interfaces>
From src/fee_gate.py: TAKER_FEE, SLIPPAGE_BUFFER (module constants; TAKER_FEE only for realized P&L).

From src/db.py (L101-121) — signature to extend additively:
  update_alpaca_trade(bot_id, trade_id, status, exit_price=None, pnl=None) -> None
  closed_at stamped for {closed,stopped,target_hit,canceled,expired,rejected}
  UPDATE alpaca_trades SET status=%s, exit_price=%s, pnl=%s, closed_at=%s WHERE id=%s AND bot_id=%s

From src/trade_logger.py (L40-43) — forwarding wrapper to extend:
  update_alpaca_trade(self, trade_id, status, exit_price=None, pnl=None)
    -> _db.update_alpaca_trade(self.bot_id, trade_id, status, exit_price, pnl)

Migration mechanism (dashboard/api/migrations/run_migrations.py): globs *.sql lexicographically,
tracks applied in _migrations table, runs each in a txn — file MUST be idempotent (IF NOT EXISTS).
Mirror the 015_order_state_resolution.sql header/style. Next free number = 016.

src/db_schema.sql alpaca_trades CREATE TABLE ~L20-45; Phase 11 additive columns L40-44; add
`fees DOUBLE PRECISION` as a trailing column after filled_avg_price (~L44). All money columns
(exit_price, pnl, filled_avg_price) are DOUBLE PRECISION.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/pnl.py pure realized_pnl helper (GREEN cases 1-5)</name>
  <files>src/pnl.py</files>
  <read_first>src/alpaca_orchestrator.py L204-211 (existing side-aware sign logic to match); tests/test_pnl.py (the RED contract from Plan 01); src/fee_gate.py</read_first>
  <action>Create `src/pnl.py` with a single pure function `realized_pnl(side, entry_fill, exit_fill, qty, taker_fee)` returning a float. Branch on `side in ("sell", "short")` for short (`(entry_fill - exit_fill) * qty`) vs long (`(exit_fill - entry_fill) * qty`), matching the existing L204-211 convention. Subtract fees = `(entry_fill*qty + exit_fill*qty) * taker_fee`. Use ONLY the passed taker_fee — do NOT import or subtract SLIPPAGE_BUFFER. Pure stdlib, no side effects, no I/O, no fallback/guard logic (fallbacks live in the monitor — the helper just does math on the numbers given). Keep it minimal (Karpathy smallest-diff).</action>
  <verify>
    <automated>python -m pytest tests/test_pnl.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - src/pnl.py defines realized_pnl(side, entry_fill, exit_fill, qty, taker_fee).
    - Side-aware: "sell"/"short" short branch, else long; matches L204-211 sign convention.
    - Fees applied to both legs; SLIPPAGE_BUFFER never referenced.
    - tests/test_pnl.py cases 1-5 all pass GREEN (18.95 / -21.05 / 20.00 goldens).
  </acceptance_criteria>
  <done>Pure, cent-exact, side-aware helper exists and turns Plan 01 cases 1-5 GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 016 fees column + db_schema.sql mirror</name>
  <files>dashboard/api/migrations/016_realized_pnl_fees.sql, src/db_schema.sql</files>
  <read_first>dashboard/api/migrations/015_order_state_resolution.sql (header/style to mirror); dashboard/api/migrations/run_migrations.py (idempotency requirement); src/db_schema.sql L20-45 (alpaca_trades DDL, columns end ~L44)</read_first>
  <action>Create `dashboard/api/migrations/016_realized_pnl_fees.sql` mirroring 015's header/comment style. Single additive/idempotent statement: `ALTER TABLE alpaca_trades ADD COLUMN IF NOT EXISTS fees DOUBLE PRECISION;`. No NOT NULL, no DEFAULT, no backfill (CLAUDE.md rule 6; Phase 14 backfills historicals). In `src/db_schema.sql`, add `fees DOUBLE PRECISION` as a trailing column after filled_avg_price (~L44) in the alpaca_trades CREATE TABLE, with a comment `-- Phase 12 realized-P&L fees (mirror of migration 016; additive, nullable).`. Do NOT touch the stale `bot_id IN ('A','B')` CHECK.</action>
  <verify>
    <automated>grep -q "ADD COLUMN IF NOT EXISTS fees DOUBLE PRECISION" dashboard/api/migrations/016_realized_pnl_fees.sql && grep -q "fees DOUBLE PRECISION" src/db_schema.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - dashboard/api/migrations/016_realized_pnl_fees.sql exists, additive + idempotent (ADD COLUMN IF NOT EXISTS), no DROP/DELETE/non-additive ALTER.
    - fees typed DOUBLE PRECISION (matches exit_price/pnl/filled_avg_price).
    - src/db_schema.sql alpaca_trades DDL contains a nullable fees DOUBLE PRECISION column.
  </acceptance_criteria>
  <done>fees column added by idempotent migration 016 and mirrored in schema; deploy-safe before code.</done>
</task>

<task type="auto">
  <name>Task 3: Thread additive fees kwarg through both update_alpaca_trade layers</name>
  <files>src/db.py, src/trade_logger.py</files>
  <read_first>src/db.py L101-121 (update_alpaca_trade signature + UPDATE); src/trade_logger.py L40-43 (forwarding wrapper)</read_first>
  <action>In `src/db.py::update_alpaca_trade`: add `fees: float | None = None` to the signature (after pnl), add `fees = %s` to the UPDATE SET clause, and add `fees` to the params tuple in the correct positional order. Keep closed_at logic unchanged. In `src/trade_logger.py::update_alpaca_trade`: add `fees: float | None = None` to the signature and forward it as `_db.update_alpaca_trade(self.bot_id, trade_id, status, exit_price, pnl, fees)`. Purely additive — default None keeps every existing caller (bot_thread.py L306/314/329/335, orchestrator external-exit L165) working with no change; a None fees writes NULL (no-op for legacy paths). Karpathy smallest-diff.</action>
  <verify>
    <automated>python -c "import ast; d=open('src/db.py').read(); t=open('src/trade_logger.py').read(); assert 'fees = %s' in d and 'fees' in t; ast.parse(d); ast.parse(t); print('OK')" && python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - src/db.py update_alpaca_trade accepts fees (default None) and writes SET fees = %s with fees in params.
    - src/trade_logger.py update_alpaca_trade accepts fees and forwards it positionally to _db.update_alpaca_trade.
    - Additive: existing callers omitting fees still work; full suite (`python -m pytest tests/ -q`) stays green (no regressions; test_close_pnl.py may still be RED until Plan 03).
  </acceptance_criteria>
  <done>fees flows caller -> TradeLogger -> db.py -> alpaca_trades.fees; backward compatible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| bot process → Postgres | fees value written via parameterized %s |
| migration runner → schema | additive DDL run before code deploy |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02 | Tampering | fees in UPDATE SET | mitigate | parameterized %s only (existing db.py pattern); nullable column |
| T-12-03 | Denial of Service | migration re-run | mitigate | ADD COLUMN IF NOT EXISTS idempotent; tracked by run_migrations _migrations table |
| T-12-SC | Tampering | package installs | accept | no new packages (pure Python); no legitimacy audit needed |
</threat_model>

<verification>
- `python -m pytest tests/test_pnl.py -x -q` GREEN (cases 1-5).
- `python -m pytest tests/ -q` no regressions (test_close_pnl.py may remain RED pending Plan 03).
- Migration 016 additive/idempotent; safe to apply on Coolify Postgres before code deploy.
</verification>

<success_criteria>
Pure realized_pnl helper GREEN for cases 1-5; fees column added (migration + schema mirror);
fees kwarg threaded additively through both update_alpaca_trade layers with zero caller regressions.
</success_criteria>

<output>
Create `.planning/phases/12-realized-pnl-from-fills/12-02-SUMMARY.md` when done.
</output>
