---
phase: 12-realized-pnl-from-fills
plan: 03
type: execute
wave: 3
depends_on: [12-01, 12-02]
files_modified:
  - src/alpaca_orchestrator.py
  - tests/test_order_resolution.py
autonomous: true
requirements: [PNL-02]

must_haves:
  truths:
    - "On a triggered close, PositionMonitor stores exit_price = the real exit fill from close_position(), not the current quote"
    - "The stored pnl is realized_pnl(side, entry_fill, exit_fill, qty, TAKER_FEE) — net of fees, from both fills"
    - "The fees total is persisted on the row and total_pnl accumulates the realized figure"
    - "Missing/zero entry or exit fills fall back (entry_price / current_price) with a logged warning, never a fake -100% loss"
    - "The exit trigger ladder (hard_stop/max_hold/ATR) and pnl_pct/trade_pnl display are unchanged"
  artifacts:
    - path: "src/alpaca_orchestrator.py"
      provides: "close block wired to src.pnl.realized_pnl + fills + fees"
      contains: "realized_pnl"
  key_links:
    - from: "src/alpaca_orchestrator.py"
      to: "src.pnl.realized_pnl"
      via: "import + call in close block"
      pattern: "realized_pnl"
    - from: "src/alpaca_orchestrator.py close block"
      to: "close_position() return dict"
      via: "result.get('filled_avg_price')"
      pattern: "filled_avg_price"
---

<objective>
Wire the monitor close path to the Plan 02 foundation: capture the exit fill from `close_position()`,
read the entry fill from the row, compute `realized_pnl` net of TAKER_FEE on both legs, and persist
`exit_price=exit_fill, pnl=realized, fees=fees`, accumulating `total_pnl` on the realized figure.
Fills that are missing/zero fall back to the quote/entry_price with a logged warning. The exit trigger
ladder and display figure are untouched (locked decision 5). This turns Plan 01 cases 6-10 GREEN.

Purpose: closes PNL-02 — the stored close number now reflects what Alpaca actually booked.
Output: edited close block in src/alpaca_orchestrator.py; defensive fees kwarg on the Phase 11
FakeLogger double.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-realized-pnl-from-fills/12-CONTEXT.md
@.planning/phases/12-realized-pnl-from-fills/12-RESEARCH.md
@CLAUDE.md

<interfaces>
Contract (from Plan 02) — src/pnl.py:
  realized_pnl(side, entry_fill, exit_fill, qty, taker_fee) -> float  (side-aware, both-legs fee)
From src/fee_gate.py: TAKER_FEE.
From src/trade_logger.py: update_alpaca_trade(trade_id, status, exit_price=None, pnl=None, fees=None).

Edit site — src/alpaca_orchestrator.py PositionMonitor._check_positions:
  L204-211: side-aware pnl_pct/trade_pnl from current_price — KEEP (drives ladder + display).
  L287-300 close block (inside `with self._lock:`):
    L289  self.alpaca.close_position(symbol)        # return value currently discarded
    L290-295 self.logger.update_alpaca_trade(trade_id, status="closed",
             exit_price=current_price, pnl=trade_pnl)
    L297  self.total_pnl += trade_pnl
    L300  alert_position_closed(symbol, side, entry_price, current_price, trade_pnl, close_reason)
  Row fields available (SELECT * load): trade.get("filled_avg_price") (entry fill), entry_price, qty, side.
  close_position() returns _parse_order dict: result.get("filled_avg_price") (exit fill; 0.0 when missing).

  External-exit reconciliation stub L165-170 (exit_price=entry_price, pnl=0.0): DELIBERATELY UNCHANGED
  — no fill data available there; leave fees unset (defaults None).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire close block to realized_pnl + fills + fees (GREEN cases 6-10)</name>
  <files>src/alpaca_orchestrator.py</files>
  <read_first>src/alpaca_orchestrator.py L150-303 (full _check_positions; close block L287-300; external-exit stub L165-170); src/pnl.py (helper contract from Plan 02); src/fee_gate.py (TAKER_FEE); src/alpaca_client.py L421-439 (_parse_order shape, filled_avg_price L432)</read_first>
  <action>Add `from src.pnl import realized_pnl` and `from src.fee_gate import TAKER_FEE` (module top; TAKER_FEE may already be imported — do not duplicate). In the close block (L287-300): capture `result = self.alpaca.close_position(symbol)`. Derive `exit_fill = result.get("filled_avg_price") or 0` then `if exit_fill <= 0: exit_fill = current_price; log.warning(...)` (log the symbol + fallback). Derive `entry_fill = trade.get("filled_avg_price") or 0` then `if entry_fill <= 0: entry_fill = entry_price; log.warning(...)`. Compute `fees = (entry_fill*qty + exit_fill*qty) * TAKER_FEE` and `realized = realized_pnl(side, entry_fill, exit_fill, qty, TAKER_FEE)`. Call `self.logger.update_alpaca_trade(trade_id=trade_id, status="closed", exit_price=exit_fill, pnl=realized, fees=fees)`. Change `self.total_pnl += trade_pnl` to `self.total_pnl += realized`. Update `alert_position_closed(...)` to pass `exit_fill` and `realized` (consistency with the stored figure; research A2). Do NOT touch L204-211 pnl_pct/trade_pnl (still drive the ladder and the L273-285 display lines) or the exit-trigger precedence. Use only TAKER_FEE — never SLIPPAGE_BUFFER. Leave the L165-170 external-exit stub unchanged (pnl=0.0, no fill data). Karpathy smallest-diff.</action>
  <verify>
    <automated>python -m pytest tests/test_close_pnl.py tests/test_pnl.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - close block captures close_position() return and reads exit_fill from result.get("filled_avg_price"), entry_fill from trade.get("filled_avg_price").
    - exit_fill<=0 falls back to current_price (logged); entry_fill<=0 falls back to entry_price (logged).
    - update_alpaca_trade called with exit_price=exit_fill, pnl=realized, fees=fees; total_pnl += realized.
    - L204-211 trade_pnl/pnl_pct and the exit ladder precedence are byte-identical (no trigger change).
    - Only TAKER_FEE used; SLIPPAGE_BUFFER not referenced. External-exit stub L165-170 unchanged.
    - tests/test_close_pnl.py cases 6-10 GREEN.
  </acceptance_criteria>
  <done>Triggered closes store the real exit fill, net realized pnl, and fees; total_pnl uses realized; fallbacks logged; ladder untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add defensive fees kwarg to Phase 11 FakeLogger double</name>
  <files>tests/test_order_resolution.py</files>
  <read_first>tests/test_order_resolution.py L54-62 (FakeLogger.update_alpaca_trade signature)</read_first>
  <action>In `tests/test_order_resolution.py`, extend `FakeLogger.update_alpaca_trade` signature to `(self, trade_id, status, exit_price=None, pnl=None, fees=None)` and store `row["fees"] = fees` when fees is not None. Purely additive so the double stays call-compatible with the new real signature; no existing resolution test passes fees, so behavior is unchanged. Do not alter any test logic.</action>
  <verify>
    <automated>python -m pytest tests/test_order_resolution.py -q && python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - FakeLogger.update_alpaca_trade accepts fees=None and stores row["fees"] when provided.
    - tests/test_order_resolution.py still passes; full suite green.
  </acceptance_criteria>
  <done>Phase 11 double matches the new update_alpaca_trade signature; suite fully green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Alpaca close_position → bot | filled_avg_price is external; 0.0 signals missing fill |
| bot → Postgres | exit_price/pnl/fees written via parameterized update |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Repudiation | zero fill → fake -100% P&L | mitigate | `<= 0` guard + logged fallback to current_price/entry_price before realized_pnl |
| T-12-05 | Tampering | silent exit-trigger drift | mitigate | L204-211 trade_pnl/pnl_pct and ladder precedence left byte-identical; only stored number changes |
| T-12-SC | Tampering | package installs | accept | no new packages; no legitimacy audit needed |
</threat_model>

<verification>
- `python -m pytest tests/test_pnl.py tests/test_close_pnl.py -q` fully GREEN (all 10 cases).
- `python -m pytest tests/ -q` green (baseline 299 passed, 2 skipped, plus new tests).
- Manual read-check: L204-211 and the exit ladder precedence unchanged; SLIPPAGE_BUFFER not referenced in the close block.
</verification>

<success_criteria>
Triggered closes persist realized P&L from both fills net of TAKER_FEE, store the real exit fill as
exit_price, persist fees, accumulate total_pnl on the realized figure, and log fallbacks — with zero
change to exit-trigger behavior. All 10 PNL-02 validation cases GREEN.
</success_criteria>

<output>
Create `.planning/phases/12-realized-pnl-from-fills/12-03-SUMMARY.md` when done.
</output>
