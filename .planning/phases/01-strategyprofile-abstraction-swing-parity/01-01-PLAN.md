---
phase: 01-strategyprofile-abstraction-swing-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/strategy_profile.py
  - tests/test_strategy_profile.py
  - src/alpaca_orchestrator.py
autonomous: true
requirements: [PROFILE-01, PROFILE-02]

must_haves:
  truths:
    - "A frozen StrategyProfile dataclass exists with the full D-02 field set"
    - "SWING preset field values equal today's effective constants byte-for-byte"
    - "PROFILES['swing'] resolves to the SWING preset"
    - "Existing env overrides (MIN_CONFLUENCE, CYCLE_SLEEP_SECONDS, etc.) still win over profile defaults"
    - "Existing signal/exit tests pass unchanged (no behavior regression)"
  artifacts:
    - path: "src/strategy_profile.py"
      provides: "Frozen StrategyProfile dataclass + SWING preset + PROFILES registry"
      contains: "class StrategyProfile"
    - path: "tests/test_strategy_profile.py"
      provides: "4 Nyquist parity assertions"
      contains: "test_swing_values_match_current_constants"
  key_links:
    - from: "src/alpaca_orchestrator.py"
      to: "src/strategy_profile.py"
      via: "import SWING/PROFILES, PROFILE = SWING, os.environ.get(NAME, str(PROFILE.field))"
      pattern: "from src.strategy_profile import"
---

<objective>
Introduce `src/strategy_profile.py` (frozen `StrategyProfile` dataclass + `SWING` preset + `PROFILES` registry) and re-source the orchestrator's style constants from the profile default WITHOUT changing behavior. SWING reproduces current swing-bot values byte-for-byte; env overrides still win (D-05).

Purpose: Backbone abstraction for the day-trading upgrade; PROFILE-02 parity is the regression-safety floor before any behavior change in later phases.
Output: New profile module, parity test file, minimal-diff orchestrator wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-strategyprofile-abstraction-swing-parity/01-CONTEXT.md
@.planning/phases/01-strategyprofile-abstraction-swing-parity/01-RESEARCH.md
@.planning/phases/01-strategyprofile-abstraction-swing-parity/01-VALIDATION.md
@src/config.py
@src/alpaca_orchestrator.py

<interfaces>
<!-- Existing frozen-dataclass house pattern (mirror this) -->
From src/config.py:34 — `@dataclass(frozen=True) class Config:` (kelly_fraction default 0.25, max_position_pct 0.05).

Orchestrator constants block (src/alpaca_orchestrator.py:50-81), env-with-default shape to preserve:
  MAX_POSITION_PCT  = float(_os.environ.get("MAX_POSITION_PCT", "0.05"))     # :52
  MIN_CONFLUENCE    = int(_os.environ.get("MIN_CONFLUENCE", "4"))           # :57
  MIN_SHORT_CONFLUENCE = int(_os.environ.get("MIN_SHORT_CONFLUENCE", "3"))  # :59
  CYCLE_SLEEP_SECONDS  = int(_os.environ.get("CYCLE_SLEEP_SECONDS", "1800"))# :73
HARD_STOP_PCT is imported from exit_advisor (:34) = -0.15 — do NOT touch its definition; profile only mirrors the value.
`_os` alias for `os` already defined at :50.
</interfaces>

<parity_contract>
SWING field → value (from RESEARCH "Current SWING Values" table — all VERIFIED):
name="swing", timeframe="1Hour", scan_interval_s=1800, bar_count=50, htf_filter_timeframe="4Hour",
ema_fast=9, ema_slow=21, rsi_period=14, adx_period=14, atr_period=14, atr_mult_stop=2.0,
atr_mult_trail=1.5, hard_stop_pct=-0.15, max_hold_hours=None, kelly_fraction=0.25,
max_position_pct=0.05, min_confluence=4, min_short_confluence=3.
(atr_mult_* are Phase-4 placeholders, not parity-load-bearing now.)
</parity_contract>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create StrategyProfile module + SWING preset + parity tests (Wave 0 scaffold)</name>
  <files>src/strategy_profile.py, tests/test_strategy_profile.py</files>
  <read_first>src/config.py (frozen dataclass pattern, :34), RESEARCH.md §"Current SWING Values" table and §"Code Examples"</read_first>
  <behavior>
    - SWING field values equal current constants (timeframe 1Hour, scan 1800, bars 50, HTF 4Hour, EMA 9/21, RSI/ADX 14, hard_stop -0.15, max_hold None, kelly 0.25, max_pos 0.05, conf 4/3)
    - StrategyProfile is frozen → assigning a field raises dataclasses.FrozenInstanceError
    - PROFILES["swing"] is SWING
    - (env-override test lives here but exercises Task 2's wiring — see note)
  </behavior>
  <action>
    Create `src/strategy_profile.py`: a `@dataclass(frozen=True) class StrategyProfile` with EXACTLY the D-02 fields in the order listed in the parity_contract, typed per RESEARCH Pattern 1 (`max_hold_hours: float | None`). Define module constant `SWING = StrategyProfile(...)` with the parity_contract values, and `PROFILES = {"swing": SWING}`. Do NOT define DAYTRADE (Phase 2). Do NOT import anything from the orchestrator (one-way dependency: orchestrator imports profile). No `os` reads in this module — it is pure constants.

    Create `tests/test_strategy_profile.py` with the 4 tests from RESEARCH §"Code Examples": `test_swing_values_match_current_constants`, `test_profile_is_frozen`, `test_profiles_registry`, `test_env_override_wins_over_profile_default`. The env-override test uses `monkeypatch.setenv("MIN_CONFLUENCE","2")` + `importlib.reload(src.alpaca_orchestrator)` and asserts `o.MIN_CONFLUENCE == 2`; it will pass only after Task 2 wires the orchestrator (expected RED until then).
  </action>
  <verify>
    <automated>python -m pytest tests/test_strategy_profile.py::test_swing_values_match_current_constants tests/test_strategy_profile.py::test_profile_is_frozen tests/test_strategy_profile.py::test_profiles_registry -x -q</automated>
  </verify>
  <done>Module imports cleanly; 3 profile-only tests green; env-override test present (RED pending Task 2).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-source orchestrator style constants from PROFILE (env still wins)</name>
  <files>src/alpaca_orchestrator.py</files>
  <read_first>src/alpaca_orchestrator.py:50-81 (constants block), RESEARCH.md §"Pattern 2", §"Refactor Surface", §"Pitfalls 1-4"</read_first>
  <behavior>
    - With no env set, MAX_POSITION_PCT/MIN_CONFLUENCE/MIN_SHORT_CONFLUENCE/CYCLE_SLEEP_SECONDS resolve to SWING defaults (0.05/4/3/1800)
    - With MIN_CONFLUENCE=2 in env + module reload, o.MIN_CONFLUENCE == 2 (env wins)
    - Existing technical_signals/exit_advisor tests unchanged
  </behavior>
  <action>
    Per D-06 minimal-diff (Karpathy): at the TOP of the constants block (immediately after `import os as _os` at :50, BEFORE :52 to avoid the import-order NameError in RESEARCH Pitfall 1) add:
      `from src.strategy_profile import SWING, PROFILES`
      `PROFILE = SWING  # Phase 2 selects via BOT_PROFILE`
    Then change ONLY these four default literals to source from PROFILE, keeping the `_os.environ.get(...)` wrapper and the existing module-global NAME intact (RESEARCH Pattern 2):
      `MAX_POSITION_PCT  = float(_os.environ.get("MAX_POSITION_PCT", str(PROFILE.max_position_pct)))`
      `MIN_CONFLUENCE    = int(_os.environ.get("MIN_CONFLUENCE", str(PROFILE.min_confluence)))`
      `MIN_SHORT_CONFLUENCE = int(_os.environ.get("MIN_SHORT_CONFLUENCE", str(PROFILE.min_short_confluence)))`
      `CYCLE_SLEEP_SECONDS = int(_os.environ.get("CYCLE_SLEEP_SECONDS", str(PROFILE.scan_interval_s)))`
    Do NOT: change `HARD_STOP_PCT` (imported from exit_advisor — leave the import as-is; profile only mirrors the value), touch `kelly_fraction` call-sites (:880/:984 keep `config.kelly_fraction` — RESEARCH Pitfall 3), rewire `technical_signals.py` periods (Phase 3), blanket-replace `"1Hour"`/`bar_count` literals at monitor/regime call-sites (RESEARCH Pitfall 4), or pull non-style constants (exposure/drawdown/etc.) into the profile. No control-flow, threshold, or ordering changes.
  </action>
  <verify>
    <automated>python -m pytest tests/test_strategy_profile.py -x -q ; python -m pytest tests/test_technical_signals.py tests/test_exit_advisor.py -q</automated>
  </verify>
  <done>All 4 strategy_profile tests green (env-override now passes); existing signal/exit tests pass unchanged; diff limited to the 4 constants + 2 added lines.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_strategy_profile.py -q` — 4/4 green (PROFILE-01 frozen+registry, PROFILE-02 parity+env-override).
- `python -m pytest -q` — full suite green, no regression.
- `git diff src/alpaca_orchestrator.py` — only 2 added lines (import + PROFILE) and 4 changed default literals; no other edits.
</verification>

<success_criteria>
- Frozen `StrategyProfile` with full D-02 field set exists (PROFILE-01).
- SWING values equal current constants byte-for-byte; env overrides still win, so bots A/B are unaffected (PROFILE-02, D-05).
- `technical_signals.py` untouched; no DAYTRADE; no behavior change.
</success_criteria>

<output>
Create `.planning/phases/01-strategyprofile-abstraction-swing-parity/01-01-SUMMARY.md` when done.
</output>
