---
phase: 20-verification-e2e
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_backfill.py
  - dashboard/api/tests/test_paper_gate.py
autonomous: true
requirements: [VERIFY-01]

must_haves:
  truths:
    - "tests/test_backfill.py:199 ENCODES THE BUG. It feeds live_symbols={'BTC/USD'} — SLASHED — a payload Alpaca NEVER emits. get_positions() returns pos.symbol RAW (src/alpaca_client.py:140-157), which is slashless for crypto, as the codebase's own comment at :384-385 states ('Alpaca position endpoint uses symbol without slash (PEPEUSD not PEPE/USD)'). The test passes green BECAUSE it mirrors the defect. This plan CORRECTS that line in place — it does NOT merely add cases beside it. Leaving it as-is preserves a green test that certifies broken behavior."
    - "THE BACKFILL IS A LOADED GUN AND THIS IS PROVEN BY EXECUTION, NOT BY READING. Against a real-shaped slashless live_symbols={'BTCUSD'}, a genuinely-HELD BTC/USD position TODAY resolves as ('resolved', {'status':'closed','exit_price':80.0,'pnl':-20.45,'fees':0.45}) — a live position closed with a FABRICATED LOSS. Cases 1, 2, 5 MUST FAIL on current main with exactly that shape. A G3 case that passes before the fix is not testing the bug."
    - "THE `unchanged` ARM IS UNREACHABLE IN PRODUCTION. src/backfill.py:72 compares a slashed row['symbol'] against a set built slash-STRIPPED at :148. `'BTC/USD' in {'BTCUSD'}` is False for EVERY held position, always. The existing suite never noticed because its only fixture for that arm is the slashed one."
    - "THE SECOND LANDMINE — the `None` third door. src/backfill.py:148 coerces get_positions() -> None to an empty set via `or []`. None means the ALPACA CALL FAILED, not 'nothing is held'. An outage therefore makes the ENTIRE BOOK look vanished and every held position resolvable. The live monitor guards this at src/alpaca_orchestrator.py:133-134; the backfill does not. Case 6 pins it. Same bug class as Phase 18's B1."
    - "dashboard/api/routes/settings.py:36 is a bare `SELECT COUNT(*) AS n FROM alpaca_trades` — NO status filter, NO P&L filter — surfaced at :192 as paper_trades_completed against the 50-trade PAPER GATE that guards live trading. `submitted` and `rejected` rows are provably written (src/bot_thread.py:362,376,382). G2 has NO test at all today. THAT IS EXACTLY WHY THE BUG SHIPPED."
    - "DO NOT ASSERT 655 -> 260. RESEARCH R1 REFUTED that arithmetic: dashboard/api/db.py:19 KNOWN_BOTS=('A','B','C','D') while Phase 17's 655/395 are A/B/C/E POSITION-CLOSED figures — two different bot sets AND two different status filters. The fix is justified by the code; the magnitude is a number nobody has verified. Any case asserting a live count is asserting a fabrication. e2e_verify.py MEASURES it (Plan 20-06)."
    - "Zero new skips. Every case here runs offline against fakes — no prod DB, no live Alpaca, no network. A new test that skips is a new test that does not exist."
  artifacts:
    - path: "tests/test_backfill.py"
      provides: "G3 cases 1-8 — the slash bug, the None door, the driver-level compare, the dry-run write fence. Case 3 (the mis-test) CORRECTED in place."
      contains: "BTCUSD"
    - path: "dashboard/api/tests/test_paper_gate.py"
      provides: "G2 cases 9-11 — non-trade rows excluded from paper_trades_completed; the bare COUNT(*) static fence"
      min_lines: 90
  key_links:
    - from: "tests/test_backfill.py"
      to: "src.universe.normalize"
      via: "the SAME helper the monitor uses (alpaca_orchestrator.py:142) — never a local .replace"
      pattern: "normalize"
    - from: "dashboard/api/tests/test_paper_gate.py"
      to: "dashboard/api/routes/settings.py"
      via: "static fence on the bare COUNT(*), same idiom as test_routes.py:224's 100_000.0 fence"
      pattern: "COUNT\\(\\*\\) AS n FROM alpaca_trades"
---

<objective>
Wave 0 (part 1 of 2): the RED suite for **G3 — the backfill slash bug** and **G2 — the paper gate**.
**Nothing in `src/` or `dashboard/api/routes/` changes here.**

Purpose: `src/backfill.py` is the tool that would repair the 395 historical `pnl = 0.0` sentinel rows.
It is itself broken, and broken in the most dangerous possible direction: run with `--apply` **today**,
it resolves every **genuinely-held, live, open** position as `closed` with a **fabricated P&L**. This
was proven by executing `resolve_stale_row` against a real-shaped Alpaca payload, not by reading it.

It survived a 488-case suite because `tests/test_backfill.py:199` feeds it `live_symbols={"BTC/USD"}` —
a **slashed** set that Alpaca never returns. The test does not miss the bug; **it encodes it.** That
line is CORRECTED here, not supplemented.

The same shape of hole exists in the paper gate: `settings.py:36`'s `COUNT(*)` counts `submitted` and
`rejected` rows toward the 50-trade gate that guards live trading, and **no test asserts otherwise.**

Output: `tests/test_backfill.py` corrected + extended (cases 1-8), `dashboard/api/tests/test_paper_gate.py`
created (cases 9-11). Cases **1, 2, 5, 6, 9, 10 FAIL on today's `main`.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-verification-e2e/20-VALIDATION.md
@.planning/phases/20-verification-e2e/20-RESEARCH.md
@.planning/phases/20-verification-e2e/20-CONTEXT.md
@CLAUDE.md

<interfaces>
EXISTING — import these, do NOT re-derive:

  # src/universe.py:17 — the ONLY normalizer. The monitor already uses it (alpaca_orchestrator.py:142).
  def normalize(symbol: str | None) -> str
      """uppercase, strip whitespace, drop the slash. Total and idempotent.
      normalize("BTC/USD") == normalize("btc/usd") == normalize("BTCUSD") == "BTCUSD"
      normalize(None) == normalize("") == ""   """

  # src/backfill.py:51 — the pure decision function under test (UNCHANGED by this plan)
  def resolve_stale_row(row, entry_order, live_symbols, close_order)
      -> ("resolved" | "unchanged" | "unresolvable", write_kwargs | None)

  # src/backfill.py:132 — the driver (UNCHANGED by this plan). Dry-run is the DEFAULT.
  def backfill(apply: bool = False) -> list[tuple[str, dict]]
      # counts keys: resolved / unchanged / unresolvable / residue
      # the ONLY write is gated at :164 by `if outcome == "resolved" and apply`

  # tests/test_backfill.py — the doubles ALREADY THERE. REUSE THEM, do not write a second style.
  FakeLogger(bot_id, seed_rows)            # .update_calls records every write
  FakeAlpacaClient(orders, positions, closed)   # .get_closed_calls records every close hunt
  _order(...) / _stale_row(...)            # fixture builders
  _wire(monkeypatch, bots, clients, candidates, residue)  # patches the driver's collaborators

CONTRACT FIXED HERE — Plan 20-03 implements it in src/backfill.py:

  resolve_stale_row's held-position compare MUST normalize BOTH sides:
      norm_live = {normalize(s) for s in live_symbols}
      if normalize(row.get("symbol")) in norm_live:  ->  ("unchanged", None)
  and the DRIVER's second compare (backfill.py:155) MUST use the same normalized set,
  so NO get_closed_orders call is issued for a held symbol.

  The `None` sentinel from get_positions() MUST be PRESERVED, never coerced:
      positions = client.get_positions()
      if positions is None:   # the Alpaca call FAILED — this is NOT "nothing is held"
          -> skip this bot entirely; leave every row unchanged; report it.
  Mirrors src/alpaca_orchestrator.py:133-134. `or []` at :148 is the landmine.

CONTRACT FIXED HERE — Plan 20-04 implements it in dashboard/api/routes/settings.py + models.py:

  paper_trades_completed derives from the CANONICAL RESOLVED predicate (src/db.py:95 is_resolved):
      status IN ('closed','stopped','target_hit') AND pnl IS NOT NULL AND pnl <> 0
  The bare `SELECT COUNT(*) AS n FROM alpaca_trades` is DELETED from settings.py.
  models.SettingsData gains  total_rows: int = 0   (additive, defaulted) so the drop stays
  EXPLAINABLE on the surface that shows it. paper_trades_target stays 50; win_rate_target
  stays 40.0. THE GATE WILL READ WORSE. THAT IS THE INTENDED OUTCOME AND IT IS NOT TO BE
  TUNED BACK.

The pre-fix behavior, VERIFIED BY EXECUTION against current main — the exact RED you must reproduce:

  normalize: BTCUSD BTCUSD
  HELD position, close order found ->  ('resolved', {'status':'closed','exit_price':80.0,
                                                     'pnl':-20.45,'fees':0.45})
  HELD position, no close order    ->  ('unresolvable', None)
  control (slashed live_symbols)   ->  ('unchanged', None)      <- the ONLY arm the suite tests
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tests/test_backfill.py — G3 cases 1-8, and CORRECT the mis-test at :199</name>
  <files>tests/test_backfill.py</files>
  <read_first>20-VALIDATION.md cases 1-8 and the Wave-0 W0-1 note; 20-RESEARCH.md "The Proof" section (the executed −$20.45 fabrication); src/backfill.py L51-91 (resolve_stale_row — the NOTE at :71, the raw compare at :72), L132-172 (the driver — `or []` at :148, the second raw compare at :155, the write gate at :164); src/alpaca_client.py L140-157 (get_positions returns pos.symbol RAW) and L382-391 (the comment that PROVES it is slashless); src/alpaca_orchestrator.py L133-134 (the None guard the backfill lacks) and L142-143 (the both-sides normalization the backfill lacks); src/universe.py L17-23; tests/test_backfill.py in full (reuse FakeLogger / FakeAlpacaClient / _order / _stale_row / _wire — do NOT write a second double style)</read_first>
  <behavior>
    All cases are PURE — fakes only, no DB, no network, no Alpaca.

    - **CORRECT case 3 in place** (`test_backfill_still_open_unchanged`, currently at :193-201):
      change `live_symbols={"BTC/USD"}` to `live_symbols={"BTCUSD"}` — the shape Alpaca actually
      emits — and add a comment naming W0-1: the slashed fixture was a payload Alpaca never returns,
      so the test passed by mirroring the defect. **Do not leave a slashed-only variant behind as
      "the old case"; case 3 in the matrix covers both shapes.**
      RED AFTER CORRECTION: returns `("unresolvable", None)`.

    - case 1 test_held_position_real_alpaca_shape_is_unchanged (**THE ONE THAT MATTERS**):
      row symbol "BTC/USD", `live_symbols={"BTCUSD"}`, a close order IS available.
      Assert `resolve_stale_row(...) == ("unchanged", None)`.
      Assert explicitly that the returned kwargs is None — **a held position NEVER carries a pnl.**
      RED TODAY: returns `("resolved", {"status":"closed","exit_price":80.0,"pnl":-20.45,...})` —
      a FABRICATED LOSS on a LIVE position. Record that verbatim tuple in the SUMMARY.
    - case 2 test_held_position_no_close_order_is_unchanged_not_unresolvable: same, `close_order=None`.
      Assert `("unchanged", None)` — **not** `unresolvable`. WHY IT IS NOT MERELY COSMETIC:
      `unresolvable` writes `pnl = NULL`, which drops the row out of `get_open_alpaca_positions`
      (src/db.py:160) and thereby KILLS its stop-loss, take-profit and exit advisor **while the
      position is still live at Alpaca**.
      RED TODAY: returns `("unresolvable", None)`.
    - case 3 test_symbol_shape_matrix: all four combinations of row `BTC/USD`|`BTCUSD` ×
      live `{"BTC/USD"}`|`{"BTCUSD"}` -> **all four "unchanged"**. Normalization is applied to BOTH
      sides. Assert via `src.universe.normalize`, and assert the module source contains no local
      `.replace("/", "")` (a second normalizer is a second thing to drift).
      RED TODAY: 2 of the 4 fail.
    - case 4 test_genuinely_vanished_position_still_resolves (**POSITIVE CONTROL**): row "BTC/USD",
      `live_symbols={"ETHUSD"}`, close order present -> `("resolved", ...)` with the correct
      `realized_pnl("buy", ...)` and fees. **The fix must not make the backfill inert.** Passes today;
      must keep passing.
    - case 5 test_driver_issues_no_close_hunt_for_a_held_symbol: drive `backfill(apply=False)` via
      `_wire` with a candidate row "BTC/USD" and `FakeAlpacaClient(positions=[{"symbol":"BTCUSD"}])`.
      Assert `client.get_closed_calls == []` AND `counts["unchanged"] == 1` AND
      `counts["resolved"] == 0`.
      RED TODAY: backfill.py:155's raw compare misses, so a close hunt IS issued and the row resolves.
      **Fixing only :72 and not :155 leaves the driver hunting closes for held positions — this case
      is what forces both.**
    - case 6 test_get_positions_none_aborts_the_bot (**THE SECOND LANDMINE**): a `FakeAlpacaClient`
      subclass whose `get_positions()` returns **None** (the Alpaca call FAILED). Seed one candidate.
      Assert: **zero** rows resolved, **zero** writes even with `apply=True`, and `get_closed_calls`
      is empty. The bot is skipped / its rows left unchanged. It MUST NOT coerce None -> `set()`.
      **AND — this is load-bearing — assert the bot's counts dict carries a DISTINGUISHING key:**
      `counts["error"] == "positions_unavailable"`.
      WHY IT IS NOT COSMETIC: that counts dict IS the **recovery ceiling** a human reads in
      `20-EVIDENCE.md` section 4 before authorizing **a write to production trade data**. Without the
      marker, an outage produces `{"resolved": 0, ...}` — **byte-identical to a clean bot with nothing
      to repair.** An operator would read "Bot A: 0 recoverable" as "nothing to fix" when it actually
      means "**Alpaca never answered.**" Plan 20-07 must REFUSE to print a ceiling for any bot carrying
      this key.
      WHY: `None` means the call failed, NOT "nothing is held". `or []` at backfill.py:148 turns an
      Alpaca outage into "every position vanished" and would terminate the entire live book. The
      monitor gets this right at alpaca_orchestrator.py:133-134; the backfill does not.
      RED TODAY: resolves the row against an empty set.
    - case 6b test_resolve_stale_row_treats_None_live_symbols_as_the_SAFE_arm: call the PURE
      `resolve_stale_row(row, entry, live_symbols=None, close_order=<present>)`. Assert it returns
      `("unchanged", None)` — the SAFE arm — and **does not raise**.
      WHY: once the fix lands, the defensive `{normalize(s) for s in live_symbols}` would **TypeError**
      on `None`. `None` means the positions call FAILED, so the only safe answer is "leave the row
      alone" — never "resolve it against nothing".
    - case 7 test_dry_run_writes_nothing (FENCE, extends the existing `test_backfill_dry_run_no_write`):
      `backfill(apply=False)` -> `FakeLogger.update_calls == []`. Additionally assert on the SOURCE
      that the only mutation site is guarded by `and apply` (positive control: the string
      `update_alpaca_trade` IS present in src/backfill.py, then assert it appears only inside an
      `if outcome == "resolved" and apply:` block). Passes today; must keep passing.
    - **W3 — SWEEP THE REMAINING SLASHED FIXTURES.** `tests/test_backfill.py:378`
      (`positions=[{"symbol": "ETH/USD"}]`) and `:433-436` (`positions=[{"symbol": "BTC/USD"}]` /
      `[{"symbol": "ETH/USD"}]`) still feed **slashed** `get_positions()` payloads. They will PASS
      post-fix (normalize is idempotent), so they are not RED — but they remain **fixtures of a payload
      Alpaca never emits**, and leaving them makes "the real Alpaca shape is now the default fixture"
      only half true. Convert them to the slashless shape, so the ONLY place a slashed `live_symbols`
      appears is case 3's four-way matrix — where it is deliberately under test.
    - case 8 test_dry_run_reports_the_recovery_ceiling: `backfill(apply=False)` returns per-bot
      `{"resolved":…, "unchanged":…, "unresolvable":…, "residue":…}` with all four keys present and
      integral. `resolved` under dry-run IS the recovery-ceiling count. Assert the SHAPE against
      fakes — **never a live number.**
  </behavior>
  <action>Edit tests/test_backfill.py. CORRECT line 199's `live_symbols={"BTC/USD"}` to `{"BTCUSD"}` in place, with a comment citing W0-1. Append the G3 cases in a clearly-headed section. Reuse the existing FakeLogger / FakeAlpacaClient / _order / _stale_row / _wire helpers verbatim; for case 6 add ONE small subclass (or a `positions=None` sentinel honoured by FakeAlpacaClient.get_positions) rather than a parallel double. Import `from src.universe import normalize` and assert against it — never hardcode "BTCUSD" as a magic string in the assertion side. Do NOT stub or monkeypatch resolve_stale_row's internals. Do NOT guard any case with hasattr/getattr/pytest.skip — a skip is not a RED. No boto3, no psycopg, no DATABASE_URL, no network.</action>
  <verify>
    <automated>python -m pytest tests/test_backfill.py -q 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_backfill.py -q` — cases **1, 2, 5, 6 FAIL**, and the CORRECTED
      `test_backfill_still_open_unchanged` **FAILS**. Cases 4, 7, 8 PASS. Zero SKIP. Zero collection ERROR.
    - Case 1's failure output contains the literal `'pnl': -20.45` (or the same fabricated non-None pnl
      on a held position). Record the verbatim assertion line in the SUMMARY — **this is the loaded gun,
      photographed.**
    - `grep -n 'live_symbols={"BTC/USD"}' tests/test_backfill.py` returns **NOTHING outside the case-3
      matrix** — the mis-test is corrected, not merely supplemented.
    - `grep -c "BTCUSD" tests/test_backfill.py` >= 5 — the real Alpaca shape is now the default fixture.
    - `grep -n "get_positions" tests/test_backfill.py | grep -i none` returns a hit — case 6's None door exists.
    - `grep -nE "hasattr|getattr|importorskip|pytest\.skip" tests/test_backfill.py` returns NOTHING.
    - `python -m pytest tests/ dashboard/api/tests/ -q 2>&1 | tail -1` — the ONLY new failures are the
      intended RED; the 488/29 baseline is otherwise intact and skips are still **29**.
  </acceptance_criteria>
  <done>The slash bug, the unreachable `unchanged` arm, the driver's second compare, and the `None`-coercion landmine each have a test that fails for the right reason on current `main` — and the test that certified the broken behavior no longer does.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: dashboard/api/tests/test_paper_gate.py — G2 cases 9-11 (the gate that guards live trading)</name>
  <files>dashboard/api/tests/test_paper_gate.py</files>
  <read_first>20-VALIDATION.md cases 9-11 and the "DO NOT assert 655 -> 260" note; 20-RESEARCH.md R1 (the refutation); dashboard/api/routes/settings.py L28-60 (the bare COUNT(*) at :36, the RESOLVED predicate ALREADY correct at :43-49, `unresolved` at :50-56) and L186-202 (paper_trades_completed=total_trades at :192, paper_trades_target=50, win_rate_target=40.0); dashboard/api/db.py L19 (KNOWN_BOTS = ("A","B","C","D") — the R1 population); src/db.py L78-95 (is_resolved — THE canonical predicate); src/bot_thread.py L303-332 and L362-382 (the `submitted` / `rejected` writes that prove the COUNT(*) counts non-trades); dashboard/api/tests/test_routes.py L1-60 (the sys.path shim at :15 and the static-fence idiom) and L218-232 (the 100_000.0 absence fence — COPY THIS SHAPE); dashboard/api/models.py (SettingsData)</read_first>
  <behavior>
    New file. `sys.path.insert(0, dashboard/api)` exactly as dashboard/api/tests/test_routes.py:15 does.
    The STATIC cases run everywhere with no DB. The behavioral case uses the same fake/gated style the
    sibling dashboard tests already use — **and must not SKIP** (see the acceptance criteria).

    - case 9 test_non_trade_rows_are_excluded_from_the_paper_gate: seed a bot with
        3 × `closed` with real pnl (+10, -5, +20)
        1 × `submitted`  (pnl NULL)
        1 × `rejected`   (pnl 0)
        1 × `open`       (pnl NULL)
        2 × `closed` with pnl = 0.0   (the historical sentinel shape)
        1 × `closed` with pnl = NULL
      -> **`paper_trades_completed == 3`.**
      RED TODAY: the bare COUNT(*) returns **9**. Six of those nine never became a resolved trade,
      and two of them (`submitted`, `rejected`) never became a POSITION at all.
    - case 10 test_the_bare_count_star_never_returns (**STATIC FENCE**, same idiom as
      test_routes.py:224's 100_000.0 fence):
        POSITIVE CONTROL FIRST — assert `"paper_trades_completed="` IS present in settings.py's source
        (so the fence cannot pass vacuously on an empty/renamed file).
        THEN assert the literal `"SELECT COUNT(*) AS n FROM alpaca_trades"` is **NOT in** settings.py.
      RED TODAY: settings.py:36 is exactly that string.
    - case 11 test_the_gate_is_made_honest_not_unlocked: assert settings.py's source contains the
      CANONICAL RESOLVED predicate (`pnl IS NOT NULL AND pnl <> 0` alongside
      `status IN ('closed', 'stopped', 'target_hit')`), AND that `paper_trades_target=50` and
      `win_rate_target=40.0` are UNCHANGED, AND `mode="paper"` is still pinned.
      **Making the gate honest is not the same as opening it.** The number will read WORSE. That is the
      intended outcome of this phase and it is NOT to be tuned back.

    DO NOT ASSERT A LIVE MAGNITUDE. No case here may assert `655`, `260`, `395`, or any other prod
    number. RESEARCH R1 proved those figures conflate two different bot sets (KNOWN_BOTS A/B/C/D vs
    Phase 17's A/B/C/E) AND two different status filters. The BUG is established by the code; the
    MAGNITUDE is not established by anything. `scripts/e2e_verify.py` (Plan 20-06) MEASURES the
    before/after per bot. A test that asserts an unverified number is the exact class of fabrication
    this milestone exists to eliminate.
  </behavior>
  <action>Create dashboard/api/tests/test_paper_gate.py. Follow the sibling dashboard tests' import shim and fixture style.

**CASE 9 IS PINNED TO ONE IMPLEMENTATION — there is NO fallback, and the obvious-looking one DOES NOT EXIST.** It MUST call the REAL route function `get_settings` with `db.get_db` monkeypatched to a fake connection that HONOURS the SQL it is handed, mirroring the `_FakeConn` idiom at `tests/test_db.py:103-126`. The fake must return the seeded rows for the bare `COUNT(*)` AND genuinely FILTER on `status IN (...)` / `pnl IS NOT NULL AND pnl <> 0` for the RESOLVED query — so that on current `main` it really returns **9** and post-fix returns **3**.

**DO NOT "drive the resolved-count query function directly" — NO SUCH FUNCTION EXISTS.** `settings.py` computes the resolved population INLINE as `len(closed_rows)` from the query at `:43-49`. An executor reaching for a non-existent helper will quietly degrade case 9 into a source grep — which is exactly the FENCE-DODGE this phase exists to prevent: a grep cannot show the count moving from 9 to 3. **DO NOT** add a `TEST_DATABASE_URL` gate — zero new skips is a hard requirement.

Cases 10 and 11 are pure source greps with a positive control first. Never reference bare DATABASE_URL. Never call Alpaca.</action>
  <verify>
    <automated>python -m pytest dashboard/api/tests/test_paper_gate.py -q 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest dashboard/api/tests/test_paper_gate.py -q` — cases **9 and 10 FAIL**; case 11's
      pinned-target half PASSES (the targets are unchanged today) and its predicate half FAILS.
      **Zero SKIP.** Zero collection ERROR.
    - Case 9's failure line reads `assert 9 == 3` (or equivalent) — a real count from a SQL-honouring
      fake driving the REAL `get_settings` route: **not** a fixture error and **not a source grep**.
    - `grep -n "get_settings" dashboard/api/tests/test_paper_gate.py` returns a hit inside case 9 — the
      route itself is driven. (There is no resolved-count helper to call; `settings.py` computes it
      inline as `len(closed_rows)`.)
    - Case 10's failure names the literal `SELECT COUNT(*) AS n FROM alpaca_trades` found in settings.py.
    - `grep -nE "\b655\b|\b260\b|\b395\b" dashboard/api/tests/test_paper_gate.py` returns **NOTHING** —
      R1's refuted magnitudes appear nowhere.
    - `grep -nE "pytest\.skip|skipif|DATABASE_URL" dashboard/api/tests/test_paper_gate.py` returns NOTHING.
    - `python -m pytest tests/ dashboard/api/tests/ -q 2>&1 | tail -1` — skipped count is still **29**.
  </acceptance_criteria>
  <done>The paper gate — the gate that guards LIVE TRADING — has a test for the first time, it fails on current `main` for the right reason, and it asserts no number that nobody has verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test suite → prod DB | a test reading bare `DATABASE_URL` writes to the live trade log |
| test suite → live Alpaca | a test constructing a real AlpacaClient hits a live paper account |
| `src/backfill.py` → prod trade log | `--apply` UPDATEs `alpaca_trades` — the loaded gun |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | a test invoking `backfill(apply=True)` against a real pool | mitigate | every driver case goes through `_wire`, which patches `db` + `TradeLogger` + `_client_for_bot`; no pool is ever constructed |
| T-20-02 | Repudiation | **a G3 case that PASSES pre-fix** — i.e. tests nothing | mitigate | cases 1, 2, 5, 6 are required to FAIL on `main`, with the verbatim failure recorded in the SUMMARY |
| T-20-03 | Repudiation | **the corrected mis-test left in place beside the new ones**, preserving a green test that certifies the bug | mitigate | :199 is EDITED IN PLACE; an acceptance grep asserts the slashed-only fixture is gone |
| T-20-04 | Repudiation | asserting the unverified `655 → 260` and calling it verification | mitigate | acceptance grep forbids 655/260/395 anywhere in the G2 file; the magnitude is MEASURED in Plan 20-06 |
| T-20-05 | DoS | the `None`-coercion landmine shipping unnoticed | mitigate | case 6 exists solely for it |
| T-20-SC | Tampering | package installs | accept | none — this phase installs no packages |
</threat_model>

<verification>
- `python -m pytest tests/test_backfill.py dashboard/api/tests/test_paper_gate.py -q` — cases 1, 2, 5, 6, 9, 10 confirmed FAILING, each for the reason stated above. If any PASSES pre-fix, STOP and fix the test.
- `python -m pytest tests/ dashboard/api/tests/ -q` — no previously-passing test broke; skipped count is still 29.
- The SUMMARY records the verbatim pre-fix return of case 1 — the fabricated `pnl` on a held position.
</verification>

<success_criteria>
G3's six must-fail cases and G2's two must-fail cases each fail for the right reason on current `main`.
`tests/test_backfill.py:199` no longer certifies broken behavior. `dashboard/api/tests/test_paper_gate.py`
exists for the first time. Zero new skips. No test touches Alpaca, SES, or the prod DB.
</success_criteria>

<output>
Create `.planning/phases/20-verification-e2e/20-01-SUMMARY.md` when done. It MUST include a table of the
six must-fail cases with the **verbatim failure line** for each, and it MUST quote case 1's pre-fix
return value in full.
</output>
