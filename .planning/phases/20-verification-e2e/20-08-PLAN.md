---
phase: 20-verification-e2e
plan: 08
type: execute
wave: 2
depends_on: [20-02]
files_modified:
  - src/trend_strategy.py
  - src/bot_c/strategy.py
autonomous: true
requirements: [VERIFY-01]

must_haves:
  truths:
    - "THE POST-T0 WINDOW — THE ONLY EVIDENCE VERIFY-02 HAS — IS CONTAMINATED BY TWO LIVE GROSS-P&L WRITERS, AND THE BIAS STRUCTURALLY EXCEEDS THE TOLERANCE. Verified: src/trend_strategy.py:172-173 computes `pnl = (current_price - entry) * q` and calls `update_alpaca_trade(row['id'], status='closed', exit_price=current_price, pnl=pnl)` with **NO `fees=` argument at all**. src/bot_c/strategy.py:400-402 does the byte-same thing. Both are LIVE dispatch paths (src/bot_thread.py:552 -> run_trend_cycle; :561 -> run_tradingagents_cycle). src/db.py:331 already DOCUMENTS the tell: 'fees may be NULL: bot_c/strategy.py:393-395 and trend_strategy.py:172-173 store a GROSS pnl and pass no fees arg. NULL fees is the TELL that pnl is gross.'"
    - "THE ARITHMETIC: TAKER_FEE = 0.0025 (src/fee_gate.py:16), so a round trip costs ~0.5% of NOTIONAL. But `window_tolerance = max($25, 0.005 * abs(alpaca_realized_window))` is 0.5% of **REALIZED**. Notional and realized differ by 10-100x in normal operation. $50k of turnover producing $500 of realized P&L leaves a ~$250 fee residue against a $25 tolerance. **The known bias exceeds the tolerance by an order of magnitude.** CONTEXT's justification for the relative band ('residue scales with turnover') is arithmetically wrong: a band computed on REALIZED P&L does not scale with turnover."
    - "THIS IS INSIDE VERIFY-01'S OWN REMIT, NOT SCOPE CREEP. VERIFY-01 reads: 'unit/integration tests cover order-state resolution, realized-P&L math **WITH FEES**, universe rejection, and the reconciliation check.' Two live writers that record GROSS P&L are a direct violation of that clause. G1 case 33 tests fees at the BACKFILL join (resolve_stale_row -> get_realized_pnl) — it does NOT touch these two writers, so nothing else in the phase catches them. Shipping the windowed check without this fix ships a check that is BIASED TO FAIL, and then reading its failure as evidence."
    - "THE FIX IS ~4 LINES PER SITE AND THE HELPERS ALREADY EXIST. `src.pnl.realized_pnl(side, entry, exit, qty, fee)` and `src.fee_gate.TAKER_FEE` are the SAME helpers src/backfill.py:84-85 already uses. Do NOT hand-roll a fee formula; do NOT invent a second one. `update_alpaca_trade` already accepts `fees=` (src/db.py:136) — the writers simply never pass it."
    - "THIS FIXES THE WRITERS GOING FORWARD ONLY. It does NOT repair a single historical row. The 643/655 rows already carrying `fees IS NULL` stay exactly as they are — repairing them is a WRITE TO PRODUCTION TRADE DATA and remains behind the blocking human checkpoint in Plan 20-07. **Because T0 is the Phase-20 deploy and this fix ships in the same deploy, the post-T0 window is fee-clean BY CONSTRUCTION** — which is the entire point."
    - "SMALLEST DIFF. Two files, two call sites. Do NOT restructure either strategy, do NOT touch entries, do NOT touch the exit logic, do NOT change what triggers a close. Only the P&L that gets RECORDED changes."
  artifacts:
    - path: "src/trend_strategy.py"
      provides: "the trend exit writer recording NET realized P&L + fees"
      contains: "realized_pnl"
    - path: "src/bot_c/strategy.py"
      provides: "the TradingAgents exit writer recording NET realized P&L + fees"
      contains: "realized_pnl"
  key_links:
    - from: "src/trend_strategy.py"
      to: "src.pnl.realized_pnl + src.fee_gate.TAKER_FEE"
      via: "the SAME helpers src/backfill.py:84-85 uses — never a second fee formula"
      pattern: "realized_pnl"
    - from: "src/bot_c/strategy.py"
      to: "TradeLogger.update_alpaca_trade(fees=...)"
      via: "the fees kwarg the writer never passed"
      pattern: "fees="
---

<objective>
Make the two live GROSS-P&L writers record NET realized P&L **with fees**, so that the post-`T0`
window Phase 20 uses as its evidence is **fee-clean by construction**.

Purpose: Phase 20's ONLY evidence for VERIFY-02 is the post-`T0` anchored window. Two writers on live
dispatch paths keep pushing **fee-less gross P&L** into exactly that window:

- `src/trend_strategy.py:172-173` → `pnl = (current_price - entry) * q`, then
  `update_alpaca_trade(..., pnl=pnl)` — **no `fees=` argument.**
- `src/bot_c/strategy.py:400-402` → byte-identical shape.

`src/db.py:331` already names this out loud: *"NULL fees is the TELL that pnl is gross."*

The bias is not marginal — **it structurally exceeds the tolerance.** Round-trip fees are ~0.5% of
**notional** (`TAKER_FEE = 0.0025`, `src/fee_gate.py:16`), while `window_tolerance` is 0.5% of
**realized**. Those quantities differ by 10-100x. $50k of turnover producing $500 of realized leaves a
~$250 fee residue against a **$25** tolerance.

So without this fix, Phase 20 would ship a reconciliation check that is **biased to fail**, and then
report its failure as a finding. That is not verification — it is manufacturing an artifact and
measuring it.

It is also squarely inside VERIFY-01, whose own words are "realized-P&L math **with fees**."

`T0` is the Phase-20 deploy and this fix rides in that same deploy — so the window is fee-clean **from
its first row**. Historical rows are NOT repaired here; they remain behind Plan 20-07's checkpoint.

Output: two writers, ~4 lines each, using the helpers `src/backfill.py` already uses.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-verification-e2e/20-02-SUMMARY.md
@.planning/phases/20-verification-e2e/20-VALIDATION.md
@.planning/phases/20-verification-e2e/20-RESEARCH.md
@CLAUDE.md

<interfaces>
  # src/pnl.py — THE realized-P&L helper. Handles the SHORT sign. Do not hand-roll a second one.
  def realized_pnl(side: str, entry: float, exit: float, qty: float, fee: float) -> float
      # NET of fees on BOTH legs. src/backfill.py:85 already calls it exactly this way.

  # src/fee_gate.py:16
  TAKER_FEE = float(os.environ.get("TAKER_FEE", "0.0025"))   # Alpaca crypto taker ~0.25%/leg

  # src/backfill.py:84-85 — THE REFERENCE PAIR. Copy this, do not improvise:
  #   fees     = (entry_fill * qty + exit_fill * qty) * TAKER_FEE
  #   realized = realized_pnl(side, entry_fill, exit_fill, qty, TAKER_FEE)

  # src/db.py:136 — the writer ALREADY accepts fees. These two call sites simply never pass it.
  def update_alpaca_trade(trade_id, status, exit_price=None, pnl=None, fees=None)

  # THE TWO DEFECTIVE CALL SITES (verified this session):
  #   src/trend_strategy.py:172-173
  #       pnl = (current_price - entry) * q if entry > 0 else 0.0
  #       logger.update_alpaca_trade(row["id"], status="closed", exit_price=current_price, pnl=pnl)
  #   src/bot_c/strategy.py:400-402
  #       pnl = (current_price - entry) * q if entry > 0 else 0.0
  #       logger.update_alpaca_trade(row["id"], status="closed", exit_price=current_price, pnl=pnl)
  #   Both reached live via src/bot_thread.py:552 (run_trend_cycle) and :561 (run_tradingagents_cycle).

  # NOTE the LONG-ONLY assumption baked into `(current_price - entry) * q`: it is sign-WRONG for a
  # short. `realized_pnl(side, ...)` handles the side correctly — pass the row's actual `side`, do not
  # assume "buy". (Phase 17's EVIDENCE flags sign-inverted shorts on exactly these fee-less rows.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: both exit writers record NET realized P&L + fees</name>
  <files>src/trend_strategy.py, src/bot_c/strategy.py</files>
  <read_first>20-02-SUMMARY.md (the RED in tests/test_gross_pnl_writers.py); src/trend_strategy.py L160-180 (the exit writer); src/bot_c/strategy.py L385-405 (the exit writer); src/backfill.py L77-87 (THE reference fee+realized pair — copy it); src/pnl.py (realized_pnl and its side handling); src/fee_gate.py L10-18 (TAKER_FEE); src/db.py L136-158 (update_alpaca_trade accepts fees=) and L324-334 (the docstring that already names these two sites as gross writers); src/bot_thread.py L550-565 (the live dispatch)</read_first>
  <behavior>
    At BOTH call sites, replace the gross long-only subtraction with the SAME pair `src/backfill.py:84-85`
    already uses:

        side  = row.get("side") or "buy"
        entry = float(row.get("entry_price") or 0)
        q     = float(row.get("qty") or 0)
        fees  = (entry * q + current_price * q) * TAKER_FEE
        pnl   = realized_pnl(side, entry, current_price, q, TAKER_FEE) if entry > 0 else 0.0
        logger.update_alpaca_trade(row["id"], status="closed",
                                   exit_price=current_price, pnl=pnl, fees=fees)

    - `fees=` MUST be passed. A NULL `fees` is the TELL that the P&L is gross (src/db.py:331) — after this
      plan, that tell must stop firing for NEW rows.
    - Use the row's actual `side`. `(current_price - entry) * q` is **sign-wrong for a short**;
      `realized_pnl` handles it. Do not preserve the long-only assumption.
    - `entry > 0` guard stays — a zero entry still records `pnl = 0.0`, unchanged.
    - Do NOT touch what triggers the close, the order placement, the logging, or the surrounding
      try/except. Only the two recorded numbers change.
    - **NO historical row is repaired.** The 643/655 rows already carrying `fees IS NULL` are untouched.
      That is Plan 20-07's blocking checkpoint, not this plan's business.
  </behavior>
  <action>Edit src/trend_strategy.py and src/bot_c/strategy.py. Import `realized_pnl` from src.pnl and `TAKER_FEE` from src.fee_gate at each module's top (both are already importable — src/backfill.py imports exactly these). Replace the two gross computations with the reference pair above and pass `fees=`. Add a one-line comment at each site naming this as the fix for VERIFY-01's "realized-P&L math WITH FEES" clause and noting that a NULL `fees` was the gross tell. Do NOT write a migration. Do NOT touch any historical row. Do NOT change entries, exits, or dispatch. Do NOT restructure either strategy.</action>
  <verify>
    <automated>python -m pytest tests/test_gross_pnl_writers.py tests/ dashboard/api/tests/ -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_gross_pnl_writers.py -q` — **ALL GREEN**: both writers pass a non-None
      `fees`; both record a NET pnl equal to `realized_pnl(side, entry, exit, qty, TAKER_FEE)`; a SHORT row
      records the CORRECT sign (the case that fails against the old `(current_price - entry) * q`).
    - `grep -n "fees=" src/trend_strategy.py src/bot_c/strategy.py` returns a hit in **each** file.
    - `grep -nE "\(current_price - entry\) \* q" src/trend_strategy.py src/bot_c/strategy.py` returns
      **NOTHING** — the gross long-only formula is gone from both.
    - `grep -n "realized_pnl" src/trend_strategy.py src/bot_c/strategy.py` returns a hit in each — the
      SHARED helper is used, not a second fee formula.
    - `git diff --stat src/trend_strategy.py src/bot_c/strategy.py` — two files, **under 25 changed lines
      total.**
    - `git diff --name-only` contains **no** migration and **no** `.sql` file — no historical row is touched.
    - `python -m pytest tests/ dashboard/api/tests/ -q 2>&1 | tail -1` — full suite green; skipped count is
      still **29**.
  </acceptance_criteria>
  <done>Both live exit writers record net realized P&L with fees, using the same helpers the backfill uses, with the correct sign for shorts. Every row written after the Phase-20 deploy — i.e. every row in the post-`T0` window — is fee-clean. No historical row was touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| exit writers → prod `alpaca_trades` | these are pre-existing legitimate writers (`src/db.py` is on the allowlist); this plan changes the VALUES they write, not the fact that they write |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-44 | Repudiation | **shipping a reconciliation check that is structurally biased to fail, then reporting the failure as a finding** | mitigate | the two gross writers are fixed BEFORE `T0`, so the window is fee-clean from its first row |
| T-20-45 | Repudiation | a sign-inverted short surviving in the fee-less rows (Phase 17 EVIDENCE flags exactly this) | mitigate | `realized_pnl(side, ...)` replaces `(current_price - entry) * q`; the RED suite has a short case that fails against the old formula |
| T-20-46 | Tampering | "while we're in there" repairing the 643 historical `fees IS NULL` rows | mitigate | no migration, no UPDATE, no backfill; the acceptance criteria assert no `.sql` file changed; historical repair stays behind Plan 20-07's blocking checkpoint |
| T-20-47 | Tampering | a second, drifting fee formula | mitigate | `src.pnl.realized_pnl` + `src.fee_gate.TAKER_FEE` — the same pair `src/backfill.py:84-85` uses |
| T-20-SC | Tampering | package installs | accept | none — this phase installs no packages |
</threat_model>

<verification>
- `python -m pytest tests/ dashboard/api/tests/ -q` — full suite green; skips still 29.
- `git diff src/trend_strategy.py src/bot_c/strategy.py` reviewed: only the recorded P&L and fees changed. No entry, exit, dispatch or order logic moved.
- **No historical row was modified. No migration was written.**
</verification>

<success_criteria>
Neither live exit writer can record a gross, fee-less, long-only-signed P&L any more. The post-`T0` window
is fee-clean by construction, so the windowed reconciliation check measures reconciliation rather than a
known fee bias.
</success_criteria>

<output>
Create `.planning/phases/20-verification-e2e/20-08-SUMMARY.md` when done. It MUST show the diff of both
call sites and MUST state that no historical row was repaired.
</output>
