---
phase: 14-stale-trade-backfill-repair
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_backfill.py
autonomous: true
requirements: [PNL-05]

must_haves:
  truths:
    - "tests/test_backfill.py exists with all 14 VALIDATION cases as RED tests using zero-network fakes (FakeLogger, FakeAlpacaClient with scripted get_closed_orders + get_positions) extending tests/test_order_resolution.py conventions"
    - "A Wave-0 smoke check confirms alpaca-py assumptions A1-A3: QueryOrderStatus.CLOSED imports, GetOrdersRequest accepts status+symbols+limit+direction+after, and crypto order.symbol carries the slash"
    - "Tests import the not-yet-existing surfaces (src.order_resolution.classify_order, src.backfill.resolve_stale_row, src.db.get_stale_alpaca_candidates, src.db.count_unresolvable_alpaca_rows, AlpacaClient.get_closed_orders) so they fail RED for the right reason (missing impl, not malformed test)"
  artifacts:
    - path: "tests/test_backfill.py"
      provides: "RED test suite covering PNL-05 (14 cases)"
      contains: "def test_backfill_"
      min_lines: 120
  key_links:
    - from: "tests/test_backfill.py"
      to: "src.backfill.resolve_stale_row"
      via: "import + call the pure resolver under test"
      pattern: "from src.backfill import|resolve_stale_row"
    - from: "tests/test_backfill.py"
      to: "src.pnl.realized_pnl"
      via: "assert stored pnl equals realized_pnl(side,entry,exit,qty,TAKER_FEE)"
      pattern: "realized_pnl"
---

<objective>
Write the RED test suite that pins the Phase-14 backfill contract before any implementation, and
cheaply confirm the three flagged alpaca-py assumptions (A1-A3) so the closing-order mechanism is
built on verified footing. This is Wave 0 — every downstream plan turns these tests GREEN.

Purpose: Nyquist gate for PNL-05. All 14 VALIDATION cases become executable specs; the smoke check
de-risks the one new mechanism (CLOSED-status query) before Plan 02 wires it.
Output: tests/test_backfill.py (RED), A1-A3 smoke confirmation recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-stale-trade-backfill-repair/14-CONTEXT.md
@.planning/phases/14-stale-trade-backfill-repair/14-RESEARCH.md
@.planning/phases/14-stale-trade-backfill-repair/14-VALIDATION.md
@CLAUDE.md

<interfaces>
From tests/test_order_resolution.py (L31-143) — copy/extend these fakes:
  FakeLogger (records update_alpaca_trade calls), FakeAlpacaClient (scripted get_order),
  _order(...) order-dict builder, _pending_row(...) row builder.
Surfaces the tests must import (defined in Plans 02/03 — RED now):
  src.order_resolution.classify_order(order) -> (db_status|None, pnl|None)
  src.backfill.resolve_stale_row(row, entry_order, live_symbols, close_order)
    -> ("resolved"|"unchanged"|"unresolvable", write_kwargs|None)
  src.db.get_stale_alpaca_candidates(bot_id, older_than_minutes=30) -> list[dict]
  src.db.count_unresolvable_alpaca_rows(bot_id) -> int
  AlpacaClient.get_closed_orders(symbol) -> list[dict]
P&L reuse contract (assert against these, do NOT reimplement):
  from src.pnl import realized_pnl ; from src.fee_gate import TAKER_FEE
  pnl == realized_pnl(side, entry_fill, exit_fill, qty, TAKER_FEE)
  fees == (entry_fill*qty + exit_fill*qty) * TAKER_FEE
Postgres integration guard idiom:
  @pytest.mark.skipif(not os.environ.get("DATABASE_URL"), reason="needs Postgres")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 smoke — confirm alpaca-py A1-A3 assumptions</name>
  <files>tests/test_backfill.py</files>
  <read_first>14-RESEARCH.md §1 (closing-order lookup) + Assumptions Log A1-A3; src/alpaca_client.py L406-417 (get_open_orders — GetOrdersRequest/QueryOrderStatus already imported) + _parse_order L421-439</read_first>
  <action>Add a smoke test at the top of tests/test_backfill.py named test_alpaca_closed_query_assumptions that asserts A1-A3 with zero network: (A1) import QueryOrderStatus from alpaca.trading.enums and assert QueryOrderStatus.CLOSED exists as a member; (A2) construct GetOrdersRequest(status=QueryOrderStatus.CLOSED, symbols=["BTC/USD"], limit=500, direction="desc") and assert it builds without error and its fields round-trip; (A3) document via an inline assertion/comment that crypto order.symbol keeps the slash ("BTC/USD") matching the alpaca_trades.symbol column — assert the request's symbols list preserves the slash (no stripping). Record the confirmed/deviated outcome for A1-A3 in the plan SUMMARY. If any assumption fails, STOP and note the deviation so Plan 02 adapts the query shape rather than silently 0-matching.</action>
  <verify>
    <automated>python -m pytest tests/test_backfill.py -q -k assumptions 2>&1 | grep -E "passed|failed|error"</automated>
  </verify>
  <acceptance_criteria>
    - QueryOrderStatus.CLOSED import succeeds (A1).
    - GetOrdersRequest builds with status+symbols+limit+direction (A2); add=after tolerated.
    - Slash-carrying symbol asserted preserved (A3).
    - A1-A3 outcome recorded in SUMMARY (confirmed or deviation noted).
  </acceptance_criteria>
  <done>alpaca-py CLOSED-query assumptions are confirmed green (or deviations flagged) before the mechanism is built.</done>
</task>

<task type="auto">
  <name>Task 2: RED suite — all 14 PNL-05 VALIDATION cases</name>
  <files>tests/test_backfill.py</files>
  <read_first>14-VALIDATION.md (the 14-case table — exact test names/scenarios); 14-RESEARCH.md §7 (test approach, fakes, name table) + §3 (realized_pnl reuse args); tests/test_order_resolution.py L31-343 (FakeLogger/FakeAlpacaClient/_order/_pending_row to copy+extend)</read_first>
  <action>Write the full RED suite in tests/test_backfill.py. Extend the copied fakes: FakeAlpacaClient gains a scripted get_closed_orders(symbol) (returns a list of _parse_order-shaped close dicts) and get_positions()/live-symbol set. Implement all 14 named cases mapping to VALIDATION rows: test_backfill_entry_canceled (entry canceled/rejected/expired 0-fill → terminal non-position, pnl=0, no close lookup), test_backfill_filled_closed (filled + position gone + close found → status='closed', exit_price=close.filled_avg_price, pnl==realized_pnl(...), fees set), test_backfill_still_open_unchanged (filled + symbol in live positions → untouched, counted unchanged), test_backfill_no_order_id_residue (NULL order_id → excluded from candidate set, counted by count_unresolvable_alpaca_rows), test_backfill_close_not_found (filled, position gone, no opposite CLOSED order → unresolvable, untouched), test_backfill_idempotent (2nd pass over terminal rows writes nothing — candidate set empty), test_backfill_dry_run_no_write (apply=False → FakeLogger.update_alpaca_trade never called, counts still computed), test_backfill_pnl_long + test_backfill_pnl_short (assert pnl==realized_pnl(side,entry_fill,exit_fill,filled_qty,TAKER_FEE) and fees==(entry*qty+exit*qty)*TAKER_FEE; short side sign positive when exit<entry), test_backfill_guard_window (rows newer than guard window excluded), test_backfill_counts (resolved/unresolvable/unchanged per bot + overall + residue reported), test_backfill_ambiguous_close (two opposite CLOSED orders → earliest filled_at after entry chosen; qty-mismatch partial → unresolvable), test_backfill_per_bot_keys (each bot uses its OWN account client — assert no bare-key/shared-client reuse). Plus the DATABASE_URL-gated test_stale_candidates_sql (skip when unset) asserting get_stale_alpaca_candidates returns only stale open/submitted rows WITH order_id older than the window and excludes the NULL-order_id row, which count_unresolvable_alpaca_rows counts. Make resolve_stale_row the primary pure unit under test. NO fenced production code — this is test code that imports the not-yet-built surfaces so it fails RED on missing impl.</action>
  <verify>
    <automated>python -m pytest tests/test_backfill.py -q 2>&1 | grep -E "passed|failed|error" | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - All 14 VALIDATION cases present under their exact names; collection succeeds (no SyntaxError).
    - Non-smoke cases fail RED for the right reason: ImportError/AttributeError on the missing surfaces (src.order_resolution.classify_order, src.backfill.resolve_stale_row, db candidate fns, AlpacaClient.get_closed_orders), not malformed assertions.
    - P&L cases assert equality against src.pnl.realized_pnl + src.fee_gate.TAKER_FEE (no reimplemented math, no hardcoded 0.0025).
    - DATABASE_URL-gated SQL test skips cleanly when the env var is unset.
    - Zero network; runs < 30s.
  </acceptance_criteria>
  <done>tests/test_backfill.py holds the complete RED PNL-05 spec; downstream plans turn it GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test process → alpaca-py enums/requests | in-process import only, no network (smoke) |
| gated test → Postgres | DATABASE_URL-gated; skipped by default |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | test asserts wrong pnl formula | mitigate | assert against src.pnl.realized_pnl + TAKER_FEE, never inline math |
| T-14-02 | Repudiation | A1-A3 unverified → silent 0-match later | mitigate | Wave-0 smoke confirms enum/request/symbol before mechanism built |
| T-14-SC | Tampering | package installs | accept | no new packages — alpaca-py/pytest already installed |
</threat_model>

<verification>
- `python -m pytest tests/test_backfill.py -q -k assumptions` GREEN (A1-A3 confirmed).
- `python -m pytest tests/test_backfill.py -q` collects all 14 cases; non-smoke cases RED on missing impl.
- No regression: `python -m pytest tests/ -q` still shows the prior baseline plus the new RED file.
</verification>

<success_criteria>
The 14 PNL-05 cases are executable RED specs, the alpaca-py CLOSED-query assumptions are confirmed
(or deviations flagged), and every downstream surface is pinned by an importing test.
</success_criteria>

<output>
Create `.planning/phases/14-stale-trade-backfill-repair/14-01-SUMMARY.md` when done (include the A1-A3 outcome).
</output>
