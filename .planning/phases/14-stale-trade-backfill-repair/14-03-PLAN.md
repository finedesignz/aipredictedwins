---
phase: 14-stale-trade-backfill-repair
plan: 03
type: execute
wave: 3
depends_on: [14-02]
files_modified:
  - src/backfill.py
  - scripts/backfill_trades.py
autonomous: true
requirements: [PNL-05]

must_haves:
  truths:
    - "src.backfill.resolve_stale_row(row, entry_order, live_symbols, close_order) is a pure function returning ('resolved'|'unchanged'|'unresolvable', write_kwargs|None): entry canceled/rejected/expired 0-fill → terminal non-position pnl=0; filled + symbol in live_symbols → unchanged; filled + gone + matching close → resolved with realized_pnl+fees; filled + gone + no findable close → unresolvable"
    - "src.backfill.backfill(apply=False) enumerates enabled bots via reconciliation._enabled_bot_ids, builds a per-bot client via reconciliation._client_for_bot (never bare/shared keys), classifies each stale candidate with classify_order, finds the closing order via get_closed_orders + earliest-opposite-after-entry heuristic, and on apply writes via TradeLogger.update_alpaca_trade; dry-run (default) computes identical counts but writes nothing"
    - "Realized P&L reuses src.pnl.realized_pnl(side, entry_fill, exit_fill, qty, TAKER_FEE) with entry_fill=filled_avg_price (fallback entry_price), exit_fill=close.filled_avg_price, qty=filled_qty; fees=(entry_fill*qty+exit_fill*qty)*TAKER_FEE — no reimplemented math, never hardcoded 0.0025"
    - "scripts/backfill_trades.py prints a per-bot + ALL counts table (resolved/unchanged/unresolvable/residue) with --apply flag, dry-run default; NEVER deletes rows"
  artifacts:
    - path: "src/backfill.py"
      provides: "pure resolve_stale_row + backfill(apply) driver"
      contains: "def resolve_stale_row"
    - path: "scripts/backfill_trades.py"
      provides: "argparse entrypoint, --apply, dry-run default, counts table"
      contains: "add_argument(\"--apply\""
  key_links:
    - from: "src/backfill.py::backfill"
      to: "reconciliation._client_for_bot / _enabled_bot_ids"
      via: "per-bot key sourcing (one account per bot)"
      pattern: "_client_for_bot|_enabled_bot_ids"
    - from: "src/backfill.py::resolve_stale_row"
      to: "src.pnl.realized_pnl"
      via: "realized P&L from entry+close fills, TAKER_FEE"
      pattern: "realized_pnl"
    - from: "src/backfill.py::backfill"
      to: "TradeLogger.update_alpaca_trade"
      via: "resolved-close write, apply-gated"
      pattern: "update_alpaca_trade"
    - from: "scripts/backfill_trades.py"
      to: "src.backfill.backfill"
      via: "thin main() delegate + counts table"
      pattern: "from src.backfill import"
---

<objective>
Wire the resolver: a pure `resolve_stale_row` decision function plus the `backfill(apply)` driver that
loops enabled bots with per-bot keys, resolves each stale candidate via the Plan-02 foundation +
reused Phase-11/12 paths, and a thin `scripts/backfill_trades.py` entrypoint (dry-run default,
`--apply` to write, counts table). Turns the Plan-01 RED suite GREEN.

Purpose: complete PNL-05 — resolve genuinely-stale rows to true terminal state, idempotently, report
counts + irreducible residue, never delete.
Output: src/backfill.py (resolve_stale_row + backfill), scripts/backfill_trades.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-stale-trade-backfill-repair/14-CONTEXT.md
@.planning/phases/14-stale-trade-backfill-repair/14-RESEARCH.md
@CLAUDE.md

<interfaces>
Plan-02 surfaces to consume (now built):
  src.order_resolution.classify_order(order) -> (db_status|None, pnl|None)
  src.db.get_stale_alpaca_candidates(bot_id, older_than_minutes=30), count_unresolvable_alpaca_rows(bot_id)
  AlpacaClient.get_closed_orders(symbol, after=None) -> [_parse_order dicts]; AlpacaClient.get_order(order_id)
Reuse verbatim (do NOT reimplement):
  src.reconciliation._enabled_bot_ids() -> [bot_id]  (bots WHERE enabled)
  src.reconciliation._client_for_bot(bot_id) -> AlpacaClient  (ALPACA_API_KEY_{id}, never bare)
  src.pnl.realized_pnl(side, entry_fill, exit_fill, qty, taker_fee)
  src.fee_gate.TAKER_FEE
  src.trade_logger.TradeLogger(bot_id).update_alpaca_trade(trade_id, status, exit_price, pnl, fees)
Phase-12 monitor close write to mirror (src/alpaca_orchestrator.py L303-316):
  entry_fill = float(row["filled_avg_price"] or row["entry_price"])
  exit_fill  = float(close_order["filled_avg_price"]); qty = float(row["filled_qty"] or row["qty"])
  fees = (entry_fill*qty + exit_fill*qty) * TAKER_FEE
  realized = realized_pnl(side, entry_fill, exit_fill, qty, TAKER_FEE)
  logger.update_alpaca_trade(trade_id, status="closed", exit_price=exit_fill, pnl=realized, fees=fees)
Entrypoint template: scripts/reconcile.py L1-36 (thin main, per-bot table, exit code).
Guard window: BACKFILL_GUARD_MINUTES default 30, passed to get_stale_alpaca_candidates.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/backfill.py — pure resolve_stale_row</name>
  <files>src/backfill.py</files>
  <behavior>
    - entry canceled/rejected/expired 0-fill → ("resolved", {status, pnl:0}) — terminal non-position
    - filled + symbol in live_symbols → ("unchanged", None)
    - filled + gone + close_order present → ("resolved", {status:"closed", exit_price, pnl, fees})
    - filled + gone + close_order None → ("unresolvable", None)
    - close qty mismatch (partial) surfaced by caller as no valid close → ("unresolvable", None)
  </behavior>
  <read_first>14-RESEARCH.md §3 (realized_pnl reuse args) + §1 (matching heuristic + partial-close→unresolvable) + §4 (write kwargs); tests/test_backfill.py (the RED contract for resolve_stale_row); src/alpaca_orchestrator.py L303-316 (monitor close write to mirror)</read_first>
  <action>Create src/backfill.py with a pure resolve_stale_row(row, entry_order, live_symbols, close_order) -> tuple[str, dict|None]. Classify the entry via classify_order(entry_order): if it returns a terminal non-position (status in {canceled,cancelled,expired,rejected}, 0 fill) return ("resolved", {"status": status, "exit_price": None, "pnl": 0, "fees": None}). If classified "open" (filled/partial → became a position): if row["symbol"] in live_symbols return ("unchanged", None); else require close_order — if None return ("unresolvable", None); if present compute entry_fill=float(row["filled_avg_price"] or row["entry_price"]), exit_fill=float(close_order["filled_avg_price"]), qty=float(row["filled_qty"] or row["qty"]), side=row["side"]; if exit_fill is 0/None return ("unresolvable", None); fees=(entry_fill*qty+exit_fill*qty)*TAKER_FEE; realized=realized_pnl(side, entry_fill, exit_fill, qty, TAKER_FEE); return ("resolved", {"status":"closed","exit_price":exit_fill,"pnl":realized,"fees":fees}). If classify returns in-flight (None,None) return ("unchanged", None). Pure — imports only classify_order, realized_pnl, TAKER_FEE; no db/alpaca I/O, no writes. Import TAKER_FEE from src.fee_gate (never hardcode 0.0025).</action>
  <verify>
    <automated>python -m pytest tests/test_backfill.py -q -k "pnl or canceled or still_open or close_not_found" 2>&1 | grep -E "passed|failed" | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - resolve_stale_row returns the exact ("resolved"|"unchanged"|"unresolvable", write_kwargs|None) contract for every branch.
    - P&L via src.pnl.realized_pnl + src.fee_gate.TAKER_FEE; entry_fill falls back to entry_price; qty uses filled_qty.
    - Pure: no db/alpaca imports, no writes; RED cases for these branches turn GREEN.
  </acceptance_criteria>
  <done>The resolution decision is a zero-I/O pure function turning the resolver RED cases green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: src/backfill.py — backfill(apply) driver + closing-order match</name>
  <files>src/backfill.py</files>
  <behavior>
    - dry-run (apply=False) computes counts, TradeLogger.update_alpaca_trade never called
    - apply=True writes resolved rows via TradeLogger.update_alpaca_trade
    - each bot uses its OWN _client_for_bot (no shared/bare keys)
    - two opposite CLOSED orders → earliest filled_at after entry chosen; qty mismatch → unresolvable
    - re-run over already-terminal rows is a no-op (candidate set empty)
  </behavior>
  <read_first>src/reconciliation.py L51-93 (_enabled_bot_ids, _client_for_bot) + L96-143 (driver structure); 14-RESEARCH.md §1 (matching heuristic), §4 (idempotency structural), §5 (key sourcing), §6 (counts + residue); tests/test_backfill.py (counts/idempotent/dry-run/per-bot cases)</read_first>
  <action>Add backfill(apply: bool = False) -> list[tuple[str, dict]] to src/backfill.py. For each bot_id in reconciliation._enabled_bot_ids(): build client = reconciliation._client_for_bot(bot_id) (per-bot keys — never bare), logger = TradeLogger(bot_id); read guard = int(os.environ.get("BACKFILL_GUARD_MINUTES","30")); candidates = db.get_stale_alpaca_candidates(bot_id, guard). Init counts {resolved,unchanged,unresolvable,residue}. Fetch live symbol set once per bot from the client's open positions. For each row: entry_order = client.get_order(row["order_id"]); if classify_order(entry_order) is "open" and symbol not in live_symbols, find the closing order — call client.get_closed_orders(row["symbol"], after=entry filled_at) and apply the matching heuristic: keep opposite-side (buy→sell / sell→buy) orders that are filled (filled_qty>0) with filled_at after the entry's filled_at, pick the EARLIEST; require the chosen close filled_qty ≈ row filled_qty within a small tolerance else treat as no valid close (partial-close aggregation out of scope). Pass row, entry_order, live_symbols, close_order to resolve_stale_row. Increment the matching count. On outcome "resolved" AND apply: logger.update_alpaca_trade(row["id"], **write_kwargs) (drop None kwargs to match the monitor call — closed rows pass exit_price/pnl/fees; terminal non-position passes status/pnl=0). Never write when apply is False. After the loop set counts["residue"] = db.count_unresolvable_alpaca_rows(bot_id). Append (bot_id, counts). Idempotency is structural — candidates fresh each call, terminal rows excluded; add no explicit guard. NEVER delete/reset rows (CLAUDE.md rule 6) — only update via TradeLogger.</action>
  <verify>
    <automated>python -m pytest tests/test_backfill.py -q 2>&1 | grep -E "passed|failed" | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - Enumerates via _enabled_bot_ids; each bot uses its own _client_for_bot (test_backfill_per_bot_keys green).
    - Closing-order heuristic: opposite side, filled, filled_at after entry, earliest chosen; qty-mismatch → unresolvable (ambiguous case green).
    - apply=False writes nothing (dry-run case green); apply=True writes via update_alpaca_trade.
    - counts include per-bot resolved/unchanged/unresolvable + residue via count_unresolvable_alpaca_rows; idempotent re-run is a no-op.
    - No DELETE/reset anywhere; whole tests/test_backfill.py suite GREEN.
  </acceptance_criteria>
  <done>The driver resolves stale candidates per bot with correct counts, idempotent, dry-run-safe, one account per bot.</done>
</task>

<task type="auto">
  <name>Task 3: scripts/backfill_trades.py entrypoint</name>
  <files>scripts/backfill_trades.py</files>
  <read_first>scripts/reconcile.py L1-36 (entrypoint template — thin main, per-bot table, exit code); 14-RESEARCH.md §6 (entrypoint shape + counts table + dry-run banner)</read_first>
  <action>Create scripts/backfill_trades.py mirroring scripts/reconcile.py. Thin main() -> int with argparse: --apply (action="store_true", help notes dry-run is the default). Call results = backfill(apply=args.apply) (from src.backfill import backfill). Print a fixed-width header row Bot / Resolved / Unchanged / Unresolvable / Residue, a separator, one line per (bot_id, counts), an ALL totals line summing every count key, and when not args.apply print a "DRY RUN — no rows written. Re-run with --apply to persist." banner. return 0. Guard with if __name__ == "__main__": sys.exit(main()). No writes here — all logic is in the driver.</action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('scripts/backfill_trades.py').read()); print('OK')" && python scripts/backfill_trades.py --help 2>&1 | grep -- --apply</automated>
  </verify>
  <acceptance_criteria>
    - --apply flag present; dry-run is the default (no flag → no writes, banner shown).
    - Prints per-bot + ALL counts table (resolved/unchanged/unresolvable/residue).
    - Thin delegate to src.backfill.backfill; --help lists --apply.
  </acceptance_criteria>
  <done>Operators run scripts/backfill_trades.py for a safe dry-run preview and --apply to persist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| script → Alpaca API | per-bot-keyed reads (get_order, get_closed_orders, positions) |
| script → Postgres | reads candidates; writes only resolved rows via TradeLogger UPDATE |
| operator CLI → mutation | --apply gates all writes; default dry-run |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06 | Elevation of Privilege | bare/shared Alpaca keys | mitigate | reuse _client_for_bot only; never read bare ALPACA_API_KEY (one account per bot) |
| T-14-07 | Tampering | destructive row edits | mitigate | UPDATE-only via TradeLogger.update_alpaca_trade; NO DELETE/reset (CLAUDE.md rule 6) |
| T-14-08 | Repudiation | accidental mass write | mitigate | dry-run default; --apply required; counts previewed identically in dry-run |
| T-14-09 | Information Disclosure | wrong close matched → bad P&L | mitigate | opposite-side + earliest-after-entry + qty-tolerance heuristic; mismatch → unresolvable, not guessed |
| T-14-SC | Tampering | package installs | accept | no new packages |
</threat_model>

<verification>
- `python -m pytest tests/test_backfill.py -q` — all 14 PNL-05 cases GREEN.
- `python -m pytest tests/ -q` — full suite green, zero regressions (baseline 320 passed, 3 skipped + new).
- `python scripts/backfill_trades.py` (dry-run) prints the per-bot counts table + DRY RUN banner, writes nothing.
</verification>

<success_criteria>
Genuinely-stale alpaca_trades rows resolve to true terminal state via the reused Phase-11/12 paths,
idempotently and with per-bot keys; the script reports resolved/unchanged/unresolvable + irreducible
residue; dry-run is the default and no row is ever deleted.
</success_criteria>

<output>
Create `.planning/phases/14-stale-trade-backfill-repair/14-03-SUMMARY.md` when done.
</output>
