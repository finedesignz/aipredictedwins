---
phase: 14-stale-trade-backfill-repair
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - src/order_resolution.py
  - src/bot_thread.py
  - src/db.py
  - src/alpaca_client.py
autonomous: true
requirements: [PNL-05]

must_haves:
  truths:
    - "src.order_resolution.classify_order(order) is a module-level pure function mapping a parsed order to (db_status|None, pnl|None) using a canonical _TERMINAL_NONPOSITION set, and BotThread._classify delegates to it in one line so all Phase-11 tests stay GREEN"
    - "src.db.get_stale_alpaca_candidates(bot_id, older_than_minutes=30) selects status IN ('open','submitted') AND order_id IS NOT NULL older than the guard window; count_unresolvable_alpaca_rows(bot_id) counts non-terminal rows with NULL order_id — both read-only, parameterized, per-bot"
    - "AlpacaClient.get_closed_orders(symbol) issues GetOrdersRequest(status=QueryOrderStatus.CLOSED, symbols=[symbol], limit=500, direction='desc', after=...) wrapped in the existing _retry, returning _parse_order-shaped dicts with the crypto slash preserved"
  artifacts:
    - path: "src/order_resolution.py"
      provides: "pure classify_order + _TERMINAL_NONPOSITION"
      contains: "def classify_order"
    - path: "src/db.py"
      provides: "get_stale_alpaca_candidates, count_unresolvable_alpaca_rows"
      contains: "def get_stale_alpaca_candidates"
    - path: "src/alpaca_client.py"
      provides: "get_closed_orders CLOSED-status lookup"
      contains: "def get_closed_orders"
  key_links:
    - from: "src/bot_thread.py::BotThread._classify"
      to: "src.order_resolution.classify_order"
      via: "one-line delegate (keeps Phase-11 tests green)"
      pattern: "classify_order"
    - from: "src/db.py::get_stale_alpaca_candidates"
      to: "alpaca_trades"
      via: "status IN ('open','submitted') AND order_id IS NOT NULL AND timestamp older than window"
      pattern: "order_id IS NOT NULL"
    - from: "src/alpaca_client.py::get_closed_orders"
      to: "alpaca get_orders"
      via: "GetOrdersRequest(status=QueryOrderStatus.CLOSED, symbols=[symbol])"
      pattern: "QueryOrderStatus.CLOSED"
---

<objective>
Build the foundation the Plan 03 driver wires against: extract Phase-11 classification into a pure
`classify_order` (BotThread delegates), add the two read-only stale-candidate/residue db queries, and
add the one new `AlpacaClient.get_closed_orders` CLOSED-status lookup. All reuse — no P&L or resolver
re-implementation.

Purpose: PNL-05 building blocks. classify_order lets a standalone script classify entry orders without
importing the heavy BotThread graph; the db fns localize the candidate SQL; get_closed_orders is the
single new mechanism (find the closing order for a filled entry whose position is gone).
Output: src/order_resolution.py, one-line BotThread delegate, two db.py fns, AlpacaClient.get_closed_orders.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-stale-trade-backfill-repair/14-CONTEXT.md
@.planning/phases/14-stale-trade-backfill-repair/14-RESEARCH.md
@CLAUDE.md

<interfaces>
From src/bot_thread.py L236,L262-277 — the logic to extract verbatim:
  _TERMINAL_NONPOSITION frozenset {canceled, cancelled, expired, rejected}
  _classify(order): filled OR filled_qty>0 -> ("open", None); terminal-nonposition + 0 fill ->
    (status, 0); in-flight -> (None, None). No self dependency beyond the frozenset.
From src/alpaca_client.py L406-417 — get_open_orders is the exact template (GetOrdersRequest +
  QueryOrderStatus already imported at module top; _retry wrapper; returns [_parse_order(o) for o ...]).
  L385 close_position strips slash — do NOT strip in get_closed_orders.
From src/db.py L133-148 get_pending_alpaca_orders (per-bot %s parameterized, dict_row, connection())
  and L151-165 get_recent_loss_symbols (timestamp::timestamptz < NOW() - (%s || ' minutes')::interval).
Guard window env idiom: BACKFILL_GUARD_MINUTES default 30 (mirror LIMIT_ORDER_TIMEOUT_S style).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract pure classify_order; BotThread._classify delegates</name>
  <files>src/order_resolution.py, src/bot_thread.py</files>
  <behavior>
    - classify_order(filled order) -> ("open", None)
    - classify_order(canceled/rejected/expired, 0 fill) -> (that status, 0)
    - classify_order(in-flight submitted) -> (None, None)
    - BotThread._classify(order) returns identical results (Phase-11 suite stays green)
  </behavior>
  <read_first>src/bot_thread.py L236 (_TERMINAL_NONPOSITION) + L262-277 (_classify); tests/test_order_resolution.py (the Phase-11 cases that must stay GREEN); 14-RESEARCH.md §3</read_first>
  <action>Create src/order_resolution.py with module-level _TERMINAL_NONPOSITION frozenset (copy the canonical set) and a pure function classify_order(order) -> tuple[str|None, int|None] implementing the exact Phase-11 ladder (filled OR filled_qty>0 → ("open", None); status in _TERMINAL_NONPOSITION with 0 fill → (status, 0); else (None, None)). No imports of db/alpaca/BotThread; reads only the order dict. Then change BotThread._classify to a one-line delegate: return classify_order(order) (import classify_order at the top of bot_thread.py). Do NOT move any other logic; keep _TERMINAL_NONPOSITION referenced from BotThread if other methods use it (re-export from the new module or keep the alias). Karpathy smallest-diff — delegate, do not relocate call sites.</action>
  <verify>
    <automated>python -m pytest tests/test_order_resolution.py -q 2>&1 | grep -E "passed|failed" | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - src/order_resolution.py exposes pure classify_order + _TERMINAL_NONPOSITION, zero db/alpaca/BotThread imports.
    - BotThread._classify is a one-line delegate to classify_order.
    - Full Phase-11 suite tests/test_order_resolution.py stays GREEN (no behavior change).
  </acceptance_criteria>
  <done>Classification is a reusable pure helper importable by a standalone script; Phase-11 tests unaffected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: db.py stale-candidate + residue-count queries</name>
  <files>src/db.py</files>
  <behavior>
    - get_stale_alpaca_candidates returns only status IN ('open','submitted') rows WITH order_id older than window
    - rows newer than the guard window are excluded
    - NULL-order_id rows are excluded from candidates but counted by count_unresolvable_alpaca_rows
  </behavior>
  <read_first>src/db.py L133-148 get_pending_alpaca_orders (style) + L151-165 get_recent_loss_symbols (interval pattern); 14-RESEARCH.md §2 (exact queries + selected columns)</read_first>
  <action>Add two read-only functions to src/db.py using connection()/dict_row/%s. (1) get_stale_alpaca_candidates(bot_id, older_than_minutes=30) -> list[dict]: SELECT id, order_id, symbol, side, qty, entry_price, filled_qty, filled_avg_price, order_type, status, timestamp FROM alpaca_trades WHERE bot_id=%s AND status IN ('open','submitted') AND order_id IS NOT NULL AND timestamp::timestamptz < NOW() - (%s || ' minutes')::interval ORDER BY timestamp ASC, params (bot_id, str(older_than_minutes)). Select must return every column the resolution ladder needs so no second query is required. (2) count_unresolvable_alpaca_rows(bot_id) -> int: SELECT COUNT(*) AS n FROM alpaca_trades WHERE bot_id=%s AND status IN ('open','submitted') AND order_id IS NULL; return row["n"]. Additive only — modify no existing function. Read-only (SELECT/COUNT); NEVER any DELETE/UPDATE here (CLAUDE.md rule 6).</action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('src/db.py').read()); import src.db as d; assert hasattr(d,'get_stale_alpaca_candidates') and hasattr(d,'count_unresolvable_alpaca_rows'); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - get_stale_alpaca_candidates: status IN ('open','submitted') AND order_id IS NOT NULL AND timestamp older than the parameterized minutes window; returns the full column set; ORDER BY timestamp ASC.
    - count_unresolvable_alpaca_rows: counts non-terminal rows with NULL order_id.
    - Both parameterized %s, per-bot, read-only; no existing db.py function touched.
    - DATABASE_URL-gated test_stale_candidates_sql (Plan 01) passes when a Postgres URL is present.
  </acceptance_criteria>
  <done>Two per-bot read-only queries localize the candidate set and the irreducible NULL-order_id residue count.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AlpacaClient.get_closed_orders CLOSED-status lookup</name>
  <files>src/alpaca_client.py</files>
  <behavior>
    - get_closed_orders("BTC/USD") issues a CLOSED-status GetOrdersRequest and returns _parse_order-shaped dicts
    - the slash in the crypto symbol is preserved (no stripping)
    - transient failures go through the existing _retry wrapper
  </behavior>
  <read_first>src/alpaca_client.py L406-417 get_open_orders (exact template) + L421-439 _parse_order + L382-389 close_position (slash-strip is close_position ONLY); 14-RESEARCH.md §1 (request shape, after-bound, matching heuristic is the DRIVER's job not this method's)</read_first>
  <action>Add AlpacaClient.get_closed_orders(symbol, after=None) mirroring get_open_orders: build GetOrdersRequest(status=QueryOrderStatus.CLOSED, symbols=[symbol], limit=500, direction="desc"), and when after is provided pass after=after to bound the window to orders created after the entry filled_at (shrinks the set, excludes pre-entry orders). Call self._trading_client.get_orders(filter=request) inside the existing _retry wrapper exactly like get_open_orders, and return [self._parse_order(o) for o in raw]. Do NOT strip the slash from symbol (that is close_position-only). This method only fetches+parses; the opposite-side / earliest-after-entry matching heuristic lives in the Plan 03 driver, not here. Reuse the already module-imported GetOrdersRequest + QueryOrderStatus (confirmed by the Plan 01 A1 smoke).</action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('src/alpaca_client.py').read()); import src.alpaca_client as a; assert hasattr(a.AlpacaClient,'get_closed_orders'); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - get_closed_orders uses QueryOrderStatus.CLOSED + symbols=[symbol] + limit=500 + direction='desc', optional after bound.
    - Wrapped in _retry; returns [_parse_order(o) ...] dicts.
    - Slash preserved (no close_position-style stripping); method is fetch+parse only (no matching logic).
  </acceptance_criteria>
  <done>The one new mechanism — CLOSED-order lookup by symbol — exists and mirrors get_open_orders.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| script → Postgres | read-only SELECT/COUNT via parameterized %s |
| AlpacaClient → Alpaca API | per-bot-keyed CLOSED-order fetch, _retry-wrapped |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Tampering | stale-candidate SQL | mitigate | parameterized %s only; read-only SELECT/COUNT, no mutation |
| T-14-04 | Information Disclosure | wrong candidate predicate resolves live in-flight rows | mitigate | order_id IS NOT NULL + guard-window age filter; A3 slash preserved |
| T-14-05 | Repudiation | classify extraction drifts from Phase 11 | mitigate | one-line delegate; Phase-11 suite gate stays green |
| T-14-SC | Tampering | package installs | accept | no new packages |
</threat_model>

<verification>
- `python -m pytest tests/test_order_resolution.py -q` GREEN (delegate preserves Phase-11).
- `python -m pytest tests/test_backfill.py -q` — classify/db/get_closed_orders import errors clear; resolver cases still RED until Plan 03.
- `python -m pytest tests/ -q` no regressions.
</verification>

<success_criteria>
Pure classify_order extracted with BotThread delegating, two read-only per-bot candidate/residue db
queries added, and AlpacaClient.get_closed_orders CLOSED-status lookup in place — all additive/reuse,
Phase-11 tests green.
</success_criteria>

<output>
Create `.planning/phases/14-stale-trade-backfill-repair/14-02-SUMMARY.md` when done.
</output>
