---
phase: 13-alpaca-reconciliation-check
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_reconciliation.py
autonomous: true
requirements: [PNL-03]

must_haves:
  truths:
    - "tests/test_reconciliation.py exists with all 10 VALIDATION cases, each initially RED (target symbols not yet implemented)"
    - "Case 6 asserts trade-log realized P&L sums status IN ('closed','stopped','target_hit') and excludes canceled/rejected/expired"
    - "Pure-math cases use zero-network fakes and pytest.approx(abs=1e-9); driver cases use in-memory FakeLogger/FakeAlpacaClient and monkeypatched notifier"
  artifacts:
    - path: "tests/test_reconciliation.py"
      provides: "10 RED test cases for reconcile_bot helper + driver + persist + three-state sum"
      contains: "def test_reconcile_within_tolerance"
  key_links:
    - from: "tests/test_reconciliation.py"
      to: "src/reconciliation.py::reconcile_bot"
      via: "import + call in pure cases"
      pattern: "reconcile_bot"
    - from: "tests/test_reconciliation.py"
      to: "src/db.py::get_realized_pnl"
      via: "three-state sum case 6"
      pattern: "get_realized_pnl"
---

<objective>
Write the failing (RED) test contract for Phase 13 reconciliation: all 10 VALIDATION cases in
`tests/test_reconciliation.py`, using the repo's zero-network fake-double convention. These tests
define the exact behavior Plans 02 and 03 must satisfy — pure `reconcile_bot` math, the three-state
realized-P&L sum, per-bot persistence, and breach alerting.

Purpose: PNL-03 requires trade-log realized P&L to be reconciled against Alpaca-derived realized P&L
with a tolerance breach flag. Tests-first locks the contract (especially case 6, the three-state
correction) before any implementation exists.
Output: tests/test_reconciliation.py (10 cases, all RED until Plans 02/03 land).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-alpaca-reconciliation-check/13-CONTEXT.md
@.planning/phases/13-alpaca-reconciliation-check/13-RESEARCH.md
@.planning/phases/13-alpaca-reconciliation-check/13-VALIDATION.md
@CLAUDE.md

<interfaces>
Contract the tests are written AGAINST (implemented in Plans 02/03 — RED until then):

src/reconciliation.py::reconcile_bot(trade_log_pnl, equity, starting_equity, unrealized_pnl, tolerance)
  -> dict {trade_log_pnl, alpaca_realized_pnl, delta, within_tolerance, tolerance}
  alpaca_realized_pnl = (equity - starting_equity) - unrealized_pnl
  delta = trade_log_pnl - alpaca_realized_pnl
  within_tolerance = abs(delta) <= tolerance   (boundary inclusive)

src/db.py::get_realized_pnl(bot_id) -> float   # SUM(pnl) over status IN ('closed','stopped','target_hit')
src/db.py::record_reconciliation(bot_id, result) -> None   # UPSERT single row per bot
src/reconciliation.py driver (Plan 03) — reconcile()/reconcile_bot_live: assembles inputs per bot,
  calls reconcile_bot, calls db.record_reconciliation, logs WARNING + notifier.send_alert on breach.

Test convention (mirror tests/test_close_pnl.py, tests/test_order_resolution.py):
- In-memory FakeLogger capturing recorded rows; FakeAlpacaClient returning fixed equity/positions.
- pytest.approx(abs=1e-9) for cent-exactness.
- caplog + monkeypatch on notifier.send_alert for the alert cases.
- DB-touching cases guarded/skippable on DATABASE_URL (keep math cases pure).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure reconcile_bot math cases (1-5, 10)</name>
  <files>tests/test_reconciliation.py</files>
  <read_first>tests/test_close_pnl.py (fake-double + pytest.approx convention); .planning/phases/13-alpaca-reconciliation-check/13-VALIDATION.md (case table); 13-RESEARCH.md Finding 7 (test list)</read_first>
  <behavior>
    - test_reconcile_within_tolerance: delta just under tolerance -> within_tolerance is True.
    - test_reconcile_over_tolerance: delta just over tolerance -> within_tolerance is False.
    - test_reconcile_boundary: abs(delta) == tolerance -> within_tolerance is True (inclusive <=).
    - test_reconcile_negative_delta: trade_log > alpaca (negative delta) over tolerance -> breach via abs().
    - test_reconcile_alpaca_derivation: alpaca_realized_pnl == (equity - starting_equity) - unrealized, exercised with a positive (long) and a negative (short) unrealized value.
    - test_reconcile_guards: zero unrealized / empty-positions path -> alpaca_realized_pnl == equity - starting_equity, no crash; result_shape assertion of the 5 dict keys folded in here.
  </behavior>
  <action>Create `tests/test_reconciliation.py` importing `reconcile_bot` from `src.reconciliation`. Write the six pure cases above with concrete golden numbers computed by hand from the derivation formula, asserting with pytest.approx(abs=1e-9). Assert the result dict has exactly keys {trade_log_pnl, alpaca_realized_pnl, delta, within_tolerance, tolerance}. No network, no DB, no fakes needed for these — pass plain floats. These MUST fail at authoring time because src/reconciliation.py does not yet exist (RED).</action>
  <verify>
    <automated>python -m pytest tests/test_reconciliation.py -q -k "reconcile" 2>&1 | grep -E "error|Error|failed|passed"</automated>
  </verify>
  <acceptance_criteria>
    - Six pure cases present with hand-computed goldens and pytest.approx(abs=1e-9).
    - Boundary case asserts inclusive (abs(delta) == tolerance is within).
    - Derivation case exercises both positive and negative unrealized.
    - Result-shape assertion checks the exact 5 keys.
    - All currently RED (ImportError / assertion) — collection does not error the whole file for the other tasks.
  </acceptance_criteria>
  <done>Pure math contract for reconcile_bot is fully specified and RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Three-state realized-P&L sum case (6)</name>
  <files>tests/test_reconciliation.py</files>
  <read_first>src/db.py get_alpaca_accuracy (three-state status set to mirror); dashboard/api/routes/equity.py _build_db_series (same set); 13-RESEARCH.md Finding 1 + Pitfall 1</read_first>
  <behavior>
    - test_realized_pnl_three_states: a bot with alpaca_trades rows across statuses closed(+pnl), stopped(+pnl), target_hit(+pnl), canceled(pnl NULL), rejected(pnl NULL), expired(pnl NULL) -> get_realized_pnl returns the SUM of only the closed+stopped+target_hit pnl, excluding the three non-position terminals; NULL pnl guarded to 0.
  </behavior>
  <action>Add `test_realized_pnl_three_states` to `tests/test_reconciliation.py`. Drive it through a FakeLogger/fake-db that exposes `get_realized_pnl(bot_id)` over a fixed in-memory row set (mirror the FakeLogger row-capture pattern in tests/test_close_pnl.py), OR mark it as a DB-integration test guarded on DATABASE_URL following tests/test_db.py — pick the fake path so it stays in the zero-network suite. Assert the returned sum equals only the closed+stopped+target_hit contributions (a distinct golden), and that adding a canceled/rejected/expired row does NOT change the sum. This is the critical Pitfall-1 guard: summing 'closed' alone must fail this test. RED until Plan 02 adds get_realized_pnl.</action>
  <verify>
    <automated>python -m pytest tests/test_reconciliation.py -q -k "three_states" 2>&1 | grep -E "error|Error|failed|passed"</automated>
  </verify>
  <acceptance_criteria>
    - test_realized_pnl_three_states asserts sum over exactly {closed,stopped,target_hit}.
    - Canceled/rejected/expired rows (NULL pnl) proven excluded and non-mutating.
    - Fails today (get_realized_pnl / fake not yet wired) — RED.
  </acceptance_criteria>
  <done>Three-state sum contract locked so a 'closed'-only implementation cannot pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Driver persist + alert + multi-bot cases (7-9)</name>
  <files>tests/test_reconciliation.py</files>
  <read_first>tests/test_close_pnl.py (FakeLogger capture pattern); src/notifier.py send_alert signature; 13-RESEARCH.md Finding 5 + Finding 7 driver tests</read_first>
  <behavior>
    - test_persist_reconciliation: a breaching bot -> driver calls db.record_reconciliation once with the helper's dict (bot_id, delta, within_tolerance=False, tolerance captured).
    - test_breach_alerts: breach -> WARNING logged AND notifier.send_alert called exactly once; within-tolerance -> INFO logged, send_alert NOT called.
    - test_multi_bot_independent: two enabled bots, one clean + one breaching, each reconciled against its OWN starting_equity/account -> two independent recorded rows, only the breaching bot alerts.
  </behavior>
  <action>Add the three driver cases to `tests/test_reconciliation.py`. Use an in-memory FakeAlpacaClient (returns fixed equity + positions-with-unrealized_pnl per bot), a FakeLogger/fake-db capturing record_reconciliation calls, monkeypatch `notifier.send_alert` to a call-counter, and caplog to assert WARNING-vs-INFO. Import the Plan-03 driver entrypoint from src.reconciliation (name it consistently with what Plan 03 will expose, e.g. reconcile_bot_live/reconcile). Assert per-bot isolation: bot with mismatched keys/starting_equity does not leak into the other's result. RED until Plan 03 driver exists.</action>
  <verify>
    <automated>python -m pytest tests/test_reconciliation.py -q 2>&1 | grep -E "error|Error|failed|passed"</automated>
  </verify>
  <acceptance_criteria>
    - test_persist_reconciliation asserts record_reconciliation called once with the helper dict.
    - test_breach_alerts asserts send_alert once on breach, zero within tolerance; WARNING vs INFO via caplog.
    - test_multi_bot_independent proves per-bot isolation (own starting_equity/account) and independent rows.
    - File collects cleanly; all 10 cases enumerable via `pytest --collect-only`; suite RED overall.
  </acceptance_criteria>
  <done>Full 10-case RED contract exists; drives Plans 02-03 to GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test process only | no external I/O; fakes replace Alpaca + DB + SES |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | test fakes drift from real signatures | mitigate | fakes mirror real get_account/get_positions/send_alert signatures from source |
| T-13-SC | Tampering | package installs | accept | no new packages (pytest already installed); no legitimacy audit needed |
</threat_model>

<verification>
- `python -m pytest tests/test_reconciliation.py --collect-only -q` lists all 10 named cases.
- Suite is RED (targets unimplemented) — expected until Plans 02/03.
- `python -m pytest tests/ -q` still green on the pre-existing 310 (new file's failures isolated to it).
</verification>

<success_criteria>
tests/test_reconciliation.py contains all 10 VALIDATION cases with hand-computed goldens and the
three-state sum guard (case 6), collectable and RED, ready to drive implementation.
</success_criteria>

<output>
Create `.planning/phases/13-alpaca-reconciliation-check/13-01-SUMMARY.md` when done.
</output>
