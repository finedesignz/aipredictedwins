---
phase: 13-alpaca-reconciliation-check
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - src/reconciliation.py
  - src/db.py
  - dashboard/api/migrations/017_reconciliation.sql
  - src/db_schema.sql
autonomous: true
requirements: [PNL-03]

must_haves:
  truths:
    - "src.reconciliation.reconcile_bot computes alpaca_realized_pnl = (equity - starting_equity) - unrealized_pnl, delta = trade_log_pnl - alpaca_realized_pnl, within_tolerance = abs(delta) <= tolerance, returning the 5-key dict"
    - "src.db.get_realized_pnl sums alpaca_trades.pnl over status IN ('closed','stopped','target_hit') for a bot, NULL-guarded, excluding canceled/rejected/expired"
    - "src.db.get_starting_equity reads starting_equity from the bots row (no hardcoded 100000) and record_reconciliation upserts one row per bot"
    - "reconciliation table created by additive/idempotent migration 017 and mirrored in src/db_schema.sql with NO bot_id CHECK constraint"
    - "tests/test_reconciliation.py pure cases (1-6,10) turn GREEN"
  artifacts:
    - path: "src/reconciliation.py"
      provides: "pure reconcile_bot(trade_log_pnl, equity, starting_equity, unrealized_pnl, tolerance) helper"
      contains: "def reconcile_bot"
    - path: "src/db.py"
      provides: "get_realized_pnl, get_starting_equity, record_reconciliation"
      contains: "def get_realized_pnl"
    - path: "dashboard/api/migrations/017_reconciliation.sql"
      provides: "CREATE TABLE IF NOT EXISTS reconciliation (single row per bot, bot_id PK)"
      contains: "reconciliation"
    - path: "src/db_schema.sql"
      provides: "reconciliation table mirror, no bot_id CHECK"
      contains: "CREATE TABLE IF NOT EXISTS reconciliation"
  key_links:
    - from: "src/db.py::get_realized_pnl"
      to: "alpaca_trades.pnl"
      via: "SUM over three terminal statuses"
      pattern: "'closed','stopped','target_hit'"
    - from: "src/db.py::record_reconciliation"
      to: "reconciliation table"
      via: "INSERT ... ON CONFLICT (bot_id) DO UPDATE"
      pattern: "ON CONFLICT .bot_id."
    - from: "src/db.py::get_starting_equity"
      to: "bots.starting_equity"
      via: "SELECT starting_equity FROM bots WHERE bot_id=%s"
      pattern: "starting_equity FROM bots"
---

<objective>
Build the foundation that Plan 03's driver wires against: the pure `reconcile_bot` math helper, the
three per-bot db accessors/persist functions (`get_realized_pnl` three-state sum, `get_starting_equity`,
`record_reconciliation`), and the additive `reconciliation` table (migration 017 + schema mirror). This
turns the Plan 01 pure cases (1-6,10) GREEN.

Purpose: PNL-03 reconciliation math + persistence contract. The helper is cent-exact and API-free; the
db functions localize all per-bot SQL; migration 017 gives a durable per-bot flag consumed by Phase 19.
Output: src/reconciliation.py, get_realized_pnl/get_starting_equity/record_reconciliation in src/db.py,
migration 017 + db_schema.sql mirror.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-alpaca-reconciliation-check/13-CONTEXT.md
@.planning/phases/13-alpaca-reconciliation-check/13-RESEARCH.md
@CLAUDE.md

<interfaces>
From src/db.py get_alpaca_accuracy (~L168) — the canonical three-state sum to mirror:
  status IN ('closed','stopped','target_hit'); pnl NULL-guarded via `r["pnl"] or 0.0`.
From dashboard/api/routes/equity.py (~L129-132) — starting_equity read pattern:
  SELECT starting_equity FROM bots WHERE bot_id = %s ; fallback default 100000.0 if row missing.
From src/db.py — connection() context manager + dict_row cursors are the standard access pattern.

Migration mechanism (dashboard/api/migrations/run_migrations.py): globs *.sql lexicographically,
tracks applied in _migrations ledger, runs each in a txn — file MUST be idempotent (IF NOT EXISTS).
Mirror 016_realized_pnl_fees.sql header/style. Next free number = 017.

src/db_schema.sql: add the identical reconciliation CREATE TABLE IF NOT EXISTS after section 9
(signals, ~L192-205), before the INDEXES banner (~L207). The live bots table dropped its bot_id
CHECK (migration 009) to allow C/D — add NO bot_id CHECK on reconciliation.

reconcile_bot contract (from tests/test_reconciliation.py, Plan 01):
  reconcile_bot(trade_log_pnl, equity, starting_equity, unrealized_pnl, tolerance) -> dict
    alpaca_realized_pnl = (equity - starting_equity) - unrealized_pnl
    delta = trade_log_pnl - alpaca_realized_pnl
    within_tolerance = abs(delta) <= tolerance
    keys: {trade_log_pnl, alpaca_realized_pnl, delta, within_tolerance, tolerance}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/reconciliation.py pure reconcile_bot helper</name>
  <files>src/reconciliation.py</files>
  <read_first>tests/test_reconciliation.py (Plan 01 pure cases 1-5,10 — the RED contract); 13-RESEARCH.md Finding 3 (formula) + Pitfall 3 (sign)</read_first>
  <action>Create `src/reconciliation.py` with a single pure function `reconcile_bot(trade_log_pnl, equity, starting_equity, unrealized_pnl, tolerance)` returning a dict. Compute `alpaca_realized_pnl = (equity - starting_equity) - unrealized_pnl` (unrealized_pnl is the signed SUM of open-position unrealized P&L — subtract it literally; a losing open position has negative unrealized and increases derived realized). `delta = trade_log_pnl - alpaca_realized_pnl`. `within_tolerance = abs(delta) <= tolerance` (boundary inclusive). Return dict with keys exactly {trade_log_pnl, alpaca_realized_pnl, delta, within_tolerance, tolerance}. Pure stdlib, no I/O, no imports of db/alpaca, no fallbacks (guards live in the driver). Karpathy smallest-diff.</action>
  <verify>
    <automated>python -m pytest tests/test_reconciliation.py -q -k "reconcile" 2>&1 | grep -E "failed|passed"</automated>
  </verify>
  <acceptance_criteria>
    - reconcile_bot signature matches contract exactly; returns the 5-key dict.
    - Formula literal: (equity - starting_equity) - unrealized_pnl; abs(delta) <= tolerance inclusive.
    - Pure — no db/alpaca imports, no I/O.
    - Plan 01 pure cases (within/over/boundary/negative/derivation/guards + result_shape) GREEN.
  </acceptance_criteria>
  <done>Cent-exact, API-free reconcile_bot exists and turns the pure cases GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: db accessors — get_realized_pnl (three-state), get_starting_equity, record_reconciliation</name>
  <files>src/db.py</files>
  <read_first>src/db.py get_alpaca_accuracy (three-state status set + NULL guard to mirror); dashboard/api/routes/equity.py L129-132 (starting_equity read); 13-RESEARCH.md Findings 1,2,4</read_first>
  <action>Add three functions to `src/db.py` using the existing connection()/dict_row pattern. (1) `get_realized_pnl(bot_id) -> float`: SELECT pnl FROM alpaca_trades WHERE bot_id=%s AND status IN ('closed','stopped','target_hit'); return sum((r["pnl"] or 0.0) for r in rows). This is the Pitfall-1 correction — the three-state set, NOT 'closed' alone. (2) `get_starting_equity(bot_id) -> float`: SELECT starting_equity FROM bots WHERE bot_id=%s; return the value, fallback 100000.0 only if the row is missing (never hardcode as primary). (3) `record_reconciliation(bot_id, result) -> None`: INSERT INTO reconciliation (bot_id, checked_at, trade_log_pnl, alpaca_realized_pnl, delta, within_tolerance, tolerance) VALUES (%s, NOW(), %s, %s, %s, %s, %s) ON CONFLICT (bot_id) DO UPDATE SET all columns to EXCLUDED — single row per bot. Parameterized %s only. Additive; touch no existing function.</action>
  <verify>
    <automated>python -c "import ast,src.db as d; ast.parse(open('src/db.py').read()); assert hasattr(d,'get_realized_pnl') and hasattr(d,'get_starting_equity') and hasattr(d,'record_reconciliation'); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - get_realized_pnl sums over status IN ('closed','stopped','target_hit') with `pnl or 0.0` NULL guard.
    - get_starting_equity reads the bots row; 100000.0 only as missing-row fallback, never hardcoded primary.
    - record_reconciliation upserts one row per bot via ON CONFLICT (bot_id) DO UPDATE, parameterized.
    - Additive only — no existing db.py function modified; module imports/parses clean.
  </acceptance_criteria>
  <done>Three per-bot db functions exist matching the codebase's three-state + starting_equity conventions.</done>
</task>

<task type="auto">
  <name>Task 3: Migration 017 reconciliation table + db_schema.sql mirror</name>
  <files>dashboard/api/migrations/017_reconciliation.sql, src/db_schema.sql</files>
  <read_first>dashboard/api/migrations/016_realized_pnl_fees.sql (header/style); dashboard/api/migrations/run_migrations.py (idempotency requirement); src/db_schema.sql section 9 signals ~L192-207 + migration 009 note (no bot_id CHECK); 13-RESEARCH.md Finding 4</read_first>
  <action>Create `dashboard/api/migrations/017_reconciliation.sql` mirroring 016's header/comment style, tagged PNL-03. Single idempotent statement: CREATE TABLE IF NOT EXISTS reconciliation with columns bot_id TEXT PRIMARY KEY, checked_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), trade_log_pnl DOUBLE PRECISION NOT NULL, alpaca_realized_pnl DOUBLE PRECISION NOT NULL, delta DOUBLE PRECISION NOT NULL, within_tolerance BOOLEAN NOT NULL, tolerance DOUBLE PRECISION NOT NULL. bot_id PK gives latest-per-bot via UPSERT. NO bot_id CHECK constraint (live DB dropped it for C/D). No DROP/DELETE/backfill (CLAUDE.md rule 6; single-row-per-bot, history deferred to a later phase). In `src/db_schema.sql`, add the identical CREATE TABLE IF NOT EXISTS reconciliation block after section 9 (signals) and before the INDEXES banner, with a comment noting it mirrors migration 017.</action>
  <verify>
    <automated>grep -q "CREATE TABLE IF NOT EXISTS reconciliation" dashboard/api/migrations/017_reconciliation.sql && grep -q "ON CONFLICT\|PRIMARY KEY" dashboard/api/migrations/017_reconciliation.sql && grep -q "CREATE TABLE IF NOT EXISTS reconciliation" src/db_schema.sql && ! grep -qi "CHECK.*bot_id" dashboard/api/migrations/017_reconciliation.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - 017_reconciliation.sql: additive + idempotent (CREATE TABLE IF NOT EXISTS), bot_id PK, 7 columns typed per DDL, NO bot_id CHECK, no DROP/DELETE.
    - src/db_schema.sql contains the identical reconciliation block, placed after signals / before INDEXES.
    - Deploy-safe to apply on Coolify Postgres via run_migrations.py before code deploy.
  </acceptance_criteria>
  <done>reconciliation table added by idempotent migration 017 and mirrored in schema; record_reconciliation has a target.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| code → Postgres | reconciliation row + reads via parameterized %s |
| migration runner → schema | additive idempotent DDL applied before code |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02 | Tampering | record_reconciliation INSERT | mitigate | parameterized %s only (existing db.py pattern) |
| T-13-03 | Denial of Service | migration 017 re-run | mitigate | CREATE TABLE IF NOT EXISTS idempotent; _migrations ledger tracked |
| T-13-04 | Information Disclosure | get_realized_pnl wrong status set | mitigate | three-state set matches get_alpaca_accuracy; case 6 test gate |
| T-13-SC | Tampering | package installs | accept | no new packages (pure Python + psycopg already installed) |
</threat_model>

<verification>
- `python -m pytest tests/test_reconciliation.py -q -k "reconcile or three_states"` GREEN (pure cases 1-6,10).
- `python -m pytest tests/ -q` no regressions on the pre-existing suite.
- Migration 017 additive/idempotent; src/db_schema.sql mirror keeps fresh bootstraps consistent.
</verification>

<success_criteria>
Pure reconcile_bot GREEN for the math cases; three-state get_realized_pnl + get_starting_equity +
record_reconciliation added; reconciliation table created by migration 017 and mirrored — all additive,
zero regressions.
</success_criteria>

<output>
Create `.planning/phases/13-alpaca-reconciliation-check/13-02-SUMMARY.md` when done.
</output>
