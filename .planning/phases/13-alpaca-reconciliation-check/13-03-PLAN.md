---
phase: 13-alpaca-reconciliation-check
plan: 03
type: execute
wave: 3
depends_on: [13-02]
files_modified:
  - src/reconciliation.py
  - src/notifier.py
  - scripts/reconcile.py
autonomous: true
requirements: [PNL-03]

must_haves:
  truths:
    - "A per-bot driver in src/reconciliation.py assembles trade_log_pnl, equity, starting_equity, and summed unrealized_pnl for each enabled bot from its OWN Alpaca account, calls reconcile_bot, and persists via db.record_reconciliation"
    - "On breach (within_tolerance False) the driver logs WARNING and calls notifier.send_alert exactly once; within tolerance logs INFO and does not alert"
    - "RECONCILIATION_TOLERANCE_USD is read from env (default 25.0) as the tolerance"
    - "scripts/reconcile.py enumerates enabled bots and prints a per-bot delta + PASS/FAIL line, read-only against Alpaca, writing only the reconciliation row"
    - "tests/test_reconciliation.py driver cases (7-9) turn GREEN and the full 10-case suite is GREEN"
  artifacts:
    - path: "src/reconciliation.py"
      provides: "driver: per-bot input assembly + reconcile_bot + record_reconciliation + log/alert"
      contains: "RECONCILIATION_TOLERANCE_USD"
    - path: "src/notifier.py"
      provides: "alert_reconciliation_breach wrapper reusing send_alert"
      contains: "def alert_reconciliation_breach"
    - path: "scripts/reconcile.py"
      provides: "runnable per-bot reconcile entrypoint printing PASS/FAIL"
      contains: "reconcile"
  key_links:
    - from: "src/reconciliation.py driver"
      to: "src/db.py::record_reconciliation"
      via: "persist result per bot"
      pattern: "record_reconciliation"
    - from: "src/reconciliation.py driver"
      to: "src/notifier.py::send_alert"
      via: "alert on breach only"
      pattern: "send_alert|alert_reconciliation_breach"
    - from: "scripts/reconcile.py"
      to: "src/reconciliation.py driver"
      via: "per-bot loop over enabled bots"
      pattern: "reconcile"
---

<objective>
Wire the reconciliation driver and runnable entrypoint on top of Plan 02's foundation: a per-bot
driver in `src/reconciliation.py` that sources each bot's own Alpaca account, assembles the four
inputs, calls `reconcile_bot`, persists the result, and logs/alerts on breach; a reused notifier
breach wrapper; and `scripts/reconcile.py` printing per-bot PASS/FAIL. Turns Plan 01 driver cases
(7-9) GREEN — full 10-case suite GREEN.

Purpose: PNL-03 requires an operable per-bot reconciliation that persists the flag (Phase 19 consumes
it), warns/alerts on breach, and can be run on demand against the live paper accounts.
Output: driver + RECONCILIATION_TOLERANCE_USD in src/reconciliation.py, alert_reconciliation_breach
in src/notifier.py, scripts/reconcile.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-alpaca-reconciliation-check/13-CONTEXT.md
@.planning/phases/13-alpaca-reconciliation-check/13-RESEARCH.md
@CLAUDE.md

<interfaces>
From Plan 02 — src/reconciliation.py::reconcile_bot(trade_log_pnl, equity, starting_equity, unrealized_pnl, tolerance) -> dict.
From Plan 02 — src/db.py: get_realized_pnl(bot_id) -> float; get_starting_equity(bot_id) -> float;
  record_reconciliation(bot_id, result) -> None.
From src/alpaca_client.py: get_account() -> object with .equity (float, ~L129);
  get_positions() -> list, each with .unrealized_pnl (float, ~L152). Read-only calls.
From src/notifier.py (~L42): send_alert(subject: str, body: str) -> bool  (plain-text SES, never raises);
  existing alert_* helpers ~L64-128 are the wrapper pattern to follow.
From dashboard/api/routes/equity.py: enabled-bot enumeration `SELECT bot_id FROM bots WHERE enabled=TRUE
  ORDER BY bot_id`; per-bot key sourcing L185-186 uses ALPACA_API_KEY_{bot_id}/ALPACA_SECRET_KEY_{bot_id}.
Per-bot Alpaca key source (one-account-per-bot HARD RULE): build one AlpacaClient per bot from the
  bots-row keys (alpaca_api_key/alpaca_secret_key, migration 002) OR the ALPACA_API_KEY_{id} env suffix
  (equity.py path). NEVER bare ALPACA_API_KEY, NEVER one client reused across bots.
Tolerance: float(os.environ.get("RECONCILIATION_TOLERANCE_USD", "25.0")) — matches config._env style.
Test contract (Plan 01 cases 7-9): driver importable from src.reconciliation; monkeypatched send_alert
  call-counter; FakeAlpacaClient/FakeLogger; caplog WARNING vs INFO; per-bot isolation.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: notifier.alert_reconciliation_breach wrapper</name>
  <files>src/notifier.py</files>
  <read_first>src/notifier.py send_alert (~L42) + existing alert_* helpers (~L64-128) for the wrapper pattern</read_first>
  <action>Add `alert_reconciliation_breach(bot_id, delta, tolerance, trade_log_pnl, alpaca_realized_pnl)` to `src/notifier.py`, following the existing alert_* helper style. It formats a subject like "Reconciliation breach: Bot {bot_id}" and a plain-text body with the delta, tolerance, and both P&L figures, then calls the existing `send_alert(subject, body)` and returns its bool. Reuse send_alert only — no new SES/email client, no new channel. Never raises (send_alert already swallows). Additive; touch no existing function.</action>
  <verify>
    <automated>python -c "import ast,src.notifier as n; ast.parse(open('src/notifier.py').read()); assert hasattr(n,'alert_reconciliation_breach'); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - alert_reconciliation_breach exists, builds subject+body, delegates to send_alert, returns bool.
    - No new email channel; reuses send_alert; never raises.
    - Additive — existing notifier functions unchanged.
  </acceptance_criteria>
  <done>Named breach-alert wrapper reusing the existing SES channel exists.</done>
</task>

<task type="auto">
  <name>Task 2: Per-bot reconciliation driver in src/reconciliation.py</name>
  <files>src/reconciliation.py</files>
  <read_first>src/reconciliation.py (Plan 02 reconcile_bot); src/alpaca_client.py get_account/get_positions; dashboard/api/routes/equity.py L178+L185-186 (enabled-bot enum + per-bot key suffix); 13-RESEARCH.md Findings 3,5,6 + Pitfall 2</read_first>
  <action>Add a driver to `src/reconciliation.py` (module `log = logging.getLogger(__name__)`) exposing: (a) `reconcile_bot_live(bot_id, alpaca_client, tolerance=None)` — reads tolerance from `RECONCILIATION_TOLERANCE_USD` (default 25.0) when None, gathers trade_log_pnl=`db.get_realized_pnl(bot_id)`, starting_equity=`db.get_starting_equity(bot_id)`, equity=`alpaca_client.get_account().equity`, unrealized_pnl=sum(p.unrealized_pnl for p in `alpaca_client.get_positions()`) (guard empty list -> 0.0), calls `reconcile_bot(...)`, calls `db.record_reconciliation(bot_id, result)`, then if `not result["within_tolerance"]` logs WARNING + calls `notifier.alert_reconciliation_breach(...)` exactly once, else logs INFO (no alert); returns the result dict. (b) `reconcile()` — enumerates enabled bots (`SELECT bot_id FROM bots WHERE enabled=TRUE`), builds ONE AlpacaClient per bot from that bot's own keys (bots-row keys or ALPACA_API_KEY_{id}/ALPACA_SECRET_KEY_{id} suffix, paper env), calls reconcile_bot_live per bot, returns list of (bot_id, result). NEVER reuse one client across bots (one-account-per-bot). Read-only Alpaca; only writes the reconciliation row. Keep the pure reconcile_bot untouched.</action>
  <verify>
    <automated>python -m pytest tests/test_reconciliation.py -q 2>&1 | grep -E "failed|passed"</automated>
  </verify>
  <acceptance_criteria>
    - reconcile_bot_live assembles the 4 inputs, calls reconcile_bot, persists via record_reconciliation.
    - Breach -> WARNING + alert_reconciliation_breach once; within tolerance -> INFO, no alert.
    - Tolerance from RECONCILIATION_TOLERANCE_USD env, default 25.0.
    - reconcile() enumerates enabled bots and builds one client per bot from its own keys (never shared, never bare keys).
    - empty positions guarded to 0.0 unrealized; Plan 01 cases 7-9 GREEN.
  </acceptance_criteria>
  <done>Per-bot driver persists + logs/alerts correctly; driver cases GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: scripts/reconcile.py runnable entrypoint</name>
  <files>scripts/reconcile.py</files>
  <read_first>scripts/check_alpaca.py (per-bot loop + header/print convention); scripts/daily_audit.py; src/reconciliation.py reconcile() (Plan 03 Task 2); 13-RESEARCH.md Finding 6</read_first>
  <action>Create `scripts/reconcile.py` — module docstring "Reconcile trade-log realized P&L vs Alpaca realized P&L per bot (PNL-03)." Call `src.reconciliation.reconcile()` and for each (bot_id, result) print a human-readable line: bot, delta (formatted $), tolerance, and PASS (within_tolerance) / FAIL (breach). Follow the flat scripts/ convention (docstring-first, a main()/bare loop). Read-only against Alpaca; the only write is the reconciliation row (done inside the driver). Optionally exit non-zero if any bot breaches (cron/CI signal). Do not duplicate reconcile logic — thin wrapper over the driver.</action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('scripts/reconcile.py').read()); print('OK')" && python -m pytest tests/ -q 2>&1 | grep -E "passed"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/reconcile.py parses, calls reconcile(), prints per-bot delta + PASS/FAIL line.
    - No reconcile logic duplicated — delegates to src/reconciliation.reconcile().
    - Read-only vs Alpaca; only DB write is the reconciliation row.
    - Full suite `python -m pytest tests/ -q` GREEN (all 10 reconciliation cases + no regressions).
  </acceptance_criteria>
  <done>`python scripts/reconcile.py` prints per-bot PASS/FAIL against each bot's own paper account.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| script → Alpaca (per bot) | read-only get_account/get_positions with per-bot keys |
| driver → Postgres | reconciliation row write via db.record_reconciliation |
| driver → SES | breach alert via reused send_alert |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | Spoofing | wrong per-bot Alpaca account | mitigate | one AlpacaClient per bot from its own keys; never bare/shared keys (one-account-per-bot rule) |
| T-13-06 | Information Disclosure | breach alert body content | accept | plain-text P&L figures via existing SES channel; no secrets in body |
| T-13-07 | Denial of Service | Alpaca API error mid-loop | mitigate | read-only calls; send_alert never raises; per-bot loop isolates one bot's failure |
| T-13-SC | Tampering | package installs | accept | no new packages (pure Python + alpaca-py/psycopg already installed) |
</threat_model>

<verification>
- `python -m pytest tests/test_reconciliation.py -q` all 10 cases GREEN.
- `python -m pytest tests/ -q` no regressions.
- `python scripts/reconcile.py` prints one PASS/FAIL line per enabled bot (against each bot's own paper account), writing only the reconciliation row.
</verification>

<success_criteria>
Per-bot driver assembles inputs from each bot's own account, reconciles, persists, and warns/alerts on
breach (INFO within tolerance); RECONCILIATION_TOLERANCE_USD drives the threshold; scripts/reconcile.py
prints per-bot PASS/FAIL; full 10-case suite GREEN.
</success_criteria>

<output>
Create `.planning/phases/13-alpaca-reconciliation-check/13-03-SUMMARY.md` when done.
</output>
