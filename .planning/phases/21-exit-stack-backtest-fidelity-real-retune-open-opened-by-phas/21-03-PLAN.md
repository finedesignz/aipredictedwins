---
phase: 21-exit-stack-backtest-fidelity-real-retune
plan: 03
type: execute
wave: 3
depends_on: [21-02]
files_modified:
  - scripts/sweep_backtest.py
  - tests/backtester/test_sweep.py
  - .planning/phases/21-exit-stack-backtest-fidelity-real-retune-open-opened-by-phas/21-BACKTEST.md
  - .planning/milestones/v1.1-REQUIREMENTS.md
autonomous: false
requirements: [TUNE-01]
must_haves:
  truths:
    - "The sweep grids over BOTH entry knobs (min_confluence, kelly_fraction<=0.25) AND exit knobs against the real 4-rung engine"
    - "Each cell is a fresh subprocess with BAR_CACHE_DIR baked (reproducibility trap avoided)"
    - "The candidate is chosen on TRAIN only; a single HOLDOUT shot runs under a NEW 21-HOLDOUT.lock"
    - "passes_bar conjunction (trades>=30 first, win_rate>=40%, dd improved & <20%, return>=baseline) is unchanged"
    - "TUNE-01 is closed honestly: ticked only if a validated non-degenerate cell beats the criterion on the real model, else recorded as an honest negative with reason"
  artifacts:
    - path: "scripts/sweep_backtest.py"
      provides: "Entry x exit grid driver + 21-HOLDOUT.lock discipline"
      contains: "21-HOLDOUT.lock"
    - path: ".planning/phases/21-exit-stack-backtest-fidelity-real-retune-open-opened-by-phas/21-BACKTEST.md"
      provides: "TRAIN grid table, chosen candidate, holdout result, TUNE-01 verdict"
    - path: "tests/backtester/test_sweep.py"
      provides: "Grid enumeration + holdout-lock-refuses-second-run + Kelly-ceiling tests"
  key_links:
    - from: "scripts/sweep_backtest.py"
      to: "src.backtester CLI exit-knob args"
      via: "subprocess argv includes --hard-stop-pct/--atr-mult-* per cell"
      pattern: "atr-mult"
---

<objective>
Extend the Phase-18 sweep driver (`scripts/sweep_backtest.py`) to grid over entry knobs
(min_confluence, kelly_fraction) AND the new exit knobs against the real 4-rung engine (Plans
01/02), mint a NEW `21-HOLDOUT.lock` (Phase 18's lock models a different exit model — reusing it
would block or conflate budgets), run the TRAIN sweep, select a candidate on TRAIN, take the single
HOLDOUT shot, and close TUNE-01 honestly per D-05.

Purpose: TUNE-01 can only be legitimately closed against the true exit model. This plan produces
the falsifiable result the Phase-18 flat barrier could not.
Output: extended driver, `21-BACKTEST.md` with grid + verdict, TUNE-01 status updated.

LOCKED before reading any grid (state tiebreaks up front, Research §Sweep Harness):
- Exit-knob grid resolution is Claude's discretion but MUST keep the run tractable (fresh
  subprocess per cell) — recommend a SMALL exit grid (e.g. hard_stop_pct in {-0.08, -0.06},
  atr_mult_stop in {2.0, 3.0}, atr_mult_trail in {1.5, 2.5}, atr_period fixed 14, max_hold_hours
  fixed 168) crossed with entry knobs; keep total TRAIN cells reasonable.
- Tiebreak order (extend Phase 18): prefer LOWER kelly, then HIGHER min_confluence, then WIDER
  hard_stop (less churn), then FEWER trades — toward less risk. State this in 21-BACKTEST.md BEFORE
  reading results.
- ADA/USD has NO train bars (starts 2026-02-13) — carry the caveat forward.
- Kelly ceiling <=0.25 is HARD — the grid may only lower Kelly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-exit-stack-backtest-fidelity-real-retune-open-opened-by-phas/21-RESEARCH.md
@.planning/phases/21-exit-stack-backtest-fidelity-real-retune-open-opened-by-phas/21-02-SUMMARY.md
@.planning/phases/18-profitable-retune/18-BACKTEST.md

<interfaces>
Sweep driver — scripts/sweep_backtest.py:
  run_cell(window, min_confluence, kelly, quarantine) -> dict (fresh subprocess, BAR_CACHE_DIR env)
  passes_bar(summary, baseline) -> (bool, reason)  # trades>=30 first; DO NOT weaken
  HOLDOUT_LOCK = PHASE_DIR/"18-HOLDOUT.lock" ; --holdout requires --candidate ; refuses second run
  metric scrape regex _METRIC on child stdout "  key value" lines (cli.py:127-129)
Backtester CLI new exit args (Plan 02): --hard-stop-pct --atr-mult-stop --atr-mult-trail --atr-period --max-hold-hours
Windows: TRAIN 2025-10-01..2026-01-31 ; HOLDOUT 2026-02-01..2026-04-30
TUNE-01 text — .planning/milestones/v1.1-REQUIREMENTS.md (PARTIAL — win rate >=40%, halted drawdown)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend sweep driver to entry x exit grid + 21-HOLDOUT.lock</name>
  <files>scripts/sweep_backtest.py, tests/backtester/test_sweep.py</files>
  <read_first>
    - scripts/sweep_backtest.py (full — run_cell, passes_bar, holdout lock logic :147-173)
    - src/backtester/cli.py (the new exit-knob args from Plan 02)
    - .planning/phases/18-profitable-retune/18-BACKTEST.md (acceptance bar, tiebreak conventions)
  </read_first>
  <action>Extend `run_cell` to accept exit knobs (hard_stop_pct, atr_mult_stop, atr_mult_trail,
  atr_period, max_hold_hours) and append them to the child argv as
  `--hard-stop-pct/--atr-mult-stop/--atr-mult-trail/--atr-period/--max-hold-hours`. Add exit-knob
  tuples at module level (small grid per objective). Nest the exit loops inside the existing
  entry loops so each TRAIN cell is a fresh subprocess with BAR_CACHE_DIR+AIPW_DB_READONLY env
  (do NOT convert to in-process — repro trap at :14-19). Point HOLDOUT_LOCK at the phase-21
  directory / "21-HOLDOUT.lock" (NEW file; leave 18's untouched). Keep passes_bar's four-way
  conjunction and trades>=30-first ordering UNCHANGED. Extend `row()` to print the exit knobs.
  Keep the --holdout-requires---candidate and second-run-refusal guards; extend --candidate parsing
  to carry the exit knobs. Write test_sweep.py: assert the grid enumerates entry x exit cells,
  passes_bar rejects trades<30 first, the Kelly ceiling still errors, and a pre-existing
  21-HOLDOUT.lock makes --holdout parser.error.</action>
  <verify>
    <automated>python -m pytest tests/backtester/test_sweep.py -q</automated>
  </verify>
  <acceptance_criteria>`grep -n "21-HOLDOUT.lock" scripts/sweep_backtest.py` hits; `grep -n
  "atr-mult" scripts/sweep_backtest.py` hits; passes_bar body unchanged (trades>=30 checked first);
  test_sweep.py green. No write path / no orders introduced (read-only preserved).</acceptance_criteria>
  <done>Driver grids entry x exit, mints its own lock, keeps holdout discipline; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Run TRAIN sweep, select candidate, take single HOLDOUT shot, write 21-BACKTEST.md</name>
  <files>.planning/phases/21-exit-stack-backtest-fidelity-real-retune-open-opened-by-phas/21-BACKTEST.md</files>
  <read_first>
    - scripts/sweep_backtest.py (extended driver)
    - .planning/phases/18-profitable-retune/18-BACKTEST.md (format to mirror)
    - .planning/phases/21-exit-stack-backtest-fidelity-real-retune-open-opened-by-phas/21-RESEARCH.md (windows, ADA caveat, tiebreak)
  </read_first>
  <action>Ensure cached bars exist (`ls data/backtest_bars/*_1Hour.json`; re-fetch via
  scripts/fetch_backtest_bars.py only if missing). Run the TRAIN sweep
  (`python scripts/sweep_backtest.py` with train defaults) capturing the full grid table. State the
  tiebreak order and the exit-grid resolution in 21-BACKTEST.md BEFORE the results table. Select ONE
  candidate on TRAIN only (best passing cell by acceptance bar + tiebreak; if none passes, pick the
  least-bad per D-05 and mark expectation). Then run the SINGLE holdout shot:
  `python scripts/sweep_backtest.py --holdout --candidate "<mc,kelly,quarantine,exit-knobs>"` which
  writes 21-HOLDOUT.lock. Record: the FIDELITY-GAP-closed note (4-rung vs flat), the TRAIN grid,
  chosen candidate + rationale, the holdout BASELINE vs CANDIDATE row, and the pass/fail verdict.
  Do NOT run holdout more than once (the lock enforces this).</action>
  <verify>
    <automated>test -f .planning/phases/21-exit-stack-backtest-fidelity-real-retune-open-opened-by-phas/21-HOLDOUT.lock && grep -q "candidate=" .planning/phases/21-exit-stack-backtest-fidelity-real-retune-open-opened-by-phas/21-HOLDOUT.lock && echo LOCK-OK</automated>
  </verify>
  <acceptance_criteria>21-BACKTEST.md exists with: tiebreak stated before results, full TRAIN grid
  table, a single named candidate chosen on TRAIN, one holdout BASELINE-vs-CANDIDATE row, and an
  explicit pass/fail verdict. 21-HOLDOUT.lock exists with candidate= and git_rev=. 18-HOLDOUT.lock
  is unmodified.</acceptance_criteria>
  <done>TRAIN sweep run, candidate chosen on train, single holdout shot recorded, 21-BACKTEST.md written.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: TUNE-01 closure verdict (honest pass or documented negative)</name>
  <what-built>The real-exit-model sweep (Plans 01-03) and its holdout result in 21-BACKTEST.md.</what-built>
  <read_first>
    - .planning/phases/21-exit-stack-backtest-fidelity-real-retune-open-opened-by-phas/21-BACKTEST.md
    - .planning/milestones/v1.1-REQUIREMENTS.md (TUNE-01 criterion)
  </read_first>
  <action>Update the TUNE-01 entry in `.planning/milestones/v1.1-REQUIREMENTS.md`: tick it CLOSED
  only if the holdout candidate is a validated, non-degenerate result that beats the criterion
  (win rate >=40% and halted/improved drawdown) on the REAL 4-rung exit model. If the sweep still
  cannot beat the criterion, record the honest NEGATIVE with the reason (D-05) — do NOT force-tick.
  Cross-link 21-BACKTEST.md.</action>
  <how-to-verify>
    1. Open 21-BACKTEST.md; confirm the candidate was chosen on TRAIN and the holdout row is a
       single shot under 21-HOLDOUT.lock.
    2. Confirm the verdict is derived from passes_bar against the real model (not the flat barrier).
    3. Confirm TUNE-01 in v1.1-REQUIREMENTS.md reflects the honest outcome (closed on a real pass,
       or PARTIAL/negative with reason) — no forced green.
  </how-to-verify>
  <resume-signal>Type "approved" to accept the TUNE-01 verdict, or describe corrections.</resume-signal>
</task>

</tasks>

<verification>
- `python -m pytest tests/backtester/test_sweep.py -q` green
- 21-HOLDOUT.lock created (single shot); 18-HOLDOUT.lock untouched
- 21-BACKTEST.md documents grid + candidate + holdout verdict against the real model
- TUNE-01 in v1.1-REQUIREMENTS.md updated honestly per D-05
</verification>

<success_criteria>
The entry x exit sweep ran against the real 4-rung engine under fresh-subprocess reproducibility
and single-shot holdout discipline, and TUNE-01 is closed on the true model — pass or documented
negative, never forced.
</success_criteria>

<output>
Create `.planning/phases/21-exit-stack-backtest-fidelity-real-retune-open-opened-by-phas/21-03-SUMMARY.md` when done
</output>
