---
phase: 21-exit-stack-backtest-fidelity-real-retune
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/exit_ladder.py
  - src/alpaca_orchestrator.py
  - tests/backtester/test_exit_parity.py
  - tests/backtester/test_exit_model.py
autonomous: true
requirements: [TUNE-01]
must_haves:
  truths:
    - "Live monitor and backtester call ONE shared exit-decision function — no forked ladder"
    - "The shared function reproduces the current live inline ladder decision on every case"
    - "The 4 deterministic rungs fire in the exact live precedence: hard_stop -> max_hold -> trailing_stop -> atr_stop"
    - "The function is side-aware (long and short) and pure (no broker, no DB, no LLM)"
  artifacts:
    - path: "src/exit_ladder.py"
      provides: "Shared pure evaluate_exit helper (single source of truth, D-02)"
      exports: ["evaluate_exit"]
      min_lines: 30
    - path: "tests/backtester/test_exit_parity.py"
      provides: "Drift-guard parity table pinning evaluate_exit to the live inline decisions"
    - path: "tests/backtester/test_exit_model.py"
      provides: "Per-rung precedence + side-awareness unit tests"
  key_links:
    - from: "src/alpaca_orchestrator.py"
      to: "src/exit_ladder.evaluate_exit"
      via: "import + call in the position-monitor exit loop"
      pattern: "evaluate_exit\\("
---

<objective>
Extract the live deterministic 4-rung exit ladder from `src/alpaca_orchestrator.py:316-345`
into ONE shared, pure, side-aware function `evaluate_exit` in a new module `src/exit_ladder.py`,
then re-wire the live monitor to call it. This is the D-02 single-source-of-truth foundation:
Plan 02 (backtester) will call the SAME function, guaranteeing zero live/backtest drift.

Purpose: Without a shared decision function the backtester would fork the ladder and silently
diverge on the next live tweak (Research Pitfall 2). Extract-once + parity-test is the low-risk path.
Output: `src/exit_ladder.py` (pure helper), re-wired live monitor, parity + per-rung tests.

NOTE — Research CORRECTS CONTEXT D-01: the live monitor has NO active soft-stop/soft-take rung
(`ExitAdvisor.should_exit` is unwired; `SOFT_STOP_PCT`/`SOFT_TAKE_PROFIT_PCT` imported but unused).
Model the 4 DETERMINISTIC rungs ONLY. Do NOT add a soft rung — it would inject LLM non-determinism
and reduce fidelity. Preserve the dormant `tightened_stop` rung behavior only if trivially reused.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-exit-stack-backtest-fidelity-real-retune-open-opened-by-phas/21-CONTEXT.md
@.planning/phases/21-exit-stack-backtest-fidelity-real-retune-open-opened-by-phas/21-RESEARCH.md

<interfaces>
Live inline ladder to extract — src/alpaca_orchestrator.py:316-345 (first-match wins):
  1. hard_stop:    pnl_pct <= profile.hard_stop_pct
  2. max_hold:     profile.max_hold_hours is not None and hours_held > profile.max_hold_hours
  3. trailing_stop: atr > 0 and TrailingStop.update_atr(trade_id, side, entry, current, atr, profile.atr_mult_trail)
  4. atr_stop:     atr > 0; SHORT: current >= entry + atr_mult_stop*atr; LONG: current <= entry - atr_mult_stop*atr
  side/pnl math (:283-290): SHORT pnl_pct=(entry-current)/entry; LONG pnl_pct=(current-entry)/entry

TrailingStop.update_atr — src/exit_advisor.py:234-278 (reuse; do NOT reimplement):
  def update_atr(self, trade_id, side, entry_price, current_price, atr, mult_trail) -> str | None
  side-aware, arms only in profit, ratchets peak/trough, returns "trailing_stop" or None.

StrategyProfile fields — src/strategy_profile.py:23-68:
  hard_stop_pct, max_hold_hours (float|None), atr_mult_trail, atr_mult_stop, atr_period
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write parity + per-rung tests (RED)</name>
  <files>tests/backtester/test_exit_parity.py, tests/backtester/test_exit_model.py</files>
  <read_first>
    - src/alpaca_orchestrator.py (lines 283-345 — the live inline ladder to pin)
    - src/exit_advisor.py (lines 234-284 — TrailingStop.update_atr semantics)
    - src/strategy_profile.py (SWING/DAYTRADE knob values)
    - tests/backtester/ (existing fixture/test conventions)
  </read_first>
  <behavior>
    - test_parity: a table of cases (side x pnl x hours_held x atr x price) where the EXPECTED
      result is computed by re-stating the live inline ladder logic; assert evaluate_exit returns
      the same string ("hard_stop"|"max_hold"|"trailing_stop"|"atr_stop"|None) for each.
    - test_hard_stop_precedence: a bar that satisfies BOTH hard_stop AND atr_stop returns "hard_stop".
    - test_max_hold: fires only when max_hold_hours is not None and hours_held > threshold; SWING's
      168.0 never fires below 168; max_hold_hours=None never time-closes.
    - test_atr_trail: arms only once in profit, ratchets, triggers "trailing_stop" on pullback.
    - test_atr_fixed: side-aware level — LONG at entry-atr_mult_stop*atr, SHORT at entry+atr_mult_stop*atr.
    - test_side_aware_short: SHORT pnl_pct sign and atr_stop direction are mirrored.
    - atr<=0 case: rungs 3 and 4 both skip.
  </behavior>
  <action>Create both test files using pytest. Import evaluate_exit from src.exit_ladder and
  TrailingStop from src.exit_advisor. Build StrategyProfile instances from src.strategy_profile
  (SWING) and dataclasses.replace for knob variants. Tests MUST fail initially (module absent).
  No fenced code beyond test bodies; assert exact return strings.</action>
  <verify>
    <automated>python -m pytest tests/backtester/test_exit_parity.py tests/backtester/test_exit_model.py -q 2>&1 | grep -Eq "ModuleNotFoundError|ImportError|error" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>Both test files exist; running them fails with an import/collection error
  (evaluate_exit not yet implemented). At least 6 distinct rung/precedence/side cases present.</acceptance_criteria>
  <done>Tests written and RED (fail because src/exit_ladder.py does not yet exist).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract evaluate_exit into src/exit_ladder.py (GREEN)</name>
  <files>src/exit_ladder.py</files>
  <read_first>
    - src/alpaca_orchestrator.py (lines 283-345)
    - src/exit_advisor.py (TrailingStop)
    - tests/backtester/test_exit_parity.py (contract just written)
  </read_first>
  <action>Create `evaluate_exit(profile, side, entry_price, current_price, hours_held, atr,
  trailing, trade_id) -> str | None`. Compute side-aware pnl_pct exactly as :283-290. Evaluate the
  four rungs first-match-wins in live order: (1) hard_stop when pnl_pct <= profile.hard_stop_pct;
  (2) max_hold when profile.max_hold_hours is not None and hours_held > profile.max_hold_hours;
  (3) trailing_stop when atr > 0 and trailing.update_atr(trade_id, side, entry_price, current_price,
  atr, profile.atr_mult_trail) is truthy; (4) atr_stop when atr > 0 and the side-aware level is
  crossed (SHORT current >= entry+atr_mult_stop*atr; LONG current <= entry-atr_mult_stop*atr).
  Return the reason string or None. Pure function: no broker, no DB, no LLM, no logging side effects.
  Do NOT add a soft rung (dead path per Research Pitfall 1). Keep it importable with no heavy deps.</action>
  <verify>
    <automated>python -m pytest tests/backtester/test_exit_parity.py tests/backtester/test_exit_model.py -q</automated>
  </verify>
  <acceptance_criteria>All Task-1 tests pass. `grep -n "def evaluate_exit" src/exit_ladder.py`
  returns a hit. Module imports without pulling ClaudeLLM/broker code.</acceptance_criteria>
  <done>evaluate_exit implemented; parity + per-rung tests GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Re-wire live monitor to call evaluate_exit</name>
  <files>src/alpaca_orchestrator.py</files>
  <read_first>
    - src/alpaca_orchestrator.py (lines 283-401 — the monitor loop and close path)
    - src/exit_ladder.py (the new helper)
  </read_first>
  <action>Replace the inline rung logic at :316-345 with a single call to
  `evaluate_exit(self.profile, side, entry_price, current_price, hours_held, atr, self._trailing,
  trade_id)` assigning its result to `threshold`. Keep the surrounding pnl/atr/hours_held setup,
  the dormant tightened_stop check (:343-345), the logging, and the broker close path (:347-401)
  UNCHANGED — this phase does not alter live exit behavior, only its source location. Preserve the
  `if not threshold: continue` guard.</action>
  <verify>
    <automated>python -m pytest tests/ -q -k "exit or monitor or orchestrator" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>`grep -n "evaluate_exit(" src/alpaca_orchestrator.py` returns a hit; the old
  inline `if pnl_pct <= self.profile.hard_stop_pct` block is gone. Full suite has no new failures
  vs baseline (541 passed / 29 skipped).</acceptance_criteria>
  <done>Live monitor calls the shared helper; behavior byte-for-byte identical; suite green.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/backtester/test_exit_parity.py tests/backtester/test_exit_model.py -q` green
- `python -m pytest -q` no regression vs baseline (541 passed / 29 skipped)
- Live monitor and (future) backtester both import evaluate_exit — single source of truth established
</verification>

<success_criteria>
evaluate_exit exists as a pure side-aware function, is proven parity-equal to the live inline
ladder, and the live monitor now delegates to it. Ready for Plan 02 to consume the same function.
</success_criteria>

<output>
Create `.planning/phases/21-exit-stack-backtest-fidelity-real-retune-open-opened-by-phas/21-01-SUMMARY.md` when done
</output>
