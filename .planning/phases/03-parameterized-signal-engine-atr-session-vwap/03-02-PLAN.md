---
phase: 03-parameterized-signal-engine-atr-session-vwap
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/alpaca_orchestrator.py
  - src/bot_thread.py
  - tests/test_technical_signals.py
autonomous: true
requirements: [SIGNAL-01]
must_haves:
  truths:
    - "All three scan_assets call-sites pass the active profile (orchestrator x2, bot_thread x1)"
    - "Swing call-site behavior is identical (SWING values match the prior literal timeframe/bar_count)"
    - "Full pytest suite is green — the 6 pre-existing stale-threshold tests are remediated"
  artifacts:
    - path: "src/bot_thread.py"
      provides: "scan_assets call threading a resolved profile"
      contains: "profile="
    - path: "src/alpaca_orchestrator.py"
      provides: "scan_assets call-sites threading PROFILE"
      contains: "profile=PROFILE"
  key_links:
    - from: "alpaca_orchestrator.py scan_assets call-sites"
      to: "module-level PROFILE"
      via: "profile=PROFILE arg"
      pattern: "scan_assets\\(.*profile=PROFILE"
    - from: "bot_thread.py scan_assets call-site"
      to: "resolved StrategyProfile"
      via: "profile= arg"
      pattern: "scan_assets\\(.*profile="
---

<objective>
Thread the active profile through all three `scan_assets` call-sites and remediate the 6
pre-existing stale-threshold test failures so the full suite is green.

Implements SIGNAL-01 wiring (D-02). Depends on Plan 01 (uses the new `analyze`/`scan_assets`
signatures and `Signal.atr_value`).

Purpose: The live BotThread path and the reference orchestrator both feed the profile in;
SWING parity keeps bots A/B unchanged. Lagging tests catch up to already-shipped thresholds.
Output: Three updated call-sites + 6 remediated tests; full `pytest -q` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-parameterized-signal-engine-atr-session-vwap/03-RESEARCH.md
@.planning/phases/03-parameterized-signal-engine-atr-session-vwap/03-CONTEXT.md
@src/bot_config.py

<interfaces>
Call-sites to update (verified):
  src/alpaca_orchestrator.py:734  scan_assets(alpaca, universe, timeframe="1Hour", bar_count=50, fetch_4h=True)
  src/alpaca_orchestrator.py:1151 scan_assets(alpaca, TOP_CRYPTO_TICKERS, timeframe="1Hour", bar_count=50)
  src/bot_thread.py:383           scan_assets(alpaca, scan_universe, timeframe="1Hour", bar_count=50, fetch_4h=not is_stock)

Profile resolution:
  - alpaca_orchestrator.py already has module-level `PROFILE` (from BOT_PROFILE env, line 56-61). Pass `profile=PROFILE`.
  - bot_thread.py has NO profile yet. BotConfig (src/bot_config.py) has no profile field. Resolve a
    profile from BOT_PROFILE env at the call-site: `from src.strategy_profile import PROFILES` then
    `PROFILES.get(os.environ.get("BOT_PROFILE","swing").lower(), SWING)`. Default swing → parity.

Pre-existing failure triage (RESEARCH §Validation — all fix-the-test, NO production threshold changes):
  1 TestTrailingStop::test_triggers_on_pullback — TRAIL_DISTANCE_PCT now 0.05 → drive price below 110*0.95=104.5
  2 TestThresholdChecks::test_hard_stop — HARD_STOP_PCT now -0.15 → use price past -15%
  3 TestThresholdChecks::test_soft_stop — SOFT_STOP_PCT now -0.08 → use price past -8%
  4 TestThresholdChecks::test_soft_take_profit — SOFT_TAKE_PROFIT_PCT now 0.15 → use price past +15%
  5 TestKellyTechnical::test_confluence_3 — MIN_CONFLUENCE now 4 → use confluence 4 (or assert side=="none" at 3)
  6 TestRSIHardBlock::test_overbought_rsi_returns_none — RSI hard-block removed → assert RSI ceiling
    suppresses long score (analyze returns a Signal), not None
  Also: test_normal_range comment references old -3% soft stop — update comment only (no logic change).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread profile through all three scan_assets call-sites (D-02)</name>
  <files>src/alpaca_orchestrator.py, src/bot_thread.py</files>
  <action>
    In `src/alpaca_orchestrator.py` add `profile=PROFILE` to both `scan_assets(...)` calls (lines
    ~734 and ~1151) using the existing module-level `PROFILE`. In `src/bot_thread.py`: resolve the
    active profile at/near the call-site (line ~383) — `from src.strategy_profile import PROFILES, SWING`
    and `_profile = PROFILES.get(os.environ.get("BOT_PROFILE", "swing").lower(), SWING)` — and pass
    `profile=_profile` to `scan_assets`. Keep the existing positional args; do NOT drop
    `timeframe="1Hour", bar_count=50` (SWING values match them, so swing behavior is identical per
    D-02). Minimal diff: add the `profile=` kwarg only. Per D-02 "keep swing call-site behavior
    identical" — verify no other scan_assets args change.
  </action>
  <verify>
    <automated>cd C:/Users/artic/GitHub/aipredictedwins; python -c "import src.alpaca_orchestrator, src.bot_thread" ; grep -nE "scan_assets\(.*profile=" src/alpaca_orchestrator.py src/bot_thread.py</automated>
  </verify>
  <done>All 3 call-sites pass profile=; modules import cleanly; swing args unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Remediate the 6 stale-threshold test failures (test-file-only)</name>
  <files>tests/test_technical_signals.py</files>
  <action>
    Fix the 6 stale tests per the triage table in `<interfaces>` — TEST EXPECTATIONS ONLY, no
    production threshold edits (D-05; thresholds already shipped in prior phases). #1-5 update the
    input prices/confluence to the current thresholds (trail 5%, hard -15%, soft -8%, soft-TP +15%,
    MIN_CONFLUENCE 4). #6 `test_overbought_rsi_returns_none` — rewrite to assert that an overbought
    RSI (> RSI_ENTRY_CEILING) suppresses the long RSI score point while `analyze()` still returns a
    Signal (RSI is now a soft ceiling, not a hard block). Update the `test_normal_range` inline
    comment that references the old -3% soft stop (comment only). After edits run the full suite.
    If any of these 6 reveals a genuine code bug (not stale expectation), STOP and document — but
    RESEARCH verified all 6 are fix-the-test.
  </action>
  <verify>
    <automated>cd C:/Users/artic/GitHub/aipredictedwins; python -m pytest -q</automated>
  </verify>
  <done>Full `pytest -q` green (0 failures); no production threshold constants changed.</done>
</task>

</tasks>

<verification>
- `python -m pytest -q` — entire suite green.
- `grep -nE "scan_assets\(.*profile=" src/alpaca_orchestrator.py src/bot_thread.py` shows 3 matches.
- `git diff src/exit_advisor.py src/alpaca_orchestrator.py` shows no threshold-constant changes (only the scan_assets profile kwarg in the orchestrator).
</verification>

<success_criteria>
- All three scan_assets call-sites pass the active profile; swing behavior identical.
- The 6 pre-existing stale tests are green via test-file edits only.
- Full `pytest -q` passes.
</success_criteria>

<output>
Create `.planning/phases/03-parameterized-signal-engine-atr-session-vwap/03-02-SUMMARY.md` when done.
</output>
