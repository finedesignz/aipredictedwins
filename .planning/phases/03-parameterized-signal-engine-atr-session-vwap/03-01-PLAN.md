---
phase: 03-parameterized-signal-engine-atr-session-vwap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/technical_signals.py
  - tests/test_technical_signals.py
autonomous: true
requirements: [SIGNAL-01, SIGNAL-02, SIGNAL-03]
must_haves:
  truths:
    - "analyze() sources EMA/RSI/ADX periods from the profile; no hardcoded 9/21/14 remain in analyze()"
    - "Signal carries atr_value computed from bar data via _atr (Wilder true-range)"
    - "Daytrade profile uses UTC-day session-anchored VWAP; swing VWAP semantics unchanged"
    - "SWING-preset analyze() output is byte-for-byte identical to pre-change (parity)"
  artifacts:
    - path: "src/technical_signals.py"
      provides: "Parameterized analyze(), scan_assets() profile param, _atr(), session-anchored _vwap_bullish()"
      contains: "def _atr"
    - path: "tests/test_technical_signals.py"
      provides: "TestATR, TestProfilePeriods, TestSessionVWAP, swing-parity regression"
  key_links:
    - from: "analyze()"
      to: "profile.ema_fast/ema_slow/rsi_period/adx_period/atr_period"
      via: "indicator period sourcing"
      pattern: "profile\\.(ema_fast|ema_slow|rsi_period|adx_period|atr_period)"
    - from: "analyze()"
      to: "_atr"
      via: "atr_value computation"
      pattern: "_atr\\(highs, lows, closes"
    - from: "_vwap_bullish"
      to: "session UTC-day anchor"
      via: "timestamps[:10] day filter"
      pattern: "session_anchor"
---

<objective>
Make `src/technical_signals.py` profile-aware and intraday-correct, with swing parity preserved.

Implements SIGNAL-01 (profile-sourced periods), SIGNAL-02 (`atr_value` on `Signal`),
SIGNAL-03 (session-anchored VWAP for daytrade). Pure-function math edits + new unit tests.
Call-site threading is Plan 02 (Wave 2).

Purpose: Daytrade can later use different periods/ATR exits; swing must not regress.
Output: Parameterized signal engine, `_atr` helper, session VWAP, parity + new tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-parameterized-signal-engine-atr-session-vwap/03-RESEARCH.md
@.planning/phases/03-parameterized-signal-engine-atr-session-vwap/03-CONTEXT.md
@src/technical_signals.py
@src/strategy_profile.py

<interfaces>
From src/strategy_profile.py — SWING preset (defaults that MUST reproduce 9/21/14 parity):
  SWING.ema_fast=9, ema_slow=21, rsi_period=14, adx_period=14, atr_period=14, name="swing"
  DAYTRADE.name="daytrade" (same periods this phase; session VWAP toggled by name)
  Import: `from src.strategy_profile import SWING`

Current analyze() hardcoded literals to replace (technical_signals.py):
  line 241 `_ema(closes, 9)`, 242 `_ema(closes, 21)`, 255 `_adx(..., 14)`, 269 `_rsi(closes, 14)`
  4H filter EMAs lines 363-364 use 9/21 — replace with profile.ema_fast/ema_slow
Current Signal None-contract: returns None when `score == 0 and short_score == 0` (line 374) — PRESERVE.
_adx TR loop (lines 108-123) is the source pattern for _atr; _adx guard is `n < period*2+1` — _atr guard is `n < period+1`.
_vwap_bullish (lines 186-204) keeps its existing two-branch behavior on the swing path.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _atr() helper + atr_value on Signal</name>
  <files>src/technical_signals.py, tests/test_technical_signals.py</files>
  <behavior>
    - _atr([10,11,12,11,13],[9,10,10,9,11],[9,11,11,10,12], 2) == 2.5 (hand fixture, per D-03 / RESEARCH §2)
    - _atr returns 0.0 when n < period+1 or len(highs)!=len(closes)!=len(lows)
    - Signal has new field atr_value: float = 0.0 (last field, after trend_4h, default preserves construction)
  </behavior>
  <action>
    Add `_atr(highs, lows, closes, period: int = 14) -> float` per D-03 / RESEARCH §2: reuse the
    `_adx` true-range formula (max of high-low, |high-prev_close|, |low-prev_close|), first ATR =
    simple mean of first `period` TRs, then Wilder smoothing `atr = (atr*(period-1)+tr)/period` for
    the rest. Guard `n < period + 1` (NOT `_adx`'s `2*period+1`). Return scalar latest ATR, 0.0 on
    insufficient data. Add `atr_value: float = 0.0` as the LAST field of the `Signal` dataclass.
    Do NOT wire ATR into exits or scoring (Phase 4; D-03/D-05). Add `TestATR` with the hand fixture
    asserting `== 2.5` and an insufficient-data case asserting `0.0`.
  </action>
  <verify>
    <automated>cd C:/Users/artic/GitHub/aipredictedwins; python -m pytest tests/test_technical_signals.py -k atr -q</automated>
  </verify>
  <done>_atr matches 2.5 fixture; Signal.atr_value field exists default 0.0; ATR tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parameterize analyze()/scan_assets periods from profile (SIGNAL-01) + populate atr_value</name>
  <files>src/technical_signals.py, tests/test_technical_signals.py</files>
  <behavior>
    - analyze(symbol, bars, bars_4h=None, profile=SWING) — profile is LAST param (D-01)
    - With profile=SWING, analyze() output identical to a fixed-bar baseline (byte-for-byte parity, SIGNAL-01 / criterion 4)
    - analyze() sources ema_fast/ema_slow/rsi_period/adx_period/atr_period from profile; 4H EMAs use profile.ema_fast/ema_slow
    - Signal.atr_value populated via _atr(highs,lows,closes,profile.atr_period) on the success path
    - scan_assets(..., profile=SWING) threads profile + profile.timeframe/bar_count/htf_filter_timeframe and passes profile to analyze()
    - None-contract preserved: analyze still returns None when score==0 and short_score==0
  </behavior>
  <action>
    Per D-01: add `profile=SWING` as the LAST param to `analyze()` (import `from src.strategy_profile
    import SWING`). Replace hardcoded periods with `profile.ema_fast`, `profile.ema_slow`,
    `profile.adx_period`, `profile.rsi_period`; the 4H-filter EMAs (lines 363-364) also use
    `profile.ema_fast/ema_slow`. Compute `atr_value = _atr(highs, lows, closes, profile.atr_period)`
    before the None check; include it ONLY in the success-path `Signal(...)` constructor (Pitfall 1 —
    do not change the None return). Per D-02: add `profile=SWING` as the LAST param to `scan_assets`;
    keep `timeframe`/`bar_count` params for back-compat but fetch using `profile.timeframe`,
    `profile.bar_count`, and `profile.htf_filter_timeframe` (HTF fetch), and pass `profile=profile`
    into `analyze`. Do NOT touch confluence/short-score scoring or thresholds (D-05). Add
    `TestProfilePeriods` (analyze with a profile object that has distinct period values is wired —
    e.g. assert the call runs and details reflect the profile) and a swing-parity regression test:
    a fixed deterministic bar fixture run through `analyze(..., profile=SWING)` whose Signal fields
    (confluence_score, short_score, adx_value, rsi_value, ema_bullish, vwap_bullish) match an
    explicit expected snapshot, proving 9/21/14 parity.
  </action>
  <verify>
    <automated>cd C:/Users/artic/GitHub/aipredictedwins; python -m pytest tests/test_technical_signals.py -k "profile or parity or uptrend_signal or signal_fields" -q</automated>
  </verify>
  <done>analyze/scan_assets take profile=SWING last; no hardcoded 9/21/14 remain in analyze; swing parity snapshot test passes; atr_value populated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Session-anchored VWAP for daytrade (SIGNAL-03)</name>
  <files>src/technical_signals.py, tests/test_technical_signals.py</files>
  <behavior>
    - _vwap_bullish(closes, volumes, vwaps, timestamps=None, session_anchor=False): when session_anchor and timestamps, anchor cumulative VWAP to bars sharing last bar's UTC day (timestamps[-1][:10])
    - Day-boundary reset: bars from a prior UTC day are excluded from the cumulative VWAP
    - session_anchor=False path is byte-for-byte unchanged (swing parity, D-04/D-05)
    - analyze() passes session_anchor = (profile.name == "daytrade") and timestamps from bars
  </behavior>
  <action>
    Per D-04 / RESEARCH §3: extend `_vwap_bullish` with optional `timestamps=None, session_anchor=False`.
    When `session_anchor and timestamps`: `last_day = timestamps[-1][:10]`; include only indices whose
    `t[:10] == last_day`; cumulative VWAP = sum(close*vol)/sum(vol) over those bars; return
    `closes[-1] > that` (False if cum_vol <= 0). Leave the existing two-branch swing behavior below
    completely unchanged. In `analyze()`, build `timestamps = [b.get("timestamp") for b in bars]` and
    call `_vwap_bullish(closes, volumes, vwaps, timestamps=timestamps, session_anchor=(profile.name == "daytrade"))`.
    Use ISO string `[:10]` slice — do NOT parse with datetime (Anti-pattern). Add `TestSessionVWAP`:
    bars spanning two UTC days where the prior-day bars would flip the verdict, asserting the daytrade
    (session_anchor) result reflects only the current day, and a regression case asserting
    session_anchor=False matches the old behavior.
  </action>
  <verify>
    <automated>cd C:/Users/artic/GitHub/aipredictedwins; python -m pytest tests/test_technical_signals.py -k "session_vwap or above_vwap or below_vwap or fallback" -q</automated>
  </verify>
  <done>Session VWAP resets at UTC-day boundary for daytrade; swing VWAP unchanged; tests pass.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_technical_signals.py -q` — new SIGNAL tests green; no NEW failures introduced (the 6 pre-existing stale failures are remediated in Plan 02).
- `grep -nE "_ema\(closes, (9|21)\)|_adx\(.*, 14\)|_rsi\(closes, 14\)" src/technical_signals.py` returns no matches inside `analyze()` (periods now profile-sourced).
</verification>

<success_criteria>
- analyze()/scan_assets take `profile=SWING` as the last param; no hardcoded 9/21/14 in analyze().
- Signal.atr_value computed via _atr; hand fixture == 2.5.
- Daytrade uses UTC-day session-anchored VWAP; swing VWAP semantics unchanged.
- Swing-parity snapshot test passes (criterion 4).
</success_criteria>

<output>
Create `.planning/phases/03-parameterized-signal-engine-atr-session-vwap/03-01-SUMMARY.md` when done.
</output>
