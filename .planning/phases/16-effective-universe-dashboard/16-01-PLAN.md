---
phase: 16-effective-universe-dashboard
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_effective_universe.py
autonomous: true
requirements: [UNIV-03]

must_haves:
  truths:
    - "tests/test_effective_universe.py exists with all 17 VALIDATION cases as RED tests (cases 1-13 pure/zero-I/O; cases 14-17 TestClient, TEST_DATABASE_URL-gated, mirroring dashboard/api/tests/test_routes.py:15-30)"
    - "Tests import the not-yet-existing surface (src.effective_universe.resolve_universe) so they fail RED on the missing impl, not on malformed asserts"
    - "Case 13 (the anti-lie invariant) is written as a PROPERTY test over the full symbol cross-product: for every symbol, if src.universe.entry_allowed(...) says blocked, the resolver NEVER reports it effective. This is the case that stops the panel drifting from the gate."
    - "Case 3 pins the shadow sets (Decision 3b): DOT/USD, LINK/USD, ETH/USD -> reason='untradeable'; DOGE/USD -> reason='meme'. Shadow sets are INJECTED into the resolver by the test (never imported from src.alpaca_orchestrator), keeping the pure tests zero-I/O."
    - "Case 4 pins precedence: quarantined > off_universe > meme > untradeable"
  artifacts:
    - path: "tests/test_effective_universe.py"
      provides: "RED suite covering UNIV-03 cases 1-17"
      contains: "def test_resolver_agrees_with_gate"
      min_lines: 200
  key_links:
    - from: "tests/test_effective_universe.py"
      to: "src.effective_universe.resolve_universe"
      via: "import + call the pure resolver under test"
      pattern: "from src.effective_universe import|resolve_universe"
    - from: "tests/test_effective_universe.py"
      to: "src.universe.entry_allowed"
      via: "case 13 cross-checks the resolver's verdict against the gate of record"
      pattern: "entry_allowed"
---

<objective>
Write the RED test suite pinning the Phase-16 effective-universe contract before any implementation.
Wave 0 — plans 02/03/04 turn these GREEN.

Purpose: Nyquist gate for UNIV-03. All 17 VALIDATION cases become executable specs. The load-bearing
spec is case 13: the resolver must delegate to `src.universe.entry_allowed`, so the dashboard can
never report a symbol tradeable that the gate would block. Case 3 pins Decision 3b (the shadow
deny-lists that today silently remove DOT/LINK/ETH from every default confluence bot's scan).
Output: tests/test_effective_universe.py (RED).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-effective-universe-dashboard/16-CONTEXT.md
@.planning/phases/16-effective-universe-dashboard/16-RESEARCH.md
@.planning/phases/16-effective-universe-dashboard/16-VALIDATION.md
@CLAUDE.md

<interfaces>
Surface under test (built in Plan 02 — RED now). Pure, zero I/O, no DB, no Alpaca, no env reads:

  src.effective_universe.resolve_universe(
      cfg,                                   # src.bot_config.BotConfig (frozen dataclass)
      *,
      exposure: dict[str, dict] | None = None,   # NORMALIZED symbol -> {"open": int, "recent": int, "display": str}
      meme: Iterable[str] | None = None,         # inject in tests; None -> lazy-load real set
      untradeable: Iterable[str] | None = None,  # inject in tests; None -> lazy-load real set
  ) -> dict

  Returns:
    {
      "bot_id": str,
      "strategy": str,
      "asset_class": str,
      "allowlist": list[str],        # ORIGINAL spelling, per-strategy (see table below)
      "quarantined": list[str],      # cfg.quarantined, as configured
      "effective": list[str],        # allowlist MINUS quarantine MINUS meme MINUS untradeable
      "blocked": [{"symbol": str, "reason": str, "open_positions": int, "recent_trades": int}],
      "starvation": bool,            # effective == []
      "leak": list[str],             # blocked with reason=="off_universe" AND (open>0 or recent>0)
      "shadow_sets_loaded": bool,    # False only if the lazy import failed (never lies by omission)
    }

  reason vocabulary + PRECEDENCE (first match wins):
      "quarantined"  >  "off_universe"  >  "meme"  >  "untradeable"

  src.effective_universe.allowlist_for(cfg) -> list[str]   # module-level pure fn, separately testable

Existing surfaces the tests exercise (do NOT change them in this plan):
  src.universe.entry_allowed(symbol, allowlist, quarantined) -> (bool, reason|None)
      reason in {None, "off_universe", "quarantined"}; quarantine precedes allowlist;
      EMPTY allowlist == "no restriction". [src/universe.py:26]
  src.universe.normalize(symbol) -> str   # "BTC/USD"/"btc/usd"/"BTCUSD" -> "BTCUSD"; total.
  src.bot_config.BotConfig  — construct directly (frozen dataclass; alpaca keys can be "").
      cfg.symbols (asset_class-driven), cfg.all_symbols (crypto ∪ stock), cfg.quarantined (flat).

Per-strategy allowlist — MIRRORS the gate call sites exactly [16-RESEARCH.md §3]:
  copytrade   -> list(cfg.all_symbols)                  # copytrade_thread.py:398
  trend_btc   -> list(cfg.symbols) + [cfg.trend_symbol] # trend_strategy.py:103 (BITX carve-out)
  otherwise   -> list(cfg.symbols)                      # bot_thread.py:355, bot_c/strategy.py:289

Shadow deny-lists (Decision 3b) — their real import sites, for Plan 02's lazy loader:
  MEME_CRYPTO          src/alpaca_evaluator.py:42   = {"DOGE/USD","SHIB/USD","PEPE/USD","BONK/USD","WIF/USD","FLOKI/USD"}
  _ALPACA_UNTRADEABLE  src/alpaca_orchestrator.py:79-84 (env ALPACA_UNTRADEABLE; default contains
                       DOT/USD, LINK/USD, ETH/USD — three of the eight default crypto symbols)
  Consumed at src/bot_thread.py:144-145 and :163-164 (raw `s.symbol not in SET` — slashed spelling).
  IN THE PURE TESTS: pass them via meme=/untradeable= kwargs. NEVER import src.alpaca_orchestrator
  from tests/ (it pulls the Alpaca SDK + MiroFish client and reads env at import).

Route cases (14-17) — fixture idiom to copy from dashboard/api/tests/test_routes.py:14-30:
  sys.path.insert(0, <repo>/dashboard/api)   # so `from main import app` resolves
  pytest.mark.skipif(not os.environ.get("TEST_DATABASE_URL"), ...)   # module-level for these cases
  os.environ["DATABASE_URL"] = os.environ["TEST_DATABASE_URL"]; os.environ.pop("DASHBOARD_TOKEN", None)
  Assert on r.status_code and r.json()["data"] (the Envelope unwrap).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — pure resolver cases 1-9 + 13 (the anti-lie invariant)</name>
  <files>tests/test_effective_universe.py</files>
  <read_first>16-VALIDATION.md (the 17-case table — use the EXACT test names in it); src/universe.py (entry_allowed/normalize — the gate of record); src/bot_config.py L72-100 (symbols / quarantined / all_symbols properties); src/bot_thread.py L138-168 (the two selectors — the ONLY place the shadow sets are applied to candidates); tests/test_universe.py (Phase-15 conventions: plain literals, zero network, direct BotConfig construction)</read_first>
  <behavior>
    - test_effective_confluence (case 1): default crypto BotConfig (8 symbols), quarantined_symbols="AVAX/USD",
      meme={"DOGE/USD"}, untradeable={"DOT/USD","LINK/USD","ETH/USD"} ->
      effective == ["BTC/USD","SOL/USD","XRP/USD","ADA/USD"] (order-stable, original spelling);
      len(blocked) == 4.
    - test_blocked_reason_quarantined (case 2): "AVAX/USD" appears in blocked with reason=="quarantined".
    - test_blocked_reason_shadow_sets (case 3, Decision 3b): with the real default sets injected,
      DOT/USD, LINK/USD, ETH/USD -> reason=="untradeable"; a meme symbol placed in the allowlist
      (crypto_universe="BTC/USD,DOGE/USD") -> DOGE/USD reason=="meme". Asserts the panel cannot
      report "8 of 8 tradeable" for a bot that really scans 5.
    - test_reason_precedence (case 4): a symbol that is simultaneously quarantined AND untradeable
      (e.g. quarantined_symbols="ETH/USD" with ETH/USD in `untradeable`) -> reason=="quarantined";
      a symbol both meme AND untradeable -> reason=="meme"; an off-universe symbol that is also
      untradeable -> reason=="off_universe". Precedence: quarantined > off_universe > meme > untradeable.
    - test_effective_trend_carveout (case 5): strategy="trend_btc", asset_class="stock",
      trend_symbol="BITX" -> "BITX" in allowlist AND in effective, even though it is not in stock_universe.
    - test_effective_copytrade_union (case 6): strategy="copytrade" -> allowlist == cfg.all_symbols
      (contains BOTH crypto pairs and stock tickers) regardless of cfg.asset_class.
    - test_effective_bot_c (case 7): strategy="tradingagents", asset_class="stock" -> allowlist == cfg.symbols
      (the stock_universe), NOT the union, NOT the crypto list.
    - test_allowlist_by_strategy_column (case 8): allowlist_for(cfg) dispatches purely on cfg.strategy —
      parametrize all four strategies over the SAME otherwise-identical cfg and assert four distinct
      allowlists. No thread, no BotManager, no DB touched (proves the API needs neither).
    - test_starvation_flag (case 9): quarantine every symbol in the allowlist -> effective == [] AND
      starvation is True; a healthy bot -> starvation is False.
    - test_resolver_agrees_with_gate (case 13, THE ANTI-LIE INVARIANT): for each of the four strategies,
      over the cross-product of (allowlist ∪ quarantine ∪ a few off-universe symbols like TRUMP/USD,
      FIL/USD), assert: entry_allowed(sym, allowlist_for(cfg), cfg.quarantined)[0] is False
      IMPLIES sym not in result["effective"]; and every symbol in result["effective"] returns
      (True, None) from entry_allowed. Also assert the blocked reason for a gate-blocked symbol
      EQUALS the gate's reason string (no relabelling).
  </behavior>
  <action>Create tests/test_effective_universe.py. Import `from src.effective_universe import resolve_universe, allowlist_for` and `from src.universe import entry_allowed, normalize` and `from src.bot_config import BotConfig`. Add a small `_cfg(**overrides)` helper that builds a BotConfig with alpaca_api_key="" / alpaca_secret_key="" and lets each test override strategy / asset_class / crypto_universe / stock_universe / quarantined_symbols / trend_symbol. ALWAYS inject the shadow sets via the meme= / untradeable= kwargs so the pure tests stay zero-I/O — do NOT import src.alpaca_orchestrator. Use the exact test names from 16-VALIDATION.md. These fail RED with ImportError on src.effective_universe. Do NOT write production code in this plan.</action>
  <verify>
    <automated>python -m pytest tests/test_effective_universe.py -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - File collects (no SyntaxError); cases fail RED on the missing module, not on malformed asserts.
    - Case 13 is a real cross-product property assertion (not a single hand-picked symbol) and cross-checks BOTH directions (blocked-implies-not-effective AND effective-implies-gate-allows).
    - Case 3 asserts reason=="untradeable" for DOT/LINK/ETH — the Decision-3b lie.
    - Case 4 asserts all four precedence orderings.
    - Zero imports of src.alpaca_orchestrator / src.bot_thread / psycopg in the pure cases.
  </acceptance_criteria>
  <done>The resolver contract, including the anti-lie invariant, is an executable RED spec.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED — leak + normalization cases 10, 11, 12</name>
  <files>tests/test_effective_universe.py</files>
  <read_first>16-RESEARCH.md §5 (leak detection + the normalization trap); src/universe.py:17 (normalize); src/db_schema.sql:24-43 (alpaca_trades columns — note `timestamp TEXT`, `status TEXT DEFAULT 'open'`); dashboard/api/routes/positions.py:63-78 and :131 (the status vocabulary: open='open', closed IN ('closed','stopped','target_hit'))</read_first>
  <behavior>
    - test_leak_flag (case 10, the TRUMP/FIL case): default crypto bot; exposure includes a symbol that
      is NOT in the allowlist ("TRUMP/USD") with {"open": 1, "recent": 3} -> "TRUMP/USD" appears in
      blocked with reason=="off_universe", carries open_positions==1 / recent_trades==3, AND is in
      result["leak"]. A second off-universe symbol with only recent trades ({"open":0,"recent":2})
      is ALSO leaked (a bot that traded off-universe last week is still a leak).
    - test_no_false_leak (case 11): an off-universe symbol with {"open":0,"recent":0} -> present in
      blocked (reason off_universe) but NOT in leak. A QUARANTINED symbol with an open position ->
      blocked reason=="quarantined" and NOT in leak (a quarantine holding an open position is expected
      wind-down, not a leak) — assert explicitly so the panel does not cry wolf.
    - test_leak_normalization (case 12): the exposure map is keyed by NORMALIZED symbol
      ("TRUMPUSD", "BTCUSD" — alpaca_trades stores mixed 'BTC/USD' and slashless 'BTCUSD' spellings).
      Assert an exposure entry keyed "TRUMPUSD" still matches the off-universe display symbol and
      leaks; and that an exposure entry keyed "BTCUSD" attaches its counts to the allowlisted
      "BTC/USD" WITHOUT leaking (it is in-universe). Assert the returned display spelling is the
      original ("BTC/USD"), never the normalized form.
  </behavior>
  <action>Extend tests/test_effective_universe.py with the leak cases. Build the `exposure` dict by hand (normalized keys -> {"open": int, "recent": int, "display": str}) — no DB, no SQL, no psycopg import. The exposure map is the resolver's ONLY view of trade history; the SQL that builds it is Plan 03's job and is tested there. Assert leak membership by original display spelling.</action>
  <verify>
    <automated>python -m pytest tests/test_effective_universe.py -q -k "leak or normalization" 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Case 10 covers BOTH open-position leak and recent-trade-only leak.
    - Case 11 explicitly asserts a quarantined symbol with an open position is NOT a leak.
    - Case 12 drives the normalized-key/original-display split in both directions.
    - Still zero I/O.
  </acceptance_criteria>
  <done>The leak contract (and the slash trap that would silently defeat it) is an executable RED spec.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RED — route cases 14-17 (TestClient, TEST_DATABASE_URL-gated)</name>
  <files>tests/test_effective_universe.py</files>
  <read_first>dashboard/api/tests/test_routes.py L1-90 (the sys.path.insert + skipif + client fixture + seeding idiom to copy verbatim); dashboard/api/routes/bots.py L17-25 (router prefix="/api", so the path is written in full) and L137 (the 404 idiom); dashboard/api/models.py L18-27 (Envelope/Meta — every route returns Envelope(data=..., meta=Meta(count=N)))</read_first>
  <behavior>
    - test_universe_route_200 (case 14): GET /api/bots/A/universe -> 200; body has data + meta;
      data has keys {bot_id, strategy, asset_class, allowlist, quarantined, effective, blocked,
      starvation, leak, shadow_sets_loaded}; blocked entries have {symbol, reason, open_positions,
      recent_trades}; meta["count"] == len(effective).
    - test_universe_route_404 (case 15): GET /api/bots/ZZZ/universe -> 404, detail mentions the bot id.
    - test_universe_route_readonly (case 16): monkeypatch/guard that the handler constructs NO Alpaca
      client and performs NO write — assert by (a) counting rows in `bots` and `alpaca_trades` before
      and after the call (unchanged), and (b) a STATIC source guard: read dashboard/api/routes/bots.py
      from disk (pathlib, resolved from the repo root, not CWD) and assert the universe handler body
      contains no "INSERT"/"UPDATE"/"DELETE" and no "AlpacaClient".
    - test_openapi_contains_universe_route (case 17): GET /openapi.json -> 200 and
      "/api/bots/{bot_id}/universe" in body["paths"], with a "get" operation. Proves the repo's
      /openapi.json + /docs contract holds with NO main.py edit.
    - Seeding: reuse the module fixture's ON CONFLICT DO NOTHING pattern to insert (a) a bots row for
      the test bot with a known crypto_universe + quarantined_symbols, and (b) an alpaca_trades row
      for an OFF-UNIVERSE symbol (e.g. TRUMP/USD, status='open') so `leak` is non-empty in the 200 case.
      alpaca_trades.timestamp is TEXT — seed it with an ISO-8601 string (e.g. datetime.now(timezone.utc).isoformat()).
  </behavior>
  <action>Append the route cases to tests/test_effective_universe.py inside a class or module section guarded by its OWN `pytest.mark.skipif(not os.environ.get("TEST_DATABASE_URL"))` (the pure cases 1-13 must still run without any DB — do NOT put the skipif at module level). Copy the client fixture from dashboard/api/tests/test_routes.py: sys.path.insert the dashboard/api dir, set DATABASE_URL from TEST_DATABASE_URL, pop DASHBOARD_TOKEN to disable auth, `from main import app`, seed, return TestClient(app). Do NOT write production code.</action>
  <verify>
    <automated>python -m pytest tests/test_effective_universe.py -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Without TEST_DATABASE_URL: cases 14-17 SKIP cleanly and cases 1-13 still collect + fail RED.
    - Case 16 asserts read-only both dynamically (row counts) and statically (no INSERT/UPDATE/DELETE/AlpacaClient in the handler).
    - Case 17 asserts the literal path "/api/bots/{bot_id}/universe" in /openapi.json.
    - `python -m pytest tests/ -q` shows no NEW failures outside tests/test_effective_universe.py.
  </acceptance_criteria>
  <done>The route contract (shape, 404, read-only, OpenAPI presence) is an executable RED spec.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test process → Alpaca | none — no Alpaca client is constructed anywhere in this suite |
| gated tests → Postgres | TEST_DATABASE_URL-gated; skipped by default |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Repudiation | the panel reports a symbol tradeable that the gate blocks (a NEW lie in the anti-lie phase) | mitigate | case 13 — cross-product property test against src.universe.entry_allowed, both directions |
| T-16-02 | Information disclosure | the universe payload leaks alpaca_api_key/secret | mitigate | case 14 asserts the exact key set of `data`; no key field may appear |
| T-16-03 | Tampering | a write path sneaks onto the read-only endpoint | mitigate | case 16 — dynamic row-count guard + static source guard |
| T-16-SC | Tampering | package installs | accept | no new packages — pytest + FastAPI TestClient already installed |
</threat_model>

<verification>
- `python -m pytest tests/test_effective_universe.py -q` collects all 17 cases; 1-13 RED on the missing
  src.effective_universe module; 14-17 SKIP without TEST_DATABASE_URL.
- `python -m pytest tests/ -q` shows no NEW failures outside tests/test_effective_universe.py
  (Phase-15 baseline: 358 passed / 5 skipped).
</verification>

<success_criteria>
All 17 UNIV-03 cases are executable RED specs. Case 13 makes it impossible for a later plan to ship a
resolver that re-derives the set math and drifts from the gate; case 3 makes it impossible to ship a
panel that says "8 of 8 tradeable" for a bot that actually scans 5.
</success_criteria>

<output>
Create `.planning/phases/16-effective-universe-dashboard/16-01-SUMMARY.md` when done.
</output>
</content>
