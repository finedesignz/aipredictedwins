---
phase: 16-effective-universe-dashboard
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_effective_universe.py
autonomous: true
requirements: [UNIV-03]

must_haves:
  truths:
    - "tests/test_effective_universe.py exists with all 18 VALIDATION cases as RED tests (cases 1-13 + 18 pure/zero-I/O; cases 14-17 TestClient, TEST_DATABASE_URL-gated, mirroring dashboard/api/tests/test_routes.py:15-30)"
    - "Tests import the not-yet-existing surface (src.effective_universe.resolve_universe) so they fail RED on the missing impl, not on malformed asserts"
    - "Case 13 (the anti-lie invariant) is a PROPERTY test over the full symbol cross-product: if src.universe.entry_allowed says blocked, the resolver NEVER reports it effective. It carries a non-vacuity guard (assert result['blocked'] non-empty, iterated-symbol count > 0, >=1 gate-blocked symbol per strategy) so an empty loop cannot pass green."
    - "Case 3 pins the shadow sets FOR THE CONFLUENCE PATH ONLY (Decision 3b, corrected): DOT/USD, LINK/USD, ETH/USD -> reason='untradeable'; DOGE/USD -> reason='meme'"
    - "Case 18 pins the B1 correction: MEME_CRYPTO/_ALPACA_UNTRADEABLE are enforced ONLY at src/bot_thread.py:144-145,163-164 (the confluence selectors) and the CLI — they appear NOWHERE in copytrade_thread.py / trend_strategy.py / bot_c/strategy.py. A copytrade bot's effective set INCLUDES ETH/USD; shadow_applied is False for copytrade/trend_btc/tradingagents."
    - "Case 4 pins precedence: quarantined > off_universe > meme > untradeable"
    - "Case 14 asserts exposure_loaded is in the payload key set (a failed exposure query must never masquerade as 'no leak')"
  artifacts:
    - path: "tests/test_effective_universe.py"
      provides: "RED suite covering UNIV-03 cases 1-18"
      contains: "def test_resolver_agrees_with_gate"
      min_lines: 220
  key_links:
    - from: "tests/test_effective_universe.py"
      to: "src.effective_universe.resolve_universe"
      via: "import + call the pure resolver under test"
      pattern: "from src.effective_universe import|resolve_universe"
    - from: "tests/test_effective_universe.py"
      to: "src.universe.entry_allowed"
      via: "case 13 cross-checks the resolver's verdict against the gate of record"
      pattern: "entry_allowed"
---

<objective>
Write the RED test suite pinning the Phase-16 effective-universe contract before any implementation.
Wave 0 — plans 02/03/04 turn these GREEN.

Purpose: Nyquist gate for UNIV-03. All 18 VALIDATION cases become executable specs. Two load-bearing
specs:
- Case 13 — the resolver must delegate to `src.universe.entry_allowed`, so the dashboard can never
  report a symbol tradeable that the gate would block.
- Case 18 — the shadow deny-lists are enforced ONLY on the confluence path. Subtracting them for every
  strategy would invent a NEW lie (striking ETH/USD through on Bot E, which really does trade ETH/USD
  when its leader does).
Output: tests/test_effective_universe.py (RED).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-effective-universe-dashboard/16-CONTEXT.md
@.planning/phases/16-effective-universe-dashboard/16-RESEARCH.md
@.planning/phases/16-effective-universe-dashboard/16-VALIDATION.md
@CLAUDE.md

<interfaces>
Surface under test (built in Plan 02 — RED now). Pure, zero I/O, no DB, no Alpaca, no env reads:

  src.effective_universe.resolve_universe(
      cfg,                                       # src.bot_config.BotConfig (frozen dataclass)
      *,
      exposure: dict[str, dict] | None = None,   # NORMALIZED symbol -> {"open": int, "recent": int, "display": str}
      exposure_loaded: bool = True,              # False when the caller's exposure query FAILED
      meme: Iterable[str] | None = None,         # inject in tests; None -> lazy-load real set
      untradeable: Iterable[str] | None = None,  # inject in tests; None -> lazy-load real set
  ) -> dict

  Returns:
    {
      "bot_id": str,
      "strategy": str,
      "asset_class": str,
      "allowlist": list[str],        # ORIGINAL spelling, per-strategy (see table below)
      "quarantined": list[str],      # cfg.quarantined, as configured
      "effective": list[str],        # allowlist MINUS quarantine [MINUS meme/untradeable IFF confluence]
      "blocked": [{"symbol": str, "reason": str, "open_positions": int, "recent_trades": int}],
      "starvation": bool,            # effective == []
      "leak": list[str],             # blocked with reason=="off_universe" AND (open>0 or recent>0)
      "shadow_applied": bool,        # True ONLY for the confluence path (B1)
      "shadow_sets_loaded": bool,    # False if the lazy import failed (never lies by omission)
      "exposure_loaded": bool,       # False if the caller's exposure query failed -> leak is UNKNOWN, not empty
    }

  reason vocabulary + PRECEDENCE (first match wins):
      "quarantined"  >  "off_universe"  >  "meme"  >  "untradeable"

  src.effective_universe.allowlist_for(cfg) -> list[str]        # module-level pure fn
  src.effective_universe.shadow_applies_to(strategy) -> bool    # module-level pure fn (B1)

Existing surfaces the tests exercise (do NOT change them in this plan):
  src.universe.entry_allowed(symbol, allowlist, quarantined) -> (bool, reason|None)
      reason in {None, "off_universe", "quarantined"}; quarantine precedes allowlist;
      EMPTY allowlist == "no restriction". [src/universe.py:26]
  src.universe.normalize(symbol) -> str   # "BTC/USD"/"btc/usd"/"BTCUSD" -> "BTCUSD"; total.
  src.bot_config.BotConfig  — construct directly (frozen dataclass; alpaca keys can be "").
      cfg.symbols (asset_class-driven), cfg.all_symbols (crypto ∪ stock), cfg.quarantined (flat).

Per-strategy allowlist — MIRRORS the gate call sites exactly [16-RESEARCH.md §3]:
  copytrade   -> list(cfg.all_symbols)                  # copytrade_thread.py:398
  trend_btc   -> list(cfg.symbols) + [cfg.trend_symbol] # trend_strategy.py:103 (BITX carve-out)
  otherwise   -> list(cfg.symbols)                      # bot_thread.py:355, bot_c/strategy.py:289

SHADOW deny-lists — WHERE THEY ARE ACTUALLY ENFORCED (B1, the corrected Decision 3b):
  MEME_CRYPTO          src/alpaca_evaluator.py:42
  _ALPACA_UNTRADEABLE  src/alpaca_orchestrator.py:79-84 (env ALPACA_UNTRADEABLE; default contains
                       DOT/USD, LINK/USD, ETH/USD — three of the eight default crypto symbols)
  Enforced ONLY at:  src/bot_thread.py:144-145 and :163-164 (select_long_candidates /
                     select_short_candidates — the CONFLUENCE cycle) and the CLI orchestrator
                     (alpaca_orchestrator.py:773-786).
  They appear NOWHERE in src/copytrade_thread.py, src/trend_strategy.py, src/bot_c/strategy.py
  (bot_thread dispatches trend_btc -> run_trend_cycle at :551 and tradingagents ->
  run_tradingagents_cycle at :560, both of which BYPASS the selectors entirely).
  => shadow_applies_to(strategy) is True ONLY for the confluence path
     (strategy not in {"copytrade", "trend_btc", "tradingagents"}).
  IN THE PURE TESTS: pass the sets via meme= / untradeable= kwargs. NEVER import
  src.alpaca_orchestrator from tests/ (it pulls the Alpaca SDK + MiroFish client and reads env).

Route cases (14-17) — fixture idiom to copy from dashboard/api/tests/test_routes.py:14-30:
  sys.path.insert(0, <repo>/dashboard/api)   # so `from main import app` resolves
  pytest.mark.skipif(not os.environ.get("TEST_DATABASE_URL"), ...)   # for THESE cases only
  os.environ["DATABASE_URL"] = os.environ["TEST_DATABASE_URL"]; os.environ.pop("DASHBOARD_TOKEN", None)
  Assert on r.status_code and r.json()["data"] (the Envelope unwrap).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — pure resolver cases 1-9, 13, 18</name>
  <files>tests/test_effective_universe.py</files>
  <read_first>16-VALIDATION.md (the 18-case table — use the EXACT test names in it); src/universe.py (entry_allowed/normalize — the gate of record); src/bot_config.py L72-100 (symbols / quarantined / all_symbols); src/bot_thread.py L138-168 (select_long/short_candidates — the ONLY place the shadow sets touch candidates) and L545-565 (the strategy dispatch that makes trend_btc / tradingagents BYPASS those selectors); src/copytrade_thread.py L390-400 (no shadow sets anywhere); tests/test_universe.py (Phase-15 conventions: plain literals, zero network, direct BotConfig construction)</read_first>
  <behavior>
    - test_effective_confluence (case 1): default crypto BotConfig (8 symbols, strategy="confluence"),
      quarantined_symbols="AVAX/USD", meme={"DOGE/USD"}, untradeable={"DOT/USD","LINK/USD","ETH/USD"} ->
      effective == ["BTC/USD","SOL/USD","XRP/USD","ADA/USD"] (order-stable, original spelling);
      len(blocked) == 4; shadow_applied is True.
    - test_blocked_reason_quarantined (case 2): "AVAX/USD" in blocked with reason=="quarantined".
    - test_blocked_reason_shadow_sets (case 3, Decision 3b, CONFLUENCE bot): with the real default sets
      injected, DOT/USD, LINK/USD, ETH/USD -> reason=="untradeable"; a meme symbol in the allowlist
      (crypto_universe="BTC/USD,DOGE/USD") -> DOGE/USD reason=="meme". Asserts the panel cannot report
      "8 of 8 tradeable" for a confluence bot that really scans 5.
    - test_reason_precedence (case 4): quarantined AND untradeable -> "quarantined"; meme AND
      untradeable -> "meme"; off-universe AND untradeable -> "off_universe".
      Precedence: quarantined > off_universe > meme > untradeable.
    - test_effective_trend_carveout (case 5): strategy="trend_btc", asset_class="stock",
      trend_symbol="BITX" -> "BITX" in allowlist AND in effective, though absent from stock_universe.
    - test_effective_copytrade_union (case 6): strategy="copytrade" -> allowlist == cfg.all_symbols
      (BOTH crypto pairs and stock tickers) regardless of cfg.asset_class.
    - test_effective_bot_c (case 7): strategy="tradingagents", asset_class="stock" -> allowlist ==
      cfg.symbols (the stock_universe), NOT the union, NOT the crypto list.
    - test_allowlist_by_strategy_column (case 8): allowlist_for(cfg) dispatches purely on cfg.strategy —
      parametrize all four strategies over the SAME otherwise-identical cfg, assert four distinct
      allowlists. No thread, no BotManager, no DB (proves the API needs none).
    - test_starvation_flag (case 9): quarantine every allowlist symbol -> effective == [] AND
      starvation is True; a healthy bot -> starvation is False.
    - test_shadow_sets_confluence_only (case 18, B1 — THE FALSE-STRIKE GUARD): with
      untradeable={"ETH/USD","DOT/USD","LINK/USD"} and meme={"DOGE/USD"} injected, over the SAME crypto
      universe:
        strategy="confluence"     -> "ETH/USD" in blocked (reason "untradeable"); shadow_applied True
        strategy="copytrade"      -> "ETH/USD" in EFFECTIVE, NOT in blocked; shadow_applied False
        strategy="trend_btc"      -> shadow_applied False; NO symbol carries reason meme/untradeable
        strategy="tradingagents"  -> shadow_applied False; ditto
      Also assert shadow_applies_to("confluence") is True and False for the other three.
      Test docstring must record WHY: the selectors at bot_thread.py:144-145,163-164 are the ONLY
      enforcement site; copytrade/trend/bot_c bypass them, so striking ETH/USD through on Bot E would
      be a brand-new lie in the anti-lie phase.
    - test_resolver_agrees_with_gate (case 13, THE ANTI-LIE INVARIANT): for each of the four strategies,
      over the cross-product of (allowlist ∪ quarantine ∪ off-universe symbols like TRUMP/USD, FIL/USD),
      assert: entry_allowed(sym, allowlist_for(cfg), cfg.quarantined)[0] is False IMPLIES
      sym not in result["effective"]; every symbol in result["effective"] returns (True, None) from
      entry_allowed; and a gate-blocked symbol's reported reason EQUALS the gate's reason string (no
      relabelling). NON-VACUITY GUARDS (mandatory, W5): assert result["blocked"] is truthy AND the
      number of symbols actually iterated is > 0 AND at least one gate-blocked symbol was seen per
      strategy — an empty iteration must NOT pass green.
  </behavior>
  <action>Create tests/test_effective_universe.py. Import `from src.effective_universe import resolve_universe, allowlist_for, shadow_applies_to`, `from src.universe import entry_allowed, normalize`, `from src.bot_config import BotConfig`. Add a `_cfg(**overrides)` helper building a BotConfig with alpaca_api_key=""/alpaca_secret_key="" and overridable strategy / asset_class / crypto_universe / stock_universe / quarantined_symbols / trend_symbol. ALWAYS inject the shadow sets via meme= / untradeable= so the pure tests stay zero-I/O — do NOT import src.alpaca_orchestrator. Use the exact test names from 16-VALIDATION.md. These fail RED with ImportError on src.effective_universe. Do NOT write production code in this plan.</action>
  <verify>
    <automated>python -m pytest tests/test_effective_universe.py -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - File collects (no SyntaxError); cases fail RED on the missing module, not on malformed asserts.
    - Case 13 checks BOTH directions AND carries the non-vacuity guards (blocked non-empty, iterated count > 0, >=1 gate-blocked symbol per strategy).
    - Case 3 asserts reason=="untradeable" for DOT/LINK/ETH on a CONFLUENCE bot.
    - Case 18 asserts ETH/USD is EFFECTIVE (not struck through) for copytrade, and shadow_applied is False for copytrade/trend_btc/tradingagents.
    - Case 4 asserts all precedence orderings.
    - Zero imports of src.alpaca_orchestrator / src.bot_thread / psycopg in the pure cases.
  </acceptance_criteria>
  <done>The resolver contract — anti-lie invariant AND the confluence-only shadow rule — is an executable RED spec.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED — leak, normalization, exposure-unknown (cases 10, 11, 11b, 12)</name>
  <files>tests/test_effective_universe.py</files>
  <read_first>16-RESEARCH.md §5 (leak detection + the normalization trap); src/universe.py:17 (normalize); src/db_schema.sql:24-43 (alpaca_trades — note `timestamp TEXT`, `status TEXT DEFAULT 'open'`); dashboard/api/routes/positions.py:67,131 (status vocabulary: open='open'; closed IN ('closed','stopped','target_hit'))</read_first>
  <behavior>
    - test_leak_flag (case 10, the TRUMP/FIL case): default crypto bot; exposure includes an off-allowlist
      symbol "TRUMP/USD" with {"open": 1, "recent": 3} -> it appears in blocked with
      reason=="off_universe", carries open_positions==1 / recent_trades==3, AND is in result["leak"].
      A second off-universe symbol with only recent trades ({"open":0,"recent":2}) is ALSO leaked.
    - test_no_false_leak (case 11): an off-universe symbol with {"open":0,"recent":0} -> in blocked
      (reason off_universe) but NOT in leak. A QUARANTINED symbol with an open position -> blocked
      reason=="quarantined" and NOT in leak (expected wind-down, not a leak) — assert explicitly.
    - test_exposure_unloaded_is_unknown_not_no_leak (case 11b, B3): resolve_universe(cfg, exposure={},
      exposure_loaded=False) -> result["exposure_loaded"] is False AND result["leak"] == [].
      The contract the panel relies on: leak==[] with exposure_loaded==False means UNKNOWN, not
      "no leak" — the panel renders a footnote (Plan 04) instead of silent all-clear.
    - test_leak_normalization (case 12): the exposure map is keyed by NORMALIZED symbol ("TRUMPUSD",
      "BTCUSD" — alpaca_trades stores mixed 'BTC/USD' and slashless spellings). Assert an entry keyed
      "TRUMPUSD" still matches the off-universe display symbol and leaks; an entry keyed "BTCUSD"
      attaches its counts to the allowlisted "BTC/USD" WITHOUT leaking. Assert the returned display
      spelling is the original ("BTC/USD"), never the normalized form.
  </behavior>
  <action>Extend tests/test_effective_universe.py with the leak cases. Build the `exposure` dict by hand (normalized keys -> {"open": int, "recent": int, "display": str}) — no DB, no SQL, no psycopg import. The exposure map is the resolver's ONLY view of trade history; the SQL that builds it is Plan 03's job and is tested there. Assert leak membership by original display spelling.</action>
  <verify>
    <automated>python -m pytest tests/test_effective_universe.py -q -k "leak or normalization or exposure" 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Case 10 covers BOTH open-position leak and recent-trade-only leak.
    - Case 11 explicitly asserts a quarantined symbol with an open position is NOT a leak.
    - Case 11b asserts exposure_loaded=False propagates (the B3 guard).
    - Case 12 drives the normalized-key / original-display split in both directions.
    - Still zero I/O.
  </acceptance_criteria>
  <done>The leak contract — plus the slash trap and the failed-query-is-not-no-leak rule — is an executable RED spec.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RED — route cases 14-17 (TestClient, TEST_DATABASE_URL-gated)</name>
  <files>tests/test_effective_universe.py</files>
  <read_first>dashboard/api/tests/test_routes.py L1-90 (the sys.path.insert + skipif + client fixture + seeding idiom to copy verbatim); dashboard/api/routes/bots.py L17-25 (router prefix="/api" — paths written in full) and L137 (the 404 idiom); dashboard/api/models.py L18-27 (Envelope/Meta)</read_first>
  <behavior>
    - test_universe_route_200 (case 14): GET /api/bots/A/universe -> 200; body has data + meta;
      data key set is EXACTLY {bot_id, strategy, asset_class, allowlist, quarantined, effective,
      blocked, starvation, leak, shadow_applied, shadow_sets_loaded, exposure_loaded} — assert
      exposure_loaded and shadow_applied are PRESENT (B3/B1) and assert NO key matching /key|secret/i
      exists; blocked entries have {symbol, reason, open_positions, recent_trades};
      meta["count"] == len(effective).
    - test_universe_route_404 (case 15): GET /api/bots/ZZZ/universe -> 404, detail names the bot id.
    - test_universe_route_readonly (case 16): (a) row counts in `bots` and `alpaca_trades` unchanged
      across the call; (b) STATIC source guard — read dashboard/api/routes/bots.py from disk (pathlib,
      resolved from the repo root, not CWD) and assert the universe handler body contains no
      "INSERT"/"UPDATE"/"DELETE" and no "AlpacaClient".
    - test_openapi_contains_universe_route (case 17): GET /openapi.json -> 200 and
      "/api/bots/{bot_id}/universe" in body["paths"] with a "get" operation. Proves the
      /openapi.json + /docs contract holds with NO main.py edit.
    - Seeding: reuse the fixture's ON CONFLICT DO NOTHING pattern to insert (a) a bots row for the test
      bot with a known crypto_universe + quarantined_symbols, and (b) an alpaca_trades row for an
      OFF-UNIVERSE symbol (e.g. TRUMP/USD, status='open') so `leak` is non-empty in the 200 case.
      alpaca_trades.timestamp is TEXT — seed an ISO-8601 string (datetime.now(timezone.utc).isoformat()),
      which the route's ::timestamptz cast must accept. This seeded run is the ONLY execution path that
      ever runs the exposure SQL, so it is mandatory, not optional (see 16-03 acceptance evidence).
      SEED THE TEST DATABASE ONLY (TEST_DATABASE_URL). Never the prod DB.
  </behavior>
  <action>Append the route cases to tests/test_effective_universe.py inside a class/section guarded by its OWN `pytest.mark.skipif(not os.environ.get("TEST_DATABASE_URL"))` — the pure cases must still run without any DB, so do NOT put the skipif at module level. Copy the client fixture from dashboard/api/tests/test_routes.py: sys.path.insert the dashboard/api dir, set DATABASE_URL from TEST_DATABASE_URL, pop DASHBOARD_TOKEN, `from main import app`, seed, return TestClient(app). Do NOT write production code.</action>
  <verify>
    <automated>python -m pytest tests/test_effective_universe.py -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Without TEST_DATABASE_URL: cases 14-17 SKIP cleanly and the pure cases still collect + fail RED.
    - Case 14 asserts exposure_loaded + shadow_applied in the key set, and no key/secret field present.
    - Case 16 asserts read-only both dynamically (row counts) and statically (no INSERT/UPDATE/DELETE/AlpacaClient).
    - Case 17 asserts the literal path "/api/bots/{bot_id}/universe" in /openapi.json.
    - `python -m pytest tests/ -q` shows no NEW failures outside tests/test_effective_universe.py.
  </acceptance_criteria>
  <done>The route contract (shape, 404, read-only, OpenAPI presence) is an executable RED spec, and the exposure SQL has a real execution path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test process → Alpaca | none — no Alpaca client is constructed anywhere in this suite |
| gated tests → Postgres | TEST_DATABASE_URL-gated; skipped by default; NEVER the prod DB |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Repudiation | the panel reports a symbol tradeable that the gate blocks | mitigate | case 13 — cross-product property test vs src.universe.entry_allowed, both directions, non-vacuity-guarded |
| T-16-12 | Repudiation | the panel strikes through a symbol the bot really CAN trade (shadow sets applied off the confluence path) | mitigate | case 18 — copytrade effective INCLUDES ETH/USD; shadow_applied False for copytrade/trend/bot_c |
| T-16-13 | Repudiation | a failed exposure query silently reads as "no leak" | mitigate | case 11b + case 14 assert exposure_loaded is present and propagates |
| T-16-02 | Information disclosure | the universe payload leaks alpaca_api_key/secret | mitigate | case 14 asserts the exact key set + no /key\|secret/i field |
| T-16-03 | Tampering | a write path sneaks onto the read-only endpoint | mitigate | case 16 — dynamic row-count guard + static source guard |
| T-16-SC | Tampering | package installs | accept | no new packages — pytest + FastAPI TestClient already installed |
</threat_model>

<verification>
- `python -m pytest tests/test_effective_universe.py -q` collects all 18 cases; the pure ones RED on the
  missing src.effective_universe module; 14-17 SKIP without TEST_DATABASE_URL.
- `python -m pytest tests/ -q` shows no NEW failures outside tests/test_effective_universe.py
  (Phase-15 baseline: 358 passed / 5 skipped).
</verification>

<success_criteria>
All 18 UNIV-03 cases are executable RED specs. Case 13 makes it impossible to ship a resolver that
drifts from the gate; case 3 makes it impossible to ship a panel that says "8 of 8 tradeable" for a
confluence bot that scans 5; case 18 makes it impossible to ship one that strikes ETH/USD through on a
copytrade bot that trades it.
</success_criteria>

<output>
Create `.planning/phases/16-effective-universe-dashboard/16-01-SUMMARY.md` when done.
</output>
</content>
