---
phase: 16-effective-universe-dashboard
plan: 04
type: execute
wave: 3
depends_on: [16-03]
files_modified:
  - dashboard/web/types/index.ts
  - dashboard/web/components/bots/UniversePanel.tsx
  - dashboard/web/components/bots/BotCard.tsx
autonomous: true
requirements: [UNIV-03]

must_haves:
  truths:
    - "The drifted TS BotFull is realigned with the Pydantic BotFull (dashboard/api/models.py:207-230) — it is missing asset_class, min_short_confluence, tradingagents_enabled, strategy, trend_ma_window, trend_symbol, trend_benchmark, quarantined_symbols. Without this, `npm run build` fails (the panel reads bot.strategy)."
    - "UniversePanel.tsx renders effective chips, blocked chips struck-through with their reason, and an 'N of M tradeable' count — computing NOTHING; it renders exactly what the API returned"
    - "The LEAK state (red, ErrorBanner class vocabulary) and the STARVATION state (amber, warning-amber token) both render"
    - "The panel is mounted in BotCard.tsx, REPLACING the raw `Assets: {bot.crypto_universe}` line at BotCard.tsx:38 (which is itself part of the lie — it shows the raw config string, not the tradeable set)"
    - "Only existing primitives are used: useAPI<T>, shared/Badge.tsx, shared/ErrorBanner.tsx class vocabulary. No new design primitive, no new package, no frontend test framework."
    - "`cd dashboard/web && npm run build` succeeds (this is the type-check gate — there is no frontend test framework and none is added)"
  artifacts:
    - path: "dashboard/web/types/index.ts"
      provides: "realigned BotFull + BlockedSymbol + BotUniverse interfaces"
      contains: "interface BotUniverse"
    - path: "dashboard/web/components/bots/UniversePanel.tsx"
      provides: "the per-bot effective-universe panel"
      contains: "useAPI<BotUniverse>"
      min_lines: 50
  key_links:
    - from: "dashboard/web/components/bots/UniversePanel.tsx"
      to: "/api/bots/{botId}/universe"
      via: "useAPI<BotUniverse>(`/api/bots/${botId}/universe`, 30_000)"
      pattern: "useAPI<BotUniverse>"
    - from: "dashboard/web/components/bots/BotCard.tsx"
      to: "UniversePanel"
      via: "mounted in place of the raw Assets: line"
      pattern: "<UniversePanel"
---

<objective>
Make the effective universe visible. Replace the raw `Assets: BTC/USD,ETH/USD,...` config string on
the bot card — the line that let TRUMP/FIL trade for weeks unnoticed — with a panel showing what the
bot can ACTUALLY trade, why each other symbol is blocked, and two loud alarms: LEAK (an off-universe
symbol with real exposure) and STARVATION (nothing tradeable at all).

Purpose: close UNIV-03. The browser computes NOTHING — a client-side
`allowlist.filter(s => !quarantined.includes(s))` would be exactly the drift this phase exists to
prevent.
Output: realigned types + UniversePanel.tsx + a one-line swap in BotCard.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-effective-universe-dashboard/16-CONTEXT.md
@.planning/phases/16-effective-universe-dashboard/16-RESEARCH.md
@dashboard/web/components/bots/BotCard.tsx
@dashboard/web/components/shared/Badge.tsx
@dashboard/web/components/shared/ErrorBanner.tsx
@dashboard/web/hooks/useAPI.ts
@dashboard/web/types/index.ts

<interfaces>
Fetch layer — USE THIS, do not add SWR/react-query [dashboard/web/hooks/useAPI.ts]:
    useAPI<T>(url: string | null, pollInterval?: number) -> { data: T | null, loading, error, refetch }
    It already unwraps the Envelope (`response.data`) and redirects to /login on 401.

Badge primitive [dashboard/web/components/shared/Badge.tsx] — variants ONLY:
    "proceed" | "veto" | "paper" | "live" | "open" | "closed" | "bullish" | "bearish" | "neutral"
    Map: effective -> "proceed";  blocked(quarantined|off_universe) -> "veto";
         blocked(meme|untradeable) -> "neutral";  (DO NOT add a variant.)

ErrorBanner class vocabulary [dashboard/web/components/shared/ErrorBanner.tsx] — reuse verbatim for LEAK:
    role="alert" · rounded-lg border border-loss-red/40 bg-loss-red/10 px-4 py-3 · text-sm text-loss-red
STARVATION uses the amber token already in the Badge palette: border-warning-amber/40 bg-warning-amber/10 text-warning-amber

API payload (Plan 03):
    GET /api/bots/{bot_id}/universe -> Envelope<BotUniverse>
    BotUniverse { bot_id, strategy, asset_class, allowlist[], quarantined[], effective[],
                  blocked: {symbol, reason, open_positions, recent_trades}[],
                  starvation: boolean, leak: string[], shadow_sets_loaded: boolean }
    reason: "quarantined" | "off_universe" | "meme" | "untradeable"

TS BotFull DRIFT — the build blocker [dashboard/web/types/index.ts:163-179 vs dashboard/api/models.py:207-230].
The TS interface is MISSING these 8 fields the API already returns:
    asset_class: string | null;  min_short_confluence: number | null;  tradingagents_enabled: boolean | null;
    strategy: string | null;     trend_ma_window: number | null;       trend_symbol: string | null;
    trend_benchmark: string | null;  quarantined_symbols: string;
Add them (nullable to match the Optional[...] Pydantic fields). This is a prerequisite, not a nice-to-have.

Frontend testing: NONE EXISTS (package.json scripts are only dev/build/start/lint). DO NOT add jest,
vitest, playwright or testing-library in this phase. Verification = `npm run build` + a screenshot.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Realign types/index.ts (BotFull) + add BlockedSymbol / BotUniverse</name>
  <files>dashboard/web/types/index.ts</files>
  <read_first>dashboard/web/types/index.ts L163-212 (BotFull + APIResponse); dashboard/api/models.py L207-230 (the Pydantic BotFull — the authority; mirror its Optional[...] fields as `| null`)</read_first>
  <behavior>
    - BotFull gains the 8 missing fields listed in <interfaces>, typed to match the Pydantic Optionals.
    - New: `export interface BlockedSymbol { symbol: string; reason: "quarantined" | "off_universe" | "meme" | "untradeable"; open_positions: number; recent_trades: number; }`
    - New: `export interface BotUniverse { bot_id: string; strategy: string; asset_class: string; allowlist: string[]; quarantined: string[]; effective: string[]; blocked: BlockedSymbol[]; starvation: boolean; leak: string[]; shadow_sets_loaded: boolean; }`
  </behavior>
  <action>Edit dashboard/web/types/index.ts. Extend BotFull with the 8 drifted fields and append the two new interfaces next to it. Do not remove or rename any existing field (BotDrawer/BotCard consume them). Smallest diff.</action>
  <verify>
    <automated>cd dashboard/web && npx tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - tsc reports no errors.
    - Every field of the Pydantic BotFull has a TS counterpart (diff the two field lists by eye and record the check in the SUMMARY).
  </acceptance_criteria>
  <done>The TS contract matches the API contract; the build can proceed.</done>
</task>

<task type="auto">
  <name>Task 2: UniversePanel.tsx</name>
  <files>dashboard/web/components/bots/UniversePanel.tsx</files>
  <read_first>dashboard/web/hooks/useAPI.ts (the exact hook signature + Envelope unwrap); dashboard/web/components/shared/Badge.tsx (the 9 allowed variants); dashboard/web/components/shared/ErrorBanner.tsx (the alert class vocabulary); dashboard/web/components/bots/BotCard.tsx (the card's existing text scale: text-xs / text-text-muted / text-text-primary — match it)</read_first>
  <behavior>
    - "use client"; props: `{ botId: string }`.
    - `const { data, loading, error } = useAPI<BotUniverse>(`/api/bots/${botId}/universe`, 30_000);`
    - loading -> a muted one-line skeleton ("Universe…"). error -> reuse <ErrorBanner error={error} />.
    - Header row: `Universe` label + the count `{effective.length} of {allowlist.length} tradeable`
      (muted; the count is the at-a-glance signal).
    - LEAK banner (renders FIRST, above the chips, when data.leak.length > 0): ErrorBanner class
      vocabulary, role="alert", text naming the symbols, e.g.
      `LEAK: TRUMP/USD, FIL/USD traded outside this bot's universe` — plus the exposure counts pulled
      from the matching `blocked` entries (open_positions / recent_trades). This is the TRUMP/FIL case.
    - STARVATION banner (when data.starvation): amber block, role="alert",
      `No tradeable symbols — every symbol is blocked. This bot cannot enter a position.`
    - Effective chips: <Badge variant="proceed">{sym}</Badge>.
    - Blocked chips: <Badge variant={reason === "meme" || reason === "untradeable" ? "neutral" : "veto"}
        className="line-through" title={reason}>{symbol}</Badge>
      with the reason rendered as a small muted suffix so it is readable without hovering
      (title alone is not accessible). Order blocked by reason precedence: quarantined, off_universe,
      meme, untradeable.
    - When data.shadow_sets_loaded === false, render a muted footnote:
      `shadow deny-lists (meme / untradeable) unavailable in this process — the tradeable count may be optimistic`.
      Never silently omit it.
    - A muted footnote naming the source of the two shadow sets is optional; the panel MUST NOT
      recompute anything — no filter/includes over allowlist vs quarantined anywhere in this file.
  </behavior>
  <action>Create dashboard/web/components/bots/UniversePanel.tsx. Import Badge and ErrorBanner from the shared components and BotUniverse from @/types. Use ONLY existing Tailwind tokens already present in the codebase (text-text-muted, text-text-primary, border-border-primary, bg-bg-card, loss-red, warning-amber, profit-green). Do NOT create a new Badge variant, a new shared primitive, or a new CSS token. Do NOT install any package. Do NOT add a test framework.</action>
  <verify>
    <automated>cd dashboard/web && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds.
    - grep of UniversePanel.tsx shows NO `.filter(` / `.includes(` deriving allow-vs-block state (the browser renders, it does not decide).
    - No new file under components/shared/; no package.json change.
  </acceptance_criteria>
  <done>The panel renders the API's truth, including both alarm states.</done>
</task>

<task type="auto">
  <name>Task 3: Mount in BotCard.tsx + capture the screenshot evidence</name>
  <files>dashboard/web/components/bots/BotCard.tsx</files>
  <read_first>dashboard/web/components/bots/BotCard.tsx (the whole file — 45 lines; line 38 is the raw `Assets: {bot.crypto_universe}` div being replaced)</read_first>
  <behavior>
    - Replace the line
        `<div className="text-xs text-text-muted truncate">Assets: <span className="text-text-primary">{bot.crypto_universe}</span></div>`
      with
        `<UniversePanel botId={bot.bot_id} />`
      Keep everything else in the card untouched (status dot, toggle, Edit button, the config grid,
      the status_detail error block).
    - Import UniversePanel at the top.
  </behavior>
  <action>Edit BotCard.tsx: one import + one line swapped. Nothing else. Then run the dashboard locally and capture the evidence: (1) `npm run build`; (2) start the API + web (or the existing container) and open /bots in a real browser via chromium-cli/Playwright CLI; (3) screenshot a bot card showing the effective chips + the struck-through blocked chips with reasons + the `N of M tradeable` count; (4) force and screenshot BOTH alarm states — LEAK by seeding an alpaca_trades row for an off-universe symbol (e.g. TRUMP/USD, status='open') for that bot, and STARVATION by PUTting quarantined_symbols = the bot's entire crypto_universe via the EXISTING PUT /api/bots/{bot_id} (no new write path). REVERT both seeds afterwards. Save the screenshots under .planning/phases/16-effective-universe-dashboard/evidence/ and reference them in the SUMMARY.</action>
  <verify>
    <automated>cd dashboard/web && npm run build 2>&1 | tail -5</automated>
    <human-check>Screenshots in .planning/phases/16-effective-universe-dashboard/evidence/ show: (a) normal card with chips + count, (b) LEAK banner naming the off-universe symbol, (c) STARVATION banner. Test seeds reverted (quarantined_symbols restored, seeded trade row deleted).</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds; `git diff dashboard/web/components/bots/BotCard.tsx` is an import line + a one-line swap.
    - Three screenshots captured; both alarm states exercised.
    - No leftover test data in the DB (verify: the seeded off-universe trade row is deleted and quarantined_symbols is back to its prior value).
    - The raw `Assets:` config string no longer appears on the card.
  </acceptance_criteria>
  <done>An operator can see, at a glance, exactly what each bot can trade — and gets shouted at when a symbol leaks or the bot is starved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser → API | authenticated fetch via useAPI (401 -> /login); the panel is read-only |
| panel → set math | NONE — the browser receives effective/blocked already computed by the gate-delegating resolver |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-09 | Repudiation | the browser re-derives effective = allowlist − quarantined and drifts from the gate | mitigate | acceptance criteria greps UniversePanel.tsx for `.filter(`/`.includes(` deciding allow-vs-block; the payload is rendered verbatim |
| T-16-10 | Information disclosure | the panel renders a field that carries an alpaca key | mitigate | BotUniverse has no key field (Plan 03 T-16-02); the panel reads only BotUniverse + bot.bot_id |
| T-16-11 | Repudiation | shadow sets fail to load and the panel silently over-reports tradeable symbols | mitigate | shadow_sets_loaded === false renders an explicit footnote; never omitted |
| T-16-SC | Tampering | package installs | accept | ZERO new packages; package.json must be unchanged (`git diff --stat dashboard/web/package.json` empty) |
</threat_model>

<verification>
- `cd dashboard/web && npm run build` succeeds (the type-check gate — there is no frontend test framework).
- `git diff --stat dashboard/web/package.json` — empty (no new dependency).
- `python -m pytest tests/ dashboard/api/tests/ -q` — still green (frontend-only plan; nothing backend touched).
- Screenshots: normal / LEAK / STARVATION under .planning/phases/16-effective-universe-dashboard/evidence/.
</verification>

<success_criteria>
The bot card no longer shows a raw config string that lies about what the bot trades. It shows the
effective set, the blocked set with reasons (including the DOT/LINK/ETH shadow-untradeable case), a
tradeable count, and two loud alarms — leak and starvation — proven by screenshot.
</success_criteria>

<output>
Create `.planning/phases/16-effective-universe-dashboard/16-04-SUMMARY.md` when done.
</output>
</content>
