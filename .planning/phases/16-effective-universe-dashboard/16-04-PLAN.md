---
phase: 16-effective-universe-dashboard
plan: 04
type: execute
wave: 3
depends_on: [16-03]
files_modified:
  - dashboard/web/types/index.ts
  - dashboard/web/components/bots/UniversePanel.tsx
  - dashboard/web/components/bots/BotCard.tsx
autonomous: true
requirements: [UNIV-03]

must_haves:
  truths:
    - "The drifted TS BotFull is realigned with the Pydantic BotFull (dashboard/api/models.py:207-230) — it is missing asset_class, min_short_confluence, tradingagents_enabled, strategy, trend_ma_window, trend_symbol, trend_benchmark, quarantined_symbols. (NOTE: this does NOT currently break `tsc` — nothing reads those fields today. It is fixed because a silently-drifted client contract is exactly the class of bug this phase exists to kill, and BotDrawer/future panels will read them.)"
    - "UniversePanel.tsx renders effective chips, blocked chips struck-through with a VISIBLE reason, and an 'N of M tradeable' count — computing NOTHING; it renders exactly what the API returned"
    - "The LEAK state (red, ErrorBanner class vocabulary) and the STARVATION state (amber, warning-amber token) both render"
    - "Degraded states are shown, never silently swallowed: exposure_loaded===false -> 'leak check unavailable' footnote (NOT an implicit all-clear); shadow_sets_loaded===false -> 'tradeable count may be optimistic' footnote; shadow_applied===false -> a note that meme/untradeable filters do not apply to this strategy"
    - "The panel is mounted in BotCard.tsx, REPLACING the raw `Assets: {bot.crypto_universe}` line at BotCard.tsx:38 (itself part of the lie — it shows the raw config string, not the tradeable set)"
    - "Only existing primitives are used: useAPI<T>, shared/Badge.tsx (props are EXACTLY {variant, children, className?} — passing `title` would be a TS excess-property error, so it is NOT passed), shared/ErrorBanner.tsx class vocabulary. No new design primitive, no new package, no frontend test framework, and Badge.tsx is NOT modified."
    - "`cd dashboard/web && npm run build` succeeds — the type-check gate (there is no frontend test framework and none is added)"
    - "ALL screenshot evidence is captured against a LOCAL stack + TEST/local database. The prod dashboard and the prod DB are NEVER mutated."
  artifacts:
    - path: "dashboard/web/types/index.ts"
      provides: "realigned BotFull + BlockedSymbol + BotUniverse interfaces"
      contains: "interface BotUniverse"
    - path: "dashboard/web/components/bots/UniversePanel.tsx"
      provides: "the per-bot effective-universe panel"
      contains: "useAPI<BotUniverse>"
      min_lines: 60
  key_links:
    - from: "dashboard/web/components/bots/UniversePanel.tsx"
      to: "/api/bots/{botId}/universe"
      via: "useAPI<BotUniverse>(`/api/bots/${botId}/universe`, 30_000)"
      pattern: "useAPI<BotUniverse>"
    - from: "dashboard/web/components/bots/BotCard.tsx"
      to: "UniversePanel"
      via: "mounted in place of the raw Assets: line"
      pattern: "<UniversePanel"
---

<objective>
Make the effective universe visible. Replace the raw `Assets: BTC/USD,ETH/USD,...` config string on the
bot card — the line that let TRUMP/FIL trade for weeks unnoticed — with a panel showing what the bot can
ACTUALLY trade, why each other symbol is blocked, and two loud alarms: LEAK (an off-universe symbol with
real exposure) and STARVATION (nothing tradeable at all).

Purpose: close UNIV-03. The browser computes NOTHING — a client-side
`allowlist.filter(s => !quarantined.includes(s))` would be exactly the drift this phase exists to prevent.
Output: realigned types + UniversePanel.tsx + a one-line swap in BotCard.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-effective-universe-dashboard/16-CONTEXT.md
@.planning/phases/16-effective-universe-dashboard/16-RESEARCH.md
@dashboard/web/components/bots/BotCard.tsx
@dashboard/web/components/shared/Badge.tsx
@dashboard/web/components/shared/ErrorBanner.tsx
@dashboard/web/hooks/useAPI.ts
@dashboard/web/types/index.ts

<interfaces>
Fetch layer — USE THIS, do not add SWR/react-query [dashboard/web/hooks/useAPI.ts]:
    useAPI<T>(url: string | null, pollInterval?: number) -> { data: T | null, loading, error, refetch }
    It already unwraps the Envelope (`response.data`) and redirects to /login on 401.

Badge primitive [dashboard/web/components/shared/Badge.tsx:3-6] — the props are EXACTLY:
    { variant, children, className? }
There is NO `title` prop. Passing title={reason} is a TypeScript EXCESS-PROPERTY ERROR and would FAIL
`npm run build` — the only Wave-3 gate. DECISION (B2): do NOT pass `title`, and do NOT modify Badge.tsx.
Render the reason as VISIBLE text next to the chip instead (better a11y than a hover-only tooltip anyway).

Badge variants — the ONLY nine allowed:
    "proceed" | "veto" | "paper" | "live" | "open" | "closed" | "bullish" | "bearish" | "neutral"
    Map: effective -> "proceed";  blocked(quarantined|off_universe) -> "veto";
         blocked(meme|untradeable) -> "neutral".   DO NOT add a variant.

ErrorBanner class vocabulary [dashboard/web/components/shared/ErrorBanner.tsx] — reuse verbatim for LEAK:
    role="alert" · rounded-lg border border-loss-red/40 bg-loss-red/10 px-4 py-3 · text-sm text-loss-red
STARVATION uses the amber token already in the Badge palette:
    border-warning-amber/40 bg-warning-amber/10 text-warning-amber

API payload (Plan 03):
    GET /api/bots/{bot_id}/universe -> Envelope<BotUniverse>
    BotUniverse { bot_id, strategy, asset_class, allowlist[], quarantined[], effective[],
                  blocked: {symbol, reason, open_positions, recent_trades}[],
                  starvation: boolean, leak: string[],
                  shadow_applied: boolean, shadow_sets_loaded: boolean, exposure_loaded: boolean }
    reason: "quarantined" | "off_universe" | "meme" | "untradeable"

TS BotFull DRIFT [dashboard/web/types/index.ts:163-179 vs dashboard/api/models.py:207-230].
The TS interface is MISSING 8 fields the API already returns:
    asset_class: string | null;  min_short_confluence: number | null;  tradingagents_enabled: boolean | null;
    strategy: string | null;     trend_ma_window: number | null;       trend_symbol: string | null;
    trend_benchmark: string | null;  quarantined_symbols: string;
W4 CORRECTION: this drift does NOT currently fail `tsc` (nothing reads bot.strategy today, and the panel
below takes only `botId` and fetches its own strategy from the API). Fix it anyway — a silently drifted
client contract is precisely the failure mode this phase exists to eliminate — but do NOT justify it as a
build blocker.

Frontend testing: NONE EXISTS (package.json scripts are only dev/build/start/lint). DO NOT add jest,
vitest, playwright or testing-library. Verification = `npm run build` + screenshots.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Realign types/index.ts (BotFull) + add BlockedSymbol / BotUniverse</name>
  <files>dashboard/web/types/index.ts</files>
  <read_first>dashboard/web/types/index.ts L163-212 (BotFull + APIResponse); dashboard/api/models.py L207-230 (the Pydantic BotFull — the authority; mirror its Optional[...] fields as `| null`)</read_first>
  <behavior>
    - BotFull gains the 8 missing fields listed in <interfaces>, typed to match the Pydantic Optionals.
    - New: `export interface BlockedSymbol { symbol: string; reason: "quarantined" | "off_universe" | "meme" | "untradeable"; open_positions: number; recent_trades: number; }`
    - New: `export interface BotUniverse { bot_id: string; strategy: string; asset_class: string; allowlist: string[]; quarantined: string[]; effective: string[]; blocked: BlockedSymbol[]; starvation: boolean; leak: string[]; shadow_applied: boolean; shadow_sets_loaded: boolean; exposure_loaded: boolean; }`
  </behavior>
  <action>Edit dashboard/web/types/index.ts. Extend BotFull with the 8 drifted fields and append the two new interfaces next to it. Do not remove or rename any existing field (BotDrawer/BotCard consume them). Smallest diff.</action>
  <verify>
    <automated>cd dashboard/web && npx tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - tsc reports no errors.
    - Every field of the Pydantic BotFull has a TS counterpart; record the field-by-field diff check in the SUMMARY.
  </acceptance_criteria>
  <done>The TS contract matches the API contract; no silent client-side drift remains.</done>
</task>

<task type="auto">
  <name>Task 2: UniversePanel.tsx</name>
  <files>dashboard/web/components/bots/UniversePanel.tsx</files>
  <read_first>dashboard/web/hooks/useAPI.ts (the exact hook signature + Envelope unwrap); dashboard/web/components/shared/Badge.tsx (props are {variant, children, className?} ONLY — no title); dashboard/web/components/shared/ErrorBanner.tsx (the alert class vocabulary); dashboard/web/components/bots/BotCard.tsx (the card's text scale: text-xs / text-text-muted / text-text-primary — match it)</read_first>
  <behavior>
    - "use client"; props: `{ botId: string }`.
    - `const { data, loading, error } = useAPI<BotUniverse>(`/api/bots/${botId}/universe`, 30_000);`
    - loading -> a muted one-line skeleton ("Universe…"). error -> reuse <ErrorBanner error={error} />.
    - Header row: `Universe` label + the count `{effective.length} of {allowlist.length} tradeable` (muted).
    - LEAK banner (renders FIRST, above the chips, when data.leak.length > 0): ErrorBanner class
      vocabulary, role="alert", naming the symbols and their exposure counts pulled from the matching
      `blocked` entries — e.g. `LEAK: TRUMP/USD traded outside this bot's universe (1 open, 3 in 30d)`.
      This is the TRUMP/FIL case.
    - STARVATION banner (when data.starvation): amber block, role="alert",
      `No tradeable symbols — every symbol is blocked. This bot cannot enter a position.`
    - Effective chips: <Badge variant="proceed">{sym}</Badge>.
    - Blocked chips: <Badge variant={reason === "meme" || reason === "untradeable" ? "neutral" : "veto"}
        className="line-through">{symbol}</Badge>
      followed by the reason as VISIBLE muted text (e.g. a `text-[10px] text-text-muted` span reading
      `untradeable`). NO `title` prop on Badge (B2 — it is not in BadgeProps and would fail the build).
      Order blocked by reason precedence: quarantined, off_universe, meme, untradeable.
    - DEGRADED-STATE FOOTNOTES (muted, never omitted):
        data.exposure_loaded === false ->
          `leak check unavailable (trade-history query failed) — an empty leak list here means UNKNOWN, not clear`
        data.shadow_sets_loaded === false ->
          `shadow deny-lists (meme / untradeable) unavailable in this process — the tradeable count may be optimistic`
        data.shadow_applied === false ->
          `meme / untradeable filters do not apply to the ${data.strategy} strategy` (they are enforced
          only in the confluence selectors — src/bot_thread.py:144-145,163-164)
    - The panel MUST NOT recompute anything — no filter/includes over allowlist vs quarantined anywhere.
  </behavior>
  <action>Create dashboard/web/components/bots/UniversePanel.tsx. Import Badge and ErrorBanner from the shared components and BotUniverse from @/types. Use ONLY existing Tailwind tokens already present in the codebase (text-text-muted, text-text-primary, border-border-primary, bg-bg-card, loss-red, warning-amber, profit-green). Do NOT create a new Badge variant, do NOT modify Badge.tsx, do NOT add a shared primitive, do NOT install any package, do NOT add a test framework.</action>
  <verify>
    <automated>cd dashboard/web && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds.
    - `git diff --stat dashboard/web/components/shared/Badge.tsx` is EMPTY (B2: no `title` prop was needed).
    - grep of UniversePanel.tsx shows NO `.filter(` / `.includes(` deriving allow-vs-block state.
    - All three degraded-state footnotes are implemented.
    - No new file under components/shared/; no package.json change.
  </acceptance_criteria>
  <done>The panel renders the API's truth — including both alarms and every degraded state.</done>
</task>

<task type="auto">
  <name>Task 3: Mount in BotCard.tsx + capture screenshot evidence AGAINST A LOCAL STACK ONLY</name>
  <files>dashboard/web/components/bots/BotCard.tsx</files>
  <read_first>dashboard/web/components/bots/BotCard.tsx (all 45 lines; line 38 is the raw `Assets: {bot.crypto_universe}` div being replaced)</read_first>
  <behavior>
    - Replace
        `<div className="text-xs text-text-muted truncate">Assets: <span className="text-text-primary">{bot.crypto_universe}</span></div>`
      with
        `<UniversePanel botId={bot.bot_id} />`
      Keep everything else untouched (status dot, toggle, Edit button, config grid, status_detail block).
    - Import UniversePanel at the top.
  </behavior>
  <action>
Edit BotCard.tsx: one import + one line swapped. Nothing else.

Then capture the evidence. HARD RULE (B4): ALL of it runs against a LOCAL stack backed by a LOCAL /
TEST Postgres (the same DB used for TEST_DATABASE_URL). You MUST NOT point the browser at
app.aipredictedwins.com, MUST NOT PUT/POST anything to the deployed API, and MUST NOT INSERT into or
UPDATE the production database. Quarantining a live bot's whole universe would starve a real trading
bot, and a seeded open TRUMP/USD row would be picked up by the Phase-13/14 reconciliation + backfill
jobs as a real position. Both are forbidden.

Procedure:
  1. `cd dashboard/web && npm run build`  (the type-check gate).
  2. Start the API against the LOCAL/TEST database (DATABASE_URL=$TEST_DATABASE_URL) and `npm run dev`
     for the web app. Both in the background.
  3. Seed the LOCAL DB directly with SQL (psql / psycopg — NOT via the API):
       - a normal bot row (its own crypto_universe + one quarantined symbol) -> screenshot A;
       - an off-universe `alpaca_trades` row (TRUMP/USD, status='open', timestamp = an ISO string)
         for that bot -> screenshot B (LEAK banner);
       - a bot row whose quarantined_symbols covers its entire crypto_universe -> screenshot C
         (STARVATION banner). Use a DEDICATED test bot row for this, not a real bot id.
  4. Open http://localhost:<web port>/bots in a real browser (chromium-cli / Playwright CLI) and
     screenshot each state. Look at each screenshot — a blank/error card is NOT done.
  5. Save to .planning/phases/16-effective-universe-dashboard/evidence/ and reference in the SUMMARY.
  6. Tear down: drop/clean the LOCAL seed rows. Nothing to revert in prod, because prod was never touched.
  </action>
  <verify>
    <automated>cd dashboard/web && npm run build 2>&1 | tail -5</automated>
    <human-check>Screenshots in .planning/phases/16-effective-universe-dashboard/evidence/ show, from a LOCALHOST url: (A) normal card with effective chips + struck-through blocked chips + visible reasons + the `N of M tradeable` count, (B) the LEAK banner naming the off-universe symbol with its exposure counts, (C) the STARVATION banner. Each screenshot's URL bar shows localhost, not app.aipredictedwins.com.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds; `git diff dashboard/web/components/bots/BotCard.tsx` is an import line + a one-line swap.
    - Three screenshots captured, all against localhost + the local/TEST DB.
    - ZERO writes to the production database and ZERO API calls to the deployed dashboard — state this explicitly in the SUMMARY.
    - The raw `Assets:` config string no longer appears on the card.
  </acceptance_criteria>
  <done>An operator can see, at a glance, exactly what each bot can trade — and gets shouted at when a symbol leaks or the bot is starved — proven without ever touching prod.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser → API | authenticated fetch via useAPI (401 -> /login); the panel is read-only |
| panel → set math | NONE — the browser receives effective/blocked already computed by the gate-delegating resolver |
| evidence capture → prod | NONE — local stack + local DB only (B4) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-09 | Repudiation | the browser re-derives effective = allowlist − quarantined and drifts from the gate | mitigate | acceptance criteria grep UniversePanel.tsx for `.filter(`/`.includes(` deciding allow-vs-block; the payload is rendered verbatim |
| T-16-11 | Repudiation | a degraded backend state (shadow sets / exposure unavailable) renders as a confident all-clear | mitigate | three mandatory footnotes; exposure_loaded===false explicitly says "UNKNOWN, not clear" |
| T-16-14 | Denial of Service | screenshot evidence starves a LIVE bot (quarantining its whole universe) or seeds a phantom open position that Phase-13/14 reconciliation ingests | mitigate | B4 — all evidence against a local stack + local/TEST DB; no PUT to the deployed API; no prod INSERT; explicit no-prod-writes attestation in the SUMMARY |
| T-16-10 | Information disclosure | the panel renders a field carrying an alpaca key | mitigate | BotUniverse has no key field (Plan 03 T-16-02); the panel reads only BotUniverse + bot.bot_id |
| T-16-SC | Tampering | package installs | accept | ZERO new packages; `git diff --stat dashboard/web/package.json` must be empty |
</threat_model>

<verification>
- `cd dashboard/web && npm run build` succeeds (the type-check gate — there is no frontend test framework).
- `git diff --stat dashboard/web/package.json dashboard/web/components/shared/Badge.tsx` — both EMPTY.
- `python -m pytest tests/ dashboard/api/tests/ -q` — still green (frontend-only plan).
- Screenshots (normal / LEAK / STARVATION) under .planning/phases/16-effective-universe-dashboard/evidence/,
  all from localhost against the local/TEST database. Prod untouched.
</verification>

<success_criteria>
The bot card no longer shows a raw config string that lies about what the bot trades. It shows the
effective set, the blocked set with visible reasons (including the DOT/LINK/ETH shadow-untradeable case
on confluence bots), a tradeable count, two loud alarms — leak and starvation — and honest footnotes when
a signal is unavailable. Proven by screenshot, with prod never touched.
</success_criteria>

<output>
Create `.planning/phases/16-effective-universe-dashboard/16-04-SUMMARY.md` when done, including the
explicit attestation: no writes to the production database, no calls to the deployed dashboard.
</output>
</content>
