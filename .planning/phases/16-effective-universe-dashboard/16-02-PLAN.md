---
phase: 16-effective-universe-dashboard
plan: 02
type: execute
wave: 1
depends_on: [16-01]
files_modified:
  - src/effective_universe.py
autonomous: true
requirements: [UNIV-03]

must_haves:
  truths:
    - "src/effective_universe.py exists: a pure resolver with ZERO I/O — no DB, no Alpaca, no HTTP, no logging, no filesystem. Its only inputs are a BotConfig, an exposure map, and (optionally) the two shadow deny-lists."
    - "resolve_universe DELEGATES every allow/block verdict for quarantine + allowlist to src.universe.entry_allowed — it NEVER re-derives the set math (VALIDATION case 13, the anti-lie invariant)"
    - "allowlist_for(cfg) mirrors the four gate call sites exactly: copytrade -> cfg.all_symbols; trend_btc -> cfg.symbols + [cfg.trend_symbol]; otherwise -> cfg.symbols"
    - "The shadow deny-lists (MEME_CRYPTO, _ALPACA_UNTRADEABLE) are SURFACED as blocked reasons 'meme'/'untradeable' (Decision 3b) via a LAZY, try/except-guarded import — they are NOT refactored, NOT copied, NOT moved to the DB (that is Phase 17/18)"
    - "Reason precedence is deterministic and documented: quarantined > off_universe > meme > untradeable"
    - "Cases 1-13 of tests/test_effective_universe.py are GREEN"
  artifacts:
    - path: "src/effective_universe.py"
      provides: "resolve_universe() + allowlist_for() — the single source of the dashboard's universe view"
      contains: "def resolve_universe"
      min_lines: 90
  key_links:
    - from: "src/effective_universe.py"
      to: "src.universe.entry_allowed"
      via: "every quarantine/allowlist verdict is delegated to the gate of record"
      pattern: "from src.universe import|entry_allowed\\("
    - from: "src/effective_universe.py"
      to: "src.alpaca_evaluator.MEME_CRYPTO + src.alpaca_orchestrator._ALPACA_UNTRADEABLE"
      via: "lazy function-local import inside _load_shadow_sets(), try/except-guarded"
      pattern: "_ALPACA_UNTRADEABLE|MEME_CRYPTO"
---

<objective>
Build `src/effective_universe.py` — the pure resolver that answers "what can this bot ACTUALLY trade
right now, and why not the rest?" It is the one place the answer is computed; the API route (Plan 03)
and the panel (Plan 04) are thin transports over it.

Purpose: turn cases 1-13 GREEN. The resolver must delegate to `src.universe.entry_allowed` so the
dashboard is, by construction, incapable of disagreeing with the gate — and must additionally subtract
the two SHADOW deny-lists (`MEME_CRYPTO`, `_ALPACA_UNTRADEABLE`) that `src/bot_thread.py:144-145,163-164`
applies on top of the gate, because their default already removes DOT/USD, LINK/USD and ETH/USD from
every default confluence bot's scan (CONTEXT Decision 3b).
Output: src/effective_universe.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-effective-universe-dashboard/16-CONTEXT.md
@.planning/phases/16-effective-universe-dashboard/16-RESEARCH.md
@tests/test_effective_universe.py

<interfaces>
The gate of record — REUSE, never reimplement [src/universe.py]:

  normalize(symbol: str | None) -> str
      "BTC/USD" / "btc/usd" / "BTCUSD" -> "BTCUSD"; "SPY" -> "SPY". Idempotent, total.

  entry_allowed(symbol, allowlist, quarantined) -> tuple[bool, str | None]
      reason in {None, "off_universe", "quarantined"}.
      Quarantine is checked BEFORE the allowlist. An EMPTY allowlist means "no restriction".

Config [src/bot_config.py:72-100]:
  cfg.symbols      -> stock_universe.split(",") if asset_class=="stock" else crypto_universe.split(",")
  cfg.all_symbols  -> order-stable dedup UNION of crypto ∪ stock  (the copytrade allowlist)
  cfg.quarantined  -> quarantined_symbols.split(",")  (flat, asset-class-AGNOSTIC)
  cfg.strategy     -> "confluence" (default) | "trend_btc" | "tradingagents" | "copytrade"
  cfg.trend_symbol -> "BITX"

The four gate call sites this resolver MUST mirror [16-RESEARCH.md §3, table]:
  confluence     src/bot_thread.py:355        allowlist = cfg.symbols
  trend_btc      src/trend_strategy.py:103-104 allowlist = list(cfg.symbols) + [cfg.trend_symbol]
  tradingagents  src/bot_c/strategy.py:289    allowlist = cfg.symbols
  copytrade      src/copytrade_thread.py:397-398 allowlist = cfg.all_symbols

The SHADOW deny-lists (Decision 3b) — exact import sites, DO NOT MOVE OR REDEFINE THEM:
  MEME_CRYPTO          src/alpaca_evaluator.py:42
  _ALPACA_UNTRADEABLE  src/alpaca_orchestrator.py:79-84  (env ALPACA_UNTRADEABLE; default contains
                                                          DOT/USD, LINK/USD, ETH/USD)
  Applied to candidates at src/bot_thread.py:144-145 and :163-164, as raw slashed-string membership.
  NOTE the dashboard container runs the FastAPI API *and* BotManager in ONE process (CLAUDE.md), and
  BotManager -> bot_thread already imports both symbols at src/bot_thread.py:75,78 — so a lazy import
  here is a module-cache hit against the SAME env the threads use. The research's "the dashboard env
  may differ" worry does not apply to the deployed topology; the try/except below covers the CLI/dev
  case where the import is unavailable.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: allowlist_for() + the shadow-set lazy loader</name>
  <files>src/effective_universe.py</files>
  <read_first>src/universe.py (the module docstring style + the pure/total posture to match); src/bot_config.py L72-100; src/bot_thread.py L75-78 (the exact import lines for MEME_CRYPTO and _ALPACA_UNTRADEABLE) and L138-168 (how they are applied); 16-RESEARCH.md §3</read_first>
  <behavior>
    - allowlist_for(cfg) -> list[str], dispatching ONLY on cfg.strategy:
        "copytrade"  -> list(cfg.all_symbols)
        "trend_btc"  -> list(cfg.symbols) + [cfg.trend_symbol]   (dedup if trend_symbol already present)
        otherwise    -> list(cfg.symbols)
      Order-stable, original spelling preserved. No thread, no DB, no BotManager.
    - _load_shadow_sets() -> tuple[frozenset[str], frozenset[str], bool]
        Function-local imports: `from src.alpaca_evaluator import MEME_CRYPTO`,
        `from src.alpaca_orchestrator import _ALPACA_UNTRADEABLE`. Wrapped in try/except Exception
        (NOT bare ImportError — the orchestrator module reads env at import and could raise otherwise).
        On success -> (frozenset(MEME_CRYPTO), frozenset(_ALPACA_UNTRADEABLE), True).
        On failure -> (frozenset(), frozenset(), False)  — the caller reports shadow_sets_loaded=False
        so the panel says "shadow deny-lists unavailable" rather than silently claiming more symbols
        are tradeable than really are.
  </behavior>
  <action>Create src/effective_universe.py with a module docstring that (a) states it is pure and zero-I/O, (b) states that all quarantine/allowlist verdicts are delegated to src.universe.entry_allowed and MUST NOT be re-derived, (c) cites the four gate call sites by file:line, and (d) cites src/alpaca_evaluator.py:42 and src/alpaca_orchestrator.py:79-84 as the shadow-set sources plus src/bot_thread.py:144-145,163-164 as where they are enforced, and records that consolidating them into `quarantined_symbols` is Phase 17/18 — NOT this phase. Implement allowlist_for() and _load_shadow_sets() per the behavior above. Do NOT import anything from src.alpaca_* at module level (it pulls the Alpaca SDK and reads env — keep this module importable with zero side effects, like src/universe.py).</action>
  <verify>
    <automated>python -m pytest tests/test_effective_universe.py -q -k "allowlist or strategy or trend_carveout or copytrade_union or bot_c" 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Cases 5, 6, 7, 8 GREEN.
    - `python -c "import src.effective_universe"` completes with no Alpaca SDK import and no env read.
    - No module-level import of src.alpaca_evaluator / src.alpaca_orchestrator.
  </acceptance_criteria>
  <done>The per-strategy allowlist mirrors the gate, and the shadow sets can be read without poisoning module import.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: resolve_universe() — effective / blocked / starvation / leak</name>
  <files>src/effective_universe.py</files>
  <read_first>16-VALIDATION.md cases 1-4, 9-13; src/universe.py:26 (entry_allowed's exact reason strings — reuse them verbatim, never relabel); 16-RESEARCH.md §5 (leak semantics + the normalization trap)</read_first>
  <behavior>
    - Signature:
        resolve_universe(cfg, *, exposure=None, meme=None, untradeable=None) -> dict
      exposure: dict keyed by NORMALIZED symbol -> {"open": int, "recent": int, "display": str}.
      meme/untradeable: injectable iterables; when BOTH are None, call _load_shadow_sets().
      When either is passed explicitly, shadow_sets_loaded is True (the caller supplied them).
    - Algorithm (single pass, zero duplicated set math):
        allow = allowlist_for(cfg); quar = cfg.quarantined
        candidates = allow + [q for q in quar not already in allow]
                          + [exposure display symbols not already present]
          -> order-stable, deduped by normalize(); display spelling = the FIRST spelling seen
             (allowlist spelling wins over the exposure spelling).
        for each candidate symbol s:
            ok, reason = entry_allowed(s, allow, quar)        # THE GATE — never re-derive
            if not ok:  -> blocked with reason (verbatim: "quarantined" | "off_universe")
            elif normalize(s) in {normalize(m) for m in meme}:        -> blocked, reason "meme"
            elif normalize(s) in {normalize(u) for u in untradeable}: -> blocked, reason "untradeable"
            else: -> effective
        Precedence therefore falls out of the branch order: quarantined > off_universe > meme > untradeable.
        (entry_allowed itself already puts quarantined ahead of off_universe.)
    - Every blocked entry carries open_positions / recent_trades pulled from exposure[normalize(s)]
      (default 0/0 when absent).
    - starvation = (effective == [])
    - leak = [display symbols where reason == "off_universe" AND (open_positions > 0 or recent_trades > 0)]
      A QUARANTINED symbol holding an open position is NOT a leak (expected wind-down) — case 11.
    - Return dict exactly as specified in 16-01's <interfaces> block, including shadow_sets_loaded.
  </behavior>
  <action>Implement resolve_universe() in src/effective_universe.py. Normalize on BOTH sides of every comparison via src.universe.normalize — never compare raw strings (alpaca_trades stores mixed 'BTC/USD' and 'BTCUSD'; src/bot_thread.py compares the shadow sets raw, so normalizing here is a strict superset of the thread's behavior and cannot under-report a block). Preserve original display spelling in allowlist/effective/blocked/leak. Keep the function total: no exceptions for empty universes, empty quarantine, or a None exposure. Do not log, do not touch the DB, do not construct an Alpaca client. Smallest diff — this is one new file plus nothing else.</action>
  <verify>
    <automated>python -m pytest tests/test_effective_universe.py -q -k "not route and not openapi" 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Cases 1-13 GREEN (route cases 14-17 still fail/skip — they are Plan 03).
    - Case 13 (test_resolver_agrees_with_gate) GREEN — proof the resolver cannot drift from the gate.
    - Case 3 GREEN — DOT/LINK/ETH report reason="untradeable"; DOGE reports "meme".
    - grep shows entry_allowed( is called inside resolve_universe; there is NO independent
      `in allowlist` / `not in quarantined` membership test making an allow/block decision.
    - `python -m pytest tests/ -q` — no new failures elsewhere.
  </acceptance_criteria>
  <done>One pure function answers the effective-universe question, and it physically cannot disagree with the gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| resolver → gate (src.universe) | in-process, pure; the gate is the authority, the resolver is a reporter |
| resolver → shadow sets | lazy import; failure is REPORTED (shadow_sets_loaded=False), never silently swallowed into an over-optimistic answer |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Repudiation | resolver re-derives the set math and drifts from the gate | mitigate | every verdict routed through entry_allowed; case 13 asserts it; acceptance criteria greps for a bypass |
| T-16-04 | Denial of Service | a module-level `import src.alpaca_orchestrator` in the API process crashes startup / pulls the Alpaca SDK | mitigate | lazy function-local import inside _load_shadow_sets(), try/except Exception, degraded-but-honest fallback |
| T-16-05 | Information disclosure | the resolver reads alpaca keys off the cfg | accept | it never touches cfg.alpaca_api_key/secret; the route's SELECT (Plan 03) omits them entirely |
| T-16-SC | Tampering | package installs | accept | no new packages |
</threat_model>

<verification>
- `python -m pytest tests/test_effective_universe.py -q` — cases 1-13 GREEN, 14-17 RED/SKIP.
- `python -m pytest tests/ -q` — no new failures (Phase-15 baseline preserved).
- `python -c "import src.effective_universe"` — no side effects, no Alpaca SDK load.
</verification>

<success_criteria>
The effective universe is computed in exactly one place, by delegating to the gate of record, with the
two shadow deny-lists surfaced (not hidden, not refactored). The dashboard now has a truth to render.
</success_criteria>

<output>
Create `.planning/phases/16-effective-universe-dashboard/16-02-SUMMARY.md` when done.
</output>
</content>
