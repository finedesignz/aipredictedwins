---
phase: 15-universe-hard-gate
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - src/universe.py
  - dashboard/api/migrations/018_universe_quarantine.sql
  - src/db_schema.sql
  - src/bot_config.py
  - dashboard/api/routes/bots.py
  - dashboard/api/models.py
  - dashboard/api/seed_bots.py
autonomous: true
requirements: [UNIV-01, UNIV-02]

must_haves:
  truths:
    - "src/universe.py exposes pure normalize() + entry_allowed() with zero I/O; the pure cases of tests/test_universe.py go GREEN"
    - "bots.quarantined_symbols TEXT DEFAULT '' exists via additive/idempotent migration 018, mirrored in src/db_schema.sql"
    - "BotConfig carries quarantined_symbols + a `quarantined` property; a row missing the key yields [] (pre-migration DB fails safe)"
    - "The column round-trips through the API: POST creates it, PUT sets it, GET echoes it (_BOT_COLS + all 3 Pydantic models)"
  artifacts:
    - path: "src/universe.py"
      provides: "pure normalize + entry_allowed predicate"
      exports: ["normalize", "entry_allowed"]
      min_lines: 20
    - path: "dashboard/api/migrations/018_universe_quarantine.sql"
      provides: "ADD COLUMN IF NOT EXISTS bots.quarantined_symbols TEXT DEFAULT ''"
      contains: "ADD COLUMN IF NOT EXISTS quarantined_symbols"
  key_links:
    - from: "src/bot_config.py"
      to: "bots.quarantined_symbols"
      via: "from_row row.get('quarantined_symbols') or ''"
      pattern: "quarantined_symbols"
    - from: "dashboard/api/routes/bots.py"
      to: "bots.quarantined_symbols"
      via: "_BOT_COLS projection + POST INSERT column list"
      pattern: "quarantined_symbols"
---

<objective>
Build the pure gate module and land the quarantine column end-to-end (DB → BotConfig → API), with
zero behavior change to any running bot. No gate is wired yet — Plan 03 does that. After this plan
the quarantine column exists and is inert (default `''` = nothing quarantined = current behavior).

Purpose: UNIV-02 (config-driven quarantine, zero code change to drop a symbol) + the pure predicate
UNIV-01 depends on.
Output: src/universe.py, migration 018 (+ db_schema mirror), BotConfig field/property, column plumbing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-universe-hard-gate/15-CONTEXT.md
@.planning/phases/15-universe-hard-gate/15-RESEARCH.md
@CLAUDE.md
@tests/test_universe.py

<interfaces>
Contract the RED tests pin (Plan 01) — implement exactly:

  normalize(symbol) -> str
      uppercase + strip "/" + strip whitespace. Total: normalize(None) / normalize("") -> "".
  entry_allowed(symbol, allowlist, quarantined) -> (bool, reason)
      1. quarantine check FIRST  -> (False, "quarantined")
      2. allowlist check second  -> (False, "off_universe")  [only when allowlist is non-empty]
      3. otherwise               -> (True, None)
      Pure, total, no raise, no I/O.

  BotConfig.quarantined_symbols: str = ""     (new field, after trend_benchmark)
  BotConfig.quarantined -> list[str]          (splits on ",", strips, drops empties;
                                               asset-class-AGNOSTIC — a flat deny-list)

Migration precedent to copy verbatim — dashboard/api/migrations/004_stock_universe.sql:4-5:
  ALTER TABLE bots
      ADD COLUMN IF NOT EXISTS stock_universe TEXT DEFAULT '...';

Pure-module precedent: src/order_resolution.py (Phase 11/14) + its delegate at src/bot_thread.py:266-268.
Import convention: `from src.universe import entry_allowed, normalize`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/universe.py — the pure gate</name>
  <files>src/universe.py</files>
  <read_first>src/order_resolution.py (the pure-module precedent: module docstring, no imports beyond stdlib, total functions); 15-RESEARCH.md §3 (symbol formats table — why normalize strips the slash) and Pattern 1</read_first>
  <behavior>
    - normalize collapses BTC/USD, btc/usd, BTCUSD to "BTCUSD"; leaves SPY as "SPY"; idempotent; total
    - entry_allowed applies quarantine BEFORE allowlist; empty allowlist == no restriction
    - reason is exactly one of None / "off_universe" / "quarantined"
  </behavior>
  <action>Create src/universe.py — a stdlib-only module with a docstring naming Phase 15 / UNIV-01 / UNIV-02. Implement normalize(symbol: str) -> str and entry_allowed(symbol, allowlist, quarantined) -> tuple[bool, str | None] per the interfaces contract. Both functions must be pure and total (never raise — a None/empty symbol normalizes to ""). Normalize every member of allowlist and quarantined at compare time so callers may pass any format. Add no classes, no config reads, no logging, no I/O — this module must be importable with zero side effects. Nothing else in this task.</action>
  <verify>
    <automated>python -m pytest tests/test_universe.py -q -k "normalize or allowed or blocked or quarantine or trend_symbol or empty" 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - The pure cases from Plan 01 (VALIDATION 1,2,3,4,5,12 + empty-allowlist) are GREEN.
    - src/universe.py imports nothing outside the stdlib; no I/O; no logging.
    - Quarantine-before-allowlist precedence holds.
  </acceptance_criteria>
  <done>The gate predicate exists and is exhaustively pinned by unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 018 + db_schema mirror + BotConfig quarantine field</name>
  <files>dashboard/api/migrations/018_universe_quarantine.sql, src/db_schema.sql, src/bot_config.py</files>
  <read_first>dashboard/api/migrations/004_stock_universe.sql (the exact ADD COLUMN IF NOT EXISTS shape) and 017_reconciliation.sql:1-18 (header comment style + the "NO bot_id CHECK" warning); src/bot_config.py L38-71 (from_row + the `symbols` property to mirror); src/db_schema.sql L8-15 (the STALE bots block)</read_first>
  <action>Three additive edits.
(a) Create dashboard/api/migrations/018_universe_quarantine.sql: a header comment (Phase 15, UNIV-02, additive + idempotent, safe to apply before the new orchestrator code deploys) then a single `ALTER TABLE bots ADD COLUMN IF NOT EXISTS quarantined_symbols TEXT DEFAULT '';`. NO constraint changes, NO data migration, NO DELETE/DROP/TRUNCATE (CLAUDE.md hard rule). Note in the header that the format must match crypto_universe (`BTC/USD`, slash included) — a bare `BTC` will not match.
(b) src/db_schema.sql: add `quarantined_symbols TEXT DEFAULT ''` to the `bots` CREATE TABLE with a `-- Phase 15 (mirror of migration 018)` comment, plus a one-line pointer comment noting that the remaining per-bot config columns live in migrations 002+ and this block is not a full mirror. Do NOT touch the `CHECK (id IN ('A','B'))` and do NOT reconcile the rest of the stale block (out of scope, would risk C/D/E).
(c) src/bot_config.py: add field `quarantined_symbols: str = ""` after `trend_benchmark`; add `quarantined_symbols=row.get("quarantined_symbols") or ""` to from_row; add a `quarantined` property mirroring `symbols` (L66-71) but asset-class-agnostic: `[s.strip() for s in self.quarantined_symbols.split(",") if s.strip()]`. A pre-migration row (key absent) must yield "" -> [] (fails safe: nothing quarantined).</action>
  <verify>
    <automated>python -m pytest tests/test_universe.py -q -k bot_config 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Migration 018 is a bare additive `ADD COLUMN IF NOT EXISTS`; re-running it is a no-op; no constraint touched.
    - src/db_schema.sql bots block gains only the one column + comments.
    - test_bot_config_quarantined is GREEN, including the missing-key -> [] case.
    - Existing tests (tests/test_bot_config.py, tests/test_risk_hardening.py) still pass.
  </acceptance_criteria>
  <done>Quarantine policy is storable in the DB and readable by every bot's BotConfig.</done>
</task>

<task type="auto">
  <name>Task 3: Column plumbing — API routes, Pydantic models, seed_bots</name>
  <files>dashboard/api/routes/bots.py, dashboard/api/models.py, dashboard/api/seed_bots.py</files>
  <read_first>15-RESEARCH.md §4 (the exact file:line checklist + Pitfall 3 `_BOT_COLS` drift); dashboard/api/routes/bots.py L20-24 (_BOT_COLS) and L82-90 (POST INSERT); dashboard/api/models.py L205-272 (BotFull/BotCreate/BotUpdate); dashboard/api/seed_bots.py L26-118 (per-bot dicts) and L136-169 (INSERT + the key-patch UPDATE branch)</read_first>
  <action>Thread `quarantined_symbols` through the four writers/readers of `bots` columns.
- dashboard/api/routes/bots.py: add `quarantined_symbols` to the `_BOT_COLS` projection (else GET never echoes the value — Pitfall 3) AND to the POST /api/bots INSERT column list + VALUES placeholder. The PUT needs NO SQL change — it builds set_clauses from non-None BotUpdate fields, so `""` (clear the list) is expressible while `None` means "leave alone". Do not widen set_clauses to accept arbitrary keys (keeps the no-injection property: column names come from the fixed Pydantic schema, values are bound params).
- dashboard/api/models.py: `quarantined_symbols: str = ""` on BotFull and BotCreate; `quarantined_symbols: Optional[str] = None` on BotUpdate.
- dashboard/api/seed_bots.py: add `"quarantined_symbols": os.environ.get("BOT_<X>_QUARANTINED", "")` to each per-bot dict (A/B/C/D) and add the column to the INSERT column list + VALUES. Do NOT widen the already-exists UPDATE branch (it is a key-patch only — widening it would clobber live operator edits).
Default everywhere is `""` = nothing quarantined = zero behavior change on deploy.</action>
  <verify>
    <automated>python -m pytest tests/ -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `quarantined_symbols` appears in _BOT_COLS, the POST INSERT, all three Pydantic models, and the seed INSERT.
    - The seed's existing-bot UPDATE branch is unchanged (still key-patch only).
    - Full suite green; no existing test regressed.
    - grep -v '^--' dashboard/api/migrations/018_universe_quarantine.sql | grep -ci 'drop\|truncate\|delete' == 0.
  </acceptance_criteria>
  <done>An operator can quarantine a symbol with one API write, and every reader sees it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| operator → PUT /api/bots/{id} | free-text `quarantined_symbols` reaches a parameterized UPDATE |
| dashboard boot → Postgres | migration 018 applies DDL |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Tampering | SQL injection via the new dynamic set_clauses column | mitigate | column names derive from the fixed BotUpdate Pydantic schema; the value is a bound `%(col)s` param. Do not widen set_clauses to arbitrary keys. |
| T-15-05 | Denial of Service | a fat-fingered quarantine write silently halts a bot | mitigate | Plan 03 logs the effective normalized quarantine/allowlist set at config swap; migration header documents the required `BTC/USD` format |
| T-15-06 | Tampering | migration alters/drops existing data | mitigate | bare `ADD COLUMN IF NOT EXISTS`, no constraint touched, no DELETE/DROP/TRUNCATE (CLAUDE.md) |
| T-15-SC | Tampering | package installs | accept | no new packages |
</threat_model>

<verification>
- `python -m pytest tests/test_universe.py -q` — pure + bot_config cases GREEN (wiring cases still RED until Plan 03).
- `python -m pytest tests/ -q` — full suite green, no regression.
- Idempotency: applying 018 twice is a no-op (`ADD COLUMN IF NOT EXISTS`).
</verification>

<success_criteria>
The pure gate exists and is proven; quarantine is a DB column readable by BotConfig and writable via
the API; nothing is gated yet and no running bot changes behavior.
</success_criteria>

<output>
Create `.planning/phases/15-universe-hard-gate/15-02-SUMMARY.md` when done.
</output>
</content>
