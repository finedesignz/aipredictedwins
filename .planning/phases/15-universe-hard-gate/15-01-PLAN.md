---
phase: 15-universe-hard-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_universe.py
autonomous: true
requirements: [UNIV-01, UNIV-02]

must_haves:
  truths:
    - "tests/test_universe.py exists with all 16 VALIDATION cases as RED tests using zero-network fakes (FakeAlpacaClient/FakeLogger from tests/test_order_resolution.py conventions)"
    - "Tests import the not-yet-existing surfaces (src.universe.normalize, src.universe.entry_allowed, BotConfig.quarantined) so they fail RED for the right reason (missing impl, not malformed test)"
    - "Case 11 (exits never gated) asserts close_position/place_market_order(side='sell') on a quarantined open position still executes — this is the safety property of the whole phase"
    - "Case 12 (trend carve-out) pins that cfg.trend_symbol (BITX) passes even though it is not in cfg.stock_universe"
  artifacts:
    - path: "tests/test_universe.py"
      provides: "RED test suite covering UNIV-01 (12 cases) + UNIV-02 (4 cases)"
      contains: "def test_off_universe_blocked"
      min_lines: 150
  key_links:
    - from: "tests/test_universe.py"
      to: "src.universe.entry_allowed"
      via: "import + call the pure predicate under test"
      pattern: "from src.universe import|entry_allowed"
    - from: "tests/test_universe.py"
      to: "src.bot_thread.BotThread._submit_order"
      via: "call _submit_order with a blocked symbol, assert no Alpaca call + one rejected row"
      pattern: "_submit_order"
---

<objective>
Write the RED test suite that pins the Phase-15 universe hard-gate contract before any
implementation. This is Wave 0 — every downstream plan turns these tests GREEN.

Purpose: Nyquist gate for UNIV-01 + UNIV-02. All 16 VALIDATION cases become executable specs.
The single most important spec is case 11: **exits are never gated** — the gate must be provably
incapable of stranding an open position in a quarantined symbol.
Output: tests/test_universe.py (RED).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-universe-hard-gate/15-CONTEXT.md
@.planning/phases/15-universe-hard-gate/15-RESEARCH.md
@.planning/phases/15-universe-hard-gate/15-VALIDATION.md
@CLAUDE.md

<interfaces>
Surfaces the tests must import (built in Plans 02/03 — RED now):

  src.universe.normalize(symbol: str) -> str
      "BTC/USD" -> "BTCUSD"; "btc/usd" -> "BTCUSD"; "SPY" -> "SPY". Idempotent. Total (never raises).

  src.universe.entry_allowed(symbol, allowlist, quarantined) -> tuple[bool, str | None]
      reason in {None, "off_universe", "quarantined"}.
      Quarantine check PRECEDES the allowlist check (a symbol both in-universe and quarantined
      returns reason == "quarantined").
      An EMPTY allowlist means "no allowlist restriction" (quarantine still applies).

  src.bot_config.BotConfig.quarantined_symbols: str = ""      (field)
  src.bot_config.BotConfig.quarantined -> list[str]           (property; splits on ",", strips,
      drops empties; asset-class-agnostic, unlike `symbols`)

Existing surfaces the tests exercise (already in tree — do NOT change them in this plan):
  src.bot_thread.BotThread._submit_order(alpaca, logger, *, symbol, qty, side, order_type,
                                         trade_data, limit_price=None) -> (trade_id, order)
      NOTE: signature takes NO cfg — the gate reads self.config. Tests MUST keep calling it
      positionally/keyword-identically to tests/test_order_resolution.py:156 and :246.
  src.bot_thread.select_long_candidates(signals, cfg, open_symbols, recent_loss_symbols)
  src.bot_thread.select_short_candidates(signals, cfg, open_symbols, recent_loss_symbols)
  src.alpaca_client.AlpacaClient.close_position(symbol)  — MUST remain ungated.

Fakes to copy/extend (tests/test_order_resolution.py L95-143):
  FakeAlpacaClient  — records .orders; add a .closed list recording close_position() calls
  FakeLogger        — records .rows (log_alpaca_trade) and .updates (update_alpaca_trade)
  _bot()            — builds a BotThread with a default BotConfig (default 8-asset universe)

Postgres integration guard idiom (case 16):
  @pytest.mark.skipif(not os.environ.get("DATABASE_URL"), reason="needs Postgres")
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — pure-module cases (1-5, 12, 15) in tests/test_universe.py</name>
  <files>tests/test_universe.py</files>
  <read_first>15-VALIDATION.md (the 16-case table — exact test names); 15-RESEARCH.md §3 (symbol formats per layer) and Pattern 1 (the entry_allowed shape); src/bot_config.py L66-71 (`symbols` property — the `quarantined` property mirrors it)</read_first>
  <behavior>
    - test_allowed_symbol_passes: entry_allowed("ETH/USD", ["BTC/USD","ETH/USD"], []) -> (True, None)
    - test_off_universe_blocked: entry_allowed("TRUMP/USD", 8-asset list, []) -> (False, "off_universe")
    - test_quarantined_blocked: entry_allowed("BTC/USD", 8-asset list, ["BTC/USD"]) -> (False, "quarantined")
      AND quarantine wins over allowlist membership (precedence assertion)
    - test_normalize_formats: normalize("BTC/USD") == normalize("BTCUSD") == normalize("btc/usd") == "BTCUSD";
      normalize("SPY") == "SPY"; normalize("") == ""; normalize(x) is idempotent
    - test_empty_quarantine_noop: quarantined=[] and quarantined=[""] both block nothing
    - test_empty_allowlist_allows: allowlist=[] -> (True, None) for any symbol (Decision 4 safety net),
      but a quarantined symbol is still blocked
    - test_trend_symbol_allowed: allowlist = cfg.symbols | {cfg.trend_symbol} -> "BITX" -> (True, None)
      while "BITX" against a bare cfg.symbols (stock_universe, no BITX) -> (False, "off_universe")
    - test_bot_config_quarantined: BotConfig.from_row({...,"quarantined_symbols":"BTC/USD, FIL/USD"}).quarantined
      == ["BTC/USD","FIL/USD"]; a row with the key MISSING or None -> [] (pre-migration DB fails safe);
      "" -> []
  </behavior>
  <action>Create tests/test_universe.py. Import the pure surfaces (`from src.universe import normalize, entry_allowed`) and `from src.bot_config import BotConfig`. Write the cases above under the exact VALIDATION names (1,2,3,4,5,12,15 plus the empty-allowlist carve-out). Use plain literals — no DB, no network, no mocks. These fail RED with ImportError on src.universe / AttributeError on BotConfig.quarantined. Do NOT write any production code in this plan.</action>
  <verify>
    <automated>python -m pytest tests/test_universe.py -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - File collects (no SyntaxError); cases fail RED on missing src.universe / BotConfig.quarantined, not on malformed asserts.
    - Quarantine-precedes-allowlist precedence is asserted explicitly.
    - Empty-allowlist-allows-everything is asserted explicitly (Decision 4).
    - Trend carve-out (BITX) asserted both ways (with and without the union).
  </acceptance_criteria>
  <done>The pure gate contract is an executable RED spec.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED — wiring cases (6-11, 13, 14, 16) with zero-network fakes</name>
  <files>tests/test_universe.py</files>
  <read_first>tests/test_order_resolution.py L95-170 and L236-258 (FakeAlpacaClient/FakeLogger/_bot + the single-rejected-row assertion to mirror); src/bot_thread.py L328-359 (_submit_order — the existing rejected-row shape) and L129-164 (the selectors); 15-RESEARCH.md §2 (exit paths) + Pitfall 5 (double-write)</read_first>
  <behavior>
    - test_submit_order_blocks_off_universe: bot cfg has the default 8-asset universe; _submit_order(symbol="TRUMP/USD", side="buy", order_type="market") -> returns (None, None); FakeAlpacaClient.orders == [] (NO Alpaca call); FakeLogger.rows has EXACTLY ONE row with status=="rejected" and pnl==0
    - test_submit_order_blocks_short_entry: same with side="sell" (a sell-to-OPEN is still an entry) -> blocked identically
    - test_submit_order_blocks_quarantined: cfg.quarantined_symbols="BTC/USD"; _submit_order(symbol="BTC/USD") -> blocked, one rejected row
    - test_submit_order_logs_rejection: caplog captures a WARNING containing bot_id, symbol, and the reason string
    - test_submit_order_allows_universe_symbol: symbol="ETH/USD" (in universe, not quarantined) -> place_market_order IS called; exactly one row, status=="submitted" (no regression, no extra rejected row)
    - test_select_long_universe_filter: signals for ETH/USD (allowed), TRUMP/USD (off-universe), BTC/USD (quarantined) -> select_long_candidates keeps only ETH/USD
    - test_select_short_universe_filter: same shape via select_short_candidates
    - test_exit_not_gated: a quarantined symbol with an OPEN position still closes — assert alpaca.close_position("BTC/USD") executes and is recorded, and that a direct place_market_order(side="sell") exit call (trend/bot_c/copytrade style) is NOT blocked. This test must fail if anyone puts the gate in AlpacaClient.
    - test_copytrade_entry_blocked: CopyTraderThread entry path with an off-universe mapped symbol (e.g. TRUMP/USD) -> no place_market_order call
    - test_bot_c_entry_blocked: bot_c entry path with an off-universe symbol -> no place_market_order call
    - test_quarantine_column_sql (DATABASE_URL-gated, skipif otherwise): information_schema shows bots.quarantined_symbols exists with column_default ''
  </behavior>
  <action>Extend tests/test_universe.py with the wiring cases. Copy FakeAlpacaClient/FakeLogger/_bot from tests/test_order_resolution.py and extend FakeAlpacaClient with a `closed` list recording close_position(symbol). Build BotConfig instances directly (frozen dataclass) with quarantined_symbols set where needed. For the copytrade/bot_c cases, drive the real production entry function with fakes (import CopyTraderThread / the bot_c entry helper) and assert the fake's order list stays empty — these fail RED now because no gate exists. Keep every test zero-network and under a second. Mirror tests/test_order_resolution.py:255's `len(logger.rows) == 1` guard so a gate that double-writes the rejected row fails. Do NOT write production code.</action>
  <verify>
    <automated>python -m pytest tests/test_universe.py -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - All 16 VALIDATION cases present under their exact names; collection succeeds.
    - Blocked-entry cases assert BOTH "no Alpaca call" AND "exactly one rejected row (pnl=0)".
    - test_exit_not_gated is written so it FAILS if the gate is ever placed in AlpacaClient.
    - The allowed-symbol case guards against a false block (regression guard for the existing suite).
    - DATABASE_URL-gated case skips cleanly when the env var is unset.
    - `python -m pytest tests/ -q` still shows the prior 336-passed baseline plus the new RED file (no existing test broken by the new file).
  </acceptance_criteria>
  <done>tests/test_universe.py holds the complete RED UNIV-01/UNIV-02 spec; Plans 02/03 turn it GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test process → Alpaca | none — fakes only, zero network |
| gated test → Postgres | DATABASE_URL-gated; skipped by default |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Denial of Service | gate strands an open position (exit blocked) | mitigate | test_exit_not_gated pins that close_position + sell-side exits are ungated; fails if the gate lands in AlpacaClient |
| T-15-02 | Repudiation | a blocked entry leaves no audit record | mitigate | assert exactly one terminal `rejected` row (pnl=0) + a WARNING carrying bot_id/symbol/reason |
| T-15-03 | Tampering | gate fails OPEN on an exception | mitigate | entry_allowed is pure + total; tests assert no try/except swallow around the gate |
| T-15-SC | Tampering | package installs | accept | no new packages — pytest already installed |
</threat_model>

<verification>
- `python -m pytest tests/test_universe.py -q` collects all 16 cases; all fail RED on missing impl.
- `python -m pytest tests/ -q` shows no NEW failures outside tests/test_universe.py.
</verification>

<success_criteria>
The 16 UNIV-01/UNIV-02 cases are executable RED specs, and the exits-are-never-gated safety property
is pinned by a test that fails if the gate is placed at the wrong layer.
</success_criteria>

<output>
Create `.planning/phases/15-universe-hard-gate/15-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
