---
phase: 15-universe-hard-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_universe.py
autonomous: true
requirements: [UNIV-01, UNIV-02]

must_haves:
  truths:
    - "tests/test_universe.py exists with all 18 VALIDATION cases as RED tests using zero-network fakes (FakeAlpacaClient/FakeLogger from tests/test_order_resolution.py conventions)"
    - "Tests import the not-yet-existing surfaces (src.universe.normalize, src.universe.entry_allowed, BotConfig.quarantined, BotConfig.all_symbols) so they fail RED for the right reason (missing impl, not malformed test)"
    - "Case 11 (exits never gated) + case 17 (static guard: 'entry_allowed' absent from src/alpaca_client.py) + case 18 (copytrade leader SELL on a HELD off-universe symbol still submits) are the automated proof that exits are never stranded"
    - "Case 12 (trend carve-out) pins that cfg.trend_symbol (BITX) passes even though it is not in cfg.stock_universe"
  artifacts:
    - path: "tests/test_universe.py"
      provides: "RED test suite covering UNIV-01 (14 cases) + UNIV-02 (4 cases)"
      contains: "def test_off_universe_blocked"
      min_lines: 170
  key_links:
    - from: "tests/test_universe.py"
      to: "src.universe.entry_allowed"
      via: "import + call the pure predicate under test"
      pattern: "from src.universe import|entry_allowed"
    - from: "tests/test_universe.py"
      to: "src.bot_thread.BotThread._submit_order"
      via: "call _submit_order with a blocked symbol, assert no Alpaca call + one rejected row"
      pattern: "_submit_order"
---

<objective>
Write the RED test suite that pins the Phase-15 universe hard-gate contract before any
implementation. This is Wave 0 — every downstream plan turns these tests GREEN.

Purpose: Nyquist gate for UNIV-01 + UNIV-02. All 18 VALIDATION cases become executable specs.
The most important specs are 11/17/18: **exits are never gated** — the gate must be provably
incapable of stranding an open position in a quarantined or off-universe symbol.
Output: tests/test_universe.py (RED).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-universe-hard-gate/15-CONTEXT.md
@.planning/phases/15-universe-hard-gate/15-RESEARCH.md
@.planning/phases/15-universe-hard-gate/15-VALIDATION.md
@CLAUDE.md

<interfaces>
Surfaces the tests must import (built in Plans 02/03 — RED now):

  src.universe.normalize(symbol: str) -> str
      "BTC/USD" -> "BTCUSD"; "btc/usd" -> "BTCUSD"; "SPY" -> "SPY". Idempotent. Total (never raises).

  src.universe.entry_allowed(symbol, allowlist, quarantined) -> tuple[bool, str | None]
      reason in {None, "off_universe", "quarantined"}.
      Quarantine check PRECEDES the allowlist check (a symbol both in-universe and quarantined
      returns reason == "quarantined").
      An EMPTY allowlist means "no allowlist restriction" (quarantine still applies).

  src.bot_config.BotConfig.quarantined_symbols: str = ""      (field)
  src.bot_config.BotConfig.quarantined -> list[str]           (property; splits on ",", strips,
      drops empties; asset-class-agnostic, unlike `symbols`)
  src.bot_config.BotConfig.all_symbols -> list[str]           (property; UNION of crypto_universe
      AND stock_universe — the COPYTRADE allowlist. Bot E mirrors across asset classes, so
      `symbols` (one asset class only) would wrongly block half its legitimate trades.)

Existing surfaces the tests exercise (already in tree — do NOT change them in this plan):
  src.bot_thread.BotThread._submit_order(alpaca, logger, *, symbol, qty, side, order_type,
                                         trade_data, limit_price=None) -> (trade_id, order)
      NOTE: signature takes NO cfg — the gate reads self.config. Tests MUST keep calling it
      positionally/keyword-identically to tests/test_order_resolution.py:156 and :246.
  src.bot_thread.select_long_candidates(signals, cfg, open_symbols, recent_loss_symbols)
  src.bot_thread.select_short_candidates(signals, cfg, open_symbols, recent_loss_symbols)
  src.bot_c.strategy._process_ticker(...)   — case 14 MUST drive this DIRECTLY with an
      off-universe ticker; the upstream _select_tickers would never yield one, which would make
      a higher-level test vacuous.
  src.copytrade_thread.CopyTraderThread._execute_signal(...)  — has NO open/close concept. The
      gate is SKIPPED entirely when normalize(mapped) is already in the live position set
      (a close/reduce), and applied only when the symbol is NOT held (a genuine open).
  src.alpaca_client.AlpacaClient.close_position(symbol)  — MUST remain ungated.

Fakes to copy/extend (tests/test_order_resolution.py L95-143):
  FakeAlpacaClient  — records .orders; add a .closed list recording close_position() calls and a
                      SCRIPTED get_positions() so the copytrade held/not-held branch is drivable
  FakeLogger        — records .rows (log_alpaca_trade) and .updates (update_alpaca_trade)
  _bot()            — builds a BotThread with a default BotConfig (default 8-asset universe)

Postgres integration guard idiom (case 16):
  @pytest.mark.skipif(not os.environ.get("DATABASE_URL"), reason="needs Postgres")
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — pure-module cases (1-5, 12, 15 + all_symbols union)</name>
  <files>tests/test_universe.py</files>
  <read_first>15-VALIDATION.md (the 18-case table — exact test names); 15-RESEARCH.md §3 (symbol formats per layer) and Pattern 1 (the entry_allowed shape); src/bot_config.py L66-71 (`symbols` property — `quarantined` and `all_symbols` mirror it)</read_first>
  <behavior>
    - test_allowed_symbol_passes: entry_allowed("ETH/USD", ["BTC/USD","ETH/USD"], []) -> (True, None)
    - test_off_universe_blocked: entry_allowed("TRUMP/USD", 8-asset list, []) -> (False, "off_universe")
    - test_quarantined_blocked: entry_allowed("BTC/USD", 8-asset list, ["BTC/USD"]) -> (False, "quarantined")
      AND quarantine wins over allowlist membership (precedence assertion)
    - test_normalize_formats: normalize("BTC/USD") == normalize("BTCUSD") == normalize("btc/usd") == "BTCUSD";
      normalize("SPY") == "SPY"; normalize("") == ""; normalize is idempotent
    - test_empty_quarantine_noop: quarantined=[] and quarantined=[""] both block nothing
    - test_empty_allowlist_allows: allowlist=[] -> (True, None) for any symbol (Decision 4 safety net),
      but a quarantined symbol is still blocked
    - test_trend_symbol_allowed: allowlist = cfg.symbols | {cfg.trend_symbol} -> "BITX" -> (True, None);
      "BITX" against a bare cfg.symbols (stock_universe, no BITX) -> (False, "off_universe")
    - test_bot_config_quarantined: BotConfig.from_row({...,"quarantined_symbols":"BTC/USD, FIL/USD"}).quarantined
      == ["BTC/USD","FIL/USD"]; key MISSING or None -> [] (pre-migration DB fails safe); "" -> []
    - test_bot_config_all_symbols: BotConfig.all_symbols spans BOTH universes regardless of
      cfg.asset_class (crypto pairs AND stock tickers present); "TRUMP/USD" is in NEITHER
  </behavior>
  <action>Create tests/test_universe.py. Import the pure surfaces (`from src.universe import normalize, entry_allowed`) and `from src.bot_config import BotConfig`. Write the cases above under the exact VALIDATION names. Plain literals — no DB, no network, no mocks. These fail RED with ImportError on src.universe / AttributeError on BotConfig.quarantined / BotConfig.all_symbols. Do NOT write production code in this plan.</action>
  <verify>
    <automated>python -m pytest tests/test_universe.py -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - File collects (no SyntaxError); cases fail RED on the missing surfaces, not on malformed asserts.
    - Quarantine-precedes-allowlist precedence asserted explicitly.
    - Empty-allowlist-allows-everything asserted explicitly (Decision 4).
    - Trend carve-out (BITX) asserted both ways.
    - all_symbols asserted to span BOTH universes (the copytrade allowlist).
  </acceptance_criteria>
  <done>The pure gate contract is an executable RED spec.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED — wiring cases (6-11, 13, 14, 16) with zero-network fakes</name>
  <files>tests/test_universe.py</files>
  <read_first>tests/test_order_resolution.py L95-170 and L236-258 (FakeAlpacaClient/FakeLogger/_bot + the single-rejected-row assertion to mirror); src/bot_thread.py L328-359 (_submit_order — the existing rejected-row shape) and L129-164 (the selectors); src/copytrade_thread.py L365-390 (_execute_signal — no open/close concept; the held-position check is what distinguishes an entry); src/bot_c/strategy.py L236-320 (_process_ticker — case 14 calls this directly); 15-RESEARCH.md §2 (exit paths) + Pitfall 5 (double-write)</read_first>
  <behavior>
    - test_submit_order_blocks_off_universe: default 8-asset universe; _submit_order(symbol="TRUMP/USD", side="buy", order_type="market") -> (None, None); FakeAlpacaClient.orders == []; FakeLogger.rows has EXACTLY ONE row, status=="rejected", pnl==0
    - test_submit_order_blocks_short_entry: same with side="sell" (a sell-to-OPEN is still an entry)
    - test_submit_order_blocks_quarantined: cfg.quarantined_symbols="BTC/USD"; symbol="BTC/USD" -> blocked, one rejected row
    - test_submit_order_logs_rejection: caplog captures a WARNING containing bot_id, symbol, reason
    - test_submit_order_allows_universe_symbol: symbol="ETH/USD" -> place_market_order IS called; exactly one row, status=="submitted" (no regression)
    - test_select_long_universe_filter: signals ETH/USD (allowed), TRUMP/USD (off-universe), BTC/USD (quarantined) -> only ETH/USD survives
    - test_select_short_universe_filter: same via select_short_candidates
    - test_exit_not_gated: a quarantined symbol with an OPEN position still closes — alpaca.close_position("BTC/USD") executes and is recorded; a direct place_market_order(side="sell") exit call is NOT blocked
    - test_copytrade_entry_blocked: _execute_signal, mapped symbol TRUMP/USD, NOT in the scripted get_positions() (a genuine open) -> no place_market_order call; result action reports blocked + reason. Allowlist under test is BotConfig.all_symbols (crypto ∪ stock), so a legitimately-mirrored stock (e.g. NVDA) on a crypto-class Bot E still PASSES — assert that too.
    - test_bot_c_entry_blocked: call src.bot_c.strategy._process_ticker DIRECTLY with an off-universe ticker (e.g. "TRUMP") — driving the higher-level cycle would be VACUOUS because _select_tickers never yields an off-universe ticker — assert no place_market_order call
    - test_quarantine_column_sql (DATABASE_URL-gated, skipif otherwise): information_schema.columns shows bots.quarantined_symbols EXISTS and its column_default STARTS WITH "''" — Postgres renders TEXT DEFAULT '' as "''::text", so do NOT assert equality with ""
  </behavior>
  <action>Extend tests/test_universe.py with the wiring cases. Copy FakeAlpacaClient/FakeLogger/_bot from tests/test_order_resolution.py; extend FakeAlpacaClient with a `closed` list recording close_position(symbol) and a scripted get_positions() so the copytrade held/not-held branch is drivable. Build BotConfig instances directly (frozen dataclass) with quarantined_symbols set where needed. For copytrade and bot_c, drive the REAL production functions with fakes (CopyTraderThread._execute_signal; bot_c.strategy._process_ticker called DIRECTLY with an off-universe ticker) and assert the fake's order list stays empty. Mirror tests/test_order_resolution.py:255's `len(logger.rows) == 1` guard so a gate that double-writes the rejected row fails. Zero network; each test under a second. Do NOT write production code.</action>
  <verify>
    <automated>python -m pytest tests/test_universe.py -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Blocked-entry cases assert BOTH "no Alpaca call" AND "exactly one rejected row (pnl=0)".
    - Copytrade case asserts the allowlist is the crypto∪stock union (a cross-asset-class mirror still passes).
    - Case 14 calls _process_ticker directly with an off-universe ticker (non-vacuous).
    - Case 16 asserts column existence + a default STARTING WITH "''" (not equality with "").
    - The allowed-symbol case guards against a false block.
    - DATABASE_URL-gated case skips cleanly when the env var is unset.
  </acceptance_criteria>
  <done>The wiring contract is an executable RED spec.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RED — exit-safety cases 17 + 18 (the phase's load-bearing safety proof)</name>
  <files>tests/test_universe.py</files>
  <read_first>15-RESEARCH.md §2 + Pitfall 1 (gating AlpacaClient strands open positions); src/copytrade_thread.py L365-390 (the held-position branch); src/alpaca_client.py L382-389 (close_position — must stay gate-free)</read_first>
  <behavior>
    - test_gate_absent_from_alpaca_client (case 17): STATIC guard — read src/alpaca_client.py from
      disk (pathlib, resolved relative to the repo root, NOT the CWD) and assert "entry_allowed"
      is NOT in the text. Automated proof the gate never lands in the exit layer; replaces a
      manual git-diff check.
    - test_copytrade_sell_held_symbol_not_gated (case 18): a leader SELL on an off-universe symbol
      that IS currently HELD (present in the scripted get_positions()) STILL submits —
      place_market_order IS called, no block. Proves B2: the gate is skipped entirely when
      normalize(mapped) is in the live position set, so an open position is never stranded.
  </behavior>
  <action>Add cases 17 and 18 to tests/test_universe.py. Case 17 resolves src/alpaca_client.py via pathlib relative to the repo root and asserts the string "entry_allowed" is absent. Case 18 scripts FakeAlpacaClient.get_positions() to return the off-universe symbol as HELD, drives CopyTraderThread._execute_signal with a SELL on that symbol, and asserts place_market_order WAS called. Case 17 passes trivially today and must KEEP passing — it is the regression tripwire for Plan 03. Case 18 fails RED until Plan 03 implements the held-position skip.</action>
  <verify>
    <automated>python -m pytest tests/test_universe.py -q -k "alpaca_client or sell_held" 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Case 17 GREEN now and asserted to stay GREEN through Plan 03.
    - Case 18 present, driving the real _execute_signal with a HELD off-universe symbol.
    - `python -m pytest tests/ -q` shows no NEW failures outside tests/test_universe.py.
  </acceptance_criteria>
  <done>Exits-are-never-gated is enforced by an automated static guard plus a live held-position test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test process → Alpaca | none — fakes only, zero network |
| gated test → Postgres | DATABASE_URL-gated; skipped by default |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Denial of Service | gate strands an open position (exit blocked) | mitigate | cases 11, 17 (static guard on src/alpaca_client.py) and 18 (held-symbol SELL still submits) |
| T-15-02 | Repudiation | a blocked entry leaves no audit record | mitigate | assert exactly one terminal `rejected` row (pnl=0) + a WARNING carrying bot_id/symbol/reason |
| T-15-03 | Tampering | gate fails OPEN on an exception | mitigate | entry_allowed is pure + total; no try/except swallow around the gate |
| T-15-SC | Tampering | package installs | accept | no new packages — pytest already installed |
</threat_model>

<verification>
- `python -m pytest tests/test_universe.py -q` collects all 18 cases; only case 17 is GREEN, the rest RED on missing impl.
- `python -m pytest tests/ -q` shows no NEW failures outside tests/test_universe.py.
</verification>

<success_criteria>
The 18 UNIV-01/UNIV-02 cases are executable RED specs, and exits-are-never-gated is pinned by an
automated static guard (case 17) plus a held-position SELL test (case 18).
</success_criteria>

<output>
Create `.planning/phases/15-universe-hard-gate/15-01-SUMMARY.md` when done.
</output>
</content>
