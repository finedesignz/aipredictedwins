---
phase: 15-universe-hard-gate
plan: 03
type: execute
wave: 3
depends_on: [15-02]
files_modified:
  - src/bot_thread.py
  - src/trend_strategy.py
  - src/bot_c/strategy.py
  - src/copytrade_thread.py
  - src/alpaca_orchestrator.py
autonomous: true
requirements: [UNIV-01]

must_haves:
  truths:
    - "An off-universe or quarantined symbol NEVER reaches Alpaca on any of the five entry paths (_submit_order, trend, bot_c, copytrade, CLI orchestrator)"
    - "A blocked entry through _submit_order writes exactly ONE terminal 'rejected' row (pnl=0) and returns (None, None) — no double-write, no new status value"
    - "Every blocked entry emits a WARNING carrying bot_id, symbol, and reason (off_universe | quarantined)"
    - "EXITS ARE NEVER GATED — 'entry_allowed' never appears in src/alpaca_client.py (static test guard), and a copytrade SELL on an already-HELD symbol skips the gate entirely"
    - "Copytrade's allowlist is BotConfig.all_symbols (crypto UNION stock), not cfg.symbols — a cross-market leader mirror still works while TRUMP/FIL (in neither universe) is blocked"
    - "The trend strategy's allowlist is cfg.symbols union {cfg.trend_symbol} so BITX (not in stock_universe) keeps trading"
  artifacts:
    - path: "src/bot_thread.py"
      provides: "hard gate at _submit_order (reads self.config) + belt-and-braces selector filters"
      contains: "entry_allowed"
  key_links:
    - from: "src/bot_thread.py"
      to: "src.universe.entry_allowed"
      via: "gate at the top of _submit_order, before the try-block"
      pattern: "entry_allowed\\("
    - from: "src/copytrade_thread.py"
      to: "src.universe.entry_allowed"
      via: "gate before place_market_order, only when the mapped symbol is NOT already held"
      pattern: "entry_allowed\\("
---

<objective>
Wire the pure gate into all five live entry sites so an off-universe or quarantined symbol can never
be entered — and prove that exits remain completely ungated. This turns the remaining RED cases in
tests/test_universe.py GREEN and closes the TRUMP/FIL leak.

Purpose: UNIV-01 — entry is hard-gated to the per-bot allowlist; the rejection is logged.
Output: gates at src/bot_thread.py (_submit_order + both selectors), src/trend_strategy.py:116,
src/bot_c/strategy.py:313, src/copytrade_thread.py:380, src/alpaca_orchestrator.py:968 and :1123.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-universe-hard-gate/15-CONTEXT.md
@.planning/phases/15-universe-hard-gate/15-RESEARCH.md
@CLAUDE.md
@src/universe.py
@tests/test_universe.py

<interfaces>
From src/universe.py (Plan 02 — use it, do not reimplement):
  normalize(symbol) -> str
  entry_allowed(symbol, allowlist, quarantined) -> (bool, reason)   # reason in {None,"off_universe","quarantined"}
Import convention (mirrors src/bot_thread.py:17): `from src.universe import entry_allowed, normalize`

From src/bot_config.py (Plan 02):
  cfg.symbols      -> list[str]   # per-bot allowlist for the ACTIVE asset class
  cfg.all_symbols  -> list[str]   # crypto UNION stock — the COPYTRADE allowlist ONLY
  cfg.quarantined  -> list[str]   # flat deny-list
  cfg.trend_symbol -> str         # "BITX" — NOT in the default stock_universe

The rejected-row shape to REUSE verbatim (src/bot_thread.py:342-348 — do NOT invent a new status):
  rejected = dict(trade_data)
  rejected.update({"status": "rejected", "order_type": order_type, "pnl": 0})
  logger.log_alpaca_trade(rejected)
  return None, None

The five entry sites (15-RESEARCH.md §1):
  E1 src/bot_thread.py:328-359   _submit_order        (confluence long L790 + short L986 — sell-to-OPEN is an ENTRY)
  E2 src/trend_strategy.py:116   place_market_order(side="buy")
  E3 src/bot_c/strategy.py:313   place_market_order(side="buy")     (inside _process_ticker)
  E4 src/copytrade_thread.py:380 place_market_order(side=side)      (_execute_signal; self.config is the BotConfig)
  E5 src/alpaca_orchestrator.py:968 (long) / :1123 (short)          (CLI; allowlist = the resolved `universe` list)

The EXIT paths — DO NOT TOUCH (15-RESEARCH.md §2):
  src/alpaca_client.py close_position / place_market_order   <- ZERO diff in this file
  src/trend_strategy.py:148, src/bot_c/strategy.py:365, PositionMonitor close (alpaca_orchestrator.py:290),
  and any copytrade order on an ALREADY-HELD symbol (a close/reduce)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hard gate at _submit_order + belt-and-braces selector filters</name>
  <files>src/bot_thread.py</files>
  <read_first>src/bot_thread.py L328-359 (_submit_order — signature takes NO cfg), L129-164 (select_long_candidates / select_short_candidates — the existing MEME_CRYPTO/_ALPACA_UNTRADEABLE filters to sit beside), L204-218 (the thread-safe self.config accessor), L17 (import convention); 15-RESEARCH.md §6 (why a required param would break tests/test_order_resolution.py:156/246) + Pitfall 5 (double-write)</read_first>
  <behavior>
    - Blocked entry: no place_market_order/place_limit_order call; exactly one 'rejected' row (pnl=0); returns (None, None); a WARNING with bot_id/symbol/reason
    - Allowed entry: unchanged — one 'submitted' row, order placed (no regression)
    - Short entry (side="sell" through _submit_order) is gated identically — it OPENS a position
    - select_long_candidates / select_short_candidates drop off-universe + quarantined signals
  </behavior>
  <action>Add the gate at the TOP of _submit_order, before the try-block. Read the config from `self.config` (the thread-safe accessor) — do NOT add a parameter to the signature: tests/test_order_resolution.py:156 and :246 call _submit_order and a required param would break them. Call `allowed, reason = entry_allowed(symbol, cfg.symbols, cfg.quarantined)`; when not allowed, emit `log.warning` including bot_id, symbol and reason, write the existing rejected-row shape verbatim (status='rejected', pnl=0 — do NOT invent a 'blocked' status; Phase 12/13 accounting keys on the existing terminal set), and `return None, None` IMMEDIATELY so the row is never double-written. Do NOT wrap the gate in try/except — entry_allowed is pure and total, and a raising gate must fail CLOSED.
Then add the same predicate to select_long_candidates and select_short_candidates as one more clause alongside the existing `s.symbol not in MEME_CRYPTO` / `not in _ALPACA_UNTRADEABLE` filters: `and entry_allowed(s.symbol, cfg.symbols, cfg.quarantined)[0]`. Belt-and-braces (log-noise reduction) — the gate of record is _submit_order.
Finally, log an INFO line once per config swap (update_config) listing the effective NORMALIZED allowlist and quarantine sets, so a fat-fingered `BTC` (which will NOT match `BTC/USD`) is visible in the logs (Pitfall 4). Minimal diff — no refactors, no new abstractions, do not touch _resolve_crypto_universe, sizing, confluence, the risk gate, or any exit path.</action>
  <verify>
    <automated>python -m pytest tests/test_universe.py tests/test_order_resolution.py tests/test_risk_hardening.py -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - test_submit_order_blocks_off_universe / _blocks_short_entry / _blocks_quarantined / _logs_rejection / _allows_universe_symbol GREEN.
    - test_select_long_universe_filter + test_select_short_universe_filter GREEN.
    - tests/test_order_resolution.py fully GREEN (signature unchanged; single-rejected-row invariant intact).
    - test_gate_absent_from_alpaca_client (case 17) still GREEN.
  </acceptance_criteria>
  <done>Every confluence entry — long or short, market or limit — passes the hard gate; exits are untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate trend + bot_c entries (E2, E3)</name>
  <files>src/trend_strategy.py, src/bot_c/strategy.py</files>
  <read_first>src/trend_strategy.py L100-125 (the BUY) and L148 (the SELL exit — leave alone); src/bot_c/strategy.py L236-320 (_process_ticker BUY at L313) and L345-370 (_exit_position — leave alone)</read_first>
  <behavior>
    - trend: allowlist = list(cfg.symbols) + [cfg.trend_symbol] — BITX passes though it is not in stock_universe; a quarantined BITX is blocked
    - bot_c: an off-universe ticker passed into _process_ticker never reaches place_market_order
    - neither SELL/exit path gains a gate
  </behavior>
  <action>Insert the gate immediately before each ENTRY place_market_order call.
- src/trend_strategy.py: before the BUY at L116, compute `allow = list(cfg.symbols) + [cfg.trend_symbol]` and call `entry_allowed(target, allow, cfg.quarantined)`; on block, `log.warning` with bot_id/symbol/reason and `return` (skip the entry). Leave the SELL at L148 completely untouched.
- src/bot_c/strategy.py: inside _process_ticker, before the BUY at L313, call `entry_allowed(symbol, cfg.symbols, cfg.quarantined)`; on block, `log.warning` with bot_id/symbol/reason and `return`. The gate must live in _process_ticker itself (not in the caller's _select_tickers) — that is where the test drives it, and it is the last chokepoint before the order. Leave _exit_position (L365) untouched.
Smallest possible diff; no refactors, no signature changes, no sizing/confluence/risk/exit changes.</action>
  <verify>
    <automated>python -m pytest tests/test_universe.py -q -k "trend or bot_c" 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - test_trend_symbol_allowed and test_bot_c_entry_blocked GREEN.
    - `grep -n entry_allowed src/trend_strategy.py src/bot_c/strategy.py` shows exactly one hit each, on the ENTRY side.
    - No gate near trend_strategy.py:148 or bot_c/strategy.py:365 (the exits).
  </acceptance_criteria>
  <done>The trend and TradingAgents entry bypasses fail closed; BITX keeps trading.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Gate copytrade (E4, held-position skip) + CLI orchestrator (E5, env quarantine)</name>
  <files>src/copytrade_thread.py, src/alpaca_orchestrator.py</files>
  <read_first>src/copytrade_thread.py L365-390 (_execute_signal — NO open/close concept; `self.config` at L412) and the symbol mapper at L69-101; src/alpaca_orchestrator.py L93-95 (the CRYPTO_UNIVERSE env-read convention to mirror), L602-616 (the resolved `universe` list), L955-975 (CLI long BUY), L1110-1130 (CLI short entry), L290 (PositionMonitor close — leave alone)</read_first>
  <behavior>
    - copytrade: the gate is SKIPPED entirely when the mapped symbol is already HELD (a close/reduce — an exit must never be stranded); applied only when NOT held (a genuine open)
    - copytrade allowlist = self.config.all_symbols (crypto UNION stock), NOT cfg.symbols — Bot E mirrors across asset classes; TRUMP/FIL are in neither universe so the leak still closes
    - CLI: both long (:968) and short (:1123) entries are gated against the resolved `universe` list + a new QUARANTINED_SYMBOLS env list; PositionMonitor's close is untouched
  </behavior>
  <action>Two sites.
(a) src/copytrade_thread.py — _execute_signal, immediately before the place_market_order at L380, in this exact order:
    1. Fetch the live positions from the bot's own alpaca client (the same client the order would use) and build `held = {normalize(p["symbol"]) for p in alpaca.get_positions()}`.
    2. If `normalize(mapped) in held` → SKIP THE GATE ENTIRELY and fall through to place_market_order unchanged. This order is a close/reduce on an already-open position; gating it would strand the position. (This is the ONLY correct open/close discriminator — _execute_signal has no such concept of its own.)
    3. Otherwise (a genuine OPEN) → `allowed, reason = entry_allowed(mapped, self.config.all_symbols, self.config.quarantined)`. On block: set `base["action"] = "blocked"` and `base["error_detail"]` to the reason, `log.warning` with bot_id/symbol/reason, and `return base` WITHOUT calling Alpaca.
    Use `self.config.all_symbols` (NOT `.symbols`) — a copy-trader mirrors a leader across asset classes, so a single-asset-class allowlist would kill legitimate cross-market copies. If the get_positions() call raises, fail CLOSED on the gate (treat as not-held and evaluate the gate) — never swallow the exception into an ungated submit.
(b) src/alpaca_orchestrator.py — define a module-level `QUARANTINED_SYMBOLS = [s.strip() for s in os.environ.get("QUARANTINED_SYMBOLS", "").split(",") if s.strip()]` next to the existing CRYPTO_UNIVERSE env read (~L93-95), mirroring its style. Then, before the long entry at L968 and the short entry at L1123, call `entry_allowed(symbol, universe, QUARANTINED_SYMBOLS)` against the resolved `universe` list for that cycle; on block, `log.warning` with symbol/reason and `continue`. Empty env = nothing quarantined = current behavior. Leave PositionMonitor's close_position (L290) untouched.
Smallest possible diff at each site — no refactors, no new helper beyond src/universe, no signature changes.</action>
  <verify>
    <automated>python -m pytest tests/ -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - test_copytrade_entry_blocked GREEN (off-universe OPEN blocked, no Alpaca call).
    - test_copytrade_sell_held_symbol_not_gated (case 18) GREEN (a SELL on a HELD off-universe symbol still submits).
    - test_gate_absent_from_alpaca_client (case 17) GREEN — the gate never landed in the exit layer.
    - `grep -c entry_allowed src/alpaca_orchestrator.py` >= 2 (both CLI entries) and `QUARANTINED_SYMBOLS` is read from the env.
    - Full suite green: `python -m pytest tests/ -q` — all 18 tests/test_universe.py cases pass, zero regressions.
  </acceptance_criteria>
  <done>All five entry paths fail closed; the TRUMP/FIL leak (copytrade + CLI dynamic universe) is closed and no open position can be stranded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| signal/leader symbol → order submission | untrusted symbol crosses into a real Alpaca order |
| operator quarantine string (DB column / QUARANTINED_SYMBOLS env) → runtime gate | free text becomes a trading deny-list |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Denial of Service | gate at the wrong layer strands an open position | mitigate | gate ONLY at the five entry sites; case 17 statically asserts "entry_allowed" is absent from src/alpaca_client.py; copytrade skips the gate for already-HELD symbols (case 18) |
| T-15-02 | Repudiation | a blocked entry leaves no audit record | mitigate | terminal `rejected` row (pnl=0, existing Phase-11 path) + WARNING with bot_id/symbol/reason |
| T-15-03 | Elevation of Privilege | gate fails OPEN (exception swallowed → order goes through) | mitigate | entry_allowed is pure + total; no try/except around the gate; a failing get_positions() in copytrade fails CLOSED (gate is evaluated) |
| T-15-05 | Denial of Service | a fat-fingered quarantine string halts a bot silently | mitigate | INFO log of the effective NORMALIZED allowlist/quarantine set at every config swap |
| T-15-07 | Tampering | a new `blocked` status breaks Phase-12/13 accounting | mitigate | reuse the existing `rejected` terminal status verbatim; no sixth status value |
| T-15-08 | Denial of Service | copytrade gated on cfg.symbols (one asset class) kills legitimate cross-market mirrors | mitigate | copytrade uses cfg.all_symbols (crypto ∪ stock); TRUMP/FIL are in neither, so the leak still closes |
| T-15-SC | Tampering | package installs | accept | no new packages |
</threat_model>

<verification>
- `python -m pytest tests/test_universe.py -q` — all 18 cases GREEN.
- `python -m pytest tests/ -q` — full suite green, zero regressions.
- Case 17 (static) proves `entry_allowed` never appears in src/alpaca_client.py.
- `grep -c entry_allowed src/bot_thread.py src/trend_strategy.py src/bot_c/strategy.py src/copytrade_thread.py src/alpaca_orchestrator.py` — every entry site covered.
</verification>

<success_criteria>
Definition of Done: `python -m pytest tests/ -q` is green with all 18 tests/test_universe.py cases
passing; an off-universe or quarantined symbol is rejected before submission on every one of the five
entry paths with a logged WARNING and (on the _submit_order path) a terminal `rejected` row; case 17
proves the gate never reached src/alpaca_client.py and case 18 proves a held position still closes;
dropping a symbol requires a single config write (bots.quarantined_symbols, or QUARANTINED_SYMBOLS
env on the CLI path) and zero code change.
</success_criteria>

<output>
Create `.planning/phases/15-universe-hard-gate/15-03-SUMMARY.md` when done.
</output>
</content>
