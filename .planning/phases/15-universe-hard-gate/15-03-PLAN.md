---
phase: 15-universe-hard-gate
plan: 03
type: execute
wave: 3
depends_on: [15-02]
files_modified:
  - src/bot_thread.py
  - src/trend_strategy.py
  - src/bot_c/strategy.py
  - src/copytrade_thread.py
  - src/alpaca_orchestrator.py
autonomous: true
requirements: [UNIV-01]

must_haves:
  truths:
    - "An off-universe or quarantined symbol NEVER reaches Alpaca on any of the five entry paths (_submit_order, trend, bot_c, copytrade, CLI orchestrator)"
    - "A blocked entry through _submit_order writes exactly ONE terminal 'rejected' row (pnl=0) and returns (None, None) — no double-write, no new status value"
    - "Every blocked entry emits a WARNING carrying bot_id, symbol, and reason (off_universe | quarantined)"
    - "EXITS ARE NEVER GATED — close_position and place_market_order(side='sell') exit calls are untouched; an open position in a quarantined symbol still closes"
    - "The trend strategy's allowlist is cfg.symbols union {cfg.trend_symbol} so BITX (not in stock_universe) keeps trading"
  artifacts:
    - path: "src/bot_thread.py"
      provides: "hard gate at _submit_order (reads self.config) + belt-and-braces selector filters"
      contains: "entry_allowed"
  key_links:
    - from: "src/bot_thread.py"
      to: "src.universe.entry_allowed"
      via: "gate at the top of _submit_order, before the try-block"
      pattern: "entry_allowed\\("
    - from: "src/copytrade_thread.py"
      to: "src.universe.entry_allowed"
      via: "gate before place_market_order on the copy entry"
      pattern: "entry_allowed\\("
---

<objective>
Wire the pure gate into all five live entry sites so an off-universe or quarantined symbol can never
be entered — and prove that exits remain completely ungated. This turns the remaining RED cases in
tests/test_universe.py GREEN and closes the TRUMP/FIL leak.

Purpose: UNIV-01 — entry is hard-gated to the per-bot allowlist; the rejection is logged.
Output: gates at src/bot_thread.py (_submit_order + both selectors), src/trend_strategy.py:116,
src/bot_c/strategy.py:313, src/copytrade_thread.py:380, src/alpaca_orchestrator.py:968 and :1123.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-universe-hard-gate/15-CONTEXT.md
@.planning/phases/15-universe-hard-gate/15-RESEARCH.md
@CLAUDE.md
@src/universe.py
@tests/test_universe.py

<interfaces>
From src/universe.py (Plan 02 — use it, do not reimplement):
  normalize(symbol) -> str
  entry_allowed(symbol, allowlist, quarantined) -> (bool, reason)   # reason in {None,"off_universe","quarantined"}
Import convention (mirrors src/bot_thread.py:17): `from src.universe import entry_allowed`

From src/bot_config.py (Plan 02):
  cfg.symbols      -> list[str]   # the per-bot allowlist (crypto or stock by asset_class)
  cfg.quarantined  -> list[str]   # flat deny-list
  cfg.trend_symbol -> str         # "BITX" — NOT in the default stock_universe

The rejected-row shape to REUSE verbatim (src/bot_thread.py:342-348 — do NOT invent a new status):
  rejected = dict(trade_data)
  rejected.update({"status": "rejected", "order_type": order_type, "pnl": 0})
  logger.log_alpaca_trade(rejected)
  return None, None

The five entry sites (15-RESEARCH.md §1):
  E1 src/bot_thread.py:328-359  _submit_order        (confluence long L790 + short L986 — sell-to-OPEN is an ENTRY)
  E2 src/trend_strategy.py:116  place_market_order(side="buy")
  E3 src/bot_c/strategy.py:313  place_market_order(side="buy")
  E4 src/copytrade_thread.py:380 place_market_order(side=side)   — self.config is the BotConfig
  E5 src/alpaca_orchestrator.py:968 (long) / :1123 (short)       — CLI; allowlist = the resolved `universe` list

The EXIT paths — DO NOT TOUCH (15-RESEARCH.md §2):
  src/alpaca_client.py close_position / place_market_order   <- ZERO diff in this file
  src/trend_strategy.py:148, src/bot_c/strategy.py:365, src/copytrade_thread.py sell-side closes
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hard gate at _submit_order + belt-and-braces selector filters</name>
  <files>src/bot_thread.py</files>
  <read_first>src/bot_thread.py L328-359 (_submit_order — note the signature takes NO cfg), L129-164 (select_long_candidates / select_short_candidates — the existing MEME_CRYPTO/_ALPACA_UNTRADEABLE filters to sit beside), L204-218 (the thread-safe self.config accessor), L17 (import convention); 15-RESEARCH.md §6 (why a required param would break tests/test_order_resolution.py:156/246) + Pitfall 5 (double-write)</read_first>
  <behavior>
    - Blocked entry: no place_market_order/place_limit_order call; exactly one 'rejected' row (pnl=0); returns (None, None); a WARNING with bot_id/symbol/reason
    - Allowed entry: unchanged — one 'submitted' row, order placed (no regression)
    - Short entry (side="sell" through _submit_order) is gated identically — it opens a position
    - select_long_candidates / select_short_candidates drop off-universe + quarantined signals
  </behavior>
  <action>Add the gate at the TOP of _submit_order, before the try-block. Read the config from `self.config` (the thread-safe accessor) — do NOT add a parameter to the signature: tests/test_order_resolution.py:156 and :246 call _submit_order positionally and a required param would break them. Call `allowed, reason = entry_allowed(symbol, cfg.symbols, cfg.quarantined)`; when not allowed, emit `log.warning` including bot_id, symbol and reason, write the existing rejected-row shape verbatim (status='rejected', pnl=0 — do NOT invent a 'blocked' status; Phase 12/13 accounting keys on the existing terminal set), and `return None, None` IMMEDIATELY so the row is never double-written. Do NOT wrap the gate in try/except — entry_allowed is pure and total, and a raising gate must fail CLOSED.
Then add the same predicate to select_long_candidates and select_short_candidates as one more clause alongside the existing `s.symbol not in MEME_CRYPTO` / `not in _ALPACA_UNTRADEABLE` filters: `and entry_allowed(s.symbol, cfg.symbols, cfg.quarantined)[0]`. These are belt-and-braces (log-noise reduction) — the gate of record is _submit_order.
Finally, log an INFO line once per config swap (update_config) listing the effective NORMALIZED allowlist and quarantine sets, so a fat-fingered `BTC` (which will NOT match `BTC/USD`) is visible in the logs (Pitfall 4). Keep the diff minimal — no refactors, no new abstractions, do not touch _resolve_crypto_universe, sizing, confluence, the risk gate, or any exit path.</action>
  <verify>
    <automated>python -m pytest tests/test_universe.py tests/test_order_resolution.py tests/test_risk_hardening.py -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - test_submit_order_blocks_off_universe / _blocks_short_entry / _blocks_quarantined / _logs_rejection / _allows_universe_symbol GREEN.
    - test_select_long_universe_filter + test_select_short_universe_filter GREEN.
    - tests/test_order_resolution.py fully GREEN (signature unchanged; single-rejected-row invariant intact).
    - `git diff --stat src/alpaca_client.py` is EMPTY (the gate is not at the client layer).
  </acceptance_criteria>
  <done>Every confluence entry — long or short, market or limit — passes the hard gate; exits are untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate the four bypass entry sites (trend, bot_c, copytrade, CLI)</name>
  <files>src/trend_strategy.py, src/bot_c/strategy.py, src/copytrade_thread.py, src/alpaca_orchestrator.py</files>
  <read_first>src/trend_strategy.py L100-125 (the BUY) and L148 (the SELL exit — leave alone); src/bot_c/strategy.py L300-320 (_process_ticker BUY) and L345-370 (_exit_position — leave alone); src/copytrade_thread.py L365-390 (the copy entry; `self.config` holds the BotConfig, see L412) ; src/alpaca_orchestrator.py L602-616 (the resolved `universe` list), L955-975 (CLI long BUY), L1110-1130 (CLI short entry)</read_first>
  <behavior>
    - trend: allowlist = set(cfg.symbols) | {cfg.trend_symbol} — BITX passes even though it is not in stock_universe; a quarantined BITX is blocked
    - bot_c: an off-universe ticker never reaches place_market_order
    - copytrade: an off-universe mapped leader symbol (TRUMP/USD, FIL/USD) never reaches place_market_order — this is the most likely source of the audited leak
    - CLI orchestrator: both the long (:968) and short (:1123) entries are gated against the resolved `universe` list
    - none of the four SELL/close paths gain a gate
  </behavior>
  <action>Insert the gate immediately before each entry `place_market_order` call — ENTRIES ONLY.
- src/trend_strategy.py: before the BUY at L116, compute `allow = list(cfg.symbols) + [cfg.trend_symbol]` and call entry_allowed(target, allow, cfg.quarantined); on block, log.warning with bot_id/symbol/reason and `return` (skip the entry). Leave the SELL at L148 completely untouched.
- src/bot_c/strategy.py: before the BUY at L313, entry_allowed(symbol, cfg.symbols, cfg.quarantined); on block, log.warning and `return`. Leave _exit_position (L365) untouched.
- src/copytrade_thread.py: before the entry place_market_order at L380, gate ONLY when the copied order OPENS a position (an entry, not a leader's close): entry_allowed(mapped, self.config.symbols, self.config.quarantined); on block set base["action"]="blocked" with base["error_detail"] carrying the reason, log.warning with bot_id/symbol/reason, and return `base` WITHOUT calling Alpaca. If the copied action is a close/reduce of an existing position, do NOT gate it.
- src/alpaca_orchestrator.py: before the long entry at L968 and the short entry at L1123, gate against the resolved `universe` list (the allowlist for the CLI path) plus an optional quarantine read from the existing env-config convention (empty = nothing quarantined); on block, log.warning + `continue`. Leave PositionMonitor's close_position (L290) untouched.
Smallest possible diff at each site — no refactors, no new helper beyond src/universe.entry_allowed, no signature changes, no sizing/confluence/risk/exit changes.</action>
  <verify>
    <automated>python -m pytest tests/test_universe.py -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - test_copytrade_entry_blocked, test_bot_c_entry_blocked, test_trend_symbol_allowed, test_exit_not_gated GREEN.
    - grep -n "entry_allowed" src/trend_strategy.py src/bot_c/strategy.py src/copytrade_thread.py src/alpaca_orchestrator.py returns a hit for each of the 5 entry lines (trend 1, bot_c 1, copytrade 1, orchestrator 2).
    - No `entry_allowed` appears anywhere near an exit/close/sell-to-close call (`git diff src/alpaca_client.py` empty; the four exit sites unchanged).
  </acceptance_criteria>
  <done>All five entry paths fail closed; the TRUMP/FIL leak (copytrade + CLI dynamic universe) is closed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The universe hard gate across all five entry paths + the config-driven quarantine column.</what-built>
  <how-to-verify>
    1. `python -m pytest tests/ -q` — full suite green (baseline 336 passed + the 16 new cases).
    2. `git diff --stat src/alpaca_client.py` — MUST be empty (proves exits are ungated).
    3. Confirm the intended prod rollout order: deploy the DASHBOARD first (it runs migration 018,
       which is additive + idempotent and inert at default ''), THEN the orchestrator services.
    4. Confirm the quarantine format contract: entries must match crypto_universe (`BTC/USD`, slash
       included). A bare `BTC` will NOT match and will silently fail to quarantine — the new INFO
       line at config swap prints the effective normalized set so a typo is visible in the logs.
    5. Decide whether to actually quarantine BTC (0-for-12, -$479) now, or leave the column empty
       and wait for Phase 17's per-symbol evidence. This phase only makes quarantining POSSIBLE.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| signal/leader symbol → order submission | untrusted symbol crosses into a real Alpaca order |
| operator quarantine string → runtime gate | free text becomes a trading deny-list |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Denial of Service | gate at the wrong layer strands an open position | mitigate | gate ONLY at the five entry sites; zero diff to src/alpaca_client.py; test_exit_not_gated is the guard |
| T-15-02 | Repudiation | a blocked entry leaves no audit record | mitigate | terminal `rejected` row (pnl=0, existing Phase-11 path) + WARNING with bot_id/symbol/reason |
| T-15-03 | Elevation of Privilege | gate fails OPEN (exception swallowed → order goes through) | mitigate | entry_allowed is pure + total; NO try/except around the gate call — a raising gate fails closed |
| T-15-05 | Denial of Service | a fat-fingered quarantine string halts a bot silently | mitigate | INFO log of the effective NORMALIZED allowlist/quarantine set at every config swap |
| T-15-07 | Tampering | a new `blocked` status breaks Phase-12/13 accounting | mitigate | reuse the existing `rejected` terminal status verbatim; no sixth status value |
| T-15-SC | Tampering | package installs | accept | no new packages |
</threat_model>

<verification>
- `python -m pytest tests/test_universe.py -q` — all 16 cases GREEN.
- `python -m pytest tests/ -q` — full suite green, zero regressions.
- `git diff --stat src/alpaca_client.py` — empty (exits provably ungated).
- `grep -c entry_allowed src/bot_thread.py src/trend_strategy.py src/bot_c/strategy.py src/copytrade_thread.py src/alpaca_orchestrator.py` — every entry site covered.
</verification>

<success_criteria>
Definition of Done: `python -m pytest tests/ -q` is green with all 16 tests/test_universe.py cases
passing; an off-universe or quarantined symbol is rejected before submission on every one of the five
entry paths with a logged WARNING and a terminal `rejected` row; `git diff src/alpaca_client.py` is
empty, proving exits are never gated; dropping a symbol requires a single config write and zero code
change.
</success_criteria>

<output>
Create `.planning/phases/15-universe-hard-gate/15-03-SUMMARY.md` when done.
</output>
</content>
