---
phase: 19-reliable-runtime
plan: 07
type: execute
wave: 5
depends_on: [19-05, 19-06]
files_modified:
  - .planning/phases/19-reliable-runtime/19-EVIDENCE.md
autonomous: false
requirements: [RUN-01, RUN-02]

must_haves:
  truths:
    - "All 29 VALIDATION cases and all 8 fences are green, and the full suite is at or above the 428 passed / 28 skipped floor."
    - "The nine must-fail cases (1, 5, 6, 13, 15, 17, 22, 23, 26) are proven to have FAILED on pre-phase main and to PASS now. A test that passed pre-fix was never testing the bug."
    - "The Coolify restart policy is VERIFIED READ-ONLY and REPORTED. It is a RED HERRING (research N4): the container is UP — it serves the API and the dashboard renders. Only the THREADS are dead. No restart policy restarts a thread. If it appears in the causal chain of the fix, the fix is wrong."
    - "alpaca_trades is byte-identical. The 395 pnl = 0.0 sentinel rows are read around, NOT repaired. Repairing them is a write to historical prod trade data requiring EXPLICIT HUMAN AUTHORIZATION and is flagged, not done — including 'while we're in there'."
    - "The paper gate stays CLOSED. The honest readout may read WORSE than the old one. That is the intended outcome and must not be 'fixed'."
    - "NO prod deploy-config or Coolify env mutation happened in this phase."
  artifacts:
    - path: ".planning/phases/19-reliable-runtime/19-EVIDENCE.md"
      provides: "the RED->GREEN proof table, the fence results, the Coolify read-only report, and the human-authorization flag on the 395 rows"
      min_lines: 60
  key_links:
    - from: "19-EVIDENCE.md"
      to: "19-VALIDATION.md"
      via: "every one of the 29 cases + 8 fences has a recorded verdict"
      pattern: "case|F[1-8]"
---

<objective>
Prove it. Then say plainly what was NOT done and why.

Purpose: this phase exists because a system reported healthy while four bots were dead. A phase that
fixes that and then declares itself done on the implementer's self-report has learned nothing. Every
claim gets an artifact: the RED→GREEN table, the full-suite count, the byte-identical trade log, the
read-only Coolify report, and the explicit flag on the 395 rows nobody is authorized to touch.

Output: `19-EVIDENCE.md` + a human verification checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-reliable-runtime/19-VALIDATION.md
@.planning/phases/19-reliable-runtime/19-RESEARCH.md
@.planning/phases/19-reliable-runtime/19-01-SUMMARY.md
@.planning/phases/19-reliable-runtime/19-03-SUMMARY.md
@.planning/phases/19-reliable-runtime/19-04-SUMMARY.md
@.planning/phases/19-reliable-runtime/19-05-SUMMARY.md
@.planning/phases/19-reliable-runtime/19-06-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: the RED→GREEN proof + the fence sweep + the Coolify read-only report</name>
  <files>.planning/phases/19-reliable-runtime/19-EVIDENCE.md</files>
  <read_first>19-VALIDATION.md (all 29 cases + all 8 fences + the Definition of Done at the bottom); 19-01-SUMMARY.md (the recorded pre-fix failure line for each of the nine must-fail cases); 19-RESEARCH.md N4 (why the restart policy is a red herring) and the Runtime State Inventory (the Coolify app UUID u7x0xw0y4qvcgeh8vyidsgyi and the fact that the policy is NOT declared in the repo — docker-compose.dev.yml:35,52,73 is DEV-ONLY and does not govern prod)</read_first>
  <action>
  Produce `.planning/phases/19-reliable-runtime/19-EVIDENCE.md` containing:

  1. **The RED→GREEN table.** One row per must-fail case (1, 5, 6, 13, 15, 17, 22, 23, 26): the case
     name, the VERBATIM pre-fix failure line (from 19-01-SUMMARY.md), the plan that closed it, and the
     post-fix PASS. Re-derive the pre-fix column by checking out the pre-phase commit into a temp
     worktree and running the case there IF 19-01-SUMMARY did not capture it verbatim — **do NOT
     `git checkout` in this canonical checkout** (CLAUDE.md rule 20: use `git worktree add`, and remove
     it after). A case that passed pre-fix was never testing the bug: if any of the nine did, say so
     LOUDLY here and treat the phase as NOT shippable.

  2. **The full-case table.** All 29 cases with PASS / SKIP(reason) / FAIL. Paste the raw
     `python -m pytest tests/ dashboard/api/tests/ -q | tail -3` output verbatim, with the counts.

  3. **The fence sweep.** F1-F8, each with the exact command run and its output:
       F1  `git diff <pre-phase-sha>..HEAD | grep -icE "^\+.*(UPDATE|DELETE).*alpaca_trades"` -> 0
           `git diff <pre-phase-sha>..HEAD --stat -- src/backfill.py` -> EMPTY
       F2  BotUpdate.kelly_fraction still `le=0.25`; the 5%/quarter-Kelly/20%-drawdown/limit-only/
           50-trade literals unchanged
       F3  `mode="paper"` and `win_rate_target=40.0` still present; the gate comparison unchanged
       F4  `grep -riE "sendgrid|resend|postmark|mailgun" src/ dashboard/` -> NOTHING
       F5  no diff to min_confluence / kelly_fraction / quarantined_symbols DEFAULT VALUES
       F6  `grep -cE "AlpacaClient|ALPACA_API_KEY" src/bot_manager.py` -> 0; reconcile still routes
           through `_client_for_bot`
       F7  no Coolify env/config mutation anywhere in the phase
       F8  `python -m pytest -q` >= 428 passed / 28 skipped

  3b. **`git diff --stat package.json dashboard/web/package.json requirements*.txt`** -> EMPTY.
      This phase installs NO packages (RESEARCH: "Package Legitimacy Audit: N/A"). If anything appears
      here, STOP — an unaudited dependency entered a trading system.

  4. **The Coolify read-only report.** Query the restart policy for app `u7x0xw0y4qvcgeh8vyidsgyi`
     (project "AI Predicted Wins") via the Coolify API — **READ ONLY. Change NOTHING.** Record the
     value. Then state plainly, in the document: **this is a RED HERRING for this outage (research N4).
     The container was UP the whole time — it served `/api/health` and the dashboard rendered
     `status: stopped`. Only the bot THREADS were dead or never spawned. No restart policy — `always`,
     `unless-stopped`, or otherwise — restarts a thread inside a live process.** If the policy is
     wrong, report it as a SEPARATE, human-authorized follow-up; it is not part of this fix. If the
     Coolify API is unreachable, record "unknown" and move on — do not block.

  5. **The diagnosis for the record (research N2).** Pull the dashboard container logs READ-ONLY and
     record which mechanism actually fired: `BotManager started` vs `BotManager unavailable: …` vs
     `BotManager: starting 0 enabled bots`. RESEARCH's A3 predicts mechanism (i) — the manager never
     started — because `seed_bots.py:167-176` back-patches NULL/empty keys from the dashboard's
     `ALPACA_API_KEY_{A,B,C,D}` env vars on every run, so the `bots` rows very likely DO have keys.
     **This does not change the plan** — Phase 19 fixed both mechanisms — but the record must not
     assume B1. Say which one it was, or say the logs no longer reach back far enough.

  6. **REQUIRES HUMAN AUTHORIZATION (the loudest section).** State that the 395 historical
     `pnl = 0.0` sentinel rows in `alpaca_trades` were **read around and NOT repaired**; that
     `src/backfill.py` exists, already drives `reconciliation._client_for_bot`, and is a **loaded gun**
     that must not be pointed at prod; that repairing those rows is a WRITE to historical prod trade
     data requiring **explicit human authorization** (and the source data may no longer exist at
     Alpaca); and that until it is authorized, the `unresolved` count on the dashboard is the honest
     representation of them. Prove the fence: `alpaca_trades` is byte-identical over the phase.

  7. **"The gate may now read worse."** State explicitly that making the paper-gate readout honest
     (dropping ~395 fabricated losses from the denominator AND replacing the hardcoded
     `100_000.0 * len(bot_ids)` bankroll) may move the win rate and equity in EITHER direction, that a
     WORSE reading is the correct and intended outcome, and that it must NOT be tuned away. The gate
     stays closed. The retune is **Phase 21**, on the sentinel-free sample this phase's honest headline
     finally makes measurable.
  </action>
  <verify>
    <automated>python -m pytest tests/ dashboard/api/tests/ -q 2>&1 | tail -3 && python -m pytest tests/test_phase19_fences.py -q 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/ dashboard/api/tests/ -q | tail -1` — **>= 428 passed, 28 skipped, 0 failed** (fence F8). The count is pasted verbatim into 19-EVIDENCE.md.
    - `python -m pytest tests/test_phase19_fences.py -q` — all 8 fences green.
    - `git diff <pre-phase-sha>..HEAD | grep -icE "^\+.*(UPDATE|DELETE).*alpaca_trades"` prints **0**. Not one write to the trade log.
    - `git diff <pre-phase-sha>..HEAD --stat -- src/backfill.py` is **EMPTY** — the loaded gun was not touched, let alone fired.
    - `git diff <pre-phase-sha>..HEAD --stat -- requirements.txt dashboard/web/package.json` is **EMPTY** — zero packages installed.
    - `grep -n "if not any_alive" src/bot_manager.py` returns **NOTHING** — the killer is confirmed dead in the shipped tree.
    - 19-EVIDENCE.md contains all nine must-fail cases with a verbatim pre-fix failure line each, and a section titled exactly `REQUIRES HUMAN AUTHORIZATION`.
  </acceptance_criteria>
  <done>Every claim in the phase has an artifact behind it, and the two things that were deliberately NOT done (the 395-row repair, the Coolify restart policy) are named, justified, and handed back to a human.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 19 — Reliable Runtime & Honest Monitoring, complete:
      - **THE KILLER BUG IS DEAD.** `src/bot_manager.py:189-190`'s `if not any_alive: return` is gone.
        All-bots-down now fires the loudest alert in the system, and repeats hourly until a bot is alive.
      - Both key predicates deleted. A keyless enabled bot is now `status='error'` + an alert, never
        invisible — and is no longer respawned every 60 seconds forever.
      - `runtime_heartbeat` (migration 019 + the `db_schema.sql` mirror) makes the manager's liveness
        queryable from OUTSIDE the process; absence-of-row means DEAD, never healthy.
      - `main.py`'s except branch alerts when the BotManager never starts.
      - The alerter self-checks whether it can actually send.
      - `reconcile()` is per-bot guarded and runs hourly from the watchdog that already ticked.
      - The headline P&L is the RECONCILED number, flagged with `pnl_source` and `stale`.
      - `pnl = 0.0` sentinels are no longer counted as losses at any of the five reader sites.
      - `settings.py`'s hardcoded `100_000.0 * len(bot_ids)` bankroll is gone.
  </what-built>
  <how-to-verify>
    1. `python -m pytest tests/ dashboard/api/tests/ -q | tail -3` — expect >= 428 passed / 28 skipped, 0 failed.
    2. `grep -n "if not any_alive" src/bot_manager.py` — expect NOTHING. That grep returning nothing is
       the whole phase.
    3. Open `.planning/phases/19-reliable-runtime/19-EVIDENCE.md`. Read the `REQUIRES HUMAN
       AUTHORIZATION` section and the Coolify report.
    4. Open the dashboard screenshots from 19-06-SUMMARY.md. Confirm you can SEE: the P&L source badge
       (reconciled / alpaca_live / trade_log), the stale flag, the `unresolved` count sitting BESIDE
       wins/losses (not inside losses), and the `manager_alive` / `alerts_configured` states.
    5. **Look at the paper-gate readout.** The honest win rate and the real bankroll may make it read
       WORSE than before. **That is correct.** Confirm you accept the honest number and do NOT want it
       tuned — the retune is Phase 21.

    DECISIONS THIS CHECKPOINT IS ASKING YOU FOR:
      (a) Do you authorize a FUTURE, SEPARATE task to repair the 395 historical `pnl = 0.0` rows from
          Alpaca activity history? **Phase 19 did not do it and will not.** (Default: NO.)
      (b) The Coolify restart policy is reported read-only and was NOT changed. Do you want a separate
          follow-up to change it? (It is a red herring for THIS outage — the container was up; only the
          threads were dead.)
      (c) `alerts_configured` on prod: if it reports FALSE, every alert this phase added is silently
          dropped, and the system is not monitored. Confirm the SES env vars
          (`AWS_ACCESS_KEY_ID`/`AWS_SECRET_ACCESS_KEY`/`AWS_DEFAULT_REGION`) are set on the dashboard
          service, or authorize setting them.
  </how-to-verify>
  <resume-signal>Type "approved" (plus your answers to a/b/c), or describe what is wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| verification → prod DB | an evidence query that writes is the exact failure this phase exists to prevent |
| verification → Coolify | a config read that becomes a config write is an unauthorized prod change |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-28 | Tampering | an evidence step "helpfully" backfilling the 395 rows | mitigate | SELECT-only. Fence F1 grep-asserts zero `UPDATE`/`DELETE` on `alpaca_trades` across the whole phase diff, and that `src/backfill.py` is untouched |
| T-19-29 | Tampering | the Coolify check mutating the restart policy | mitigate | READ-ONLY API call. Fence F7. If the API is unreachable, record "unknown" — never "fix" it |
| T-19-30 | Tampering | a worktree-less `git checkout` of the pre-phase SHA thrashing a sibling session's HEAD | mitigate | CLAUDE.md rule 20 — `git worktree add` into a temp dir, then `git worktree remove`. NEVER `git checkout` in the canonical tree |
| T-19-31 | Elevation of Privilege | an honest-but-worse paper gate being tuned back to passing | mitigate | the checkpoint asks the human to explicitly ACCEPT the worse number; fence F3 pins mode/target/comparison |
| T-19-SC | Tampering | package installs | mitigate | `git diff --stat` on `requirements.txt` / `package.json` must be EMPTY; a non-empty diff STOPS the phase |
</threat_model>

<verification>
- Full suite: >= 428 passed / 28 skipped, zero failed.
- All 29 cases + 8 fences accounted for in 19-EVIDENCE.md.
- `alpaca_trades` byte-identical across the phase diff.
- Zero packages installed. Zero Coolify mutations.
</verification>

<success_criteria>
The phase can prove — with commands and output, not assertions — that the killer bug is dead, that the
numbers are honest, that the trade log was not touched, and that the two things deliberately left
undone are named and handed to a human.
</success_criteria>

<output>
Create `.planning/phases/19-reliable-runtime/19-07-SUMMARY.md` when done.
</output>
</content>
