---
phase: 19-reliable-runtime
plan: 07
type: execute
wave: 5
depends_on: [19-05, 19-06]
files_modified:
  - .planning/phases/19-reliable-runtime/19-EVIDENCE.md
autonomous: false
requirements: [RUN-01, RUN-02]

must_haves:
  truths:
    - "All 29 VALIDATION cases, the three plan-check additions (1b, 5b, 12b), and all 8 fences are green, and the full suite is at or above the 428 passed / 28 skipped floor."
    - "The ELEVEN must-fail cases (1, 1b, 5, 5b, 6, 13, 15, 17, 22, 23, 26) are proven to have FAILED on pre-phase main and to PASS now. A test that passed pre-fix was never testing the bug."
    - "Case 1b is the phase's real proof: it drives the WHOLE watchdog tick with a SUCCEEDING revive and still gets the ALL BOTS DOWN alert. Case 1 alone would have gone green against a tick order that can never fire the alert in production."
    - "The Coolify restart policy is VERIFIED READ-ONLY and REPORTED. It is a RED HERRING (research N4): the container is UP — it serves the API and the dashboard renders. Only the THREADS are dead. No restart policy restarts a thread."
    - "alpaca_trades is byte-identical. The 395 pnl = 0.0 sentinel rows are read around, NOT repaired. Repairing them is a write to historical prod trade data requiring EXPLICIT HUMAN AUTHORIZATION and is flagged, not done — including 'while we're in there'."
    - "The paper gate stays CLOSED. The honest readout may read WORSE than the old one. That is the intended outcome and must not be 'fixed'."
    - "NO prod deploy-config or Coolify env mutation happened in this phase. NO packages were installed."
  artifacts:
    - path: ".planning/phases/19-reliable-runtime/19-EVIDENCE.md"
      provides: "the RED->GREEN proof table, the fence results, the Coolify read-only report, the log diagnosis, the human-authorization flag on the 395 rows, and the paper-trade-count caveat"
      min_lines: 70
  key_links:
    - from: "19-EVIDENCE.md"
      to: "19-VALIDATION.md"
      via: "every case + fence has a recorded verdict"
      pattern: "case|F[1-8]"
---

<objective>
Prove it. Then say plainly what was NOT done and why.

Purpose: this phase exists because a system reported healthy while four bots were dead. A phase that
fixes that and then declares itself done on the implementer's self-report has learned nothing. Every
claim gets an artifact: the RED→GREEN table, the full-suite count, the byte-identical trade log, the
read-only Coolify report, and the explicit flag on the 395 rows nobody is authorized to touch.

Output: `19-EVIDENCE.md` + a human verification checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-reliable-runtime/19-VALIDATION.md
@.planning/phases/19-reliable-runtime/19-RESEARCH.md
@.planning/phases/19-reliable-runtime/19-01-SUMMARY.md
@.planning/phases/19-reliable-runtime/19-03-SUMMARY.md
@.planning/phases/19-reliable-runtime/19-04-SUMMARY.md
@.planning/phases/19-reliable-runtime/19-05-SUMMARY.md
@.planning/phases/19-reliable-runtime/19-06-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: the RED→GREEN proof + the fence sweep + the Coolify read-only report</name>
  <files>.planning/phases/19-reliable-runtime/19-EVIDENCE.md</files>
  <read_first>19-VALIDATION.md (all 29 cases + 8 fences + the Definition of Done); 19-01-SUMMARY.md (the recorded VERBATIM pre-fix failure line for each of the eleven must-fail cases); 19-RESEARCH.md N4 (why the restart policy is a red herring), N2 (why the live cause may be mechanism (i), not B1), and the Runtime State Inventory (the Coolify app UUID u7x0xw0y4qvcgeh8vyidsgyi; the policy is NOT declared in the repo — docker-compose.dev.yml:35,52,73 is DEV-ONLY); dashboard/api/routes/settings.py:33-36 (paper_trades_completed = COUNT(*) over ALL alpaca_trades rows)</read_first>
  <action>
  Produce `.planning/phases/19-reliable-runtime/19-EVIDENCE.md` containing:

  1. **The RED→GREEN table.** One row per must-fail case — **1, 1b, 5, 5b, 6, 13, 15, 17, 22, 23, 26** —
     with the case name, the VERBATIM pre-fix failure line (from 19-01-SUMMARY.md), the plan that closed
     it, and the post-fix PASS. If 19-01-SUMMARY did not capture a line verbatim, re-derive it by
     checking the pre-phase commit out into a TEMP WORKTREE and running the case there — **do NOT
     `git checkout` in this canonical checkout** (CLAUDE.md rule 20: `git worktree add`, then
     `git worktree remove`). A case that passed pre-fix was never testing the bug: if any of the eleven
     did, say so LOUDLY and treat the phase as NOT shippable.
     **Call out case 1b explicitly as the phase's real proof** — it drives the whole tick with a
     SUCCEEDING revive. Case 1 alone would have gone green against a tick order in which
     `_revive_dead_bots` respawns every crash-looping thread before the liveness check, so that
     `alive > 0` on every tick and the alert never fires in production.

  2. **The full-case table.** All 29 cases (+ 1b, 5b, 12b) with PASS / SKIP(reason) / FAIL. Paste the
     raw `python -m pytest tests/ dashboard/api/tests/ -q | tail -3` output verbatim, with counts.
     Note that cases 16 and 18 are numerically vacuous by construction (zeros sum to zero) and that
     their STATIC halves carry the weight — do not overclaim them.

  3. **The fence sweep.** F1-F8, each with the exact command and its output:
       F1  `git diff <pre-phase-sha>..HEAD | grep -icE "^\+.*(UPDATE|DELETE).*alpaca_trades"` -> 0
           `git diff <pre-phase-sha>..HEAD --stat -- src/backfill.py` -> EMPTY
       F2  BotUpdate.kelly_fraction still `le=0.25`; the 5%/quarter-Kelly/20%-DD/limit-only/50-trade
           literals unchanged
       F3  `mode="paper"` and `win_rate_target=40.0` still present; the gate comparison unchanged
       F4  `grep -riE "sendgrid|resend|postmark|mailgun" src/ dashboard/` -> NOTHING
       F5  no diff to min_confluence / kelly_fraction / quarantined_symbols DEFAULT VALUES
       F6  `grep -cE "AlpacaClient|ALPACA_API_KEY" src/bot_manager.py` -> 0; reconcile still routes
           through `_client_for_bot`
       F7  no Coolify env/config mutation anywhere in the phase
       F8  `python -m pytest -q` >= 428 passed / 28 skipped

     Plus the two structural greps that prove the plan-check blockers are actually closed:
       `grep -n "if not any_alive" src/bot_manager.py` -> NOTHING
       `sed -n '/def _check_bots_down/,/    def /p' src/bot_manager.py | grep -c "is_alive()"` -> **0**
           (the alert cannot see post-revive liveness — B1 closed)
       `sed -n '/def _has_keys/,/^$/p' src/bot_manager.py | grep -c "alpaca_secret_key"` -> **1**
           (an empty-SECRET row is keyless — B2 closed; `src/config.py:74-76` does not raise, so nothing
           downstream would have caught it)

  3b. **`git diff --stat requirements*.txt dashboard/web/package.json`** -> EMPTY. This phase installs
      NO packages (RESEARCH: "Package Legitimacy Audit: N/A"). Anything here STOPS the phase — an
      unaudited dependency entered a trading system.

  4. **The Coolify read-only report.** Query the restart policy for app `u7x0xw0y4qvcgeh8vyidsgyi`
     (project "AI Predicted Wins") via the Coolify API — **READ ONLY. Change NOTHING.** Record the
     value. Then state plainly: **this is a RED HERRING for this outage (research N4). The container was
     UP the whole time — it served `/api/health` and the dashboard rendered `status: stopped`. Only the
     bot THREADS were dead or never spawned. No restart policy — `always`, `unless-stopped`, or
     otherwise — restarts a thread inside a live process.** If the policy is wrong, report it as a
     SEPARATE, human-authorized follow-up. If the Coolify API is unreachable, record "unknown" and move
     on — do not block.

  5. **The diagnosis for the record (research N2).** Pull the dashboard container logs READ-ONLY and
     record which mechanism actually fired: `BotManager started` vs `BotManager unavailable: …` vs
     `BotManager: starting 0 enabled bots`. RESEARCH's A3 predicts mechanism (i) — the manager never
     started — because `seed_bots.py:167-176` back-patches NULL/empty keys from the dashboard's
     `ALPACA_API_KEY_{A,B,C,D}` env vars on every run, so the `bots` rows very likely DO have keys.
     **This does not change the plan** — Phase 19 fixed both mechanisms — but the record must not assume
     B1. Say which it was, or say the logs no longer reach back far enough.

  5b. **The paper-trade counter caveat (found in plan-check; no code change this phase).**
     `dashboard/api/routes/settings.py:33-36` sets `paper_trades_completed = COUNT(*)` over **ALL**
     `alpaca_trades` rows — including `submitted` and `rejected` rows that never held a position. So the
     50-paper-trade gate is counting non-trades. Phase 19 made the win rate and the bankroll honest but
     did **not** touch this counter. Record it as a known, unfixed overstatement in the gate readout and
     flag it for a follow-up (it belongs with the retune's measurement work, Phase 21). Do NOT fix it
     here — it is out of RUN-01/RUN-02's scope and would be an unplanned change to the gate.

  6. **REQUIRES HUMAN AUTHORIZATION (the loudest section).** State that the 395 historical `pnl = 0.0`
     sentinel rows in `alpaca_trades` were **read around and NOT repaired**; that `src/backfill.py`
     exists, already drives `reconciliation._client_for_bot`, and is a **loaded gun** that must not be
     pointed at prod; that repairing those rows is a WRITE to historical prod trade data requiring
     **explicit human authorization** (and the source data may no longer exist at Alpaca); and that
     until it is authorized, the `unresolved` count on the dashboard is the honest representation of
     them. Prove the fence: `alpaca_trades` is byte-identical over the phase.

  7. **"The gate may now read worse."** State explicitly that making the paper-gate readout honest
     (dropping ~395 fabricated losses from the denominator AND replacing the hardcoded
     `100_000.0 * len(bot_ids)` bankroll) may move the win rate and equity in EITHER direction, that a
     WORSE reading is the correct and intended outcome, and that it must NOT be tuned away. The gate
     stays closed. The retune is **Phase 21**, on the sentinel-free sample this phase's honest headline
     finally makes measurable.
  </action>
  <verify>
    <automated>python -m pytest tests/ dashboard/api/tests/ -q 2>&1 | tail -3 && python -m pytest tests/test_phase19_fences.py -q 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/ dashboard/api/tests/ -q | tail -1` — **>= 428 passed, 28 skipped, 0 failed** (fence F8). Pasted verbatim into 19-EVIDENCE.md.
    - `python -m pytest tests/test_phase19_fences.py -q` — all 8 fences green.
    - `git diff <pre-phase-sha>..HEAD | grep -icE "^\+.*(UPDATE|DELETE).*alpaca_trades"` prints **0**. Not one write to the trade log.
    - `git diff <pre-phase-sha>..HEAD --stat -- src/backfill.py` is **EMPTY** — the loaded gun was not touched, let alone fired.
    - `git diff <pre-phase-sha>..HEAD --stat -- requirements.txt dashboard/web/package.json` is **EMPTY** — zero packages installed.
    - `grep -n "if not any_alive" src/bot_manager.py` returns **NOTHING**; `sed -n '/def _check_bots_down/,/    def /p' src/bot_manager.py | grep -c "is_alive()"` returns **0**; `sed -n '/def _has_keys/,/^$/p' src/bot_manager.py | grep -c "alpaca_secret_key"` returns **1**. All three are pasted into the evidence doc — they are the proof that both plan-check blockers are closed.
    - 19-EVIDENCE.md contains all ELEVEN must-fail cases with a verbatim pre-fix failure line each, and sections titled exactly `REQUIRES HUMAN AUTHORIZATION` and `The gate may now read worse`.
  </acceptance_criteria>
  <done>Every claim in the phase has an artifact behind it, and the three things deliberately NOT done (the 395-row repair, the Coolify restart policy, the paper-trade counter) are named, justified, and handed back to a human.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 19 — Reliable Runtime & Honest Monitoring, complete:
      - **THE KILLER BUG IS DEAD.** `src/bot_manager.py:189-190`'s `if not any_alive: return` is gone.
        All-bots-down now fires the loudest alert in the system and repeats hourly until a bot is alive.
      - **And it actually fires.** Liveness is snapshotted at tick START, before the revive can respawn
        crash-looping threads — otherwise the alert would have been silent in the dominant failure mode
        (a bot that dies and is revived every cycle).
      - Both key predicates deleted, and a bot missing EITHER Alpaca key (api OR secret) is now
        `status='error'` + an alert — never invisible, and never respawned into a 401 loop every 60s.
      - `runtime_heartbeat` (migration 019 + the `db_schema.sql` mirror) makes the manager's liveness
        queryable from OUTSIDE the process; absence-of-row means DEAD, never healthy.
      - `main.py`'s except branch alerts when the BotManager never starts.
      - The alerter self-checks whether it can send, AND reports when its last send failed.
      - `reconcile()` is per-bot guarded and runs hourly from the tick that already existed.
      - The headline P&L is the RECONCILED number, flagged with `pnl_source` and `stale`.
      - `pnl = 0.0` sentinels are no longer counted as losses at any of the five reader sites.
      - `settings.py`'s hardcoded `100_000.0 * len(bot_ids)` bankroll is gone.
  </what-built>
  <how-to-verify>
    1. `python -m pytest tests/ dashboard/api/tests/ -q | tail -3` — expect >= 428 passed / 28 skipped, 0 failed.
    2. `grep -n "if not any_alive" src/bot_manager.py` — expect NOTHING. That grep returning nothing is
       the whole phase.
    3. `sed -n '/def _check_bots_down/,/    def /p' src/bot_manager.py | grep -c "is_alive()"` — expect
       **0**. The alert reads liveness from a pre-revive snapshot, so a crash-looping bot cannot mask it.
    4. Open `.planning/phases/19-reliable-runtime/19-EVIDENCE.md`. Read `REQUIRES HUMAN AUTHORIZATION`,
       the Coolify report, and the paper-trade-counter caveat.
    5. Open the dashboard screenshots from 19-06-SUMMARY.md. Confirm you can SEE: the P&L source badge
       (reconciled / alpaca_live / trade_log), the stale flag, the `unresolved` count sitting BESIDE
       wins/losses (not inside losses), and the `manager_alive` / `alerts_configured` states.
    6. **Look at the paper-gate readout.** The honest win rate and the real bankroll may make it read
       WORSE than before. **That is correct.** Confirm you accept the honest number and do NOT want it
       tuned — the retune is Phase 21.

    DECISIONS THIS CHECKPOINT IS ASKING YOU FOR:
      (a) Do you authorize a FUTURE, SEPARATE task to repair the 395 historical `pnl = 0.0` rows from
          Alpaca activity history? **Phase 19 did not do it and will not.** (Default: NO.)
      (b) The Coolify restart policy is reported read-only and was NOT changed. Do you want a separate
          follow-up? (It is a red herring for THIS outage — the container was up; only the threads
          were dead.)
      (c) `alerts_configured` / `alerts_last_error` on prod: if configured is FALSE — or last_error is
          non-null — every alert this phase added is silently dropped and the system is not monitored.
          Confirm the SES env vars (`AWS_ACCESS_KEY_ID` / `AWS_SECRET_ACCESS_KEY` / `AWS_DEFAULT_REGION`)
          are set on the dashboard service, or authorize setting them.
      (d) `paper_trades_completed` counts ALL `alpaca_trades` rows (incl. `submitted`/`rejected`), so the
          50-trade gate is counting non-trades. Fix it in a follow-up? (Phase 19 did not touch it.)
  </how-to-verify>
  <resume-signal>Type "approved" (plus your answers to a/b/c/d), or describe what is wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| verification → prod DB | an evidence query that writes is the exact failure this phase exists to prevent |
| verification → Coolify | a config read that becomes a config write is an unauthorized prod change |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-28 | Tampering | an evidence step "helpfully" backfilling the 395 rows | mitigate | SELECT-only. Fence F1 grep-asserts zero `UPDATE`/`DELETE` on `alpaca_trades` across the whole phase diff, and that `src/backfill.py` is untouched |
| T-19-29 | Tampering | the Coolify check mutating the restart policy | mitigate | READ-ONLY API call. Fence F7. If unreachable, record "unknown" — never "fix" it |
| T-19-30 | Tampering | a worktree-less `git checkout` of the pre-phase SHA thrashing a sibling session's HEAD | mitigate | CLAUDE.md rule 20 — `git worktree add` into a temp dir, then `git worktree remove`. NEVER `git checkout` in the canonical tree |
| T-19-31 | Elevation of Privilege | an honest-but-worse paper gate being tuned back to passing | mitigate | the checkpoint asks the human to explicitly ACCEPT the worse number; fence F3 pins mode/target/comparison |
| T-19-39 | Repudiation | declaring the killer bug dead on case 1 alone | mitigate | the evidence doc must show case **1b** (integrated tick, succeeding revive) green, plus the `_check_bots_down`-contains-no-`is_alive()` grep |
| T-19-SC | Tampering | package installs | mitigate | `git diff --stat` on `requirements.txt` / `package.json` must be EMPTY; a non-empty diff STOPS the phase |
</threat_model>

<verification>
- Full suite: >= 428 passed / 28 skipped, zero failed.
- All 29 cases (+1b, +5b, +12b) and 8 fences accounted for in 19-EVIDENCE.md.
- `alpaca_trades` byte-identical across the phase diff.
- Zero packages installed. Zero Coolify mutations.
</verification>

<success_criteria>
The phase can prove — with commands and output, not assertions — that the killer bug is dead *in the
call shape production actually uses*, that the numbers are honest, that the trade log was not touched,
and that the three things deliberately left undone are named and handed to a human.
</success_criteria>

<output>
Create `.planning/phases/19-reliable-runtime/19-07-SUMMARY.md` when done.
</output>
</content>
