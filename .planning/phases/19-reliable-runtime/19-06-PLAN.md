---
phase: 19-reliable-runtime
plan: 06
type: execute
wave: 4
depends_on: [19-04]
files_modified:
  - dashboard/api/models.py
  - dashboard/api/routes/portfolio.py
  - dashboard/api/routes/settings.py
  - dashboard/web/types/index.ts
  - dashboard/web/app/page.tsx
  - dashboard/web/app/settings/page.tsx
  - dashboard/web/components/settings/BotStatus.tsx
autonomous: true
requirements: [RUN-02, RUN-01]

must_haves:
  truths:
    - "The headline P&L is the Alpaca-derived RECONCILED number (alpaca_realized_pnl + live unrealized), NOT the trade-log sum. That is the whole of RUN-02."
    - "portfolio.py:111-122 SILENTLY falls back from Alpaca to the trade-log sum today with NO flag on the response. The consumer can no longer be misled about which number it is looking at: `pnl_source` ∈ {reconciled, alpaca_live, trade_log} is always present."
    - "`stale: bool` is surfaced — no reconciliation row, OR checked_at older than 2 x RECONCILE_INTERVAL_HOURS. The number is STILL SHOWN when stale, but FLAGGED. The dashboard never presents an unreconciled figure as reconciled."
    - "manager_alive is computed from heartbeat ABSENCE or STALENESS and NEVER defaults healthy (research N10). A reader that default-trusts a missing row reintroduces the exact silent failure this phase exists to kill."
    - "alerts_configured is surfaced as a BOOL. Never a key, never a key prefix, never a region, never an env var value (ASVS V4 / the security fence)."
    - "Research N8: the API additions are runtime-SAFE (every Pydantic field has a default; TS ignores extra JSON keys) — but the new fields are INVISIBLE until the TS interfaces AND the components that render them are extended. RUN-02 says the headline REFLECTS reconciled numbers, so the flag has to be VISIBLE. The frontend IS in scope."
    - "No SSE, no WebSocket, no new poll loop. CONTEXT decision 5 DEFERS the live push channel. This phase's job is that the truth EXISTS and is QUERYABLE, not that it streams."
    - "Making the paper gate honest may make it read WORSE. Do not 'fix' that."
  artifacts:
    - path: "dashboard/api/models.py"
      provides: "pnl_source / stale / unresolved / reconciled on PortfolioData; manager_alive / alerts_configured / bots_alive / bots_enabled / last_heartbeat on HealthStatus"
      contains: "pnl_source"
    - path: "dashboard/api/routes/portfolio.py"
      provides: "headline = reconciled alpaca_realized_pnl + unrealized; pnl_source; stale"
      contains: "pnl_source"
    - path: "dashboard/api/routes/settings.py"
      provides: "manager_alive from the heartbeat; alerts_configured from the notifier self-check"
      contains: "manager_alive"
    - path: "dashboard/web/types/index.ts"
      provides: "the TS interfaces that make the new fields visible instead of silently dropped"
      contains: "pnl_source"
  key_links:
    - from: "dashboard/api/routes/portfolio.py"
      to: "reconciliation table"
      via: "SELECT the per-bot row; headline P&L = alpaca_realized_pnl + unrealized"
      pattern: "FROM reconciliation"
    - from: "dashboard/api/routes/settings.py"
      to: "dashboard/api/db.py::heartbeat_is_fresh"
      via: "manager_alive — absence and staleness BOTH mean dead"
      pattern: "heartbeat_is_fresh"
    - from: "dashboard/web/app/page.tsx"
      to: "Portfolio.pnl_source / Portfolio.stale"
      via: "a visible badge — an unreconciled number is never presented as reconciled"
      pattern: "pnl_source|stale"
---

<objective>
The headline finally shows the reconciled number, and says so when it can't.

Purpose: `dashboard/api/migrations/017_reconciliation.sql`'s header says *"Consumed by the dashboard
headline in Phase 19."* 19-05 made the reconciliation actually run. This plan is what reads it. And
`portfolio.py:111-122` currently falls back from the live Alpaca number to the raw trade-log sum
**silently, with no flag on the response** — so nobody can tell which number they are looking at.
Meanwhile the runtime truth 19-02/19-03 made queryable (`runtime_heartbeat`, `alerts_configured`) is
sitting in the database with nothing surfacing it.

Output: `pnl_source` / `stale` / `reconciled` / `unresolved` on the portfolio envelope;
`manager_alive` / `alerts_configured` / `bots_alive` / `bots_enabled` / `last_heartbeat` on the health
surface; and the frontend types + components that actually render them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-reliable-runtime/19-RESEARCH.md
@.planning/phases/19-reliable-runtime/19-01-SUMMARY.md
@.planning/phases/19-reliable-runtime/19-02-SUMMARY.md
@.planning/phases/19-reliable-runtime/19-04-SUMMARY.md
@$HOME/.claude/design-preferences.md
@CLAUDE.md

<interfaces>
Available from 19-02 / 19-04 / 19-05:
  dashboard/api/db.py:  HEARTBEAT_STALE_SECONDS: int          # env, default 180
                        get_heartbeat(conn, component="bot_manager") -> dict | None   # None when NO ROW
                        heartbeat_is_fresh(beat_at, now=None, stale_seconds=None) -> bool  # None -> False
  src/notifier.py:      alerts_configured() -> bool
  reconciliation table (017): bot_id PK, checked_at, trade_log_pnl, alpaca_realized_pnl, delta,
                              within_tolerance, tolerance     # UPSERT, one row per bot, hourly (19-05)
  Both routes already compute `unresolved` (19-04).

Model additions (add ONLY these; every field gets a DEFAULT — research N8, backward-compatible):
  PortfolioData += pnl_source: str = "trade_log"      # {"reconciled","alpaca_live","trade_log"}
                += stale: bool = True
                += unresolved: int = 0
                += reconciled: Optional[dict] = None  # {alpaca_realized_pnl, trade_log_pnl, delta,
                                                      #  within_tolerance, checked_at}
  HealthStatus  += manager_alive: bool = False
                += alerts_configured: bool = False
                += bots_alive: int = 0
                += bots_enabled: int = 0
                += last_heartbeat: Optional[str] = None
  SettingsData  += unresolved: int = 0

Headline derivation (the reconciler's own identity — src/reconciliation.py:33 —
`alpaca_realized = (equity - starting) - unrealized`, so `starting + alpaca_realized + unrealized = equity`):
  reconciliation row present:
      pnl_source = "reconciled"
      total_pnl  = row.alpaca_realized_pnl + unrealized      (unrealized from the live acct; 0.0 if no acct)
      equity     = acct.equity if acct else starting_equity + total_pnl
  no row, Alpaca acct reachable:
      pnl_source = "alpaca_live";  total_pnl = equity - starting_equity        # today's :111-118 path
  no row, Alpaca unreachable:
      pnl_source = "trade_log";    total_pnl = closed_pnl (RESOLVED-only, from 19-04)   # today's :119-122
  stale = (no row) OR (now - checked_at) > 2 * RECONCILE_INTERVAL_HOURS

The frontend surfaces to extend (research N8 — TS silently drops unknown JSON keys, so these fields
are INVISIBLE until they are added here):
  dashboard/web/types/index.ts:1-14    interface Portfolio
  dashboard/web/types/index.ts:106-126 interface BotSettings (incl. its nested `health` object)
  dashboard/web/app/page.tsx                    — the headline P&L card
  dashboard/web/app/settings/page.tsx           — the paper-gate / health panel
  dashboard/web/components/settings/BotStatus.tsx — per-bot runtime status
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: models + the reconciled headline with pnl_source/stale (cases 19, 20, 21)</name>
  <files>dashboard/api/models.py, dashboard/api/routes/portfolio.py</files>
  <read_first>dashboard/api/models.py L32-44 (PortfolioData — note EVERY field already has a default, which is why additions are runtime-safe, research N8), L149-171 (HealthStatus + SettingsData); dashboard/api/routes/portfolio.py L47-60 (the bots-row read: starting_equity + the per-bot Alpaca keys), L103-137 (the Alpaca fetch at :109 and the SILENT fallback at :111-122 — the whole point of pnl_source — and the PortfolioData construction at :124-137); src/reconciliation.py L16-42 (reconcile_bot's identity: alpaca_realized = (equity - starting) - unrealized — the headline derivation depends on it); src/db.py:334-356 (record_reconciliation — the exact column names the route SELECTs); dashboard/api/tests/test_portfolio_win_rate.py cases 19, 20, 21 (the RED spec)</read_first>
  <behavior>
    - reconciliation row {alpaca_realized_pnl: +500, trade_log_pnl: +900}: headline total_pnl reports
      +500 (+ live unrealized), NOT +900. pnl_source == "reconciled".
    - No row, Alpaca reachable -> pnl_source == "alpaca_live", total_pnl == equity - starting_equity.
    - No row, Alpaca unreachable -> pnl_source == "trade_log", total_pnl == the RESOLVED-only closed sum.
    - stale is True when there is no row, True when checked_at is 3h old (interval=1h), False at 30min.
    - The reconciled dict (alpaca_realized_pnl, trade_log_pnl, delta, within_tolerance, checked_at) is
      returned so the frontend can show the out-of-tolerance flag.
  </behavior>
  <action>Add the PortfolioData / HealthStatus / SettingsData fields listed in <interfaces> to dashboard/api/models.py — **each with a default**, so existing consumers are unaffected (research N8). In portfolio.py's `_portfolio_for_bot`, add `_reconciliation_for_bot(conn, bot_id) -> dict | None` (`SELECT checked_at, trade_log_pnl, alpaca_realized_pnl, delta, within_tolerance FROM reconciliation WHERE bot_id = %s`, wrapped in try/except -> None so a pre-migration DB does not 500). Then replace the SILENT fallback at :111-122 with the three-branch derivation in <interfaces>, setting `pnl_source` and `stale` on EVERY branch (never leave them unset — an absent flag is how the current bug hides). `stale = row is None or (now - checked_at) > 2 * RECONCILE_INTERVAL_HOURS * 3600` — read the interval from the same `RECONCILE_INTERVAL_HOURS` env var 19-05 uses (default 1). Pass `reconciled=<the dict or None>`, `pnl_source`, `stale`, and the `unresolved` count 19-04 already computes into PortfolioData. Keep `_fetch_alpaca_account`'s 8s timeout and its `{}`-on-failure contract unchanged. Do NOT re-derive the P&L math — reuse the reconciler's stored `alpaca_realized_pnl`. Do NOT write to alpaca_trades. Do NOT add SSE or a poll loop.</action>
  <verify>
    <automated>python -m pytest dashboard/api/tests/test_portfolio_win_rate.py -q 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest dashboard/api/tests/test_portfolio_win_rate.py -q` — cases **19, 20, 21 GREEN** (all previously RED); 15/16/18 still green.
    - `python -c "import sys; sys.path.insert(0,'dashboard/api'); from models import PortfolioData, HealthStatus; p=PortfolioData(); h=HealthStatus(); print(p.pnl_source, p.stale, p.unresolved, p.reconciled, h.manager_alive, h.alerts_configured)"` prints `trade_log True 0 None False False` — **every default is the PESSIMISTIC one. A missing signal is never healthy.**
    - `grep -c "pnl_source" dashboard/api/routes/portfolio.py` returns **>= 3** (one per branch — reconciled / alpaca_live / trade_log).
    - `grep -n "FROM reconciliation" dashboard/api/routes/portfolio.py` returns a hit.
    - `grep -cE "UPDATE alpaca_trades|DELETE FROM alpaca_trades|backfill|EventSource|WebSocket" dashboard/api/routes/portfolio.py` returns **0** (no trade write; no SSE — CONTEXT defers it).
  </acceptance_criteria>
  <done>The headline is the reconciled number, and when it isn't, it says so. The silent Alpaca→trade-log fallback is now a labelled fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the health surface — manager_alive (absence ⇒ dead) + alerts_configured (case 26)</name>
  <files>dashboard/api/routes/settings.py</files>
  <read_first>dashboard/api/routes/settings.py L68-88 (the `running` flag today: mgr.status() with a fallback to the bots.status COLUMN — both of which say "stopped" without saying WHY), L113-144 (the health block: HealthStatus(claude_cli, alpaca_api, database, db_size_mb) and the SettingsData construction); dashboard/api/db.py (get_heartbeat / heartbeat_is_fresh / HEARTBEAT_STALE_SECONDS from 19-02); dashboard/api/tests/test_heartbeat.py + test_routes.py case 26 (the RED spec)</read_first>
  <behavior>
    - Empty runtime_heartbeat -> GET /api/settings reports manager_alive == False. NEVER True.
    - beat_at 300s old (stale=180) -> manager_alive == False. beat_at 30s old -> True.
    - alerts_configured is a bool, and no AWS/Alpaca key material appears anywhere in the response body.
    - bots_alive / bots_enabled / last_heartbeat ride the same HealthStatus object.
  </behavior>
  <action>In settings.py's health block: read the heartbeat with `get_heartbeat(conn)` and set `manager_alive = heartbeat_is_fresh(hb["beat_at"]) if hb else False` — **the `else False` is load-bearing** (research N10: `self._watchdog.start()` at bot_manager.py:79 sits AFTER the start_all query that can throw at :67-70, so a heartbeat written BY the watchdog cannot report its own non-existence; ABSENCE IS THE SIGNAL and the reader must NEVER default healthy). Populate `bots_alive` / `bots_enabled` / `last_heartbeat` (ISO string) from the same row, defaulting to 0/0/None when absent. Set `alerts_configured` by calling `src.notifier.alerts_configured()` inside a try/except that yields **False** on any failure (a failing self-check is not a healthy one). Leave the existing `running` flag as-is (it answers a different question — "does the manager think a thread is alive"; `manager_alive` answers "is the manager itself alive at all"). Pass the `unresolved` count 19-04 computes into SettingsData. Surface **BOOLS ONLY** — never an AWS key, a key prefix, a region, or any env var value (ASVS V4; the entire status_detail vocabulary for a broken bot is the literal "missing alpaca keys"). Do NOT change mode, win_rate_target, equity_target, or the gate comparison (fence F3). Do NOT add SSE or a poll loop (CONTEXT decision 5 defers it).</action>
  <verify>
    <automated>python -m pytest dashboard/api/tests/test_heartbeat.py dashboard/api/tests/test_routes.py -q 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest dashboard/api/tests/test_heartbeat.py dashboard/api/tests/test_routes.py -q` — case **26 GREEN** end-to-end through `GET /api/settings` (previously RED), 17/22 still green.
    - `grep -n "heartbeat_is_fresh" dashboard/api/routes/settings.py` returns a hit, and the same line (or the next) carries an explicit `else False` / `if hb else False`.
    - `grep -n "manager_alive=True\|manager_alive = True" dashboard/api/routes/settings.py` returns **NOTHING** — there is no unconditional healthy path.
    - `grep -icE "AWS_ACCESS_KEY|AWS_SECRET|alpaca_api_key|alpaca_secret" dashboard/api/routes/settings.py` returns **0** — no credential ever reaches the response.
    - `python -m pytest tests/test_phase19_fences.py -q` — green (F3: mode still "paper", win_rate_target still 40.0).
  </acceptance_criteria>
  <done>"BotManager never started" now has a visible, queryable signature on the health surface instead of a swallowed warning line — and the alerter reports whether it can actually send.</done>
</task>

<task type="auto">
  <name>Task 3: make it VISIBLE — TS interfaces + the three components (research N8)</name>
  <files>dashboard/web/types/index.ts, dashboard/web/app/page.tsx, dashboard/web/app/settings/page.tsx, dashboard/web/components/settings/BotStatus.tsx</files>
  <read_first>dashboard/web/types/index.ts L1-14 (interface Portfolio) and L106-126 (interface BotSettings + its nested `health` object) — these are why the new fields are currently invisible; dashboard/web/app/page.tsx (the headline P&L card — where the badge goes); dashboard/web/app/settings/page.tsx (the paper-gate + health panel); dashboard/web/components/settings/BotStatus.tsx (per-bot runtime status); ~/.claude/design-preferences.md (color/spacing/typography — read BEFORE any UI decision)</read_first>
  <action>Extend `interface Portfolio` with `pnl_source: "reconciled" | "alpaca_live" | "trade_log"; stale: boolean; unresolved: number; reconciled?: { alpaca_realized_pnl: number; trade_log_pnl: number; delta: number; within_tolerance: boolean; checked_at: string } | null;` and `interface BotSettings` with `unresolved: number` plus `health.manager_alive: boolean; health.alerts_configured: boolean; health.bots_alive: number; health.bots_enabled: number; health.last_heartbeat: string | null`. Then RENDER them — the whole point of RUN-02 is that the flag is VISIBLE (research N8: TS silently drops unknown JSON keys, so without this the fields exist and nobody ever sees them): (a) `app/page.tsx` — a small badge beside the headline P&L showing the source ("Reconciled" / "Alpaca live" / "Trade log"), and a warning treatment when `stale` is true. An unreconciled number is NEVER presented as reconciled. Show `unresolved` beside wins/losses (label it "unresolved", never "losses"). (b) `app/settings/page.tsx` — surface `manager_alive` (a hard-red "BotManager NOT RUNNING" state when false), `alerts_configured` (a warning when false: "alerts are NOT configured — this system cannot tell you when it breaks"), and `bots_alive / bots_enabled`. (c) `components/settings/BotStatus.tsx` — show a bot in `status='error'` with its `status_detail` ("missing alpaca keys") rather than rendering it as merely stopped. Also surface `within_tolerance === false` as a visible reconciliation-breach flag (ROADMAP success criterion 3). Follow ~/.claude/design-preferences.md for color/spacing/type. Add NO new dependency, NO chart library, NO SSE/EventSource/WebSocket, NO new poll loop (CONTEXT decision 5 DEFERS the live push channel).</action>
  <verify>
    <automated>cd dashboard/web && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd dashboard/web && npx tsc --noEmit` — exits 0, zero type errors.
    - `cd dashboard/web && npm run build` — succeeds.
    - `grep -c "pnl_source" dashboard/web/types/index.ts dashboard/web/app/page.tsx` — a hit in BOTH (typed AND rendered; a type with no renderer is invisible, which is the bug).
    - `grep -n "manager_alive" dashboard/web/app/settings/page.tsx` returns a hit.
    - `grep -rnE "EventSource|new WebSocket|setInterval\(.*fetch" dashboard/web/app/page.tsx dashboard/web/app/settings/page.tsx` returns **NOTHING** (no SSE, no new poll loop — CONTEXT defers both).
    - EVIDENCE (CLAUDE.md verification loop): run the dashboard locally, drive it with a real browser, and capture screenshots of (1) the headline with a `reconciled` badge, (2) the headline with a `stale` warning, (3) the settings panel with `manager_alive=false`. A blank/error page is NOT done. Attach the paths to the SUMMARY.
  </acceptance_criteria>
  <done>The reconciled number, the staleness flag, the unresolved count, the dead-manager state, and the unconfigured-alerter warning are all VISIBLE to a human looking at the dashboard — not just present in a JSON body nobody renders.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| API → browser | the new health/portfolio fields ride the authed `/api/settings` and `/api/portfolio` routes |
| reconciliation row → headline | a stale row must never be presented as current |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-24 | Information Disclosure | credential material on the new health surface | mitigate | booleans and ints ONLY. `alerts_configured: bool`; grep-asserted that no AWS/Alpaca key name appears in settings.py's response path (ASVS V4) |
| T-19-25 | Spoofing | a stale/absent reconciliation presented as the reconciled truth | mitigate | `pnl_source` is set on EVERY branch and `stale` defaults to `True`; the frontend renders a warning treatment when stale |
| T-19-26 | Spoofing | a missing heartbeat row rendering as healthy | mitigate | `manager_alive` defaults to `False` in the model AND in the route (`if hb else False`); grep-asserted there is no unconditional `manager_alive = True` |
| T-19-27 | Repudiation | new fields that exist in JSON but are never rendered (research N8) | mitigate | Task 3 extends the TS interfaces AND the three components; browser screenshots are required evidence |
| T-19-SC | Tampering | package installs (npm) | mitigate | **NO new packages.** If any task believes it needs one, STOP — the phase installs nothing (RESEARCH: "Package Legitimacy Audit: N/A — this phase installs no external packages") |
</threat_model>

<verification>
- `python -m pytest dashboard/api/tests/ -q` — all green.
- `cd dashboard/web && npx tsc --noEmit && npm run build` — clean.
- `git diff --stat dashboard/web/package.json` — **EMPTY**. No new dependency.
- `grep -rniE "sendgrid|resend|EventSource|WebSocket" dashboard/web/app dashboard/web/components` — NOTHING new.
- Browser evidence captured for the three states (reconciled / stale / manager-down).
</verification>

<success_criteria>
A human looking at the dashboard can see: what the P&L number actually IS (reconciled vs live vs trade
log), whether it is stale, how many trades could not be resolved, whether the BotManager is alive at
all, and whether the alerter can even send. None of that was visible before.
</success_criteria>

<output>
Create `.planning/phases/19-reliable-runtime/19-06-SUMMARY.md` when done, with the three browser
screenshot paths.
</output>
</content>
