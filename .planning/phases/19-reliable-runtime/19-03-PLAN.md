---
phase: 19-reliable-runtime
plan: 03
type: execute
wave: 3
depends_on: [19-02]
files_modified:
  - src/bot_manager.py
  - dashboard/api/main.py
autonomous: true
requirements: [RUN-01]

must_haves:
  truths:
    - "THE KILLER BUG DIES HERE. src/bot_manager.py:186-190 does `if not any_alive: return` BEFORE the trade-silence alert. All-bots-down — the single most important state in the system — is GUARANTEED SILENT today, because it defers to a death alert that CANNOT fire (the key filter at :104-107 already removed the rows). Four bots stopped and nothing said a word. This is a two-line delete plus one new alert."
    - "The key predicate (`AND alpaca_api_key IS NOT NULL AND alpaca_api_key != ''`) is byte-identical at :67-70 (start_all) and :104-107 (_revive_dead_bots). A keyless-but-enabled bot is invisible to BOTH — never spawned, never revived, never alerted. Both predicates are DELETED. One query: `WHERE enabled = TRUE`."
    - "A keyless enabled bot becomes status='error' + status_detail='missing alpaca keys' + ONE alert (1h cooldown). An enabled bot must always be either RUNNING or LOUDLY BROKEN. 'Invisible' is not a state we ship."
    - "The keyless branch runs BEFORE _maybe_send_death_alert (research N5). Otherwise a keyless bot has thread is None on the very first tick, gets a death alert about a thread that NEVER EXISTED, and then _spawn attempts a credential-less bot EVERY 60 SECONDS FOREVER — the 1h cooldown suppresses the EMAIL, not the spawn."
    - "manager_alive is computed from row ABSENCE or STALENESS, never default-healthy. The never-started ALERT fires from dashboard/api/main.py's except branch (:65-66), NOT from BotManager — the watchdog CANNOT report its own non-existence, because self._watchdog.start() (:79) sits AFTER the start_all query that can throw (:67-70). Research N10."
    - "The keyless fix routes through _on_status_change (:299-309) — the SOLE SQL writer of bots.status — and is therefore thread-class-agnostic. RESEARCH REFUTED the CONTEXT claim that BotThread is the only status writer: CopyTraderThread (Bot E) has a second _set_status at copytrade_thread.py:140-144. _spawn's strategy dispatch (:287-292) must keep working."
    - "The Coolify restart policy is NOT part of this fix (research N4). The container is UP — it serves the API. Only the THREADS are dead. No restart policy restarts a thread. It is verified read-only and reported in 19-07."
    - "No write to alpaca_trades. Not one."
  artifacts:
    - path: "src/bot_manager.py"
      provides: "_enabled_rows (no key predicate), keyless->error branch, _check_bots_down, _heartbeat, the deleted any_alive escape, the startup alert-path self-check"
      contains: "_check_bots_down"
    - path: "dashboard/api/main.py"
      provides: "the never-started alert fired from the lifespan's except branch"
      contains: "alert_manager_never_started"
  key_links:
    - from: "src/bot_manager.py::_check_bots_down"
      to: "src/notifier.py::alert_all_bots_down"
      via: "zero live threads among enabled bots -> the loudest alert, 1h cooldown, repeats until one is alive"
      pattern: "alert_all_bots_down"
    - from: "src/bot_manager.py::_revive_dead_bots"
      to: "src/bot_manager.py::_on_status_change"
      via: "keyless bot -> ('error', 'missing alpaca keys') — the sole SQL writer, bot-class-agnostic"
      pattern: "missing alpaca keys"
    - from: "src/bot_manager.py::_heartbeat"
      to: "runtime_heartbeat"
      via: "UPSERT via self._pool each watchdog tick"
      pattern: "runtime_heartbeat"
    - from: "dashboard/api/main.py"
      to: "src/notifier.py::alert_manager_never_started"
      via: "the except branch at :65-66 — the manager cannot alert about its own non-existence"
      pattern: "alert_manager_never_started"
---

<objective>
Kill the bug that let four bots stop in silence.

Purpose: `src/bot_manager.py:189-190` reads, verbatim:

    if not any_alive:
        return  # bots are down; death alert handles that separately

The trade-silence alert — the ONLY alert that fires on "nothing is happening" — **disables itself
precisely when nothing is happening for the worst possible reason.** It defers to a death alert that
cannot fire, because that alert only iterates rows the key filter at `:104-107` already removed.
All-bots-down is the one state guaranteed to be silent. That is the outage.

Output: the two-line delete; both key predicates gone; keyless bots loudly broken instead of invisible;
a dedicated ALL BOTS DOWN alert that repeats until a bot is alive; a heartbeat the outside can query;
and a never-started alert fired from the one place that can fire it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-reliable-runtime/19-RESEARCH.md
@.planning/phases/19-reliable-runtime/19-01-SUMMARY.md
@.planning/phases/19-reliable-runtime/19-02-SUMMARY.md
@CLAUDE.md

<interfaces>
Available from 19-02 (already landed):
  src/notifier.py:  alerts_configured() -> bool
                    alert_all_bots_down(bots_enabled: int, hours_since_trade: float | None) -> bool
                    alert_bot_misconfigured(bot_id: str, label: str, reason: str) -> bool
                    alert_manager_never_started(error: str) -> bool
  runtime_heartbeat(component TEXT PK, beat_at TIMESTAMPTZ, bots_alive INT, bots_enabled INT)
    — migration 019 + the src/db_schema.sql section-11 mirror.

Existing, unchanged, and load-bearing:
  src/bot_manager.py:17-20   _WATCHDOG_INTERVAL=60, _DEATH_ALERT_COOLDOWN=3600,
                             _SILENCE_HOURS=env TRADE_SILENCE_ALERT_HOURS (24), _SILENCE_CHECK_INTERVAL=3600
  src/bot_manager.py:85-95   _watchdog_loop — each step has its OWN try/except; a raising step must
                             never kill the loop. New steps follow the SAME shape.
  src/bot_manager.py:121-139 _maybe_send_death_alert — the 1h-per-bot cooldown dict (:123-127) is the
                             pattern the new cooldowns copy.
  src/bot_manager.py:145-151 _check_trade_silence's interval guard — the interval-guard pattern.
  src/bot_manager.py:274-297 _spawn — dispatches CopyTraderThread when cfg.strategy == "copytrade",
                             else BotThread. Two thread classes. Keep it.
  src/bot_manager.py:299-309 _on_status_change — the SOLE SQL writer of bots.status. Every status
                             write goes through here.
  dashboard/api/main.py:46-71 the lifespan. :55 `if db_url:` gates the whole block; :65-66 swallows.

Contracts fixed by the 19-01 RED tests (implement EXACTLY — the tests import by name):
  BotManager._enabled_rows(self) -> list[dict]      # SELECT * FROM bots WHERE enabled = TRUE
  BotManager._check_bots_down(self) -> None
  BotManager._heartbeat(self) -> None
  _BOTS_DOWN_COOLDOWN = 3600
  _HEARTBEAT_COMPONENT = "bot_manager"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: drop both key predicates; keyless bot becomes loudly broken (cases 5-9b)</name>
  <files>src/bot_manager.py</files>
  <read_first>src/bot_manager.py L64-83 (start_all — the key predicate at :67-70 and the "starting %d enabled bots" log), L101-119 (_revive_dead_bots — the byte-identical key predicate at :104-107; note the death alert at :114 fires BEFORE the spawn at :117), L121-139 (_maybe_send_death_alert + its cooldown dict), L274-309 (_spawn's strategy dispatch, _on_status_change); src/bot_config.py (BotConfig.from_row — which columns it needs); tests/test_bot_manager.py cases 5, 6, 7, 8, 9, 9b (the RED spec)</read_first>
  <behavior>
    - A keyless enabled bot is VISIBLE: _on_status_change(bot_id, "error", "missing alpaca keys") is
      called on EVERY tick (status stays authoritative), and alert_bot_misconfigured fires ONCE per
      hour (cooldown), and _spawn is NEVER called for it, and NO death alert mentions it.
    - A keyed enabled bot with a live thread: nothing happens. No status write, no alert, no respawn.
    - A keyed enabled bot with a dead thread: death alert + respawn, exactly as today.
    - A copytrade bot with a dead thread respawns as CopyTraderThread, not BotThread.
  </behavior>
  <action>Extract the bots query into `_enabled_rows(self) -> list[dict]` running `SELECT * FROM bots WHERE enabled = TRUE` — **the key predicate is DELETED**. Call it from BOTH start_all (replacing :67-70) and _revive_dead_bots (replacing :104-107). In start_all, a keyless row must NOT be spawned: branch it to _on_status_change(bot_id, "error", "missing alpaca keys") + _maybe_send_misconfig_alert and `continue`. In _revive_dead_bots, insert the keyless branch **BEFORE** the `if thread is None or not thread.is_alive():` check — i.e. before _maybe_send_death_alert at :114 (research N5): if `not row.get("alpaca_api_key")`, call _on_status_change(bot_id, "error", "missing alpaca keys"), call _maybe_send_misconfig_alert(bot_id, row) (a new 1h-cooldown method mirroring _maybe_send_death_alert:121-139, with its OWN cooldown dict), and `continue` — do NOT spawn, do NOT death-alert. Getting this order wrong produces a death alert about a thread that never existed AND a credential-less spawn attempt every 60 seconds forever. Route the status write through _on_status_change (:299-309) — the SOLE SQL writer — so the fix is thread-class-agnostic (research REFUTED the "one status writer class" claim: CopyTraderThread has a second _set_status at copytrade_thread.py:140-144). Do NOT change _spawn's strategy dispatch. The status_detail string is the literal `"missing alpaca keys"` and NOTHING else — never a key, a key prefix, or an env var value.</action>
  <verify>
    <automated>python -m pytest tests/test_bot_manager.py -q 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_bot_manager.py -k "keyless or revive or healthy or strategy" -q` — cases **5, 6, 7, 8, 9, 9b GREEN** (5 and 6 were RED).
    - `grep -c "alpaca_api_key IS NOT NULL" src/bot_manager.py` returns **0** — both predicates are gone.
    - `grep -c "SELECT \* FROM bots WHERE enabled = TRUE" src/bot_manager.py` returns **1** (one query, called twice — not two copies).
    - `grep -n "missing alpaca keys" src/bot_manager.py` returns exactly 1 hit, and `sed -n '/def _revive_dead_bots/,/def _maybe_send_death_alert/p' src/bot_manager.py | grep -n "missing alpaca keys\|_maybe_send_death_alert"` shows the keyless line BEFORE the death-alert line (research N5 ordering).
    - `python -m pytest tests/test_phase19_fences.py -q` still green (no risk-rule / provider / retune drift).
  </acceptance_criteria>
  <done>A keyless enabled bot is now error+alerted instead of invisible, and it is not respawned in a 60-second loop forever. Healthy bots are byte-for-byte unaffected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DELETE the any_alive escape; add ALL BOTS DOWN + the heartbeat (cases 1-4, 24, 25)</name>
  <files>src/bot_manager.py</files>
  <read_first>src/bot_manager.py L145-212 (_check_trade_silence in full — the interval guard at :149-151, the silence-window guard at :154, the MAX(timestamp) query at :157-164, and THE KILLER at :186-190), L85-95 (the watchdog loop — each step gets its own try/except; new steps follow it exactly), L121-139 (the cooldown pattern); tests/test_bot_manager.py cases 1, 2, 3, 4, 24, 25 (the RED spec)</read_first>
  <behavior>
    - Zero live threads among enabled bots -> alert_all_bots_down fires. Repeats once per
      _BOTS_DOWN_COOLDOWN (3600s) window and KEEPS FIRING until at least one bot is alive.
    - At least one live thread -> ZERO all-bots-down alerts. The loudest alert must not cry wolf.
    - Bots alive + last trade 30h ago -> the trade-silence alert fires ON ITS OWN MERITS. The two
      alerts are now INDEPENDENT, not mutually suppressing.
    - Each watchdog tick UPSERTs runtime_heartbeat(component='bot_manager', beat_at=NOW(),
      bots_alive, bots_enabled).
    - A raising step never kills the watchdog loop.
  </behavior>
  <action>**DELETE lines 186-190 of src/bot_manager.py** — the `# Check at least one bot is running` comment, the `any_alive` computation, and `if not any_alive: return`. Trade silence is now evaluated on its own merits. (The body text at :208 that says "Both bots are currently running" must go too — it is no longer true by construction.) Add `_check_bots_down(self)`: count enabled bots via _enabled_rows(); under self._lock count threads that are alive; if enabled > 0 and alive == 0, fire alert_all_bots_down(bots_enabled, hours_since_trade) behind a 1h cooldown timestamp (`self._last_bots_down_alert`, mirroring _maybe_send_death_alert:123-127) — and RESET that timestamp to 0.0 whenever at least one bot IS alive, so a recovered-then-re-failed system alerts immediately rather than sitting in a stale cooldown. Add `_heartbeat(self)`: UPSERT into runtime_heartbeat via self._pool — `INSERT INTO runtime_heartbeat (component, beat_at, bots_alive, bots_enabled) VALUES (%s, NOW(), %s, %s) ON CONFLICT (component) DO UPDATE SET beat_at = EXCLUDED.beat_at, bots_alive = EXCLUDED.bots_alive, bots_enabled = EXCLUDED.bots_enabled` — mirroring db.record_reconciliation's UPSERT style. Wire BOTH new steps into _watchdog_loop, EACH in its own try/except, exactly like :88-95. Order per tick: _revive_dead_bots -> _check_bots_down -> _check_trade_silence -> _heartbeat. In start_all, log alerts_configured() at INFO ("BotManager: alert path configured=%s") — a trading system whose alerter is unconfigured is not monitored, and today nothing would tell you. Do NOT add a second thread. Do NOT add a scheduler. Do NOT touch the Coolify restart policy (research N4: the container is UP; no restart policy restarts a thread).</action>
  <verify>
    <automated>python -m pytest tests/test_bot_manager.py -q 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_bot_manager.py -q` — **ALL cases GREEN, including 1, 2, 3, 4, 25**. Case 1 (THE KILLER) was RED and is now GREEN. Case 24 (reconcile throttle) may still be RED — 19-05 owns `_maybe_reconcile`; record that.
    - `grep -n "if not any_alive" src/bot_manager.py` returns **NOTHING**. `grep -c "death alert handles that separately" src/bot_manager.py` returns **0**.
    - `grep -c "Both bots are currently running" src/bot_manager.py` returns **0** (the stale body text is gone).
    - `grep -n "runtime_heartbeat" src/bot_manager.py` returns a hit inside an `ON CONFLICT (component) DO UPDATE` UPSERT.
    - `sed -n '/def _watchdog_loop/,/def _revive_dead_bots/p' src/bot_manager.py | grep -c "except Exception"` >= 4 — every step (revive, bots-down, silence, heartbeat) has its own guard; one raising step cannot kill the loop.
    - `grep -cE "UPDATE alpaca_trades|DELETE FROM alpaca_trades|backfill" src/bot_manager.py` returns **0** (fence F1).
  </acceptance_criteria>
  <done>All-bots-down is now the loudest alert in the system instead of the one state guaranteed to be silent. The heartbeat makes the manager's liveness queryable from outside the process.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: main.py alerts when the manager never starts (case 28, research N10)</name>
  <files>dashboard/api/main.py</files>
  <read_first>dashboard/api/main.py L46-71 (the whole lifespan: :55's `if db_url:` gate that skips BotManager entirely on a missing DATABASE_URL, the sys.path insert at :57-60, the construction at :61-63, and the SWALLOW at :65-66); dashboard/api/tests/test_lifespan_alert.py (the RED spec)</read_first>
  <behavior>
    - BotManager construction or start_all() raising -> alert_manager_never_started is called with the
      exception string, the warning is still logged, app.state.bot_manager is None, and the dashboard
      still serves read-only. The exception does NOT propagate (the API must still come up).
    - DATABASE_URL absent -> the same never-started alert path is taken (today :55 skips the block
      ENTIRELY and says nothing at all).
    - The alert import failing must not crash the lifespan.
  </behavior>
  <action>In dashboard/api/main.py's lifespan: in the `except Exception as exc:` branch at :65-66, after the existing `_log.warning`, call `src.notifier.alert_manager_never_started(str(exc))` inside its OWN try/except (an alert failure must never take the API down). Also handle the `if db_url:` gate at :55 — when DATABASE_URL is absent, log a warning AND fire the same never-started alert (with a "DATABASE_URL not set" message), because today that path is completely silent. This alert MUST live here and NOT in BotManager: `self._watchdog.start()` (bot_manager.py:79) sits AFTER the start_all query that can throw (:67-70), so the watchdog cannot report its own non-existence — research N10. Import notifier lazily inside the branch (the sys.path insert at :57-60 is what makes `src.` importable in the container). Change NOTHING else in main.py — no route changes, no auth changes, no new endpoint.</action>
  <verify>
    <automated>python -m pytest dashboard/api/tests/test_lifespan_alert.py -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest dashboard/api/tests/test_lifespan_alert.py -q` — case **28 GREEN** (previously RED).
    - `grep -n "alert_manager_never_started" dashboard/api/main.py` returns >= 2 hits (the except branch AND the missing-DATABASE_URL branch).
    - `git diff --stat dashboard/api/main.py` shows a change confined to the `lifespan` function — routes, auth, and CORS are untouched.
    - A forced-raise lifespan still yields a serving app: the test asserts `app.state.bot_manager is None` and no exception escapes.
    - `python -m pytest tests/ dashboard/api/tests/ -q 2>&1 | tail -1` — >= 428 passed / 28 skipped (fence F8). Case 24 may remain the sole RED (19-05 owns it) — record it.
  </acceptance_criteria>
  <done>"BotManager never started" is now an email instead of a `_log.warning` into the void — fired from the only place in the system that can observe it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| watchdog → Postgres | the heartbeat UPSERT and the bots SELECT run against the live DB |
| BotManager → SES | four alert classes can now fire on a timer |
| bots row → thread spawn | a row with no credentials must never reach `_spawn` |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-10 | DoS (self) | alert flood from a persistent outage | mitigate | 1h cooldown per alert class, reusing the `_last_death_alert` dict pattern (:123-127); the cooldown resets only when a bot is actually alive |
| T-19-11 | DoS (self) | credential-less `_spawn` retried every 60s forever (research N5) | mitigate | the keyless branch `continue`s BEFORE the death alert and BEFORE `_spawn` — asserted by case 6 |
| T-19-12 | Information Disclosure | `status_detail` echoing an Alpaca key | mitigate | the detail string is the literal `"missing alpaca keys"`; grep-asserted to appear exactly once |
| T-19-13 | Denial of monitoring | one raising watchdog step killing the whole loop | mitigate | every step gets its own `try/except`, mirroring :88-95; grep-asserted >= 4 guards |
| T-19-14 | Tampering | a write to `alpaca_trades` slipping into the manager | mitigate | fence F1 + a grep gate in this plan's acceptance criteria; `src/backfill.py` is never imported |
| T-19-SC | Tampering | package installs | accept | none — no new packages |
</threat_model>

<verification>
- `python -m pytest tests/test_bot_manager.py dashboard/api/tests/test_lifespan_alert.py -q` — green (except case 24, owned by 19-05).
- `grep -n "if not any_alive" src/bot_manager.py` — NOTHING. The killer is dead.
- `python -m pytest tests/ dashboard/api/tests/ -q | tail -1` — >= 428 passed / 28 skipped.
- `git diff --stat` touches ONLY src/bot_manager.py and dashboard/api/main.py.
</verification>

<success_criteria>
An enabled bot is now always either RUNNING or LOUDLY BROKEN. All-bots-down alerts and keeps alerting.
A never-started manager alerts. The heartbeat is queryable from outside the process. Nothing in the
trade log was touched.
</success_criteria>

<output>
Create `.planning/phases/19-reliable-runtime/19-03-SUMMARY.md` when done. It MUST show the removed
`if not any_alive: return` as a before/after diff — that is the phase's headline.
</output>
</content>
