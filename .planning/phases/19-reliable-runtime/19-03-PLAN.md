---
phase: 19-reliable-runtime
plan: 03
type: execute
wave: 3
depends_on: [19-02]
files_modified:
  - src/bot_manager.py
  - dashboard/api/main.py
autonomous: true
requirements: [RUN-01]

must_haves:
  truths:
    - "THE KILLER BUG DIES HERE. src/bot_manager.py:186-190 does `if not any_alive: return` BEFORE the trade-silence alert. All-bots-down — the single most important state in the system — is GUARANTEED SILENT today, because it defers to a death alert that CANNOT fire (the key filter at :104-107 already removed the rows). Four bots stopped and nothing said a word."
    - "TICK ORDER IS LOAD-BEARING. _revive_dead_bots (:101-119) RESPAWNS dead threads via _spawn (:117) -> thread.start(). If liveness is evaluated AFTER revive, every revivable bot is is_alive() == True again by then -> alive > 0 -> **THE ALL-BOTS-DOWN ALERT NEVER FIRES**, in the DOMINANT failure mode (threads crash-loop each cycle: bad keys, Alpaca 401, an unhandled exception in _main_loop). It would fire only when every enabled bot is keyless or _spawn itself raises. Liveness MUST be SNAPSHOTTED at tick START, BEFORE revive, and passed in as a PARAMETER."
    - "The same trap applies to the cooldown: it must NOT be reset merely because a revive briefly succeeded inside the tick. Reset it ONLY when a bot was alive AT SNAPSHOT TIME — otherwise a crash-looping system resets its cooldown every 60s and 'repeats until one is alive' is a lie."
    - "THE KEYLESS PREDICATE MUST CHECK BOTH KEYS. `if not row.get('alpaca_api_key')` lets a row with an api_key and an EMPTY secret through to _spawn. Traced: src/bot_config.py:46-47 (`or \"\"` — empty is LEGAL) -> src/bot_thread.py:118-126 (_make_alpaca_config builds Config straight from the row, NO env reads) -> src/config.py:74-76 (Config is a frozen dataclass, keys default to \"\", and **DOES NOT RAISE**). CLAUDE.md's 'empty bare keys fail-clear (raise)' is the ORCHESTRATOR SERVICE's config path — it does NOT hold for BotConfig/BotThread. Result: a client with an empty secret -> 401 -> run() catches -> _set_status('error') -> thread exits -> revived AGAIN next tick, FOREVER, email throttled to 1h. Exactly the N5 hot loop."
    - "Both key predicates (:67-70 start_all, :104-107 _revive_dead_bots) are DELETED. One query: `WHERE enabled = TRUE`. A keyless enabled bot becomes status='error' + status_detail='missing alpaca keys' + ONE alert (1h cooldown). An enabled bot must always be either RUNNING or LOUDLY BROKEN. 'Invisible' is not a state we ship."
    - "The keyless branch runs BEFORE _maybe_send_death_alert (research N5). Otherwise a keyless bot gets a death alert about a thread that NEVER EXISTED, and _spawn is attempted EVERY 60 SECONDS FOREVER — the 1h cooldown suppresses the EMAIL, not the SPAWN."
    - "The never-started ALERT fires from dashboard/api/main.py's except branch (:65-66), NOT from BotManager — the watchdog CANNOT report its own non-existence, because self._watchdog.start() (:79) sits AFTER the start_all query that can throw (:67-70). Research N10."
    - "The keyless fix routes through _on_status_change (:299-309) — the SOLE SQL writer of bots.status — and is therefore thread-class-agnostic. RESEARCH REFUTED the CONTEXT claim that BotThread is the only status writer: CopyTraderThread (Bot E) has a second _set_status at copytrade_thread.py:140-144."
    - "The Coolify restart policy is NOT part of this fix (research N4). The container is UP — it serves the API. Only the THREADS are dead. No restart policy restarts a thread."
    - "No write to alpaca_trades. Not one."
  artifacts:
    - path: "src/bot_manager.py"
      provides: "_tick (liveness snapshotted BEFORE revive), _enabled_rows (no key predicate), _has_keys (BOTH keys), keyless->error branch, _check_bots_down, _heartbeat, the deleted any_alive escape, the startup alert-path self-check"
      contains: "_check_bots_down"
    - path: "dashboard/api/main.py"
      provides: "the never-started alert fired from the lifespan's except branch"
      contains: "alert_manager_never_started"
  key_links:
    - from: "src/bot_manager.py::_tick"
      to: "src/bot_manager.py::_check_bots_down"
      via: "alive_before is snapshotted BEFORE _revive_dead_bots can respawn anything"
      pattern: "alive_before"
    - from: "src/bot_manager.py::_check_bots_down"
      to: "src/notifier.py::alert_all_bots_down"
      via: "zero live threads at snapshot -> the loudest alert, 1h cooldown, repeats until one is alive"
      pattern: "alert_all_bots_down"
    - from: "src/bot_manager.py::_has_keys"
      to: "src/bot_manager.py::_on_status_change"
      via: "keyless (EITHER key empty) -> ('error', 'missing alpaca keys') — the sole SQL writer, bot-class-agnostic"
      pattern: "missing alpaca keys"
    - from: "dashboard/api/main.py"
      to: "src/notifier.py::alert_manager_never_started"
      via: "the except branch at :65-66 — the manager cannot alert about its own non-existence"
      pattern: "alert_manager_never_started"
---

<objective>
Kill the bug that let four bots stop in silence — and do not let the fix be masked by its own revive.

Purpose: `src/bot_manager.py:189-190` reads, verbatim:

    if not any_alive:
        return  # bots are down; death alert handles that separately

The trade-silence alert — the ONLY alert that fires on "nothing is happening" — **disables itself
precisely when nothing is happening for the worst possible reason.** It defers to a death alert that
cannot fire, because that alert only iterates rows the key filter at `:104-107` already removed.

Two ways a careless fix stays silent anyway:
- **Order.** `_revive_dead_bots` (`:101-119`) respawns via `_spawn` (`:117`) → `thread.start()`. Check
  liveness after that and every crash-looping bot looks alive. The alert never fires in production.
- **Predicate.** Checking only `alpaca_api_key` lets an empty-**secret** row spawn, 401, exit, and be
  revived forever (`config.py:74-76` — `Config` does not raise on empty keys).

Output: the two-line delete; liveness snapshotted before revive; both key predicates gone; keyless
(EITHER key) bots loudly broken; an ALL BOTS DOWN alert that repeats until a bot is alive; a queryable
heartbeat; a never-started alert from the one place that can fire it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-reliable-runtime/19-RESEARCH.md
@.planning/phases/19-reliable-runtime/19-01-SUMMARY.md
@.planning/phases/19-reliable-runtime/19-02-SUMMARY.md
@CLAUDE.md

<interfaces>
Available from 19-02 (already landed):
  src/notifier.py:  alerts_configured() -> bool
                    last_alert_error() -> str | None
                    alert_all_bots_down(bots_enabled: int, hours_since_trade: float | None) -> bool
                    alert_bot_misconfigured(bot_id: str, label: str, reason: str) -> bool
                    alert_manager_never_started(error: str) -> bool
  runtime_heartbeat(component TEXT PK, beat_at TIMESTAMPTZ, bots_alive INT, bots_enabled INT)
    — migration 019 + the src/db_schema.sql section-11 mirror.

Existing, unchanged, and load-bearing:
  src/bot_manager.py:17-20   _WATCHDOG_INTERVAL=60, _DEATH_ALERT_COOLDOWN=3600,
                             _SILENCE_HOURS=env TRADE_SILENCE_ALERT_HOURS (24), _SILENCE_CHECK_INTERVAL=3600
  src/bot_manager.py:85-95   _watchdog_loop — each step has its OWN try/except; a raising step must
                             never kill the loop. The new _tick keeps that shape.
  src/bot_manager.py:101-119 _revive_dead_bots — **_spawn at :117 STARTS a live thread. THIS is the
                             masking mechanism. Liveness must be read before this runs.**
  src/bot_manager.py:121-139 _maybe_send_death_alert — the 1h-per-bot cooldown dict (:123-127).
  src/bot_manager.py:145-151 _check_trade_silence's interval guard — THE interval-guard pattern.
  src/bot_manager.py:157-164 the MAX(timestamp) query; :181 the hours_since computation — the source of
                             `hours_since_trade`.
  src/bot_manager.py:274-297 _spawn — CopyTraderThread when cfg.strategy == "copytrade", else BotThread.
  src/bot_manager.py:299-309 _on_status_change — the SOLE SQL writer of bots.status.
  src/bot_config.py:46-47    `row.get("alpaca_api_key") or ""` — an EMPTY key is LEGAL here.
  src/bot_thread.py:118-126  _make_alpaca_config — Config straight from BotConfig, NO env reads.
  src/config.py:74-76        Config: frozen dataclass, keys default to "", **DOES NOT RAISE**.
  dashboard/api/main.py:46-71 the lifespan. :55 `if db_url:` gates the whole block; :65-66 swallows.
  dashboard/Dockerfile:64    `ENV PYTHONPATH="/app:..."` — with the :57-60 sys.path insert, this is what
                             makes `src.*` importable inside the dashboard process.

Contracts fixed by the 19-01 RED tests (implement EXACTLY — the tests import by name):
  _BOTS_DOWN_COOLDOWN = 3600
  _HEARTBEAT_COMPONENT = "bot_manager"
  BotManager._enabled_rows(self) -> list[dict]          # SELECT * FROM bots WHERE enabled = TRUE
  BotManager._has_keys(row) -> bool                     # BOTH keys — staticmethod
  BotManager._tick(self) -> None                        # ONE watchdog iteration, driven by case 1b
  BotManager._check_bots_down(self, alive_before: int, enabled: int,
                              hours_since_trade: float | None) -> None
  BotManager._heartbeat(self) -> None
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: drop both key predicates; keyless (EITHER key) becomes loudly broken (cases 5, 5b, 6-9b)</name>
  <files>src/bot_manager.py</files>
  <read_first>src/bot_manager.py L64-83 (start_all — the key predicate at :67-70), L101-119 (_revive_dead_bots — the byte-identical predicate at :104-107; the death alert at :114 fires BEFORE the spawn at :117), L121-139, L274-309; src/bot_config.py L40-55 (from_row — `or ""` at :46-47: an EMPTY key is legal and does NOT raise); src/bot_thread.py L118-126 (_make_alpaca_config — no env reads, no validation); src/config.py L74-76 (Config: frozen dataclass, `alpaca_api_key: str = ""`, **does NOT raise**); tests/test_bot_manager.py cases 5, 5b, 6, 7, 8, 9, 9b (the RED spec)</read_first>
  <behavior>
    - A row with EITHER key empty/NULL is keyless: _on_status_change(bot_id, "error", "missing alpaca
      keys") on EVERY tick (status stays authoritative), alert_bot_misconfigured ONCE per hour, _spawn
      NEVER called for it, and NO death alert mentions it.
    - Specifically {alpaca_api_key: "AKTEST", alpaca_secret_key: ""} is keyless (case 5b). An
      api-key-only check would spawn it into a permanent 401 revive loop.
    - A keyed enabled bot with a live thread: nothing happens.
    - A keyed enabled bot with a dead thread: death alert + respawn, as today.
    - A copytrade bot with a dead thread respawns as CopyTraderThread.
  </behavior>
  <action>Extract the bots query into `_enabled_rows(self) -> list[dict]` running `SELECT * FROM bots WHERE enabled = TRUE` — **the key predicate is DELETED**. Call it from BOTH start_all (replacing :67-70) and _revive_dead_bots (replacing :104-107). Add `_has_keys(row) -> bool` as a staticmethod: `return bool(row.get("alpaca_api_key") and row.get("alpaca_secret_key"))` — **BOTH keys**. An api-key-only check is NOT sufficient: `bot_config.py:46-47` coerces a missing secret to `""`, `bot_thread.py:118-126` passes it straight into `Config`, and `config.py:74-76`'s frozen dataclass **does not raise** — so the bot spawns, 401s on its first Alpaca call, exits, and is revived every 60s forever. In start_all, a `not _has_keys(row)` row must NOT be spawned: _on_status_change(bot_id, "error", "missing alpaca keys") + _maybe_send_misconfig_alert, then `continue`. In _revive_dead_bots, insert the SAME branch **BEFORE** the `if thread is None or not thread.is_alive():` check — i.e. before _maybe_send_death_alert at :114 (research N5): status='error', misconfig alert (a new 1h-cooldown method mirroring _maybe_send_death_alert:121-139 with its OWN cooldown dict), then `continue` — do NOT spawn, do NOT death-alert. Route every status write through _on_status_change (:299-309) — the SOLE SQL writer — so the fix is thread-class-agnostic (CopyTraderThread has a second _set_status at copytrade_thread.py:140-144). Do NOT change _spawn's strategy dispatch. status_detail is the literal `"missing alpaca keys"` and NOTHING else — never a key, a key prefix, or an env value.</action>
  <verify>
    <automated>python -m pytest tests/test_bot_manager.py -q 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_bot_manager.py -k "keyless or secret or revive or healthy or strategy" -q` — cases **5, 5b, 6, 7, 8, 9, 9b GREEN** (5, 5b and 6 were RED).
    - `grep -c "alpaca_api_key IS NOT NULL" src/bot_manager.py` returns **0** — both predicates are gone.
    - `grep -c "SELECT \* FROM bots WHERE enabled = TRUE" src/bot_manager.py` returns **1** (one query, called twice — not two copies).
    - `sed -n '/def _has_keys/,/^$/p' src/bot_manager.py | grep -c "alpaca_secret_key"` returns **1** — the predicate really checks the SECRET too. An implementation that omits it passes every other test and silently reintroduces the 401 hot loop.
    - `grep -n "missing alpaca keys" src/bot_manager.py` returns exactly 1 hit, and `sed -n '/def _revive_dead_bots/,/def _maybe_send_death_alert/p' src/bot_manager.py | grep -n "missing alpaca keys\|_maybe_send_death_alert"` shows the keyless line BEFORE the death-alert line (N5 ordering).
    - `python -m pytest tests/test_phase19_fences.py -q` still green.
  </acceptance_criteria>
  <done>A bot missing EITHER Alpaca key is now error+alerted instead of invisible-or-hot-looping. Healthy bots are byte-for-byte unaffected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DELETE the any_alive escape; snapshot liveness BEFORE revive; ALL BOTS DOWN + heartbeat (cases 1, 1b, 2-4, 25)</name>
  <files>src/bot_manager.py</files>
  <read_first>src/bot_manager.py L85-95 (_watchdog_loop — each step gets its own try/except); L101-119 (**_revive_dead_bots RESPAWNS: _spawn at :117 calls thread.start(), so a revived thread is is_alive() immediately — this is the masking mechanism**); L145-212 (_check_trade_silence in full: the interval guard at :149-151, the MAX(timestamp) query at :157-164 and the hours_since computation at :181 — REUSE them for hours_since_trade — THE KILLER at :186-190, and the now-false body text at :208 "Both bots are currently running"); L121-139 (the cooldown pattern); tests/test_bot_manager.py cases 1, 1b, 2, 3, 4, 25 (**case 1b is the one that FAILS a wrong tick order**)</read_first>
  <behavior>
    - Zero live threads AT TICK START (before any revive) -> alert_all_bots_down fires, EVEN THOUGH the
      revive later in the same tick brings threads back up. Repeats once per _BOTS_DOWN_COOLDOWN (3600s)
      and KEEPS FIRING until a bot is alive AT SNAPSHOT TIME.
    - At least one live thread at snapshot -> ZERO all-bots-down alerts.
    - Bots alive + last trade 30h ago -> the trade-silence alert fires ON ITS OWN MERITS. The two alerts
      are INDEPENDENT, not mutually suppressing.
    - Each tick UPSERTs runtime_heartbeat(component='bot_manager', beat_at=NOW(), bots_alive,
      bots_enabled).
    - A raising step never kills the loop; later steps in the same tick still run.
  </behavior>
  <action>
  **DELETE lines 186-190 of src/bot_manager.py** — the `# Check at least one bot is running` comment,
  the `any_alive` computation, and `if not any_alive: return`. Trade silence is now evaluated on its own
  merits. Also delete the now-false body text at :208 ("Both bots are currently running").

  Extract `_tick(self)` — ONE watchdog iteration — and have `_watchdog_loop` call it. **The order inside
  `_tick` is load-bearing and is the whole point of case 1b:**

    1. SNAPSHOT liveness FIRST, before anything can respawn:
         with self._lock:
             alive_before = sum(1 for t in self._threads.values() if t.is_alive())
       Also read `enabled = len(self._enabled_rows())` and `hours_since_trade` — reuse the MAX(timestamp)
       query + the hours_since computation at :157-164 / :181. On a DB error, or a NULL last-trade
       (fresh deployment), pass **None** — never fabricate a number.
    2. `self._check_bots_down(alive_before, enabled, hours_since_trade)`
    3. `self._revive_dead_bots()`
    4. `self._check_trade_silence()`
    5. `self._heartbeat()`
    6. `self._maybe_reconcile()`   (added by 19-05)
  Each step in its OWN `try/except Exception` that logs a warning, exactly like :88-95.

  **Do NOT compute liveness inside `_check_bots_down`.** `_revive_dead_bots` calls `_spawn` (:117) ->
  `thread.start()`, so any bot revived in the same tick is `is_alive() == True`. If the check ran after
  the revive — or recomputed liveness itself — then in the DOMINANT failure mode (threads crash-loop
  every cycle on bad keys / a 401 / an unhandled exception in `_main_loop`) `alive > 0` on EVERY tick
  and **the alert would NEVER fire.** It would fire only when every enabled bot is keyless or `_spawn`
  itself raises. `alive_before` is a parameter for exactly this reason.

  `_check_bots_down(alive_before, enabled, hours_since_trade)`: if `enabled > 0 and alive_before == 0`,
  fire `alert_all_bots_down(enabled, hours_since_trade)` behind a 1h cooldown timestamp
  (`self._last_bots_down_alert`, mirroring :123-127). **Reset that timestamp to 0.0 ONLY when
  `alive_before > 0`** — never merely because a revive succeeded later in the tick; otherwise a
  crash-looping system resets its cooldown every 60s and the "repeats until one is alive" guarantee
  (case 2) is a lie.

  `_heartbeat(self)`: UPSERT via self._pool — `INSERT INTO runtime_heartbeat (component, beat_at,
  bots_alive, bots_enabled) VALUES (%s, NOW(), %s, %s) ON CONFLICT (component) DO UPDATE SET
  beat_at = EXCLUDED.beat_at, bots_alive = EXCLUDED.bots_alive, bots_enabled = EXCLUDED.bots_enabled` —
  mirroring db.record_reconciliation's UPSERT style. Report POST-revive liveness here (the heartbeat is
  a status report; the ALERT trigger is the pre-revive snapshot).

  In start_all, log `alerts_configured()` and `last_alert_error()` at INFO
  ("BotManager: alert path configured=%s last_error=%s") — a trading system whose alerter is
  unconfigured is not monitored, and today nothing would tell you.

  Do NOT add a second thread. Do NOT add a scheduler. Do NOT touch the Coolify restart policy
  (research N4: the container is UP; no restart policy restarts a thread).
  </action>
  <verify>
    <automated>python -m pytest tests/test_bot_manager.py -q 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_bot_manager.py -q` — **ALL cases GREEN, including 1 AND 1b**. Case 1b is the proof that matters: it drives the whole tick with a SUCCEEDING revive and still gets the alert. Case 24 may remain RED — 19-05 owns `_maybe_reconcile`; record that.
    - `grep -n "if not any_alive" src/bot_manager.py` returns **NOTHING**. `grep -c "death alert handles that separately" src/bot_manager.py` returns **0**. `grep -c "Both bots are currently running" src/bot_manager.py` returns **0**.
    - `grep -c "def _check_bots_down(self, alive_before" src/bot_manager.py` returns **1** — liveness is a PARAMETER, not recomputed inside.
    - `sed -n '/def _check_bots_down/,/    def /p' src/bot_manager.py | grep -c "is_alive()"` returns **0** — the check cannot re-read post-revive liveness even by accident.
    - `sed -n '/def _tick/,/    def /p' src/bot_manager.py` shows `alive_before` computed BEFORE `_revive_dead_bots` is called, and `_check_bots_down` invoked with it.
    - `grep -n "runtime_heartbeat" src/bot_manager.py` returns a hit inside an `ON CONFLICT (component) DO UPDATE` UPSERT.
    - `sed -n '/def _tick/,/def _revive_dead_bots/p' src/bot_manager.py | grep -c "except Exception"` >= 4 — every step has its own guard.
    - `grep -cE "UPDATE alpaca_trades|DELETE FROM alpaca_trades|backfill" src/bot_manager.py` returns **0** (fence F1).
  </acceptance_criteria>
  <done>All-bots-down is the loudest alert in the system instead of the one state guaranteed to be silent — and it still fires in the crash-loop case, where a naive tick order would have masked it forever.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: main.py alerts when the manager never starts (case 28, research N10)</name>
  <files>dashboard/api/main.py</files>
  <read_first>dashboard/api/main.py L46-71 (the whole lifespan: :55's `if db_url:` gate that skips BotManager entirely on a missing DATABASE_URL, the sys.path insert at :57-60, the construction at :61-63, and the SWALLOW at :65-66); dashboard/Dockerfile L64 (`ENV PYTHONPATH="/app:..."` — this plus the :57-60 insert is what makes `from src.notifier import ...` importable here, and why the import must be FUNCTION-LOCAL); dashboard/api/tests/test_lifespan_alert.py (the RED spec)</read_first>
  <behavior>
    - BotManager construction or start_all() raising -> alert_manager_never_started(str(exc)) is called,
      the warning is still logged, app.state.bot_manager is None, the dashboard still serves read-only,
      and the exception does NOT propagate.
    - DATABASE_URL absent -> the same never-started alert path (today :55 skips the block ENTIRELY and
      says nothing at all).
    - An alert-import or alert-send failure must NOT crash the lifespan.
  </behavior>
  <action>In dashboard/api/main.py's lifespan: in the `except Exception as exc:` branch at :65-66, after the existing `_log.warning`, call `alert_manager_never_started(str(exc))` inside its OWN try/except (an alert failure must never take the API down). Import it **FUNCTION-LOCALLY** (`from src.notifier import alert_manager_never_started`) INSIDE the branch — the sys.path insert at :57-60 plus `PYTHONPATH=/app` (dashboard/Dockerfile:64) is what makes `src.*` importable in this process, and a module-level import would run BEFORE that insert. Also handle the `if db_url:` gate at :55 — when DATABASE_URL is absent, log a warning AND fire the same never-started alert ("DATABASE_URL not set"), because today that path is completely silent. This alert MUST live here and NOT in BotManager: `self._watchdog.start()` (bot_manager.py:79) sits AFTER the start_all query that can throw (:67-70), so the watchdog cannot report its own non-existence — research N10. Change NOTHING else in main.py — no route changes, no auth changes, no new endpoint.</action>
  <verify>
    <automated>python -m pytest dashboard/api/tests/test_lifespan_alert.py -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest dashboard/api/tests/test_lifespan_alert.py -q` — case **28 GREEN** (previously RED).
    - `grep -n "alert_manager_never_started" dashboard/api/main.py` returns >= 2 hits (the except branch AND the missing-DATABASE_URL branch).
    - `grep -n "^from src\|^import src" dashboard/api/main.py` returns **NOTHING** — the `src.*` import is function-local, after the :57-60 sys.path insert.
    - `git diff --stat dashboard/api/main.py` shows a change confined to `lifespan` — routes, auth, and CORS untouched.
    - A forced-raise lifespan still yields a serving app: `app.state.bot_manager is None` and no exception escapes.
    - `python -m pytest tests/ dashboard/api/tests/ -q 2>&1 | tail -1` — >= 428 passed / 28 skipped (fence F8). Case 24 may remain the sole RED (19-05 owns it) — record it.
  </acceptance_criteria>
  <done>"BotManager never started" is now an email instead of a `_log.warning` into the void — fired from the only place in the system that can observe it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| watchdog → Postgres | the heartbeat UPSERT and the bots SELECT run against the live DB |
| BotManager → SES | four alert classes can now fire on a timer |
| bots row → thread spawn | a row missing EITHER credential must never reach `_spawn` |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-35 | Repudiation | **the revive masking the all-bots-down alert (liveness read AFTER `_spawn`)** | mitigate | `alive_before` is snapshotted at tick START and passed as a PARAMETER; `_check_bots_down` is grep-asserted to contain NO `is_alive()` call; case 1b drives the integrated tick with a SUCCEEDING revive |
| T-19-36 | Spoofing | **an empty-SECRET row spawning into a permanent 401 revive loop** | mitigate | `_has_keys` requires BOTH keys — nothing downstream validates (`config.py:74-76` does not raise); case 5b; grep-asserted |
| T-19-10 | DoS (self) | alert flood from a persistent outage | mitigate | 1h cooldown per alert class (:123-127 pattern); the cooldown resets ONLY when a bot was alive AT SNAPSHOT |
| T-19-11 | DoS (self) | credential-less `_spawn` retried every 60s forever (N5) | mitigate | the keyless branch `continue`s BEFORE the death alert and BEFORE `_spawn` — case 6 |
| T-19-12 | Information Disclosure | `status_detail` echoing an Alpaca key | mitigate | the detail string is the literal `"missing alpaca keys"`; grep-asserted to appear exactly once |
| T-19-13 | Denial of monitoring | one raising step killing the whole loop | mitigate | every step in `_tick` gets its own `try/except`, mirroring :88-95 |
| T-19-14 | Tampering | a write to `alpaca_trades` slipping into the manager | mitigate | fence F1 + a grep gate here; `src/backfill.py` is never imported |
| T-19-SC | Tampering | package installs | accept | none — no new packages |
</threat_model>

<verification>
- `python -m pytest tests/test_bot_manager.py dashboard/api/tests/test_lifespan_alert.py -q` — green (except case 24, owned by 19-05).
- `grep -n "if not any_alive" src/bot_manager.py` — NOTHING. The killer is dead.
- `sed -n '/def _check_bots_down/,/    def /p' src/bot_manager.py | grep -c "is_alive()"` — **0**. The alert cannot see post-revive liveness.
- `sed -n '/def _has_keys/,/^$/p' src/bot_manager.py | grep -c "alpaca_secret_key"` — **1**. The hot loop is closed.
- `python -m pytest tests/ dashboard/api/tests/ -q | tail -1` — >= 428 passed / 28 skipped.
- `git diff --stat` touches ONLY src/bot_manager.py and dashboard/api/main.py.
</verification>

<success_criteria>
An enabled bot is now always either RUNNING or LOUDLY BROKEN. All-bots-down alerts — and keeps alerting
— even while the watchdog is busily reviving crash-looping threads. A never-started manager alerts. The
heartbeat is queryable from outside the process. Nothing in the trade log was touched.
</success_criteria>

<output>
Create `.planning/phases/19-reliable-runtime/19-03-SUMMARY.md` when done. It MUST show the removed
`if not any_alive: return` as a before/after diff, AND state the `_tick` order with `alive_before`
snapshotted ahead of the revive — that ordering is as load-bearing as the delete itself.
</output>
</content>
