---
phase: 19-reliable-runtime
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_bot_manager.py
  - tests/test_notifier_selfcheck.py
  - tests/test_reconciliation.py
  - tests/test_db.py
  - tests/test_phase19_fences.py
  - dashboard/api/tests/test_heartbeat.py
  - dashboard/api/tests/test_lifespan_alert.py
  - dashboard/api/tests/test_portfolio_win_rate.py
  - dashboard/api/tests/test_routes.py
autonomous: true
requirements: [RUN-01, RUN-02]

must_haves:
  truths:
    - "tests/test_bot_manager.py DOES NOT EXIST today. It is created here and it is the highest-value missing file in the phase — RUN-01 is entirely untested."
    - "Cases 1, 5, 6, 13, 15, 17, 22, 23, 26 MUST FAIL against current main. A case that passes pre-fix is not testing the bug. Each one gets an explicit recorded RED reason."
    - "Case 1 is THE KILLER: src/bot_manager.py:186-190 does `if not any_alive: return` BEFORE the trade-silence alert. All-bots-down is GUARANTEED SILENT today. The test asserts >=1 ALL BOTS DOWN alert with zero live threads — today it captures ZERO."
    - "Case 6 guards research N5: post-B1-fix a keyless bot has thread is None, would fall into _maybe_send_death_alert (bot_manager.py:114) with the WRONG message and then _spawn a credential-less bot EVERY 60s FOREVER. The keyless branch must precede the death alert."
    - "Case 23 guards research N1 — the phase's own landmine. reconciliation.reconcile() (:139-143) has NO per-bot try and _client_for_bot RAISES on a keyless bot (:86-90), while _enabled_bot_ids (:51-59) has NO key predicate. Scheduling it hourly unguarded turns ONE misconfigured bot into TOTAL SILENT reconciliation failure — and the B1 fix makes keyless-enabled bots visible, so it lands exactly when we touch it."
    - "Case 26 guards research N10 — the watchdog cannot report its own non-existence. self._watchdog.start() (:79) sits AFTER the start_all query that can throw (:67-70). manager_alive MUST be computed from row ABSENCE or STALENESS and must NEVER default healthy."
    - "No test touches SES, live Alpaca, or the prod DB. Cases 1-14 and 26-27 are pure (fakes only). DB-gated cases use TEST_DATABASE_URL ONLY and skip VISIBLY."
    - "Every static/grep case runs a POSITIVE CONTROL first (the scanned slice is non-empty and contains a known-present token) so it cannot pass vacuously."
  artifacts:
    - path: "tests/test_bot_manager.py"
      provides: "VALIDATION cases 1-9b, 24, 25, 29 — all of RUN-01's runtime behavior"
      min_lines: 200
    - path: "tests/test_notifier_selfcheck.py"
      provides: "VALIDATION cases 10-12 — the alert path proves it can send"
      min_lines: 45
    - path: "tests/test_phase19_fences.py"
      provides: "Fences F1-F8 — no prod trade write, no risk-rule change, no new provider, no retune"
      min_lines: 70
    - path: "dashboard/api/tests/test_heartbeat.py"
      provides: "VALIDATION cases 26-27 — absence and staleness both mean DEAD"
      min_lines: 40
    - path: "dashboard/api/tests/test_lifespan_alert.py"
      provides: "VALIDATION case 28 — main.py's except branch alerts on a never-started manager"
      min_lines: 35
  key_links:
    - from: "tests/test_bot_manager.py"
      to: "src.bot_manager.BotManager"
      via: "constructed with a fake pool; _check_bots_down / _heartbeat / _maybe_reconcile do not exist yet — that IS the RED"
      pattern: "_check_bots_down|_heartbeat|_maybe_reconcile"
    - from: "tests/test_notifier_selfcheck.py"
      to: "src.notifier.alerts_configured"
      via: "the unit under test does not exist yet"
      pattern: "alerts_configured"
---

<objective>
Wave 0: the RED suite for all 29 cases + 8 fences in `19-VALIDATION.md`. **Nothing in `src/` or
`dashboard/api/routes/` or `dashboard/web/` changes in this plan.**

Purpose: Phase 19 exists because four bots stopped and nothing said a word. The mechanism that was
supposed to say something (`_check_trade_silence`) disables itself with `if not any_alive: return`
at `src/bot_manager.py:189-190` — the exact condition it should scream about. That bug survived
because `tests/test_bot_manager.py` **does not exist**. Every fix in this phase gets a test that was
failing for the right reason before the fix landed, or the fix is not proven.

Output: five new test files, four extended, the whole Phase-19 spec, RED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-reliable-runtime/19-VALIDATION.md
@.planning/phases/19-reliable-runtime/19-RESEARCH.md
@.planning/phases/19-reliable-runtime/19-CONTEXT.md
@CLAUDE.md

<interfaces>
These units DO NOT EXIST YET. Plans 19-02 … 19-06 create them. Their contracts are **fixed here** —
the tests are written against these signatures and the implementations must match them exactly.

  # src/notifier.py  (Plan 19-02)
  def alerts_configured() -> bool
      """Config PRESENCE only — no SES call, no send. Mirrors _get_ses_client's own
      resolution order (notifier.py:27-39) EXACTLY:
        SECRETS_PATH.exists() -> True
        else: bool(AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY)"""

  def alert_all_bots_down(bots_enabled: int, hours_since_trade: float | None) -> bool
      # subject MUST contain the literal "ALL BOTS DOWN"
  def alert_bot_misconfigured(bot_id: str, label: str, reason: str) -> bool
      # subject MUST NOT contain "died"/"dead". reason is the WHOLE detail; never key material.
  def alert_manager_never_started(error: str) -> bool
      # fired from dashboard/api/main.py's except branch, NOT from BotManager (research N10)

  # src/bot_manager.py  (Plans 19-03, 19-05)
  _BOTS_DOWN_COOLDOWN  = 3600          # 1h, repeats until one bot is alive
  _HEARTBEAT_COMPONENT = "bot_manager"
  _RECONCILE_INTERVAL_HOURS = float(os.environ.get("RECONCILE_INTERVAL_HOURS", "1"))

  class BotManager:
      def _enabled_rows(self) -> list[dict]
          """SELECT * FROM bots WHERE enabled = TRUE  — NO KEY PREDICATE.
          Replaces the byte-identical filtered query at :67-70 AND :104-107."""
      def _check_bots_down(self) -> None      # zero live threads among enabled -> alert_all_bots_down
      def _heartbeat(self) -> None            # UPSERT runtime_heartbeat via self._pool
      def _maybe_reconcile(self) -> None      # hourly, interval-guarded, PER-BOT try (research N1)

  # src/reconciliation.py  (Plan 19-05)
  def reconcile(tolerance: float | None = None) -> list[tuple[str, dict]]
      """PER-BOT try/except. A bot whose _client_for_bot raises is LOGGED and SKIPPED;
      the healthy bots still reconcile and persist. No exception escapes."""

  # dashboard/api/db.py  (Plan 19-02)
  HEARTBEAT_STALE_SECONDS: int          # env HEARTBEAT_STALE_SECONDS, default 180
  def get_heartbeat(conn, component: str = "bot_manager") -> dict | None   # None when NO ROW
  def heartbeat_is_fresh(beat_at, now=None, stale_seconds=None) -> bool
      """beat_at is None  -> False.   ABSENCE IS THE SIGNAL. Never default healthy."""

  # dashboard/api/models.py  (Plan 19-06)
  PortfolioData  += pnl_source: str = "trade_log"   # {"reconciled","alpaca_live","trade_log"}
                 += stale: bool = True
                 += unresolved: int = 0
                 += reconciled: Optional[dict] = None
  HealthStatus   += manager_alive: bool = False     # BOOL ONLY — never echo key material
                 += alerts_configured: bool = False
                 += bots_alive: int = 0
                 += bots_enabled: int = 0
                 += last_heartbeat: Optional[str] = None
  SettingsData   += unresolved: int = 0

  # src/db.py  (Plans 19-02, 19-04)
  get_alpaca_accuracy(bot_id, last_n=None) -> dict
      # ADDITIVE new key "unresolved". All nine existing keys keep their names (research N6).
      # RESOLVED := pnl IS NOT NULL AND pnl <> 0

Existing pieces the tests drive (import them; do NOT re-derive):
  src/bot_manager.py:101-119   _revive_dead_bots        (the key predicate at :104-107 is the bug)
  src/bot_manager.py:121-139   _maybe_send_death_alert  (1h cooldown pattern at :123-127)
  src/bot_manager.py:145-212   _check_trade_silence     (THE KILLER at :186-190)
  src/bot_manager.py:274-297   _spawn                   (dispatches CopyTraderThread vs BotThread on cfg.strategy)
  src/bot_manager.py:299-309   _on_status_change        (the SOLE SQL writer of bots.status)
  src/notifier.py:42-61        send_alert               (SWALLOWS every failure — that is why case 10 exists)
  src/reconciliation.py:51-59  _enabled_bot_ids         (enabled = TRUE, NO key predicate)
  src/reconciliation.py:62-93  _client_for_bot          (RAISES ValueError on a keyless bot, :86-90)
  src/db.py:224-258            get_alpaca_accuracy      (`losses = resolved - wins` — the 0.0 bug)
  tests/test_db.py:103-132     _FakeConn + `accuracy` fixture — REUSE THIS, do not write a second style

RESEARCH REFUTES ONE CONTEXT CLAIM (case 9b exists because of it): bots.status has **TWO writer
classes**, not one. CopyTraderThread (Bot E) has its own _set_status at src/copytrade_thread.py:140-144.
The SOLE SQL writer is still BotManager._on_status_change. Any revive/keyless fix must be
thread-class-agnostic.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tests/test_bot_manager.py — cases 1-9b, 24, 25, 29 (all of RUN-01's runtime)</name>
  <files>tests/test_bot_manager.py</files>
  <read_first>19-VALIDATION.md cases 1-9b, 24, 25, 29 and fences; src/bot_manager.py L1-95 (module constants, __init__, start_all's KEY PREDICATE at :67-70, the watchdog loop's per-step try/except at :88-95), L101-139 (_revive_dead_bots + the key predicate again at :104-107, _maybe_send_death_alert + its 1h cooldown dict at :123-127), L145-212 (_check_trade_silence — the interval guard at :149-151 IS the pattern the new steps copy, and THE KILLER at :186-190), L274-309 (_spawn's strategy dispatch, _on_status_change); src/bot_config.py (BotConfig.from_row — what a bots row must contain); tests/conftest.py (the MagicMock house style)</read_first>
  <behavior>
    - Shared fakes at the top of the file, NO network / NO DB / NO SES:
        FakeThread: .is_alive() returns a settable flag; .stop()/.join()/.start() are no-ops; .config
          has a .label. A dead thread is FakeThread(alive=False).
        FakePool: a context-manager .connection() yielding a FakeConn whose .execute(sql, params)
          returns the seeded `bots` rows for a SELECT on `bots`, a {"last_trade": <ts>} row for the
          MAX(timestamp) silence query, and RECORDS every UPSERT/UPDATE it is handed into a list.
        capture_alerts fixture: monkeypatch src.notifier.send_alert (and the new typed wrappers) with
          a recorder appending (subject, body) — boto3 is NEVER imported.
        make_manager(rows, threads): construct BotManager WITHOUT running __init__'s real
          ConnectionPool — monkeypatch psycopg_pool.ConnectionPool (or build via
          object.__new__ + explicit attribute assignment). The watchdog thread must NEVER be started;
          the tests call _revive_dead_bots / _check_trade_silence / _check_bots_down / _heartbeat /
          _maybe_reconcile DIRECTLY.

    - case 1  test_all_bots_down_alerts (THE KILLER): one enabled+keyed bot row, self._threads = {}
      (or all FakeThread(alive=False)); last trade 30h ago. Drive the watchdog's down-check step.
      Assert >= 1 captured alert whose subject contains "ALL BOTS DOWN".
      RED TODAY: bot_manager.py:189-190 returns before any alert and _check_bots_down does not exist
      -> zero alerts captured. If this passes pre-fix, the test is wrong — STOP and fix the test.
    - case 2  test_all_bots_down_repeats_each_cooldown_window: tick -> 1 alert. Tick again immediately
      -> still 1 (cooldown). Advance the manager's cooldown clock past 3600s (monkeypatch time.time)
      -> tick -> 2. An outage that persists keeps screaming.
    - case 3  test_one_bot_alive_means_no_all_bots_down_alert: one FakeThread(alive=True).
      Assert ZERO "ALL BOTS DOWN" alerts. The loudest alert must not cry wolf.
    - case 4  test_trade_silence_is_evaluated_on_its_own_merits: bots ALIVE, last trade 30h ago
      (> _SILENCE_HOURS=24). Assert a TRADE-SILENCE alert fires and it is NOT an ALL BOTS DOWN alert.
      Deleting :189-190 must not break the pre-existing silence alert — the two are now independent,
      not mutually suppressing.
    - case 5  test_keyless_enabled_bot_is_error_and_alerts: row {bot_id:"X", enabled:True,
      alpaca_api_key:""}. Run _revive_dead_bots with a spy on _on_status_change. Assert it was called
      with ("X", "error", "missing alpaca keys") AND exactly one alert whose subject/body identifies X
      as misconfigured.
      RED TODAY: the key predicate at :104-107 removes the row from the result set entirely — nothing
      is called at all. "Invisible" is not a state we ship.
    - case 6  test_keyless_bot_is_not_spawned_and_gets_no_death_alert (research N5): same fixture, spy
      on _spawn. Assert _spawn was NOT called for X, and NO alert subject contains "died"/"dead".
      Post-B1-fix the keyless bot has thread is None and would otherwise hit _maybe_send_death_alert
      (:114) with a message about a thread that never existed, then _spawn a credential-less bot every
      60 SECONDS FOREVER (the cooldown suppresses the EMAIL, not the spawn). The branch must precede
      the death alert.
    - case 7  test_keyless_alert_respects_the_one_hour_cooldown: two ticks inside an hour -> exactly 1
      alert, but _on_status_change("X","error",...) is re-asserted on BOTH ticks (status stays
      authoritative; only the email is throttled).
    - case 8  test_healthy_keyed_bot_is_unaffected: keyed enabled row + FakeThread(alive=True).
      Assert no status change, no alert, no respawn. Dropping the key predicate changed nothing for
      healthy bots.
    - case 9  test_dead_thread_is_revived: keyed enabled row + FakeThread(alive=False). Assert _spawn
      called for that bot_id AND a death alert fired.
    - case 9b test_revive_dispatches_on_strategy (research refutation C1): row with
      strategy="copytrade", dead thread. Assert the class handed to the threads dict is
      CopyTraderThread, not BotThread. Patch both classes with recording stand-ins so no real thread
      starts. Bot E is enabled:false today — this guards a LATENT break, not a live one.
    - case 24 test_reconcile_step_self_throttles_and_never_kills_the_watchdog: monkeypatch
      src.reconciliation.reconcile with a counting spy. Call _maybe_reconcile 3x inside an hour ->
      spy called ONCE. Then make the spy raise -> assert _maybe_reconcile (or the loop's per-step
      try/except, mirroring :88-95) swallows it and does not propagate.
    - case 25 test_heartbeat_upserts_a_row: two enabled rows, one FakeThread alive. Call _heartbeat().
      Assert the FakePool recorded an UPSERT into runtime_heartbeat for component='bot_manager' with
      bots_alive=1 and bots_enabled=2. (The OUTSIDE-the-process read half of case 25 is
      dashboard/api/tests/test_heartbeat.py, Task 2.)
    - case 29 test_migration_019_exists_in_both_files (STATIC, research N3): POSITIVE CONTROL FIRST —
      assert src/db_schema.sql contains "CREATE TABLE IF NOT EXISTS reconciliation" (proving we really
      read the schema file and that 017 IS mirrored there today). THEN assert
      dashboard/api/migrations/019_runtime_heartbeat.sql EXISTS and contains
      "CREATE TABLE IF NOT EXISTS runtime_heartbeat", AND that src/db_schema.sql ALSO contains
      "runtime_heartbeat". db_schema.sql:212-215 hand-mirrors migration 017 — a table added ONLY as a
      migration is missing from every fresh-DB bootstrap. Both files, or the table silently does not exist.
  </behavior>
  <action>Create tests/test_bot_manager.py. Import `from src.bot_manager import BotManager` — the class exists, but `_check_bots_down`, `_heartbeat`, `_maybe_reconcile` and `_enabled_rows` do NOT, so those cases fail with AttributeError. That is the correct RED. Do NOT stub the methods, do NOT guard with getattr/hasattr, do NOT pytest.skip — a skip is not a RED. Never construct a real ConnectionPool (it would dial the DB): monkeypatch `src.bot_manager.ConnectionPool` to return the FakePool before instantiating. Never let the watchdog thread start (do not call start_all; if a test needs it, monkeypatch `threading.Thread`). Freeze time with a monkeypatched `src.bot_manager.time.time` so cooldown cases are deterministic — never `sleep`. Cases 5/6/7/9/9b spy via monkeypatch.setattr on the BOUND methods (`_spawn`, `_on_status_change`) so the assertions are about call arguments, not side effects.</action>
  <verify>
    <automated>python -m pytest tests/test_bot_manager.py -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_bot_manager.py -q` — 13 collected. Cases **1, 5, 6** FAIL (they are the killer + the keyless pair). Cases 2, 24, 25, 29 also FAIL (the methods/migration do not exist). Zero SKIP. Zero ERROR at import time.
    - Case 1's failure is "zero ALL BOTS DOWN alerts captured" or AttributeError on `_check_bots_down` — **not** a fixture/typo error. Record the exact failure line in the SUMMARY.
    - Cases 3, 4, 8, 9 may already PASS (they assert behavior that survives the fix) — that is fine and expected.
    - `grep -nE "boto3|requests|httpx|psycopg\.connect|DATABASE_URL" tests/test_bot_manager.py` returns NOTHING (no SES, no DB, no network).
    - `grep -c "ALL BOTS DOWN" tests/test_bot_manager.py` >= 2 (case 1 asserts it fires, case 3 asserts it does not).
    - `grep -nE "hasattr|getattr\(mgr|importorskip|pytest\.skip" tests/test_bot_manager.py` returns NOTHING — a skip is not a RED.
    - `python -m pytest tests/ -q 2>&1 | tail -1` — no PREVIOUSLY-PASSING test has broken.
  </acceptance_criteria>
  <done>The killer bug (case 1), the keyless-invisible bug (case 5), and the every-60s-forever respawn (case 6) each have a test that fails for the right reason on current main. RUN-01 now has a test file at all.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: alert path, heartbeat read, lifespan alert, reconcile guard, fences — cases 10-12, 23, 26-28, F1-F8</name>
  <files>tests/test_notifier_selfcheck.py, tests/test_reconciliation.py, tests/test_phase19_fences.py, dashboard/api/tests/test_heartbeat.py, dashboard/api/tests/test_lifespan_alert.py</files>
  <read_first>19-VALIDATION.md cases 10-12, 23, 26-28 and fences F1-F8; src/notifier.py L16-19 (SECRETS_PATH/SENDER/RECIPIENT), L22-39 (_get_ses_client's EXACT resolution order — the self-check must not disagree with it), L42-61 (send_alert SWALLOWS everything — this is why case 10 exists); src/reconciliation.py L51-59 (_enabled_bot_ids — enabled=TRUE, NO key predicate), L62-93 (_client_for_bot RAISES at :86-90), L96-131 (reconcile_bot_live), L134-143 (reconcile — NO try, the N1 landmine); tests/test_reconciliation.py L1-40 (the existing zero-network double style — follow it); dashboard/api/main.py L46-71 (the lifespan; the swallowed failure at :65-66); dashboard/api/db.py (get_db / KNOWN_BOTS — the dashboard's OWN db module, distinct from src.db)</read_first>
  <behavior>
    - case 10 test_ses_unconfigured_reports_false: monkeypatch src.notifier.SECRETS_PATH to a Path
      whose .exists() is False; monkeypatch.delenv AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY.
      Assert `alerts_configured() is False` (identity, not truthiness).
      RED TODAY: alerts_configured does not exist. This is the "believes it is alerting and is not"
      failure — send_alert swallows every exception (notifier.py:59-61), so an unconfigured SES is
      completely invisible. Config presence is the only detectable signal.
    - case 11 test_ses_configured_reports_true_on_either_channel: (a) SECRETS_PATH.exists() -> True,
      env unset -> True. (b) SECRETS_PATH absent, both AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY
      set -> True. (c) only ONE of the two env vars set -> False. The check must mirror
      _get_ses_client's own order (notifier.py:27-39) EXACTLY — it must not disagree with the code
      that actually sends. Assert boto3 is never imported (monkeypatch sys.modules sentinel, or assert
      no SES client is constructed).
    - case 12 test_alerts_configured_is_a_bool_and_leaks_nothing: assert isinstance(alerts_configured(),
      bool). With AWS_ACCESS_KEY_ID="AKIAEXAMPLE123", assert that string (and any 4-char prefix of it)
      appears NOWHERE in the alert bodies produced by alert_bot_misconfigured("X","Bot X","missing
      alpaca keys"). Security fence: booleans only; "missing alpaca keys" is the ENTIRE detail string.
    - case 23 test_reconcile_is_guarded_per_bot (research N1 — the phase's landmine), in
      tests/test_reconciliation.py: monkeypatch _enabled_bot_ids -> ["A","X"]; _client_for_bot raises
      ValueError for "X" and returns a fake client for "A"; reconcile_bot_live is a spy recording which
      bots it ran. Call reconcile(). Assert: NO exception escapes; "A" WAS reconciled and persisted;
      "X" was logged as failed and is absent from the results.
      RED TODAY: reconcile() (:139-143) has no try — the ValueError from _client_for_bot propagates and
      reconciles ZERO bots, including the healthy ones. Scheduling this hourly unguarded converts one
      misconfigured bot into TOTAL SILENT reconciliation failure, and the B1 fix makes keyless-enabled
      bots MORE likely.
    - case 26 test_absence_of_row_means_dead (research N10), in dashboard/api/tests/test_heartbeat.py:
      `heartbeat_is_fresh(None)` is False. `get_heartbeat(fake_conn_with_no_rows)` returns None.
      A reader that defaults healthy on a missing row reintroduces the exact silent failure this phase
      exists to kill: self._watchdog.start() (bot_manager.py:79) sits AFTER the start_all query that
      can throw (:67-70), so a heartbeat written BY the watchdog cannot report its own non-existence.
      ABSENCE IS THE SIGNAL.
      RED TODAY: dashboard/api/db.py has neither function.
    - case 27 test_stale_heartbeat_means_dead: with HEARTBEAT_STALE_SECONDS=180 —
      beat_at = now-300s -> heartbeat_is_fresh(...) is False; beat_at = now-30s -> True.
      Cover naive AND tz-aware datetimes (psycopg may hand back either). Freshness, not existence.
    - case 28 test_lifespan_alerts_when_the_manager_never_starts, in
      dashboard/api/tests/test_lifespan_alert.py: force BotManager construction / start_all to raise
      (monkeypatch the import target used at main.py:61-63); run the lifespan; assert
      alert_manager_never_started (or send_alert) was invoked FROM main.py, and that app.state
      .bot_manager is None (the dashboard still serves read-only).
      RED TODAY: main.py:65-66 only calls _log.warning — into the void. The manager cannot alert about
      its own non-existence (N10), so the alert MUST come from here.
    - fences, in tests/test_phase19_fences.py (all STATIC over the working tree, each with a POSITIVE
      CONTROL so it cannot pass vacuously):
        F1 no prod trade write: grep src/ + dashboard/ + scripts/ for `UPDATE alpaca_trades` /
          `DELETE FROM alpaca_trades`; assert src/backfill.py is imported by NO module under
          src/bot_manager.py, dashboard/api/routes/, or dashboard/api/main.py.
          SELF-TEST: the same detector, applied to src/db.py's update_alpaca_trade, MUST MATCH —
          proving the detector actually fires. Allowlist only the pre-existing writer sites that exist
          on main today (capture them as a frozen literal set in the test).
        F2 risk rules untouched: BotUpdate.kelly_fraction keeps `le=0.25` (dashboard/api/models.py);
          max 5% position / 20% drawdown / limit-orders-only / 50-paper-trade gate literals unchanged.
        F3 paper gate closed: SettingsData.mode == "paper"; win_rate_target == 40.0; the gate
          comparison logic is unchanged. Making the readout HONEST may make it read WORSE — that is
          intended and is not a failure.
        F4 no new email provider: grep -riE "sendgrid|resend|postmark|mailgun" over src/ + dashboard/
          returns NOTHING; every new alert is a wrapper in src/notifier.py calling send_alert.
        F5 no retune: no diff to min_confluence / kelly_fraction / quarantined_symbols DEFAULT VALUES.
        F6 one Alpaca account per bot: the scheduled reconcile routes through
          reconciliation._client_for_bot; grep asserts bot_manager does NOT read bare
          ALPACA_API_KEY / construct an AlpacaClient directly.
        F7 no prod deploy-config change: no Coolify env/config mutation anywhere in the phase diff.
          (Research N4: the restart policy is a RED HERRING — the container is UP; only the threads are
          dead. No restart policy restarts a thread. It is verified read-only and REPORTED in 19-07.)
        F8 suite floor: this fence is the `pytest -q` >= 428 passed / 28 skipped assertion, recorded in
          the SUMMARY rather than self-asserted (a test cannot assert its own suite's total).
  </behavior>
  <action>Create tests/test_notifier_selfcheck.py, tests/test_phase19_fences.py, dashboard/api/tests/test_heartbeat.py, dashboard/api/tests/test_lifespan_alert.py; EXTEND tests/test_reconciliation.py with case 23 (append — do not restructure the existing cases; follow its existing zero-network double style). Import `from src.notifier import alerts_configured, alert_all_bots_down, alert_bot_misconfigured, alert_manager_never_started` and `from db import get_heartbeat, heartbeat_is_fresh, HEARTBEAT_STALE_SECONDS` — none exist yet, so those modules fail to import and every case in them is RED. That is correct; do NOT stub them. dashboard/api tests add `sys.path.insert(0, dashboard/api)` the same way dashboard/api/tests/test_routes.py:15 already does. No boto3, no psycopg, no network, no TEST_DATABASE_URL needed for any case in this task — all five files are pure. F1's allowlist is generated in THIS task from current main and committed as a literal, so after the phase lands it is a PIN, not a fresh capture.</action>
  <verify>
    <automated>python -m pytest tests/test_notifier_selfcheck.py tests/test_reconciliation.py tests/test_phase19_fences.py dashboard/api/tests/test_heartbeat.py dashboard/api/tests/test_lifespan_alert.py -q 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - Cases **10, 11, 12, 23, 26, 27, 28** FAIL (the units do not exist / reconcile has no try). Case 23's failure is the ValueError from `_client_for_bot` propagating out of `reconcile()` — **not** a fixture error.
    - Fences F1-F7 all PASS today (they are pins on the current, correct state) and F1's SELF-TEST proves the detector fires on `src/db.py::update_alpaca_trade`. A fence that passes on an EMPTY slice is a bug — every fence asserts its positive control first.
    - Previously-passing cases in `tests/test_reconciliation.py` still pass.
    - `grep -nE "boto3|psycopg|requests|httpx" tests/test_notifier_selfcheck.py dashboard/api/tests/test_heartbeat.py` returns NOTHING.
    - `grep -rnE "\bDATABASE_URL\b" tests/test_phase19_fences.py dashboard/api/tests/test_heartbeat.py dashboard/api/tests/test_lifespan_alert.py | grep -v TEST_DATABASE_URL` returns NOTHING (prod fence).
  </acceptance_criteria>
  <done>The alert path, the heartbeat's absence-is-dead semantics, main.py's never-started alert, and the N1 per-bot reconcile guard each have a failing test. All eight fences are pinned and non-vacuous.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the RESOLVED predicate + the honest headline — cases 13-22</name>
  <files>tests/test_db.py, dashboard/api/tests/test_portfolio_win_rate.py, dashboard/api/tests/test_routes.py</files>
  <read_first>19-VALIDATION.md cases 13-22; src/db.py L224-258 (get_alpaca_accuracy — `wins = sum(... (r["pnl"] or 0) > 0)`, `losses = resolved - wins`, and the comment at :228 that ADMITS a 0.0 is counted), L322-331 (get_starting_equity — the missing-row-only 100000 fallback), L334-356 (record_reconciliation — the UPSERT shape the tests seed against); tests/test_db.py L90-172 (_FakeResult / _FakeConn / the `accuracy` fixture / _fixture_rows / _t — REUSE THIS EXACT STYLE, and note test_null_pnl_excluded_from_denominator currently asserts `losses == 4  # 3 real losses + the ONE genuine 0.0` — that assertion is what changes); dashboard/api/routes/portfolio.py L61-101 (the win-rate arithmetic at :76-80 AND the daily-P&L query at :93-101 which has NO pnl filter at all — research N7), L103-137 (the SILENT Alpaca -> trade-log fallback at :111-122 with no flag on the response); dashboard/api/routes/settings.py L37-65 (the paper-gate win rate at :62-64 and the HARDCODE at :65 `equity = 100_000.0 * len(bot_ids) + total_pnl`), L113-144 (health + the paper gate readout); dashboard/api/tests/test_portfolio_win_rate.py (the static + TEST_DATABASE_URL-gated split — follow it); dashboard/api/tests/test_routes.py L1-60 (the module-level skipif + seeded client fixture)</read_first>
  <behavior>
    RESOLVED := `pnl IS NOT NULL AND pnl <> 0`. A `pnl = 0.0` sentinel is NOT a loss: it is excluded
    from the win-rate NUMERATOR **and** DENOMINATOR and from the realized-P&L sum, and reported as its
    own `unresolved` count beside wins/losses. Reference impl: src/symbol_stats.py's `zero_pnl` bucket.

    - case 13 test_zero_pnl_is_not_a_loss (tests/test_db.py, via the existing `accuracy` fixture):
      row set = 1 win (+10), 1 real loss (-5), 1 sentinel (0.0), 1 NULL. Assert wins==1, losses==1,
      resolved==2, win_rate==approx(0.5), unresolved==2.
      RED TODAY: db.py:244-246 gives resolved=3, losses=2, win_rate=0.333. The existing
      test_null_pnl_excluded_from_denominator (`losses == 4  # ... the ONE genuine 0.0`) must be
      UPDATED IN PLACE to the new predicate — that is the fix landing, NOT a regression (research N6).
      Extend _FakeConn to honour `pnl <> 0` the same way it already honours `pnl IS NOT NULL`.
    - case 14 test_accuracy_dict_gains_unresolved_additively: every existing key still present
      (total_trades, resolved, wins, losses, win_rate, total_pnl, avg_pnl, crypto_pnl, stock_pnl)
      PLUS "unresolved". Consumers index by key (src/trade_logger.py:52-53 ->
      src/alpaca_orchestrator.py:650,:1327; scripts/symbol_report.py:271) and must not break.
    - case 15 test_portfolio_win_rate_excludes_sentinels (dashboard, static + DB-gated): same 4-row set.
      Assert wins==1, losses==1, trades_resolved==2, unresolved==2.
      RED TODAY: portfolio.py:77-80 books the 0.0 as a loss via `losses = len(closed) - wins`.
      STATIC half (runs everywhere): POSITIVE CONTROL that portfolio.py contains
      "status IN ('closed', 'stopped', 'target_hit')", THEN assert it contains "pnl <> 0" (or the
      Python-side is_resolved equivalent).
    - case 16 test_portfolio_realized_sum_excludes_sentinels: closed-P&L sum == +5 (= +10 + -5),
      computed over RESOLVED rows only. Zeros contribute zero to a sum so the NUMBER is unchanged —
      the point is that the population is now consistent with the win rate. No silent disagreement.
    - case 17 test_paper_gate_win_rate_excludes_sentinels (settings.py — THE PAPER GATE): same row set
      via GET /api/settings. Assert win_rate is computed over resolved-only rows.
      RED TODAY: settings.py:62-64. This is the gate that guards LIVE TRADING. Making it honest may
      make it read WORSE. That is intended. The gate is NOT unlocked here.
    - case 18 test_daily_pnl_uses_the_same_predicate (research N7): daily rows including a sentinel and
      a NULL. Assert the same RESOLVED predicate governs portfolio.py:93-101 — which today has NO
      `pnl IS NOT NULL` filter at all. Not a correctness bug (zeros sum to zero) but a FIFTH reader
      disagreeing about what a trade is.
    - case 19 test_headline_pnl_is_the_reconciled_number: seed a `reconciliation` row with
      alpaca_realized_pnl=+500, trade_log_pnl=+900 (a $400 delta). Assert the headline reports +500
      (+ live unrealized), NOT +900. The overstated trade-log sum stops being the headline.
    - case 20 test_pnl_source_is_surfaced_and_correct: (a) fresh reconciliation row -> "reconciled".
      (b) no row, Alpaca reachable -> "alpaca_live". (c) no row, Alpaca unreachable -> "trade_log".
      portfolio.py:111-122 SILENTLY falls back today with no flag. The consumer can no longer be misled
      about which number it is looking at. Stub _fetch_alpaca_account (monkeypatch) — NEVER hit Alpaca.
    - case 21 test_stale_is_surfaced: stale := no row, OR checked_at older than
      2 x RECONCILE_INTERVAL_HOURS. (a) no row -> True. (b) checked_at 3h old (interval=1h) -> True.
      (c) 30min old -> False. The number is still shown when stale — but FLAGGED. The dashboard never
      presents an unreconciled figure as reconciled.
    - case 22 test_settings_equity_is_not_hardcoded (dashboard/api/tests/test_routes.py): seed
      bots.starting_equity to a NON-100000 value (e.g. 50_000). Assert GET /api/settings `equity`
      derives from bots.starting_equity, NOT from 100_000 * len(bot_ids).
      STATIC half: grep-assert the literal `100_000.0 * len(` is ABSENT from settings.py (POSITIVE
      CONTROL first: the file contains "equity=" / "win_rate_target").
      RED TODAY: settings.py:65. A hardcoded bankroll in the paper-gate readout is exactly how a gate
      gets passed by a bug.
  </behavior>
  <action>EXTEND tests/test_db.py (cases 13, 14) — reuse the existing `accuracy` fixture and _FakeConn (tests/test_db.py:103-132); teach _FakeConn the `pnl <> 0` clause exactly the way it already handles `pnl IS NOT NULL`; UPDATE the existing `losses == 4` assertion in place to the new predicate and leave a one-line comment saying Phase 19 changed it. EXTEND dashboard/api/tests/test_portfolio_win_rate.py (cases 15, 16, 18, 19, 20, 21) — keep its existing STATIC + TEST_DATABASE_URL-gated split; the static halves run everywhere with a positive control, the behavioral halves skip VISIBLY without TEST_DATABASE_URL. EXTEND dashboard/api/tests/test_routes.py (cases 17, 22) — it already has a module-level skipif on TEST_DATABASE_URL and a seeded client fixture; add the settings cases there, and add the `100_000.0 * len(` grep assertion as a SEPARATE ungated static test so the hardcode fence runs with no DB. NEVER reference bare DATABASE_URL. NEVER call the real Alpaca API — monkeypatch portfolio._fetch_alpaca_account. Cases 19/20/21 seed the `reconciliation` table (an ADDITIVE, non-trade table — allowed) and never touch alpaca_trades except in the TEST database.</action>
  <verify>
    <automated>python -m pytest tests/test_db.py dashboard/api/tests/test_portfolio_win_rate.py dashboard/api/tests/test_routes.py -q 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - Cases **13, 15 (static), 17 (static), 22 (static)** FAIL without any database. Cases 14, 16, 18, 19, 20, 21 and the behavioral halves of 15/17/22 SKIP VISIBLY without TEST_DATABASE_URL (never silently).
    - Case 13's failure is `resolved == 3, losses == 2` (the current arithmetic) — **not** a fixture error.
    - `grep -n "unresolved" tests/test_db.py` returns >= 2 hits (the new key is actually asserted).
    - `grep -rnE "\bDATABASE_URL\b" dashboard/api/tests/test_portfolio_win_rate.py | grep -v TEST_DATABASE_URL` — the ONE pre-existing `os.environ["DATABASE_URL"] = os.environ["TEST_DATABASE_URL"]` assignment inside the gated test is allowed; no NEW bare reference is introduced.
    - `grep -n "100_000.0 \* len(" dashboard/api/tests/test_routes.py` returns a hit (the hardcode fence really greps for the literal).
    - `python -m pytest tests/ dashboard/api/tests/ -q 2>&1 | tail -1` — the ONLY new failures are the intended RED; the 428/28 baseline is otherwise intact.
  </acceptance_criteria>
  <done>The RESOLVED predicate has a failing test at every reader site, the paper-gate readout is pinned, the $100k hardcode has a grep fence, and pnl_source/stale/reconciled have contracts before any route changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test suite → prod DB | a test reading bare `DATABASE_URL` writes to the live trade log |
| test suite → live Alpaca | a test instantiating a real AlpacaClient hits a live paper account |
| test suite → AWS SES | a test importing boto3 sends a real email to the owner |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | a DB-gated test pointed at prod | mitigate | TEST_DATABASE_URL only; F1's static fence with a SELF-TEST proving the detector fires |
| T-19-02 | Tampering | `src/backfill.py` invoked "while we're in there" | mitigate | F1 asserts backfill is imported by no manager/route module; the 395 sentinels are READ-AROUND only |
| T-19-03 | DoS | a test that really constructs `ConnectionPool` and dials Postgres | mitigate | `src.bot_manager.ConnectionPool` is monkeypatched to a FakePool before every BotManager construction |
| T-19-04 | Information Disclosure | an alert body echoing AWS/Alpaca key material | mitigate | case 12 asserts no key string or prefix appears in any alert body; `alerts_configured` is a bool |
| T-19-05 | Repudiation | a "RED" case that is actually a SKIP, or a static case passing on an empty slice | mitigate | no importorskip/hasattr guards; every static case asserts a POSITIVE CONTROL first |
| T-19-SC | Tampering | package installs | accept | none — this phase installs no packages |
</threat_model>

<verification>
- `python -m pytest tests/ dashboard/api/tests/ -q` — the only new failures are the intended RED set.
- Cases **1, 5, 6, 13, 15, 17, 22, 23, 26** are each confirmed FAILING, with the exact failure reason recorded in the SUMMARY. A case that passes pre-fix is not testing the bug — if any of the nine passes, STOP and fix the test.
- `grep -rn "backfill" tests/test_phase19_fences.py` — F1 really names the loaded gun.
</verification>

<success_criteria>
All 29 VALIDATION cases and all 8 fences have a named test. The nine must-fail cases fail for the
right reason. `tests/test_bot_manager.py` exists for the first time. No test touches SES, Alpaca, or
the prod DB.
</success_criteria>

<output>
Create `.planning/phases/19-reliable-runtime/19-01-SUMMARY.md` when done. It MUST include a table of
the nine must-fail cases with the verbatim failure line for each.
</output>
</content>
