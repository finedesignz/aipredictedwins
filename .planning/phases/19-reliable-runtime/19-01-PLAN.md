---
phase: 19-reliable-runtime
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_bot_manager.py
  - tests/test_notifier_selfcheck.py
  - tests/test_reconciliation.py
  - tests/test_db.py
  - tests/test_phase19_fences.py
  - dashboard/api/tests/test_heartbeat.py
  - dashboard/api/tests/test_lifespan_alert.py
  - dashboard/api/tests/test_portfolio_win_rate.py
  - dashboard/api/tests/test_routes.py
autonomous: true
requirements: [RUN-01, RUN-02]

must_haves:
  truths:
    - "tests/test_bot_manager.py DOES NOT EXIST today. It is created here and it is the highest-value missing file in the phase — RUN-01 is entirely untested."
    - "Cases 1, 1b, 5, 5b, 6, 13, 15, 17, 22, 23, 26 MUST FAIL against current main. A case that passes pre-fix is not testing the bug. Each gets a recorded VERBATIM failure line."
    - "CASE 1b IS NOT OPTIONAL. Case 1 drives _check_bots_down DIRECTLY with self._threads = {}, so it would go GREEN even against a tick order that can NEVER fire the alert in production. _revive_dead_bots (bot_manager.py:101-119) RESPAWNS dead threads via _spawn (:117) -> thread.start(), so any bot revived earlier in the SAME tick is is_alive() == True by the time a later _check_bots_down runs. In the DOMINANT failure mode (threads crash-loop each cycle: bad keys, Alpaca 401, unhandled exception in _main_loop) that means alive > 0 and NO ALL-BOTS-DOWN ALERT, EVER. Case 1b drives the WHOLE watchdog body as one integrated tick and is the only test that can catch it."
    - "THE KEYLESS PREDICATE MUST CHECK BOTH KEYS. A row with alpaca_api_key SET and alpaca_secret_key EMPTY passes an api-key-only check and gets SPAWNED. Traced: src/bot_config.py:46-47 coerces a missing key to `or \"\"` (empty is LEGAL) -> src/bot_thread.py:118-126 _make_alpaca_config builds Config straight from the row with NO env reads -> src/config.py:74-76 Config is a frozen dataclass with `alpaca_api_key: str = \"\"` and DOES NOT RAISE. CLAUDE.md's 'empty bare keys fail-clear (raise)' is the ORCHESTRATOR SERVICE's config path and DOES NOT HOLD for BotConfig/BotThread. Result: a client with an empty secret -> 401 -> run() catches -> _set_status('error') -> thread exits -> revived again next tick, FOREVER, email throttled to 1h. Exactly the N5 hot loop. Case 5b covers it."
    - "The FakeConn HONOURS the SQL it is handed. If the query text carries `alpaca_api_key IS NOT NULL`, the fake filters those rows out — otherwise cases 5/6's stated RED reason is FALSE (on main the rows come back anyway) and case 8 ('the predicate change did not affect healthy bots') is VACUOUS."
    - "Case 6 guards research N5: post-fix a keyless bot has thread is None, would fall into _maybe_send_death_alert (:114) with the WRONG message and then _spawn a credential-less bot EVERY 60s FOREVER (the 1h cooldown throttles the EMAIL, not the SPAWN)."
    - "Case 23 guards research N1. reconciliation.reconcile() (:139-143) has NO per-bot try and _client_for_bot RAISES on a keyless bot (:86-90), while _enabled_bot_ids (:51-59) has NO key predicate. Scheduling it hourly unguarded turns ONE misconfigured bot into TOTAL SILENT reconciliation failure."
    - "Case 26 guards research N10 — the watchdog cannot report its own non-existence. self._watchdog.start() (:79) sits AFTER the start_all query that can throw (:67-70). manager_alive MUST be computed from row ABSENCE or STALENESS and must NEVER default healthy."
    - "No test touches SES, live Alpaca, or the prod DB. Cases 1-14 and 26-27 are pure (fakes only). DB-gated cases use TEST_DATABASE_URL ONLY and skip VISIBLY."
    - "Every static/grep case runs a POSITIVE CONTROL first (the scanned slice is non-empty and contains a known-present token) so it cannot pass vacuously."
  artifacts:
    - path: "tests/test_bot_manager.py"
      provides: "VALIDATION cases 1, 1b, 2-9b, 24, 25, 29 — all of RUN-01's runtime behavior"
      min_lines: 230
    - path: "tests/test_notifier_selfcheck.py"
      provides: "VALIDATION cases 10-12 + 12b — the alert path proves it can send, and reports when it did not"
      min_lines: 55
    - path: "tests/test_phase19_fences.py"
      provides: "Fences F1-F8 — no prod trade write, no risk-rule change, no new provider, no retune"
      min_lines: 70
    - path: "dashboard/api/tests/test_heartbeat.py"
      provides: "VALIDATION cases 26-27 — absence and staleness both mean DEAD"
      min_lines: 40
    - path: "dashboard/api/tests/test_lifespan_alert.py"
      provides: "VALIDATION case 28 — main.py's except branch alerts on a never-started manager"
      min_lines: 35
  key_links:
    - from: "tests/test_bot_manager.py case 1b"
      to: "src.bot_manager.BotManager._tick"
      via: "ONE integrated tick with revive ENABLED — the only call shape that exists in production"
      pattern: "_tick"
    - from: "tests/test_bot_manager.py case 5b"
      to: "src.bot_config.BotConfig.from_row"
      via: "a row with an api_key but an EMPTY secret_key must still be treated as keyless"
      pattern: "alpaca_secret_key"
    - from: "tests/test_notifier_selfcheck.py"
      to: "src.notifier.alerts_configured"
      via: "the unit under test does not exist yet"
      pattern: "alerts_configured"
---

<objective>
Wave 0: the RED suite for all 29 cases + 8 fences in `19-VALIDATION.md`, **plus the three cases the
plan-check exposed** (1b — the integrated tick; 5b — the empty-secret row; 12b — the swallowed
delivery error). **Nothing in `src/` or `dashboard/api/routes/` or `dashboard/web/` changes here.**

Purpose: Phase 19 exists because four bots stopped and nothing said a word. The mechanism that was
supposed to say something (`_check_trade_silence`) disables itself with `if not any_alive: return` at
`src/bot_manager.py:189-190` — the exact condition it should scream about. That bug survived because
`tests/test_bot_manager.py` **does not exist**.

Two traps would let a "fixed" phase stay silent:
1. If `_check_bots_down` runs AFTER `_revive_dead_bots` in the same tick, every revivable bot is
   already `is_alive()` again and the alert **never fires in production**. A unit test that calls
   `_check_bots_down` directly cannot see that. **Case 1b** can.
2. If the keyless predicate only checks `alpaca_api_key`, a row with an **empty secret** is still
   spawned, 401s, exits, and is revived forever. **Case 5b** catches it.

Output: five new test files, four extended, the whole Phase-19 spec, RED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-reliable-runtime/19-VALIDATION.md
@.planning/phases/19-reliable-runtime/19-RESEARCH.md
@.planning/phases/19-reliable-runtime/19-CONTEXT.md
@CLAUDE.md

<interfaces>
These units DO NOT EXIST YET. Plans 19-02 … 19-06 create them. Their contracts are **fixed here** —
the tests import by name and the implementations must match exactly.

  # src/notifier.py  (Plan 19-02)
  def alerts_configured() -> bool
      """Config PRESENCE only — no SES call, no send. Mirrors _get_ses_client's own
      resolution order (notifier.py:27-39) EXACTLY:
        SECRETS_PATH.exists() -> True
        else: bool(AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY)"""
  def last_alert_error() -> str | None
      """The last exception string swallowed by send_alert (notifier.py:59-61), else None.
      Config presence != delivery: a valid-LOOKING config still 403s on an unverified SES
      identity, and send_alert swallows it. alerts_configured() cannot see that."""

  def alert_all_bots_down(bots_enabled: int, hours_since_trade: float | None) -> bool
      # subject MUST contain the literal "ALL BOTS DOWN"
  def alert_bot_misconfigured(bot_id: str, label: str, reason: str) -> bool
      # subject/body MUST NOT say "died"/"dead". reason is the WHOLE detail; never key material.
  def alert_manager_never_started(error: str) -> bool
      # fired from dashboard/api/main.py's except branch, NOT from BotManager (research N10)

  # src/bot_manager.py  (Plans 19-03, 19-05)
  _BOTS_DOWN_COOLDOWN  = 3600          # 1h; repeats until one bot is alive
  _HEARTBEAT_COMPONENT = "bot_manager"
  _RECONCILE_INTERVAL_HOURS = float(os.environ.get("RECONCILE_INTERVAL_HOURS", "1"))

  class BotManager:
      def _enabled_rows(self) -> list[dict]
          """SELECT * FROM bots WHERE enabled = TRUE  — NO KEY PREDICATE.
          Replaces the byte-identical filtered query at :67-70 AND :104-107."""
      @staticmethod
      def _has_keys(row: dict) -> bool
          """BOTH keys: bool(row.get("alpaca_api_key") and row.get("alpaca_secret_key")).
          An api-key-only check lets an empty-SECRET row through to _spawn — see case 5b."""
      def _tick(self) -> None
          """ONE watchdog iteration, extracted so a test can drive the REAL call shape.
          Liveness is snapshotted BEFORE revive — the order is load-bearing (case 1b)."""
      def _check_bots_down(self, alive_before: int, enabled: int,
                           hours_since_trade: float | None) -> None
      def _heartbeat(self) -> None            # UPSERT runtime_heartbeat via self._pool
      def _maybe_reconcile(self) -> None      # hourly, interval-guarded, PER-BOT try (research N1)

  # src/reconciliation.py  (Plan 19-05)
  def reconcile(tolerance: float | None = None) -> list[tuple[str, dict]]
      """PER-BOT try/except. A bot whose _client_for_bot raises is LOGGED and SKIPPED;
      the healthy bots still reconcile and persist. No exception escapes."""

  # dashboard/api/db.py  (Plan 19-02)
  HEARTBEAT_STALE_SECONDS: int          # env HEARTBEAT_STALE_SECONDS, default 180
  def get_heartbeat(conn, component: str = "bot_manager") -> dict | None   # None when NO ROW
  def heartbeat_is_fresh(beat_at, now=None, stale_seconds=None) -> bool
      """beat_at is None -> False.  ABSENCE IS THE SIGNAL. Never default healthy."""

  # dashboard/api/models.py  (Plan 19-06)
  PortfolioData  += pnl_source: str = "trade_log"   # {"reconciled","alpaca_live","trade_log"}
                 += stale: bool = True
                 += unresolved: int = 0
                 += reconciled: Optional[dict] = None
  HealthStatus   += manager_alive: bool = False     # BOOL ONLY — never echo key material
                 += alerts_configured: bool = False
                 += alerts_last_error: Optional[str] = None
                 += bots_alive: int = 0
                 += bots_enabled: int = 0
                 += last_heartbeat: Optional[str] = None
  SettingsData   += unresolved: int = 0

  # src/db.py  (Plans 19-02, 19-04)
  get_alpaca_accuracy(bot_id, last_n=None) -> dict
      # ADDITIVE new key "unresolved". All nine existing keys keep their names (research N6).
      # RESOLVED := pnl IS NOT NULL AND pnl <> 0

Existing pieces the tests drive (import them; do NOT re-derive):
  src/bot_manager.py:67-70     start_all's KEY PREDICATE (byte-identical to :104-107)
  src/bot_manager.py:101-119   _revive_dead_bots — RESPAWNS via _spawn (:117) -> thread.start().
                               THIS is why tick ORDER matters (case 1b).
  src/bot_manager.py:121-139   _maybe_send_death_alert (1h cooldown dict at :123-127)
  src/bot_manager.py:145-212   _check_trade_silence (interval guard :149-151; the MAX(timestamp) query
                               at :157-164 is where hours_since_trade comes from; THE KILLER at :186-190)
  src/bot_manager.py:274-297   _spawn — CopyTraderThread when cfg.strategy == "copytrade", else BotThread
  src/bot_manager.py:299-309   _on_status_change — the SOLE SQL writer of bots.status
  src/bot_config.py:46-47      alpaca_api_key / alpaca_secret_key are `row.get(...) or ""` — EMPTY IS LEGAL
  src/bot_thread.py:118-126    _make_alpaca_config — builds Config straight from BotConfig, NO env reads
  src/config.py:74-76          Config: frozen dataclass, keys default to "", **DOES NOT RAISE**
  src/notifier.py:42-61        send_alert — SWALLOWS every failure (why cases 10 and 12b exist)
  src/reconciliation.py:51-59  _enabled_bot_ids (enabled = TRUE, NO key predicate)
  src/reconciliation.py:62-93  _client_for_bot (RAISES ValueError on a keyless bot, :86-90)
  src/db.py:61-66              _bootstrap_schema() — executes db_schema.sql WHOLESALE (why case 29 needs
                               BOTH files)
  src/db.py:224-258            get_alpaca_accuracy (`losses = resolved - wins` — the 0.0 bug)
  tests/test_db.py:103-132     _FakeConn + `accuracy` fixture — REUSE THIS, do not write a second style

RESEARCH REFUTES ONE CONTEXT CLAIM (case 9b exists because of it): bots.status has **TWO writer
classes**. CopyTraderThread (Bot E) has its own _set_status at src/copytrade_thread.py:140-144. The
SOLE SQL writer is still BotManager._on_status_change.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tests/test_bot_manager.py — cases 1, 1b, 2-9b, 24, 25, 29 (all of RUN-01's runtime)</name>
  <files>tests/test_bot_manager.py</files>
  <read_first>19-VALIDATION.md cases 1-9b, 24, 25, 29; src/bot_manager.py L1-95 (constants, __init__, start_all's KEY PREDICATE at :67-70, the loop's per-step try/except at :88-95), L101-139 (_revive_dead_bots — _spawn at :117 STARTS a live thread; the death alert at :114 fires BEFORE it; the cooldown dict at :123-127), L145-212 (the interval guard at :149-151, the MAX(timestamp) query at :157-164, THE KILLER at :186-190), L274-309; src/bot_config.py L40-55 (`or ""` at :46-47 — an EMPTY key is legal); src/bot_thread.py L118-126 (no env reads); src/config.py L74-76 (does NOT raise on empty keys); tests/conftest.py; tests/test_db.py L103-115 (the SQL-honouring _FakeConn — copy this idea)</read_first>
  <behavior>
    - Shared fakes at the top, NO network / NO DB / NO SES:
        FakeThread: settable is_alive() flag; stop()/join() no-ops; **start() FLIPS the flag to True**
          (this is what makes case 1b meaningful — a revived thread really does become alive).
          .config has a .label.
        FakeConn / FakePool: .connection() is a context manager yielding a FakeConn whose
          .execute(sql, params) **HONOURS THE SQL IT IS HANDED**, mirroring tests/test_db.py:103-115:
            * SELECT on `bots` returns the seeded rows — and **if the SQL text contains
              "alpaca_api_key IS NOT NULL", it FILTERS OUT rows with an empty/NULL api_key.**
              WITHOUT this, cases 5/6's stated RED reason is FALSE (on main those rows would come back
              anyway and the tests would fail for an unrelated reason) and case 8 is VACUOUS.
            * SELECT with MAX(timestamp) returns {"last_trade": <seeded ts>}
            * every UPSERT/UPDATE is RECORDED into a list for assertions.
        capture_alerts fixture: monkeypatch src.notifier.send_alert AND the typed wrappers with a
          recorder appending (subject, body). boto3 is NEVER imported.
        make_manager(rows, threads): monkeypatch src.bot_manager.ConnectionPool to FakePool BEFORE
          constructing BotManager. The watchdog thread is NEVER started.

    - case 1  test_all_bots_down_alerts (THE KILLER, unit shape): one enabled+keyed row,
      self._threads = {} (or all dead); last trade 30h ago. Drive the down-check. Assert >= 1 alert
      whose subject contains "ALL BOTS DOWN".
      RED TODAY: :189-190 returns before any alert, and _check_bots_down does not exist.
    - case 1b test_all_bots_down_alerts_through_a_whole_tick (**THE ONE THAT ACTUALLY MATTERS**):
      drive the REAL tick body (`mgr._tick()`) with revive ENABLED and `_spawn` stubbed to install a
      FakeThread whose .start() sets alive=True — i.e. the revive SUCCEEDS, exactly as in production.
      Seed TWO enabled+keyed bots whose threads are BOTH dead at tick start. Assert an "ALL BOTS DOWN"
      alert IS STILL CAPTURED.
      WHY: _revive_dead_bots (:101-119) calls _spawn (:117) -> thread.start(). If liveness is checked
      AFTER revive, every revivable bot is alive again by then -> alive > 0 -> **the alert NEVER FIRES**
      in the DOMINANT failure mode (threads crash-loop each cycle on bad keys / a 401 / an unhandled
      exception in _main_loop). Case 1 alone cannot see this: it calls _check_bots_down directly with
      an empty thread dict, a shape that never occurs in production. Liveness MUST be snapshotted at
      tick START, before revive. Write this test so that a "fix" which merely reorders the alert AFTER
      revive FAILS it.
      Then tick a SECOND time (both threads crash-dead again) and assert the outage is still recorded —
      the cooldown must not be reset merely because a revive briefly succeeded.
      RED TODAY: `_tick` does not exist.
    - case 2  test_all_bots_down_repeats_each_cooldown_window: tick -> 1 alert; immediate re-tick ->
      still 1 (cooldown); advance monkeypatched time past 3600s -> tick -> 2.
    - case 3  test_one_bot_alive_means_no_all_bots_down_alert: one FakeThread alive at tick START.
      Assert ZERO "ALL BOTS DOWN" alerts. The loudest alert must not cry wolf.
    - case 4  test_trade_silence_is_evaluated_on_its_own_merits: bots ALIVE, last trade 30h ago.
      Assert a TRADE-SILENCE alert fires and it is NOT an all-bots-down one.
    - case 5  test_keyless_enabled_bot_is_error_and_alerts: row {bot_id:"X", enabled:True,
      alpaca_api_key:"", alpaca_secret_key:""}. Spy on _on_status_change. Assert
      ("X","error","missing alpaca keys") AND exactly one misconfig alert.
      RED TODAY: the key predicate at :104-107 removes the row (the FakeConn honours it) — nothing is
      called at all.
    - case 5b test_api_key_without_secret_is_also_keyless (**THE HOT LOOP A NAIVE FIX LEAVES OPEN**):
      row {bot_id:"Y", enabled:True, alpaca_api_key:"AKTEST", alpaca_secret_key:""}. Assert the SAME
      outcome as case 5: status='error', one misconfig alert, **_spawn NOT called**.
      WHY: bot_config.py:46-47 coerces the missing secret to "" (LEGAL); bot_thread.py:118-126 builds
      Config straight from it with no env reads; config.py:74-76 Config is a frozen dataclass whose
      keys default to "" and **DOES NOT RAISE**. So `if not row.get("alpaca_api_key")` lets this row
      through -> spawn -> 401 on the first Alpaca call -> run() catches -> _set_status("error") ->
      thread exits -> revived AGAIN next tick, FOREVER, email throttled to 1h. Exactly the N5 loop.
      CLAUDE.md's "empty bare keys fail-clear (raise)" applies to the ORCHESTRATOR SERVICE's config
      path, NOT to BotConfig/BotThread.
    - case 6  test_keyless_bot_is_not_spawned_and_gets_no_death_alert (N5): the case-5 AND case-5b
      fixtures, spy on _spawn. Assert _spawn NOT called for either, and NO alert subject contains
      "died"/"dead".
    - case 7  test_keyless_alert_respects_the_one_hour_cooldown: two ticks inside an hour -> exactly 1
      alert, but _on_status_change("error") is re-asserted on BOTH ticks.
    - case 8  test_healthy_keyed_bot_is_unaffected: row with BOTH keys + FakeThread(alive=True). No
      status change, no alert, no respawn. (Non-vacuous ONLY because the FakeConn honours the
      predicate: on main this row survives the filter; post-fix it survives its absence.)
    - case 9  test_dead_thread_is_revived: keyed enabled row + dead FakeThread. Assert _spawn called
      AND a death alert fired.
    - case 9b test_revive_dispatches_on_strategy (refutation C1): strategy="copytrade", dead thread ->
      the spawned class is CopyTraderThread. Patch both classes with recording stand-ins.
    - case 24 test_reconcile_step_self_throttles_and_never_kills_the_watchdog: spy on
      src.reconciliation.reconcile. _maybe_reconcile x3 inside an hour -> called ONCE. Make it raise ->
      the tick swallows it and LATER STEPS STILL RUN (assert the heartbeat was still written that tick).
    - case 25 test_heartbeat_upserts_a_row: two enabled rows, one thread alive. Assert the FakePool
      recorded an UPSERT into runtime_heartbeat for component='bot_manager' with bots_alive=1,
      bots_enabled=2.
    - case 29 test_migration_019_exists_in_both_files (STATIC, N3): POSITIVE CONTROL FIRST —
      src/db_schema.sql contains "CREATE TABLE IF NOT EXISTS reconciliation". THEN assert
      dashboard/api/migrations/019_runtime_heartbeat.sql exists and contains
      "CREATE TABLE IF NOT EXISTS runtime_heartbeat", AND src/db_schema.sql ALSO contains
      "runtime_heartbeat". src/db.py:61-66 `_bootstrap_schema()` executes db_schema.sql WHOLESALE, so a
      migration-only table is absent from every fresh-DB bootstrap.
  </behavior>
  <action>Create tests/test_bot_manager.py. Import `from src.bot_manager import BotManager` — the class exists, but `_tick`, `_has_keys`, `_enabled_rows`, `_check_bots_down`, `_heartbeat`, `_maybe_reconcile` do NOT, so those cases fail with AttributeError. That is the correct RED. Do NOT stub the methods, do NOT guard with getattr/hasattr, do NOT pytest.skip. Never construct a real ConnectionPool: monkeypatch `src.bot_manager.ConnectionPool` to the FakePool before instantiating. Never start the watchdog thread. Freeze time with a monkeypatched `src.bot_manager.time.time` — never `sleep`. The FakeConn MUST honour the `alpaca_api_key IS NOT NULL` clause when present in the SQL text. Cases 5/5b/6/7/9/9b spy via monkeypatch.setattr on the BOUND methods so the assertions are about call arguments. Case 1b is the ONLY case that drives the integrated tick — it is the phase's real proof.</action>
  <verify>
    <automated>python -m pytest tests/test_bot_manager.py -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_bot_manager.py -q` — 15 collected. Cases **1, 1b, 5, 5b, 6** FAIL; cases 2, 24, 25, 29 also FAIL (methods/migration do not exist). Zero SKIP. Zero import-time ERROR.
    - Case 1b's failure is `AttributeError: _tick` (not a fixture error), and its docstring names the masking-by-revive mechanism. Record the verbatim line in the SUMMARY.
    - `grep -n 'alpaca_secret_key": ""' tests/test_bot_manager.py` returns a hit (case 5b's empty-secret row really exists).
    - `grep -n "alpaca_api_key IS NOT NULL" tests/test_bot_manager.py` returns a hit **inside the FakeConn** — the fake honours the predicate, so cases 5/6's RED reason is real and case 8 is non-vacuous.
    - `grep -c "ALL BOTS DOWN" tests/test_bot_manager.py` >= 3 (cases 1, 1b assert it fires; case 3 asserts it does not).
    - `grep -nE "boto3|requests|httpx|psycopg\.connect|DATABASE_URL" tests/test_bot_manager.py` returns NOTHING.
    - `grep -nE "hasattr|importorskip|pytest\.skip" tests/test_bot_manager.py` returns NOTHING — a skip is not a RED.
    - `python -m pytest tests/ -q 2>&1 | tail -1` — no PREVIOUSLY-PASSING test has broken.
  </acceptance_criteria>
  <done>The killer bug (case 1), the tick-order mask that would keep it silent in production (case 1b), the keyless-invisible bug (case 5), the empty-secret hot loop (case 5b), and the every-60s-forever respawn (case 6) each have a test that fails for the right reason on current main.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: alert path, heartbeat read, lifespan alert, reconcile guard, fences — cases 10-12b, 23, 26-28, F1-F8</name>
  <files>tests/test_notifier_selfcheck.py, tests/test_reconciliation.py, tests/test_phase19_fences.py, dashboard/api/tests/test_heartbeat.py, dashboard/api/tests/test_lifespan_alert.py</files>
  <read_first>19-VALIDATION.md cases 10-12, 23, 26-28 and fences F1-F8; src/notifier.py L16-19, L22-39 (_get_ses_client's EXACT resolution order), L42-61 (send_alert SWALLOWS everything — why cases 10 and 12b exist); src/reconciliation.py L51-59, L62-93 (RAISES at :86-90), L96-131, L134-143 (NO try — the N1 landmine); tests/test_reconciliation.py L1-40 (the zero-network double style); dashboard/api/main.py L46-71 (the swallowed failure at :65-66); dashboard/api/db.py; dashboard/Dockerfile L64 (`ENV PYTHONPATH="/app:..."` — what makes `import src.*` legal inside the dashboard process)</read_first>
  <behavior>
    - case 10 test_ses_unconfigured_reports_false: SECRETS_PATH.exists() False + both AWS env vars
      unset -> `alerts_configured() is False` (identity, not truthiness).
      RED TODAY: alerts_configured does not exist. send_alert swallows every exception (:59-61), so an
      unconfigured SES is completely invisible today.
    - case 11 test_ses_configured_reports_true_on_either_channel: (a) SECRETS_PATH exists -> True.
      (b) file absent, BOTH AWS vars set -> True. (c) only ONE var set -> False. Mirrors
      _get_ses_client's order (:27-39) EXACTLY. boto3 is never imported.
    - case 12 test_alerts_configured_is_a_bool_and_leaks_nothing: isinstance(..., bool); with
      AWS_ACCESS_KEY_ID="AKIAEXAMPLE123", that string (and any 4-char prefix) appears NOWHERE in the
      body of alert_bot_misconfigured("X","Bot X","missing alpaca keys").
    - case 12b test_send_alert_records_its_last_error (**config presence != delivery**): make the SES
      call raise. Assert send_alert still returns False and never raises, AND `last_alert_error()` now
      returns the exception string. A valid-LOOKING config still 403s on an unverified SES identity;
      alerts_configured() cannot see that and send_alert swallows it.
      RED TODAY: last_alert_error does not exist.
    - case 23 test_reconcile_is_guarded_per_bot (N1), in tests/test_reconciliation.py: _enabled_bot_ids
      -> ["A","X"]; _client_for_bot raises ValueError for "X", returns a fake for "A". reconcile() then:
      NO exception escapes; "A" reconciled AND persisted; "X" logged as failed and absent from results.
      RED TODAY: :139-143 has no try — the ValueError propagates and reconciles ZERO bots.
    - case 26 test_absence_of_row_means_dead (N10): heartbeat_is_fresh(None) is False;
      get_heartbeat(<no rows>) returns None. self._watchdog.start() (:79) sits AFTER the start_all query
      that can throw (:67-70), so the watchdog cannot report its own non-existence. ABSENCE IS THE SIGNAL.
    - case 27 test_stale_heartbeat_means_dead: stale=180 — now-300s -> False; now-30s -> True. Cover
      naive AND tz-aware datetimes.
    - case 28 test_lifespan_alerts_when_the_manager_never_starts: force BotManager/start_all to raise;
      run the lifespan; assert alert_manager_never_started was invoked FROM main.py and
      app.state.bot_manager is None (the dashboard still serves read-only).
      RED TODAY: :65-66 only calls _log.warning — into the void.
    - fences, in tests/test_phase19_fences.py (STATIC, each with a POSITIVE CONTROL):
        F1 no prod trade write: grep src/ + dashboard/ + scripts/ for `UPDATE alpaca_trades` /
          `DELETE FROM alpaca_trades`; assert src/backfill.py is imported by NO module under
          src/bot_manager.py, dashboard/api/routes/, dashboard/api/main.py. SELF-TEST: the same detector
          applied to src/db.py's update_alpaca_trade MUST MATCH. Allowlist the pre-existing writer sites
          as a frozen literal set captured from main IN THIS TASK.
        F2 risk rules: BotUpdate.kelly_fraction keeps `le=0.25`; the 5%/20%-DD/limit-only/50-trade
          literals unchanged.
        F3 paper gate: mode == "paper"; win_rate_target == 40.0; the gate comparison unchanged. An
          HONEST readout may read WORSE — intended, not a failure.
        F4 no new email provider: grep -riE "sendgrid|resend|postmark|mailgun" over src/ + dashboard/
          -> NOTHING; every new alert is a wrapper in src/notifier.py calling send_alert.
        F5 no retune: no diff to min_confluence / kelly_fraction / quarantined_symbols DEFAULTS.
        F6 one Alpaca account per bot: reconcile routes through _client_for_bot; bot_manager contains
          NO `AlpacaClient` and NO bare `ALPACA_API_KEY` read.
        F7 no prod deploy-config change (N4: the restart policy is a RED HERRING — the container is UP;
          only the threads are dead).
        F8 suite floor: `pytest -q` >= 428 passed / 28 skipped — recorded in the SUMMARY, not
          self-asserted.
  </behavior>
  <action>Create tests/test_notifier_selfcheck.py, tests/test_phase19_fences.py, dashboard/api/tests/test_heartbeat.py, dashboard/api/tests/test_lifespan_alert.py; EXTEND tests/test_reconciliation.py with case 23 (append; follow its existing zero-network double style). Import `from src.notifier import alerts_configured, last_alert_error, alert_all_bots_down, alert_bot_misconfigured, alert_manager_never_started` and `from db import get_heartbeat, heartbeat_is_fresh, HEARTBEAT_STALE_SECONDS` — none exist yet, so those modules fail to import and every case in them is RED. dashboard/api tests add `sys.path.insert(0, dashboard/api)` the way dashboard/api/tests/test_routes.py:15 already does. No boto3, no psycopg, no network, no TEST_DATABASE_URL needed — all five files are pure. F1's allowlist is generated in THIS task from current main and committed as a literal, so after the phase lands it is a PIN, not a fresh capture.</action>
  <verify>
    <automated>python -m pytest tests/test_notifier_selfcheck.py tests/test_reconciliation.py tests/test_phase19_fences.py dashboard/api/tests/test_heartbeat.py dashboard/api/tests/test_lifespan_alert.py -q 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - Cases **10, 11, 12, 12b, 23, 26, 27, 28** FAIL. Case 23's failure is the ValueError from `_client_for_bot` propagating out of `reconcile()` — **not** a fixture error.
    - Fences F1-F7 all PASS today, and F1's SELF-TEST proves the detector fires on `src/db.py::update_alpaca_trade`.
    - Previously-passing cases in `tests/test_reconciliation.py` still pass.
    - `grep -nE "boto3|psycopg|requests|httpx" dashboard/api/tests/test_heartbeat.py` returns NOTHING.
    - `grep -rnE "\bDATABASE_URL\b" tests/test_phase19_fences.py dashboard/api/tests/test_heartbeat.py dashboard/api/tests/test_lifespan_alert.py | grep -v TEST_DATABASE_URL` returns NOTHING.
  </acceptance_criteria>
  <done>The alert path (including the delivery error `alerts_configured` cannot see), the heartbeat's absence-is-dead semantics, main.py's never-started alert, and the N1 per-bot reconcile guard each have a failing test. All eight fences are pinned and non-vacuous.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the RESOLVED predicate + the honest headline — cases 13-22</name>
  <files>tests/test_db.py, dashboard/api/tests/test_portfolio_win_rate.py, dashboard/api/tests/test_routes.py</files>
  <read_first>19-VALIDATION.md cases 13-22; src/db.py L224-258 (get_alpaca_accuracy — `(r["pnl"] or 0) > 0`, `losses = resolved - wins`, and the comment at :228 that ADMITS the 0.0 is counted); tests/test_db.py L90-172 (_FakeResult / _FakeConn / the `accuracy` fixture / _fixture_rows / _t — REUSE THIS EXACT STYLE; note test_null_pnl_excluded_from_denominator currently asserts `losses == 4  # 3 real losses + the ONE genuine 0.0` — that assertion is what changes); dashboard/api/routes/portfolio.py L61-101 (the arithmetic at :76-80 AND the daily-P&L query at :93-101 with NO pnl filter at all — N7), L103-137 (the SILENT fallback at :111-122); dashboard/api/routes/settings.py L37-65 (the paper-gate win rate at :62-64 and the HARDCODE at :65), L113-144; dashboard/api/tests/test_portfolio_win_rate.py (the static + gated split); dashboard/api/tests/test_routes.py L1-60</read_first>
  <behavior>
    RESOLVED := `pnl IS NOT NULL AND pnl <> 0`. A sentinel is excluded from the win-rate NUMERATOR
    **and DENOMINATOR** and from the realized-P&L sum, and reported as its own `unresolved` count.
    Reference impl: src/symbol_stats.py's `zero_pnl` bucket.

    - case 13 test_zero_pnl_is_not_a_loss (tests/test_db.py, via the existing `accuracy` fixture — PURE,
      no database): rows = 1 win (+10), 1 real loss (-5), 1 sentinel (0.0), 1 NULL. Assert wins==1,
      losses==1, resolved==2, win_rate==approx(0.5), unresolved==2.
      RED TODAY: db.py:244-246 gives resolved=3, losses=2, win_rate=0.333. Extend _FakeConn to honour
      `pnl <> 0` the way it already honours `pnl IS NOT NULL`.
    - case 14 test_accuracy_dict_gains_unresolved_additively (**PURE — it FAILS today, it does NOT
      skip**): all nine existing keys present PLUS "unresolved". Consumers index by key
      (trade_logger.py:52-53 -> alpaca_orchestrator.py:650,:1327; scripts/symbol_report.py:271).
    - case 15 test_portfolio_win_rate_excludes_sentinels: STATIC half runs everywhere (POSITIVE CONTROL
      that portfolio.py contains "status IN ('closed', 'stopped', 'target_hit')", THEN assert the
      `pnl <> 0` predicate) — **the static half is what FAILS today**. BEHAVIORAL half (DB-gated):
      wins==1, losses==1, trades_resolved==2, unresolved==2.
    - case 16 test_portfolio_realized_sum_excludes_sentinels: closed-P&L sum == +5 over RESOLVED rows.
      **NUMERICALLY VACUOUS BY CONSTRUCTION — zeros sum to zero, so the number does not move.** Its
      value is the STATIC half (the sum's population is now the win rate's population). Say so in the
      docstring; do not overclaim it as a behavioral catch.
    - case 17 test_paper_gate_win_rate_excludes_sentinels (settings.py — THE PAPER GATE): static half
      FAILS today; behavioral half DB-gated. This is the gate that guards LIVE TRADING. Making it honest
      may make it read WORSE. That is intended. The gate is NOT unlocked here.
    - case 18 test_daily_pnl_uses_the_same_predicate (N7): the same predicate at portfolio.py:93-101,
      which today has NO filter at all. **Also numerically vacuous (zeros sum to zero)** — the static
      half is the real assertion. A fifth reader disagreeing about what a trade IS is how this bug
      survives.
    - case 19 test_headline_pnl_is_the_reconciled_number: seed reconciliation {alpaca_realized_pnl:+500,
      trade_log_pnl:+900}. Assert the headline reports +500 (+ live unrealized), NOT +900.
    - case 20 test_pnl_source_is_surfaced_and_correct: (a) fresh row -> "reconciled". (b) no row, Alpaca
      reachable -> "alpaca_live". (c) no row, Alpaca unreachable -> "trade_log". portfolio.py:111-122
      SILENTLY falls back today with no flag. Stub _fetch_alpaca_account — NEVER hit Alpaca.
    - case 21 test_stale_is_surfaced: no row -> True; checked_at 3h old (interval=1h) -> True;
      30min -> False. The number is still shown when stale — but FLAGGED.
    - case 22 test_settings_equity_is_not_hardcoded: seed bots.starting_equity = 50_000; assert
      GET /api/settings `equity` derives from it. STATIC half (UNGATED): grep-assert the literal
      `100_000.0 * len(` is ABSENT from settings.py (POSITIVE CONTROL first).
      RED TODAY (static half): settings.py:65.
  </behavior>
  <action>EXTEND tests/test_db.py (cases 13, 14) — reuse the `accuracy` fixture and _FakeConn (tests/test_db.py:103-132); teach _FakeConn the `pnl <> 0` clause exactly the way it already handles `pnl IS NOT NULL`; UPDATE the existing `losses == 4` assertion in place with a one-line comment saying Phase 19 changed it. EXTEND dashboard/api/tests/test_portfolio_win_rate.py (cases 15, 16, 18, 19, 20, 21) — keep its STATIC + TEST_DATABASE_URL-gated split; static halves run everywhere with a positive control, behavioral halves skip VISIBLY. EXTEND dashboard/api/tests/test_routes.py (cases 17, 22) — add the settings cases there, and add the `100_000.0 * len(` grep assertion as a SEPARATE UNGATED static test so the hardcode fence runs with no DB. NEVER reference bare DATABASE_URL. NEVER call the real Alpaca API. Cases 19/20/21 seed the `reconciliation` table (additive, non-trade — allowed) and touch alpaca_trades only in the TEST database. In the docstrings of cases 16 and 18, state plainly that they are numerically vacuous by construction and that their static halves carry the weight.</action>
  <verify>
    <automated>python -m pytest tests/test_db.py dashboard/api/tests/test_portfolio_win_rate.py dashboard/api/tests/test_routes.py -q 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - Without TEST_DATABASE_URL: cases **13 and 14 FAIL** (they are PURE — driven by the `_FakeConn` fixture; they do NOT skip), and the STATIC halves of **15, 17, 22** FAIL. The behavioral halves of 15/16/17/18/19/20/21/22 SKIP VISIBLY (never silently).
    - Case 13's failure is `resolved == 3, losses == 2` (the current arithmetic); case 14's is `KeyError: 'unresolved'` — neither is a fixture error.
    - `grep -n "unresolved" tests/test_db.py` returns >= 2 hits.
    - `grep -c "vacuous" dashboard/api/tests/test_portfolio_win_rate.py` >= 2 — cases 16 and 18 declare their numeric vacuity out loud.
    - `grep -n "100_000.0 \* len(" dashboard/api/tests/test_routes.py` returns a hit, in an UNGATED test.
    - `python -m pytest tests/ dashboard/api/tests/ -q 2>&1 | tail -1` — the ONLY new failures are the intended RED; the 428/28 baseline is otherwise intact.
  </acceptance_criteria>
  <done>The RESOLVED predicate has a failing test at every reader site, the paper-gate readout is pinned, the $100k hardcode has an ungated grep fence, and pnl_source/stale/reconciled have contracts before any route changes. The two vacuous cases say so out loud instead of overclaiming.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test suite → prod DB | a test reading bare `DATABASE_URL` writes to the live trade log |
| test suite → live Alpaca | a test instantiating a real AlpacaClient hits a live paper account |
| test suite → AWS SES | a test importing boto3 sends a real email to the owner |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | a DB-gated test pointed at prod | mitigate | TEST_DATABASE_URL only; F1's static fence with a SELF-TEST proving the detector fires |
| T-19-02 | Tampering | `src/backfill.py` invoked "while we're in there" | mitigate | F1 asserts backfill is imported by no manager/route module; the 395 sentinels are READ-AROUND only |
| T-19-03 | DoS | a test that really constructs `ConnectionPool` and dials Postgres | mitigate | `src.bot_manager.ConnectionPool` is monkeypatched to a FakePool before every BotManager construction |
| T-19-04 | Information Disclosure | an alert body echoing AWS/Alpaca key material | mitigate | case 12 asserts no key string or prefix appears in any alert body; `alerts_configured` is a bool |
| T-19-05 | Repudiation | a "RED" case that is actually a SKIP, or a static case passing on an empty slice | mitigate | no importorskip/hasattr guards; every static case asserts a POSITIVE CONTROL first |
| T-19-32 | Repudiation | **case 1 going GREEN against a call shape that never occurs in production** | mitigate | **case 1b** drives the integrated `_tick()` with revive ENABLED and `_spawn` installing a LIVE thread — the only shape that exists in prod |
| T-19-33 | Repudiation | **a FakeConn ignoring the WHERE clause, making cases 5/6's RED reason false and case 8 vacuous** | mitigate | the FakeConn HONOURS `alpaca_api_key IS NOT NULL`, mirroring tests/test_db.py:103-115 |
| T-19-34 | Spoofing | **an empty-SECRET row passing an api-key-only keyless check and being spawned into a 401 hot loop** | mitigate | **case 5b** — traced through bot_config.py:46-47 -> bot_thread.py:118-126 -> config.py:74-76 (Config does NOT raise) |
| T-19-SC | Tampering | package installs | accept | none — this phase installs no packages |
</threat_model>

<verification>
- `python -m pytest tests/ dashboard/api/tests/ -q` — the only new failures are the intended RED set.
- Cases **1, 1b, 5, 5b, 6, 13, 15, 17, 22, 23, 26** are each confirmed FAILING, with the exact failure reason recorded in the SUMMARY. If any passes pre-fix, STOP and fix the test.
- `grep -rn "backfill" tests/test_phase19_fences.py` — F1 really names the loaded gun.
</verification>

<success_criteria>
All 29 VALIDATION cases, the three plan-check additions (1b, 5b, 12b), and all 8 fences have a named
test. The eleven must-fail cases fail for the right reason. `tests/test_bot_manager.py` exists for the
first time. No test touches SES, Alpaca, or the prod DB.
</success_criteria>

<output>
Create `.planning/phases/19-reliable-runtime/19-01-SUMMARY.md` when done. It MUST include a table of
the eleven must-fail cases with the verbatim failure line for each.
</output>
</content>
