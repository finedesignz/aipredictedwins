---
phase: 19-reliable-runtime
plan: 05
type: execute
wave: 4
depends_on: [19-03]
files_modified:
  - src/reconciliation.py
  - src/bot_manager.py
autonomous: true
requirements: [RUN-02]

must_haves:
  truths:
    - "LANDMINE N1 — THIS PLAN'S WHOLE REASON FOR EXISTING. reconciliation.reconcile() (:139-143) has NO per-bot try, and _client_for_bot RAISES ValueError on a keyless bot (:86-90), while _enabled_bot_ids (:51-59) selects `enabled = TRUE` with NO key predicate. Scheduling it hourly UNGUARDED turns ONE misconfigured bot into TOTAL, SILENT reconciliation failure — the watchdog's outer except would just log a warning. And 19-03's B1 fix makes keyless-enabled bots VISIBLE, so this lands exactly when we touch it. Wrap PER BOT before scheduling."
    - "TODAY, one keyless-but-enabled bot already makes the manual scripts/reconcile.py throw and reconcile ZERO bots — including the healthy ones. This is a live latent bug, not a hypothetical."
    - "The hourly step self-throttles with its own timestamp, mirroring _check_trade_silence's interval guard (bot_manager.py:149-151). NO new thread. NO cron. NO scheduler. NO new container."
    - "One Alpaca account per bot. The scheduled reconcile keeps routing through reconciliation._client_for_bot (:62-93) — never a shared or bare-key client (CLAUDE.md hard rule, fence F6)."
    - "reconcile()'s only DB write is the reconciliation UPSERT (db.record_reconciliation, an additive non-trade table). It is READ-ONLY against Alpaca and against alpaca_trades."
    - "A raising reconcile step must never kill the watchdog loop (its own try/except, mirroring :88-95)."
  artifacts:
    - path: "src/reconciliation.py"
      provides: "reconcile() with a per-bot try/except — one bad bot cannot starve the others"
      contains: "except Exception"
    - path: "src/bot_manager.py"
      provides: "_maybe_reconcile — the hourly, interval-guarded watchdog step"
      contains: "_maybe_reconcile"
  key_links:
    - from: "src/bot_manager.py::_maybe_reconcile"
      to: "src/reconciliation.py::reconcile"
      via: "the existing watchdog tick + a RECONCILE_INTERVAL_HOURS timestamp guard"
      pattern: "reconciliation.reconcile|from src import reconciliation"
    - from: "src/reconciliation.py::reconcile"
      to: "src/reconciliation.py::_client_for_bot"
      via: "one Alpaca client per bot — never shared, never bare keys"
      pattern: "_client_for_bot"
---

<objective>
Schedule the reconciliation that Phase 13 built and nobody ever ran — and defuse the landmine that
would otherwise make scheduling it a downgrade.

Purpose: `dashboard/api/migrations/017_reconciliation.sql`'s own header says *"Consumed by the dashboard
headline in Phase 19."* Phase 13 built `reconcile_bot` (pure, cent-exact), `reconcile_bot_live`
(persists + alerts on breach), and `reconcile()` (loops enabled bots, one client each). Its only entry
point is `scripts/reconcile.py` — a manual CLI. **No cron, no scheduler, no route.**

But `reconcile()` has **no per-bot try** (`:139-143`), and `_client_for_bot` **raises** on a keyless bot
(`:86-90`), and `_enabled_bot_ids` (`:51-59`) selects on `enabled = TRUE` with **no key predicate**. So
one misconfigured bot already reconciles **zero** bots today — and 19-03's fix makes keyless-enabled
bots *visible*, which makes this fire. Guard it, then schedule it.

Output: a per-bot-guarded `reconcile()`, and one hourly interval-guarded step inside the watchdog tick
that already exists.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-reliable-runtime/19-RESEARCH.md
@.planning/phases/19-reliable-runtime/19-01-SUMMARY.md
@.planning/phases/19-reliable-runtime/19-03-SUMMARY.md
@CLAUDE.md

<interfaces>
Existing, correct, and NOT to be rewritten (research: "already cent-exact and API-free — do not touch"):
  src/reconciliation.py:16-42    reconcile_bot(trade_log_pnl, equity, starting_equity, unrealized_pnl,
                                 tolerance) -> dict   # pure, 5 keys, inclusive tolerance boundary
  src/reconciliation.py:47-48    _tolerance() -> float          # env RECONCILIATION_TOLERANCE_USD, $25
  src/reconciliation.py:51-59    _enabled_bot_ids() -> list[str]  # enabled = TRUE, NO key predicate
  src/reconciliation.py:62-93    _client_for_bot(bot_id)        # ONE ACCOUNT PER BOT. RAISES ValueError
                                 at :86-90 when a bot has no keys. KEEP IT RAISING — the caller guards.
  src/reconciliation.py:96-131   reconcile_bot_live(bot_id, client, tolerance) -> dict
                                 # persists via db.record_reconciliation; alerts on breach
  src/db.py:334-356              record_reconciliation — UPSERT, one row per bot

The site being fixed (research N1), verbatim:
  src/reconciliation.py:139-143
      results: list[tuple[str, dict]] = []
      for bot_id in _enabled_bot_ids():
          client = _client_for_bot(bot_id)                      # <-- RAISES. No try. Kills the loop.
          results.append((bot_id, reconcile_bot_live(bot_id, client, tolerance)))
      return results

From 19-03 (already landed):
  src/bot_manager.py::_watchdog_loop  — each step in its OWN try/except (:88-95 is the shape)
  src/bot_manager.py::_check_trade_silence:149-151 — THE interval-guard pattern to copy

Contract fixed by the 19-01 RED tests (case 24):
  _RECONCILE_INTERVAL_HOURS = float(os.environ.get("RECONCILE_INTERVAL_HOURS", "1"))
  BotManager._maybe_reconcile(self) -> None
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: reconcile() gets a per-bot guard — one bad bot cannot starve the others (case 23)</name>
  <files>src/reconciliation.py</files>
  <read_first>src/reconciliation.py L51-59 (_enabled_bot_ids — `enabled = TRUE`, NO key predicate: a keyless bot IS in this list), L62-93 (_client_for_bot — the ValueError at :86-90 is CORRECT and stays; it is the guard that enforces one-account-per-bot), L96-131 (reconcile_bot_live — what a successful bot does), L134-143 (reconcile — the unguarded loop, the landmine); tests/test_reconciliation.py case 23 (the RED spec) and the file's existing zero-network double style</read_first>
  <behavior>
    - _enabled_bot_ids() -> ["A", "X"]; _client_for_bot("X") raises ValueError; _client_for_bot("A")
      returns a client. reconcile() then: reconciles and PERSISTS "A"; logs "X" as failed; returns
      results containing "A" and NOT "X"; and raises NOTHING.
    - reconcile_bot_live raising (an Alpaca timeout, say) for one bot is caught the same way.
    - The healthy bots' results are unchanged from today when every bot is healthy.
  </behavior>
  <action>Wrap the loop body of `reconcile()` (:139-143) in a per-bot `try/except Exception` — BOTH the `_client_for_bot` call AND the `reconcile_bot_live` call are inside it. On failure: `log.error("Reconciliation failed for bot %s: %s", bot_id, exc)` and `continue`. The healthy bots still reconcile and persist. `reconcile()` returns only the successful `(bot_id, result)` pairs and never propagates. Keep `_client_for_bot` RAISING — it is the one-account-per-bot enforcement point (CLAUDE.md hard rule, fence F6); the caller is what changes, not the guard. Do NOT add a key predicate to `_enabled_bot_ids` — a keyless enabled bot SHOULD be attempted and SHOULD be logged as failed; making it invisible here would recreate the exact B1 bug 19-03 just deleted. Do NOT touch `reconcile_bot` (pure, cent-exact, already tested). Do NOT introduce a shared Alpaca client. Smallest possible diff — this is a try/except, not a rewrite.</action>
  <verify>
    <automated>python -m pytest tests/test_reconciliation.py -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_reconciliation.py -q` — case **23 GREEN** (was RED: the ValueError propagated and reconciled ZERO bots), and every pre-existing reconciliation test still passes.
    - `sed -n '/^def reconcile(/,/^$/p' src/reconciliation.py | grep -c "except Exception"` returns **1** — the per-bot guard exists.
    - `grep -c "_client_for_bot" src/reconciliation.py` unchanged from main — the one-account-per-bot path is intact (fence F6).
    - `grep -n "alpaca_api_key IS NOT NULL" src/reconciliation.py` returns NOTHING — no key predicate was added to `_enabled_bot_ids`.
    - `git diff --stat src/reconciliation.py` shows a diff of **< 15 lines** (Karpathy: smallest diff).
  </acceptance_criteria>
  <done>One misconfigured bot is now a logged failure instead of total, silent reconciliation failure. The landmine is defused BEFORE the hourly schedule steps on it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: _maybe_reconcile — the hourly step inside the watchdog that already ticks (case 24)</name>
  <files>src/bot_manager.py</files>
  <read_first>src/bot_manager.py L17-20 (the module constants — the new interval joins them), L85-95 (_watchdog_loop after 19-03: revive -> bots-down -> silence -> heartbeat, each in its OWN try/except), L145-151 (_check_trade_silence's interval guard — `now = time.time(); if now - self._last_X < _INTERVAL: return; self._last_X = now` — copy this EXACTLY), L40-56 (__init__ — where the new `self._last_reconcile: float = 0.0` goes); tests/test_bot_manager.py case 24 (the RED spec)</read_first>
  <behavior>
    - Three calls inside one hour -> reconciliation.reconcile() is invoked ONCE.
    - reconcile() raising -> the exception is swallowed by the step's own try/except and the watchdog
      loop survives; later steps in the same tick still run.
    - The first tick after RECONCILE_INTERVAL_HOURS elapses invokes it again.
  </behavior>
  <action>Add `_RECONCILE_INTERVAL_HOURS = float(os.environ.get("RECONCILE_INTERVAL_HOURS", "1"))` beside the other module constants (:17-20), and `self._last_reconcile: float = 0.0` in `__init__`. Add `_maybe_reconcile(self)`: the interval guard copied verbatim in shape from `_check_trade_silence:149-151` (`now = time.time(); if now - self._last_reconcile < _RECONCILE_INTERVAL_HOURS * 3600: return; self._last_reconcile = now`), then `from src import reconciliation; reconciliation.reconcile()` (lazy import, matching how `_maybe_send_death_alert:130` lazily imports the notifier). Wire it into `_watchdog_loop` as the LAST step of the tick, in its OWN `try/except Exception` that logs a warning — exactly like the other steps at :88-95. Do NOT spawn a second thread. Do NOT add APScheduler/schedule/celery/cron — the supervisor already ticks every 60s (research: "Do not add a second thread or a scheduler"). Do NOT add a frontend poll loop (CLAUDE.md: reactive over polling; SSE is DEFERRED by CONTEXT and is not added this phase). Do NOT build an Alpaca client in bot_manager — reconcile() owns that via `_client_for_bot` (fence F6).</action>
  <verify>
    <automated>python -m pytest tests/test_bot_manager.py -q 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_bot_manager.py -q` — **ALL cases GREEN**, including case **24** (the last RED in the file).
    - `sed -n '/def _watchdog_loop/,/def _revive_dead_bots/p' src/bot_manager.py | grep -c "except Exception"` returns **>= 5** — revive, bots-down, silence, heartbeat, reconcile each have their own guard.
    - `grep -cE "^import schedule|APScheduler|celery|crontab|threading.Thread" src/bot_manager.py` returns **1** (the pre-existing watchdog Thread at :58-62) — **no second thread, no scheduler was added**.
    - `grep -cE "AlpacaClient|ALPACA_API_KEY" src/bot_manager.py` returns **0** (fence F6 — the manager never builds its own Alpaca client).
    - `python -m pytest tests/ dashboard/api/tests/ -q 2>&1 | tail -1` — >= 428 passed / 28 skipped, zero failures.
  </acceptance_criteria>
  <done>The reconciliation that Phase 13 built now actually runs — hourly, in the supervisor that already exists, guarded per bot, and unable to kill the watchdog if it throws.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| watchdog → live Alpaca | the hourly reconcile now calls each bot's Alpaca account from inside the dashboard container |
| watchdog → Postgres | the reconciliation UPSERT runs on a timer |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-19 | Denial of monitoring | ONE keyless bot silently zeroing ALL reconciliation (research N1) | mitigate | per-bot `try/except` inside `reconcile()`; case 23 asserts the healthy bot still persists while the bad one is logged and skipped |
| T-19-20 | Denial of monitoring | a raising reconcile killing the watchdog loop (and with it the ALL BOTS DOWN alert) | mitigate | its own `try/except` in `_watchdog_loop`, mirroring :88-95; case 24 asserts the loop survives |
| T-19-21 | Elevation of Privilege | a shared/bare-key Alpaca client crossing bot boundaries while scheduling | mitigate | reconcile keeps routing through `_client_for_bot` (:62-93); `bot_manager` is grep-asserted to contain no `AlpacaClient` / `ALPACA_API_KEY` (fence F6) |
| T-19-22 | DoS | hammering Alpaca every 60s tick | mitigate | interval guard: once per `RECONCILE_INTERVAL_HOURS` (default 1h), copied from `_check_trade_silence:149-151` |
| T-19-23 | Tampering | reconcile writing to trade data | accept | its only write is the `reconciliation` UPSERT (additive, non-trade table — CONTEXT decision 6). Read-only against `alpaca_trades` and against Alpaca |
| T-19-SC | Tampering | package installs | accept | none — no new packages |
</threat_model>

<verification>
- `python -m pytest tests/test_reconciliation.py tests/test_bot_manager.py -q` — all green, case 23 and case 24 both closed.
- `python -m pytest tests/ dashboard/api/tests/ -q | tail -1` — >= 428 passed / 28 skipped.
- `git diff --stat` touches ONLY src/reconciliation.py and src/bot_manager.py.
- `git diff | grep -iE "^\+.*(schedule|apscheduler|celery|cron|Thread\()"` — NOTHING. No new supervisor.
</verification>

<success_criteria>
`reconcile()` runs hourly from the supervisor that already exists, one Alpaca account per bot, per-bot
guarded, and cannot kill the watchdog. One misconfigured bot costs exactly one bot's reconciliation.
</success_criteria>

<output>
Create `.planning/phases/19-reliable-runtime/19-05-SUMMARY.md` when done.
</output>
</content>
