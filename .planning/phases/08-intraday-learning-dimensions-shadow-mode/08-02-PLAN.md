---
phase: 08-intraday-learning-dimensions-shadow-mode
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/trade_memory.py
  - src/learning_loop.py
  - src/bot_thread.py
  - src/alpaca_orchestrator.py
  - tests/test_learning_dimensions.py
autonomous: true
requirements: [LEARN-04, LEARN-05]
must_haves:
  truths:
    - "record_trade_context persists time_of_day_bucket + volatility_regime at entry across all 4 record sites"
    - "All 4 entry record dicts pass atr_value so volatility_regime can be computed"
    - "hold_minutes is computed at close from entry/exit ISO timestamps and passed via new optional kwarg"
    - "update_trade_outcome accepts hold_minutes=None by default (back-compat)"
    - "generate_lessons adds dimension-conditioned passes, skipping NULL/unknown rows, respecting min_sample"
  artifacts:
    - path: "src/trade_memory.py"
      provides: "Extended record_trade_context INSERT + update_trade_outcome kwarg + dimension lessons"
      contains: "hold_minutes"
    - path: "src/learning_loop.py"
      provides: "_sync_trade_outcomes computes hold_minutes and passes it"
      contains: "hold_minutes"
  key_links:
    - from: "src/bot_thread.py"
      to: "record_trade_context"
      via: "entry dict carries atr_value"
      pattern: "atr_value"
    - from: "src/learning_loop.py"
      to: "update_trade_outcome"
      via: "hold_minutes kwarg"
      pattern: "hold_minutes="
---

<objective>
Wire the three dimensions into the live recording path: persist time_of_day_bucket +
volatility_regime at entry (all 4 sites), fill hold_minutes at close, and make
generate_lessons/update_strategy_scores condition on the new dimensions additively.

Purpose: LEARN-04 (recording) + LEARN-05 (lessons). Uses the helpers + migration from 08-01.
Output: Extended INSERT/UPDATE, close-time hold_minutes, dimension lesson passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-intraday-learning-dimensions-shadow-mode/08-RESEARCH.md
@.planning/phases/08-intraday-learning-dimensions-shadow-mode/08-01-SUMMARY.md

<read_first>
- src/trade_memory.py lines 43-99 (record_trade_context INSERT), 195-213 (update_trade_outcome), 219-378 (generate_lessons), 610-714 (update_strategy_scores)
- src/learning_loop.py lines 90-118 (_sync_trade_outcomes SELECT + update loop)
- src/bot_thread.py long record dict ~613, short record dict ~799
- src/alpaca_orchestrator.py long record dict ~968 (thinner — no bull/bear args), short record dict ~1110
- src/technical_signals.py Signal.atr_value (~line 465)
</read_first>

<interfaces>
Consumes 08-01 helpers (already in src/trade_memory.py):
  time_of_day_bucket(entry_iso) -> str ; volatility_regime(atr, price) -> str

New/extended signatures this plan PRODUCES:
  update_trade_outcome(self, trade_id, outcome, pnl, hold_minutes: float | None = None)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist entry dimensions + thread atr_value through all 4 record sites</name>
  <files>src/trade_memory.py, src/bot_thread.py, src/alpaca_orchestrator.py, tests/test_learning_dimensions.py</files>
  <behavior>
    - Each of the 4 entry record dicts includes "atr_value": signal.atr_value (D-03).
    - record_trade_context INSERT adds time_of_day_bucket + volatility_regime columns/placeholders, computing them from timestamp and trade_data atr_value/price_at_entry.
    - Test via FakeTradeMemory.recorded (or a direct unit on a helper): a recorded entry carries the two dimension keys; orchestrator thin dict (no atr) yields volatility_regime computable as "unknown".
  </behavior>
  <action>Extend record_trade_context (src/trade_memory.py:66-94) INSERT: add columns time_of_day_bucket, volatility_regime with 2 more %s placeholders; values = time_of_day_bucket(timestamp) and volatility_regime(trade_data.get("atr_value", 0.0), trade_data.get("price_at_entry", 0.0)). Add `"atr_value": signal.atr_value` to ALL 4 record_trade_context dicts (bot_thread long ~613 + short ~799; alpaca_orchestrator long ~968 + short ~1110) per D-03 / Pitfall 2 — minimal-diff, just the one key each. Add a unit test asserting computed dimensions are persisted for a long+short path (drive the candidate loop with FakeTradeMemory and assert recorded payloads include atr_value; assert the helper output for the thin orchestrator dict is "unknown" when atr absent).</action>
  <verify>
    <automated>python -m pytest tests/test_learning_dimensions.py -k "record_dimensions or entry" -x -q</automated>
  </verify>
  <done>4 record dicts pass atr_value; INSERT persists time_of_day_bucket + volatility_regime; test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: hold_minutes at close (update_trade_outcome kwarg + _sync_trade_outcomes)</name>
  <files>src/trade_memory.py, src/learning_loop.py, tests/test_learning_dimensions.py</files>
  <behavior>
    - _hold_minutes("2026-06-08T14:00:00+00:00", "2026-06-08T15:30:00+00:00") == 90.0
    - _hold_minutes(None, x) is None; _hold_minutes("bad", "bad") is None
    - update_trade_outcome(..., hold_minutes=90.0) sets hold_minutes column; default kwarg None leaves it unset (back-compat — Pitfall 5)
  </behavior>
  <action>Add optional kwarg hold_minutes: float | None = None to update_trade_outcome (src/trade_memory.py:195); when not None, include hold_minutes in the UPDATE SET. In src/learning_loop.py:_sync_trade_outcomes extend the SELECT to also fetch at.timestamp AS entry_ts and at.closed_at AS exit_ts; add a module/method helper _hold_minutes(entry_ts, exit_ts) (parse ISO in Python per Pitfall 4 — TEXT timestamps, NOT SQL subtraction); pass hold_minutes=_hold_minutes(row["entry_ts"], row["exit_ts"]) into update_trade_outcome. Keep existing callers/tests working (kwarg optional). Add unit tests for _hold_minutes cases above.</action>
  <verify>
    <automated>python -m pytest tests/test_learning_dimensions.py -k hold_minutes -x -q</automated>
  </verify>
  <done>update_trade_outcome back-compat kwarg works; _sync_trade_outcomes computes + passes hold_minutes; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Dimension-conditioned lessons + scores (additive, NULL-skip, min_sample)</name>
  <files>src/trade_memory.py, tests/test_learning_dimensions.py</files>
  <behavior>
    - generate_lessons still emits existing (signal_type, symbol) and signal-only lessons unchanged.
    - A new pass groups closed rows by (signal_type, time_of_day_bucket) and (signal_type, volatility_regime); groups with the dimension NULL or "unknown" are skipped; group size < min_sample skipped (LEARN-05 / D-04).
    - Live advice key (symbol, signal_type) is UNCHANGED — dimensions inform lessons/scores ONLY (do not touch get_advice / runtime veto-scale key).
  </behavior>
  <action>Extend the generate_lessons SELECT (src/trade_memory.py:230-241) to also fetch time_of_day_bucket, volatility_regime, hold_minutes. After the existing per-symbol and signal-only passes, add additive grouping passes by (signal_type, time_of_day_bucket) and (signal_type, volatility_regime), reusing _analyze_pattern and the same min_sample gate; skip rows where the dimension is None/"unknown". Emit lesson rows with applies_to encoding the dimension (do NOT change trade_lessons schema). Optionally extend update_strategy_scores with a dimension GROUP BY pass (HAVING COUNT(*) >= 2) encoding the dimension into signal_type (e.g. "technical_confluence_4@us_pm") per RESEARCH A3 — additive, existing inserts unchanged. Do NOT alter get_advice. Add a test: seed FakeTradeMemory or a stubbed conn with closed rows carrying a dimension, assert a dimension-conditioned lesson is produced and NULL-dimension rows do not form a group.</action>
  <verify>
    <automated>python -m pytest tests/test_learning_dimensions.py -k "dimension_lesson or lesson" -x -q</automated>
  </verify>
  <done>Existing lessons unchanged; new dimension passes additive + NULL/min_sample-safe; advice key untouched; test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| code→Postgres | INSERT/UPDATE via psycopg %s placeholders (no string interpolation) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02 | Tampering | record_trade_context / update_trade_outcome SQL | mitigate | keep %s placeholders for new columns; no f-string SQL values |
| T-08-03 | Denial of Service | _sync_trade_outcomes per-row parse | accept | row-by-row loop already exists; ISO parse is O(1), bounded by open trades |
</threat_model>

<verification>
`python -m pytest tests/test_learning_dimensions.py -q` green; full suite `python -m pytest tests/ -q` green (230+).
Confirm get_advice / runtime veto-scale key (symbol, signal_type) unchanged (grep diff scope).
</verification>

<success_criteria>
Entry dimensions persisted at all 4 sites with atr_value threaded; hold_minutes filled at close
via back-compat kwarg; dimension lessons additive + NULL-tolerant; advice key unchanged; suite green.
</success_criteria>

<output>
Create `.planning/phases/08-intraday-learning-dimensions-shadow-mode/08-02-SUMMARY.md` when done
</output>
