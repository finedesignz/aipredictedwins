---
phase: 08-intraday-learning-dimensions-shadow-mode
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - dashboard/api/migrations/014_intraday_learning_dims.sql
  - src/db_schema.sql
  - src/trade_memory.py
  - tests/conftest.py
  - tests/test_learning_dimensions.py
  - tests/test_shadow_gate.py
autonomous: true
requirements: [LEARN-04, LEARN-06]
must_haves:
  truths:
    - "Migration 014 adds 3 nullable columns to trade_context and is idempotent (applies twice cleanly)"
    - "db_schema.sql trade_context block carries the same 3 ADD COLUMN IF NOT EXISTS"
    - "time_of_day_bucket(entry_iso) returns a UTC session label"
    - "volatility_regime(atr, price) returns low/med/high, and 'unknown' when atr<=0"
    - "FakeTradeMemory exposes count_closed_trades()"
  artifacts:
    - path: "dashboard/api/migrations/014_intraday_learning_dims.sql"
      provides: "Additive nullable migration for the 3 intraday dimensions"
      contains: "ADD COLUMN IF NOT EXISTS"
    - path: "src/trade_memory.py"
      provides: "Pure helpers time_of_day_bucket, volatility_regime"
      contains: "def volatility_regime"
    - path: "tests/test_learning_dimensions.py"
      provides: "Unit tests for pure dimension helpers"
    - path: "tests/test_shadow_gate.py"
      provides: "Test scaffold for the shadow gate"
  key_links:
    - from: "src/db_schema.sql"
      to: "trade_context"
      via: "ADD COLUMN IF NOT EXISTS mirror"
      pattern: "ADD COLUMN IF NOT EXISTS (time_of_day_bucket|hold_minutes|volatility_regime)"
---

<objective>
Lay the schema + pure-function foundation for Phase 8: the additive migration (014), its
mirror in the runtime bootstrap DDL, the two pure entry-dimension helpers, and the test
scaffold (including FakeTradeMemory.count_closed_trades) that downstream plans assert against.

Purpose: All other Phase 8 work consumes these contracts (helpers, migration, fake method).
Define them first so executors of 08-02 / 08-03 build against fixed signatures.
Output: Migration 014, db_schema mirror, two pure helpers, conftest method, two test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-intraday-learning-dimensions-shadow-mode/08-RESEARCH.md
@.planning/phases/08-intraday-learning-dimensions-shadow-mode/08-CONTEXT.md

<read_first>
- dashboard/api/migrations/006_learning_tables.sql — migration convention (idempotent, ADD COLUMN IF NOT EXISTS)
- dashboard/api/migrations/run_migrations.py — multi-statement exec via conn.pgconn.exec_, _migrations ledger
- src/db_schema.sql lines ~161-167 — STALE minimal trade_context block to mirror columns into
- src/trade_memory.py lines 30-99 — TradeMemory.__init__, record_trade_context (helper call site context)
- tests/conftest.py lines 70-120 — FakeTradeMemory (add count_closed_trades here)
</read_first>

<interfaces>
Contracts this plan DEFINES (downstream 08-02/08-03 use these exactly):

```python
# src/trade_memory.py — module-level pure helpers (no DB)
def time_of_day_bucket(entry_iso: str | None) -> str: ...
    # UTC session labels: asia (00-07), eu (07-13), us_am (13-17), us_pm (17-21), off (21-24).
    # Returns "unknown" if entry_iso is None/unparseable.

def volatility_regime(atr: float, price: float) -> str: ...
    # r = atr/price if price>0 else 0; low < 0.01, med 0.01-0.025, high >= 0.025.
    # Returns "unknown" if atr <= 0.

# tests/conftest.py — FakeTradeMemory gains:
def count_closed_trades(self) -> int:  # returns self._closed_count (default 0; settable in __init__)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure dimension helpers + their tests (RED first)</name>
  <files>tests/test_learning_dimensions.py, src/trade_memory.py</files>
  <behavior>
    - time_of_day_bucket("2026-06-08T14:00:00+00:00") == "us_am"; 02:00→"asia"; 09:00→"eu"; 19:00→"us_pm"; 23:00→"off"
    - time_of_day_bucket(None) == "unknown"; time_of_day_bucket("garbage") == "unknown"
    - volatility_regime(0.5, 100.0) == "low" (0.5%); volatility_regime(1.5, 100.0) == "med"; volatility_regime(3.0, 100.0) == "high"
    - volatility_regime(0.0, 100.0) == "unknown"; volatility_regime(1.0, 0.0) == "unknown" (atr>0 but price 0 → r=0 → but atr>0 so threshold applies; price<=0 → "unknown")
  </behavior>
  <action>RED: write tests/test_learning_dimensions.py covering the behavior cases above (import from src.trade_memory). Run, confirm failure. GREEN: add module-level pure functions time_of_day_bucket and volatility_regime to src/trade_memory.py per the <interfaces> contract (D-02 thresholds: low<1% / med / high≥2.5% ATR-as-%-of-price; "unknown" if atr<=0; session labels for time bucket). Use datetime.fromisoformat with try/except ValueError→"unknown". Keep both functions side-effect-free (no DB). Minimal diff — do not touch record_trade_context yet.</action>
  <verify>
    <automated>python -m pytest tests/test_learning_dimensions.py -k "time_of_day or volatility_regime" -x -q</automated>
  </verify>
  <done>Both helpers exist as pure functions; all listed behavior cases pass.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 014 + db_schema.sql mirror + idempotency test</name>
  <files>dashboard/api/migrations/014_intraday_learning_dims.sql, src/db_schema.sql, tests/test_learning_dimensions.py</files>
  <action>Per LEARN-04 / D-01: create dashboard/api/migrations/014_intraday_learning_dims.sql with three `ALTER TABLE trade_context ADD COLUMN IF NOT EXISTS` statements — time_of_day_bucket TEXT, hold_minutes DOUBLE PRECISION, volatility_regime TEXT (all nullable, NO NOT NULL, NO DEFAULT, NO backfill) plus the two `CREATE INDEX IF NOT EXISTS` from RESEARCH Pattern 1. Mirror the SAME three ADD COLUMN IF NOT EXISTS into src/db_schema.sql's trade_context block (after the CREATE TABLE, additive — keep both DDL paths in sync per Pitfall 1; respects global rule 6, additive only). Add a test asserting the migration file text contains all three column names and uses ADD COLUMN IF NOT EXISTS (string-level idempotency assertion — no DB required); if DATABASE_URL is set, optionally apply run_migrations twice and assert no error (skip otherwise).</action>
  <verify>
    <automated>python -m pytest tests/test_learning_dimensions.py -k migration -x -q</automated>
  </verify>
  <done>Migration 014 exists (3 additive nullable cols + 2 indexes), db_schema.sql mirrors the 3 cols, migration test green.</done>
</task>

<task type="auto">
  <name>Task 3: FakeTradeMemory.count_closed_trades + shadow-gate test scaffold</name>
  <files>tests/conftest.py, tests/test_shadow_gate.py</files>
  <action>Per LEARN-06 test prep: add count_closed_trades(self) -> int to FakeTradeMemory (returns a self._closed_count set in __init__, default 0, accept closed_count= kwarg). Create tests/test_shadow_gate.py with a minimal scaffold that imports FakeTradeMemory and asserts count_closed_trades() returns the injected value. The full should_enforce_learning tests are added in 08-03 (this is the Wave-0 scaffold so the gate is testable without DB). Do NOT implement should_enforce_learning here.</action>
  <verify>
    <automated>python -m pytest tests/test_shadow_gate.py -x -q</automated>
  </verify>
  <done>FakeTradeMemory.count_closed_trades exists and is injectable; scaffold test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| code→Postgres | DDL/DML via psycopg parameterized queries; migration is additive DDL only |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | migration 014 trade_context | mitigate | additive-only (ADD COLUMN IF NOT EXISTS), no NOT NULL/backfill/drop — respects global rule 6 |
| T-08-SC | Tampering | package installs | accept | no new packages this phase (RESEARCH: N/A) |
</threat_model>

<verification>
`python -m pytest tests/test_learning_dimensions.py tests/test_shadow_gate.py -q` green.
Full suite still green: `python -m pytest tests/ -q` (230+ baseline).
</verification>

<success_criteria>
Migration 014 + db_schema mirror present (additive, idempotent), both pure helpers correct,
FakeTradeMemory.count_closed_trades injectable, new test files green, full suite green.
</success_criteria>

<output>
Create `.planning/phases/08-intraday-learning-dimensions-shadow-mode/08-01-SUMMARY.md` when done
</output>
