---
phase: 08-intraday-learning-dimensions-shadow-mode
plan: 03
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified:
  - src/trade_memory.py
  - src/bot_thread.py
  - src/alpaca_orchestrator.py
  - tests/test_shadow_gate.py
autonomous: true
requirements: [LEARN-06]
must_haves:
  truths:
    - "should_enforce_learning(memory, bot_id) returns False (shadow) when closed count < LEARNING_SHADOW_UNTIL_TRADES (default 30)"
    - "should_enforce_learning returns True (enforce) when closed count >= threshold"
    - "Explicit LEARNING_ENFORCE=0 forces shadow regardless of count (precedence)"
    - "count_closed_trades counts trade_context rows with outcome IN ('win','loss') for the bot"
    - "All 8 static LEARNING_ENFORCE seam checks are replaced by one per-cycle enforce value"
    - "In shadow mode the seams log 'learn_shadow: WOULD veto/scale' and take NO action"
  artifacts:
    - path: "src/trade_memory.py"
      provides: "count_closed_trades + should_enforce_learning helper"
      contains: "def should_enforce_learning"
  key_links:
    - from: "src/bot_thread.py"
      to: "should_enforce_learning"
      via: "per-cycle enforce local"
      pattern: "should_enforce_learning\\("
    - from: "src/alpaca_orchestrator.py"
      to: "should_enforce_learning"
      via: "per-cycle enforce local"
      pattern: "should_enforce_learning\\("
---

<objective>
Replace the static Phase-7 LEARNING_ENFORCE flag with the count-based shadow gate: a single
should_enforce_learning(memory, bot_id) helper computed once per cycle, swapped into all 8
veto/scale seams across both runtimes, with explicit LEARNING_ENFORCE=0 forcing shadow.

Purpose: LEARN-06. Uses count_closed_trades scaffold from 08-01.
Output: count_closed_trades + should_enforce_learning helpers; 8 seams converted; shadow logging.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-intraday-learning-dimensions-shadow-mode/08-RESEARCH.md
@.planning/phases/08-intraday-learning-dimensions-shadow-mode/08-01-SUMMARY.md
@.planning/phases/08-intraday-learning-dimensions-shadow-mode/08-02-SUMMARY.md

<read_first>
- src/trade_memory.py (add count_closed_trades + should_enforce_learning near the helpers)
- src/bot_thread.py: long seams at lines 537, 539, 554; short seams at 730, 732, 747; module-level LEARNING_ENFORCE definition (grep `LEARNING_ENFORCE`)
- src/alpaca_orchestrator.py: long seams at 886, 888, 901; short seams at 1033, 1035, 1048
- tests/conftest.py FakeTradeMemory.count_closed_trades (added in 08-01)
- tests/test_shadow_gate.py scaffold (08-01)
</read_first>

<interfaces>
Produces (per RESEARCH Pattern 4):

```python
# src/trade_memory.py — method on TradeMemory
def count_closed_trades(self) -> int:
    # SELECT COUNT(*) FROM trade_context WHERE bot_id=%s AND outcome IN ('win','loss')

# src/trade_memory.py — module-level helper
def should_enforce_learning(memory, bot_id: str, shadow_until: int | None = None) -> bool:
    # if os.environ.get("LEARNING_ENFORCE") == "0": return False   # D-06 precedence
    # until = shadow_until or int(os.environ.get("LEARNING_SHADOW_UNTIL_TRADES", "30"))
    # return memory.count_closed_trades() >= until
    # memory is None -> return False (no-op, back-compat)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: count_closed_trades + should_enforce_learning helper (RED first)</name>
  <files>src/trade_memory.py, tests/test_shadow_gate.py</files>
  <behavior>
    - count_closed_trades runs COUNT(*) outcome IN ('win','loss') for self.bot_id.
    - should_enforce_learning(mem(closed=29)) is False (shadow); closed=30 is True (enforce).
    - LEARNING_ENFORCE="0" env → False regardless of count (monkeypatch); precedence over count.
    - LEARNING_SHADOW_UNTIL_TRADES env overrides default 30; explicit shadow_until arg overrides env.
    - should_enforce_learning(None, bot_id) returns False (memory=None no-op).
  </behavior>
  <action>Add count_closed_trades(self) -> int to TradeMemory per <interfaces> (parameterized %s, returns row["n"]). Add module-level should_enforce_learning(memory, bot_id, shadow_until=None) per <interfaces> — explicit LEARNING_ENFORCE=="0" wins (D-06), else count-based against LEARNING_SHADOW_UNTIL_TRADES (default 30); memory is None → False. Expand tests/test_shadow_gate.py to cover all behavior cases using FakeTradeMemory(closed_count=...) and monkeypatch.setenv.</action>
  <verify>
    <automated>python -m pytest tests/test_shadow_gate.py -k "shadow_gate or enforce_override or enforce" -x -q</automated>
  </verify>
  <done>Both helpers exist; shadow-below / enforce-above / explicit-0-override / None-noop all green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace 8 static LEARNING_ENFORCE seams with one per-cycle gate</name>
  <files>src/bot_thread.py, src/alpaca_orchestrator.py, tests/test_shadow_gate.py</files>
  <behavior>
    - Each candidate loop computes `enforce = should_enforce_learning(memory, bot_id)` ONCE per cycle (not per candidate — Pitfall: avoid N count queries).
    - All 8 `LEARNING_ENFORCE` references (bot_thread long 537/539/554 + short 730/732/747; orchestrator long 886/888/901 + short 1033/1035/1048) read the local `enforce`.
    - When not enforce: veto seam logs `learn_shadow: WOULD veto` and does NOT `continue`; scale seam logs `learn_shadow: WOULD scale ×N` and does NOT apply the adjustment/threshold clamp.
    - When enforce: behavior identical to Phase 7 (veto continues; scale applies).
  </behavior>
  <action>In both src/bot_thread.py and src/alpaca_orchestrator.py: compute `enforce = should_enforce_learning(memory, bot_id)` once at the top of each candidate-evaluation cycle (before the long/short loops). Replace each of the 8 `LEARNING_ENFORCE` checks with `enforce`. At the veto seam: keep the existing learn_veto info log but when `not enforce` emit a `learn_shadow: WOULD veto %s` log and fall through (no `continue`). At the scale seam: when `not enforce` emit `learn_shadow: WOULD scale ×%.2f` and leave adj/eff_max/eff_min at their default (non-learning) values. Import should_enforce_learning from src.trade_memory. Minimal diff — do not change the runtime advice key or sizing math beyond gating. Remove the now-unused module-level static LEARNING_ENFORCE boolean only if nothing else references it (grep first; the env is still read inside the helper). Add a test driving a candidate loop with FakeTradeMemory(closed_count below threshold) asserting no veto/scale applied + shadow path reached (inject enforce via the helper or monkeypatch).</action>
  <verify>
    <automated>python -m pytest tests/test_shadow_gate.py tests/test_learning_wiring.py -x -q</automated>
  </verify>
  <done>All 8 seams use the single per-cycle gate; shadow logs WOULD and no-ops; enforce path matches Phase 7; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| env→runtime | LEARNING_ENFORCE / LEARNING_SHADOW_UNTIL_TRADES read from environment |
| code→Postgres | count_closed_trades via parameterized %s |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Elevation of Privilege | shadow gate auto-enforce at 30 closed trades | mitigate | explicit LEARNING_ENFORCE=0 always forces shadow (manual override precedence per D-06) |
| T-08-05 | Denial of Service | per-candidate count query | mitigate | compute enforce ONCE per cycle, not per candidate |
</threat_model>

<verification>
`python -m pytest tests/test_shadow_gate.py tests/test_learning_wiring.py -q` green.
Full suite `python -m pytest tests/ -q` green (230+). Grep confirms no stray `LEARNING_ENFORCE` static-bool seam remains.
</verification>

<success_criteria>
Single count-based gate is the sole source of truth at all 8 seams; explicit-0 forces shadow;
shadow mode logs WOULD and takes no action; enforce mode matches Phase 7; full suite green.
</success_criteria>

<output>
Create `.planning/phases/08-intraday-learning-dimensions-shadow-mode/08-03-SUMMARY.md` when done
</output>
