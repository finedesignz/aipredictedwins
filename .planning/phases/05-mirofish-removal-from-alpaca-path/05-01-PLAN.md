---
phase: 05-mirofish-removal-from-alpaca-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/alpaca_orchestrator.py
  - src/bot_thread.py
autonomous: true
requirements: [EXIT-01, EXIT-04]

must_haves:
  truths:
    - "The Alpaca trading path makes zero LLM/Claude calls at startup, daily, or during position monitoring."
    - "PositionMonitor constructs without an ExitAdvisor and exits remain deterministic (TrailingStop + hard/soft pct constants)."
    - "Both orchestrator main() and BotThread instantiate PositionMonitor without an exit_advisor arg."
    - "exit_advisor.py, risk_gate.py, mirofish_client.py, claude_llm.py still exist on disk."
    - "Full test suite stays green and both modules still import."
  artifacts:
    - path: "src/alpaca_orchestrator.py"
      provides: "Alpaca orchestrator with ExitAdvisor + ClaudeLLM auth blocks removed"
      contains: "from src.exit_advisor import TrailingStop"
    - path: "src/bot_thread.py"
      provides: "BotThread PositionMonitor instantiation without exit_advisor"
  key_links:
    - from: "src/alpaca_orchestrator.py"
      to: "src/exit_advisor.TrailingStop"
      via: "kept import (deterministic exits)"
      pattern: "TrailingStop"
    - from: "src/alpaca_orchestrator.py PositionMonitor.run"
      to: "trailing stop + HARD/SOFT pct constants"
      via: "deterministic threshold checks (no should_exit)"
      pattern: "self\\._trailing"
---

<objective>
Strip every MiroFish/LLM dependency out of the live Alpaca trading path. After Phase 4
shipped deterministic ATR exits, the `ExitAdvisor` and the Claude-CLI auth plumbing are dead
weight on the Alpaca side. Remove them with a minimal, removal-only diff.

Purpose: The Alpaca path becomes fully deterministic — zero LLM calls — closing EXIT-01 and
EXIT-04. Kalshi files stay on disk (paused, not deleted).
Output: `src/alpaca_orchestrator.py` and `src/bot_thread.py` with `ExitAdvisor` + Claude-auth
blocks removed; all kept deterministic pieces (`TrailingStop`, `RulesGate`, pct constants)
untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mirofish-removal-from-alpaca-path/05-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Verified removal surface — executor uses these exact line targets; no exploration needed. -->

KEEP these imports (still used by deterministic exits / banner / order params):
- `src/exit_advisor.py` exports STILL USED in path: `TrailingStop`, `HARD_STOP_PCT`,
  `SOFT_STOP_PCT`, `SOFT_TAKE_PROFIT_PCT`.
  - alpaca_orchestrator: HARD_STOP_PCT used at lines ~337 (banner), ~917, ~1018 (order stop_loss).
  - bot_thread: SOFT_TAKE_PROFIT_PCT/HARD_STOP_PCT/SOFT_STOP_PCT used at lines ~570-571, ~711-712.

REMOVE from path (verified unused after removal):
- `ExitAdvisor` — imported + `exit_advisor = ExitAdvisor()` + ctor param + `self.exit_advisor`.
- `check_position_thresholds` — imported in alpaca_orchestrator line 34 ONLY, never called → drop from import.
- `ClaudeLLM` — local import + startup verify block + daily auth re-check (alpaca_orchestrator only).

bot_thread.py has NO Claude-auth mirror (grep confirms) — EXIT-04 touches alpaca_orchestrator.py only.

alpaca_orchestrator.py removal targets:
- L34 import: `from src.exit_advisor import ExitAdvisor, TrailingStop, check_position_thresholds, HARD_STOP_PCT, SOFT_STOP_PCT, SOFT_TAKE_PROFIT_PCT`
  → `from src.exit_advisor import TrailingStop, HARD_STOP_PCT, SOFT_STOP_PCT, SOFT_TAKE_PROFIT_PCT`
- L109 ctor: `def __init__(self, alpaca, logger, exit_advisor: ExitAdvisor, profile=SWING):` → drop `exit_advisor: ExitAdvisor,`
- L113: `self.exit_advisor = exit_advisor ...` → delete line
- L323 banner risk_mode string: drop "+ MiroFish exit advisor"
- L325 banner title: "Alpaca Technical + MiroFish Guardian Bot" → drop MiroFish/Guardian wording
- L568: `exit_advisor = ExitAdvisor()` → delete
- L599-611: entire "Verify Claude CLI auth" block (import ClaudeLLM, _claude_check, is_available, call, send_alert lines) → delete
- L614: `monitor = PositionMonitor(alpaca, logger, exit_advisor, PROFILE)` → `PositionMonitor(alpaca, logger, PROFILE)`
- L616: "(MiroFish exit advisor active)" → drop or "(deterministic exits)"
- L633: `_last_auth_check = daily_start ...` → delete
- L645-653: "Daily Claude auth health check" block → delete (keep daily_pnl reset at L654-655)
- L1103, L1182: "MiroFish Guardian" report/description strings → tidy wording (Claude's discretion, minimal)

bot_thread.py removal targets:
- L60 import: `from src.exit_advisor import ExitAdvisor, HARD_STOP_PCT, SOFT_STOP_PCT, SOFT_TAKE_PROFIT_PCT`
  → `from src.exit_advisor import HARD_STOP_PCT, SOFT_STOP_PCT, SOFT_TAKE_PROFIT_PCT`
- L196: `exit_advisor = ExitAdvisor()` → delete
- L227: `monitor = PositionMonitor(alpaca, logger, exit_advisor, _monitor_profile)` → `PositionMonitor(alpaca, logger, _monitor_profile)`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove ExitAdvisor + Claude-auth from alpaca_orchestrator.py (EXIT-01, EXIT-04)</name>
  <files>src/alpaca_orchestrator.py</files>
  <action>
    Per D-01/D-02/D-03 (EXIT-01) and D-04 (EXIT-04), apply the removal targets listed in the
    `<interfaces>` block for alpaca_orchestrator.py. Specifically:
    (1) Trim the L34 exit_advisor import to keep ONLY `TrailingStop, HARD_STOP_PCT, SOFT_STOP_PCT,
    SOFT_TAKE_PROFIT_PCT` — drop `ExitAdvisor` and `check_position_thresholds` (verified: those
    three pct constants are still used at banner L337 and order stop_loss L917/L1018; the kept
    `TrailingStop` powers deterministic exits; `check_position_thresholds` has no call site).
    (2) Drop the `exit_advisor: ExitAdvisor` param from `PositionMonitor.__init__` (L109) and
    delete the `self.exit_advisor = exit_advisor` assignment (L113). Leave `self._trailing =
    TrailingStop()` (L121) intact.
    (3) Delete `exit_advisor = ExitAdvisor()` (L568) and update the monitor instantiation (L614)
    to `PositionMonitor(alpaca, logger, PROFILE)`.
    (4) Delete the startup Claude-CLI verify block (L599-611, including the local `from
    src.claude_llm import ClaudeLLM`, `_claude_check`, `is_available()`, `call("Reply with OK")`,
    and both `send_alert` lines) and the daily auth health-check block (L633 `_last_auth_check`
    init + L645-653) — keep the `daily_pnl = 0.0` / `daily_start = today` reset at L654-655.
    (5) Tidy banner/report wording (L323, L325, L616, L1103, L1182) to drop "MiroFish"/"Guardian"
    /"exit advisor" references — Claude's discretion, minimal diff, no other refactor.
    Keep `RulesGate` and the deterministic risk-gate path (D-05) untouched. Do NOT delete any file.
  </action>
  <verify>
    <automated>cd C:\Users\artic\GitHub\aipredictedwins; python -c "import ast,sys; src=open('src/alpaca_orchestrator.py').read(); body=[l for l in src.splitlines() if not l.lstrip().startswith('#')]; j=chr(10).join(body); assert 'ExitAdvisor' not in j, 'ExitAdvisor still referenced'; assert 'ClaudeLLM' not in j, 'ClaudeLLM still referenced'; assert 'should_exit' not in j, 'should_exit still referenced'; assert 'TrailingStop' in j, 'TrailingStop import lost'; assert 'HARD_STOP_PCT' in j, 'HARD_STOP_PCT lost'; ast.parse(src); print('OK')"</automated>
  </verify>
  <done>No ExitAdvisor/ClaudeLLM/should_exit in non-comment code; TrailingStop + pct constants retained; file parses.</done>
</task>

<task type="auto">
  <name>Task 2: Remove ExitAdvisor from bot_thread.py (EXIT-01)</name>
  <files>src/bot_thread.py</files>
  <action>
    Per D-02 (EXIT-01), apply the bot_thread.py removal targets from `<interfaces>`:
    (1) Trim the L60 import to `from src.exit_advisor import HARD_STOP_PCT, SOFT_STOP_PCT,
    SOFT_TAKE_PROFIT_PCT` — drop `ExitAdvisor` (the three pct constants are still used at
    L570-571 and L711-712 for order target/stop params).
    (2) Delete `exit_advisor = ExitAdvisor()` (L196).
    (3) Update the monitor instantiation (L227) to `PositionMonitor(alpaca, logger,
    _monitor_profile)`.
    bot_thread.py has NO Claude-auth block (grep-confirmed), so EXIT-04 does not touch this file.
    Minimal diff, no other refactor.
  </action>
  <verify>
    <automated>cd C:\Users\artic\GitHub\aipredictedwins; python -c "import ast; src=open('src/bot_thread.py').read(); j=chr(10).join(l for l in src.splitlines() if not l.lstrip().startswith('#')); assert 'ExitAdvisor' not in j, 'ExitAdvisor still referenced'; assert 'HARD_STOP_PCT' in j, 'pct constant lost'; ast.parse(src); print('OK')"</automated>
  </verify>
  <done>No ExitAdvisor reference in bot_thread.py code; pct constants retained; PositionMonitor called without exit_advisor; file parses.</done>
</task>

<task type="auto">
  <name>Task 3: Verify removal — imports, tests green, MiroFish files intact</name>
  <files>src/alpaca_orchestrator.py, src/bot_thread.py</files>
  <action>
    Final goal-backward verification (D-03 clean-removal claim, no behavior change). Confirm:
    (1) Both modules import cleanly (smoke). (2) The three MiroFish files remain on disk
    (exit_advisor.py, risk_gate.py, mirofish_client.py — and claude_llm.py). (3) The full test
    suite stays green (208+ tests). If any test regressed, the removal touched a still-live
    symbol — re-check the kept-import list before changing test expectations. Do NOT edit tests
    to paper over a regression.
  </action>
  <verify>
    <automated>cd C:\Users\artic\GitHub\aipredictedwins; python -c "import src.alpaca_orchestrator, src.bot_thread; import os; assert all(os.path.exists(f) for f in ['src/exit_advisor.py','src/risk_gate.py','src/mirofish_client.py','src/claude_llm.py']), 'a MiroFish file was deleted'; print('imports+files OK')"; python -m pytest tests/ -q</automated>
  </verify>
  <done>Both modules import; all four MiroFish/Claude files exist; pytest passes (208+).</done>
</task>

</tasks>

<verification>
- `grep` finds no `ExitAdvisor`, `ClaudeLLM`, or `should_exit` in the non-comment code of
  alpaca_orchestrator.py or bot_thread.py.
- `TrailingStop` import + `HARD_STOP_PCT`/`SOFT_STOP_PCT`/`SOFT_TAKE_PROFIT_PCT` retained in both.
- `python -m pytest tests/ -q` green (208+).
- exit_advisor.py / risk_gate.py / mirofish_client.py / claude_llm.py still present.
</verification>

<success_criteria>
EXIT-01 and EXIT-04 closed: the Alpaca trading path has zero LLM calls (no startup verify, no
daily auth recheck, no ExitAdvisor), deterministic exits intact, RulesGate untouched, Kalshi
files retained on disk, full suite green.
</success_criteria>

<output>
Create `.planning/phases/05-mirofish-removal-from-alpaca-path/05-01-SUMMARY.md` when done
</output>
