---
phase: 10-verification-backtest
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/daytrade_5min/BTC_USD.json
  - tests/fixtures/daytrade_5min/ETH_USD.json
  - tests/fixtures/daytrade_5min/SOL_USD.json
  - scripts/backtest_signal_frequency.py
  - tests/test_signal_frequency.py
autonomous: true
requirements: [VERIFY-02]
must_haves:
  truths:
    - "A committed 5Min fixture of >=100 bars per symbol exists for the daytrade universe subset"
    - "scripts/backtest_signal_frequency.py replays the fixture through scan_assets(profile=DAYTRADE, fetch_4h=False) and prints a per-symbol + total candidate-frequency report"
    - "The harness runs deterministically with no network by default; live-fetch is behind an explicit --live flag"
    - "tests/test_signal_frequency.py asserts a sane candidate-frequency range on the fixture (>0, not absurd) as a regression guard"
  artifacts:
    - path: "scripts/backtest_signal_frequency.py"
      provides: "Signal-frequency backtest CLI (fixture replay + optional --live)"
      min_lines: 60
    - path: "tests/test_signal_frequency.py"
      provides: "Regression guard asserting sane frequency range on the committed fixture"
      min_lines: 20
    - path: "tests/fixtures/daytrade_5min/BTC_USD.json"
      provides: ">=100 5Min OHLCV bars"
      min_lines: 1
  key_links:
    - from: "scripts/backtest_signal_frequency.py"
      to: "src.technical_signals.scan_assets"
      via: "replay client honoring profile.timeframe=5Min / bar_count=100"
      pattern: "scan_assets"
    - from: "tests/test_signal_frequency.py"
      to: "scripts/backtest_signal_frequency.py"
      via: "import the pure frequency function, run on committed fixture"
      pattern: "import"
    - from: "scripts/backtest_signal_frequency.py"
      to: "src.backtester.data_loader"
      via: "reuse normalise_bar / fixture loading"
      pattern: "data_loader"
---

<objective>
Deliver the VERIFY-02 signal-FREQUENCY backtest harness (not P&L).

Purpose: Before any live daytrade paper run, validate that the DAYTRADE profile produces a sane
number of candidates on historical 5-min bars — guards against a regression that silences scanning
(0 candidates) or floods it (every bar a candidate). The existing src/backtester/ is a P&L engine;
this is a thin frequency harness reusing the REAL `scan_assets(profile=DAYTRADE, fetch_4h=False)`.

Output: a committed ≥100×5Min fixture, `scripts/backtest_signal_frequency.py` (fixture default +
`--live`), and `tests/test_signal_frequency.py` asserting a sane frequency range.

NOTE: The existing tests/backtester/fixtures/BTC_USD.json is 60×1Hour — WRONG timeframe and too
short (DAYTRADE.bar_count=100, analyze early-returns <30 bars). A NEW 5Min fixture is required.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-verification-backtest/10-RESEARCH.md
@.planning/phases/10-verification-backtest/10-CONTEXT.md

@src/technical_signals.py
@src/strategy_profile.py
@src/backtester/data_loader.py
@src/alpaca_client.py

<interfaces>
<!-- CRITICAL: scan_assets IGNORES its own timeframe/bar_count args. -->

src/technical_signals.py — scan_assets (line 469, sourcing at line 502):
```
bars = alpaca_client.get_bars(symbol, timeframe=profile.timeframe, limit=profile.bar_count)
```
So for DAYTRADE the replay client MUST respond to get_bars(symbol, timeframe="5Min", limit=100).
`analyze()` early-returns None if len(bars) < 30; `_adx` needs period*2+1 = 29 bars.

src/strategy_profile.py — DAYTRADE: timeframe="5Min", bar_count=100, min_confluence=4,
min_short_confluence=3. Import the real DAYTRADE preset (do not hardcode constants).

src/alpaca_client.py — get_bars returns list[dict] with keys
timestamp/open/high/low/close/volume/vwap (vwap defaults 0 if absent).

src/backtester/data_loader.py — reuse load_bars / normalise_bar for fixture loading + vwap defaults
+ slash-symbol filenames. Do NOT re-derive a loader (Don't Hand-Roll).

Replay client shape (from RESEARCH):
```
class _ReplayClient:
    def __init__(self, bars_by_symbol, window_end_idx): ...
    def get_bars(self, symbol, timeframe="5Min", limit=100):
        return self._bars[symbol][self._end - limit:self._end]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit a deterministic >=100 5Min fixture</name>
  <files>tests/fixtures/daytrade_5min/BTC_USD.json, tests/fixtures/daytrade_5min/ETH_USD.json, tests/fixtures/daytrade_5min/SOL_USD.json</files>
  <action>
    Create a committed 5Min fixture for the DAYTRADE universe subset (BTC/USD, ETH/USD, SOL/USD —
    enough for the regression test per D-04; full 8 only in --live). Each file: a JSON list of
    >=100 (target 200-300 to allow a rolling window) OHLCV bars with keys timestamp (5-min spaced
    ISO), open/high/low/close/volume/vwap. The data must feed the real engine: >=30 bars so analyze
    runs, >=29 so _adx runs. Either synthesize deterministic OHLCV (preferred for a pinnable exact
    candidate count) or capture a real slice via --live (Task 2) then commit. The series should
    produce a KNOWN, nonzero, non-absurd DAYTRADE candidate count when replayed. Match the
    normalise_bar shape in src/backtester/data_loader.py so loading reuses it.
  </action>
  <verify>
    <automated>python -c "import json,glob; fs=glob.glob('tests/fixtures/daytrade_5min/*.json'); assert len(fs)>=3, fs; [exit('short') for f in fs if len(json.load(open(f)))<100]; print('ok', len(fs))"</automated>
  </verify>
  <done>3 fixture files exist, each with >=100 5Min bars carrying OHLCV+timestamp+vwap keys.</done>
</task>

<task type="auto">
  <name>Task 2: Build the frequency backtest harness CLI</name>
  <files>scripts/backtest_signal_frequency.py</files>
  <action>
    Create scripts/backtest_signal_frequency.py satisfying D-03/D-04/D-05. Expose a PURE function
    (e.g. run_frequency(bars_by_symbol, symbols, window, profile=DAYTRADE) -> report dict with
    per-symbol candidate counts + totals + window count) so the test can import it. Implement a
    _ReplayClient that wraps the loaded bar lists and serves get_bars(symbol, timeframe, limit) by
    slicing a rolling window (it MUST ignore the passed timeframe/limit semantics enough to return
    the profile-sized slice, since scan_assets passes profile.timeframe="5Min"/profile.bar_count=100
    — Pitfall 1). For each rolling window (slide end index across the fixture), build a _ReplayClient
    and call scan_assets(client, symbols, fetch_4h=False, profile=DAYTRADE); count returned signals
    where confluence_score >= DAYTRADE.min_confluence (long) and short_score >=
    DAYTRADE.min_short_confluence (short). Aggregate per-symbol + totals.

    CLI args: --fixture-dir (default tests/fixtures/daytrade_5min), --window, --symbols, --live.
    Default = fixture replay (no network, no clock — D-04). --live uses
    src.backtester.data_loader.load_bars_from_alpaca to fetch real 5Min bars (gated behind the flag;
    never run in CI). Reuse src/backtester/data_loader for loading/normalise_bar. Print a short
    human-readable report (D-05): candidates per symbol, totals, window count, and a
    STRONG/MARGINAL/NO-EDGE verdict line keyed off total frequency. Import the real DAYTRADE preset.
  </action>
  <verify>
    <automated>python scripts/backtest_signal_frequency.py --fixture-dir tests/fixtures/daytrade_5min 2>&1 | grep -iE "candidate|total"</automated>
  </verify>
  <done>Running the script on the committed fixture prints a per-symbol + total frequency report deterministically with no network; --live path documented and flag-gated.</done>
</task>

<task type="auto">
  <name>Task 3: Regression-guard frequency test</name>
  <files>tests/test_signal_frequency.py</files>
  <action>
    Create tests/test_signal_frequency.py importing the pure run_frequency function from
    scripts/backtest_signal_frequency.py, loading the committed fixture, and asserting a SANE range
    (D-05): total_candidates > 0 (guards a regression that silences scanning) AND total_candidates
    not absurdly high (e.g. <= 0.8 * windows * symbols — guards a broken min_confluence gate). Once
    the fixture is finalized, ALSO pin the exact count (assert total == N) with a comment recording N
    + the fixture date/hash, so any signal-engine math change that shifts frequency trips the test.
    Deterministic, offline — never invoke --live. If scripts/ is not importable as a package, add a
    sys.path shim or move the pure function to an importable module (Claude's discretion, minimal diff).
  </action>
  <verify>
    <automated>python -m pytest tests/test_signal_frequency.py -x -q</automated>
  </verify>
  <done>Test imports the harness, runs on the fixture, asserts a sane range (and pinned exact count), passes green, no network.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness → live Alpaca (--live only) | Network fetch behind explicit flag; default path is offline fixture |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Denial of service | non-deterministic CI from datetime.now() in live get_bars | mitigate | Default to fixture replay (no clock/network); gate live behind --live; test never runs --live (Pitfall 3) |
| T-10-04 | Tampering | fixture too short under-feeds engine → false 0-count | mitigate | Commit >=100 bars/symbol; analyze<30 and _adx<29 guards documented (Pitfall 2) |
| T-10-SC | Tampering | npm/pip installs | accept | No new packages — reuses pytest, alpaca-py, in-repo data_loader (audit N/A per RESEARCH) |
</threat_model>

<verification>
- `python scripts/backtest_signal_frequency.py --fixture-dir tests/fixtures/daytrade_5min` prints a report deterministically.
- `python -m pytest tests/test_signal_frequency.py -q` green.
- Full suite: `python -m pytest tests/ -q` ≥272 green (run at wave merge).
</verification>

<success_criteria>
- Committed 5Min fixture (>=100 bars/symbol, daytrade subset) drives the real scan_assets.
- Harness reports per-symbol + total candidate frequency with a verdict, offline by default, --live documented.
- Regression test asserts a sane frequency range (and pinned exact count) on the fixture.
</success_criteria>

<output>
Create `.planning/phases/10-verification-backtest/10-02-SUMMARY.md` when done.
</output>
