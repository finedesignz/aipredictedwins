---
phase: 10-verification-backtest
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/10-verification-backtest/COVERAGE-MAP.md
  - tests/test_learning_realloop.py
  - tests/test_technical_signals.py
autonomous: true
requirements: [VERIFY-01]
must_haves:
  truths:
    - "A coverage-map doc maps every VERIFY-01 surface to its existing green test with a verdict"
    - "A session-anchored VWAP assertion exists in tests/test_technical_signals.py"
    - "The REAL BotThread._run_cycle vetoes a candidate (no order placed) when seeded advice has should_trade=False under enforce"
    - "The REAL BotThread._run_cycle does NOT veto in shadow mode (order still placed)"
    - "The REAL BotThread._run_cycle scales order qty by confidence_adjustment<1 under enforce"
    - "The REAL BotThread._run_cycle ignores confidence_adjustment in shadow mode (unscaled qty)"
  artifacts:
    - path: ".planning/phases/10-verification-backtest/COVERAGE-MAP.md"
      provides: "VERIFY-01 surface → test mapping with verdicts"
      contains: "MIRROR"
    - path: "tests/test_learning_realloop.py"
      provides: "Real-loop veto/scale integration tests (enforce + shadow)"
      min_lines: 80
    - path: "tests/test_technical_signals.py"
      provides: "Session-anchored VWAP assertion"
      contains: "session_anchor"
  key_links:
    - from: "tests/test_learning_realloop.py"
      to: "src.bot_thread.BotThread._run_cycle"
      via: "direct method call with injected stub alpaca + seeded FakeTradeMemory"
      pattern: "_run_cycle"
    - from: "tests/test_learning_realloop.py"
      to: "place_market_order"
      via: "assert call count (veto) and qty kwarg (scale)"
      pattern: "place_market_order"
---

<objective>
Close the VERIFY-01 coverage audit and the Phase-7 mirror-helper gap.

Purpose: The learning veto/scale path tests currently assert against `_advice_consume`, a
contract re-implementation living in `tests/test_learning_wiring.py` (lines 82-97), NOT the
production entry loop. A regression that dropped the real `if not advice["should_trade"]` veto or
the `adj = advice.get("confidence_adjustment")` scale would NOT be caught. This plan documents the
full coverage map and adds a real-loop integration test driving the actual `BotThread._run_cycle`.

Output: `COVERAGE-MAP.md` (audit doc), `tests/test_learning_realloop.py` (4 real-loop tests),
and a session-VWAP assertion added to `tests/test_technical_signals.py` if missing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-verification-backtest/10-RESEARCH.md
@.planning/phases/10-verification-backtest/10-CONTEXT.md

@src/bot_thread.py
@tests/conftest.py
@tests/test_learning_wiring.py

<interfaces>
<!-- The real seam. Drive THIS, not the _advice_consume mirror. -->

src/bot_thread.py — BotThread._run_cycle (line 307):
```
def _run_cycle(self, cfg, alpaca, logger, risk_gate, starting_bankroll,
               cycle_count, memory=None, learning_loop=None, universe=None) -> None
```
LONG veto/scale wiring (lines 522-577):
  - `advice = memory.get_advice(symbol, signal_type, sentiment, price_change)`
  - `if not advice["should_trade"]: if enforce: continue` (veto) `else: log shadow`
  - `elif enforce: adj = advice.get("confidence_adjustment", 1.0)` (scale)
  - `_kelly_technical(..., confidence_adjustment=adj, ...)` → `alpaca.place_market_order(symbol, qty=sizing["shares"], side="buy")`
  - `enforce = should_enforce_learning(memory, bot_id)` computed once per cycle (line 443).
  - Inside the scan call, `_run_cycle` sources profile from env:
    `_profile = PROFILES.get(os.environ.get("BOT_PROFILE","swing").lower(), SWING)`
    then `scan_assets(alpaca, universe, timeframe="1Hour", bar_count=50, fetch_4h=not is_stock, profile=_profile)`.
  - `_db.get_recent_loss_symbols(bot_id, hours=24)` (line 363) and `_db.persist_scan_signals(...)` (line 395) hit Postgres — monkeypatch on `src.bot_thread._db`.

tests/conftest.py — FakeTradeMemory (line 72):
```
FakeTradeMemory(advice=None, thresholds=None, closed_count=0)
  .get_advice(symbol, signal_type, sentiment, price_change) -> dict
  .get_dynamic_thresholds() -> dict
  .record_trade_context(trade_data) -> int
  .count_closed_trades() -> int
```
mock_alpaca fixture (line 47): MagicMock with get_positions/get_latest_price/get_bars/close_position.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write VERIFY-01 coverage-map audit doc</name>
  <files>.planning/phases/10-verification-backtest/COVERAGE-MAP.md</files>
  <action>
    Create the coverage-map doc satisfying D-01. Build a table mapping each VERIFY-01 critical
    surface to its existing test file(s) and a verdict (GREEN keep / MIRROR gap). Source the rows
    from the Coverage Map in 10-RESEARCH.md (surfaces: profile presets / SWING parity, ATR exit
    math, fee gate, session VWAP, learning sizing math, learning veto wiring, learning scale
    wiring, shadow-mode wiring, signal_type alignment, shadow gate count, learning dimensions).
    Mark the three `_advice_consume` path tests (test_veto_skips_candidate,
    test_adjustment_scales_size_in_path, test_shadow_mode_no_effect) as MIRROR — the gap. State
    explicitly that the gap is closed by tests/test_learning_realloop.py (Task 3). Do NOT duplicate
    existing green tests — this is documentation only.
  </action>
  <verify>
    <automated>test -f .planning/phases/10-verification-backtest/COVERAGE-MAP.md && grep -q MIRROR .planning/phases/10-verification-backtest/COVERAGE-MAP.md && grep -q "test_learning_realloop" .planning/phases/10-verification-backtest/COVERAGE-MAP.md</automated>
  </verify>
  <done>Doc exists, maps all VERIFY-01 surfaces, flags the 3 mirror tests as the gap, points at the real-loop test.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm/add session-anchored VWAP assertion</name>
  <files>tests/test_technical_signals.py</files>
  <action>
    Audit per D-01: confirm an assertion exercising the session_anchor=True branch of `_vwap_bullish`
    (session-anchored VWAP) exists in tests/test_technical_signals.py. If present, leave it — do not
    duplicate. If absent, ADD one minimal test asserting the session-anchored VWAP path produces the
    expected bullish/bearish verdict on a small deterministic bar set (anchor resets intraday window).
    Reference the session-VWAP semantics in src/technical_signals.py `_vwap_bullish`. Minimal diff.
  </action>
  <verify>
    <automated>grep -q session_anchor tests/test_technical_signals.py && python -m pytest tests/test_technical_signals.py -q</automated>
  </verify>
  <done>A session_anchor=True assertion exists in test_technical_signals.py and the file's tests pass green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Real-loop veto/scale integration test (enforce + shadow)</name>
  <files>tests/test_learning_realloop.py</files>
  <behavior>
    Drive the REAL BotThread._run_cycle (not _advice_consume). 4 cases:
    - test_realloop_veto_enforce: FakeTradeMemory(advice={should_trade:False,...}, closed_count=999),
      LEARNING_ENFORCE unset → enforce=True → place_market_order NOT called.
    - test_realloop_veto_shadow: same advice, monkeypatch.setenv("LEARNING_ENFORCE","0") → enforce=False
      → place_market_order IS called (shadow logs, does not skip).
    - test_realloop_scale_enforce: advice={should_trade:True, confidence_adjustment:0.5}, closed_count=999
      → order placed with qty ≈ unscaled baseline × 0.5 (capture qty kwarg, compare to a
      confidence_adjustment=1.0 baseline run pre-cap).
    - test_realloop_scale_shadow: same advice, LEARNING_ENFORCE=0 → adj stays 1.0 → unscaled qty.
  </behavior>
  <action>
    Create tests/test_learning_realloop.py. Construct a BotThread (or the minimal instance needed to
    call the bound `_run_cycle`) and invoke `_run_cycle` directly with injected dependencies — this
    is the real production veto/scale path closing the Phase-7 mirror gap. Do NOT re-implement
    `_advice_consume`; do NOT drive alpaca_orchestrator.main() (not a unit seam).

    Stubs (no network, no DB):
    - alpaca: extend the conftest mock_alpaca or build a small fake — get_account() →
      {"buying_power":100000.0,"equity":100000.0}; get_latest_price(sym) → fixed float; get_bars(...)
      → engineered bars; place_market_order(...) → record call (capture symbol, qty, side), return
      {"order_id":"X","status":"accepted"}.
    - cfg: set skip_risk_gate=True (line 469 bypasses the LLM panel — simplest path to sizing), plus
      symbols/min_confluence/kelly_fraction/max_position_pct/rsi_ceiling matching a passing LONG
      candidate. Set BOT_PROFILE so the env-sourced _profile is deterministic.
    - logger (TradeLogger): stub get_open_alpaca_positions() → [], log_alpaca_trade(...) → fake trade_id.
    - memory: seeded FakeTradeMemory per case (closed_count=999 to force enforce; advice dict per case).
    - monkeypatch src.bot_thread._db: get_recent_loss_symbols → set(), persist_scan_signals → no-op
      (Pitfall 6 — avoids Postgres).
    - Ensure a LONG candidate survives to sizing: either stub `scan_assets` (monkeypatch
      src.bot_thread.scan_assets) to return one Signal with confluence_score ≥ cfg.min_confluence,
      rsi_value < cfg.rsi_ceiling, trend_4h != "bearish"; OR feed get_bars data that yields such a
      Signal through the real engine. Prefer stubbing scan_assets for determinism — the learning
      wiring under test is downstream of scanning.

    Force enforce deterministically: closed_count=999 + LEARNING_ENFORCE unset → enforce=True; force
    shadow via monkeypatch.setenv("LEARNING_ENFORCE","0"). Assert via place_market_order call count
    (veto: 0 vs 1) and the captured qty kwarg (scale: half of the adj=1.0 baseline, compared
    pre-cap so the max_position_pct cap doesn't mask the ratio).
  </action>
  <verify>
    <automated>python -m pytest tests/test_learning_realloop.py -x -q</automated>
  </verify>
  <done>All 4 tests pass, each drives the real _run_cycle, veto asserted by call-count and scale by qty kwarg, covering enforce AND shadow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → production code | Tests drive real _run_cycle; only risk is a false-green test, not a runtime attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | test stubs masking real wiring | mitigate | Drive real `_run_cycle` (not the mirror); assert on place_market_order call-count/qty so a dropped veto/scale fails the test |
| T-10-02 | Information disclosure | no live creds in CI test | mitigate | Fully stubbed alpaca + monkeypatched _db; no network, no DB |
| T-10-SC | Tampering | npm/pip installs | accept | No new packages (pytest, alpaca-py already present) — audit N/A per RESEARCH |
</threat_model>

<verification>
- `python -m pytest tests/test_learning_realloop.py tests/test_technical_signals.py -q` green.
- `grep MIRROR .planning/phases/10-verification-backtest/COVERAGE-MAP.md` returns the gap row.
- Full suite: `python -m pytest tests/ -q` ≥272 green (run at wave merge).
</verification>

<success_criteria>
- COVERAGE-MAP.md maps every VERIFY-01 surface with a verdict and points at the real-loop test.
- Session-anchored VWAP assertion present and green.
- 4 real-loop tests prove veto (call-count) and scale (qty) against the actual _run_cycle, enforce + shadow.
- No duplicated green tests; minimal diff.
</success_criteria>

<output>
Create `.planning/phases/10-verification-backtest/10-01-SUMMARY.md` when done.
</output>
