---
phase: 17-per-symbol-performance
plan: 02
type: execute
wave: 1
depends_on: [17-01]
files_modified:
  - src/db.py
autonomous: true
requirements: [TUNE-02]

must_haves:
  truths:
    - "db.get_resolved_trades(bot_id=None, since=None) exists — ONE read-only parameterized SELECT over alpaca_trades, no writes anywhere"
    - "WHERE status IN ('closed','stopped','target_hit') — the literal copied from src/db.py:215 / :256-257, so a Phase-15 gate block ('rejected', pnl=0) can NEVER enter the sample"
    - "The optional window filter is `\"timestamp\"::timestamptz >= %s` — never a raw TEXT compare (src/db_schema.sql:28 is `timestamp TEXT`)"
    - "bot_id and since reach SQL ONLY as psycopg %s params — never f-string interpolation"
    - "NULL pnl is returned AS-IS, never coerced with `or 0.0` (that is symbol_stats' job to exclude and count)"
    - "Case 14 (TEST_DATABASE_URL-gated real SQL) passes against a LOCAL Postgres; no other src/db.py function is modified"
  artifacts:
    - path: "src/db.py"
      provides: "get_resolved_trades — the one read-only SELECT feeding TUNE-02"
      contains: "def get_resolved_trades"
  key_links:
    - from: "src/db.py::get_resolved_trades"
      to: "alpaca_trades"
      via: "SELECT filtered to the position-closed terminal set"
      pattern: "status IN \\('closed', 'stopped', 'target_hit'\\)"
    - from: "src/db.py::get_resolved_trades"
      to: "the window slice"
      via: "TEXT column cast before comparison"
      pattern: "\"timestamp\"::timestamptz >= %s"
---

<objective>
Add the single read-only query Phase 17 is allowed to run: `db.get_resolved_trades`.

Purpose: SQL selects, Python decides (17-CONTEXT decision 1). This function is the FIRST of two layers
standing between the report and a fabricated loss column — the terminal-status filter here (belt) plus
the pure aggregator's own defence (braces, Plan 03). A `rejected` row carries `pnl = 0`
(`src/bot_thread.py:309,:317,:332`), and post-Phase-15 the gate emits one per blocked entry; if it
reached the sample it would score as a zero-P&L LOSS against exactly the symbols an operator
quarantined, and Phase 18 would quarantine more on that fabricated evidence.
Output: src/db.py::get_resolved_trades (turns VALIDATION case 14 GREEN; case 16's fence must stay green).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-per-symbol-performance/17-CONTEXT.md
@.planning/phases/17-per-symbol-performance/17-RESEARCH.md
@.planning/phases/17-per-symbol-performance/17-VALIDATION.md
@src/db.py
@CLAUDE.md

<interfaces>
Signature (plan of record, 17-RESEARCH §1):

  def get_resolved_trades(bot_id: str | None = None, since=None) -> list[dict]:
      """Position-closed rows for per-symbol stats (TUNE-02). READ-ONLY.

      Terminal set mirrors get_alpaca_accuracy (db.py:215) / get_realized_pnl (db.py:256-257)
      EXACTLY: ('closed','stopped','target_hit'). Non-position terminals
      (canceled/cancelled/expired/rejected) never held a position — and a Phase-15 gate
      block writes a 'rejected' row with pnl=0 (bot_thread.py:309), which would otherwise
      score as a LOSS. NULL pnl is returned as-is, never coerced (symbol_stats excludes and
      counts it — src/db.py:228,259 coerce; this must NOT).
      """

Shape:
  SELECT bot_id, symbol, asset_class, side, status, pnl, fees,
         "timestamp" AS entry_ts, closed_at
  FROM alpaca_trades
  WHERE status IN ('closed', 'stopped', 'target_hit')
  [AND bot_id = %s]
  [AND "timestamp"::timestamptz >= %s]
  ORDER BY "timestamp" ASC

Existing house idioms to reuse (do NOT invent new ones):
  connection()            — src/db.py, yields psycopg3 conn with row_factory=dict_row (:29),
                            so .fetchall() already returns list[dict]. No manual row mapping.
  "timestamp"::timestamptz cast precedent — src/db.py:168, :204; dashboard/api/routes/bots.py:93-104.
  `timestamp` is quoted throughout the codebase — quote it here too.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/db.py::get_resolved_trades — one read-only SELECT</name>
  <files>src/db.py</files>
  <read_first>src/db.py L20-40 (pool, dict_row), L151-172 (the ::timestamptz cast precedent), L211-241 (get_alpaca_accuracy — copy its status literal, do NOT copy its `or 0` coercion), L246-259 (get_realized_pnl — the terminal set of record and its docstring); src/db_schema.sql L24-51 (alpaca_trades: `timestamp TEXT` at :28, `pnl`/`fees` DOUBLE PRECISION NULLable); src/order_resolution.py L11 (_TERMINAL_NONPOSITION); tests/test_symbol_stats.py case 14 (the contract this must satisfy)</read_first>
  <behavior>
    - Case 14 (TEST_DATABASE_URL-gated): get_resolved_trades() returns dicts whose key set is EXACTLY
      {bot_id, symbol, asset_class, side, status, pnl, fees, entry_ts, closed_at}.
    - A seeded 'rejected' row (pnl=0) is ABSENT from the result — the status filter runs in SQL.
    - get_resolved_trades(since=now-90d) excludes a row entered 200 days ago and includes a fresh one:
      the "timestamp"::timestamptz cast executes without error against the TEXT column.
    - get_resolved_trades(bot_id="A") returns only bot A's rows.
    - A seeded 'closed' row with pnl=NULL comes back with pnl None (NOT 0.0).
  </behavior>
  <action>Append get_resolved_trades to src/db.py next to get_realized_pnl (the same "Reconciliation / stats" region). Build the SQL as a list of clause strings joined with "\n" and a parallel params list, appending "AND bot_id = %s" only when bot_id is truthy and 'AND "timestamp"::timestamptz >= %s' only when since is not None — exactly the shape in 17-RESEARCH §1. Parameterize with %s ONLY; never f-string bot_id or since into the query (V5 / T-17-05). Use `with connection() as conn: return conn.execute(sql, tuple(params)).fetchall()` — dict_row (db.py:29) already yields dicts, so add no row mapping. Return NULL pnl as-is: do NOT write `or 0.0` anywhere in this function; the docstring must say so and cite db.py:228,259 as what NOT to do. Cite the terminal-set source line in a comment. Modify NO other function in src/db.py — get_alpaca_accuracy and get_realized_pnl stay byte-identical (their coercion is a known, reported divergence, not something Phase 17 fixes). Add zero INSERT/UPDATE/DELETE/ALTER.</action>
  <verify>
    <automated>TEST_DATABASE_URL="$TEST_DATABASE_URL" python -m pytest tests/test_symbol_stats.py -q -k "resolved_trades_sql or readonly" 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_symbol_stats.py -k readonly -q` PASSES (the static fence sees no INSERT/UPDATE/DELETE/ALTER in the new function).
    - With a LOCAL Postgres: `TEST_DATABASE_URL=postgresql://localhost/aipw_test python -m pytest tests/test_symbol_stats.py -k resolved_trades_sql -q` reports `1 passed` (0 skipped).
    - `git diff src/db.py | grep -ciE "^\+.*\b(INSERT|UPDATE|DELETE|ALTER|DROP|TRUNCATE)\b"` prints `0`.
    - `git diff --stat` lists src/db.py ONLY.
    - `git diff src/db.py` shows NO change to get_alpaca_accuracy or get_realized_pnl (added lines only).
    - `grep -n 'timestamp"::timestamptz >= %s' src/db.py` returns a line inside get_resolved_trades.
    - `python -m pytest tests/ dashboard/api/tests/ -q | tail -1` shows >= 373 passed, no new failures.
    - NEVER run any of this against the prod DATABASE_URL.
  </acceptance_criteria>
  <done>The one query exists, filters the sample in SQL, casts the TEXT timestamp, parameterizes its inputs, and writes nothing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| CLI args (`--bot`, `--window`) → SQL | untrusted operator input crosses into the query text |
| this process → Postgres | read-only by construction; test evidence runs on LOCAL/TEST_DATABASE_URL only |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Repudiation | a 'rejected'/'canceled' row enters the sample and scores as a loss | mitigate | `WHERE status IN ('closed','stopped','target_hit')` in SQL — the belt (braces in Plan 03); pinned by case 14(b) |
| T-17-05 | Tampering | SQL injection via bot_id / since | mitigate | psycopg `%s` params only; no f-string; `--window` is `argparse type=int` upstream |
| T-17-04 | Tampering | a write path reaches the prod trade log | mitigate | function is a SELECT; case 16 static fence greps the diff for INSERT/UPDATE/DELETE/ALTER |
| T-17-06 | Information disclosure | mis-filtered window silently returns the wrong regime | mitigate | `"timestamp"::timestamptz` cast — a lexicographic TEXT compare mis-filters silently (Pitfall 3); pinned by case 14(c) |
| T-17-SC | Tampering | package installs | accept | none — no new packages |
</threat_model>

<verification>
- `python -m pytest tests/test_symbol_stats.py -q` — case 14 passes with TEST_DATABASE_URL set (skips without); case 16 passes.
- `python -m pytest tests/ dashboard/api/tests/ -q` — >= 373 passed / 9 skipped, no regressions.
- Manual grep gate: `git diff src/db.py | grep -ciE "^\+.*\b(INSERT|UPDATE|DELETE|ALTER)\b"` == 0.
</verification>

<success_criteria>
Phase 17 can read the resolved-trade dataset and cannot write to it. The Phase-15 gate's `rejected` rows
are filtered out at the SQL layer, before any statistic sees them.
</success_criteria>

<output>
Create `.planning/phases/17-per-symbol-performance/17-02-SUMMARY.md` when done.
</output>
