---
phase: 17-per-symbol-performance
plan: 04
type: execute
wave: 2
depends_on: [17-02, 17-03]
files_modified:
  - scripts/symbol_report.py
  - .planning/phases/17-per-symbol-performance/EVIDENCE.md
autonomous: false
requirements: [TUNE-02]
revision: 2
revision_reason: "plan-check B1/B2 disclosure lines, B3 prod read/write split, W2/W3 — see 17-VALIDATION.md Revision 2"

must_haves:
  truths:
    - "scripts/symbol_report.py prints a markdown table to stdout and writes .planning/phases/17-per-symbol-performance/EVIDENCE.md"
    - "Flags: --bot, --window <days>, --min-sample (default 5), --json. `--apply` DOES NOT EXIST — there is no write path anywhere in the script"
    - "Default is FULL HISTORY; --window computes since = now(utc) - timedelta(days=N) and passes it as a %s param"
    - "FOUR loud summary lines, always printed even at zero: null_pnl_total, zero_pnl_total (B1), gross_pnl_rows_total (B2), null_fees_total"
    - "B1 disclosure: zero_pnl_total counts position-closed rows with pnl == 0.0 — the external-exit sentinel (src/alpaca_orchestrator.py:167-176). They are NOT losses and NOT trades. A non-zero count is a Phase-18/20 finding."
    - "B2 disclosure: EVIDENCE.md states in plain words that the realized_pnl of gross_pnl_rows cells is NOT fee-adjusted (src/bot_c/strategy.py:393-395, src/trend_strategy.py:172-173 store GROSS pnl with NULL fees). total_fees is INCOMPLETE. Fees are NEVER subtracted."
    - "W2 finding recorded in EVIDENCE.md: src/db.py:201 get_recent_loss_symbols uses status IN ('closed','stopped') — a FOURTH status-set spelling, drops 'target_hit', live in the entry cooldown. Reported, NOT fixed."
    - "Cells annotated already_quarantined / off_universe by READING src.effective_universe.resolve_universe — never re-deriving; matched through normalize() on BOTH sides"
    - "W3 — rank_cells() returns ONLY sample=='sufficient' cells; the full table still shows every cell"
    - "KNOWN LIMITATION printed per bot: symbol_stats realized_pnl vs db.get_realized_pnl vs delta (that function coerces NULL to 0.0 AND sums sentinel zeros). Print all three; do not reconcile it away."
    - "B3 — EVIDENCE.md may be generated against PROD but SELECT-ONLY (get_resolved_trades / get_realized_pnl; no write path, no --apply). The SEEDING test (case 14) is TEST_DATABASE_URL-only and never runs here."
  artifacts:
    - path: "scripts/symbol_report.py"
      provides: "the read-only TUNE-02 report CLI"
      contains: "def main() -> int"
      min_lines: 110
    - path: ".planning/phases/17-per-symbol-performance/EVIDENCE.md"
      provides: "per-symbol / per-bot winners+losers evidence for Phase 18"
      contains: "zero_pnl_total"
  key_links:
    - from: "scripts/symbol_report.py"
      to: "src.db.get_resolved_trades"
      via: "the one read-only SELECT"
      pattern: "get_resolved_trades"
    - from: "scripts/symbol_report.py"
      to: "src.symbol_stats.aggregate"
      via: "the pure aggregator; three group keys, one code path"
      pattern: "aggregate\\("
    - from: "scripts/symbol_report.py"
      to: "src.effective_universe.resolve_universe"
      via: "already_quarantined / off_universe annotation, read not re-derived"
      pattern: "resolve_universe"
---

<objective>
Ship the read-only report CLI and generate the Phase-18 evidence.

Purpose: turn the trustworthy dataset into defensible per-symbol / per-bot numbers — winners and losers as
concrete figures, with the sample guard visible and **every data defect stated out loud rather than
absorbed**:

- `zero_pnl_total` (**B1**) — position-closed rows with `pnl == 0.0`, the external-exit sentinel
  (`src/alpaca_orchestrator.py:167-176`). Not losses. Not trades.
- `gross_pnl_rows_total` (**B2**) — counted rows whose `fees` is NULL, i.e. whose `pnl` is probably
  **GROSS** (`src/bot_c/strategy.py:393-395`, `src/trend_strategy.py:172-173`). Their `realized_pnl` is
  **not fee-adjusted**, and `total_fees` under-reports drag for exactly those bots.
- `null_pnl_total` — resolution defects.
- The `get_realized_pnl` divergence — printed, not reconciled.
- The **W2** finding — `src/db.py:201` uses a fourth status-set spelling in the live entry cooldown.

The report **ANNOTATES**; it does not **DECIDE**. Phase 18 (TUNE-01/TUNE-03) consumes this and owns the
`quarantined_symbols` write.

Output: scripts/symbol_report.py + EVIDENCE.md (turns VALIDATION cases 17-18 GREEN; case 16's fence — which
now also asserts `--apply` is absent — must stay green).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-per-symbol-performance/17-CONTEXT.md
@.planning/phases/17-per-symbol-performance/17-RESEARCH.md
@.planning/phases/17-per-symbol-performance/17-VALIDATION.md
@scripts/backfill_trades.py
@CLAUDE.md

<interfaces>
CLI (mirror scripts/backfill_trades.py STRUCTURALLY — module docstring with usage lines, argparse,
`main() -> int`, `sys.exit(main())`, fixed-width print table — but the write half is DELIBERATELY ABSENT):

  python scripts/symbol_report.py                       # all bots, FULL HISTORY (the default)
  python scripts/symbol_report.py --bot A --window 90
  python scripts/symbol_report.py --min-sample 10
  python scripts/symbol_report.py --json
  python -m scripts.symbol_report

  --bot         str,  optional     -> get_resolved_trades(bot_id=...)   [%s param, never f-string]
  --window      int,  optional     -> since = datetime.now(timezone.utc) - timedelta(days=N)
  --min-sample  int,  default 5    -> aggregate(min_sample=...)         [src.symbol_stats.MIN_SAMPLE]
  --json        flag               -> dump cells + rollups + summary as JSON instead of the table
  --apply       *** DOES NOT EXIST *** — there is no write path. (Pinned by VALIDATION case 16.)

Module-level helpers the tests import (cases 17/18):
  annotate(cells, bots_rows) -> cells        # stamps already_quarantined / off_universe
  rank_cells(cells) -> list[dict]            # W3: returns ONLY sample == "sufficient" cells, by expectancy
  render_markdown(cells, rollups, summary) -> str

Upstream surfaces (built in Plans 02/03 — call, do not reimplement):
  src.db.get_resolved_trades(bot_id=None, since=None) -> list[dict]       # READ-ONLY SELECT
  src.symbol_stats.aggregate(rows, min_sample=MIN_SAMPLE, key=("bot_id","symbol")) -> list[dict]
      also key=("symbol",)  -> all-bots per-symbol roll-up
      also key=("bot_id",)  -> per-bot roll-up
  src.effective_universe.resolve_universe(cfg) -> {..., "blocked": [{symbol, reason, ...}], ...}
      reason in {quarantined, off_universe, meme, untradeable}   [src/effective_universe.py:164-170]
  src.universe.normalize(symbol) -> str
  src.bot_config.BotConfig.from_row(row)
  src.db.get_realized_pnl(bot_id) -> float    # the DIVERGENT number — PRINT it, do not adopt it

Annotation rule (17-CONTEXT decision 8, 17-RESEARCH §5):
  Build cfg per bot from `SELECT * FROM bots` — WITHOUT the `enabled = TRUE` filter (a disabled bot with
  historical trades must still be annotated). A cell whose bot row is MISSING is stamped
  `annotation: "unavailable"`, never a silent blank. Index resolve_universe(cfg)["blocked"] by
  normalize(symbol); stamp:
    already_quarantined = (reason == "quarantined")
    off_universe        = (reason in {"off_universe", "meme", "untradeable"})
  Match through normalize() on BOTH sides.

Report sections, in order:
  1. Header: DB label (prod-readonly | local), window (or "full history"), --min-sample, row counts.
  2. Per-(bot, symbol) table — EVERY cell, insufficient ones asterisked. Columns:
     bot | symbol (display) | trades | wins | losses | win_rate | realized_pnl | total_fees |
     avg_win | avg_loss | expectancy | best | worst | zero_pnl | null_pnl | gross_pnl_rows |
     sample | quarantined | off_universe
  3. All-bots-per-symbol roll-up, then the per-bot roll-up.
  4. RANKING — best/worst by expectancy over `sufficient` cells ONLY (`rank_cells`). Insufficient cells are
     excluded from the RANKING, never from table 2.
  5. SUMMARY — always printed, even when zero:
       null_pnl_total: N         resolution defects (pnl IS NULL on a position-closed row)
       zero_pnl_total: N         pnl == 0.0 on a position-closed row — the external-exit sentinel
                                 (src/alpaca_orchestrator.py:167-176). NOT losses, NOT trades.
       gross_pnl_rows_total: N   counted rows with NULL fees — pnl is probably GROSS
                                 (src/bot_c/strategy.py:393-395, src/trend_strategy.py:172-173).
                                 Their realized_pnl is NOT fee-adjusted; total_fees under-reports drag.
       null_fees_total: N
     A non-zero value in any of these is a FINDING for Phase 18/20 — Phase 17 does not fix it.
  6. KNOWN LIMITATIONS:
     (a) per bot, side by side:
         symbol_stats realized_pnl = X | db.get_realized_pnl() = Y | delta = Y - X
         The delta is the NULL-pnl rows (coerced to 0.0 by that function) plus the sentinel zeros it sums.
         The divergence is CORRECT and INTENDED. Do not reconcile it away; do NOT change get_realized_pnl.
     (b) W2: src/db.py:201 get_recent_loss_symbols filters `status IN ('closed','stopped')` — a FOURTH
         spelling of the status set, dropping 'target_hit', LIVE in the entry cooldown. Reported here as a
         Phase-18/20 finding; Phase 17 changes no bot behavior.

  NEVER print or compute `realized_pnl - total_fees`. `pnl` at two of four writers is already net
  (src/pnl.py:28); at the other two it is gross with no fee data at all. Subtracting is wrong either way.
  NO "quarantine"/"disable" verdict. Rank, annotate, stop.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: scripts/symbol_report.py — read-only CLI, annotation, sufficient-only ranking, loud defect disclosure</name>
  <files>scripts/symbol_report.py</files>
  <read_first>scripts/backfill_trades.py L1-46 (the CLI convention to mirror — minus --apply); tests/test_symbol_stats.py cases 16-18 (the contract: fence with positive control + self-test, no --apply, annotation via resolve_universe, rank_cells returns only sufficient cells); src/effective_universe.py L115-203 (resolve_universe return shape + reason vocabulary); src/bot_config.py (BotConfig.from_row); src/db.py::get_resolved_trades + get_realized_pnl (L246-259 — note it coerces NULL to 0.0 AND sums sentinel zeros, which is exactly the delta we print); src/db.py L194-208 (the W2 finding to record verbatim)</read_first>
  <behavior>
    - test_annotation_from_resolve_universe (case 17): a quarantined symbol's cell -> already_quarantined
      True; an off-allowlist symbol (TRUMP/USD) -> off_universe True; a live allowlisted symbol -> both
      False. Matched through normalize() on both sides; the verdict comes from resolve_universe, never from
      re-derived set math.
    - test_ranking_is_sufficient_only (case 18, W3): `all(c["sample"] == "sufficient" for c in
      rank_cells(cells))`; an insufficient cell with the WORST expectancy is ABSENT from rank_cells() and
      PRESENT in the full table.
    - test_phase17_is_read_only (case 16): the source contains no INSERT/UPDATE/DELETE/ALTER and no
      "--apply" (asserted against a fence that carries a positive control and is self-tested).
  </behavior>
  <action>Create scripts/symbol_report.py mirroring scripts/backfill_trades.py's shape (module docstring with the usage lines above, argparse, `main() -> int`, `if __name__ == "__main__": sys.exit(main())`). Flags exactly: --bot, --window (type=int), --min-sample (type=int, default src.symbol_stats.MIN_SAMPLE), --json. DO NOT add --apply, --fix, --write or any other mutating flag; the module docstring says so explicitly. main(): rows = get_resolved_trades(bot_id=args.bot, since=since); cells = aggregate(rows, min_sample=args.min_sample); by_symbol = aggregate(rows, ..., key=("symbol",)); by_bot = aggregate(rows, ..., key=("bot_id",)); annotate(cells, bots_rows) where bots_rows comes from a plain `SELECT * FROM bots` (NO enabled filter); print render_markdown(...) to stdout and Write the same text to .planning/phases/17-per-symbol-performance/EVIDENCE.md (pathlib, resolved from the repo root, not CWD). --json prints json.dumps of cells + rollups + summary instead. Keep annotate(), rank_cells() and render_markdown() at module level and PURE (data in, data/str out) so tests call them with fixtures and no DB. rank_cells() filters to sample == "sufficient" and sorts by expectancy (W3). Enumerate report fields EXPLICITLY — never a `**row` splat (a `bots` row carries alpaca keys and EVIDENCE.md is committed to git). Construct NO AlpacaClient; make zero network calls. Print the four summary counters (null_pnl_total, zero_pnl_total, gross_pnl_rows_total, null_fees_total) UNCONDITIONALLY, each with the one-line explanation from the interfaces block above, and both KNOWN LIMITATIONS (the per-bot get_realized_pnl delta, and the W2 get_recent_loss_symbols status-set divergence). Zero INSERT/UPDATE/DELETE/ALTER; no `realized_pnl - total_fees` anywhere.</action>
  <verify>
    <automated>python -m pytest tests/test_symbol_stats.py -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_symbol_stats.py -q` — ALL cases green with a LOCAL TEST_DATABASE_URL (case 14 not skipped); all-but-14 green + 1 skipped without it.
    - `python scripts/symbol_report.py --help` lists exactly `--bot`, `--window`, `--min-sample`, `--json` (plus -h). `python scripts/symbol_report.py --apply` exits non-zero with `unrecognized arguments: --apply`.
    - `grep -c -- "--apply" scripts/symbol_report.py` prints `0`.
    - `grep -v '^\s*#' scripts/symbol_report.py | grep -ciE "\b(INSERT|UPDATE|DELETE|ALTER|DROP|TRUNCATE)\b"` prints `0`.
    - `grep -cE "realized_pnl *- *total_fees|- *total_fees" scripts/symbol_report.py` prints `0`.
    - `grep -c "zero_pnl_total\|gross_pnl_rows_total\|null_pnl_total\|null_fees_total" scripts/symbol_report.py` returns >= 4 (all four counters printed).
    - `grep -c "get_recent_loss_symbols" scripts/symbol_report.py` returns >= 1 (the W2 finding is emitted).
    - `python -m pytest tests/ dashboard/api/tests/ -q | tail -1` shows >= 373 passed, no new failures.
    - `git diff --stat` lists scripts/symbol_report.py ONLY.
  </acceptance_criteria>
  <done>A reproducible, read-only command produces the per-symbol/per-bot evidence, annotated with the gate's own answer, ranked over sufficient cells only, with every data defect disclosed rather than absorbed.</done>
</task>

<task type="auto">
  <name>Task 2: Generate EVIDENCE.md — SELECT-only (prod read is allowed; seeding is not)</name>
  <files>.planning/phases/17-per-symbol-performance/EVIDENCE.md</files>
  <read_first>17-VALIDATION.md Revision 2 — "Nyquist Compliance" (the read/write split) + "Known limitations"; 17-CONTEXT scope fences</read_first>
  <action>
    **B3 — the read/write split, stated explicitly. Do not conflate these two things:**
    - The **SEEDING test** (VALIDATION case 14) INSERTS rows. It is gated on `TEST_DATABASE_URL`, asserts
      `TEST_DATABASE_URL != DATABASE_URL`, and **NEVER runs against prod**. It is NOT part of this task.
    - **This task MAY run against the prod `DATABASE_URL`** — because `scripts/symbol_report.py` is
      **SELECT-only**: it calls `get_resolved_trades` (a SELECT) and `get_realized_pnl` (a SELECT), has no
      `--apply`, and issues no INSERT/UPDATE/DELETE/ALTER (case 16's self-tested fence proves it). A
      read-only report against the live trade log is the whole point of the phase — a local copy would
      produce evidence about the wrong data.

    Run the report TWICE and capture both into EVIDENCE.md:
      (1) `python scripts/symbol_report.py`              # full history
      (2) `python scripts/symbol_report.py --window 90`
    Confirm the artifact contains: both tables; the winner/loser RANKING over `sufficient` cells only; all
    four summary counters (`null_pnl_total`, `zero_pnl_total`, `gross_pnl_rows_total`, `null_fees_total`)
    even at zero; the per-bot KNOWN LIMITATION comparison (symbol_stats realized_pnl | db.get_realized_pnl |
    delta); and the **W2** finding (`src/db.py:201` `get_recent_loss_symbols` uses `status IN
    ('closed','stopped')` — a fourth status-set spelling that drops `'target_hit'`, live in the entry
    cooldown — reported, not fixed).

    Prepend a header stating: which DB the numbers came from and that access was **SELECT-only**; the run
    timestamp; and one sentence — *this report ranks and annotates; Phase 18 decides.* Add the B2 sentence
    verbatim: *the realized_pnl of cells with gross_pnl_rows > 0 is NOT fee-adjusted (those rows were
    written with a gross pnl and no fee data); total_fees under-reports drag for those bots.*

    Verify no key/secret material appears anywhere in the file before committing.
  </action>
  <verify>
    <automated>test -f .planning/phases/17-per-symbol-performance/EVIDENCE.md && grep -c "zero_pnl_total\|gross_pnl_rows_total\|null_pnl_total" .planning/phases/17-per-symbol-performance/EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - EVIDENCE.md exists and contains `null_pnl_total`, `zero_pnl_total`, `gross_pnl_rows_total`, `null_fees_total`, a ranking section, and BOTH the full-history and `--window 90` runs.
    - The B2 sentence ("NOT fee-adjusted") and the W2 finding (`get_recent_loss_symbols`) are both present.
    - The per-bot KNOWN LIMITATION line carries all three numbers (symbol_stats realized_pnl | db.get_realized_pnl | delta).
    - `grep -ciE "api[_-]?key|secret|token|password" .planning/phases/17-per-symbol-performance/EVIDENCE.md` prints `0`.
    - The header states the DB and that access was SELECT-only.
    - **No row was written to any database by this task.** (`scripts/symbol_report.py` has no write path; the seeding test was not run.)
  </acceptance_criteria>
  <done>Phase 18 has concrete, reproducible, honestly-qualified numbers — with every defect the data still carries stated on the face of the report.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>scripts/symbol_report.py (read-only, no --apply) and EVIDENCE.md generated SELECT-only from the live trade log.</what-built>
  <how-to-verify>
    1. `python scripts/symbol_report.py --help` — confirm there is NO --apply flag.
    2. Open `.planning/phases/17-per-symbol-performance/EVIDENCE.md` and confirm:
       - the winners/losers ranking covers `sufficient` cells only;
       - `insufficient` cells are still visible in the full table (a 2-trade TRUMP-shaped leak must not be hidden);
       - `zero_pnl_total` is present — these are external-exit sentinels (`closed` rows with `pnl=0.0`), NOT losses. A large number here means many positions exited outside the bot's own close path;
       - `gross_pnl_rows_total` is present, and the "NOT fee-adjusted" sentence is there. Those cells' P&L is overstated by the unrecorded fees;
       - `null_pnl_total` is present (non-zero = a FINDING for Phase 18/20, not a Phase-17 bug);
       - the per-bot KNOWN LIMITATION line shows symbol_stats realized_pnl vs db.get_realized_pnl vs delta;
       - the W2 finding (`get_recent_loss_symbols` status-set divergence) is recorded;
       - no "quarantine X" verdict appears anywhere.
    3. Confirm the header says the DB access was SELECT-only, and that no rows were written.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| operator CLI args → SQL | `--bot` / `--window` cross into the query — `%s` params only |
| the report → git | EVIDENCE.md is committed; anything printed is published |
| this process → Postgres | **SELECT-only.** The report may read prod. The SEEDING test (case 14) is TEST_DATABASE_URL-only and never runs here. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Tampering | a write path (`--apply`, an UPDATE) reaches the trade log | mitigate | no mutating flag exists; case 16's fence (positive control + self-test) greps for `--apply` and INSERT/UPDATE/DELETE/ALTER |
| T-17-14 | Tampering | synthetic rows seeded into prod | mitigate | the seeding test is TEST_DATABASE_URL-gated and asserts `!= DATABASE_URL`; this task runs only the SELECT-only script |
| T-17-05 | Tampering | SQL injection via `--bot` | mitigate | passed to get_resolved_trades as a `%s` param; `--window` is `type=int` |
| T-17-09 | Information disclosure | an alpaca key from a `bots` row lands in the committed EVIDENCE.md | mitigate | report fields enumerated explicitly — no `**row` splat; Task 2 greps the artifact for key/secret/token |
| T-17-12 | Repudiation | sentinel zeros silently swell the loss column | mitigate | `zero_pnl_total` printed unconditionally with its cause (B1) |
| T-17-13 | Repudiation | gross P&L presented as net; $0 drag reported for overstated bots | mitigate | `gross_pnl_rows_total` + the explicit "NOT fee-adjusted" sentence (B2); fees never subtracted |
| T-17-10 | Repudiation | the report emits a quarantine verdict and Phase 18 rubber-stamps it | mitigate | `rank_cells` returns `sufficient` cells only; no verdict; Phase 18 decides |
| T-17-11 | Repudiation | the get_realized_pnl divergence is silently reconciled away, hiding defects | mitigate | both numbers + the delta printed side by side; get_realized_pnl is NOT modified |
| T-17-SC | Tampering | package installs | accept | none — no new packages |
</threat_model>

<verification>
- `python -m pytest tests/test_symbol_stats.py -q` — all cases green with a LOCAL TEST_DATABASE_URL.
- `python -m pytest tests/ dashboard/api/tests/ -q` — >= 373 passed / 9 skipped, no regressions.
- `python scripts/symbol_report.py --apply` -> `unrecognized arguments: --apply`.
- EVIDENCE.md committed: both runs, all four defect counters, the B2 "NOT fee-adjusted" sentence, the W2
  finding, the per-bot get_realized_pnl delta — and no secret material.
</verification>

<success_criteria>
A reproducible read-only command turns the reconciled trade log into per-symbol/per-bot winners and losers
with concrete numbers; marks thin samples without hiding them; refuses to score sentinel zeros or gate
blocks as losses; states out loud which rows carry gross (not net) P&L; prints the `get_realized_pnl`
divergence rather than papering over it — and decides nothing. No bot behavior changed; no row written
anywhere; migration 019 still free.
</success_criteria>

<output>
Create `.planning/phases/17-per-symbol-performance/17-04-SUMMARY.md` when done.
</output>
