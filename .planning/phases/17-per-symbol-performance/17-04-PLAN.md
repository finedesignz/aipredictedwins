---
phase: 17-per-symbol-performance
plan: 04
type: execute
wave: 2
depends_on: [17-02, 17-03]
files_modified:
  - scripts/symbol_report.py
  - .planning/phases/17-per-symbol-performance/EVIDENCE.md
autonomous: false
requirements: [TUNE-02]

must_haves:
  truths:
    - "scripts/symbol_report.py prints a markdown table to stdout and writes .planning/phases/17-per-symbol-performance/EVIDENCE.md"
    - "Flags: --bot, --window <days>, --min-sample (default 5), --json. `--apply` DOES NOT EXIST — there is no write path anywhere in the script"
    - "Default is FULL HISTORY (a short default would starve the sample guard); --window computes since = now(utc) - timedelta(days=N) and passes it as a %s param"
    - "Cells are annotated already_quarantined / off_universe by READING src.effective_universe.resolve_universe — never re-deriving set math; matched through normalize() on BOTH sides"
    - "The report RANKS by expectancy over `sufficient` cells only, and emits NO quarantine/disable verdict — Phase 18 pulls the trigger"
    - "A loud `null_pnl_total` summary line is printed even when zero"
    - "KNOWN LIMITATION printed: the per-bot roll-up will NOT equal db.get_realized_pnl(bot_id) when NULL-pnl rows exist (that function coerces them to 0.0). Print BOTH numbers side by side; do not reconcile it away"
    - "EVIDENCE.md generated read-only from a real trade log — LOCAL/TEST_DATABASE_URL only, NEVER prod"
  artifacts:
    - path: "scripts/symbol_report.py"
      provides: "the read-only TUNE-02 report CLI"
      contains: "def main() -> int"
      min_lines: 90
    - path: ".planning/phases/17-per-symbol-performance/EVIDENCE.md"
      provides: "per-symbol / per-bot winners+losers evidence for Phase 18"
      contains: "null_pnl_total"
  key_links:
    - from: "scripts/symbol_report.py"
      to: "src.db.get_resolved_trades"
      via: "the one read-only SELECT"
      pattern: "get_resolved_trades"
    - from: "scripts/symbol_report.py"
      to: "src.symbol_stats.aggregate"
      via: "the pure aggregator; three group keys, one code path"
      pattern: "aggregate\\("
    - from: "scripts/symbol_report.py"
      to: "src.effective_universe.resolve_universe"
      via: "already_quarantined / off_universe annotation, read not re-derived"
      pattern: "resolve_universe"
---

<objective>
Ship the read-only report CLI and generate the Phase-18 evidence.

Purpose: turn the trustworthy dataset into defensible per-symbol / per-bot numbers — winners and losers
as concrete figures, with the sample guard visible, the NULL-pnl defect count loud, and the
`get_realized_pnl` divergence printed rather than reconciled away. The report ANNOTATES; it does not
DECIDE. Phase 18 (TUNE-01/TUNE-03) consumes this and owns the `quarantined_symbols` write.
Output: scripts/symbol_report.py + EVIDENCE.md (turns VALIDATION cases 17-18 GREEN; case 16's fence must
stay green, and it now also asserts `--apply` is absent).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-per-symbol-performance/17-CONTEXT.md
@.planning/phases/17-per-symbol-performance/17-RESEARCH.md
@.planning/phases/17-per-symbol-performance/17-VALIDATION.md
@scripts/backfill_trades.py
@CLAUDE.md

<interfaces>
CLI (mirror scripts/backfill_trades.py STRUCTURALLY — module docstring with usage lines, argparse,
`main() -> int`, `sys.exit(main())`, fixed-width print table — but the write half is DELIBERATELY ABSENT):

  python scripts/symbol_report.py                       # all bots, FULL HISTORY (the default)
  python scripts/symbol_report.py --bot A --window 90
  python scripts/symbol_report.py --min-sample 10
  python scripts/symbol_report.py --json
  python -m scripts.symbol_report

  --bot         str,  optional     -> get_resolved_trades(bot_id=...)   [%s param, never f-string]
  --window      int,  optional     -> since = datetime.now(timezone.utc) - timedelta(days=N)
  --min-sample  int,  default 5    -> aggregate(min_sample=...)         [src.symbol_stats.MIN_SAMPLE]
  --json        flag               -> dump the cells as JSON instead of the table
  --apply       *** DOES NOT EXIST *** — there is no write path. (Pinned by VALIDATION case 16.)

Module-level helpers the tests import (cases 17/18):
  annotate(cells, bots_rows) -> cells        # stamps already_quarantined / off_universe
  render_markdown(cells, rollups, summary) -> str

Upstream surfaces (built in Plans 02/03 — call, do not reimplement):
  src.db.get_resolved_trades(bot_id=None, since=None) -> list[dict]
  src.symbol_stats.aggregate(rows, min_sample=MIN_SAMPLE, key=("bot_id","symbol")) -> list[dict]
      also invoked with key=("symbol",)  -> all-bots per-symbol roll-up
      and         key=("bot_id",)        -> per-bot roll-up
  src.effective_universe.resolve_universe(cfg) -> {..., "blocked": [{symbol, reason, ...}], ...}
      reason in {quarantined, off_universe, meme, untradeable}   [src/effective_universe.py:164-170]
  src.universe.normalize(symbol) -> str
  src.bot_config.BotConfig.from_row(row)
  src.db.get_realized_pnl(bot_id) -> float    # the DIVERGENT number — print it, do not adopt it

Annotation rule (17-CONTEXT decision 8, 17-RESEARCH §5):
  Build cfg per bot from a `SELECT ... FROM bots` — WITHOUT the `enabled = TRUE` filter (a disabled bot
  with historical trades must still get annotated; Open Question 1). A cell whose bot row is MISSING is
  stamped `annotation: "unavailable"`, never a silent blank.
  Index resolve_universe(cfg)["blocked"] by normalize(symbol); stamp:
    already_quarantined = (reason == "quarantined")
    off_universe        = (reason in {"off_universe", "meme", "untradeable"})
  Match through normalize() on BOTH sides.

Report sections (in order):
  1. Header: window (or "full history"), --min-sample, row counts.
  2. Per-(bot, symbol) table — EVERY cell, including `insufficient` ones (asterisked). Columns:
     bot | symbol (display) | trades | wins | losses | win_rate | realized_pnl | total_fees |
     avg_win | avg_loss | expectancy | best | worst | null_pnl | sample | quarantined | off_universe
  3. All-bots-per-symbol roll-up, then the per-bot roll-up.
  4. RANKING — best/worst by expectancy over `sufficient` cells ONLY. Insufficient cells are excluded
     from the RANKING, never from table 2.
  5. Summary lines (always printed, even when zero):
       null_pnl_total: N   (a non-zero count is a FINDING for Phase 18/20 — this phase does not fix it)
       null_fees_total: N  (qualifies the total_fees drag figure only)
     KNOWN LIMITATION, per bot, side by side:
       symbol_stats realized_pnl = X   |   db.get_realized_pnl() = Y   |   delta = Y - X  (= the NULL-pnl
       rows that get_realized_pnl coerces to 0.0). The divergence is CORRECT and INTENDED. Do not
       reconcile it away, and do NOT change get_realized_pnl.
  NO "quarantine"/"disable"/"remove" verdict string anywhere in the output. Rank, annotate, stop.

REMINDER — `total_fees` is DRAG DISCLOSURE. `alpaca_trades.pnl` is already net (src/pnl.py:28).
NEVER print or compute `realized_pnl - total_fees`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: scripts/symbol_report.py — read-only CLI, annotation, ranking, no verdict</name>
  <files>scripts/symbol_report.py</files>
  <read_first>scripts/backfill_trades.py L1-46 (the CLI convention to mirror — minus --apply); tests/test_symbol_stats.py cases 16-18 (the contract: no INSERT/UPDATE/DELETE/ALTER, no --apply, annotation via resolve_universe, no verdict string); src/effective_universe.py L115-203 (resolve_universe return shape + reason vocabulary); src/bot_config.py (BotConfig.from_row); src/reconciliation.py L51-59 (_enabled_bot_ids — the house bot-enumeration idiom, but note the enabled=TRUE caveat above); src/db.py::get_resolved_trades + get_realized_pnl</read_first>
  <behavior>
    - test_annotation_from_resolve_universe (case 17): a cell for a quarantined symbol -> already_quarantined
      True; an off-allowlist symbol (TRUMP/USD) -> off_universe True; a live allowlisted symbol -> both
      False. Matched through normalize() on both sides; the verdict comes from resolve_universe, never
      from re-derived set math.
    - test_no_verdict_emitted (case 18): render_markdown() ranks by expectancy over `sufficient` cells
      only (an insufficient cell with the worst expectancy is ABSENT from the ranking section but PRESENT
      in the full table), and the rendered text contains no case-insensitive "quarantine"/"disable"/
      "remove" verdict sentence.
    - test_phase17_is_read_only (case 16): the source contains no INSERT/UPDATE/DELETE/ALTER and no
      "--apply".
  </behavior>
  <action>Create scripts/symbol_report.py mirroring scripts/backfill_trades.py's shape (module docstring with the usage lines above, argparse, main() -> int, `if __name__ == "__main__": sys.exit(main())`). Flags exactly: --bot, --window (type=int), --min-sample (type=int, default src.symbol_stats.MIN_SAMPLE), --json. DO NOT add --apply, --fix, --write, or any other mutating flag; the docstring says so explicitly. main(): rows = get_resolved_trades(bot_id=args.bot, since=since); cells = aggregate(rows, min_sample=args.min_sample); by_symbol = aggregate(rows, min_sample=args.min_sample, key=("symbol",)); by_bot = aggregate(rows, min_sample=args.min_sample, key=("bot_id",)); annotate(cells, bots_rows) where bots_rows comes from a plain `SELECT * FROM bots` (no enabled filter); then print render_markdown(...) to stdout and Write it to .planning/phases/17-per-symbol-performance/EVIDENCE.md (pathlib, resolved from the repo root, not CWD). --json prints json.dumps of the cells + rollups + summary instead. Keep annotate() and render_markdown() at module level and pure (they take data, return data/str) so the tests can call them with fixtures and no DB. Enumerate report fields EXPLICITLY — never `**row` splat (a splat could leak an alpaca key from a bots row into a committed file). Construct NO AlpacaClient; make zero network calls. Print the null_pnl_total / null_fees_total lines unconditionally, and the per-bot KNOWN LIMITATION line comparing symbol_stats realized_pnl against db.get_realized_pnl(bot_id) with the delta. Zero INSERT/UPDATE/DELETE/ALTER.</action>
  <verify>
    <automated>python -m pytest tests/test_symbol_stats.py -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_symbol_stats.py -q` — ALL 18 cases green with TEST_DATABASE_URL set (case 14 not skipped); 17 green + 1 skipped without it.
    - `python scripts/symbol_report.py --help` lists exactly `--bot`, `--window`, `--min-sample`, `--json` (plus -h). `python scripts/symbol_report.py --apply` exits non-zero with `unrecognized arguments: --apply`.
    - `grep -c -- "--apply" scripts/symbol_report.py` prints `0`.
    - `grep -v '^\s*#' scripts/symbol_report.py | grep -ciE "\b(INSERT|UPDATE|DELETE|ALTER|DROP|TRUNCATE)\b"` prints `0`.
    - `grep -ciE "quarantine this|should quarantine|disable |remove " scripts/symbol_report.py` prints `0` (the `already_quarantined` column header is not a verdict).
    - `grep -cE "realized_pnl - |- *total_fees" scripts/symbol_report.py` prints `0` (fees never double-subtracted).
    - `python -m pytest tests/ dashboard/api/tests/ -q | tail -1` shows >= 373 passed, no new failures.
    - `git diff --stat` lists scripts/symbol_report.py ONLY.
  </acceptance_criteria>
  <done>A reproducible, read-only command produces the per-symbol/per-bot evidence, annotated with the gate's own answer, ranked but un-verdicted.</done>
</task>

<task type="auto">
  <name>Task 2: Generate EVIDENCE.md (read-only, LOCAL/TEST DB only)</name>
  <files>.planning/phases/17-per-symbol-performance/EVIDENCE.md</files>
  <read_first>17-VALIDATION.md "Nyquist Compliance" + "Known limitation"; 17-CONTEXT scope fences (READ-ONLY; never the prod DB)</read_first>
  <action>Point DATABASE_URL at a LOCAL/TEST Postgres carrying a copy of the trade log — NEVER the prod DATABASE_URL, and NEVER any credential that can write to prod. Run the report TWICE and capture both into EVIDENCE.md: (1) `python scripts/symbol_report.py` (full history) and (2) `python scripts/symbol_report.py --window 90`. Confirm the artifact contains: both tables, the winner/loser RANKING over `sufficient` cells, the `null_pnl_total` line (even if 0), the `null_fees_total` line, and the per-bot KNOWN LIMITATION comparison (symbol_stats realized_pnl vs db.get_realized_pnl vs delta). Prepend a short header stating the DB the numbers came from (label it local/test), the run timestamp, and one sentence: this report ranks and annotates; Phase 18 decides. Verify no key/secret material appears anywhere in the file before committing.</action>
  <verify>
    <automated>test -f .planning/phases/17-per-symbol-performance/EVIDENCE.md && grep -c "null_pnl_total" .planning/phases/17-per-symbol-performance/EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - EVIDENCE.md exists and contains `null_pnl_total`, `null_fees_total`, a ranking section, and both the full-history and --window 90 runs.
    - `grep -ciE "api[_-]?key|secret|token|password" .planning/phases/17-per-symbol-performance/EVIDENCE.md` prints `0`.
    - The per-bot KNOWN LIMITATION line is present with all three numbers (symbol_stats realized_pnl | db.get_realized_pnl | delta).
    - The DB used is stated in the header and is NOT prod.
  </acceptance_criteria>
  <done>Phase 18 has concrete, reproducible, honestly-qualified numbers to act on.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>scripts/symbol_report.py (read-only, no --apply) and EVIDENCE.md generated from a local/test copy of the trade log.</what-built>
  <how-to-verify>
    1. `python scripts/symbol_report.py --help` — confirm there is NO --apply flag.
    2. Open `.planning/phases/17-per-symbol-performance/EVIDENCE.md` and confirm:
       - the winners/losers ranking is over `sufficient` cells only;
       - `insufficient` cells are still visible in the full table (a 2-trade TRUMP-shaped leak must not be hidden);
       - `null_pnl_total` is present (a non-zero value is a FINDING for Phase 18/20, not a Phase-17 bug);
       - the per-bot KNOWN LIMITATION line shows symbol_stats realized_pnl vs db.get_realized_pnl vs delta;
       - no "quarantine X" verdict appears anywhere.
    3. Confirm the DB in the header is the LOCAL/TEST one, not prod.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| operator CLI args → SQL | `--bot` / `--window` cross into the query — `%s` params only |
| the report → git | EVIDENCE.md is committed; anything printed is published |
| this process → Postgres | read-only; evidence runs on a LOCAL/TEST DB, never prod |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Tampering | a write path (`--apply`, an UPDATE) reaches the trade log | mitigate | no mutating flag exists; case 16 static fence greps for --apply and INSERT/UPDATE/DELETE/ALTER |
| T-17-05 | Tampering | SQL injection via `--bot` | mitigate | passed to get_resolved_trades as a `%s` param; `--window` is `type=int` |
| T-17-09 | Information disclosure | an alpaca key from a `bots` row lands in the committed EVIDENCE.md | mitigate | report fields enumerated explicitly — no `**row` splat; Task 2 greps the artifact for key/secret/token |
| T-17-10 | Repudiation | the report emits a quarantine verdict and Phase 18 rubber-stamps it | mitigate | case 18 — rank + annotate only; no verdict string |
| T-17-11 | Repudiation | the get_realized_pnl divergence is silently reconciled away, hiding NULL-pnl defects | mitigate | both numbers + the delta printed side by side; get_realized_pnl is NOT modified |
| T-17-SC | Tampering | package installs | accept | none — no new packages |
</threat_model>

<verification>
- `python -m pytest tests/test_symbol_stats.py -q` — 18/18 green with TEST_DATABASE_URL (0 skipped).
- `python -m pytest tests/ dashboard/api/tests/ -q` — >= 373 passed / 9 skipped, no regressions.
- `python scripts/symbol_report.py --apply` -> `unrecognized arguments: --apply`.
- EVIDENCE.md committed, contains both runs + the null_pnl_total + the KNOWN LIMITATION comparison,
  and contains no secret material.
</verification>

<success_criteria>
A reproducible read-only command turns the reconciled trade log into per-symbol/per-bot winners and
losers with concrete numbers, marks thin samples without hiding them, states the NULL-pnl defect count
out loud, prints the `get_realized_pnl` divergence rather than papering over it — and decides nothing.
No bot behavior changed; no prod row touched; migration 019 still free.
</success_criteria>

<output>
Create `.planning/phases/17-per-symbol-performance/17-04-SUMMARY.md` when done.
</output>
