---
phase: 17-per-symbol-performance
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_symbol_stats.py
autonomous: true
requirements: [TUNE-02]

must_haves:
  truths:
    - "tests/test_symbol_stats.py exists with all 18 VALIDATION cases as RED tests (cases 1-13, 15-18 pure/zero-I/O; case 14 gated on TEST_DATABASE_URL)"
    - "Tests import the not-yet-existing surface (src.symbol_stats.aggregate, MIN_SAMPLE) so they fail RED on ImportError, not on malformed asserts"
    - "Case 10 (THE loudest trap) feeds rejected/canceled/cancelled/expired/open/submitted rows to aggregate() and demands trades == 0 AND losses == 0 — a Phase-15 gate block (pnl=0, bot_thread.py:309) must NEVER score as a loss"
    - "Case 11 pins NULL pnl on a position-closed row as EXCLUDED from trades/wins/losses/realized_pnl/expectancy AND counted into null_pnl — never coerced to 0.0 (the anti-get_alpaca_accuracy rule)"
    - "Case 4 asserts expectancy against BOTH expressions (win_rate*avg_win + (1-win_rate)*avg_loss AND realized_pnl/trades) with pytest.approx on a mixed win/loss/zero fixture"
    - "Case 5 asserts realized_pnl == sum(pnl) EXACTLY — fees are NOT subtracted again (src/pnl.py:28 already returned gross - fees)"
    - "Case 16 is a static read-only fence over src/symbol_stats.py, src/db.py::get_resolved_trades and scripts/symbol_report.py: no INSERT/UPDATE/DELETE/ALTER, and no --apply flag"
    - "Case 14 executes the real SELECT against a LOCAL/TEST Postgres only — never prod"
  artifacts:
    - path: "tests/test_symbol_stats.py"
      provides: "RED suite covering TUNE-02 cases 1-18"
      contains: "def test_nonposition_terminal_never_counts"
      min_lines: 260
  key_links:
    - from: "tests/test_symbol_stats.py"
      to: "src.symbol_stats.aggregate"
      via: "import + call the pure aggregator under test"
      pattern: "from src.symbol_stats import|aggregate\\("
    - from: "tests/test_symbol_stats.py"
      to: "src.universe.normalize"
      via: "case 9 cross-checks the group key against the gate's canonicalizer"
      pattern: "normalize"
    - from: "tests/test_symbol_stats.py"
      to: "src.db.get_resolved_trades"
      via: "case 14 executes the real SQL (TEST_DATABASE_URL-gated)"
      pattern: "get_resolved_trades"
---

<objective>
Write the RED test suite pinning the Phase-17 per-symbol-stats contract before any implementation.
Wave 0 — plans 02/03/04 turn these GREEN.

Purpose: Nyquist gate for TUNE-02. All 18 VALIDATION cases become executable specs. Three load-bearing
specs:
- Case 10 — a `rejected` row (Phase-15 gate block, written with `pnl=0` at `src/bot_thread.py:309`)
  must NEVER count as a trade or a loss. Left unfiltered, every quarantined symbol accrues a fake
  losing record, Phase 18 acts on that "evidence", quarantines more, and the lie feeds itself.
- Case 11 — NULL `pnl` on a position-closed row is a resolution defect, excluded and COUNTED, never
  coerced to 0.0. Coercing it (what `get_alpaca_accuracy` does) makes a broken row read as
  break-even — the exact class of lie this milestone kills.
- Case 4 — `expectancy == realized_pnl / trades` is the invariant that catches sign drift, NULL
  mishandling and denominator bugs in one assertion.
Output: tests/test_symbol_stats.py (RED).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-per-symbol-performance/17-CONTEXT.md
@.planning/phases/17-per-symbol-performance/17-RESEARCH.md
@.planning/phases/17-per-symbol-performance/17-VALIDATION.md
@CLAUDE.md

<interfaces>
Surface under test (built in Plans 02/03/04 — RED now).

  src.symbol_stats.MIN_SAMPLE = 5          # module constant, the default for the kwarg

  src.symbol_stats.aggregate(
      rows,                                 # list[dict] — psycopg3 dict_row shape (src/db.py:29),
                                            #   so a plain dict fixture IS a row
      min_sample: int = MIN_SAMPLE,
      key: tuple[str, ...] = ("bot_id", "symbol"),
  ) -> list[dict]

  Per-cell fields:
    bot_id, symbol (NORMALIZED), display (first-seen raw spelling), asset_class,
    trades, wins, losses, win_rate, realized_pnl, total_fees, avg_win, avg_loss,
    expectancy, best, worst, first_trade, last_trade, null_pnl, null_fees, sample

  Locked definitions (17-CONTEXT decisions 2/3/7):
    win            pnl > 0
    loss           pnl <= 0, INCLUDING pnl == 0.0        (flat is not a win — pnl is already fee-net)
    trades         rows with non-NULL pnl in the cell
    realized_pnl   sum(pnl)                              (ALREADY net of fees — NEVER subtract fees again)
    total_fees     sum(fees), NULL fees -> 0.0, counted into null_fees   (drag disclosure ONLY)
    avg_win        sum(pnl>0)/wins, 0.0 when wins == 0
    avg_loss       sum(pnl<=0)/losses, 0.0 when losses == 0 — carried NEGATIVE
    expectancy     win_rate*avg_win + (1-win_rate)*avg_loss  ==  realized_pnl / trades   [INVARIANT]
    NULL pnl       EXCLUDED from every statistic; counted into null_pnl; NEVER `pnl or 0.0`
    sample         "sufficient" if trades >= min_sample else "insufficient" — cell is STILL EMITTED
    zero denoms    every ratio 0.0; aggregate([]) == []; no ZeroDivisionError anywhere

  src.db.get_resolved_trades(bot_id: str | None = None, since=None) -> list[dict]
    One read-only parameterized SELECT over alpaca_trades.
    WHERE status IN ('closed','stopped','target_hit')      [src/db.py:215, :256-257]
    optional  AND bot_id = %s
    optional  AND "timestamp"::timestamptz >= %s           [timestamp is TEXT — src/db_schema.sql:28]
    Returns: bot_id, symbol, asset_class, side, status, pnl, fees, entry_ts, closed_at

Existing surfaces the tests exercise (do NOT change them in this plan):
  src.universe.normalize(symbol) -> str   # "BTC/USD"/"btc/usd"/"BTCUSD" -> "BTCUSD"; total. [src/universe.py:17]
  src.effective_universe.resolve_universe(cfg, ...) -> dict with blocked=[{symbol, reason, ...}];
      reason in {quarantined, off_universe, meme, untradeable}. [src/effective_universe.py:115]
  src.bot_config.BotConfig — frozen dataclass, construct directly (alpaca keys may be "").

Terminal sets (copy the literals, do not invent a third spelling):
  position-closed (THE SAMPLE) : 'closed', 'stopped', 'target_hit'       [src/db.py:215]
  terminal non-position        : 'canceled','cancelled','expired','rejected'  [src/order_resolution.py:11]
  non-terminal                 : 'open', 'submitted'

WHY case 10 is the loudest trap: src/bot_thread.py:309,:317,:332 all call
update_alpaca_trade(trade_id, "rejected", pnl=0) — a rejected row carries pnl = 0, NOT NULL. Under the
locked win definition (pnl == 0.0 is a LOSS), any rejected row reaching aggregate() scores as a
zero-P&L LOSS. Post-Phase-15 the gate emits exactly one such row per blocked entry, for exactly the
symbols an operator quarantined.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — core math + the two traps (cases 1-5, 10, 11, 11b, 13)</name>
  <files>tests/test_symbol_stats.py</files>
  <read_first>17-VALIDATION.md (the 18-case table — use the EXACT test names in it); src/db.py L211-241 (get_alpaca_accuracy — the win definition to mirror and the `or 0` coercion to NOT mirror) and L246-259 (get_realized_pnl — the terminal set of record); src/pnl.py L10-28 (returns gross - fees, i.e. alpaca_trades.pnl is ALREADY net); src/bot_thread.py L305-335 (the three rejected-with-pnl=0 writes); src/order_resolution.py L11 (_TERMINAL_NONPOSITION); tests/test_pnl.py (pure-fixture + RED-first convention)</read_first>
  <behavior>
    - Add a `_row(**over)` helper returning a plain dict with the get_resolved_trades key set
      (bot_id, symbol, asset_class, side, status, pnl, fees, entry_ts, closed_at); default
      status="closed", bot_id="A", symbol="BTC/USD", asset_class="crypto", fees=1.0.
    - test_win_definition_positive (case 1): pnl=10.0 -> wins==1, losses==0, win_rate==1.0.
    - test_zero_pnl_is_a_loss (case 2): pnl=0.0 on a `closed` row -> losses==1, wins==0. It is a LOSS —
      not a win, and NOT "excluded". Docstring: flat is not a win after fees (pnl is already fee-net).
    - test_avg_loss_is_negative (case 3): pnl=-20.0 and -10.0 -> avg_loss == -15.0 (NEGATIVE, not 15.0).
      Sign discipline is the precondition for case 4.
    - test_expectancy_invariant (case 4, LOAD-BEARING): mixed fixture (e.g. pnl = +30, +10, 0.0, -20, -5)
      -> assert expectancy == pytest.approx(win_rate*avg_win + (1-win_rate)*avg_loss) AND
      expectancy == pytest.approx(realized_pnl / trades). Both expressions, same cell.
    - test_fees_not_double_subtracted (case 5): fixture pnl=[100.0, -40.0], fees=[3.0, 2.0] ->
      realized_pnl == pytest.approx(60.0) EXACTLY (NOT 55.0); total_fees == pytest.approx(5.0).
      A row with fees=None -> contributes 0.0 to total_fees and 1 to null_fees, while its pnl still counts.
    - test_nonposition_terminal_never_counts (case 10, THE LOUDEST TRAP): feed aggregate() rows with
      status in ("rejected","canceled","cancelled","expired","open","submitted"), each with pnl=0 (the
      literal shape bot_thread.py:309 writes) -> for every emitted cell (if any) trades == 0 AND
      losses == 0 AND realized_pnl == 0.0. Then a MIXED fixture: 3 'closed' rows + 4 'rejected' rows on
      the SAME symbol -> trades == 3, losses counts only the closed losers. Docstring must record WHY:
      a Phase-15 gate block scoring as a loss makes Phase 18 quarantine on fabricated evidence.
    - test_null_pnl_excluded_and_counted (case 11): a `closed` row with pnl=None alongside two real rows
      -> trades == 2 (NOT 3), null_pnl == 1, the None row contributes to NEITHER wins NOR losses NOR
      realized_pnl NOR expectancy, and expectancy still equals realized_pnl/trades. Explicitly assert
      losses does NOT include it (i.e. it was never coerced to 0.0 and re-read as a case-2 loss).
    - test_all_null_pnl_cell (case 11b): a cell whose rows are ALL NULL-pnl -> trades == 0,
      null_pnl == n, win_rate/expectancy/avg_win/avg_loss all 0.0, sample == "insufficient", and the
      cell IS STILL EMITTED (the defect is loud, not silent).
    - test_empty_and_zero_denominators (case 13): aggregate([]) == []; an all-wins cell -> avg_loss == 0.0;
      an all-losses cell -> avg_win == 0.0; assert no ZeroDivisionError is raised anywhere.
  </behavior>
  <action>Create tests/test_symbol_stats.py. Import `from src.symbol_stats import aggregate, MIN_SAMPLE` at module top — this ImportError IS the RED proof (same convention as tests/test_pnl.py:11). Fixtures are plain dicts (psycopg3 row_factory=dict_row, src/db.py:29 — a dict IS a row); zero DB, zero network, no Alpaca fake needed. Use the exact test names from 17-VALIDATION.md. Do NOT write production code in this plan.</action>
  <verify>
    <automated>python -m pytest tests/test_symbol_stats.py -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - File collects (no SyntaxError). Run fails RED with `ModuleNotFoundError: No module named 'src.symbol_stats'` — NOT with AssertionError/NameError from malformed asserts.
    - Case 4 asserts BOTH expectancy expressions with pytest.approx.
    - Case 5 asserts realized_pnl == 60.0 (not 55.0) on the stated fixture.
    - Case 10 asserts trades == 0 AND losses == 0 for every non-position status, and 3-of-7 on the mixed fixture.
    - Case 11 asserts trades == 2, null_pnl == 1, and that losses did NOT absorb the NULL row.
    - `grep -n "test_zero_pnl_is_a_loss\|test_expectancy_invariant\|test_nonposition_terminal_never_counts\|test_null_pnl_excluded_and_counted" tests/test_symbol_stats.py` returns 4 lines.
  </acceptance_criteria>
  <done>The win/loss/expectancy math, the gate-block trap and the NULL-pnl rule are executable RED specs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED — sample guard, normalization, roll-ups, annotation, no-verdict (cases 6-9, 15, 17, 18)</name>
  <files>tests/test_symbol_stats.py</files>
  <read_first>src/universe.py L17-23 (normalize — the group key of record); src/effective_universe.py L115-203 (resolve_universe return shape; blocked[].reason vocabulary); src/bot_config.py L72-100 (symbols / quarantined / all_symbols); tests/test_effective_universe.py (Phase-16 BotConfig-construction + zero-I/O conventions to copy)</read_first>
  <behavior>
    - test_min_sample_threshold (case 6): MIN_SAMPLE == 5; a cell with 4 trades -> sample == "insufficient";
      a cell with 5 -> sample == "sufficient".
    - test_insufficient_is_marked_not_hidden (case 7): the 4-trade cell IS PRESENT in the returned list
      with full stats (trades/realized_pnl/expectancy populated) — only its eligibility to be RANKED is
      lost. Docstring: hiding it would hide a TRUMP-shaped leak, the exact failure the guard must not cause.
    - test_min_sample_is_a_kwarg (case 8): the SAME fixture with min_sample=10 re-stamps a 5-trade cell as
      "insufficient"; the module constant MIN_SAMPLE is merely the default. Phase 18 raises it without an edit here.
    - test_normalization_collapses_slash (case 9): rows spelled "BTC/USD", "BTCUSD" and "btc/usd" ->
      exactly ONE cell, symbol == "BTCUSD" (== normalize("BTC/USD")), trades == the sum, and
      display == "BTC/USD" (the first-seen raw spelling). Assert len([c for c in out if c["bot_id"]=="A"]) == 1.
    - test_rollup_by_key (case 15): the same aggregate() over one fixture with
      key=("bot_id","symbol") / key=("symbol",) / key=("bot_id",) yields cells, all-bots-per-symbol rows,
      and per-bot rows. Assert the per-bot roll-up's realized_pnl == sum of non-NULL pnl for that bot, and
      that summing the ("bot_id","symbol") cells' realized_pnl reproduces the ("bot_id",) roll-up.
    - test_annotation_from_resolve_universe (case 17): build a BotConfig with
      quarantined_symbols="AVAX/USD" and crypto_universe="BTC/USD,SOL/USD,AVAX/USD"; call
      resolve_universe(cfg) and index blocked by normalize(symbol). Assert the helper under test stamps an
      AVAX cell already_quarantined=True, a TRUMP/USD cell (off the allowlist) off_universe=True, and a
      BTC cell False/False. Match through normalize() on BOTH sides. The annotation is READ from
      resolve_universe — never re-derived.
    - test_no_verdict_emitted (case 18): the rendered report text ranks by expectancy over `sufficient`
      cells ONLY (an insufficient cell with the worst expectancy is absent from the ranking section while
      still present in the full table), and the rendered text contains NO "quarantine"/"disable"/"remove"
      verdict string (case-insensitive) outside the `already_quarantined` COLUMN HEADER. Phase 18 pulls the
      trigger, not Phase 17.
  </behavior>
  <action>Extend tests/test_symbol_stats.py. Cases 17/18 import the renderer/annotator surface built in Plan 04 (e.g. `from scripts.symbol_report import annotate, render_markdown`) — keep those imports INSIDE the test functions so the module-level ImportError of Task 1 stays the single clean RED signal. Construct BotConfig directly (no DB). Zero I/O throughout.</action>
  <verify>
    <automated>python -m pytest tests/test_symbol_stats.py -q -k "sample or normal or rollup or annotation or verdict" 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Case 9 asserts ONE cell keyed "BTCUSD" with display "BTC/USD".
    - Case 7 asserts the insufficient cell is in the returned list (not filtered out).
    - Case 15 asserts all three group keys through the SAME function.
    - Case 17 goes through resolve_universe + normalize on both sides; no re-derived set math.
    - Case 18 asserts the absence of a verdict string in the rendered output.
    - Zero imports of psycopg / src.db / src.alpaca_orchestrator in these cases.
  </acceptance_criteria>
  <done>The sample guard, group key, roll-ups, annotation and the annotate-don't-decide fence are executable RED specs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RED — real-SQL case 14 (DB-gated) + the static read-only fence (case 16)</name>
  <files>tests/test_symbol_stats.py</files>
  <read_first>tests/test_universe.py L415-432 (the DATABASE_URL-gated real-SQL idiom to copy verbatim); src/db.py L20-40 (pool + dict_row) and L151-172 (the "timestamp"::timestamptz cast precedent); src/db_schema.sql L24-51 (alpaca_trades — timestamp TEXT at :28, pnl/fees NULLable)</read_first>
  <behavior>
    - test_get_resolved_trades_sql (case 14, the ONLY execution path the SELECT ever gets):
      `@pytest.mark.skipif(not os.environ.get("TEST_DATABASE_URL"), reason="needs local Postgres")`.
      Set DATABASE_URL from TEST_DATABASE_URL inside the test (never point it at prod). Seed a LOCAL/TEST
      Postgres with ON CONFLICT DO NOTHING: one 'closed' row (pnl=12.5, fees=0.4), one 'rejected' row
      (pnl=0), one 'closed' row entered 200 days ago; alpaca_trades.timestamp is TEXT — seed an ISO-8601
      string (datetime.now(timezone.utc).isoformat()). Then assert:
        (a) get_resolved_trades() returns dicts whose key set is EXACTLY
            {bot_id, symbol, asset_class, side, status, pnl, fees, entry_ts, closed_at};
        (b) NO row with status 'rejected' appears (the terminal filter runs in SQL);
        (c) get_resolved_trades(since=datetime.now(timezone.utc) - timedelta(days=90)) excludes the
            200-day-old row and includes the fresh one — the "timestamp"::timestamptz cast executes
            without error against the TEXT column;
        (d) get_resolved_trades(bot_id="A") returns only bot A's rows.
      Seeding writes to the TEST DATABASE ONLY. NEVER the prod DB.
    - test_phase17_is_read_only (case 16, the static fence — runs with NO DB): read the three source files
      from disk via pathlib resolved from the repo root (not CWD):
        src/symbol_stats.py, scripts/symbol_report.py, and the get_resolved_trades function body sliced
        out of src/db.py.
      Assert, over comment-stripped source (drop lines whose stripped form starts with '#' so this test's
      own explanatory prose can never self-invalidate the gate):
        (a) no case-insensitive match for r"\b(INSERT|UPDATE|DELETE|ALTER|DROP|TRUNCATE)\b" in any of the three;
        (b) "--apply" does NOT appear anywhere in scripts/symbol_report.py;
        (c) src/symbol_stats.py imports nothing from src.db / psycopg (the pure module stays I/O-free).
  </behavior>
  <action>Append both cases to tests/test_symbol_stats.py. Case 14 gets its OWN skipif — do NOT put it at module level, the 17 pure cases must run with no DB. Import src.db lazily inside case 14 (src/db.py:22 reads os.environ["DATABASE_URL"], albeit lazily in _create_pool). Case 16 needs no DB and no import of the modules under test — it reads them as TEXT, so it stays meaningful even while they are still missing (assert the file exists, then fence it). Do NOT write production code.</action>
  <verify>
    <automated>python -m pytest tests/test_symbol_stats.py -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Without TEST_DATABASE_URL: case 14 SKIPs cleanly; the other 17 collect and fail RED on the missing module.
    - With TEST_DATABASE_URL pointed at a LOCAL Postgres: case 14 executes real SQL (currently RED on the missing db.get_resolved_trades).
    - Case 16 strips comments before grepping (no bare unfiltered `== 0` gate on raw file text).
    - `python -m pytest tests/ dashboard/api/tests/ -q` shows NO new failures outside tests/test_symbol_stats.py (baseline: 373 passed / 9 skipped).
  </acceptance_criteria>
  <done>The SELECT has a real execution path, and the read-only fence is enforced by a test rather than a reviewer's memory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test process → Alpaca | none — Phase 17 constructs no Alpaca client and makes zero network calls |
| gated test → Postgres | TEST_DATABASE_URL-gated, LOCAL only; skipped by default; NEVER the prod DB |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Repudiation | a Phase-15 gate block (`rejected`, pnl=0) scores as a losing trade and Phase 18 quarantines on fabricated evidence | mitigate | case 10 — every non-position status fed to aggregate() must yield trades == 0 AND losses == 0 |
| T-17-02 | Repudiation | a NULL-pnl resolution defect is coerced to 0.0 and reads as a break-even trade | mitigate | case 11 / 11b — excluded, counted into null_pnl, loudly reported |
| T-17-03 | Repudiation | fees double-subtracted, fabricating a loss on every trade | mitigate | case 5 — realized_pnl == sum(pnl) EXACTLY; total_fees is drag disclosure only |
| T-17-04 | Tampering | a write path reaches the prod trade log | mitigate | case 16 — static fence: no INSERT/UPDATE/DELETE/ALTER, no `--apply` |
| T-17-05 | Tampering | SQL injection via `--bot` / `--window` | mitigate | psycopg `%s` params only — never f-string; pinned by case 14 |
| T-17-SC | Tampering | package installs | accept | no new packages — pytest 8.3.5 already installed |
</threat_model>

<verification>
- `python -m pytest tests/test_symbol_stats.py -q` collects all 18 cases; 17 fail RED on
  `ModuleNotFoundError: No module named 'src.symbol_stats'`; case 14 SKIPs without TEST_DATABASE_URL.
- `python -m pytest tests/ dashboard/api/tests/ -q` shows no NEW failures outside
  tests/test_symbol_stats.py (baseline 373 passed / 9 skipped).
</verification>

<success_criteria>
All 18 TUNE-02 cases are executable RED specs. Case 10 makes it impossible to ship an aggregator that
scores a gate block as a loss. Case 11 makes it impossible to ship one that launders a resolution defect
into a break-even trade. Case 5 makes it impossible to ship one that double-charges fees. Case 16 makes
it impossible to ship a write path.
</success_criteria>

<output>
Create `.planning/phases/17-per-symbol-performance/17-01-SUMMARY.md` when done.
</output>
