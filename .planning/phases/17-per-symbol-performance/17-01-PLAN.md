---
phase: 17-per-symbol-performance
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_symbol_stats.py
autonomous: true
requirements: [TUNE-02]
revision: 3
revision_reason: "plan-check Rev-3: B1 pool rebind + live-connection positive control (env-string check is NOT a prod fence), B2 the count/rate divergence test, B3 docstring-stripping fence + argparse-surface --apply check, W3/W5 — see 17-VALIDATION.md Revision 3 table"

must_haves:
  truths:
    - "tests/test_symbol_stats.py exists with all 23 test functions from 17-VALIDATION Revision 3, each case assigned to exactly one task (no orphan case)"
    - "Tests import the not-yet-existing surface (src.symbol_stats.aggregate, MIN_SAMPLE) so they fail RED on ImportError, not on malformed asserts"
    - "B1 (Rev2) — a `closed` row with pnl=0.0 (src/alpaca_orchestrator.py:167-176 external-exit sentinel) is bucketed into zero_pnl: NOT a win, NOT a loss, NOT in trades. Cases 2 / 3."
    - "B2 (Rev2) — a COUNTED row with fees IS NULL is flagged gross_pnl_rows; a zero/null-pnl row with NULL fees counts into null_fees but NOT gross_pnl_rows (R3-W1: distinct sets). Case 7."
    - "R3-B1 (CRITICAL) — case 18 REBINDS src.db._pool (close it, set to None, repoint DATABASE_URL) and asserts the LIVE connection's info.dbname/info.host match the parsed TEST_DATABASE_URL BEFORE its first INSERT, and resets _pool on entry and exit. An env-string comparison is NOT a prod fence: src/db.py:18,40-45 caches a process-global pool bound at first use."
    - "R3-B2 — case 17 proves the real divergence is in the COUNTS: resolved - trades == zero_pnl + null_pnl, and win_rate != naive_win_rate (get_alpaca_accuracy books zeros and NULLs as LOSSES, src/db.py:228-229). The SUMS agree by construction."
    - "R3-B3 — case 20's fence strips `#` lines AND DOCSTRINGS before the mutating-keyword regex (a fence that fires on prose is a prose-detector), and checks --apply on the ARGPARSE SURFACE, not raw text"
    - "B5 (Rev2) — case 20 carries a positive control (slice non-empty AND contains 'SELECT bot_id, symbol') BEFORE the negative regex, plus case 21 proving the same helper FAILS on update_alpaca_trade"
    - "W3 (Rev2) — case 23 asserts the ranking list contains ONLY sample=='sufficient' cells (structural), not the absence of verdict words"
  artifacts:
    - path: "tests/test_symbol_stats.py"
      provides: "RED suite covering TUNE-02, 17-VALIDATION Revision 3 (23 test functions)"
      contains: "def test_zero_pnl_is_bucketed_not_a_loss"
      min_lines: 340
  key_links:
    - from: "tests/test_symbol_stats.py"
      to: "src.symbol_stats.aggregate"
      via: "import + call the pure aggregator under test"
      pattern: "from src.symbol_stats import|aggregate\\("
    - from: "tests/test_symbol_stats.py"
      to: "src.universe.normalize"
      via: "case 11 cross-checks the group key against the gate's canonicalizer"
      pattern: "normalize"
    - from: "tests/test_symbol_stats.py"
      to: "src.db._pool"
      via: "case 18 rebinds the process-global pool to TEST_DATABASE_URL and proves it on the live connection before seeding"
      pattern: "_pool"
---

<objective>
Write the RED test suite pinning the Phase-17 per-symbol-stats contract before any implementation.
Wave 0 — plans 02/03/04 turn these GREEN.

Purpose: Nyquist gate for TUNE-02. Six load-bearing specs. Four of them exist because plan-check proved an
earlier premise FALSE against the repo:

- **Cases 2 / 3 (Rev-2 B1)** — `src/alpaca_orchestrator.py:167-176` marks every externally-exited position
  `status="closed", pnl=0.0`. That row **PASSES** the terminal-status filter and is **not NULL**. Scoring
  `pnl == 0.0` as a loss would fabricate a losing record for every one of them. `pnl == 0.0` goes to a
  **`zero_pnl` bucket — never scored**.
- **Case 7 (Rev-2 B2 + R3-W1)** — `src/bot_c/strategy.py:393-395` and `src/trend_strategy.py:172-173` store
  **GROSS** `pnl` and pass **no `fees`**, so `fees` lands NULL. NULL `fees` on a **counted** row is the
  **TELL** — flag it `gross_pnl_rows`, disclose it, never subtract fees. `null_fees` is the WIDER set (it
  includes zero/null-pnl rows); the two counters must be able to diverge.
- **Case 17 (R3-B2 — NEW)** — the P&L **sum** agrees with `get_realized_pnl` **by construction**; a delta
  print would be identically `0.00` and would tell the operator the data is clean. The real damage is in the
  **denominator**: `src/db.py:228-229` `losses = resolved - wins` books **every sentinel zero and every NULL
  as a LOSS**. Case 17 pins that divergence so the report cannot lie.
- **Case 18 (R3-B1 — CRITICAL, prod safety)** — `src/db.py:18` `_pool` is a **process-global** bound to
  `DATABASE_URL` at first use (`:22`, `:40-45`), and three existing `DATABASE_URL`-gated tests already bind
  it in the same pytest process. Asserting two env **strings** differ does **not** control which database the
  connection opens. The test must **rebind the pool** and prove it on the **live connection** before it
  INSERTs anything.
- **Cases 20 / 21 (Rev-2 B5 + R3-B3)** — the read-only fence must **FIRE**, and must fire on **code**, not on
  **prose**. Strip docstrings; check `--apply` on the argparse surface; self-test against
  `update_alpaca_trade`.

Output: tests/test_symbol_stats.py (RED).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-per-symbol-performance/17-CONTEXT.md
@.planning/phases/17-per-symbol-performance/17-RESEARCH.md
@.planning/phases/17-per-symbol-performance/17-VALIDATION.md
@CLAUDE.md

<interfaces>
Surface under test (built in Plans 02/03/04 — RED now).

  src.symbol_stats.MIN_SAMPLE = 5          # module constant, the default for the kwarg

  src.symbol_stats.aggregate(
      rows,                                 # list[dict] — psycopg3 dict_row shape (src/db.py:29);
                                            #   a plain dict fixture IS a row
      min_sample: int = MIN_SAMPLE,
      key: tuple[str, ...] = ("bot_id", "symbol"),
  ) -> list[dict]

  Per-cell fields:
    bot_id, symbol (NORMALIZED), display (first-seen raw spelling), asset_class,
    trades, wins, losses, win_rate, realized_pnl, total_fees, avg_win, avg_loss,
    expectancy, best, worst, first_trade, last_trade,
    zero_pnl, null_pnl, gross_pnl_rows, null_fees, sample

  BUCKETING — a position-closed row lands in exactly ONE bucket (17-VALIDATION Rev 3):
    win        pnl > 0        counted
    loss       pnl < 0        counted
    zero_pnl   pnl == 0.0     NOT counted   <- indistinguishable from the external-exit sentinel
    null_pnl   pnl is None    NOT counted   <- resolution defect; never `or 0.0`
    trades = wins + losses    (excludes zero_pnl AND null_pnl)

  FEE FLAGS — DISTINCT SETS (R3-W1):
    null_fees        ALL rows in the cell with `fees is None` (incl. zero_pnl / null_pnl rows)
    gross_pnl_rows   the COUNTED SUBSET (win/loss only) with `fees is None` — their pnl is probably GROSS
                     (bot_c/strategy.py:393-395, trend_strategy.py:172-173 store gross and pass no fees)
    gross_pnl_rows <= null_fees ALWAYS. total_fees is INCOMPLETE drag disclosure — NEVER subtracted.

  realized_pnl   sum(pnl) over counted rows      (NEVER minus total_fees)
  avg_win        sum(pnl>0)/wins,   0.0 if wins == 0
  avg_loss       sum(pnl<0)/losses, 0.0 if losses == 0 — carried NEGATIVE
  expectancy     win_rate*avg_win + (1-win_rate)*avg_loss  ==  realized_pnl / trades   [INVARIANT]
  sample         "sufficient" if trades >= min_sample else "insufficient" — cell STILL EMITTED
  zero denoms    every ratio 0.0; aggregate([]) == []; no ZeroDivisionError

  src.db.get_resolved_trades(bot_id: str | None = None, since=None) -> list[dict]
    One read-only parameterized SELECT over alpaca_trades.
    WHERE status IN ('closed','stopped','target_hit')        [src/db.py:215]
    optional  AND bot_id = %s
    optional  AND "timestamp"::timestamptz >= %s             [timestamp is TEXT — src/db_schema.sql:28]
    ORDER BY "timestamp"::timestamptz ASC                    [Rev-2 W1 — not a lexicographic TEXT sort]
    Returns: bot_id, symbol, asset_class, side, status, pnl, fees, entry_ts, closed_at

Existing surfaces the tests exercise (do NOT change them):
  src.universe.normalize(symbol) -> str          # src/universe.py:17
  src.effective_universe.resolve_universe(cfg)   # src/effective_universe.py:115; blocked[].reason
  src.bot_config.BotConfig                       # frozen dataclass; construct directly

THE POOL IS A PROCESS-GLOBAL (R3-B1 — read this before writing case 18):
  src/db.py:18    _pool: ConnectionPool | None = None
  src/db.py:22    url = os.environ["DATABASE_URL"]        # read ONCE, at first use
  src/db.py:40-45 def get_pool(): if _pool is None: _pool = _create_pool()
  tests/test_db.py:10, tests/test_reconciliation.py:165, tests/test_backfill.py:466 are DATABASE_URL-gated
  and drive src.db IN THE SAME PYTEST PROCESS. With DATABASE_URL=prod, _pool is ALREADY BOUND TO PROD before
  case 18 runs. Monkeypatching the env var afterwards does NOTHING.

Terminal sets (copy the literals; this repo already has FOUR spellings — do not add a fifth):
  position-closed (THE SAMPLE) : 'closed', 'stopped', 'target_hit'            [src/db.py:215]
  terminal non-position        : 'canceled','cancelled','expired','rejected'  [src/order_resolution.py:11]
  non-terminal                 : 'open', 'submitted'
  (live in the cooldown, NOT ours to fix): 'closed','stopped'                 [src/db.py:201]
  NOTE (R3-W4): 'stopped' and 'target_hit' have ZERO writers — every real row will be 'closed'.

THE TWO ZERO-PNL WRITERS — both real, both verified:
  1. src/bot_thread.py:309,317,332      -> status='rejected', pnl=0     (non-position; the SQL filter drops it)
  2. src/alpaca_orchestrator.py:167-176 -> status='closed',   pnl=0.0   (POSITION-CLOSED — the filter does
     NOT drop it. Same shape at bot_c/strategy.py:393 and trend_strategy.py:172 when entry_price == 0.)

ALL COMMANDS ARE `python -m pytest`, FROM THE REPO ROOT (R3-W5): there is no scripts/__init__.py and no
pytest.ini/setup.cfg/pyproject.toml — a bare `pytest` cannot import `scripts.symbol_report`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — buckets, the two fabricated-loss traps, the fee split, the naive-accuracy divergence (cases 1-7, 12-15, 17)</name>
  <files>tests/test_symbol_stats.py</files>
  <read_first>17-VALIDATION.md Revision 3 (the 23-case table + the B1/B2 explainers — use the EXACT test names); src/alpaca_orchestrator.py L160-180 (the external-exit sentinel: status="closed", pnl=0.0) and L305-320 (the NET writer); src/bot_c/strategy.py L388-396 and src/trend_strategy.py L166-174 (GROSS pnl, NO fees arg, NO side handling); src/db.py L101-122 (update_alpaca_trade — fees defaults to None and is written NULL), L211-241 (get_alpaca_accuracy — `wins = sum(1 for r if (pnl or 0) > 0)`, `losses = resolved - wins`, `avg_pnl = total_pnl / resolved` — the arithmetic case 17 must reproduce as `naive_*`) and L246-259 (get_realized_pnl — `sum((pnl or 0.0))`); src/bot_thread.py L305-335 (the three rejected-with-pnl=0 writes); tests/test_pnl.py (pure-fixture + RED-first convention)</read_first>
  <behavior>
    - `_row(**over)` helper -> plain dict with the get_resolved_trades key set (bot_id, symbol, asset_class,
      side, status, pnl, fees, entry_ts, closed_at); defaults status="closed", bot_id="A",
      symbol="BTC/USD", asset_class="crypto", side="buy", fees=1.0.
    - test_win_definition_positive (case 1): pnl=10.0 -> wins==1, losses==0, win_rate==1.0.
    - test_zero_pnl_is_bucketed_not_a_loss (case 2): a `closed` row with pnl=0.0 ->
      losses == 0 AND wins == 0 AND trades == 0 AND zero_pnl == 1 AND realized_pnl == 0.0.
      Docstring cites src/alpaca_orchestrator.py:167-176 — a genuine flat trade is INDISTINGUISHABLE from
      the external-exit sentinel, so neither is scored.
    - test_external_exit_sentinel_not_a_loss (case 3): the literal sentinel fixture
      (status="closed", exit_price == entry_price, pnl=0.0) alongside 2 real losers on the SAME symbol ->
      losses == 2 (NOT 3), trades == 2, zero_pnl == 1. Also cover the bot_c/trend shape (entry_price == 0
      -> pnl = 0.0 on a closed row). Without this, EVERY externally-exited position fabricates a losing
      record and the status filter cannot catch it.
    - test_avg_loss_is_negative (case 4): pnl=-20.0, -10.0 -> avg_loss == -15.0 (NEGATIVE).
    - test_expectancy_invariant (case 5): fixture mixing wins, losses, ONE 0.0 and ONE None
      (e.g. +30, +10, 0.0, None, -20, -5) -> expectancy == approx(win_rate*avg_win + (1-win_rate)*avg_loss)
      AND == approx(realized_pnl / trades), with trades == 4 (the 0.0 and the None excluded from BOTH sides).
    - test_fees_not_double_subtracted (case 6): pnl=[100.0,-40.0], fees=[3.0,2.0] ->
      realized_pnl == approx(60.0) EXACTLY (NOT 55.0); total_fees == approx(5.0).
    - test_null_fees_vs_gross_pnl_rows (case 7, R3-W1 — THE TWO COUNTERS MUST BE DISTINCT SETS): fixture with
      (a) a WIN with fees=None, (b) a WIN with fees=2.0, (c) a zero_pnl row with fees=None, (d) a null_pnl row
      with fees=None -> trades == 2, gross_pnl_rows == 1 (only the COUNTED fees=None row),
      null_fees == 3 (ALL fees=None rows), total_fees == approx(2.0), and assert
      `gross_pnl_rows < null_fees` — proving they are not the same counter twice. Docstring cites
      bot_c/strategy.py:393-395 and trend_strategy.py:172-173: NULL fees is the TELL that pnl is GROSS;
      total_fees must not be read as "this bot paid $0 drag".
    - test_nonposition_terminal_never_counts (case 12 — NON-VACUOUS): rows with status in
      ("rejected","canceled","cancelled","expired","open","submitted"), each pnl=0 ->
      `assert aggregate(rows) == []` — an EMPTY LIST, asserted explicitly. THEN the mixed fixture: 3 'closed'
      + 4 'rejected' on the same symbol -> trades == 3 AND the rejected rows land in NO bucket (zero_pnl == 0
      too — they are dropped BEFORE bucketing). Docstring: a Phase-15 gate block scoring as a loss makes
      Phase 18 quarantine on fabricated evidence.
    - test_null_pnl_excluded_and_counted (case 13): a `closed` row with pnl=None + two real rows ->
      trades == 2, null_pnl == 1, zero_pnl == 0 (None and 0.0 are DIFFERENT buckets — it was never coerced),
      losses does NOT include it, expectancy still == realized_pnl/trades.
    - test_all_null_pnl_cell (case 14): all rows NULL-pnl -> trades == 0, null_pnl == n, every ratio 0.0,
      sample == "insufficient", cell STILL EMITTED.
    - test_empty_and_zero_denominators (case 15): aggregate([]) == []; all-wins cell -> avg_loss == 0.0;
      all-losses cell -> avg_win == 0.0; no ZeroDivisionError.
    - test_naive_accuracy_divergence (case 17, R3-B2 — NEW): fixture = 3 wins + 2 losses + 1 sentinel zero
      + 1 NULL, one bot, one symbol. Compute in the test the NAIVE numbers exactly as src/db.py:228-238 does:
        resolved       = len(rows)
        naive_wins     = sum(1 for r in rows if (r["pnl"] or 0) > 0)
        naive_losses   = resolved - naive_wins           # <- books the zero AND the NULL as LOSSES
        naive_win_rate = naive_wins / resolved
        naive_total    = sum((r["pnl"] or 0.0) for r in rows)
      Then assert against the cell:
        cell["trades"] == 5  and  resolved - cell["trades"] == cell["zero_pnl"] + cell["null_pnl"]  (== 2)
        cell["losses"] == 2  and  naive_losses == 4        # the fabricated losses, named
        cell["win_rate"] != pytest.approx(naive_win_rate)  # THE DIVERGENCE — must be falsifiable
        cell["realized_pnl"] == pytest.approx(naive_total) # THE SUMS AGREE BY CONSTRUCTION
      Docstring: a `realized_pnl` delta vs db.get_realized_pnl is IDENTICALLY ZERO and can never fire —
      printing it would tell the operator the data is clean. The defect is in the DENOMINATOR.
  </behavior>
  <action>Create tests/test_symbol_stats.py. Import `from src.symbol_stats import aggregate, MIN_SAMPLE` at module top — this ImportError IS the RED proof (convention: tests/test_pnl.py:11). Fixtures are plain dicts; zero DB, zero network, no Alpaca fake. Use the EXACT test names from 17-VALIDATION.md Revision 3. Every zero-vs-None assertion must distinguish the two buckets explicitly (`zero_pnl` vs `null_pnl`) — different defects, different causes, never conflated. Case 17 must RE-DERIVE the naive arithmetic inline from the fixture (do not import get_alpaca_accuracy — it needs a DB); the point is that the two definitions disagree on the same rows. Do NOT write production code in this plan.</action>
  <verify>
    <automated>python -m pytest tests/test_symbol_stats.py -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - File collects (no SyntaxError). Fails RED with `ModuleNotFoundError: No module named 'src.symbol_stats'` — NOT with AssertionError/NameError from malformed asserts.
    - Cases 2/3 assert `losses == 0` and `zero_pnl == 1` on a `closed` + `pnl=0.0` row.
    - Case 7 asserts `gross_pnl_rows == 1`, `null_fees == 3`, AND `gross_pnl_rows < null_fees` (R3-W1 — the counters are distinct sets).
    - Case 12 contains a literal `assert aggregate(...) == []` (non-vacuous).
    - Case 5 asserts BOTH expectancy expressions with pytest.approx, with the 0.0 and None rows excluded from both sides.
    - Case 6 asserts realized_pnl == 60.0 (not 55.0).
    - **Case 17 asserts `resolved - trades == zero_pnl + null_pnl`, `win_rate != approx(naive_win_rate)`, AND `realized_pnl == approx(naive_total)` (R3-B2).**
    - `grep -c "def test_zero_pnl_is_bucketed_not_a_loss\|def test_external_exit_sentinel_not_a_loss\|def test_null_fees_vs_gross_pnl_rows\|def test_nonposition_terminal_never_counts\|def test_null_pnl_excluded_and_counted\|def test_naive_accuracy_divergence" tests/test_symbol_stats.py` returns 6.
  </acceptance_criteria>
  <done>Both fabricated-loss paths (gate block AND external-exit sentinel), the gross/net fee split, the NULL rule, and the count/rate divergence against the number the dashboard currently shows are executable RED specs — none of them vacuous.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED — sample guard, normalization, roll-ups, annotation, sufficient-only ranking (cases 8-11, 16, 22, 23)</name>
  <files>tests/test_symbol_stats.py</files>
  <read_first>src/universe.py L17-23 (normalize — the group key of record); src/effective_universe.py L115-203 (resolve_universe return shape; blocked[].reason vocabulary); src/bot_config.py L72-100 (symbols / quarantined / all_symbols); tests/test_effective_universe.py (Phase-16 BotConfig-construction + zero-I/O conventions)</read_first>
  <behavior>
    - test_min_sample_threshold (case 8): MIN_SAMPLE == 5; 4 trades -> "insufficient"; 5 -> "sufficient".
      PLUS the zero_pnl interaction, asserted explicitly: `trades` excludes zero_pnl and null_pnl rows, so a
      cell of 5 rows of which 1 is a sentinel zero has trades == 4 and is INSUFFICIENT.
    - test_insufficient_is_marked_not_hidden (case 9): the 4-trade cell IS PRESENT with full stats; only its
      RANKING eligibility is lost. Docstring: hiding it hides a leak.
    - test_min_sample_is_a_kwarg (case 10): min_sample=10 re-stamps a 5-trade cell "insufficient".
    - test_normalization_collapses_slash (case 11): "BTC/USD" + "BTCUSD" + "btc/usd" -> exactly ONE cell,
      symbol == "BTCUSD", display == "BTC/USD", trades == the sum.
    - test_rollup_by_key (case 16): the same aggregate() with key=("bot_id","symbol") / ("symbol",) /
      ("bot_id",). Assert the per-bot roll-up's realized_pnl == sum of COUNTED pnl for that bot, and that
      summing the cells' realized_pnl reproduces the per-bot roll-up. Assert the defect counters
      (zero_pnl / null_pnl / gross_pnl_rows / null_fees) roll up too — they are sums, not ratios.
    - test_annotation_from_resolve_universe (case 22): BotConfig with quarantined_symbols="AVAX/USD",
      crypto_universe="BTC/USD,SOL/USD,AVAX/USD"; call resolve_universe(cfg), index blocked by
      normalize(symbol). The annotator stamps an AVAX cell already_quarantined=True, a TRUMP/USD cell
      off_universe=True, a BTC cell False/False. Match through normalize() on BOTH sides. Read from
      resolve_universe — never re-derive.
    - test_ranking_is_sufficient_only (case 23 — STRUCTURAL): build cells where the WORST expectancy belongs
      to an `insufficient` cell. Assert `all(c["sample"] == "sufficient" for c in rank_cells(cells))`; assert
      the insufficient worst-expectancy cell is ABSENT from the ranking and PRESENT in the full table. Assert
      on the ranked cell LIST — NOT by grepping rendered text for words like "quarantine" (an impl would
      never emit them, so such a grep can never fail).
  </behavior>
  <action>Extend tests/test_symbol_stats.py. Cases 22/23 import the annotator/renderer surface built in Plan 04 (`from scripts.symbol_report import annotate, rank_cells, render_markdown`) — keep those imports INSIDE the test functions so the module-level ImportError of Task 1 stays the single clean RED signal. That import also requires the repo root on sys.path, which is why every command is `python -m pytest` from the repo root (R3-W5). Construct BotConfig directly. Zero I/O.</action>
  <verify>
    <automated>python -m pytest tests/test_symbol_stats.py -q -k "sample or normal or rollup or annotation or ranking" 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Case 8 asserts the zero_pnl interaction: a 5-row cell with 1 sentinel zero has trades == 4 -> "insufficient".
    - Case 11 asserts ONE cell keyed "BTCUSD" with display "BTC/USD".
    - Case 9 asserts the insufficient cell is in the returned list.
    - Case 16 asserts all three group keys through the SAME function, and that all four defect counters roll up.
    - Case 22 goes through resolve_universe + normalize on both sides; no re-derived set math.
    - Case 23 asserts `all(c["sample"] == "sufficient" for c in rank_cells(...))` — structural and falsifiable. It does NOT grep for verdict words.
    - Zero imports of psycopg / src.db / src.alpaca_orchestrator in these cases.
  </acceptance_criteria>
  <done>The sample guard, group key, roll-ups, annotation and the sufficient-only ranking are executable RED specs, all falsifiable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RED — real-SQL case 18 (POOL REBIND + live-connection positive control) + the docstring-immune read-only fence (cases 19, 20, 21)</name>
  <files>tests/test_symbol_stats.py</files>
  <read_first>**src/db.py L15-45 — READ THIS FIRST AND UNDERSTAND IT BEFORE WRITING CASE 18: `_pool` is a PROCESS-GLOBAL; `_create_pool()` reads os.environ["DATABASE_URL"] ONCE; `get_pool()` only builds it `if _pool is None`. Monkeypatching the env var after the pool is bound does NOTHING.**; tests/test_db.py L1-15, tests/test_reconciliation.py L160-170, tests/test_backfill.py L460-470 (three DATABASE_URL-gated tests that bind that same pool in the same pytest process); src/db.py L101-122 (update_alpaca_trade — the fence's NEGATIVE control: it contains UPDATE), L151-172 (the ::timestamptz cast precedent); src/db_schema.sql L24-51 (timestamp TEXT; pnl/fees NULLable); 17-VALIDATION.md Revision 3 rows R3-B1 / R3-B3 and the "Why case 18's POOL fence exists" section</read_first>
  <behavior>
    - test_get_resolved_trades_sql (case 18, R3-B1 — the ONLY execution path the SELECT ever gets, and the one
      test in this phase that WRITES):
      `@pytest.mark.skipif(not os.environ.get("TEST_DATABASE_URL"), reason="needs LOCAL Postgres")`.
      Use a fixture (or the first statements of the body) that REBINDS THE POOL, in this exact order, and
      COMPLETES ALL FIVE STEPS BEFORE THE FIRST INSERT:
        1. `test_url = os.environ["TEST_DATABASE_URL"]`
           `assert test_url != os.environ.get("DATABASE_URL"), "refusing to seed prod"`   # necessary, NOT sufficient
        2. `import src.db as _db`; if `_db._pool is not None`: `_db._pool.close()`; then `_db._pool = None`
           (the pool may ALREADY be bound to prod by tests/test_db.py, tests/test_reconciliation.py or
           tests/test_backfill.py running earlier in the same process)
        3. `monkeypatch.setenv("DATABASE_URL", test_url)`  — only NOW can _create_pool() see it
        4. POSITIVE CONTROL ON THE LIVE CONNECTION: `parsed = urllib.parse.urlparse(test_url)`; open a
           connection from the (re-created) pool and assert
             `conn.info.dbname == parsed.path.lstrip("/")` AND
             `conn.info.host   == (parsed.hostname or "localhost")`
           Only after BOTH assertions pass may the test INSERT anything. This is what proves the connection
           actually points at the local DB — the env-string comparison in step 1 does not.
        5. TEARDOWN (finally / fixture yield): `_db._pool.close()`; `_db._pool = None` — so the test pool
           never leaks into the other DB-gated tests.
      Then seed with ON CONFLICT DO NOTHING:
        - a 'closed' row (pnl=12.5, fees=0.4), fresh timestamp;
        - a 'rejected' row (pnl=0);
        - a 'closed' row entered 200 days ago;
        - a 'closed' row with pnl=NULL.
      alpaca_trades.timestamp is TEXT — seed ISO-8601 (datetime.now(timezone.utc).isoformat()).
      Assert:
        (a) key set is EXACTLY {bot_id, symbol, asset_class, side, status, pnl, fees, entry_ts, closed_at};
        (b) NO 'rejected' row in the result (the status filter runs in SQL);
        (c) get_resolved_trades(since=now-90d) EXCLUDES the 200-day-old row and INCLUDES the fresh one — the
            "timestamp"::timestamptz cast executes against the TEXT column without error and does not mis-filter;
        (d) get_resolved_trades(bot_id="A") returns only bot A's rows;
        (e) the NULL-pnl row comes back with pnl None, NOT 0.0.
    - test_window_cast_is_in_the_sql (case 19, static): read src/db.py, slice the get_resolved_trades body,
      assert it contains BOTH `"timestamp"::timestamptz >= %s` AND `ORDER BY "timestamp"::timestamptz`
      (Rev-2 W1). A pure test cannot verify a SQL cast — this grep plus 18(c) is what covers it.
    - test_phase17_is_read_only (case 20, R3-B3 — the fence; runs with NO DB): for each source
      (src/symbol_stats.py, scripts/symbol_report.py, and the get_resolved_trades body sliced from src/db.py),
      read from disk via pathlib resolved from the REPO ROOT (not CWD), then STRIP BOTH:
        - comment lines (stripped form starts with '#'), AND
        - **DOCSTRINGS** — every module/class/function docstring. Preferred: `ast.parse` the file and blank
          out each `ast.Expr` whose value is an `ast.Constant` of type `str` (by lineno/end_lineno); fallback
          for the db.py slice: drop triple-quoted blocks with a regex. **THE PLANS INSTRUCT THE EXECUTOR TO
          EXPLAIN THE INSERT/UPDATE/DELETE PROHIBITION IN THE MODULE DOCSTRINGS — a fence that greps raw text
          would fail on that PROSE while still missing a real write hidden in an f-string. It must scan CODE.**
      Then:
        POSITIVE CONTROL FIRST — assert the stripped source is NON-EMPTY (len > 200 chars) and, for the db.py
        slice, assert `"SELECT bot_id, symbol" in body`. An empty slice must NOT pass vacuously.
        THEN the negative assertion — no case-insensitive match for r"\b(INSERT|UPDATE|DELETE|ALTER|DROP|TRUNCATE)\b".
        ALSO (the --apply check, on the ARGPARSE SURFACE, not raw text):
          `assert re.search(r'add_argument\(\s*["\']--(apply|write|fix|delete)', symbol_report_src) is None`
        ALSO: src/symbol_stats.py imports nothing from src.db / psycopg.
    - test_readonly_fence_actually_fires (case 21, the self-test): apply THE SAME strip+scan helper to
      `update_alpaca_trade` (src/db.py:101-122) and assert it DOES match (it contains UPDATE). If this
      self-test is green while case 20 is also green, the fence is PROVEN to fire. `db.py` contains
      INSERT/UPDATE at :70, :118, :283, :347 — a fence that cannot see them is a fake.
  </behavior>
  <action>Append these to tests/test_symbol_stats.py. Case 18 gets its OWN skipif gated on TEST_DATABASE_URL — NOT module level, and NOT DATABASE_URL (that is prod, and this test seeds). **The pool rebind is the load-bearing part: rebinding the ENV VAR alone does not repoint the connection, because src/db.py:42 short-circuits on a non-None `_pool` that an earlier DB-gated test may already have bound to prod. Close the pool, null it, repoint the env, then PROVE it on `conn.info.dbname`/`conn.info.host` before the first INSERT, and null it again on teardown.** Factor the fence into module-level helpers `_slice_function(path, name) -> str`, `_strip_comments_and_docstrings(text) -> str` and `_scan_source(text) -> set[str]` (returns the mutating keywords found in the stripped text) so the self-test reuses the exact same code path — that shared helper is what makes the fence falsifiable. Import src.db lazily inside case 18. Do NOT write production code.</action>
  <verify>
    <automated>python -m pytest tests/test_symbol_stats.py -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Without TEST_DATABASE_URL: case 18 SKIPs; the pure cases collect and fail RED on the missing module.
    - **Case 18 asserts the LIVE connection's `info.dbname` and `info.host` match the parsed TEST_DATABASE_URL BEFORE the first INSERT, and sets `src.db._pool = None` on entry AND on exit (R3-B1). `grep -n "_pool = None" tests/test_symbol_stats.py` returns at least 2 lines; `grep -n "info.dbname" tests/test_symbol_stats.py` returns a line.**
    - Case 18's env-string assertion (`"refusing to seed prod"`) is still present — necessary, but explicitly NOT the fence.
    - **Case 20's fence strips DOCSTRINGS as well as `#` lines (R3-B3): `grep -n "docstring\|ast.Expr\|ast.Constant" tests/test_symbol_stats.py` returns a line.**
    - **Case 20's `--apply` check is on the argparse surface (`add_argument\(\s*["\']--(apply|write|fix|delete)`), NOT a raw-text grep for the token.**
    - Case 20 asserts the positive control (non-empty + `"SELECT bot_id, symbol"`) BEFORE the negative regex.
    - `test_readonly_fence_actually_fires` PASSES TODAY (it runs against the existing `update_alpaca_trade`), proving the regex fires — GREEN even while everything else is RED.
    - The `::timestamptz` grep covers BOTH the WHERE and the ORDER BY.
    - `python -m pytest tests/ dashboard/api/tests/ -q` shows NO new failures outside tests/test_symbol_stats.py (baseline 373 passed / 9 skipped).
  </acceptance_criteria>
  <done>The SELECT has a real execution path that is PROVEN — on the live connection, before it writes a byte — to point at the local test database, and the read-only fence is proven to fire on code rather than on prose.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test process → Alpaca | none — Phase 17 constructs no Alpaca client and makes zero network calls |
| seeding test → Postgres | **TEST_DATABASE_URL ONLY, enforced by REBINDING `src.db._pool` and asserting `conn.info.dbname`/`info.host` on the LIVE connection before the first INSERT**; skipped by default; **NEVER prod** |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Repudiation | a Phase-15 gate block (`rejected`, pnl=0) scores as a losing trade | mitigate | case 12 — `aggregate(nonposition_rows) == []`, asserted as an empty list (non-vacuous) |
| T-17-12 | Repudiation | **an external-exit sentinel (`closed`, pnl=0.0) scores as a losing trade** — the status filter does NOT catch it | mitigate | **cases 2 / 3 — the `zero_pnl` bucket; never scored** (`alpaca_orchestrator.py:167-176`) |
| T-17-13 | Repudiation | **gross P&L reported as net; `total_fees` reads $0 drag for exactly the bots whose P&L is overstated** | mitigate | **case 7 — `gross_pnl_rows` on any COUNTED row with NULL fees, distinct from the wider `null_fees`** (`bot_c/strategy.py:393`, `trend_strategy.py:172`) |
| T-17-15 | Repudiation | **the report prints a `realized_pnl` delta that is identically 0.00, telling the operator the data is clean** | mitigate | **case 17 — the divergence is in the COUNTS (`resolved - trades == zero_pnl + null_pnl`; `win_rate != naive_win_rate`), not the sum** (R3-B2, `src/db.py:228-229`) |
| T-17-02 | Repudiation | a NULL-pnl resolution defect is coerced to 0.0 and reads as break-even | mitigate | cases 13 / 14 — excluded, counted, loud; `null_pnl` and `zero_pnl` are DIFFERENT buckets |
| T-17-03 | Repudiation | fees double-subtracted | mitigate | case 6 — `realized_pnl == sum(pnl)` exactly |
| T-17-04 | Tampering | a write path reaches the prod trade log | mitigate | case 20 — fence with a positive control, **docstring stripping**, and case 21 proving it fires (R3-B3) |
| T-17-14 | Tampering | **the test suite SEEDS synthetic trades into the PROD trade log** — `src/db.py:18,40-45` caches a process-global pool that an earlier DB-gated test may already have bound to prod, so an env-var monkeypatch is INERT | mitigate | **case 18 closes and nulls `src.db._pool`, repoints DATABASE_URL, and asserts `conn.info.dbname`/`info.host` against the parsed TEST_DATABASE_URL on the LIVE connection BEFORE its first INSERT; teardown nulls the pool again** (R3-B1) |
| T-17-SC | Tampering | package installs | accept | none — pytest 8.3.5 already installed |
</threat_model>

<verification>
- `python -m pytest tests/test_symbol_stats.py -q` — the pure cases fail RED on
  `ModuleNotFoundError: No module named 'src.symbol_stats'`; case 18 SKIPs without TEST_DATABASE_URL;
  `test_readonly_fence_actually_fires` PASSES (proving the fence regex fires against `update_alpaca_trade`).
- `TEST_DATABASE_URL=postgresql://localhost/aipw_test python -m pytest tests/test_symbol_stats.py -q -k sql`
  — case 18 runs, and its live-connection positive control (`conn.info.dbname` / `conn.info.host`) passes
  BEFORE any INSERT.
- `python -m pytest tests/ dashboard/api/tests/ -q` — no NEW failures outside tests/test_symbol_stats.py
  (baseline 373 passed / 9 skipped). **`python -m pytest` from the repo root, always (R3-W5).**
</verification>

<success_criteria>
Every fabricated-loss path is pinned: the gate block (case 12, non-vacuously), the external-exit sentinel
(cases 2/3), the NULL coercion (case 13). The gross/net fee lie is pinned (case 7). The count/rate divergence
against the number the dashboard shows is pinned (case 17). The read-only fence is proven to fire on CODE, not
prose (cases 20/21). **The seeding test cannot reach prod even when the pool was already bound to it (case 18).**
</success_criteria>

<output>
Create `.planning/phases/17-per-symbol-performance/17-01-SUMMARY.md` when done.
</output>
