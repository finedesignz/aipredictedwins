---
phase: 04-deterministic-atr-exits
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/alpaca_orchestrator.py
  - src/bot_thread.py
autonomous: true
requirements: [EXIT-02, EXIT-03]
must_haves:
  truths:
    - "The position monitor decides exits with deterministic ATR math, not the MiroFish LLM — ExitAdvisor.should_exit() is never called for the exit decision."
    - "Soft-zone crossings close on an ATR stop or ATR trailing stop instead of running to the hard stop (no exit-path gap before Phase 5)."
    - "hard_stop_pct and max_hold_hours fire as absolute overrides; SWING (max_hold_hours None) never time-closes."
    - "Exit precedence is first-match: hard_stop_pct -> max_hold -> ATR trailing stop -> ATR stop."
    - "ATR is computed live at profile.timeframe, not hardcoded 1Hour; ATR<=0 safely falls back to hard_stop/max_hold only."
    - "Both consumers (orchestrator main + bot_thread) pass their active profile to the shared PositionMonitor."
  artifacts:
    - path: "src/alpaca_orchestrator.py"
      provides: "PositionMonitor profile param + ATR decision ladder"
      contains: "def __init__(self, alpaca"
    - path: "src/bot_thread.py"
      provides: "profile threaded into PositionMonitor instantiation"
      contains: "PositionMonitor("
  key_links:
    - from: "src/alpaca_orchestrator.py PositionMonitor._check_all_positions"
      to: "technical_signals._atr at profile.timeframe"
      via: "live bar fetch + ATR compute"
      pattern: "_atr\\("
    - from: "src/alpaca_orchestrator.py PositionMonitor._check_all_positions"
      to: "TrailingStop.update_atr"
      via: "ATR trailing-stop decision rung"
      pattern: "update_atr"
    - from: "src/bot_thread.py monitor instantiation"
      to: "PROFILES.get(BOT_PROFILE, SWING)"
      via: "constructor profile arg"
      pattern: "PositionMonitor\\(.*SWING|PROFILES"
---

<objective>
Replace the soft-threshold MiroFish LLM exit branch in the shared `PositionMonitor` with a deterministic ATR decision ladder, add `hard_stop_pct`/`max_hold_hours` absolute overrides, and thread the active profile into both instantiation sites. Makes the 04-01 RED tests pass.

Purpose: EXIT-02 (ATR stop + ATR trailing stop, side-aware, at profile.timeframe) and EXIT-03 (absolute overrides, swing-safe). The exit path must be deterministic and LLM-free before Phase 5 strips MiroFish — no gap.
Output: edited `PositionMonitor` (one shared class → both orchestrator and prod bot_thread), profile threaded into the two constructor calls.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-deterministic-atr-exits/04-RESEARCH.md
@.planning/phases/04-deterministic-atr-exits/04-CONTEXT.md
@./CLAUDE.md
@src/alpaca_orchestrator.py
@src/strategy_profile.py

<interfaces>
ONE class, TWO instantiation sites (verified — grep shows exactly these):
  src/alpaca_orchestrator.py:620  monitor = PositionMonitor(alpaca, logger, exit_advisor)
  src/bot_thread.py:226           monitor = PositionMonitor(alpaca, logger, exit_advisor)
bot_thread already imports `PROFILES, SWING` (L62) and already resolves the profile this exact way at L384:
  _profile = PROFILES.get(os.environ.get("BOT_PROFILE", "swing").lower(), SWING)
orchestrator main() already has `PROFILE` in scope (Phase 2 selection).

Surface to change — PositionMonitor._check_all_positions (alpaca_orchestrator.py L137-305):
  - __init__ L109: add `profile=SWING` param; store self.profile; import SWING.
  - hours_held parse currently inside soft branch (L240-244) — MOVE up to per-position, unconditional.
  - side-aware pnl_pct already L199-206 (keep).
  - L208-223 long-only pct trail + pct threshold block — replaced by ATR ladder.
  - L235-236 hard/tightened/trailing deterministic close — preserve close mechanics.
  - L237-272 the `elif threshold in ("soft_stop","soft_take_profit")` branch calling
    self.exit_advisor.should_exit() — THE ONLY LLM decision call — DELETE/REPLACE with ATR.
  - close path L289-305 unchanged.

From src/exit_advisor.py (extended in 04-01):
  TrailingStop.update_atr(trade_id, side, entry_price, current_price, atr, mult_trail) -> "trailing_stop"|None
From src/technical_signals.py:
  _atr(highs, lows, closes, period) -> float   # 0.0 on insufficient bars
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ATR decision ladder + profile in PositionMonitor</name>
  <files>src/alpaca_orchestrator.py</files>
  <behavior>
    - Constructor accepts `profile` (default SWING); stored as self.profile.
    - Per position, compute hours_held unconditionally; fetch bars at self.profile.timeframe, limit=self.profile.atr_period+5; atr=_atr(highs,lows,closes,atr_period) (0.0 if short).
    - First-match ladder (D-05): 1) pnl_pct <= profile.hard_stop_pct -> "hard_stop"; 2) profile.max_hold_hours is not None and hours_held > max_hold_hours -> "max_hold"; 3) atr>0 and self._trailing.update_atr(...) -> "trailing_stop"; 4) atr>0 and side-aware ATR stop level breached -> "atr_stop"; else keep dormant tightened_stop rung, else continue.
    - exit_advisor.should_exit() is NOT called anywhere in the decision.
    - atr<=0 -> only rungs 1+2 can fire (no instant exit).
  </behavior>
  <action>Add `profile=SWING` to `__init__` (import SWING from src.strategy_profile) and store `self.profile = profile`. Rewrite the decision section (L208-272) as a first-match ladder per the behavior block, implementing D-01..D-05. Compute ATR once per position from a live `self.alpaca.get_bars(symbol, timeframe=self.profile.timeframe, limit=self.profile.atr_period + 5)` fetch, parsing "high"/"low"/"close" and calling `_atr` (import from src.technical_signals). Long ATR stop level = entry - atr_mult_stop*atr; short = entry + atr_mult_stop*atr (compare side-aware to current_price). ATR trail via `self._trailing.update_atr(trade_id, side, entry_price, current_price, atr, self.profile.atr_mult_trail)`. Move the hours_held parse (old L240-244) above the ladder so max_hold is evaluated per position. Guard `profile.max_hold_hours is not None` (Pitfall 3 — swing never time-closes). If atr<=0, skip rungs 3+4 (Pitfall 2). DELETE the `elif threshold in ("soft_stop","soft_take_profit")` LLM branch including the `self.exit_advisor.should_exit(...)` call (Pitfall 1 — replaced, not gapped). Keep `self.exit_advisor` attribute and import present-but-unused this phase (Phase 5 removes; Claude's-Discretion = keep, do not touch Claude-CLI auth). Keep the tightened_stop rung dormant (harmless). Keep the close path (L289-305) and side-aware pnl (L199-206) exactly. close_reason values: "hard_stop","max_hold","trailing_stop","atr_stop". Minimal diff (Karpathy) — touch only the decision section + constructor.</action>
  <verify>
    <automated>python -m pytest tests/test_atr_exits.py -q 2>&1 | grep -v '^#' | grep -q 'passed' ; python -m pytest tests/test_atr_exits.py -q 2>&1 | grep -v '^#' | grep -qv 'failed'</automated>
  </verify>
  <done>All eight 04-01 tests GREEN, including test_no_llm_call (should_exit not called), test_override_precedence, test_swing_no_time_close, test_zero_atr_safe. Commit: `feat(04-02): deterministic ATR exit ladder + overrides in PositionMonitor`.</done>
</task>

<task type="auto">
  <name>Task 2: Thread profile into both instantiation sites</name>
  <files>src/alpaca_orchestrator.py, src/bot_thread.py</files>
  <action>Pass the active profile to the two `PositionMonitor(...)` calls (D-06). In `src/alpaca_orchestrator.py` main() L620: `PositionMonitor(alpaca, logger, exit_advisor, PROFILE)` (PROFILE already in scope from Phase 2; if not, resolve `PROFILES.get(os.environ.get("BOT_PROFILE","swing").lower(), SWING)`). In `src/bot_thread.py` L226: `PositionMonitor(alpaca, logger, exit_advisor, PROFILES.get(os.environ.get("BOT_PROFILE","swing").lower(), SWING))` — bot_thread already imports PROFILES, SWING (L62) and resolves this same expression at L384, so reuse that form (assign once, reuse if a local already exists). Verify via grep there are exactly two instantiation sites. No other behavior change. Minimal diff.</action>
  <verify>
    <automated>python -m pytest tests/ -q 2>&1 | grep -v '^#' | grep -q 'passed' ; python -c "import src.bot_thread, src.alpaca_orchestrator; print('import-ok')"</automated>
  </verify>
  <done>Both modules import cleanly; both sites pass a profile; full suite `python -m pytest tests/ -q` green (swing parity preserved). Commit: `feat(04-02): thread active profile into both PositionMonitor sites`.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -q` fully green (200+ baseline + 8 new ATR tests).
- No-LLM: `grep -n "should_exit" src/alpaca_orchestrator.py` shows no call inside `_check_all_positions` decision path (test_no_llm_call enforces).
- ATR timeframe: `grep -n "get_bars" src/alpaca_orchestrator.py` inside the monitor uses `self.profile.timeframe`, not literal `"1Hour"`.
- Swing parity: SWING hard_stop_pct=-0.15 (== old HARD_STOP_PCT), max_hold_hours None — no time-close; swing bots A/B unaffected.
- Exactly two `PositionMonitor(` instantiation sites, both passing a profile.
</verification>

<success_criteria>
- EXIT-02: side-aware ATR stop + ATR trailing stop live at profile.timeframe.
- EXIT-03: hard_stop_pct + max_hold_hours absolute overrides; first-match precedence documented in code.
- Exit decision is deterministic and LLM-free; ExitAdvisor import/attr retained (Phase 5 removes).
- Full suite green; swing behavior unchanged.
</success_criteria>

<output>
Create `.planning/phases/04-deterministic-atr-exits/04-02-SUMMARY.md` when done.
</output>
