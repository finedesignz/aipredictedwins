---
phase: 04-deterministic-atr-exits
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/test_atr_exits.py
  - src/exit_advisor.py
autonomous: true
requirements: [EXIT-02]
must_haves:
  truths:
    - "A failing test suite exists that asserts ATR stop math, trail ratchet, max-hold, precedence, and no-LLM-call before implementation."
    - "TrailingStop tracks side-aware ATR-distance trailing (high-water for longs, low-water for shorts)."
  artifacts:
    - path: "tests/test_atr_exits.py"
      provides: "EXIT-02/EXIT-03 behavioral specification (RED)"
      contains: "def test_"
    - path: "tests/conftest.py"
      provides: "fake-bars + mock AlpacaClient/TradeLogger fixtures"
    - path: "src/exit_advisor.py"
      provides: "TrailingStop ATR + short extension"
      contains: "def update_atr"
  key_links:
    - from: "src/exit_advisor.py TrailingStop.update_atr"
      to: "technical_signals._atr output (atr float)"
      via: "atr-distance trail level computation"
      pattern: "update_atr"
---

<objective>
Lay the test foundation (Nyquist) and extend the trailing-stop tracker for ATR-distance, side-aware trailing. This unblocks the monitor rewrite in 04-02.

Purpose: Tests must exist and fail before the monitor changes (no exit-path gap, no LLM in the exit decision). `TrailingStop` is currently long-only and pct-based; ATR trailing for both sides is a prerequisite contract for the decision ladder.
Output: `tests/conftest.py`, `tests/test_atr_exits.py` (RED), `TrailingStop.update_atr` (long high-water + short low-water, ATR distance).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-deterministic-atr-exits/04-RESEARCH.md
@.planning/phases/04-deterministic-atr-exits/04-CONTEXT.md
@.planning/phases/04-deterministic-atr-exits/04-VALIDATION.md
@./CLAUDE.md

<interfaces>
From src/technical_signals.py:
  def _atr(highs: list[float], lows: list[float], closes: list[float], period: int = 14) -> float
  # Wilder ATR, latest value; returns 0.0 if len(closes) < period+1 or list lengths mismatch.

From src/strategy_profile.py (read these fields in tests):
  StrategyProfile.timeframe, .atr_period, .atr_mult_stop, .atr_mult_trail, .hard_stop_pct, .max_hold_hours
  SWING:    timeframe="1Hour", atr_period=14, atr_mult_stop=2.0, atr_mult_trail=1.5, hard_stop_pct=-0.15, max_hold_hours=None
  DAYTRADE: timeframe="5Min",  atr_period=14, atr_mult_stop=1.5, atr_mult_trail=2.0, hard_stop_pct=-0.04, max_hold_hours=6.0
  PROFILES = {"swing": SWING, "daytrade": DAYTRADE}

From src/exit_advisor.py (current TrailingStop — long-only, pct-based; DO NOT break existing update()):
  class TrailingStop:
      def __init__(self): self._peaks: dict[int,float] = {}
      def update(self, trade_id, entry_price, current_price) -> str | None   # long-only pct trail
      def remove(self, trade_id)

From src/alpaca_orchestrator.py PositionMonitor (the class 04-02 edits; tests construct it):
  def __init__(self, alpaca, logger, exit_advisor)   # 04-02 adds: , profile=SWING
  # side-aware pnl already at L199-206; close path L289-305 unchanged.
  # Bars shape used by monitor: list of dicts with keys "high","low","close".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test fixtures (conftest)</name>
  <files>tests/conftest.py</files>
  <action>Create pytest fixtures for the ATR-exit tests. Provide: (a) `fake_bars(highs, lows, closes)` helper returning `list[dict]` with keys "high","low","close" so `_atr` and the monitor's bar-parse both consume them; (b) a `make_bars_for_atr(atr_target, n, period)` deterministic generator (constant true-range bars so `_atr` returns a known value — e.g. each bar high-low = atr_target, flat closes — verify against `technical_signals._atr` so the expected ATR is exact, not assumed); (c) `mock_alpaca` (MagicMock with `get_positions`->[], `get_latest_price`, `get_bars`, `close_position`); (d) `mock_logger` (MagicMock with `get_open_alpaca_positions`, `update_alpaca_trade`); (e) `mock_advisor` = MagicMock() to assert `.should_exit` not called. Do NOT hit network or DB. If pytest is not installed, install it (`pip install pytest`).</action>
  <verify>
    <automated>python -c "import tests.conftest" 2>&1 | python -c "import sys;print('ok' if not sys.stdin.read().strip() else sys.stdin.read())"</automated>
  </verify>
  <done>Fixtures importable; `make_bars_for_atr` produces bars whose `_atr` equals the documented target (asserted in a sanity test).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Failing spec (RED) for ATR exits + no-LLM</name>
  <files>tests/test_atr_exits.py</files>
  <behavior>
    - test_long_atr_stop_level: long, ATR known, price <= entry - atr_mult_stop*ATR -> close reason "atr_stop".
    - test_short_atr_stop_level: short, price >= entry + atr_mult_stop*ATR -> "atr_stop".
    - test_atr_trail_ratchet: long high-water only ratchets up; trail = highwater - atr_mult_trail*ATR; drop into it -> "trailing_stop". Short mirror (low-water down-only).
    - test_zero_atr_safe: ATR<=0 (insufficient bars) -> NO instant exit; only hard_stop_pct/max_hold can fire.
    - test_max_hold_fires: DAYTRADE, hours_held > 6 -> "max_hold".
    - test_swing_no_time_close: SWING (max_hold_hours None) never returns "max_hold" regardless of hours_held.
    - test_override_precedence: position simultaneously satisfying hard_stop_pct AND max_hold AND ATR trail closes with reason "hard_stop" (first match wins: hard_stop -> max_hold -> trailing_stop -> atr_stop).
    - test_no_llm_call: drive each exit type through PositionMonitor with mock_advisor; assert mock_advisor.should_exit.assert_not_called().
  </behavior>
  <action>Write the test cases above against `PositionMonitor` constructed as `PositionMonitor(mock_alpaca, mock_logger, mock_advisor, profile)` (the `profile` 4th arg is added in 04-02 — these tests are RED until then; that is intended). Each test seeds `mock_logger.get_open_alpaca_positions` with one trade dict {id, symbol, side, entry_price, qty, timestamp} and `mock_alpaca.get_latest_price`/`get_bars` to hit the target branch, then calls `monitor._check_all_positions()` and asserts `mock_alpaca.close_position` called (or not) and the `alert_position_closed` reason — patch `src.alpaca_orchestrator.alert_position_closed` to capture `close_reason`. Use real SWING/DAYTRADE profiles from strategy_profile. Compute expected ATR via the conftest generator. Per EXIT-02 (D-01/D-02) and EXIT-03 (D-04/D-05). Run and confirm they FAIL on the missing `profile` param / missing ATR logic (RED).</action>
  <verify>
    <automated>python -m pytest tests/test_atr_exits.py -q 2>&1 | grep -v '^#' | grep -Eq 'error|failed|passed'</automated>
  </verify>
  <done>All eight tests present and FAILING (RED) — failure is the missing 4th constructor arg and/or absent ATR/max-hold logic, not import errors. Commit: `test(04-01): add failing ATR-exit + no-LLM spec`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend TrailingStop for ATR distance + shorts (GREEN for trail unit)</name>
  <files>src/exit_advisor.py</files>
  <behavior>
    - update_atr(trade_id, side, entry_price, current_price, atr, mult_trail): long tracks high-water in _peaks, trail=highwater - mult_trail*atr, returns "trailing_stop" when current<=trail (only after high-water advanced past entry). Short tracks low-water in new _troughs, trail=lowwater + mult_trail*atr, returns "trailing_stop" when current>=trail.
    - atr<=0 -> returns None (no trail).
    - existing pct `update()` and `remove()` unchanged; remove() clears both _peaks and _troughs.
  </behavior>
  <action>Add `_troughs: dict[int,float] = {}` to `__init__` and a new method `update_atr(self, trade_id, side, entry_price, current_price, atr, mult_trail)` per the behavior block (D-02). Long: ratchet high-water up only; arm only once high-water > entry; trail level = highwater - mult_trail*atr. Short (`side in ("sell","short")`): ratchet low-water down only; arm once low-water < entry; trail = lowwater + mult_trail*atr; trigger when current >= trail. Return "trailing_stop" or None; if atr<=0 return None. On trigger, call self.remove(trade_id). Extend `remove()` to pop from `_troughs` too. Leave `update()` and the pct constants untouched (still used by bot_thread entry logging) — minimal diff (Karpathy).</action>
  <verify>
    <automated>python -m pytest tests/test_atr_exits.py::test_atr_trail_ratchet -x -q 2>&1 | grep -v '^#' | grep -q 'passed'</automated>
  </verify>
  <done>`test_atr_trail_ratchet` passes (long + short). `update()`/`remove()` back-compat intact. Commit: `feat(04-01): TrailingStop ATR-distance + short trailing`.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_atr_exits.py -q` runs; trail-ratchet test green, remaining ATR/max-hold/precedence/no-LLM tests still RED (close by 04-02).
- `python -m pytest -q` — full suite still green except the intentionally-RED 04-01 tests awaiting 04-02.
- `TrailingStop.update()` (pct, long-only) behavior unchanged — no regression for bot_thread entry logging.
</verification>

<success_criteria>
- conftest fixtures importable; deterministic ATR generator verified against `_atr`.
- Eight EXIT-02/EXIT-03 tests authored; `update_atr` makes the trail-ratchet test pass.
- No network/DB in tests.
</success_criteria>

<output>
Create `.planning/phases/04-deterministic-atr-exits/04-01-SUMMARY.md` when done.
</output>
