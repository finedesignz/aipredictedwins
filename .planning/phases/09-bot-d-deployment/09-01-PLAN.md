---
phase: 09-bot-d-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/trade_logger.py
  - tests/test_trade_logger_shim.py
  - tests/test_alpaca_client_failclear.py
autonomous: true
requirements: [BOT-01]
must_haves:
  truths:
    - "A bot started with BOT_ID=D constructs TradeLogger without raising (no longer rejected)"
    - "An invalid BOT_ID (e.g. Z) still raises ValueError (allow-list still enforced)"
    - "An orchestrator with empty ALPACA_API_KEY/SECRET still raises clearly — no silent reuse of A/B keys"
  artifacts:
    - path: "src/trade_logger.py"
      provides: "Widened BOT_ID allow-list including D via shared KNOWN_BOT_IDS constant"
      contains: "KNOWN_BOT_IDS"
    - path: "tests/test_alpaca_client_failclear.py"
      provides: "Regression test that empty bare keys raise (fail-clear, no A/B fallback)"
  key_links:
    - from: "src/trade_logger.py"
      to: "BOT_ID env var"
      via: "membership check against KNOWN_BOT_IDS"
      pattern: "KNOWN_BOT_IDS"
---

<objective>
BOT-01 code side: make the orchestrator accept `BOT_ID=D` (currently the hard blocker — `trade_logger.py:26` rejects any id not in `("A","B")`), while preserving fail-clear behavior (AlpacaClient raises on empty bare keys; NO silent fallback to A/B keys per D-01).

Purpose: Bot D cannot boot until the allow-list is widened. This is the single code blocker identified in RESEARCH.
Output: Widened allow-list (shared constant), extended shim test, new fail-clear regression test.

NOTE: This plan is CODE (executable now). The live Alpaca account that produces D's keys is INFRA-DEFERRED — see plan 09-03 HALT handoff. BOT-01 is partially complete here (code); the live-account half is documented, not faked.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-bot-d-deployment/09-RESEARCH.md
@./CLAUDE.md

<interfaces>
From src/trade_logger.py (current — bot_id kwarg path accepts any non-empty string; the BOT_ID env path is the A/B-only blocker):
```python
class TradeLogger:
    def __init__(self, db_path: str = "data/trades.db", bot_id: str | None = None):
        if bot_id is not None:
            if not bot_id:
                raise ValueError("bot_id must be a non-empty string")
            self.bot_id = bot_id
        else:
            self.bot_id = os.environ.get("BOT_ID", "")
            if self.bot_id not in ("A", "B"):   # <-- BLOCKER (line 26)
                raise ValueError(...)
```

From src/config.py:155-156 (bare keys, default ""; verified NO suffix/fallback logic):
```python
alpaca_api_key=_env("ALPACA_API_KEY", ""),
alpaca_secret_key=_env("ALPACA_SECRET_KEY", ""),
```

From src/alpaca_client.py:94-97 (verified fail-clear):
```python
raise ValueError("ALPACA_API_KEY and ALPACA_SECRET_KEY must be set in .env")
```

Existing tests/test_trade_logger_shim.py mocks src.db at sys.modules level (no DB/DATABASE_URL needed). It currently uses BOT_ID="C" as its invalid case — that must change to a value NOT in the new allow-list (use "Z").
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen BOT_ID allow-list to include D via shared constant</name>
  <files>src/trade_logger.py</files>
  <behavior>
    - BOT_ID=A / B / C / D each construct TradeLogger (env path) without raising
    - BOT_ID=d (lowercase) is normalized to "D" and accepted (pitfall 4)
    - BOT_ID=Z raises ValueError matching "BOT_ID"
    - Missing BOT_ID still raises ValueError matching "BOT_ID"
    - bot_id kwarg path unchanged (any non-empty string still accepted; empty still raises)
  </behavior>
  <action>Add a module-level `KNOWN_BOT_IDS = ("A", "B", "C", "D")` constant (per D-01 / pitfall 1: single source of truth). In the env-var branch (line 25-30), uppercase-normalize `self.bot_id = os.environ.get("BOT_ID", "").upper()` (pitfall 4: bot_id casing) and replace the `not in ("A", "B")` check with `not in KNOWN_BOT_IDS`. Update the error message to reference the allowed set generically (e.g. "must be one of A/B/C/D"). Do NOT touch the `bot_id` kwarg branch — multi-bot UUID callers depend on it. Do NOT add any `_D` suffix or key-fallback logic to config.py — orchestrator stays bare-key (anti-pattern in RESEARCH). Minimal diff per Karpathy.</action>
  <verify>
    <automated>python -m pytest tests/test_trade_logger_shim.py -x</automated>
  </verify>
  <done>KNOWN_BOT_IDS constant exists with D; BOT_ID=D constructs; BOT_ID=Z raises; suite passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update shim test for D + add fail-clear regression test</name>
  <files>tests/test_trade_logger_shim.py, tests/test_alpaca_client_failclear.py</files>
  <behavior>
    - test_valid_bot_id_d: BOT_ID=D → logger.bot_id == "D"
    - test_lowercase_bot_id_normalized: BOT_ID=d → logger.bot_id == "D"
    - existing invalid-id test changed from "C" to "Z" (C is now valid)
    - fail-clear: building AlpacaClient (or Config-fed client init) with empty ALPACA_API_KEY/SECRET raises ValueError mentioning the keys; never substitutes A/B values
  </behavior>
  <action>In test_trade_logger_shim.py: change `test_invalid_bot_id_raises` to set BOT_ID="Z" (since C/D are now valid). Add `test_valid_bot_id_d` and `test_lowercase_bot_id_normalized`. Create tests/test_alpaca_client_failclear.py (Wave 0 gap from RESEARCH §Phase Requirements→Test Map): with monkeypatched empty `ALPACA_API_KEY`/`ALPACA_SECRET_KEY`, assert the AlpacaClient init path raises ValueError matching the key message (verifies D-01 no-silent-fallback at the code boundary). Mock/skip any network client construction — only the empty-key guard is under test. Follow the existing file's sys.modules-mock style so no DATABASE_URL is needed.</action>
  <verify>
    <automated>python -m pytest tests/test_trade_logger_shim.py tests/test_alpaca_client_failclear.py -x</automated>
  </verify>
  <done>Both test files pass; D accepted, Z rejected, empty-key path raises clearly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| operator → Coolify env → orchestrator | Human pastes bare Alpaca keys per bot service (untrusted: could paste A's key into D) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Spoofing | Bot D Alpaca account identity | mitigate | Code stays fail-clear (empty keys raise). Cross-account reuse is operator-side and undetectable in code — mitigated by post-deploy account-number check in 09-03 HALT recipe |
| T-09-02 | Tampering | BOT_ID env value | mitigate | Uppercase-normalize + allow-list membership; unknown ids raise rather than silently partitioning bad rows |
| T-09-SC | Tampering | pip installs | accept | Zero external installs this phase (RESEARCH: no Package Legitimacy Audit needed) |
</threat_model>

<verification>
- `python -m pytest tests/test_trade_logger_shim.py tests/test_alpaca_client_failclear.py -x` green
- Full backend suite still green: `python -m pytest -q` (261+ baseline)
- `grep -n KNOWN_BOT_IDS src/trade_logger.py` shows D in the tuple
</verification>

<success_criteria>
BOT_ID=D boots the orchestrator's TradeLogger; invalid ids still rejected; empty bare keys still raise (no A/B fallback). Suite green.
</success_criteria>

<output>
Create `.planning/phases/09-bot-d-deployment/09-01-SUMMARY.md` when done.
</output>
