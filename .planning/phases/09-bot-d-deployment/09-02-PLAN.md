---
phase: 09-bot-d-deployment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/api/db.py
  - dashboard/api/seed_bots.py
  - dashboard/web/components/trades/TradeTable.tsx
  - dashboard/web/components/positions/PositionCard.tsx
  - tests/test_dashboard_db.py
  - tests/test_seed_bots.py
autonomous: true
requirements: [BOT-03]
must_haves:
  truths:
    - "Dashboard 'all' aggregate includes Bot D (KNOWN_BOTS contains D)"
    - "is_specific_bot('D') returns True"
    - "seed_bots seeds a bot_id='D' row when ALPACA_API_KEY_D/_SECRET_KEY_D are set, and skips it when absent"
    - "Trade rows and position cards with bot_id='D' render the 'D' badge (not mislabeled as 'A')"
  artifacts:
    - path: "dashboard/api/db.py"
      provides: "KNOWN_BOTS including D"
      contains: "\"D\""
    - path: "dashboard/api/seed_bots.py"
      provides: "Idempotent D bot-row seed block (daytrade defaults)"
      contains: "ALPACA_API_KEY_D"
    - path: "tests/test_dashboard_db.py"
      provides: "KNOWN_BOTS / is_specific_bot('D') coverage"
    - path: "tests/test_seed_bots.py"
      provides: "D-row seeding coverage (env present vs absent)"
  key_links:
    - from: "dashboard/api/seed_bots.py"
      to: "bots table"
      via: "idempotent upsert keyed on bot_id='D'"
      pattern: "bot_id.*D"
---

<objective>
BOT-03 (fully code-complete this phase): the dashboard must list Bot D and attribute its trades/equity correctly, with no binary A/B assumption in the UI (D-02).

Purpose: Without these edits D is excluded from "all" aggregates and mislabeled as "A" in two hardcoded UI cells (RESEARCH pitfall 3).
Output: KNOWN_BOTS includes D; seed_bots seeds the D row; two UI cells render bot_id generically; new tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-bot-d-deployment/09-RESEARCH.md

<interfaces>
From dashboard/api/db.py:18 (attribution already dynamic; just extend the tuple):
```python
KNOWN_BOTS: tuple[str, ...] = ("A", "B", "C")
def is_specific_bot(bot: str) -> bool:
    return bot in KNOWN_BOTS
```

From dashboard/api/seed_bots.py (A/B/C blocks are explicit `if key_x and secret_x:` upserts reading `ALPACA_API_KEY_{X}` / `ALPACA_SECRET_KEY_{X}`; the C block at lines 70-93 is the closest template — daytrade D mirrors it). The final guard logs "No ALPACA_API_KEY_A/B/C env vars found — skipping seed" at line 96.

From dashboard/web/components/trades/TradeTable.tsx:114-120 (binary — mislabels D as A):
```tsx
const isB = val.includes("B");
... className={... isB ? amber : blue} > {isB ? "B" : "A"}
```

From dashboard/web/components/positions/PositionCard.tsx:21-26 (binary color; label falls through to raw value for D but styling is inconsistent):
```tsx
background: position.bot === "A" || position.bot === "Agent A" ? blue : amber,
{position.bot === "Agent A" ? "A" : position.bot === "Agent B" ? "B" : position.bot}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add D to KNOWN_BOTS and seed the D bot row</name>
  <files>dashboard/api/db.py, dashboard/api/seed_bots.py, tests/test_dashboard_db.py, tests/test_seed_bots.py</files>
  <behavior>
    - KNOWN_BOTS == ("A","B","C","D"); is_specific_bot("D") is True; is_specific_bot("both") is False
    - seed_bots: with ALPACA_API_KEY_D + ALPACA_SECRET_KEY_D set → a bot dict with bot_id="D" is built; without → no D row built
    - D block uses daytrade defaults: label "Agent D — Daytrade", asset_class "crypto" (A2: matches A/B), kelly/confluence from BOT_D_* env with sensible daytrade defaults
  </behavior>
  <action>db.py: change line 18 to `KNOWN_BOTS: tuple[str, ...] = ("A", "B", "C", "D")` (per D-02 / RESEARCH BOT-03). seed_bots.py: add a `key_d = os.environ.get("ALPACA_API_KEY_D")` / `secret_d` block mirroring the C block (lines 70-93) with `bot_id="D"`, `label=os.environ.get("BOT_D_LABEL", "Agent D — Daytrade")`, `asset_class="crypto"`, daytrade-tuned defaults via `BOT_D_*` env (kelly 0.25, confluence per daytrade profile), and keep the upsert idempotent. Update the final fallback log string to mention D. Create tests/test_dashboard_db.py (Wave 0 gap) asserting KNOWN_BOTS membership + is_specific_bot("D"). Create tests/test_seed_bots.py (Wave 0 gap) that monkeypatches `_D` env and asserts the D entry is built (mock/avoid the psycopg.connect by testing the bot-list assembly, or factor the assembly so it is unit-testable without DB — prefer monkeypatching env and asserting the in-memory `bots` list contains D; if seed_bots couples assembly to connect, test via a thin extraction or skip DB with a connect monkeypatch). Minimal diff.</action>
  <verify>
    <automated>python -m pytest tests/test_dashboard_db.py tests/test_seed_bots.py -x</automated>
  </verify>
  <done>KNOWN_BOTS has D; is_specific_bot("D") True; D row seeded only when _D env present; both test files pass.</done>
</task>

<task type="auto">
  <name>Task 2: Generalize the two hardcoded A/B UI cells</name>
  <files>dashboard/web/components/trades/TradeTable.tsx, dashboard/web/components/positions/PositionCard.tsx</files>
  <action>TradeTable.tsx bot cell (114-120): drop the binary `isB`/`isA` logic. Derive a normalized single-letter label from the value (strip "Agent " prefix, uppercase first letter; e.g. "Agent D"→"D", "D"→"D") and pick a color from a small per-letter map (A=blue, B=amber, C/D=distinct accents — reuse existing theme tokens) with a neutral fallback so any future bot renders correctly, not just A/B. PositionCard.tsx (18-28): replace the binary `position.bot === "A"...` ternaries with the same normalization + color map so bot_id="D" gets a "D" badge with consistent styling. Keep DOM/structure and class conventions identical (Karpathy minimal diff) — only the label/color derivation changes. No binary assumption anywhere.</action>
  <verify>
    <automated>grep -nE "isB|=== \"Agent A\"|=== \"A\"" dashboard/web/components/trades/TradeTable.tsx dashboard/web/components/positions/PositionCard.tsx; test -z "$(grep -nE 'isB' dashboard/web/components/trades/TradeTable.tsx)"</automated>
  </verify>
  <done>Neither component contains binary A/B branching; a bot_id="D" trade row and position card render a "D" badge. Build/typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| dashboard env → seed_bots → bots table | Suffixed `_D` keys seed attribution; wrong key → wrong account shown |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Information disclosure | Cross-bot P&L attribution | mitigate | KNOWN_BOTS + per-bot `ALPACA_API_KEY_{bot_id}` keep D's equity partitioned; D row keyed on bot_id='D' |
| T-09-04 | Tampering | UI bot badge | mitigate | Generic label derivation removes binary mislabel (D shown as D, not A) |
</threat_model>

<verification>
- `python -m pytest tests/test_dashboard_db.py tests/test_seed_bots.py -x` green
- Full backend suite green: `python -m pytest -q`
- `grep -n '"D"' dashboard/api/db.py` shows D in KNOWN_BOTS
- Dashboard web build/typecheck passes (no TS errors in the two edited components)
</verification>

<success_criteria>
Dashboard lists Bot D in aggregates, seeds its row from `_D` env, and labels its trades/positions as "D". BOT-03 fully code-complete.
</success_criteria>

<output>
Create `.planning/phases/09-bot-d-deployment/09-02-SUMMARY.md` when done.
</output>
