---
phase: 09-bot-d-deployment
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - docs/deployment/bot-d-coolify-recipe.md
autonomous: false
requirements: [BOT-01, BOT-02]
user_setup:
  - service: alpaca
    why: "Bot D needs its OWN paper account (one-account-per-bot HARD RULE) — produces D's keys"
    env_vars:
      - name: ALPACA_API_KEY_D
        source: "Alpaca dashboard → Bot D paper account → Generate API keys"
      - name: ALPACA_SECRET_KEY_D
        source: "Alpaca dashboard → Bot D paper account (secret shown once)"
    dashboard_config:
      - task: "Create Bot D paper trading account (must NOT be A/B/C's account)"
        location: "https://app.alpaca.markets"
  - service: coolify
    why: "Bot D runs as a separate orchestrator service (BOT-02)"
    dashboard_config:
      - task: "Create Bot D orchestrator service mirroring Bot B; add bare ALPACA_API_KEY/SECRET; add suffixed _D keys to existing dashboard service"
        location: "Coolify project u7x0xw0y4qvcgeh8vyidsgyi"
must_haves:
  truths:
    - "CLAUDE.md Alpaca Accounts table includes a Bot D row with its env vars and profile"
    - "An exact, repeatable Coolify env recipe for Bot D's service exists (bare keys on orchestrator, suffixed _D on dashboard)"
    - "The post-deploy one-account-per-bot verification (D account number != A/B/C) is documented as a required step"
    - "LEARNING_SHADOW_UNTIL_TRADES is documented"
  artifacts:
    - path: "docs/deployment/bot-d-coolify-recipe.md"
      provides: "HALT recipe: Alpaca account creation + Coolify service env + post-deploy account-number check"
      contains: "ALPACA_API_KEY"
    - path: "CLAUDE.md"
      provides: "Bot D row in Alpaca Accounts table + LEARNING_SHADOW_UNTIL_TRADES doc"
      contains: "Bot D"
  key_links:
    - from: "docs/deployment/bot-d-coolify-recipe.md"
      to: "one-account-per-bot HARD RULE"
      via: "post-deploy /v2/account number check"
      pattern: "account number"
---

<objective>
D-05 + BOT-02 infra handoff: document Bot D so future sessions know it exists, and surface the two outward-facing INFRA steps (Alpaca paper-account signup, Coolify service creation) as explicit authorized human steps — NOT faked (D-04 HALT boundary).

Purpose: BOT-02 and the live-account half of BOT-01 are infra-deferred. This plan delivers the docs + exact recipe so the handoff is unambiguous; the live account/service creation itself is performed by the human (or via Coolify API only if explicitly authorized).
Output: Bot D row in CLAUDE.md, LEARNING_SHADOW_UNTIL_TRADES documented, and a deployment recipe doc with the HALT steps + post-deploy verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-bot-d-deployment/09-RESEARCH.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document Bot D in CLAUDE.md + write the Coolify recipe doc</name>
  <files>CLAUDE.md, docs/deployment/bot-d-coolify-recipe.md</files>
  <action>CLAUDE.md "Alpaca Accounts — ONE ACCOUNT PER BOT" table: add a Bot D row — env vars `ALPACA_API_KEY_D` / `ALPACA_SECRET_KEY_D` (note: these are the SUFFIXED dashboard vars; the orchestrator service uses BARE `ALPACA_API_KEY`/`SECRET`), `BOT_ID=D`, `BOT_PROFILE=daytrade`, "Bot D's paper account". Add a short note documenting `LEARNING_SHADOW_UNTIL_TRADES` (Phase 8, default 30 — shadow-mode learning gate) since D inherits it. Create docs/deployment/bot-d-coolify-recipe.md capturing RESEARCH §Code-vs-Infra (b): the two HALT steps verbatim — HALT-1 (create Bot D Alpaca PAPER account, generate keys), HALT-2 (mirror Bot B's orchestrator service in Coolify project u7x0xw0y4qvcgeh8vyidsgyi). Include the orchestrator env table (BARE keys: BOT_ID=D, BOT_PROFILE=daytrade, ALPACA_API_KEY, ALPACA_SECRET_KEY, ALPACA_ENV=paper, DATABASE_URL shared, LEARNING_SHADOW_UNTIL_TRADES=30, BOT_LABEL="Agent D") AND the dashboard env addition (SUFFIXED ALPACA_API_KEY_D / _SECRET_KEY_D on the EXISTING dashboard app — do not recreate). State the two anti-patterns (no `_D` suffix on orchestrator config.py; never reuse A/B/C account). Document the MANDATORY post-deploy check: GET D's `/v2/account` and confirm the account number differs from A/B/C (one-account-per-bot HARD RULE / pitfall 2). Note assumption A1 (verify Bot B's live service literally uses bare ALPACA_API_KEY before creating D's). Do NOT fabricate keys or claim the account/service was created.</action>
  <verify>
    <automated>grep -q "Bot D" CLAUDE.md && grep -q "LEARNING_SHADOW_UNTIL_TRADES" CLAUDE.md && grep -qi "account number" docs/deployment/bot-d-coolify-recipe.md && grep -q "ALPACA_API_KEY" docs/deployment/bot-d-coolify-recipe.md</automated>
  </verify>
  <done>CLAUDE.md has the Bot D row + LEARNING_SHADOW_UNTIL_TRADES; recipe doc contains both HALT steps, both env tables, and the post-deploy account-number check.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: HALT — create Bot D Alpaca account + Coolify service (authorized human/API steps)</name>
  <action>Present this checkpoint to the user and BLOCK. Do not fabricate keys or claim the account/service exists. If the user authorizes and Coolify API creds are present in ~/.claude/secrets/services.json, create the orchestrator service via the Coolify API per the recipe (HALT-2) and run the post-deploy account-number check; otherwise hand off. Record the approved/deferred outcome in the SUMMARY.</action>
  <what-built>All Bot D CODE is complete (09-01, 09-02) and the exact deployment recipe is documented (docs/deployment/bot-d-coolify-recipe.md). The two remaining steps are outward-facing and require your authorization — they cannot be faked.</what-built>
  <how-to-verify>
    1. Create Bot D's Alpaca PAPER account at https://app.alpaca.markets — must be a NEW account, not A/B/C's. Generate API keys; record KEY + SECRET (secret shown once).
    2. Provide the keys for storage in Coolify env only (never commit). Add suffixed `ALPACA_API_KEY_D`/`ALPACA_SECRET_KEY_D` to the EXISTING dashboard service; set BARE `ALPACA_API_KEY`/`ALPACA_SECRET_KEY` on the new orchestrator service.
    3. Create Bot D's Coolify orchestrator service mirroring Bot B (project u7x0xw0y4qvcgeh8vyidsgyi) with the env table from the recipe (BOT_ID=D, BOT_PROFILE=daytrade, ALPACA_ENV=paper, shared DATABASE_URL, LEARNING_SHADOW_UNTIL_TRADES=30). Command: `python -m src.alpaca_orchestrator --mode paper`.
    4. POST-DEPLOY CHECK (mandatory): GET D's `/v2/account` and confirm the account number differs from A/B/C. If identical → STOP, one-account-per-bot HARD RULE violated.
    If you authorize Coolify-API automation AND creds are present in ~/.claude/secrets/services.json, step 3 may be done via the Coolify API instead of the UI. Step 1 cannot be automated.
  </how-to-verify>
  <resume-signal>Type "approved" once the account + service exist and the account-number check passed, or paste the keys to proceed with authorized Coolify-API creation. Reply "defer" to ship the code/docs now and leave infra for a later session (recorded in SUMMARY).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| human → Alpaca signup → keys → Coolify env | Outward-facing brokerage account + prod service provisioning |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Spoofing | Bot D account == A/B/C account | mitigate | Mandatory post-deploy `/v2/account` number comparison documented as a blocking step (pitfall 2) |
| T-09-06 | Repudiation | Faked infra completion | mitigate | HALT checkpoint requires human "approved"/"defer"; code never fabricates keys or claims account creation |
| T-09-07 | Information disclosure | Alpaca secret in git | mitigate | Recipe instructs Coolify env / ~/.claude/secrets only; never commit |
</threat_model>

<verification>
- `grep -q "Bot D" CLAUDE.md` and `grep -q "LEARNING_SHADOW_UNTIL_TRADES" CLAUDE.md`
- Recipe doc contains both HALT steps, orchestrator + dashboard env tables, and the account-number check
- SUMMARY records the infra-deferred status (BOT-02 + live-account BOT-01) and whether the human approved or deferred
</verification>

<success_criteria>
Bot D documented for future sessions; the two HALT infra steps surfaced as authorized human actions with an exact, verified-on-check recipe; nothing faked.
</success_criteria>

<output>
Create `.planning/phases/09-bot-d-deployment/09-03-SUMMARY.md` when done. Record BOT-02 and the live-account half of BOT-01 as infra-deferred (with approved/deferred outcome).
</output>
