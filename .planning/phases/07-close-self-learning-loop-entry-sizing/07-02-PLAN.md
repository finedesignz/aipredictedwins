---
phase: 07-close-self-learning-loop-entry-sizing
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/bot_thread.py
  - tests/test_learning_wiring.py
autonomous: true
requirements: [LEARN-01, LEARN-02, LEARN-03]
must_haves:
  truths:
    - "bot_thread LONG path passes confidence_adjustment and dynamic min/max into _kelly_technical (no second veto added)"
    - "bot_thread SHORT path consults get_advice, vetoes on should_trade=False, scales size, and records trade context"
    - "veto and scale are gated behind LEARNING_ENFORCE (default enforce); LEARNING_ENFORCE=0 logs would-be effect only"
    - "short signal_type is canonical technical_short_{score} in both get_advice and record_trade_context"
    - "memory=None path is unchanged (no veto/scale)"
  artifacts:
    - path: "src/bot_thread.py"
      provides: "Fully wired LONG (extended) + SHORT (fresh) learning consumption in the live runtime"
      contains: "LEARNING_ENFORCE"
  key_links:
    - from: "src/bot_thread.py"
      to: "_kelly_technical"
      via: "passes confidence_adjustment + min/max from advice/thresholds"
      pattern: "confidence_adjustment="
    - from: "src/bot_thread.py SHORT"
      to: "memory.record_trade_context"
      via: "records short context with technical_short_ signal_type"
      pattern: "technical_short_"
---

<objective>
Wire learning consumption into the LIVE runtime (bot_thread.py — D-03 priority). LONG path already
vetoes; EXTEND it (add scaling + dynamic thresholds, no second veto). SHORT path has nothing; ADD
the full advisory block (veto + scale + thresholds) AND the missing record_trade_context.

Purpose: bot_thread is the production path. Closing the loop here is what actually changes trades.
Output: bot_thread.py with both paths consuming advice/thresholds, gated by LEARNING_ENFORCE.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-close-self-learning-loop-entry-sizing/07-RESEARCH.md
@.planning/phases/07-close-self-learning-loop-entry-sizing/07-01-SUMMARY.md
@src/bot_thread.py

<interfaces>
LONG block at bot_thread.py:513-549: advice already computed (514-533), veto at 522 (continue),
fee gate 535-541, _kelly_technical at 543-549 (kelly_fraction=cfg.kelly_fraction,
max_position_pct=cfg.max_position_pct). record_trade_context at 591-614 (signal_type
"technical_confluence_{score}"). signal.confluence_score is /4 in this file.
SHORT block at 673-745: signal_type local var "technical_short_{short_score}" (682);
_kelly_technical 698-704; NO get_advice, NO record_trade_context. sentiment must be short_score/4.0.
Extended _kelly_technical now accepts confidence_adjustment=1.0, min_position_pct=None (from 07-01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LEARNING_ENFORCE flag + per-cycle thresholds + extend LONG path</name>
  <files>src/bot_thread.py</files>
  <action>Add module-level LEARNING_ENFORCE = os.environ.get("LEARNING_ENFORCE", "1") == "1" near
  the other module constants. In the entry cycle, after `memory` is known and BEFORE the candidate
  loop, compute thresholds ONCE per cycle: `thresholds = memory.get_dynamic_thresholds() if memory
  is not None else None` (RESEARCH pitfall 5 — never per-candidate). In the LONG block (513-549):
  do NOT add a second veto — keep the existing 522 veto but wrap the `continue` so it only fires
  when LEARNING_ENFORCE (log the skip regardless; when not enforcing, log would-be veto and fall
  through). Capture adj = advice["confidence_adjustment"] if LEARNING_ENFORCE else 1.0. At the
  _kelly_technical call (543), pass confidence_adjustment=adj, min_position_pct=(thresholds and
  thresholds["min_position_pct"] if LEARNING_ENFORCE else None), and
  max_position_pct=min(cfg.max_position_pct, thresholds["max_position_pct"]) when thresholds and
  LEARNING_ENFORCE else cfg.max_position_pct (static MAX always wins — D-04). All new reads guarded
  by `if memory is not None` so memory=None is unchanged (pitfall 4). Keep sentiment=confluence_score/4.0
  consistent with recorded value (D-02 / open question 2).</action>
  <verify>
    <automated>python -m pytest tests/test_learning_wiring.py -q 2>&1 | tail -6</automated>
  </verify>
  <done>LONG path passes adjustment + dynamic caps; single veto wrapped in LEARNING_ENFORCE; memory=None untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add full advisory block + record_trade_context to SHORT path</name>
  <files>src/bot_thread.py</files>
  <action>In the SHORT block (673-745) mirror the LONG advisory logic with side-correct values:
  signal_type = f"technical_short_{short_score}" (canonical per D-02, matches existing 682);
  call memory.get_advice(symbol=symbol, signal_type=signal_type, sentiment=short_score/4.0,
  price_change=change_pct) inside `if memory is not None`; veto-with-continue gated by
  LEARNING_ENFORCE (log learn_veto); pass confidence_adjustment + min/max (same min(static,dynamic)
  rule) into _kelly_technical at 698. After the short order is placed and trade_id obtained, ADD
  memory.record_trade_context({...}) mirroring the LONG recorder (591-614) with signal_type
  technical_short_{score}, sentiment=short_score/4.0, bear-leaning trajectory/arguments — this is
  currently MISSING and short advice is dead without it (RESEARCH open question 1, decided: include).
  Wrap in try/except like LONG. Un-skip tests/test_learning_wiring.py::test_signal_type_alignment.</action>
  <verify>
    <automated>python -m pytest tests/test_learning_wiring.py -q 2>&1 | tail -6</automated>
  </verify>
  <done>SHORT path vetoes, scales, and records context with technical_short_ signal_type; LEARNING_ENFORCE gates effect.</done>
</task>

<task type="auto">
  <name>Task 3: Un-skip path tests + full-suite gate</name>
  <files>tests/test_learning_wiring.py, src/bot_thread.py</files>
  <action>Un-skip test_veto_skips_candidate and test_shadow_mode_no_effect (drive the candidate loop
  / advice consumption with the fake TradeMemory fixture; assert vetoed candidate places no order,
  and LEARNING_ENFORCE=0 produces no veto/scale via monkeypatch of the module flag). Run full suite.</action>
  <verify>
    <automated>python -m pytest -q 2>&1 | tail -6</automated>
  </verify>
  <done>Full suite green; veto + shadow tests pass; 217 existing tests unaffected when memory=None.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| learning DB → live order placement | advice vetoes/scales real (paper) orders in bot_thread |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | DoS | double-veto regression on LONG path | mitigate | Extend existing veto only; no second veto; covered by test_veto_skips_candidate |
| T-07-04 | Tampering | signal_type drift makes advice silently empty | mitigate | Canonical technical_short_{score}; test_signal_type_alignment |
| T-07-05 | Elevation | dynamic max breaching static cap on live orders | mitigate | min(cfg.max_position_pct, dynamic) at call site |
</threat_model>

<verification>
- python -m pytest -q → green
- LEARNING_ENFORCE present; both paths consume advice/thresholds; SHORT records context
</verification>

<success_criteria>
- LONG extended (no second veto), SHORT fully wired + recording, all gated by LEARNING_ENFORCE
- memory=None path behaviorally identical to today
</success_criteria>

<output>
Create `.planning/phases/07-close-self-learning-loop-entry-sizing/07-02-SUMMARY.md` when done
</output>
