---
phase: 07-close-self-learning-loop-entry-sizing
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/alpaca_orchestrator.py
  - tests/test_learning_wiring.py
autonomous: true
requirements: [LEARN-01, LEARN-02, LEARN-03]
must_haves:
  truths:
    - "orchestrator LONG path consults get_advice (currently missing), vetoes, scales, feeds dynamic caps"
    - "orchestrator SHORT path consults get_advice, vetoes, scales, and records trade context with canonical technical_short_{score}"
    - "veto and scale gated behind the same LEARNING_ENFORCE flag; LEARNING_ENFORCE=0 logs would-be effect only"
    - "orchestrator behaviorally identical to bot_thread for the same inputs"
    - "memory=None path unchanged"
  artifacts:
    - path: "src/alpaca_orchestrator.py"
      provides: "Fresh learning consumption on both orchestrator entry paths, mirroring bot_thread"
      contains: "LEARNING_ENFORCE"
  key_links:
    - from: "src/alpaca_orchestrator.py LONG"
      to: "memory.get_advice"
      via: "fresh advisory call before sizing"
      pattern: "get_advice\\("
    - from: "src/alpaca_orchestrator.py SHORT"
      to: "memory.record_trade_context"
      via: "records short context with canonical technical_short_"
      pattern: "technical_short_"
---

<objective>
Wire learning consumption into the legacy CLI runtime (alpaca_orchestrator.py). Neither path
consults advice today. ADD fresh advisory blocks (veto + scale + dynamic thresholds) to BOTH long
and short, mirroring bot_thread exactly (D-03 behaviorally identical), and ADD the missing short
record_trade_context with the canonical technical_short_{score} string (NOT the short_technical_
order-log string — RESEARCH signal_type divergence trap).

Purpose: Keep both runtimes identical so a future consolidation is safe and tests cover both.
Output: alpaca_orchestrator.py with both paths fully wired, gated by LEARNING_ENFORCE.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-close-self-learning-loop-entry-sizing/07-RESEARCH.md
@.planning/phases/07-close-self-learning-loop-entry-sizing/07-01-SUMMARY.md
@src/alpaca_orchestrator.py

<interfaces>
LONG block 852-870: NO get_advice; _kelly_technical 864-870 (max_position_pct=MAX_POSITION_PCT);
record_trade_context 922-931 (signal_type "technical_confluence_{confluence_score}", 924).
SHORT block 940-982: NO get_advice; _kelly_technical 976-982 (max_position_pct=MAX_POSITION_PCT);
NO record_trade_context — order log uses market_sentiment "short_technical_{score}" (1006) which is
NOT the canonical string. Use technical_short_{score} for context + advice.
memory = TradeMemory() at 586 (when _HAS_LEARNING). Module constants MAX_POSITION_PCT,
MAX_TOTAL_EXPOSURE_PCT. _kelly_technical lives in this file; 07-01 added confidence_adjustment +
min_position_pct kwargs. Define LEARNING_ENFORCE here too (same env semantics as bot_thread).
sentiment uses /4.0 for advice lookups (open question 2 — standardize on /4.0).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LEARNING_ENFORCE + per-cycle thresholds + LONG advisory block</name>
  <files>src/alpaca_orchestrator.py</files>
  <action>Add module-level LEARNING_ENFORCE = _os.environ.get("LEARNING_ENFORCE", "1") == "1" near
  MAX_POSITION_PCT. Compute thresholds = memory.get_dynamic_thresholds() ONCE per entry cycle when
  memory is not None (not per-candidate — pitfall 5). In the LONG block (852-870), before the
  _kelly_technical call, ADD a fresh advisory block guarded by `if memory is not None`: call
  get_advice(symbol, signal_type=f"technical_confluence_{confluence_score}", sentiment=score/4.0,
  price_change=...); if not advice["should_trade"]: log learn_veto and `continue` when
  LEARNING_ENFORCE (log-only otherwise). Pass confidence_adjustment=(advice adj if LEARNING_ENFORCE
  else 1.0), min_position_pct=(thresholds["min_position_pct"] if thresholds and LEARNING_ENFORCE
  else None), max_position_pct=min(MAX_POSITION_PCT, thresholds["max_position_pct"]) when thresholds
  and LEARNING_ENFORCE else MAX_POSITION_PCT (static hard cap always wins — D-04). Mirror bot_thread
  LONG semantics exactly.</action>
  <verify>
    <automated>python -m pytest tests/test_learning_wiring.py -q 2>&1 | tail -6</automated>
  </verify>
  <done>LONG path consults advice, vetoes/scales/feeds caps; memory=None unchanged; LEARNING_ENFORCE gates.</done>
</task>

<task type="auto">
  <name>Task 2: SHORT advisory block + canonical record_trade_context</name>
  <files>src/alpaca_orchestrator.py</files>
  <action>In the SHORT block (940-982): ADD advisory block mirroring Task 1 with
  signal_type=f"technical_short_{score}" (canonical — NOT short_technical_), sentiment=score/4.0;
  veto-with-continue gated by LEARNING_ENFORCE; pass confidence_adjustment + min/max (same
  min(MAX_POSITION_PCT, dynamic) rule) into _kelly_technical at 976. After the short order places and
  trade_id is obtained, ADD memory.record_trade_context({...}) mirroring the LONG recorder (922-931)
  with signal_type technical_short_{score} — currently MISSING. Leave the existing order-log
  market_sentiment string as-is (it is in alpaca_trades, not trade_context; do not migrate). Wrap in
  try/except. This unblocks short advice for both runtimes.</action>
  <verify>
    <automated>python -m pytest tests/test_learning_wiring.py -q 2>&1 | tail -6</automated>
  </verify>
  <done>SHORT path vetoes/scales and records context with technical_short_; no short_technical_ in context.</done>
</task>

<task type="auto">
  <name>Task 3: Behavioral-parity assertion + full-suite gate</name>
  <files>tests/test_learning_wiring.py, src/alpaca_orchestrator.py</files>
  <action>Un-skip test_adjustment_scales_size_in_path; add an assertion that orchestrator and
  bot_thread produce the same sizing for identical inputs + fake memory (parity per D-03). Run full
  suite to confirm defaults keep 217 existing tests green.</action>
  <verify>
    <automated>python -m pytest -q 2>&1 | tail -6</automated>
  </verify>
  <done>Full suite green; both runtimes behaviorally identical; memory=None path unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| learning DB → orchestrator order placement | advice vetoes/scales orders in the CLI runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Tampering | short_technical_ vs technical_short_ divergence → silent empty advice | mitigate | Canonical technical_short_ in context+advice; parity test |
| T-07-07 | Elevation | dynamic max breaching MAX_POSITION_PCT | mitigate | min(MAX_POSITION_PCT, dynamic) at call site |
| T-07-08 | Repudiation | short trades not recorded → no learning signal | mitigate | Add short record_trade_context |
</threat_model>

<verification>
- python -m pytest -q → green
- Both orchestrator paths consume advice/thresholds identically to bot_thread; short records context
</verification>

<success_criteria>
- LONG + SHORT fresh-wired, short recording added, gated by LEARNING_ENFORCE
- Behaviorally identical to bot_thread; memory=None unchanged
</success_criteria>

<output>
Create `.planning/phases/07-close-self-learning-loop-entry-sizing/07-03-SUMMARY.md` when done
</output>
