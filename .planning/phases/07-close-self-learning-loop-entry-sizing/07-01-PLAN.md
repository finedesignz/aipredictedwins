---
phase: 07-close-self-learning-loop-entry-sizing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_learning_wiring.py
  - tests/conftest.py
  - src/alpaca_orchestrator.py
autonomous: true
requirements: [LEARN-02, LEARN-03]
must_haves:
  truths:
    - "_kelly_technical accepts confidence_adjustment and min_position_pct without breaking existing 217 tests"
    - "confidence_adjustment scales the position size before the hard cap clamp"
    - "adjustment up to 1.5 with high Kelly never produces adjusted_pct > effective max_position_pct"
    - "dynamic min_position_pct floors the position; dynamic max ceiling never exceeds static MAX_POSITION_PCT"
    - "tests/test_learning_wiring.py exists with failing-then-passing assertions for LEARN-01/02/03 + shadow + signal_type"
  artifacts:
    - path: "tests/test_learning_wiring.py"
      provides: "Unit tests for veto, scaling, dynamic thresholds, hard cap, shadow, signal_type alignment"
      contains: "def test_"
    - path: "src/alpaca_orchestrator.py"
      provides: "Extended _kelly_technical signature with confidence_adjustment + min_position_pct"
      contains: "confidence_adjustment"
  key_links:
    - from: "tests/test_learning_wiring.py"
      to: "_kelly_technical"
      via: "direct import + call with adjustment kwargs"
      pattern: "_kelly_technical\\("
---

<objective>
Establish the test contract for Phase 7 learning consumption and extend the shared sizing
primitive `_kelly_technical` (defined in alpaca_orchestrator.py, imported by bot_thread.py) to
accept learning inputs. This is Wave 0 (test scaffold) + the sizing contract both downstream
wiring plans depend on.

Purpose: Tests must exist before wiring (Nyquist). The signature change is the single seam both
entry-path plans build against — landing it first prevents a scavenger hunt.
Output: tests/test_learning_wiring.py (RED then GREEN for the sizing math), conftest fixture for a
fake TradeMemory, extended _kelly_technical.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-close-self-learning-loop-entry-sizing/07-RESEARCH.md
@.planning/phases/07-close-self-learning-loop-entry-sizing/07-CONTEXT.md
@src/alpaca_orchestrator.py
@tests/test_fee_gate.py

<interfaces>
Current _kelly_technical (src/alpaca_orchestrator.py:384) — imported into bot_thread.py:66:
  def _kelly_technical(confluence, current_price, bankroll,
                       kelly_fraction=0.25, max_position_pct=0.05) -> dict
  returns {side, kelly_pct, adjusted_pct, dollar_amount, shares, capped}
  current body: adjusted_pct = kelly_pct * kelly_fraction; capped = adjusted_pct > max_position_pct; clamp.

get_advice(symbol, signal_type, sentiment, price_change) -> dict
  should_trade: bool (False only when win_rate<0.30 AND >=3 closed similar)
  confidence_adjustment: float in [0.0, 1.5]
  win_rate_for_pattern, sample_size, reasoning, similar_trades, lessons

get_dynamic_thresholds() -> dict
  min_position_pct (0.01-0.02), max_position_pct (0.03-0.05),
  bullish_threshold, bearish_threshold, signal_scores, overall_win_rate, total_closed_trades
  no-data default: min 0.02 / max 0.05.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test scaffold + fake TradeMemory fixture</name>
  <files>tests/test_learning_wiring.py, tests/conftest.py</files>
  <behavior>
    - test_adjustment_scales_size: _kelly_technical(confluence=5, ..., confidence_adjustment=0.5)
      yields adjusted_pct == half of the unadjusted adjusted_pct (pre-cap), for inputs where the
      unadjusted result is below the cap.
    - test_hard_cap_inviolate: _kelly_technical(confluence=5, bankroll large, confidence_adjustment=1.5,
      max_position_pct=0.05) → adjusted_pct == 0.05 and capped is True. Effective max passed by caller
      is min(static, dynamic); test that passing max_position_pct=0.05 caps regardless of adjustment.
    - test_dynamic_thresholds_applied: min_position_pct=0.02 floors a tiny position up to 0.02;
      max_position_pct=0.03 ceilings a large one to 0.03.
    - test_min_floor_not_applied_to_zero: confluence < MIN_CONFLUENCE → side "none", floor never raises it.
    - Placeholder/xfail-free stubs for veto/shadow/signal_type covered by plans 02/03:
      test_veto_skips_candidate, test_adjustment_scales_size_in_path, test_signal_type_alignment,
      test_shadow_mode_no_effect — these may import the candidate-loop helpers and will go GREEN
      after plans 02/03; mark with pytest.mark.skip(reason="wired in 07-02/07-03") so this plan's
      suite is green, downstream plans un-skip.
  </behavior>
  <action>Create tests/test_learning_wiring.py importing _kelly_technical from src.alpaca_orchestrator.
  Mirror the pure-function, monkeypatched-constants style of tests/test_fee_gate.py. Add a fake
  TradeMemory class in tests/conftest.py (or inline) exposing get_advice(...) and
  get_dynamic_thresholds() returning canned dicts (constructor takes the canned values) so plans
  02/03 can stub the candidate loop without a real DB. Write the four _kelly_technical math tests to
  assert against the NEW behavior (scale-then-floor-then-cap). Mark the path-level tests skip per
  D-LEARN-01/02 (un-skipped by downstream plans). Run RED first (math tests fail against current
  signature), then implement Task 2 to go GREEN.</action>
  <verify>
    <automated>python -m pytest tests/test_learning_wiring.py -q 2>&1 | tail -5</automated>
  </verify>
  <done>test_learning_wiring.py exists; math tests fail pre-Task-2 (RED) then pass post-Task-2; skipped path tests do not error; fake TradeMemory importable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend _kelly_technical with confidence_adjustment + min_position_pct (LEARN-02, LEARN-03)</name>
  <files>src/alpaca_orchestrator.py</files>
  <behavior>
    - New kwargs confidence_adjustment: float = 1.0, min_position_pct: float | None = None.
    - Order after `adjusted_pct = kelly_pct * kelly_fraction` (line 414):
      1. adjusted_pct *= confidence_adjustment  (LEARN-02)
      2. if min_position_pct and adjusted_pct > 0: adjusted_pct = max(adjusted_pct, min_position_pct)  (LEARN-03 floor)
      3. existing cap clamp at max_position_pct stays LAST (hard ceiling inviolate).
    - Defaults (1.0, None) preserve current behavior → 217 existing tests green.
  </behavior>
  <action>Per D-04/D-05 and RESEARCH _kelly_technical recommendation: add the two kwargs to the
  signature at src/alpaca_orchestrator.py:384 and insert steps 1-2 immediately after line 414, leaving
  the capped-clamp (416-418) last. Do NOT change the early-return MIN_CONFLUENCE branch. The caller is
  responsible for passing max_position_pct = min(static MAX_POSITION_PCT, dynamic_max) so the static
  hard cap always wins — this function just clamps to the max it receives.</action>
  <verify>
    <automated>python -m pytest tests/test_learning_wiring.py tests/test_position_sizer.py -q 2>&1 | tail -8</automated>
  </verify>
  <done>Math tests in test_learning_wiring.py pass; existing sizing tests still pass; full suite unaffected by defaults.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite regression gate</name>
  <files>src/alpaca_orchestrator.py</files>
  <action>Run the full pytest suite to confirm the signature extension with default kwargs breaks
  none of the 217 existing tests (Karpathy: minimal diff, no behavior change when learning absent).</action>
  <verify>
    <automated>python -m pytest -q 2>&1 | tail -5</automated>
  </verify>
  <done>Full suite green (217+ existing plus new math tests; path tests skipped).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| learning DB → sizing | TradeMemory advice/thresholds influence dollar amount; must not breach hard caps |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | confidence_adjustment scaling position size | mitigate | Hard cap clamp applied LAST; test_hard_cap_inviolate proves adjustment=1.5 cannot exceed max_position_pct |
| T-07-02 | Elevation | dynamic max_position_pct exceeding static MAX_POSITION_PCT | mitigate | Caller passes min(static, dynamic); function clamps to received max only |
| T-07-SC | Tampering | package installs | accept | No new packages this phase (pure in-process wiring) |
</threat_model>

<verification>
- python -m pytest -q → full suite green
- _kelly_technical signature has confidence_adjustment + min_position_pct with backward-compatible defaults
</verification>

<success_criteria>
- tests/test_learning_wiring.py committed with math + skipped path tests
- _kelly_technical extended, hard cap inviolate, 217 existing tests still pass
</success_criteria>

<output>
Create `.planning/phases/07-close-self-learning-loop-entry-sizing/07-01-SUMMARY.md` when done
</output>
