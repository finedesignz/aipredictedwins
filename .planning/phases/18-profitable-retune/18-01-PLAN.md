---
phase: 18-profitable-retune
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_external_exit_resolution.py
  - tests/test_db_readonly.py
  - tests/test_db.py
  - tests/backtester/test_cli_overrides.py
  - tests/backtester/test_engine.py
  - tests/test_rollout_config.py
  - dashboard/api/tests/test_portfolio_win_rate.py
autonomous: true
requirements: [TUNE-01, TUNE-03]

must_haves:
  truths:
    - "Every one of the 29 cases in 18-VALIDATION.md has a named test. RED first: the suite FAILS before any src/ or dashboard/ change lands."
    - "Case 2 asserts `pnl is None`, NOT `pnl == 0` — `0 == False` and falsy-None are the exact traps this phase exists to close."
    - "Case 4 (the slash trap) uses live_symbols={'BTCUSD'} (slash-STRIPPED, exactly as src/alpaca_orchestrator.py:159 builds it) against row['symbol']='BTC/USD' and asserts NO WRITE. Getting this wrong mass-closes every held position."
    - "Case 5 asserts a raising Alpaca call yields {} — a transient error NEVER writes a NULL-terminal row."
    - "Case 18 asserts the confluence ceiling is 4 (each scoring branch has exactly four `score += 1`) and that min_confluence=5 yields ZERO trades. The score is NOT to be 'fixed'."
    - "Every static/grep-style case runs a POSITIVE CONTROL first (the scanned slice is non-empty and contains a known-present token) so it cannot pass vacuously."
    - "No test touches the network, prod Alpaca, or DATABASE_URL. DB-gated cases use TEST_DATABASE_URL only and SKIP (visibly) when unset."
  artifacts:
    - path: "tests/test_external_exit_resolution.py"
      provides: "VALIDATION cases 1-8 — the sentinel writer never fabricates"
      min_lines: 120
    - path: "tests/test_db_readonly.py"
      provides: "VALIDATION cases 23-25 — AIPW_DB_READONLY"
      min_lines: 50
    - path: "tests/backtester/test_cli_overrides.py"
      provides: "VALIDATION cases 14-17, 22 — the new CLI knobs + the Kelly ceiling"
      min_lines: 80
    - path: "tests/test_rollout_config.py"
      provides: "VALIDATION cases 26-29 — config-only rollout, Kelly ceiling at the API, prod fence"
      min_lines: 60
    - path: "dashboard/api/tests/test_portfolio_win_rate.py"
      provides: "VALIDATION case 13 — the dashboard's TWO duplicated denominators"
      min_lines: 40
  key_links:
    - from: "tests/test_external_exit_resolution.py"
      to: "src.alpaca_orchestrator._resolve_external_exit"
      via: "the pure unit under test (does not exist yet — that is the RED)"
      pattern: "_resolve_external_exit"
    - from: "tests/test_external_exit_resolution.py"
      to: "tests/test_backfill.py FakeAlpaca"
      via: "reuse the existing fake double — no network"
      pattern: "FakeAlpaca"
---

<objective>
Wave 0: write the RED suite for all 29 cases in `18-VALIDATION.md`. Nothing in `src/` or `dashboard/`
changes in this plan.

Purpose: Phase 18 touches the position monitor (which can mass-close live positions), the paper-gate
win-rate readout, and the sizing knobs. Every one of those is a way to lose money quietly. The tests
go first so that each downstream fix is proven by a test that was failing for the right reason before
the fix landed.

Output: seven test files (five new, two extended), the whole Phase-18 spec, RED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-profitable-retune/18-VALIDATION.md
@.planning/phases/18-profitable-retune/18-RESEARCH.md
@CLAUDE.md

<interfaces>
The unit under test in cases 1-8 DOES NOT EXIST YET. Plan 18-03 creates it. Its contract is fixed here:

  # src/alpaca_orchestrator.py
  def _resolve_external_exit(alpaca, row: dict, live_symbols: set[str] | None) -> dict:
      """Resolve a vanished position to a REAL exit, or to NULL. NEVER fabricates.
      Returns update_alpaca_trade kwargs, or {} meaning DO NOT WRITE."""

  Returns:
    {}                                                            -> still held / in-flight / Alpaca errored
    {"status": "closed", "exit_price": <float>, "pnl": <float>, "fees": <float|None>}   -> resolved
    {"status": "closed", "exit_price": None, "pnl": None, "fees": None}                 -> unresolvable
    {"status": "canceled", "exit_price": None, "pnl": 0, "fees": None}                  -> terminal 0-fill (honest zero)

Existing pure pieces the tests assert against (import them; do NOT re-derive):
  src/backfill.py:51   resolve_stale_row(row, entry_order, live_symbols, close_order) -> (outcome, kwargs|None)
  src/backfill.py:92   _match_close(closes, entry_order, row) -> order|None   (_QTY_TOLERANCE at :124-127)
  src/pnl.py:10        realized_pnl(side, entry_fill, exit_fill, qty, taker_fee) -> float   (fee-net BOTH legs)
  src/order_resolution.py:14  classify_order(order) -> (db_status, pnl)
  src/universe.py:17   normalize(symbol) -> str
  src/universe.py:26   entry_allowed(symbol, symbols, quarantined) -> (bool, reason|None)

Fake double: reuse `FakeAlpaca` from tests/test_backfill.py:96-120 (get_order / get_positions /
get_closed_orders; no network). Import it or copy the pattern — do not write a second Alpaca fake shape.

Backtester surface under test (fields Plan 18-05 will ADD to the frozen PhaseConfig):
  symbols: tuple[str, ...] = ()      quarantined: tuple[str, ...] = ()      rsi_ceiling: float = 65.0
  CLI: --min-confluence, --kelly-fraction, --symbols, --exclude-symbols
  PhaseConfig is frozen=True -> construct variants with dataclasses.replace().
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tests/test_external_exit_resolution.py — VALIDATION cases 1-8 (the sentinel writer)</name>
  <files>tests/test_external_exit_resolution.py</files>
  <read_first>18-VALIDATION.md cases 1-8; tests/test_backfill.py L1-140 (FakeAlpaca + the row/order dict shapes); src/backfill.py L51-128 (resolve_stale_row branches, _match_close, _QTY_TOLERANCE); src/pnl.py L10-38 (realized_pnl + TAKER_FEE); src/order_resolution.py L14-28; src/alpaca_orchestrator.py L148-180 (the block being replaced — note live_symbols is built SLASH-STRIPPED at :159); src/alpaca_client.py L401-464 (get_order / get_closed_orders / _parse_order field names)</read_first>
  <behavior>
    - case 1 test_fabrication_literals_are_gone (static): read src/alpaca_orchestrator.py, slice the
      monitor reconcile block. POSITIVE CONTROL FIRST: assert the slice is non-empty and contains
      "not in live_symbols". THEN assert it contains no `pnl=0.0` and no `exit_price=trade.get("entry_price"`.
      RED today because both literals are present at :175-176.
    - case 2 test_unresolvable_writes_null_never_zero: filled entry, symbol absent from live_symbols,
      get_closed_orders -> []. Assert kwargs == {"status":"closed","exit_price":None,"pnl":None,"fees":None}
      and assert `kwargs["pnl"] is None` EXPLICITLY (never `== 0`, never falsy).
    - case 3 test_resolvable_recovers_the_real_fee_net_pnl: entry buy filled_avg_price=100.0 filled_qty=2;
      one opposite-side filled close at 110.0 qty 2, filled_at after entry. Assert status=="closed",
      exit_price==110.0, pnl == pytest.approx(realized_pnl("buy",100.0,110.0,2,TAKER_FEE)), and assert
      pnl != 0.0 AND pnl != 20.0 (proves fees are actually subtracted on both legs).
    - case 4 test_held_position_is_left_alone_slash_trap: row["symbol"]="BTC/USD",
      live_symbols={"BTCUSD"} (slash-STRIPPED, as :159 builds it). Assert result == {} — NO WRITE.
      This is the mass-close regression; it must fail loudly if normalization is wrong.
    - case 5 test_transient_alpaca_error_writes_nothing: get_closed_orders raises RuntimeError.
      Assert result == {} and assert it is NOT the NULL-terminal dict.
    - case 6 test_terminal_zero_fill_keeps_the_honest_zero: entry status="canceled", filled_qty=0.
      Assert status=="canceled", exit_price is None, pnl == 0 (this zero is TRUE — no position existed).
    - case 7 test_partial_close_is_unresolvable: close order filled_qty == half the entry's.
      Assert pnl is None (_QTY_TOLERANCE rejects it).
    - case 8 test_missing_order_id_resolves_to_null: row["order_id"] is None, symbol gone.
      Assert status=="closed" and pnl is None.
  </behavior>
  <action>Create tests/test_external_exit_resolution.py. Import the unit as `from src.alpaca_orchestrator import _resolve_external_exit` — it does not exist yet, so the module-level import fails and all eight cases are RED. That is correct and intended; do NOT stub it, do NOT guard the import with pytest.importorskip (a skip is not a RED). Build rows as plain dicts matching src/db.py:125-130's `SELECT *` shape (id, bot_id, symbol, side, status, entry_price, order_id, filled_qty, filled_avg_price, fees). Build orders as the dicts src/alpaca_client.py:_parse_order returns (id, symbol, side, status, filled_qty, filled_avg_price, filled_at). Reuse FakeAlpaca from tests/test_backfill.py (import it; if it is not importable, replicate its exact three-method surface). No network, no DB, no env reads.</action>
  <verify>
    <automated>python -m pytest tests/test_external_exit_resolution.py -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_external_exit_resolution.py -q` — 8 collected, 8 FAIL/ERROR (RED). Zero pass, zero skip.
    - The failure reason is the missing `_resolve_external_exit` import, NOT a typo in the test.
    - `grep -c "is None" tests/test_external_exit_resolution.py` >= 4 (identity assertions, not truthiness).
    - `grep -nE "requests|httpx|DATABASE_URL|psycopg" tests/test_external_exit_resolution.py` returns NOTHING.
    - `grep -n "BTCUSD" tests/test_external_exit_resolution.py` returns a hit (the slash trap is actually exercised).
  </acceptance_criteria>
  <done>The eight sentinel cases exist and fail for the right reason. `pnl is None` is asserted by identity; the slash trap and the transient-error trap each have a dedicated failing test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: win-rate + read-only + dashboard RED — cases 9-13, 23-25</name>
  <files>tests/test_db.py, tests/test_db_readonly.py, dashboard/api/tests/test_portfolio_win_rate.py</files>
  <read_first>18-VALIDATION.md cases 9-13 and 23-25; src/db.py L18-60 (_pool global, get_pool, _bootstrap_schema, connection) and L211-244 (get_alpaca_accuracy — the (pnl or 0) > 0 / losses = resolved - wins shape); tests/test_db.py L1-60 (existing accuracy assertions — READ BEFORE CHANGING, case 12); dashboard/api/routes/portfolio.py L60-80; dashboard/api/routes/settings.py L30-70 and L130-140 (win_rate vs win_rate_target=40.0 — the paper-gate readout); dashboard/api/tests/ (existing conftest + TEST_DATABASE_URL skip pattern)</read_first>
  <behavior>
    - case 9 test_null_pnl_excluded_from_denominator: fixture = 2 wins, 3 real losses, 1 pnl=0.0,
      4 pnl=NULL. Assert resolved == 6 (NOT 10), wins == 2, losses == 4, win_rate == approx(2/6).
      pnl=0.0 is STILL COUNTED as a loss here — Decision 2 licenses excluding NULL only, not zero.
    - case 10 test_old_arithmetic_gives_a_different_answer: compute the pre-fix expression inline on the
      SAME fixture; assert win_rate_new != win_rate_old AND losses_new < losses_old (non-vacuous).
    - case 11 test_sums_unaffected: total_pnl == approx(sum of the 6 real pnls); adding more NULL rows
      does not move total_pnl (the bug was ALWAYS in the denominator, never the numerator).
    - case 12 test_accuracy_dict_shape_unchanged: every key still present (total_trades, resolved, wins,
      losses, win_rate, total_pnl, avg_pnl, crypto_pnl, stock_pnl) — src/alpaca_orchestrator.py:577,:1254
      read this dict by key; a dropped key crashes the live bot.
    - case 13 (dashboard) test_portfolio_and_settings_exclude_null_pnl: STATIC — read
      dashboard/api/routes/portfolio.py and settings.py; POSITIVE CONTROL FIRST (each slice contains
      "status IN ('closed', 'stopped', 'target_hit')"), then assert each contains "pnl IS NOT NULL".
      BEHAVIORAL (TEST_DATABASE_URL-gated, skip visibly if unset): seed the case-9 fixture, GET
      /api/portfolio, assert win_rate == approx(33.3, abs=0.1) — NOT 20.0.
    - case 23 test_readonly_skips_bootstrap: monkeypatch src.db._bootstrap_schema with a spy. Reset
      src.db._pool = None. With AIPW_DB_READONLY=1, get_pool() -> spy NEVER called. Reset _pool = None,
      unset the flag, get_pool() -> spy WAS called. BOTH directions (asserting only the skip would pass
      if bootstrap were deleted). Reset _pool = None in teardown.
    - case 24 test_readonly_is_enforced_server_side (TEST_DATABASE_URL-gated): assert the connection's
      dbname/host match the parsed TEST_DATABASE_URL **BEFORE** the probe (the prod guard), then
      CREATE TABLE _aipw_probe (x int) raises psycopg.errors.ReadOnlySqlTransaction (SQLSTATE 25006),
      and SELECT 1 still succeeds.
    - case 25 test_default_unset_is_byte_identical: with AIPW_DB_READONLY unset, default_transaction_read_only
      is off and _bootstrap_schema ran.
  </behavior>
  <action>Create tests/test_db_readonly.py (cases 23-25) and dashboard/api/tests/test_portfolio_win_rate.py (case 13); EXTEND tests/test_db.py with cases 9-12 — read its existing accuracy assertions first and update them in place ONLY if their fixture contains NULL/0.0 rows (case 12 requires the returned dict shape to stay identical). Cases 9-12 must reach get_alpaca_accuracy without a live DB: build the fixture via the existing tests/test_db.py pattern (TEST_DATABASE_URL-gated seed, or the existing fake-cursor pattern if that is what the file already uses — follow the file, do not introduce a third style). For 23/25, use monkeypatch.setenv/delenv and set `src.db._pool = None` in BOTH setup and teardown (a leaked pool makes the assertion pass for the wrong reason — RESEARCH P8). Every DB-gated test uses TEST_DATABASE_URL ONLY and `pytest.mark.skipif(not os.environ.get("TEST_DATABASE_URL"))` — NEVER DATABASE_URL, and the skip must be visible in `-q` output, never silently swallowed.</action>
  <verify>
    <automated>python -m pytest tests/test_db.py tests/test_db_readonly.py dashboard/api/tests/test_portfolio_win_rate.py -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_db.py tests/test_db_readonly.py dashboard/api/tests/test_portfolio_win_rate.py -q` — the NEW cases (9, 10, 11, 13-static, 23) FAIL; previously-passing tests in tests/test_db.py still pass; 24/25/13-behavioral SKIP visibly without TEST_DATABASE_URL.
    - Case 23 fails because `AIPW_DB_READONLY` is not yet read by get_pool — not because of a fixture error.
    - `grep -nE "\bDATABASE_URL\b" tests/test_db_readonly.py dashboard/api/tests/test_portfolio_win_rate.py | grep -v TEST_DATABASE_URL` returns NOTHING (prod fence).
    - `grep -c "_pool = None" tests/test_db_readonly.py` >= 2 (setup AND teardown).
  </acceptance_criteria>
  <done>The three win-rate denominators and the read-only flag each have a failing test. The dashboard's two duplicated denominators are pinned STATICALLY (so the fix cannot be declared done by fixing src/db.py alone) and BEHAVIORALLY (33.3, not 20.0).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: backtester + rollout RED — cases 14-22, 26-29</name>
  <files>tests/backtester/test_cli_overrides.py, tests/backtester/test_engine.py, tests/test_rollout_config.py</files>
  <read_first>18-VALIDATION.md cases 14-22 and 26-29; 18-RESEARCH.md §2.2 (the exact new PhaseConfig fields + CLI args) and §2.3 (the materialized sizing grid: conf=3 -> 1.50/2.00/2.50%, conf=4 -> 3.00/4.00/5.00% of equity); src/backtester/config.py L28-40 (PhaseConfig, frozen=True); src/backtester/engine.py L20-140 (SIGNAL_WINDOW=50, SCAN_INTERVAL_BARS=30, _KELLY_PROBS, _position_dollar_amount, the :124 entry gate); src/backtester/cli.py L20-80 (SYMBOLS, the --disable dataclasses.replace idiom, the bar-load loop); tests/backtester/test_engine.py (existing conventions + how it loads the BTC fixture); src/technical_signals.py L361-406 (the two scoring branches — FOUR `score += 1` each; volume spike excluded at :405-406); src/universe.py L26-51 (entry_allowed; empty allowlist == no restriction); src/bot_config.py L18-19, L34, L49-50, L68, L78-80 (from_row + the `quarantined` property); dashboard/api/models.py L254-276 (BotUpdate); dashboard/api/routes/bots.py L184-212 (the dynamic SET + mgr.update hot-swap)</read_first>
  <behavior>
    - case 14 test_min_confluence_is_load_bearing: same bars, mc=3 vs mc=4. Assert
      len(trades_at_3) > len(trades_at_4) STRICTLY, and BOTH > 0. ("It runs" would pass on a knob wired to nothing.)
    - case 15 test_kelly_fraction_changes_notional: same bars, k=0.15 vs k=0.25. First trade's notional at
      0.25 strictly greater, and approx-equal to the §2.3 grid (conf=4 -> 5.00% vs 3.00% of equity).
    - case 16 test_kelly_ceiling_is_enforced_at_the_cli: main(["--phase","0","--train","--kelly-fraction","0.50"])
      raises SystemExit with code 2 (parser.error). Assert 0.25 is ACCEPTED. HARD RULE: Kelly may only go DOWN.
    - case 17 test_five_percent_cap_never_breached: across conf in {3,4} x k in {0.15,0.20,0.25},
      _position_dollar_amount(...)/equity <= 0.05 + 1e-9.
    - case 18 test_min_confluence_5_is_structurally_empty: (a) STATIC — each of the two scoring branches in
      src/technical_signals.py contains exactly FOUR `score += 1` (positive control: the slice is non-empty).
      (b) BEHAVIORAL — engine at min_confluence=5 over the fixture bars -> len(trades) == 0.
      (c) max(confluence_score over all signals) <= 4. Do NOT "fix" the score to reach 5.
    - case 19 test_quarantine_uses_the_live_gate: engine with quarantined=("BTC/USD",) over BTC+ETH bars ->
      ZERO BTC trades, NON-ZERO ETH trades. STATIC: src/backtester/engine.py imports entry_allowed from
      src.universe, and contains NO `in exclude` / `.replace("/","")` ad-hoc gate.
    - case 20 test_rsi_ceiling_is_enforced_and_non_vacuous: engine skips a signal with
      rsi_value >= config.rsi_ceiling; run(rsi_ceiling=65.0) yields STRICTLY FEWER trades than
      run(rsi_ceiling=inf) on the same bars.
    - case 21 test_no_incidental_behavior_change: with symbols=(), quarantined=(), rsi_ceiling=float("inf"),
      the engine's trade_history over the fixture is byte-identical to a GOLDEN list committed in the test.
      GENERATE THE GOLDEN FROM THE CURRENT (pre-Phase-18) ENGINE IN THIS TASK and commit it — after 18-05
      lands it is a pin, not a fresh capture.
    - case 22 test_sweep_is_reproducible: same cached bars + same config run twice -> identical trade_history
      and identical compute_summary. STATIC: no `random` / `np.random` import in src/backtester/.
    - case 26 test_quarantine_is_config_only: BotConfig.from_row({..., "quarantined_symbols":
      "BTC/USD,ETH/USD,TRUMP/USD,FIL/USD,ARB/USD"}) -> cfg.quarantined == that list; select_long_candidates
      DROPS a confluence=4 BTC signal and KEEPS an identical SOL one. Zero src/ diff required.
    - case 27 test_put_bots_accepts_all_three_knobs (TEST_DATABASE_URL-gated): PUT {"min_confluence":4,
      "kelly_fraction":0.20, "quarantined_symbols":"BTC/USD,ETH/USD"} -> 200, bots row updated,
      BotManager.update called with those keys.
    - case 28 test_kelly_above_ceiling_cannot_be_rolled_out: PUT {"kelly_fraction": 0.50} is REJECTED
      (the API-side mirror of case 16 — the CLI guard is worthless if the rollout path can still raise Kelly).
    - case 29 test_no_prod_write_anywhere: static fence over tests/ and scripts/sweep*.py — no bare
      DATABASE_URL reference (TEST_DATABASE_URL only), no --apply/--write/--fix argparse surface on the sweep
      driver. SELF-TEST: the same detector applied to src/db.py:101-122 (update_alpaca_trade) MUST MATCH —
      proving the detector actually fires (without it an empty slice passes vacuously).
  </behavior>
  <action>Create tests/backtester/test_cli_overrides.py (14-17, 22) and tests/test_rollout_config.py (26-29); EXTEND tests/backtester/test_engine.py (18-21). Construct PhaseConfig variants with dataclasses.replace — it is frozen. Cases referencing `symbols`/`quarantined`/`rsi_ceiling` and the four CLI args are RED because Plan 18-05 has not added them yet (expect TypeError/AttributeError/SystemExit-2-not-raised); do NOT stub the fields to make them green. Case 21's golden list IS generated in this task from the CURRENT engine (run it, print trade_history, paste the literal into the test) — that is what makes it a pin rather than a tautology after 18-05. Case 28 may be RED if the API does not yet reject kelly_fraction > 0.25; if so, Plan 18-07 closes it — record which. Cases 19/22 may use the existing 60-bar BTC fixture (they are engine-semantics tests, not sweep tests); case 19 additionally needs an ETH fixture — if none exists, synthesize a small deterministic ETH bar list in the test file itself (this is a unit test, NOT the sweep — the sweep's real bars are Plan 18-02). Rollout tests are TEST_DATABASE_URL-gated and skip visibly.</action>
  <verify>
    <automated>python -m pytest tests/backtester/ tests/test_rollout_config.py -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/backtester/ tests/test_rollout_config.py -q` — cases 14, 15, 16, 19, 20, 21 FAIL (the new PhaseConfig fields / CLI args do not exist); cases 17, 18, 22, 26 may already PASS (they assert existing truths — that is fine and is the point of case 18); 27/28 SKIP without TEST_DATABASE_URL. Previously-passing tests/backtester/ tests still pass.
    - Case 18's static assertion passes TODAY (the ceiling is already 4) and its behavioral assertion (mc=5 -> 0 trades) passes TODAY. If either fails, STOP — the C1 correction is wrong and the phase must be re-researched.
    - Case 29's SELF-TEST passes: the fence detector fires on src/db.py:101-122.
    - `grep -n "float(\"inf\")\|float('inf')" tests/backtester/test_engine.py` returns a hit (case 21's pin disables the RSI guard, per RESEARCH §2.2's tension resolution).
    - `python -m pytest tests/ -q | tail -1` — no PREVIOUSLY-PASSING test has broken (new failures are only the intended RED).
  </acceptance_criteria>
  <done>The knobs, the ceiling, the quarantine gate, the RSI fidelity fix, the bit-identical pin, and the config-only rollout each have a test. The Kelly ceiling is asserted at BOTH the CLI and the API.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test suite → prod DB | a test that reads DATABASE_URL instead of TEST_DATABASE_URL writes to prod |
| test suite → live Alpaca | a test that instantiates a real AlpacaClient hits the live account |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | a DB-gated test pointed at prod | mitigate | TEST_DATABASE_URL only + case 24's dbname/host pre-check + case 29's static fence WITH a self-test |
| T-18-02 | Tampering | a test calling the live Alpaca API | mitigate | FakeAlpaca (tests/test_backfill.py:96) everywhere; grep gate for requests/httpx |
| T-18-03 | Repudiation | a static/grep test that passes on an EMPTY slice (vacuous green) | mitigate | every static case runs a POSITIVE CONTROL first (cases 1, 13, 18, 19, 29) |
| T-18-04 | Repudiation | a "RED" case that is actually a skip | mitigate | no pytest.importorskip on the missing unit; DB skips must be VISIBLE in -q |
| T-18-SC | Tampering | package installs | accept | none — no new packages |
</threat_model>

<verification>
- `python -m pytest tests/ dashboard/api/tests/ -q` — the ONLY new failures are the intended RED cases; the 395-passing baseline is otherwise intact.
- `grep -rnE "\bDATABASE_URL\b" tests/ dashboard/api/tests/ | grep -v TEST_DATABASE_URL` — NOTHING.
- Case 18 is GREEN on day one (the confluence ceiling really is 4).
</verification>

<success_criteria>
All 29 VALIDATION cases have a named test. Each downstream fix in Phase 18 now has a test that is
failing for the right reason before it lands — including the two that would destroy money silently:
the slash trap (case 4) and the transient-error-becomes-a-NULL-write trap (case 5).
</success_criteria>

<output>
Create `.planning/phases/18-profitable-retune/18-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
