---
phase: 18-profitable-retune
plan: 06
type: execute
wave: 3
depends_on: [18-02, 18-04, 18-05]
files_modified:
  - scripts/sweep_backtest.py
  - .planning/phases/18-profitable-retune/18-BACKTEST.md
autonomous: true
requirements: [TUNE-01, TUNE-03]

must_haves:
  truths:
    - "THE LIVE GRID IS 12 CELLS, NOT 18 — say so in 18-BACKTEST.md's headline. 18 TRAIN cells are RUN: min_confluence in {3,4,5} x kelly_fraction in {0.15,0.20,0.25} x {quarantine ON, OFF}. The SIX mc=5 cells are PROVABLY EMPTY (the confluence score ceiling is 4 — C1). RUN THEM ANYWAY and record them in 18-BACKTEST.md as `trades=0 -> auto-fail on criterion 3 (>= 30 trades)`. That is honest, costs one loop iteration, and documents WHY the grid point is dead instead of silently dropping it. Do NOT 'fix' the score to reach 5 — that would be a NEW STRATEGY, which the phase fences forbid."
    - "The TIEBREAK RULE IS WRITTEN INTO 18-BACKTEST.md BEFORE THE GRID IS LOOKED AT: prefer the LOWER kelly_fraction, then the HIGHER min_confluence — break ties toward LESS RISK and FEWER TRADES."
    - "ONE candidate, picked on TRAIN. ONE holdout run, on that candidate, plus one on the baseline. PICKING ON THE HOLDOUT IS FORBIDDEN. If the candidate fails the holdout, that is the RESULT — do not go back and pick another."
    - "The BASELINE is run on the FINAL engine (rsi_ceiling=65.0) against the SAME cached bars as the candidate. NEVER compare a pre-fix baseline against a post-fix candidate — that delta is the ENGINE, not the retune (P3)."
    - "ACCEPTANCE BAR (all on HOLDOUT, a CONJUNCTION): win_rate >= 0.40 AND max_drawdown improved vs baseline AND max_drawdown < 0.20 AND return/expectancy >= baseline AND trade_count >= 30. The >= 30 floor exists precisely so a config cannot 'win' by not trading. ENFORCE IT IN THE DRIVER, NOT BY EYE."
    - "IF NOTHING CLEARS THE BAR: ship the quarantine ALONE — it is independently justified by 17-04-EVIDENCE.md's mechanical rule and does not need the sweep's permission. Leave min_confluence and kelly_fraction at their current values, and RECORD THE NEGATIVE RESULT HONESTLY AND IN FULL. A failed sweep is a FINDING, not a problem to be forced through."
    - "18-BACKTEST.md carries an explicit FIDELITY GAP section: the loss cooldown, the 4H trend filter (the engine passes bars_4h=None so trend_4h is ALWAYS 'unknown'), the SHORT side, and the soft/trailing/advisor exit stack are NOT modelled. The holdout number is 'the LONG-ONLY, HARD-EXIT LOWER BOUND' — NOT a promise about live P&L. DO NOT OVERSELL IT."
    - "The sweep NEVER writes to the DB and never trades. The baseline knob read uses AIPW_DB_READONLY=1. The driver has no --apply/--write/--fix surface."
    - "CELLS RUN VIA `BAR_CACHE_DIR=data/backtest_bars` AND **NEVER** `--fixture-dir` — the cache namespace (`BTC_USD_1Hour.json`, data_loader.py:82) is NOT the fixture namespace (`BTC_USD.json`, :43). Passing --fixture-dir kills every cell with `sys.exit(1)` 'No bar data available'."
    - "W2 — THE HOLDOUT IS RUN ONCE, ENFORCED IN CODE: the driver writes `.planning/phases/18-profitable-retune/18-HOLDOUT.lock` on its first holdout run and `parser.error`s if the lock already exists. Without it, `--holdout --candidate A` then `--candidate B` is just peeking twice, and the one-shot rule is honor-system only."
  artifacts:
    - path: "scripts/sweep_backtest.py"
      provides: "the reproducible 18-cell TRAIN driver + the acceptance-bar evaluator (a function, not an eyeball)"
      min_lines: 90
    - path: ".planning/phases/18-profitable-retune/18-BACKTEST.md"
      provides: "the full 18-cell table, the tiebreak rule, the baseline knob values (the revert target), the declared fidelity gaps, the single holdout result, and the verdict"
      min_lines: 120
  key_links:
    - from: "scripts/sweep_backtest.py"
      to: "src.backtester.cli"
      via: "each cell is one invocation against the SAME cached bars"
      pattern: "min_confluence|kelly_fraction|exclude_symbols"
    - from: ".planning/phases/18-profitable-retune/18-BACKTEST.md"
      to: "the revert"
      via: "the previous bots-row values, recorded verbatim so the revert is copy-paste"
      pattern: "Baseline"
---

<objective>
Run the grid on TRAIN. Pick ONE candidate. Run HOLDOUT ONCE. Write `18-BACKTEST.md`.

Purpose: this is TUNE-01. Everything before it existed to make this number mean something — real bars
(18-02) so the quarantine arm is not vacuous, an honest win rate (18-04) so the acceptance bar is
readable, and an engine that gates entries like the live bot (18-05) so the number describes the
strategy we actually run.

**The most likely failure mode of this plan is a number that looks good and isn't.** Three guards:
the >= 30-trade floor (a config cannot win by refusing to trade), the one-shot holdout (the number is
not selected-on), and the declared fidelity gaps (the number is not oversold).

Output: `scripts/sweep_backtest.py` + `18-BACKTEST.md`. **If nothing clears the bar, that IS the output.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-profitable-retune/18-RESEARCH.md
@.planning/phases/18-profitable-retune/18-CONTEXT.md
@.planning/phases/17-per-symbol-performance/17-04-EVIDENCE.md
@CLAUDE.md

<interfaces>
THE GRID (CONTEXT Decision 6):
  min_confluence in {3, 4, 5}          # the mc=5 row is PROVABLY EMPTY (ceiling is 4 — C1). Run it. Record trades=0.
  kelly_fraction in {0.15, 0.20, 0.25} # 0.25 is a CEILING. Kelly may only go DOWN. > 0.25 is a parser.error.
  quarantine     in {ON, OFF}
  => 18 TRAIN cells (12 live + 6 provably empty).

QUARANTINE (from 17-04-EVIDENCE.md; CONTEXT Decision 5's mechanical rule: a real-P&L cell with
trades >= 5 AND win_rate < 25% AND expectancy < 0):
  --exclude-symbols "BTC/USD,ETH/USD,TRUMP/USD,FIL/USD,ARB/USD"
KEEP: SOL, AVAX, ADA (their losses are EXIT-side, not entry-side), XRP, UNI, CRV, DOT, LINK.
Slash form, exact — the documented convention (src/bot_config.py:36-37).

WINDOWS (src/backtester/cli.py:22-25 — already exactly the CONTEXT windows):
  TRAIN   2025-10-01 -> 2026-01-31   (--train)
  HOLDOUT 2026-02-01 -> 2026-04-30   (--holdout)

ONE CELL — note the CACHE env var and the ABSENCE of --fixture-dir:
  BAR_CACHE_DIR=data/backtest_bars python -m src.backtester --phase 0 --train \
    --min-confluence 4 --kelly-fraction 0.20 \
    --symbols "BTC/USD,ETH/USD,SOL/USD,XRP/USD,ADA/USD,AVAX/USD,DOT/USD,LINK/USD" \
    --exclude-symbols "BTC/USD,ETH/USD,TRUMP/USD,FIL/USD,ARB/USD"

**NEVER PASS `--fixture-dir data/backtest_bars`.** It routes to load_bars_fixture, which reads
`BTC_USD.json` (data_loader.py:43), while 18-02's cache holds `BTC_USD_1Hour.json` (save_bars_cache,
:82). Every symbol FileNotFoundErrors, cli.py logs "Skipping %s" for each, and then `sys.exit(1)`s
with "No bar data available" — **all 18 cells + the baseline + the holdout would die.** The cache path
(`BAR_CACHE_DIR`, data_loader.py:20) date-filters on read (:68-69) and engine.run re-slices
(engine.py:56), so ONE union cache serves TRAIN and HOLDOUT with the windows intact.

THE ACCEPTANCE BAR — a CONJUNCTION, all on HOLDOUT (CONTEXT Decision 6):
  1. win_rate >= 0.40
  2. max_drawdown IMPROVED vs baseline AND max_drawdown < 0.20
  3. return / expectancy >= baseline
  4. trade_count >= 30      <- the tripwire against "100% win rate on 1 trade"

THE TIEBREAK (RESEARCH Open Question 1) — WRITE IT INTO 18-BACKTEST.md BEFORE LOOKING AT THE GRID:
  prefer the LOWER kelly_fraction; then the HIGHER min_confluence. (Toward less risk, fewer trades.)

THE BASELINE — read it, do not guess it:
  AIPW_DB_READONLY=1 python -c "
  from src.db import connection
  with connection() as c:
      for r in c.execute('SELECT bot_id, strategy, min_confluence, kelly_fraction, quarantined_symbols FROM bots ORDER BY bot_id'):
          print(r)"
  (or GET /api/bots). Record VERBATIM in 18-BACKTEST.md — these values ARE the revert (Decision 7).
  Assumption A1: Bot B may still be at kelly_fraction=0.50. READ it; do not assume. If it is, it comes
  to <= 0.25 REGARDLESS of what the sweep says — the quarter-Kelly ceiling is a hardcoded risk rule
  and does not need the backtest's permission.

THE FOUR DECLARED FIDELITY GAPS (C2 — the engine does NOT model these; the doc MUST say so):
  1. the loss cooldown (db.get_recent_loss_symbols) — bot_thread.py:143
  2. the 4H trend filter — bot_thread.py:148; the engine passes bars_4h=None, so trend_4h is ALWAYS "unknown"
  3. the SHORT side — select_short_candidates (bot_thread.py:152-167); every live short is invisible to the backtest
  4. the soft / trailing / MiroFish-advisor exit stack — the engine has HARD thresholds ONLY
  => the holdout number is "the LONG-ONLY, HARD-EXIT LOWER BOUND", not a promise about live P&L.
  Phase-17 found the SOL/AVAX/ADA losses are EXIT-side (avg_loss > avg_win) — PRECISELY the dimension
  the engine models LEAST faithfully. Say that out loud in the doc.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/sweep_backtest.py — the 18-cell TRAIN driver + the acceptance-bar evaluator</name>
  <files>scripts/sweep_backtest.py</files>
  <read_first>src/backtester/cli.py after 18-05 (the four new args, the date constants, --fixture-dir); src/backtester/metrics.py L1-60 (sharpe_ratio:6, max_drawdown:19, win_rate:34, monitor_pnl:40, compute_summary:44 — the EXACT field names the driver reads; do NOT compute new metrics, CONTEXT forbids touching them); src/backtester/engine.py (run()'s return shape: trade_history + summary); scripts/symbol_report.py L1-40 (the Phase-17 read-only script conventions: argparse shape, AIPW_DB_READONLY, no write surface); scripts/fetch_backtest_bars.py (the cache dir produced by 18-02)</read_first>
  <action>Create scripts/sweep_backtest.py. It runs the 18 TRAIN cells (mc in {3,4,5} x k in {0.15,0.20,0.25} x quarantine {on,off}) against the SAME cached bars in data/backtest_bars, collecting compute_summary per cell. It NEVER re-fetches bars mid-sweep — the cache is the ONLY reproducibility control (the engine has no RNG, so bar provenance is the sole risk). It prints a markdown table: mc | kelly | quarantine | trades | win_rate | max_drawdown | expectancy | total_return | PASS/FAIL-reason. The evaluator is a FUNCTION, not an eyeball: `passes_bar(summary, baseline) -> (bool, reason)` implementing the four-way conjunction, with `trades >= 30` checked FIRST so a zero-trade cell fails with reason "trade count 0 < 30" rather than "win_rate 0.0 < 0.40" — the mc=5 cells must be legible as STRUCTURALLY EMPTY, not as merely bad configs. Add a `--holdout` mode that runs EXACTLY TWO cells: the named candidate and the baseline. It MUST REFUSE to run the full grid on holdout: `--holdout` without an explicit `--candidate mc,k,quarantine` is a parser.error ("picking on the holdout is forbidden — CONTEXT Decision 6"). **W2 — ENFORCE ONE-SHOT IN CODE, not on the honor system: on a holdout run, if `.planning/phases/18-profitable-retune/18-HOLDOUT.lock` EXISTS, `parser.error("the holdout has already been run (see 18-HOLDOUT.lock) — running it again with a different --candidate IS picking on the holdout, and CONTEXT Decision 6 forbids it")`; otherwise write the lock (candidate, timestamp, git rev) BEFORE running.** **Each cell MUST run as a FRESH SUBPROCESS** — `subprocess.run([sys.executable, "-m", "src.backtester", ...], env={**os.environ, "BAR_CACHE_DIR": "data/backtest_bars"}, ...)` — and **NO --fixture-dir flag** (see the <interfaces> note; --fixture-dir kills every cell). The subprocess is NOT a style choice: `data_loader.py:20` reads `BAR_CACHE_DIR` **at import time** and bakes it into default args (`:59`, `:144`), and `cli.py:76` never passes `cache_dir`. So an in-process driver (`from src.backtester.engine import run` in a loop, mutating `os.environ` per cell) silently reads the stale import-time value — `data/bar_cache`, which exists and is EMPTY — and every cell falls through to `load_bars_from_alpaca` (`:152-153`), re-fetching live bars mid-sweep. That destroys the reproducibility invariant this driver exists to hold, and the result would feed a prod bots-row change. The driver has NO --apply / --write / --fix flag, never imports src.db for a write, never calls submit_order, and sets AIPW_DB_READONLY=1 for the baseline knob read. Deterministic: the same bars + the same config run twice produce identical output (VALIDATION case 22).</action>
  <verify>
    <automated>python -c "import ast,pathlib; s=pathlib.Path('scripts/sweep_backtest.py').read_text(); ast.parse(s); assert not any(f in s for f in ['--apply','--write','--fix']), 'write surface present'; assert 'passes_bar' in s and '30' in s; print('DRIVER FENCE OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The fence check prints `DRIVER FENCE OK`.
    - `python scripts/sweep_backtest.py --help` — exit 0; shows a `--holdout` mode and a `--candidate` arg.
    - `python scripts/sweep_backtest.py --holdout` WITHOUT `--candidate` — exits 2 with an error mentioning that picking on the holdout is forbidden. **A non-2 exit is a HARD FAIL of this task.**
    - **W2 — a SECOND `--holdout --candidate ...` invocation exits 2** because `18-HOLDOUT.lock` exists. `grep -n "18-HOLDOUT.lock" scripts/sweep_backtest.py` returns a hit.
    - **`grep -c "fixture-dir\|fixture_dir" scripts/sweep_backtest.py` returns 0**, and `grep -n "BAR_CACHE_DIR" scripts/sweep_backtest.py` returns a hit — cells load bars from the CACHE, not the fixture namespace.
    - **`grep -n "subprocess" scripts/sweep_backtest.py` returns a hit** — each cell is a fresh process, so `BAR_CACHE_DIR` is read at that process's import. An in-process loop CANNOT pick up the env var (data_loader.py:20 reads it at import and bakes it into defaults at :59/:144; cli.py:76 never passes cache_dir), and would silently re-fetch from Alpaca mid-sweep.
    - Bars are never re-fetched mid-sweep: with the network unavailable (or `ALPACA_API_KEY` unset), a full TRAIN sweep still completes from cache alone.
    - `grep -nE "\brandom\b|np\.random" scripts/sweep_backtest.py` returns NOTHING (determinism).
    - `grep -n "AIPW_DB_READONLY" scripts/sweep_backtest.py` returns a hit (the baseline read is fenced).
    - `grep -rnE "INSERT|UPDATE |DELETE |submit_order|place_order" scripts/sweep_backtest.py` returns NOTHING.
    - `python -m pytest tests/ -q | tail -1` — >= 395 passed (VALIDATION case 29's fence scans this script and must still pass).
  </acceptance_criteria>
  <done>A reproducible driver that cannot write to prod, cannot trade, cannot run the grid on the holdout, and evaluates the acceptance bar in code rather than by eye.</done>
</task>

<task type="auto">
  <name>Task 2: Run TRAIN (18 cells) -> pick ONE candidate -> run HOLDOUT ONCE -> write 18-BACKTEST.md</name>
  <files>.planning/phases/18-profitable-retune/18-BACKTEST.md</files>
  <read_first>.planning/phases/17-per-symbol-performance/17-04-EVIDENCE.md (the per-symbol evidence that INDEPENDENTLY justifies the quarantine — it is what lets the quarantine ship even if the sweep fails); 18-RESEARCH.md §3 (sweep discipline) and §2.3 (the materialized sizing grid — sanity-check the cell notionals against it: conf=4 x k=0.25 must hit the 5.00% cap exactly)</read_first>
  <action>ORDER MATTERS — do these in sequence.
(1) READ THE BASELINE with `AIPW_DB_READONLY=1`: the current min_confluence / kelly_fraction / quarantined_symbols / strategy for bots A, B, C, D. Record VERBATIM in 18-BACKTEST.md — they are the revert target. Note Bot B's kelly_fraction: if it is still 0.50, it comes to <= 0.25 regardless of the sweep (the ceiling is a hardcoded risk rule, not a tuning result).
(2) WRITE THE TIEBREAK RULE INTO 18-BACKTEST.md **BEFORE** RUNNING THE GRID: lower kelly_fraction first, then higher min_confluence. State it, commit it, THEN look at the numbers.
(3) Run all 18 TRAIN cells. Record EVERY cell — including the six mc=5 zeros, each annotated: `trades=0 — the confluence score CEILING IS 4 (src/technical_signals.py:371-406; the volume spike is deliberately excluded at :405-406 because VolSpike=True trades went 0-for-17). min_confluence=5 is STRUCTURALLY UNREACHABLE. Auto-fail on criterion 3 (>= 30 trades). NOT "fixed" — fixing the score would be a new strategy.`
(4) Run the BASELINE config on TRAIN and on HOLDOUT, on the FINAL engine (rsi_ceiling=65.0), against the SAME cached bars (P3 — never compare a pre-fix baseline against a post-fix candidate).
(5) Pick ONE candidate on TRAIN using the four-way bar + the pre-stated tiebreak.
(6) Run HOLDOUT ONCE on that candidate. **If it fails the holdout bar, DO NOT go back and pick a different one.** That is picking on the holdout and it is forbidden. Record the failure.
(7) Write 18-BACKTEST.md with: a **headline stating the grid is 12 LIVE CELLS + 6 STRUCTURALLY EMPTY (W3)** — not "18 cells", which overstates the search; a reproducibility header (cache path, the per-symbol bar counts from 18-02, the exact CLI invocation per cell **including `BAR_CACHE_DIR=data/backtest_bars` and NO --fixture-dir**, `git rev-parse HEAD`); the tiebreak rule; the baseline knob values; the full 18-cell TRAIN table; the candidate + why; the single HOLDOUT row beside the baseline HOLDOUT row; the four-criterion PASS/FAIL verdict; and a **FIDELITY GAP** section listing the four unmodelled things verbatim, plus the sentence "the holdout number is the LONG-ONLY, HARD-EXIT LOWER BOUND — it is not a promise about live P&L", and the note that Phase-17 found the SOL/AVAX/ADA losses to be EXIT-side, precisely the dimension the engine models least faithfully.
**IF NO GRID POINT CLEARS THE BAR: the verdict is "SHIP THE QUARANTINE ONLY."** It is independently justified by 17-04-EVIDENCE.md's mechanical rule and does not need the sweep's permission. Leave min_confluence and kelly_fraction at their current values (except Bot B's 0.50 -> 0.25, which is the risk ceiling, not a retune). Record the negative result honestly and in full.
Do NOT force a number through. Do NOT widen the grid. Do NOT relax the bar. Do NOT re-run the holdout. Do NOT write to the DB in this task — the rollout is Plan 18-07.</action>
  <verify>
    <automated>python -c "import pathlib; d=pathlib.Path('.planning/phases/18-profitable-retune/18-BACKTEST.md').read_text().lower(); missing=[k for k in ['fidelity','baseline','tiebreak','holdout','lower bound','min_confluence=5'] if k not in d]; assert not missing, f'MISSING: {missing}'; print('BACKTEST DOC OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `BACKTEST DOC OK` (all six required sections present).
    - 18-BACKTEST.md contains **all 18 TRAIN rows**, including the six `mc=5, trades=0` rows with the structural-emptiness annotation. `grep -c "^| 5 " 18-BACKTEST.md` >= 6.
    - **W3 — the headline says the LIVE grid is 12 cells (+6 structurally empty), not 18.**
    - **W2 — `18-HOLDOUT.lock` exists after the holdout run, and a second holdout invocation exits 2.**
    - The **tiebreak rule appears ABOVE the grid table** in the document (it was stated before the grid was read).
    - The **baseline knob values for bots A/B/C/D** are recorded verbatim — a copy-pasteable revert.
    - Exactly **ONE** candidate row and exactly **TWO** holdout rows (candidate + baseline). `grep -ci "holdout" 18-BACKTEST.md` >= 2 and there is no second candidate.
    - The **FIDELITY GAP** section names all four unmodelled things (loss cooldown, 4H trend filter, shorts, soft/trailing/advisor exits) and the phrase "lower bound".
    - The verdict is explicit: either a PASSING candidate with all four criteria shown green, or **"SHIP THE QUARANTINE ONLY"** with the negative result recorded in full. Both are acceptable outcomes; a forced number is not.
    - No DB write occurred: `AIPW_DB_READONLY=1` was set for the baseline read. `git diff --stat` lists only the doc + the script.
  </acceptance_criteria>
  <done>18-BACKTEST.md exists, is reproducible from its own contents, is honest about what the engine does not model, and reaches a verdict — including, legitimately, a negative one.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| a backtest number → a live parameter change | this doc is what licenses the rollout; an overfit or oversold number is how a bad config reaches a real account |
| the sweep driver → prod DB / Alpaca | must be structurally incapable of writing or trading |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-28 | Repudiation | **a config "wins" by not trading (100% win rate on 1 trade)** | mitigate | `trades >= 30` is checked FIRST in `passes_bar()` — enforced in the driver, not by eye |
| T-18-29 | Repudiation | **picking the candidate on the holdout (overfitting)** | mitigate | `--holdout` without `--candidate` is a parser.error; ONE holdout run; a holdout failure is RECORDED, not re-rolled |
| T-18-30 | Repudiation | **the holdout number is OVERSOLD as a promise about live P&L** | mitigate | the mandatory FIDELITY GAP section: shorts, 4H filter, loss cooldown, and the soft/trailing/advisor exits are all unmodelled — "LONG-ONLY, HARD-EXIT LOWER BOUND" |
| T-18-31 | Repudiation | **the six mc=5 cells silently dropped, hiding that the grid was 2/3 the size claimed** | mitigate | run them, record them, annotate them as structurally empty (C1) |
| T-18-32 | Repudiation | a baseline run on the OLD engine compared to a candidate on the NEW one (P3) | mitigate | both run on the FINAL engine, same session, same cached bars |
| T-18-33 | Repudiation | forcing a number through a failed sweep | mitigate | CONTEXT Decision 6's explicit escape hatch: ship the quarantine only; **a failed sweep is a FINDING** |
| T-18-34 | Tampering | the sweep driver writing to prod / placing an order | mitigate | no --apply/--write/--fix; AIPW_DB_READONLY=1 on the baseline read; grep gates |
| T-18-35 | Elevation of Privilege | a cell at kelly > 0.25 | mitigate | unrunnable — the CLI parser.error from 18-05 |
| T-18-SC | Tampering | package installs | accept | none — no new packages |
</threat_model>

<verification>
- `python scripts/sweep_backtest.py --holdout` without `--candidate` — exit 2.
- 18-BACKTEST.md: 18 TRAIN rows, 1 candidate, 2 holdout rows, the tiebreak stated above the grid, the fidelity-gap section, the baseline revert values, an explicit verdict.
- `python -m pytest tests/ -q` — >= 395 passed.
</verification>

<success_criteria>
There is a single, reproducible, honestly-scoped number — or an honestly-recorded negative result. In
either case the next plan knows exactly what to roll out and exactly how to revert it.
</success_criteria>

<output>
Create `.planning/phases/18-profitable-retune/18-06-SUMMARY.md` when done.
</output>
</content>
