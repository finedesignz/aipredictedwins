---
phase: 18-profitable-retune
plan: 07
type: execute
wave: 4
depends_on: [18-06]
files_modified:
  - dashboard/api/models.py
  - dashboard/api/seed_bots.py
  - src/bot_config.py
  - .planning/phases/18-profitable-retune/18-BACKTEST.md
autonomous: false
requirements: [TUNE-03]

must_haves:
  truths:
    - "THE ROLLOUT IS CONFIG-ONLY. All three knobs are REAL COLUMNS on `bots` (migrations 002_multi_bot.sql, 018_universe_quarantine.sql) and REAL FIELDS on BotUpdate (dashboard/api/models.py:258, 259, 276). PUT /api/bots/{bot_id} (routes/bots.py:184-212) builds its SET clause dynamically from non-None fields and then calls mgr.update() to HOT-SWAP the live BotConfig. ZERO CODE IS REQUIRED TO APPLY OR REVERT."
    - "THE QUARTER-KELLY CEILING HAS **THREE** UNGUARDED PATHS, AND ONE OF THEM DEFAULTS TO 0.50. (1) BotUpdate.kelly_fraction (dashboard/api/models.py:255-262) has no upper bound -> PUT can raise Kelly. (2) BotCreate/BotFull kelly_fraction (models.py:212, :240) have no upper bound -> `POST /api/bots` (routes/bots.py:147-153) can CREATE a bot at 0.50. (3) **dashboard/api/seed_bots.py:53 — `float(os.environ.get(\"BOT_B_KELLY\", \"0.50\"))`, written by a RAW SQL INSERT (:144-155) that BYPASSES pydantic entirely.** Prod is safe today ONLY because seed skips existing rows (:135-137) — any fresh DB, or a deleted-and-reseeded Bot B row, silently restores kelly_fraction=0.50 with no guard firing. ALL THREE are closed in this plan."
    - "Bot B's historical 0.50 must be unreachable from EVERY direction: the CLI (18-05), the PUT, the POST, and the SEED."
    - "The REVERT is the same PUT with the previous values, which 18-BACKTEST.md records verbatim. Copy-paste. No deploy, no restart, no code."
    - "PAPER MODE ONLY. Live trading stays PAPER-GATED (50+ paper trades, win rate > 40%, $100k equity). Nothing in this plan touches that gate."
    - "Quarter-Kelly is a CEILING. Kelly may only go DOWN. If Bot B is still at 0.50, it comes to <= 0.25 REGARDLESS of what the sweep concluded — that is a hardcoded risk rule, not a tuning result."
    - "Quarantine format is SLASH form, exact: 'BTC/USD,ETH/USD,TRUMP/USD,FIL/USD,ARB/USD' — the Phase-15 convention, what `symbols` uses, and what a human reading the row expects."
    - "Apply to every bot whose strategy == 'confluence' (Decision 5: 'quarantined for every confluence bot'). READ each bot's strategy BEFORE writing, and record which bots were touched in 18-BACKTEST.md."
    - "ONE ALPACA ACCOUNT PER BOT — unchanged. This plan writes a `bots` row; it does not create, move, or share an account."
  artifacts:
    - path: "dashboard/api/models.py"
      provides: "BotUpdate rejects kelly_fraction > 0.25 — the API-side mirror of the CLI ceiling"
      contains: "0.25"
  key_links:
    - from: "PUT /api/bots/{bot_id}"
      to: "BotManager.update -> the live BotConfig"
      via: "the existing hot-swap (routes/bots.py:208) — no deploy"
      pattern: "mgr.update"
---

<objective>
Roll out the Phase-18 result as a **`bots` row update**, and close the last Kelly hole.

Purpose: TUNE-03 says "reversible via config/env." It already is — every knob is a DB column, the
route hot-swaps the live `BotConfig`, and the revert is the same `PUT` with the old values. The only
thing missing is the guard: the CLI refuses `--kelly-fraction 0.50`, but the **rollout path does not**.
Bot B's historical `0.50` must be unreachable from every direction, not just the one we happened to
test.

Output: one validation guard + the applied config + the revert command, recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-profitable-retune/18-BACKTEST.md
@.planning/phases/18-profitable-retune/18-RESEARCH.md
@CLAUDE.md

<interfaces>
THE ROLLOUT CHAIN — every link verified, zero code needed to apply or revert:

| Link | Evidence |
|---|---|
| Columns exist on `bots` | migrations 002_multi_bot.sql (min_confluence, kelly_fraction), 018_universe_quarantine.sql (quarantined_symbols). NOTE: src/db_schema.sql:11-19 is only a PARTIAL mirror — the MIGRATIONS are authoritative. Do not "fix" the mirror (Phase 20). |
| BotConfig reads them | src/bot_config.py:18 (kelly_fraction=0.25), :19 (min_confluence=4), :38 (quarantined_symbols=""); from_row :49/:50/:68; the `quarantined` property :78-80 |
| The API accepts them | dashboard/api/models.py:258 (kelly_fraction), :259 (min_confluence), :276 (quarantined_symbols — `""` clears the list; `None` means leave alone) |
| The route writes + hot-swaps | dashboard/api/routes/bots.py:184-212 — `updates = {k: v for k, v in body.model_dump().items() if v is not None}` (:187), dynamic SET (:191), then `mgr.update(bot_id, updates)` (:208) |
| They bite live | src/bot_thread.py:141 (min_confluence), :146/:165/:355 (entry_allowed(..., cfg.quarantined)), :798/:996 (kelly_fraction) |

THE APPLY:
  curl -X PUT https://app.aipredictedwins.com/api/bots/A \
    -H 'Content-Type: application/json' \
    -d '{"min_confluence": <from 18-BACKTEST.md>, "kelly_fraction": <from 18-BACKTEST.md>,
         "quarantined_symbols": "BTC/USD,ETH/USD,TRUMP/USD,FIL/USD,ARB/USD"}'

THE REVERT: the same PUT with the values recorded in 18-BACKTEST.md's Baseline section.

THE THREE GAPS TO CLOSE (VALIDATION case 28 + plan-check B3) — verified at file:line this session:

  1. dashboard/api/models.py:255-262  BotUpdate.kelly_fraction: Optional[float] = None   <- no bound
  2. dashboard/api/models.py:212 (BotFull) and :240 (BotCreate)  kelly_fraction: float = 0.25  <- no bound
     -> POST /api/bots (routes/bots.py:147-153) can CREATE a bot at 0.50.
  3. dashboard/api/seed_bots.py:53
        "kelly_fraction": float(os.environ.get("BOT_B_KELLY", "0.50")),
     written by a RAW SQL INSERT (seed_bots.py:144-155) that NEVER touches pydantic.
     It only skips when the row already exists (:135-137) — so prod is safe TODAY, but a fresh DB or a
     deleted-and-reseeded Bot B row restores 0.50 and NO GUARD FIRES. **This is the path that defaults
     to a risk-rule violation.**

  Fix: `le=0.25` on all three model fields, AND in seed_bots.py change the BOT_B_KELLY default to
  "0.25" AND clamp in build_bots(): `min(float(os.environ.get("BOT_B_KELLY", "0.25")), 0.25)` — an env
  var must not be able to raise Kelly above the ceiling either.

  **PLUS the READ-SIDE clamp (plan-check W1) — this is what makes the ceiling TOTAL rather than
  perimeter-only.** The three fixes above close every WRITE path, but `src/bot_config.py:49` reads
  `kelly_fraction` from the bots row and passes it straight through with no ceiling. Bot B's LIVE row is
  very likely still at 0.50 today (seeded before any of these bounds existed), and nothing clamps it on
  read — so until the rollout PUT lands, a bot is still sizing at half-Kelly in violation of the
  hardcoded CLAUDE.md rule. `BotConfig.from_row` is the single choke point EVERY bot reads through.
  Change `:49` to `kelly_fraction=min(float(row["kelly_fraction"] if row.get("kelly_fraction") is not None else 0.25), 0.25)`.
  A pre-existing out-of-bounds row must be clamped at read, not merely rejected at write — otherwise the
  ceiling only protects rows written after this phase.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: close ALL THREE Kelly paths — BotUpdate, BotCreate/BotFull, and the SEED (which defaults to 0.50)</name>
  <files>dashboard/api/models.py, dashboard/api/seed_bots.py, src/bot_config.py</files>
  <read_first>tests/test_rollout_config.py (the RED spec — cases 26-28); dashboard/api/models.py L205-245 (BotFull :212 and BotCreate :240 — kelly_fraction: float = 0.25, NO upper bound) and L250-280 (BotUpdate: kelly_fraction :258, min_confluence :259, quarantined_symbols :276 with its `""` clears / `None` leaves-alone comment); dashboard/api/seed_bots.py L45-60 (**:53 — BOT_B_KELLY defaults to "0.50"**) and L130-158 (the RAW SQL INSERT that bypasses pydantic; the `if count == 0` skip at :135-137 that is the only reason prod is safe today); dashboard/api/routes/bots.py L147-153 (POST /api/bots -> BotCreate) and L184-212 (the dynamic SET from non-None fields + the mgr.update hot-swap — do NOT change this route); src/bot_config.py L18-19, L34, L78-80</read_first>
  <behavior>
    - case 28: PUT {"kelly_fraction": 0.50} is REJECTED (422). The CLI guard is worthless if the rollout
      path can still raise Kelly.
    - case 28b: POST /api/bots {"kelly_fraction": 0.50} is REJECTED (422) — BotCreate/BotFull bounded.
    - case 28c: seed_bots.build_bots() with BOT_B_KELLY unset yields Bot B at 0.25 (NOT 0.50); with
      BOT_B_KELLY="0.50" set, it is CLAMPED to 0.25. The seed writes by raw SQL and never sees pydantic,
      so the clamp must live in build_bots() itself.
    - case 27: PUT {"min_confluence": 4, "kelly_fraction": 0.20, "quarantined_symbols": "BTC/USD,ETH/USD"}
      -> 200; the bots row is updated; BotManager.update is called with those keys.
    - case 26: BotConfig.from_row with the quarantine string -> cfg.quarantined is the parsed list, and
      select_long_candidates DROPS a confluence=4 BTC signal while KEEPING an identical SOL one.
      ZERO src/ diff is required for that behavior.
  </behavior>
  <action>Close all three paths. (1) Constrain `kelly_fraction` on BotUpdate (models.py:258): `Field(default=None, gt=0, le=0.25, description="Quarter-Kelly is a hardcoded CEILING (CLAUDE.md risk rules); it may only go DOWN.")` — a 0.50 payload is a 422 before it reaches the SET clause. (2) **MANDATORY, not conditional: the same `le=0.25` bound on BotFull.kelly_fraction (models.py:212) and BotCreate.kelly_fraction (models.py:240)** — they exist, they are unbounded, and `POST /api/bots` (routes/bots.py:147-153) can create a bot at 0.50 through them. (3) **dashboard/api/seed_bots.py:53 — change the BOT_B_KELLY default from "0.50" to "0.25" AND clamp: `min(float(os.environ.get("BOT_B_KELLY", "0.25")), 0.25)`.** The seed writes via a RAW SQL INSERT (:144-155) that never touches pydantic, so the model bounds do NOT protect it; it is safe today only because it skips existing rows (:135-137), which means a fresh DB or a reseeded Bot B row silently restores 0.50. The clamp must be in build_bots() so an env var cannot raise Kelly either. Do NOT change dashboard/api/routes/bots.py — the route's dynamic SET + mgr.update hot-swap is exactly the mechanism TUNE-03 depends on, and it needs no change. Do NOT add a min_confluence upper bound (5 is legal to REQUEST even though it is structurally unreachable — that is a strategy fact, not a validation error, and clamping it would hide C1 rather than document it). Do NOT touch src/db_schema.sql's partial `bots` mirror (Phase 20). Smallest possible diff: one Field constraint.</action>
  <verify>
    <automated>python -m pytest tests/test_rollout_config.py -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_rollout_config.py -q` — case 28 PASSES (26 already passed; 27 passes with TEST_DATABASE_URL, skips visibly without).
    - `python -c "from dashboard.api.models import BotUpdate; import pydantic; \
      BotUpdate(kelly_fraction=0.25); \
      exec('try:\n BotUpdate(kelly_fraction=0.50)\n raise SystemExit(\"NOT REJECTED\")\nexcept pydantic.ValidationError:\n print(\"0.50 REJECTED, 0.25 OK\")')"` — prints `0.50 REJECTED, 0.25 OK`.
    - `python -m pytest tests/test_rollout_config.py -q` — cases 28b and 28c PASS.
    - **`grep -n "0.50" dashboard/api/seed_bots.py` returns NOTHING for kelly** — BOT_B_KELLY defaults to "0.25" and is clamped by `min(..., 0.25)`.
    - `python -c "import os; os.environ['BOT_B_KELLY']='0.50'; from dashboard.api.seed_bots import build_bots; b=[x for x in build_bots() if x['bot_id']=='B'][0]; assert b['kelly_fraction'] <= 0.25, b; print('SEED CLAMPED')"` — prints `SEED CLAMPED`.
    - `POST /api/bots` with `{"kelly_fraction": 0.50}` -> **422** (BotCreate bounded).
    - `git diff dashboard/api/routes/bots.py` is **EMPTY** — the rollout route needs no change; that is TUNE-03's whole claim.
    - `git diff --stat` lists **dashboard/api/models.py and dashboard/api/seed_bots.py ONLY**.
    - `python -m pytest tests/ dashboard/api/tests/ -q | tail -1` — >= 395 passed.
  </acceptance_criteria>
  <done>Kelly above the quarter-Kelly ceiling is unreachable from the CLI (18-05) AND from the rollout API. Bot B's historical 0.50 cannot come back.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The Phase-18 result, ready to apply as a `bots` row update. Zero code ships with it.

    From 18-BACKTEST.md:
    - The BASELINE values for bots A/B/C/D (the revert target), read with AIPW_DB_READONLY=1.
    - The VERDICT: either a candidate (min_confluence, kelly_fraction) that cleared all four holdout
      criteria, or "SHIP THE QUARANTINE ONLY" with the negative result recorded.
    - The QUARANTINE: BTC/USD, ETH/USD, TRUMP/USD, FIL/USD, ARB/USD (slash form, exact) — applied to
      every bot whose `strategy == "confluence"`.
    - Bot B's kelly_fraction comes to <= 0.25 regardless of the verdict (hardcoded ceiling).

    This is a WRITE TO PROD. It is the one deliberate prod write in Phase 18 (CONTEXT Decision 7).
    It is reversible by copy-pasting the Baseline section back through the same PUT.
  </what-built>
  <how-to-verify>
    1. Read `.planning/phases/18-profitable-retune/18-BACKTEST.md`. Confirm the verdict, the candidate
       (or the "quarantine only" outcome), and the Baseline revert values.
    2. Confirm the PUT payloads shown below match that verdict, per bot, and that NO bot receives a
       kelly_fraction above 0.25.
    3. Confirm the bots being touched are the ones whose `strategy == "confluence"` (read from the DB,
       not assumed).
    4. Approve, and the PUTs are executed against https://app.aipredictedwins.com/api/bots/{A,B,C,D}.
    5. After the PUT: `GET /api/bots` shows the new values; the dashboard headline win rate (now honest,
       per 18-04) and the paper-gate readout (settings.py win_rate vs win_rate_target=40.0) still render.
    6. PAPER MODE ONLY. The live-trading gate (50+ paper trades, win rate > 40%, $100k equity) is untouched.
  </how-to-verify>
  <resume-signal>Type "approved" to apply the PUTs, or name the values to change.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Apply the config via PUT. Record what was touched and the exact revert.</name>
  <files>.planning/phases/18-profitable-retune/18-BACKTEST.md</files>
  <action>After approval: for each bot whose `strategy == "confluence"` (READ it first — do not assume; Decision 5 and RESEARCH Open Question 2), issue `PUT /api/bots/{bot_id}` with ONLY the fields that change (the route ignores None, so send exactly what the verdict calls for): `min_confluence` and `kelly_fraction` from the 18-BACKTEST.md verdict (omit both if the verdict was "quarantine only"), and `quarantined_symbols` = "BTC/USD,ETH/USD,TRUMP/USD,FIL/USD,ARB/USD" (slash form, exact). If Bot B's kelly_fraction is still 0.50, it goes to <= 0.25 in the same PUT regardless of the verdict — the ceiling is a hardcoded risk rule. Then verify with `GET /api/bots` that every row reflects the new values and that the BotManager hot-swap took (no restart, no deploy — routes/bots.py:208 calls mgr.update()). Append to 18-BACKTEST.md a "Rollout" section: which bots were touched, the exact PUT payload sent to each, the timestamp, and the **verbatim copy-pasteable REVERT curl per bot** built from the Baseline section. Do NOT run a migration. Do NOT restart or redeploy the bot services. Do NOT touch the 395 historical sentinel rows. Do NOT enable live trading — PAPER MODE ONLY, and the 50-paper-trade gate stands.</action>
  <verify>
    <automated>curl -s https://app.aipredictedwins.com/api/bots | python -c "import sys,json; bots=json.load(sys.stdin); [print(b['id'], b.get('strategy'), b.get('min_confluence'), b.get('kelly_fraction'), repr(b.get('quarantined_symbols'))) for b in (bots if isinstance(bots,list) else bots.get('bots',[]))]; assert all((b.get('kelly_fraction') or 0) <= 0.25 for b in (bots if isinstance(bots,list) else bots.get('bots',[]))), 'A BOT IS ABOVE THE QUARTER-KELLY CEILING'; print('CEILING HOLDS')"</automated>
  </verify>
  <acceptance_criteria>
    - `GET /api/bots` reflects the new `min_confluence` / `kelly_fraction` / `quarantined_symbols` for every touched bot.
    - The verify command prints `CEILING HOLDS` — **no bot has kelly_fraction > 0.25.** (Bot B is at <= 0.25.)
    - `quarantined_symbols` is the exact slash-form string `BTC/USD,ETH/USD,TRUMP/USD,FIL/USD,ARB/USD` on every confluence bot.
    - 18-BACKTEST.md has a **Rollout** section with the exact PUT payload per bot AND a copy-pasteable **revert curl** per bot.
    - **`git diff --stat` for src/ is EMPTY** — the rollout shipped as DATA, not code. That is TUNE-03's claim, proven.
    - No deploy, no restart, no migration was run.
    - The bots remain in PAPER mode; the live gate is untouched.
  </acceptance_criteria>
  <done>The retune (or the quarantine alone) is live in paper, applied as a row update, and revertible by copy-pasting one curl per bot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| the rollout API → live bot behavior | PUT /api/bots hot-swaps the in-memory BotConfig of a bot trading a real (paper) Alpaca account |
| operator payload → risk parameters | a hand-typed kelly_fraction reaches the sizer with no gate today |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-36 | **Elevation of Privilege** | **the rollout PUT can raise Kelly above the hardcoded ceiling — the CLI guard (18-05) does not cover the API** | mitigate | `le=0.25` on BotUpdate.kelly_fraction -> 422. Case 28 |
| T-18-36b | **Elevation of Privilege** | **`POST /api/bots` can CREATE a bot at kelly 0.50 — BotCreate/BotFull (models.py:212, :240) are unbounded** | mitigate | `le=0.25` on both. Case 28b |
| T-18-36c | **Elevation of Privilege** | **seed_bots.py:53 DEFAULTS Bot B to kelly 0.50 and writes it by RAW SQL (:144-155), bypassing pydantic entirely. Safe today only because it skips existing rows (:135-137) — a fresh DB or a reseeded row silently restores a risk-rule violation with no guard firing.** | mitigate | default -> "0.25" AND `min(..., 0.25)` clamp inside build_bots(), so neither the default nor an env var can raise Kelly. Case 28c |
| T-18-37 | Tampering | a wrong quarantine string (bare `BTC` instead of `BTC/USD`) silently fails to deny | mitigate | slash form, exact — the Phase-15 documented convention; `GET /api/bots` verifies the stored string byte-for-byte |
| T-18-38 | Repudiation | the retune cannot be reverted | mitigate | the Baseline values are recorded VERBATIM in 18-BACKTEST.md before the write; the revert is the same PUT. `git diff src/` is EMPTY — nothing to un-deploy |
| T-18-39 | Tampering | a bot's Alpaca account changed / shared | mitigate | out of scope — this plan writes strategy knobs on a `bots` row and touches no credential. ONE ACCOUNT PER BOT stands |
| T-18-40 | Elevation of Privilege | live trading enabled off a paper-validated retune | mitigate | PAPER MODE ONLY; the 50-paper-trade / 40% / $100k gate is untouched |
| T-18-SC | Tampering | package installs | accept | none — no new packages |
</threat_model>

<verification>
- `python -m pytest tests/test_rollout_config.py -q` — cases 26-28 green.
- `GET /api/bots` — new values live; no bot above kelly_fraction 0.25.
- `git diff --stat src/` — EMPTY (the rollout is data, not code).
- 18-BACKTEST.md carries a copy-pasteable revert curl per bot.
</verification>

<success_criteria>
The Phase-18 result is live in paper as a `bots` row update. Reverting it is one curl per bot, copied
out of the doc. No code shipped with it, and no path — CLI or API — can put Kelly above 0.25.
</success_criteria>

<output>
Create `.planning/phases/18-profitable-retune/18-07-SUMMARY.md` when done.
</output>
</content>
