---
phase: 18-profitable-retune
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/fetch_backtest_bars.py
  - data/backtest_bars/
autonomous: true
requirements: [TUNE-01]

must_haves:
  truths:
    - "⛔ WAVE-0 BLOCKER. Real cached Alpaca bars for 8 symbols x 2025-10-01 -> 2026-04-30 exist on disk BEFORE the first sweep cell runs."
    - "tests/backtester/fixtures/ has exactly ONE file (BTC_USD.json, 60 bars) and BTC is the headline quarantine target. A fixture-only sweep with --exclude-symbols BTC/USD has ZERO tradeable symbols, so every cell returns trades=0, drawdown=0.0, and the quarantine arm 'wins' VACUOUSLY. This plan is what stops the sweep producing a confidently wrong answer."
    - "The fetch script is READ-ONLY against Alpaca market data. It NEVER imports src.db, NEVER reads DATABASE_URL, NEVER places an order, and has NO --apply/--write/--fix surface."
    - "Bars are fetched ONCE and cached (src/backtester/data_loader.py:75 save_bars_cache). All 18 cells + baseline + holdout run against the SAME cache — an Alpaca re-fetch is not guaranteed byte-identical, and the cache is the ONLY reproducibility control (the engine has no RNG)."
    - "Per-symbol bar counts are REPORTED before cell #1 (assumption A4). A symbol short of bars yields thin cells and must be visible, not silent."
    - "SIGNAL_WINDOW=50 + SCAN_INTERVAL_BARS=30 -> a symbol needs >= 50 + 30 bars to afford even ONE scan. Any symbol below that threshold is FLAGGED."
  artifacts:
    - path: "scripts/fetch_backtest_bars.py"
      provides: "one-shot, read-only bar fetch + cache + a per-symbol coverage report"
      min_lines: 60
    - path: "data/backtest_bars/"
      provides: "the cached 1H bars the entire sweep replays"
  key_links:
    - from: "scripts/fetch_backtest_bars.py"
      to: "src.backtester.data_loader.load_bars_cached / save_bars_cache"
      via: "the EXISTING cache path — do not invent a second bar store"
      pattern: "from src.backtester.data_loader import"
---

<objective>
Fetch and cache real 1-hour Alpaca bars for the 8-symbol universe across the full TRAIN + HOLDOUT
range (2025-10-01 -> 2026-04-30), and report per-symbol coverage.

Purpose: **this is the Wave-0 blocker.** `tests/backtester/fixtures/` contains exactly one file —
`BTC_USD.json`, 60 bars — and that symbol is BTC, the headline quarantine target. Run the sweep on
fixtures and the `--exclude-symbols BTC/USD` arm has **zero tradeable symbols**: every cell returns
`trades=0`, `max_drawdown=0.0`, and the quarantine "wins" because it never traded. That is a
confidently wrong answer, arrived at rigorously. No sweep cell may run until this plan lands.

Output: `scripts/fetch_backtest_bars.py` + a populated bar cache + a coverage table for `18-BACKTEST.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-profitable-retune/18-RESEARCH.md
@CLAUDE.md

<interfaces>
Existing, reuse — do NOT write a second bar loader:
  src/backtester/data_loader.py:41   load_bars_fixture(symbol, fixture_dir)   # "BTC/USD" -> BTC_USD.json
  src/backtester/data_loader.py:54   load_bars_cached(symbol, start, end, cache_dir=...)
  src/backtester/data_loader.py:75   save_bars_cache(symbol, bars, cache_dir=...)

Windows (src/backtester/cli.py:22-25 — already exactly the CONTEXT windows):
  TRAIN_START   = 2025-10-01     TRAIN_END   = 2026-01-31
  HOLDOUT_START = 2026-02-01     HOLDOUT_END = 2026-04-30
Fetch the UNION: 2025-10-01 -> 2026-04-30, once.

Universe (src/backtester/cli.py:27-30): BTC/USD, ETH/USD, SOL/USD, XRP/USD, ADA/USD, AVAX/USD,
DOT/USD, LINK/USD.

Scan arithmetic (src/backtester/engine.py:22-23): SIGNAL_WINDOW = 50, SCAN_INTERVAL_BARS = 30.
A symbol with N bars affords roughly (N - 50) / 30 scan opportunities. N < 80 -> at most one. FLAG it.

Alpaca credentials come from .env (market-data read is free and does not touch an account's positions).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/fetch_backtest_bars.py — read-only fetch + cache + coverage report</name>
  <files>scripts/fetch_backtest_bars.py</files>
  <read_first>src/backtester/data_loader.py L1-90 (load_bars_cached / save_bars_cache — the cache-dir convention, the on-disk JSON shape, the symbol->filename mapping); src/backtester/cli.py L20-46 and L70-80 (SYMBOLS, the date constants, the bar-load loop the sweep will use); src/alpaca_client.py L1-60 (how the client is constructed from env — market data only); scripts/symbol_report.py L1-40 (the Phase-17 read-only script conventions to mirror: argparse shape, no writes, AIPW_DB_READONLY)</read_first>
  <action>Create scripts/fetch_backtest_bars.py. argparse: --start (default 2025-10-01), --end (default 2026-04-30), --symbols (comma-separated, default the 8-crypto universe from src.backtester.cli.SYMBOLS), --cache-dir (default data/backtest_bars), --timeframe 1H. For each symbol: call the EXISTING src.backtester.data_loader.load_bars_cached (which fetches on miss and calls save_bars_cache) — do NOT write a parallel fetch/serialize path. Print a coverage table to stdout: symbol | bars | first_ts | last_ts | scans_afforded = max(0, (bars - 50) // 30) | FLAG when bars < 80. Exit non-zero if ANY symbol returns zero bars (a silently-empty symbol is how the sweep goes vacuous). The script has NO --apply / --write / --fix flag, does NOT import src.db, does NOT read DATABASE_URL, and places no orders — it is market-data read-only (VALIDATION case 29's fence scans it). Set AIPW_DB_READONLY=1 defensively at the top anyway, so that if any transitive import ever reaches src.db it cannot bootstrap DDL.</action>
  <verify>
    <automated>python -c "import ast,pathlib; src=pathlib.Path('scripts/fetch_backtest_bars.py').read_text(); assert 'src.db' not in src and 'DATABASE_URL' not in src.replace('AIPW_DB_READONLY',''); assert '--apply' not in src and '--write' not in src and '--fix' not in src; print('FENCE OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import ast; ast.parse(open('scripts/fetch_backtest_bars.py').read())"` — parses.
    - The fence check above prints `FENCE OK`.
    - `grep -n "from src.backtester.data_loader import" scripts/fetch_backtest_bars.py` returns a hit (the existing cache path is reused, not re-implemented).
    - `grep -nE "submit_order|place_order|create_order|INSERT|UPDATE |DELETE " scripts/fetch_backtest_bars.py` returns NOTHING.
  </acceptance_criteria>
  <done>A one-shot, read-only, no-DB, no-order bar fetcher that reuses the existing cache path and reports coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Run the fetch. Populate the cache. Report coverage. FAIL LOUDLY on a thin symbol.</name>
  <files>data/backtest_bars/</files>
  <action>Run: `python scripts/fetch_backtest_bars.py --start 2025-10-01 --end 2026-04-30`. Capture the full coverage table verbatim — it goes into 18-BACKTEST.md's reproducibility section (assumption A4). Verify all 8 symbols returned bars and that none is FLAGged (bars < 80). If a symbol IS thin or empty: do NOT silently drop it and do NOT substitute the fixture. Record it explicitly, and STOP to report — a universe short a symbol changes what the sweep is measuring, and the quarantine arm is exactly the thing a missing symbol corrupts. Record the cache directory path and the per-symbol bar counts + first/last timestamps. Do NOT commit the bar JSONs if they are large (>5MB total): instead gitignore data/backtest_bars/ and commit ONLY the coverage table into 18-BACKTEST.md, with the exact fetch command so it is reproducible. The cache must remain UNTOUCHED for the whole sweep — every cell, the baseline, and the holdout replay the SAME bytes (RESEARCH P3: never compare a baseline on one bar set against a candidate on another).</action>
  <verify>
    <automated>python -c "import pathlib,json; d=pathlib.Path('data/backtest_bars'); fs=sorted(d.glob('*.json')); assert len(fs) >= 8, f'only {len(fs)} symbols cached'; [print(f.stem, len(json.loads(f.read_text()))) for f in fs]"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command lists >= 8 cached symbol files with their bar counts.
    - EVERY symbol has >= 80 bars (else the run STOPS and reports — a symbol below 80 affords at most one scan).
    - BTC/USD AND at least 5 non-BTC symbols are present — the quarantine arm must have somewhere to trade. (`--exclude-symbols BTC/USD,ETH/USD,TRUMP/USD,FIL/USD,ARB/USD` must leave >= 5 tradeable symbols: SOL, XRP, ADA, AVAX, DOT, LINK.)
    - `python -m src.backtester --phase 0 --train --min-confluence 3 --fixture-dir data/backtest_bars 2>&1 | tail -3` — SMOKE ONLY, not a sweep cell: it produces a non-zero trade count. If it produces zero trades on the full universe at the LOOSEST threshold, the cache is wrong and the sweep would be vacuous.
    - The coverage table (symbol | bars | first_ts | last_ts) is captured verbatim for 18-BACKTEST.md.
  </acceptance_criteria>
  <done>Real bars for all 8 symbols across both windows are cached on disk, coverage is reported, and a smoke run proves the sweep will not be vacuous. The Wave-0 blocker is cleared.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| script → Alpaca | market-data read only; the script must be structurally incapable of placing an order |
| script → prod DB | the script must never import src.db or read DATABASE_URL |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05 | Repudiation | **a fixture-only sweep: BTC is the ONLY fixture AND the headline quarantine target, so the quarantine arm wins with zero trades** | mitigate | real cached bars for all 8 symbols; Task 2 asserts >= 5 non-quarantined symbols remain tradeable; the >= 30-trade acceptance criterion is the downstream tripwire |
| T-18-06 | Repudiation | bars re-fetched per cell -> cells not comparable | mitigate | fetch ONCE, cache, replay the same bytes for every cell + baseline + holdout (RESEARCH P3) |
| T-18-07 | Tampering | an analysis script bootstrapping DDL against prod | mitigate | no src.db import; AIPW_DB_READONLY=1 set defensively; fence grep in Task 1's verify |
| T-18-08 | Elevation of Privilege | a "fetch" script that can trade | mitigate | grep gate: no submit_order/place_order/create_order |
| T-18-SC | Tampering | package installs | accept | none — alpaca-py 0.43.2 already present |
</threat_model>

<verification>
- 8 cached symbol JSONs on disk, each >= 80 bars.
- The smoke run at `--min-confluence 3` over the cache yields a non-zero trade count.
- The fence check prints `FENCE OK`.
</verification>

<success_criteria>
The sweep can no longer produce a vacuous answer. With BTC quarantined there are still 5+ symbols
with real bars to trade, so a "quarantine wins" result would mean something.
</success_criteria>

<output>
Create `.planning/phases/18-profitable-retune/18-02-SUMMARY.md` when done.
</output>
</content>
