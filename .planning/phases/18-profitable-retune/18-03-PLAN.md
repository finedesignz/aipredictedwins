---
phase: 18-profitable-retune
plan: 03
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - src/alpaca_orchestrator.py
autonomous: true
requirements: [TUNE-01]

must_haves:
  truths:
    - "The sentinel writer at src/alpaca_orchestrator.py:167-176 NO LONGER fabricates. `pnl=0.0` and `exit_price=trade.get('entry_price')` are GONE from the monitor reconcile block."
    - "A vanished position is resolved to its REAL, FEE-NET exit from Alpaca order history — via the EXISTING Phase-11/14 resolver (backfill.resolve_stale_row:51, backfill._match_close:92, pnl.realized_pnl:10, order_resolution.classify_order:14, alpaca_client.get_closed_orders:419). ZERO new resolver logic, ZERO new P&L formula."
    - "The ONE new behavior: the `unresolvable` branch writes status='closed', exit_price=NULL, pnl=NULL. backfill leaves such a row `open` (a batch script can retry); the monitor CANNOT (it would re-alert forever)."
    - "TRAP A (mass-close, VALIDATION case 4) — the monitor builds live_symbols SLASH-STRIPPED ('BTCUSD', :159) while resolve_stale_row compares row['symbol'] ('BTC/USD') against it (backfill.py:71). Normalize BOTH sides through src.universe.normalize. Get this wrong and the fix CLOSES EVERY HELD POSITION on its first cycle."
    - "TRAP B (transient error, VALIDATION case 5) — `unresolvable` means 'Alpaca ANSWERED and there is no matching close'. It NEVER means 'Alpaca did not answer'. Every Alpaca call is try/except'd; a failure returns {} (leave the row open, retry next cycle) — never a NULL write, never a fabricated close."
    - "A terminal 0-fill entry (canceled/rejected/expired) still writes pnl=0 — that zero is TRUE (no position ever existed), not fabricated."
    - "NO backfill, NO UPDATE, NO DELETE of the 395 historical sentinel rows. They stop being CREATED and stop being COUNTED. Nothing else."
  artifacts:
    - path: "src/alpaca_orchestrator.py"
      provides: "_resolve_external_exit — a pure, testable orchestration function; the monitor becomes a thin loop"
      contains: "_resolve_external_exit"
  key_links:
    - from: "src/alpaca_orchestrator.py::_resolve_external_exit"
      to: "src.backfill.resolve_stale_row"
      via: "the EXISTING resolver — imported, never re-implemented"
      pattern: "from src.backfill import"
    - from: "src/alpaca_orchestrator.py::_resolve_external_exit"
      to: "src.universe.normalize"
      via: "the slash-mismatch fix — both sides of the live_symbols comparison"
      pattern: "normalize"
---

<objective>
Replace the sentinel writer. A position that vanishes from Alpaca gets its REAL exit resolved from
order history — or `pnl=NULL`. It never again gets a fabricated `0.0`.

Purpose: 395 of 655 position-closed rows (60%) carry `status='closed', exit_price=entry_price,
pnl=0.0` — a lie the monitor writes for every externally-exited position. The win rate is
**unmeasurable** while that is true, and TUNE-01's own acceptance bar (win_rate >= 40%) is therefore
unreadable. This is the first thing that must land.

**Phase 18 needs almost no new logic here.** Phases 11-14 already built and tested every pure piece.
This plan is *wiring*. Any diff that introduces a new resolver, a new P&L formula, or a new symbol
gate has gone wrong.

Output: `src/alpaca_orchestrator.py::_resolve_external_exit` — turns VALIDATION cases 1-8 GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-profitable-retune/18-RESEARCH.md
@.planning/phases/18-profitable-retune/18-VALIDATION.md
@src/universe.py
@CLAUDE.md

<interfaces>
The RED spec: tests/test_external_exit_resolution.py (Plan 18-01), cases 1-8.

  def _resolve_external_exit(alpaca, row: dict, live_symbols: set[str] | None) -> dict:
      """Resolve a vanished position to a REAL exit, or to NULL. NEVER fabricates.
      Returns update_alpaca_trade kwargs, or {} meaning DO NOT WRITE."""

REUSE (all pure, all Phase-11/14, all already tested by tests/test_backfill.py ~20 cases):
  src/order_resolution.py:14  classify_order(order) -> (db_status, pnl)
                              filled/filled_qty>0 -> ("open", None); terminal 0-fill -> (status, 0); in-flight -> (None, None)
  src/backfill.py:92          _match_close(closes, entry_order, row) -> order|None
                              opposite side, filled_qty>0, filled_at strictly after entry, EARLIEST, qty within _QTY_TOLERANCE
  src/pnl.py:10               realized_pnl(side, entry_fill, exit_fill, qty, taker_fee) -> float  (fee-net BOTH legs)
  src/backfill.py:51          resolve_stale_row(row, entry_order, live_symbols, close_order)
                              -> (outcome, kwargs|None), outcome in {"resolved","unchanged","unresolvable"}
  src/alpaca_client.py:401    get_order(order_id)
  src/alpaca_client.py:419    get_closed_orders(symbol, after=None)   # Phase-14, built for EXACTLY this
  src/universe.py:17          normalize(symbol) -> str                # the slash canonicalizer

WRITE PATH — no change needed:
  src/db.py:101-122  update_alpaca_trade(trade_id, status, exit_price: float|None = None,
                                         pnl: float|None = None, fees: float|None = None)
  Already accepts exit_price=None / pnl=None (:105-106), writes them straight through (:118), and
  stamps closed_at because 'closed' is in the terminal set (:111). NULL is an ALREADY-UNDERSTOOD
  signal: Phases 11-14 established `pnl IS NULL` == "unresolved", and Phase 17 confirmed
  null_pnl_total == 0 today, so the channel is clean.

THE BLOCK BEING REPLACED (src/alpaca_orchestrator.py:165-176):
  live_symbols is built SLASH-STRIPPED at :159 ("BTCUSD"). resolve_stale_row compares
  row["symbol"] ("BTC/USD") against it at backfill.py:71. THAT IS THE MASS-CLOSE TRAP.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _resolve_external_exit — wire the existing resolver; NULL, never 0.0</name>
  <files>src/alpaca_orchestrator.py</files>
  <read_first>tests/test_external_exit_resolution.py (the RED spec — cases 1-8); src/alpaca_orchestrator.py L145-185 (PositionMonitor._check_all_positions — the loop, how live_symbols is built at :159, and the fabrication at :165-176); src/backfill.py L40-130 (resolve_stale_row's FOUR branches, _match_close, _QTY_TOLERANCE — read every branch, they map exactly onto what the monitor needs); src/pnl.py L1-38 (realized_pnl + TAKER_FEE); src/order_resolution.py L1-30; src/alpaca_client.py L395-465 (get_order, get_closed_orders signature + the `after` param, _parse_order's field names); src/db.py L101-130 (update_alpaca_trade already accepts None; get_open_alpaca_positions is SELECT * so order_id/filled_qty/filled_avg_price are already on the row); src/universe.py L17-23 (normalize)</read_first>
  <behavior>
    - case 2: unresolvable -> exactly {"status":"closed","exit_price":None,"pnl":None,"fees":None}; `pnl is None`.
    - case 3: resolvable -> the REAL fee-net pnl from realized_pnl (!= 0.0, != gross).
    - case 4: a HELD position ("BTC/USD" vs live_symbols={"BTCUSD"}) -> {} — NO WRITE. The mass-close trap.
    - case 5: get_order/get_closed_orders raises -> {} — NO WRITE. The transient-error trap.
    - case 6: terminal 0-fill entry -> pnl == 0 (an HONEST zero; do not NULL it).
    - case 7: partial/ambiguous close -> pnl is None (the resolver refuses to guess).
    - case 8: row without order_id -> status "closed", pnl is None.
  </behavior>
  <action>Add a module-level `_resolve_external_exit(alpaca, row, live_symbols) -> dict` to src/alpaca_orchestrator.py, mirroring backfill's pure/loop split so it is unit-testable with fake doubles. Body: (1) if `row.get("order_id")` is falsy -> return the NULL-terminal dict immediately (case 8). (2) Wrap EVERY Alpaca call in try/except Exception -> log a warning and `return {}` (case 5 — a transient failure leaves the row OPEN for the next cycle; it must NEVER fall through into the unresolvable branch). (3) `entry_order = alpaca.get_order(row["order_id"])`; if None -> NULL-terminal dict. (4) `closes = alpaca.get_closed_orders(row["symbol"], after=entry_order.get("filled_at"))` — pass the SLASH form; get_closed_orders preserves it (alpaca_client.py:419). (5) `close_order = _match_close(closes, entry_order, row)`. (6) Build a NORMALIZED live-symbol set and pass it to resolve_stale_row: `norm_live = {normalize(s) for s in (live_symbols or ())}` and hand resolve_stale_row a row/live_symbols pair that compare on the SAME canonical form — the cleanest shape is to pass `norm_live` together with a row whose symbol is normalized for the comparison only (do NOT mutate the caller's row dict; do NOT change resolve_stale_row's contract, it is shared with the Phase-14 backfill script). Whichever shape you pick, VALIDATION case 4 is the arbiter: "BTC/USD" against {"BTCUSD"} MUST resolve "unchanged". (7) `outcome, kw = resolve_stale_row(...)`: "resolved" -> return kw; "unchanged" -> return {}; "unresolvable" -> return {"status":"closed","exit_price":None,"pnl":None,"fees":None}. Then REPLACE the :165-176 block in _check_all_positions with a thin loop: for each open trade, `kw = _resolve_external_exit(self.alpaca, trade, live_symbols)`; `if kw: self.logger.update_alpaca_trade(trade_id=trade["id"], **kw)`; log which outcome was taken. DELETE the `pnl=0.0` and `exit_price=trade.get("entry_price", 0)` literals — case 1 greps for their absence. Import resolve_stale_row / _match_close / normalize at module top (not inside the function). Do NOT write a new P&L formula, a new order-status chain, a new close-matcher, or a new symbol gate — every one of those already exists. Do NOT touch the 395 historical rows: no backfill, no UPDATE, no DELETE. Do NOT change resolve_stale_row, _match_close, realized_pnl, or classify_order — they are shared with the Phase-14 backfill script and its ~20 tests.</action>
  <verify>
    <automated>python -m pytest tests/test_external_exit_resolution.py tests/test_backfill.py -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_external_exit_resolution.py -q` — **8 passed** (cases 1-8 GREEN).
    - `python -m pytest tests/test_backfill.py -q` — still fully green (the shared resolver was NOT modified).
    - `git diff --stat` lists **src/alpaca_orchestrator.py ONLY**.
    - `git diff src/backfill.py src/pnl.py src/order_resolution.py src/alpaca_client.py` is **EMPTY** — the existing resolver was reused, not rewritten.
    - `grep -n "pnl=0.0" src/alpaca_orchestrator.py` returns NOTHING in the monitor reconcile block (case 1's static gate).
    - `grep -n "exit_price=trade.get(\"entry_price\"" src/alpaca_orchestrator.py` returns NOTHING.
    - `grep -c "try:" src/alpaca_orchestrator.py` increased — every Alpaca call in the new function is wrapped (case 5).
    - `grep -n "from src.universe import" src/alpaca_orchestrator.py` returns a hit — normalize is imported, not hand-rolled with `.replace("/","")`.
    - `grep -rnE "DELETE FROM alpaca_trades|UPDATE alpaca_trades SET pnl" src/ scripts/` returns NOTHING new (the 395 historical sentinels are untouched).
    - `python -m pytest tests/ -q | tail -1` — >= 395 passed, no regression.
  </acceptance_criteria>
  <done>A vanished position resolves to its real fee-net exit or to NULL. A held position is left alone. A transient Alpaca error writes nothing. The fabrication literals are gone from the source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Alpaca API → the monitor | may 500, may return partial/ambiguous order history, may lack the closing order entirely — all untrusted |
| the monitor → the trade DB | this write path terminates trade rows; a wrong write is a corrupted P&L record AND (via the alerting loop) a lost live position |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-09 | Repudiation | **a fabricated `pnl=0.0` / `exit_price=entry_price` on an unknown exit** | mitigate | resolve from order history via realized_pnl; else write NULL. NULL is the honest value — case 2 asserts `pnl is None` by identity |
| T-18-10 | **Denial of Service (LIVE MONEY)** | **the slash mismatch: live_symbols is "BTCUSD", row["symbol"] is "BTC/USD" -> EVERY held position looks vanished -> the monitor MASS-CLOSES the book on its first cycle** | mitigate | normalize BOTH sides through src.universe.normalize; **VALIDATION case 4 is a dedicated blocking test** |
| T-18-11 | **Denial of Service (LIVE MONEY)** | **a transient Alpaca 500 falls into the `unresolvable` branch and terminates a live, HELD position as closed/NULL** | mitigate | try/except every Alpaca call -> return {} (no write, retry next cycle). `unresolvable` == "Alpaca answered and there is no matching close" — case 5 |
| T-18-12 | Repudiation | an over-eager fix NULLs the HONEST zero of a terminal 0-fill entry | mitigate | classify_order's terminal-0-fill branch keeps `pnl=0` — case 6 |
| T-18-13 | Tampering | "helpfully" backfilling/UPDATE-ing the 395 historical sentinels | mitigate | explicitly forbidden by CONTEXT Decision 2; grep gate in acceptance criteria |
| T-18-14 | Repudiation | a SECOND resolver / a second P&L formula becomes a second source of truth | mitigate | `git diff` on src/backfill.py, src/pnl.py, src/order_resolution.py MUST be empty |
| T-18-SC | Tampering | package installs | accept | none — no new packages |
</threat_model>

<verification>
- `python -m pytest tests/test_external_exit_resolution.py -q` — 8 passed.
- `python -m pytest tests/test_backfill.py tests/ -q` — >= 395 passed; the shared resolver's own suite is untouched and green.
- `git diff --stat` — one file.
</verification>

<success_criteria>
The monitor stops lying. Every new position-closed row carries either a real fee-net P&L or an honest
NULL — and no held position is ever closed by the monitor because of a slash or a 500.
</success_criteria>

<output>
Create `.planning/phases/18-profitable-retune/18-03-SUMMARY.md` when done.
</output>
</content>
